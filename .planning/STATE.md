---
gsd_state_version: 1.0
milestone: v1.0
milestone_name: milestone
status: executing
stopped_at: "Plan 03.1-12 EXECUTED 2026-05-04 — C1' eliminate FOG-17a integer-wrap-modulo (FOG-18) + C3' remove MapOptions.cameraConstraint stress-test diagnostic (DEBUG-02). Two atomic commits: refactor(03.1-12) f8b9a21 + feat(03.1-12) 50fa14e. ZERO shader edits across both commits (MIRL constraint preserved). Full test suite GREEN (180 + 1 skipped); flutter analyze 0 issues; dart format clean; tool tests 18/18. C2' / FOG-19 zoom-invariant basis DEFERRED to follow-up plan after fresh /gsd:discuss-phase 3.1 round resolves dimensional-mismatch design question. Phase 3.1 v1 requirement count grows from 71 to 73 (FOG-18 + DEBUG-02 added). Phase 3.1 plans 12 of 13 complete (Plan 03.1-13 Walk #5 pending). Next: Plan 03.1-13 sideload UAT Walk #5 to validate this Dart-only fix bundle on iPhone 17 Pro."
last_updated: "2026-05-04T12:58:09.606Z"
last_activity: "2026-05-04 — Plan 03.1-12 EXECUTED. C1' (FOG-18 — eliminate FOG-17a integer-wrap-modulo) + C3' (DEBUG-02 — remove MapOptions.cameraConstraint stress-test diagnostic) landed as two atomic commits (f8b9a21 + 50fa14e). ZERO shader source modifications (MIRL constraint preserved). Full test suite GREEN; analyze 0 issues; format clean; tool tests 18/18. C2' / FOG-19 zoom-invariant basis DEFERRED to follow-up plan. Phase 3.1: 12 of 13 plans complete; Plan 03.1-13 Walk #5 pending."
progress:
  total_phases: 6
  completed_phases: 3
  total_plans: 34
  completed_plans: 32
  percent: 100
---

# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-04-30)

**Core value:** The fog-of-war stays perfectly locked to the map during pan, zoom, and combined gestures on a sideloaded iOS build.
**Current focus:** Phase 1 — Foundation **CLOSED**. Phase 2 — Map (no fog) **CLOSED** (PERF-02 PASS). Phase 3 — Fog of War **CLOSED with HYPOTHESIS DENIED** (P03-08 walk 2026-05-01). Phase 3.1 — Fix Fog Pan-Translation **IN PROGRESS (5th iteration ongoing; Plan 03.1-12 EXECUTED 2026-05-04; Walk #5 pending in Plan 03.1-13)** — Plan 03.1-12 Dart-only fix bundle landed: C1' eliminate FOG-17a integer-wrap-modulo (FOG-18) + C3' remove MapOptions.cameraConstraint stress-test diagnostic (DEBUG-02). Two atomic commits (f8b9a21 + 50fa14e); ZERO shader source modifications (MIRL constraint preserved); full test suite GREEN (180 + 1 skipped); flutter analyze 0 issues; dart format clean; tool tests 18/18. C2' / FOG-19 zoom-invariant world-coordinate basis DEFERRED to follow-up plan after fresh `/gsd:discuss-phase 3.1` round resolves the refZoom-px-vs-screen-px dimensional-mismatch design question. **Plan 03.1-13 (Walk #5) pending** validates this fix bundle on iPhone 17 Pro and captures Q1b observation as input for the future C2' follow-up plan; new intermediate verdict outcome `CONFIRMED-AFTER-FIX-PARTIAL` introduced (Q1 + C3 closed but Q1b residual still present awaiting C2 follow-up). Phase 4 + Phase 5 STAY BLOCKED.

## Current Position

Phase: 3.1 of 6 (Fix Fog Pan-Translation — INSERTED gap-closure phase, in 5th iteration; Plan 03.1-12 EXECUTED 2026-05-04; Plan 03.1-13 Walk #5 pending)
Plan: 12 of 13 Phase 3.1 plans complete. Plan 03.1-13 (Walk #5) pending. Per CONTEXT §Iteration policy: no hard cap on walk count.
Status: Plan 03.1-12 EXECUTED 2026-05-04 — Dart-only fix bundle (Walk #4 follow-up) for two of the three Plan-03.1-12+ candidate axes captured by Walk #4. Two atomic commits landed on main: `refactor(03.1-12) f8b9a21 — C1' eliminate FOG-17a integer-wrap-modulo (introduce FOG-18)` + `feat(03.1-12) 50fa14e — C3' remove MapOptions.cameraConstraint stress-test diagnostic (introduce DEBUG-02)`. **C1' / FOG-18:** Walk #4 debug-spiral positive control (digit-atlas cells truly periodic on 384) showed ZERO steppiness during max-zoom pan, while production fog showed visible SNAP at every wrap event — falsifying FOG-17a's premise that the noise function was truly periodic on `kPocFogNoiseTilePx (=384)`. The wrap event itself was the bug, not the precision penalty FOG-17a was designed to address. _FogPainter.paint() now forwards camera.pixelOrigin directly (decomposed into intPx+fracPx for documentation continuity but with the modulo removed); `kPocFogIntegerWrapPeriodPx` constant deleted; `kPocFogSmoothCoordinateMaxDelta` updated to literal 2000.0. fp32 24-bit mantissa supports exact-integer values up to 16.7M raw-px, well above Walk #4's max observed pixelOrigin 4.26M. **C3' / DEBUG-02:** developer's verbatim Walk #4 request *"we should disable the bounding box that block us from going further to ensure that hard step do not reappear 100 km away"* — `MapOptions.cameraConstraint` parameter removed; flutter_map 7.0.2 default `CameraConstraint.unconstrained()` applies. Lets developer pan to extreme world coordinates (~100 km from Melun) during Walk #5 to verify FOG-18 doesn't introduce new precision-induced artefacts at high pixelOrigin magnitudes. Pure scope-extension diagnostic (not a fix axis). UX-02 InteractionOptions retained; minZoom/maxZoom retained; `kPocBboxLat*/Lon*` constants retained as stress-test-disabled (annotated docstrings). **C2' / FOG-19 DEFERRED:** zoom-invariant world-coordinate basis surfaced a dimensional-mismatch design question (refZoom-px basis vs screen-px uResolution mixing in shader `worldPx = fragUv * uResolution + uPixelOrigin` formula); resolution requires fresh `/gsd:discuss-phase 3.1` round to pick between (a) change uResolution semantics in both shaders (MIRL violation), (b) introduce uZoomScale uniform (relaxes MIRL), (c) Dart-side interpolation across zoom-snap boundaries, (d) other approach surfaced by discuss-phase. C2' deferral pattern established: when a fix axis surfaces an architectural tension that needs design conversation, defer to a follow-up plan after fresh discuss-phase rather than ship a half-working fix. **MIRL constraint preserved:** ZERO shader source modifications across both task commits — `git diff HEAD~2..HEAD -- assets/shaders/atmospheric_fog.frag assets/shaders/atmospheric_fog_debug_spiral.frag` empty. **Test gates:** flutter test 180 passed + 1 skipped (the existing FOG-09 RED-guard); flutter analyze --fatal-infos 0 issues; dart format --line-length 160 --set-exit-if-changed clean; dart test tool/test/ 18/18 GREEN. **REQUIREMENTS.md updated:** FOG-18 + DEBUG-02 entries added; Phase 3.1 count grows from 15 to 17; v1 total grows from 71 to 73. **ROADMAP.md updated:** Plan 03.1-12 row flipped from `- [ ]` to `- [x]` with execution summary; phase 03.1 plan-progress recalculated by gsd-tools (12 summaries / 13 plans). **Rule 3 — Blocking deviation:** `fog_world_coordinate_noise_test.dart` + `fog_debug_spiral_continuity_test.dart` were not in plan's `<files>` for Task 1 but their use of the deleted `kPocFogIntegerWrapPeriodPx` constant blocked compile; both updated to remove the reference (now FOG-18-aware; the integer-wrap-event sub-test in `fog_world_coordinate_noise_test.dart` was repurposed as historical reference documentation). **Rule 1 — Bug deviation:** Task 2's removal of `cameraConstraint` made `MapOptions(...)` const-eligible — analyzer flagged `prefer_const_constructors` + `unnecessary_const`; fixed by making the constructor const and dropping the now-unnecessary const on `interactionOptions`. Next action: Plan 03.1-13 (Walk #5) sideload UAT on iPhone 17 Pro to validate this Dart-only fix bundle; verdict outcomes documented in Plan 03.1-13's PLAN.md.
Last activity: 2026-05-04 — Plan 03.1-12 EXECUTED. C1' FOG-18 (eliminate FOG-17a integer-wrap-modulo) + C3' DEBUG-02 (remove MapOptions.cameraConstraint stress-test diagnostic) landed as two atomic commits. ZERO shader edits (MIRL preserved). Full test suite GREEN. C2' / FOG-19 DEFERRED to follow-up plan after fresh discuss-phase round. Plan 03.1-13 Walk #5 pending.

Progress: [█████████░] 94% (32 of 34 plans complete: Phase 1 + Phase 2 closed, Phase 3 closed with denied hypothesis, Phase 3.1 12 of 13 plans landed — Plan 03.1-12 Dart-only fix bundle EXECUTED 2026-05-04; Plan 03.1-13 Walk #5 pending. Phases 4 + 5 NOT started — blocked on Phase 3.1 closure.)

## Performance Metrics

**Velocity:**
- Total Phase 1 plans completed: 7 of 7
- Average duration (excluding Plan 07 sideload-UAT cycle): ~10 min/plan
- Total Phase 1 execution time: ~5h (including the 4h Plan 07 cycle)

**By Phase:**

| Phase | Plans | Total | Avg/Plan |
|-------|-------|-------|----------|
| 01-foundation | 7/7 | ~5h | ~43 min/plan (Plan 07 dominates due to sideload UAT cycle) |

**Recent Trend:**
- Last 5 plans: P03 (30 min), P04 (4 min), P05 (5 min), P06 (10 min), P07 (~4h with sideload UAT)
- Trend: short for software-only plans (4-10 min), long for plans with manual UAT (P03 30 min for CI workflow round-trip; P07 ~4h for full sideload + 16-step walk)

*Updated after each plan completion*
| Phase 01-foundation P04 | 4 min | 2 tasks | 5 files |
| Phase 01-foundation P02 | 9 min | 3 tasks | 10 files |
| Phase 01-foundation P03 | 30 min | 3 tasks | 9 files |
| Phase 01 P01 | 7 min | 3 tasks | 12 files |
| Phase 01-foundation P05 | 5 min | 2 tasks | 4 files |
| Phase 01-foundation P06 | 10 min | 2 tasks | 4 files |
| Phase 01-foundation P07 | ~4h | 2 tasks (1 auto + 1 checkpoint:human-verify) | 11 files (4 created + 2 sideload-prep + 1 docs + 4 modified) |
| Phase 02-map-no-fog P01 | 59 min | 3 tasks | 21 files (16 created + 5 modified) |
| Phase 02-map-no-fog P03 | 4 min | 2 tasks | 4 files |
| Phase 02-map-no-fog P02 | 5 min | 2 tasks | 5 files |
| Phase 02-map-no-fog P04 | 8 min | 2 tasks | 4 files |
| Phase 02-map-no-fog P05 | 21 min | 1 tasks | 5 files |
| Phase 02-map-no-fog P06 | ~50 min (planning + UAT walk + closure docs) | 3 tasks (1 auto + 1 checkpoint:human-verify + 1 auto) | 5 files (1 created + 4 modified) |
| Phase 03-fog-of-war-the-hypothesis P01 | 16 min | 3 tasks (3 auto, 0 checkpoints) | 28 files (16 created + 5 production-file edits + 3 test-file edits + 4 generated/gitignored l10n synth) |
| Phase 03-fog-of-war-the-hypothesis P02 | 5 min | 2 tasks | 2 files |
| Phase 03-fog-of-war-the-hypothesis P04 | 4 min | 1 tasks | 2 files |
| Phase 03-fog-of-war-the-hypothesis P03 | 5 min | 2 tasks | 3 files |
| Phase 03-fog-of-war-the-hypothesis P06 | 7 min | 2 (TDD: 4 commits) tasks | 4 files (2 production + 2 test) files |
| Phase 03-fog-of-war-the-hypothesis P05 | 14 min | 2 tasks | 6 files |
| Phase 03-fog-of-war-the-hypothesis P07 | 90 min | 2 tasks | 5 files |
| Phase 03-fog-of-war-the-hypothesis P08 | ~1h end-to-end (pre-walk gate sequence + walk + closure docs) | 3 tasks (Task 1 auto pre-walk gates + Task 2 checkpoint:human-verify walk + Task 3 auto closure docs) | 5 files (1 created: 03-08-SUMMARY.md; 4 modified: 03-FALSIFICATION.md, 03-UAT.md, REQUIREMENTS.md, STATE.md, ROADMAP.md) |
| Phase 03.1-fix-fog-pan-translation P01 | 6 min | 2 tasks | 5 files |
| Phase 03.1-fix-fog-pan-translation P02 | 11 min | 3 tasks | 11 files (1 created + 10 modified) |
| Phase 03.1-fix-fog-pan-translation P04 | 11 min | 3 tasks | 10 files |
| Phase 03.1-fix-fog-pan-translation P05 | 14 min | 3 tasks (each TDD: 6 commits total) | 9 files (4 production + 4 test + 1 planning doc) |
| Phase 03.1-fix-fog-pan-translation P06 | 73 min | 3 tasks | 5 files |
| Phase 03.1-fix-fog-pan-translation P08 | 9 min | 3 tasks | 7 files |
| Phase 03.1-fix-fog-pan-translation P07 (Task 3 only) | ~16 min | 1 resumed task (Branch B-3) | 5 files (2 shaders + 2 tests + 1 constants helper trace; plus REQUIREMENTS/ROADMAP/STATE meta) |
| Phase 03.1-fix-fog-pan-translation P09 | ~12 hours end-to-end (Task 1 skeleton + ~12h Task 2 walk window incl developer sideload + Task 3 verdict authoring ~25 min) | 3 tasks (1 auto skeleton + 1 checkpoint:human-verify walk + 1 auto verdict) | 5 files (2 created: 03.1-FALSIFICATION-3.md + 03.1-UAT-3.md; 3 modified: REQUIREMENTS.md + ROADMAP.md + STATE.md) |
| Phase 03.1-fix-fog-pan-translation P10 | 25 min | 3 tasks | 13 files |
| Phase 03.1 P12 | ~25min | 2 tasks | 9 files |

## Accumulated Context

### Decisions

Decisions are logged in PROJECT.md Key Decisions table.
Recent decisions affecting current work:

- Roadmapping: Stack locked to `flutter_map 7.0.2` + `vector_map_tiles 8.0.0` + `vector_map_tiles_pmtiles 1.5.0` (only resolvable chain); state mgmt = plain `StatefulWidget` + `setState` + constructor-injected services; iOS-primary on iPhone 17 Pro (ProMotion 120 Hz), Pixel 4a only at Phase 3 + Phase 5.
- Roadmapping: Falsification Criterion C (parent-FPS comparison) DROPPED — POC stands on absolute FPS + lock-correctness alone.
- Roadmapping: 5-phase structure under coarse granularity; Bootstrap + CI + logger + share + FPS counter + permission gate folded into a single walkable Phase 1 ("install → permission screen → share logs").
- Roadmapping: Phase 2 PERF-02 (≥ 40 fps no-fog on iPhone 17 Pro) is the hard gate before Phase 3 fog work begins; failing it forces label-thinning before the hypothesis is testable.
- [Phase 01-foundation]: Plan 01-04: ported FileLogger verbatim with three POC adaptations (UTC ISO-8601 basic filename, hardcoded Level.ALL, shared_preferences dropped). Every other line preserved from parent — RandomAccessFile + flushSync per record, synchronous Stream.listen handler. ~14 tests authored across 3 files. — Production-fatal subsystem — parent's logger is the result of debugging two iOS-fatal bugs (jetsam page-cache loss, Stream.listen re-entrancy). Re-implementing from scratch would re-introduce both. Tests cover bootstrap, level, ms precision, idempotency, prune, FileSystemException handling (static-source assertion per W-4), UTC format, and lifecycle flush behaviour.
- [Phase 01-foundation]: Plan 01-02: ported tool/* CI gates verbatim from parent project (header/license/deps-md), authored DEPENDENCIES.md telemetry table, rewrote ios/Runner/Info.plist with whenInUse-only location + ITSAppUsesNonExemptEncryption=false, created PrivacyInfo.xcprivacy with Required Reason API declarations
- [Phase 01-foundation]: Plan 01-03: hand-rolled MirkViewportBbox replaces freezed-generated parent class; package:mirkfall imports rewritten to package:mirk_poc_debug; CI workflow lands at Wave 0 (red-then-green until Plan 07 hardens main.dart). — POC pubspec drops freezed per RESEARCH.md §Standard Stack §NOT included; preserving each donor file's import-style minimises diff for future verbatim re-syncs from parent; landing CI at Wave 0 lets Plan 07 see immediately whether gates job goes green on the first push.
- [Phase 01-foundation]: Plan 01-01: adopted existing Flutter scaffold instead of running flutter create (Rule 3 deviation — scaffold from prior session matched user-approved bundle ID com.thongvan.mirkPocDebug). Strict-pinned Path A pubspec.yaml committed with test: 1.30.0 added per Plan 01-02 coordination flag. Constants port: 30 of parent's 880 constants — only what Phase 1 + BOOT-08 donor files reference. Asset bundle test for shader uses IPLR magic-byte verification (not loadString) because Flutter's shaders: block compiles to binary at build time. — scaffold-adoption avoids overwriting Plan 01-02 parallel work; test 1.30.0 enables Plan 01-02 tool tests; constants subset prevents Phase 2+ dead code; IPLR-aware test mirrors the runtime FragmentProgram.fromAsset idiom
- [Phase 01-foundation]: Plan 01-05: buildPocAppBar PreferredSizeWidget factory + FpsCounterOverlay StatefulWidget landed (LOG-04 + PERF-01). 9 widget tests green, strict analyze clean, GOSL headers verified. Three Rule 1 deviations: (1) SharePlus.instance.share(ShareParams) replaces deprecated Share.shareXFiles in share_plus 12.0.2; (2) dropped unnecessary dart:ui FontFeature import; (3) AppLocalizations import via package:mirk_poc_debug/l10n (Plan 01-01 deferred-items pre-flagged this).
- [Phase 01-foundation]: Plan 01-06: PermissionGateScreen (StatefulWidget + WidgetsBindingObserver lifecycle re-check — W-2 fix) + PermissionDeniedScreen (Stateless single Open-Settings) landed (AUTH-01..04). 11 widget tests green; strict analyze clean on lib/presentation + test/presentation. Pattern established: tester.binding.handleAppLifecycleStateChanged + mid-test mock mutation simulates the post-Settings round-trip without a real iOS device. PermissionHandlerPlatform.instance test seam reused as the zero-dep mock pattern. — Lifecycle re-check is THE differentiator vs. cold-restart UX; per CONTEXT.md user must NOT need to re-open the app to escape the denied screen. Single suppression `// ignore_for_file: depend_on_referenced_packages` on test files (transitive permission_handler_platform_interface) follows Plan 01-04 pattern; production lib carries no suppressions.
- [Phase 01-foundation]: Plan 01-07: lib/main.dart bootstrap (LOG-03 ordering: WidgetsFlutterBinding.ensureInitialized → await FileLogger.bootstrap → addObserver(FileLoggerLifecycleObserver) → FlutterError.onError → runZonedGuarded(runApp(MirkPocApp))) + lib/app.dart MirkPocApp + lib/presentation/router.dart 3-route GoRouter (all context.go) + lib/presentation/screens/map_screen.dart placeholder all landed. CI green on `main` (gates+android+ios), both APK+IPA artifacts downloadable. iPhone 17 Pro sideload UAT: verbal `approved` — LOG-05 (Mail round-trip with valid JSONL log) ✓, PERF-01 (FPS counter `<value> fps / 120 Hz` ProMotion-aware) ✓, AUDIT-03 (DEPENDENCIES.md zero `Yes/automatic` rows) ✓; AUTH-04 round-trip marked complete-with-known-limitation (cold-restart-from-/denied auto-nav routing bug deferred per user's POC-scope call). 8 deviations: 6 during Task 1 (l10n import correction, deferred-items analyze gates resolved, Windows file_logger_test fix, CI gate scripts against POC dep graph, CI pre-format gen-l10n, remote main bootstrap merge); 2 sideload-time fixes (CFBundleName mirk_poc_debug → MirkPocDebug for SideStore re-sign per docs/flutter-ios-specifics.md §2; ios/Podfile committed with PERMISSION_LOCATION=1 macro per §1). New 744-line `docs/flutter-ios-specifics.md` captures all recurring iOS Flutter recipes (Podfile macros, CFBundleName, FileLogger anatomy, gen-l10n CI gotcha, location 2-step pattern with auto-resume hook) for future projects.
- [Phase 02-map-no-fog]: Plan 02-01: Wave 0 scaffold landed — 16 new files (7 production stubs + 9 RED test files) + 5 file edits (constants, l10n EN/FR, router /error route, MapScreen.fromServices stub). MapScreenServices immutable DTO (lib/domain/map/map_screen_services.dart) is the constructor-injection seam; ErrorScreen is the only fully-implemented Phase 2 production widget. 19 new constants (camera/zoom/pan envelope, animation timing, GPS distanceFilter, PMTiles paths, blue-dot spec). 5 new l10n keys (recenterTooltip, compassTooltip, errorScreenTitle/Body/DetailLabel) in EN + FR. LOC-03 static-source CI gate auto-discovered by existing `dart test tool/test/` step. flutter analyze clean across lib/test/tool. flutter test (30s/test): 48 passed (Phase 1 untouched + 1 trivial mounted-guard) / 27 RED Phase 2 assertions / 0 hangs. 2 deviations (Rule 3 — Blocking: MapScreen.fromServices named-constructor stub added so screen tests compile; Rule 1 — Bug: replaced synthetic temp-file pmtilesPath with literal placeholder string after first test run hung the suite for 10 min on vector_map_tiles parsing of empty file). Plan 02-02 (MAP-01 PMTiles copier impl + permission-gate wiring + @visibleForTesting testOverride seam) unblocked.
- [Phase 02-map-no-fog]: Plan 02-03: GeolocatorService.stream() with pinned LocationSettings(accuracy: best, distanceFilter: 5) + Logger('domain.location') INFO emission. BlueDotMarker.build returns CircleMarker(7 px / #2B7CD6 / 2 px white stroke / pixels-not-metres) — every value sourced from kPocBlueDot* constants. LOC-03 static-source CI gate hardened to strip Dart // /// /* */ comments before substring scan, so the educational 'do NOT call `Geolocator.getLastKnownPosition`' docstring is preserved without false-positiving. Test seam: hand-rolled _CapturingGeolocatorPlatform fake (mirrors Phase 1 PermissionHandlerPlatform.instance pattern), zero new dev_dependencies. 2 deviations (Rule 3 - Blocking: comment-aware gate fix; Rule 3 - Blocking: wave-2 race amend to remove sibling's recenter_fab_test.dart from my Task 2 commit).
- [Phase 02-map-no-fog]: Plan 02-02: MAP-01 PMTiles copy fully implemented + hooked into both grant paths of PermissionGateScreen. Idempotency = existence + size-parity (no SHA256 per ROB-02 deferral). @visibleForTesting testEnsureCopiedOverride seam keeps gate-screen widget tests off the real filesystem. Both _onCtaPressed (CTA grant) and _checkAndMaybeNavigate (initState + AppLifecycleState.resumed) converge through a single _ensureMapDataAndNavigate helper per CONTEXT.md mandate. FileSystemException routes to /error with extra==e.message. 5 PMTiles tests + 3 MAP-01 widget tests + 6 Phase 1 regression tests all GREEN; flutter analyze + LOC-03 + BOOT-02 CI gates clean. 0 deviations.
- [Phase 02-map-no-fog]: Plan 02-04: RecenterFab (LOC-04 + LOC-05) + MapCompass landed. Hand-rolled AnimationController + CurvedAnimation listener pattern for both widgets — no flutter_map_animations dependency added. Top-level mapCompassShortestPathToNorth(double) helper exposes the formula ((-current + 540) % 360) - 180 and pins RESEARCH Open Question #2 with 6 unit tests (350°→+10°, 10°→-10°, 180°→±180°, 270°→+90°, etc.). MapEventRotate plumbing: production reads event.camera.rotation (degrees), test fake constructs real MapCamera(crs: const Epsg3857(), nonRotatedSize: kImpossibleSize, rotation: degrees). 18 GREEN tests (6 RecenterFab widget + 6 MapCompass widget + 6 helper unit). 3 deviations: 1 parallel-sibling git-state recovery (Task 1 RED commit absorbed into GREEN), 2 Wave-0 RED-test bug fixes (MapEventRotate noSuchMethod fake → real constructor; cancels-on-dispose baseline capture moved post-pumpAndSettle).
- [Phase 02-map-no-fog]: Plan 02-05: MapScreen Phase-1 placeholder rewritten as full StatefulWidget — FlutterMap + VectorTileLayer (ProtomapsThemes.lightV3, source key 'protomaps') + conditional CircleLayer/BlueDot + RecenterFab + MapCompass + FpsCounterOverlay all composed; synchronous void dispose with fire-and-forget cancel/close; production /map route via FutureBuilder around getApplicationSupportDirectory; tests use tester.runAsync for real dart:io archive open + path_provider mock for vector_map_tiles cache resolver. 2 deviations: Rule 1 — MapCompass.initState try/catches camera read so it can mount on the same frame as FlutterMap (Plan 02-04 latent bug surfaced only in screen-level composition); Rule 3 — tester.pump doesn't advance real dart:io futures, switched to tester.runAsync. — Plan 02-04's all-fakes unit tests masked the camera-read crash; pump-the-real-tree integration tests catch this class of bug. Plan-prescribed _ProductionMapScreen factory removed (plan called it awkward; router builder constructs MapScreen.fromServices directly). Theme prefix vtr.Theme via 'as vtr' import — symmetric with vector_map_tiles' own 'hide Theme' pattern. Archive-close test seam deferred (real archive opens reliably from temp file in tests; dispose-cancels-GPS-subscription test proves dispose path runs).
- [Phase 02-map-no-fog]: Plan 02-06: PERF-02 sideload UAT walk verbal `approved` on iPhone 17 Pro against CI run 25212559648 (SHA 46b8fcc). Developer's verbatim verdict: *"everything works well, 120 fps when doing stuff, revert to 4 when not doing anything"*. Sustained ~120 fps observed during pan / pinch / combined gestures at zoom 13–15 over 200 m walk through central Melun — **3× headroom over the ≥ 40 fps gate**, massive frame-budget margin for Phase 3's fog shader. Idle ~4 fps is expected Flutter no-dirty-frames behaviour (frames scheduled only on dirty/animating frames; same pattern observed in Phase 1 sideload UAT with FpsCounterOverlay from Plan 01-05) — NOT a regression. Light-touch evidence record honouring developer's verbal verdict (no per-zoom FPS table or per-gesture tally captured for this POC walk; the 3× margin renders fine-grained capture unnecessary). Sideload bug-fix `46b8fcc fix(02-05): swallow vector_map_tiles CancellationException in test teardown` (separate bug-investigation subagent) was needed to unblock CI's iOS gate before the IPA could be built. Phase 2 CLOSES with all 12 requirement IDs (MAP × 6, LOC × 5, PERF-02) Complete. Phase 3 (Fog of War — THE HYPOTHESIS) unblocked.
- [Phase 03-fog-of-war-the-hypothesis]: Plan 03-01: Wave 0 keystone scaffold landed — 12 production stubs (RevealDiscRepository, distanceMetres, SdfCache, SdfRebuildLogger, FrameDeltaProbe + FrameDeltaRollup, FogLayer, computeFogClipPath, FrameDeltaProbeOverlay, ShaderSanityScreen) returning UnimplementedError or SizedBox.shrink + 12 RED test files (1 GREEN day-1 slot-count gate, 13 RED behavioural assertions, 6 skipped pending Plan 03-05/06/07 test seams) + Phase 3 constants block (10 new constants — kPocRevealDiscRadiusMeters, kPocFrameDeltaProbeOverlayTopPx/RightPx, log rollup cadences, ring buffer cap, sanity screen synthetic radius, frame-delta probe colour-coding thresholds, fog shader asset path) + 5 l10n keys EN+FR (shaderSanityTooltip, frameDeltaProbe{Median,P95,Max}Label, shaderSanityScreenTitle) + GoRouter `/sanity` route → ShaderSanityScreen + AppBar `Icons.science` action button (visual order [science][share]) + 03-FALSIFICATION.md skeleton (Criteria A + B written; evidence + verdict empty pending Plan 03-08). MapScreenServices DTO extended with required `discRepository` + `frameDeltaProbe` fields; production /map route + every test call site updated. flutter analyze --fatal-infos lib/+test/ clean; flutter test reports +97 GREEN / ~6 SKIPPED / -13 RED — exact falsification contract Wave 0 promised. 5 deviations: 4 Rule 3 - Blocking (testWidgets `skip:` is `bool?` not `String` in flutter_test 3.41 — reasons embedded in test description; MapCamera.nonRotatedSize is `Point<double>` not `ui.Size`; dart:ui FragmentShader is a base class — fog_layer_test.dart simplified to skipped placeholder pointing at Plan 03-05 production-side test seam; poc_app_bar tooltip queries narrowed via `find.ancestor(of: byIcon(Icons.share), matching: IconButton)` so LOG-04 share-tooltip assertion still holds with the new science button), 1 Rule 1 - Bug (multi-line `// ignore:` directive on `_rebuildLogger` did not suppress `unused_field` — restructured to single-line ignore directly above the field with rationale moved into a `///` docstring). Wave 1 unblocked: Plans 03-02..03-04 each consume specific stubs and flip specific RED tests to GREEN.
- [Phase 03-fog-of-war-the-hypothesis]: Plan 03-04: FOG-08 FrameDeltaProbe full implementation landed — Stopwatch-backed monotonic clock + 240-cap FIFO ring buffer + idempotent 1-Hz Timer.periodic + broadcast StreamController<FrameDeltaRollup> + JSONL emission via Logger('infrastructure.mirk.frame_delta'). Dual-clock discipline pinned: Stopwatch.elapsedMicroseconds for ALL delta math (immune to NTP correction during 5-min walk per RESEARCH.md Pitfall 4), DateTime.now() ONLY for the epochSecond rollup tag (REQUIRED for grep-correlation with SdfRebuildLogger). Test #7 asserts epochSecond magnitude > 1.7e9 to defend the invariant — anyone 'simplifying' to a single Stopwatch-derived clock would fail on first run. @visibleForTesting debugRecordRawDelta seam keeps production methods (recordCameraSnapshot/recordFogUniformPopulation) untouched while letting tests assert ±0 µs precision. Defence-in-depth: non-monotonic snapshotMicros clamps delta at 0 (math.max), never throws — paint path bug-tolerant. JSONL line emits all 8 keys (epochSecond + sampleCount + 4 µs stats + 3 ms convenience getters at 3-decimal) for parser parity with SdfRebuildLogger. 7 RED → 7 GREEN; 1 deviation (Rule 1 - Bug: removed unnecessary dart:async import in test file, transitively re-exported via flutter_test). Wave 1 (Plans 03-02..03-04) COMPLETE; Plan 03-05 unblocked.
- [Phase 03-fog-of-war-the-hypothesis]: Plan 03-02: RevealDiscRepository (FOG-01) + distanceMetres (FOG-02) shipped — in-memory ChangeNotifier with defensive-copy snapshot via List.unmodifiable(_discs) (paint-time safe under concurrent mutation); top-level Haversine helper using kEarthRadiusMeters (single source of truth across revealed-domain code, mirrors donor reveal_disc.dart). 5 RED → GREEN flips (3 repo + 2 distanceMetres). 1 Rule 2 deviation: hoisted 180.0 inline literal into _degreesPerHalfTurn private constant for CLAUDE.md no-magic-numbers compliance. Both surfaces unblock Plan 03-05 FogLayer (snapshot iteration) and Plan 03-07 MapScreen integration (append + move-distance gate).
- [Phase 03-fog-of-war-the-hypothesis]: Plan 03-03: SdfCache hash key composes Object.hash(viewport, Object.hashAll(discHashes), discs.length); per-disc lat/lon quantised to 1e6 (~10 cm), radius to 1e3 (1 mm), tames consecutive-frame floating-point drift at the same GPS fix. SdfRebuildLogger exposes injectable rollupInterval (defaults to kPocSdfLogRollupSeconds=1s); stop() flushes pending samples synchronously to guard against losing the final rollup if MapScreen.dispose runs mid-window. Logger.root.level = Level.ALL required in test setUpAll() (mirrors Phase 1+2 pattern) — package:logging defaults to Level.WARNING, swallowing INFO rollup lines. Beyond-plan stop-flush test added (3 SdfRebuildLogger tests total) to pin the synchronous final-flush contract; rollupInterval seam dropped active-rollup test from 1100 ms to 250 ms (4x faster suite).
- [Phase 03-fog-of-war-the-hypothesis]: Plan 03-06: FrameDeltaProbeOverlay (FOG-08 user-facing) + ShaderSanityScreen pre-walk gate landed. Overlay subscribes to probe.rollups and renders 3 colour-coded lines (med/p95/max) at 1 Hz cadence — no internal Timer; cadence inherited from probe rollup emission. Pre-rollup placeholder uses dash so 'no samples yet' differs from 'samples = 0'. ShaderSanityScreen loads atmospheric_fog.frag + builds synthetic 80 m disc SDF + paints fog through FogShaderUniforms.setAll (same call shape as FogLayer). programLoaderOverride constructor seam isolates FragmentProgram.fromAsset from headless test runners. 5 RED → 5 GREEN: 2 overlay (placeholder + colour bands) + 3 sanity (loading spinner + error state + FR title). 3 deviations: Rule 1 test data fix (10→20 samples so floor(22*0.95)=20 places p95 in yellow, not collapsing onto max), Rule 3 fake_async pending-Timer fix (explicit probe.stop() inline), Rule 1 unused dart:ui import. Plan 03-07 (MapScreen Stack composition) and Plan 03-08 (sideload UAT walk) unblocked.
- [Phase 03-fog-of-war-the-hypothesis]: Plan 03-05: FogLayer + computeFogClipPath + FogShaderRenderer interface landed (FOG-04..07 keystone + FOG-08 wire). FOG-07 KEYSTONE test GREEN — readCount==1 initial, +1 per forced rebuild over 3 rebuilds, mechanically defends against BUG-014 white-ellipse symptom. Painter holds LIVE Stopwatch BY REFERENCE; paint() reads elapsedMicroseconds fresh per call (anti-frozen-uTime, PERF-03 prerequisite). 4 deviations: 2 Rule 3 - Blocking (flutter_map 7.0.2 actual API differs from plan — size:Point<double> + latLngToScreenPoint not size:Size + latLngToScreenOffset; dart:ui 3.41 FragmentShader is base — made FogLayer.shader nullable, tests pass null), 1 Rule 1 - Bug (FOG-04 test direction — find.descendant not find.ancestor; FlutterMap does NOT auto-wrap children in MobileLayerTransformer per flutter_map widget.dart lines 97-108), 1 Rule 1 - Bug (dart format reflowed lines + auto-fixed prefer_const_constructors). Phase 3 RED count: 0 (was 2 fog_clip_path tests; both flipped GREEN + 1 added). Plan 03-07 unblocked.
- [Phase 03-fog-of-war-the-hypothesis]: Plan 03-07: MapScreen × Phase 3 production assembly. Hand-rolled disc ID rvd_<microsSinceEpoch>_<randomU32>_<counter> per RESEARCH §Open Question 4 (no ulid dep). fogProgramLoaderOverride field added to MapScreenServices DTO — same constructor-injection seam as Plan 03-06 ShaderSanityScreen.programLoaderOverride; production wiring leaves null. Graceful no-fog fallback on shader load failure (catch + log severe + leave _fogShader null + FogLayer never mounts) — pre-walk /sanity gate prevents shipping broken IPA. In-body teardown for fog tests touching FrameDeltaProbe broadcast StreamController (addTearDown + cancel-in-flight overlay subscription = 10-min hang under flutter_test 3.41). 3 deviations — 2 Rule 3 - Blocking [program loader hang, addTearDown hang], 1 Rule 1 - Bug [dart format reflow]; full suite 126 GREEN / 0 SKIPPED / 0 RED.
- [Phase 03-fog-of-war-the-hypothesis]: Plan 03-08: **HYPOTHESIS DENIED.** Sideload UAT walk on iPhone 17 Pro 2026-05-01 against CI run 25224334312 (SHA 280dd04) in central Melun. Pre-walk gates all GREEN (flutter test 126/126, flutter analyze 0 warnings, dart format clean, dart test tool/test/ 18/18 GREEN, CI green on 280dd04, IPA downloaded to `.uat-tmp/mirk-poc-debug-unsigned.ipa`). `/sanity` shader-compile gate held (developer saw "mirk" on the walk screen). Walk evidence: developer's verbatim *"mirk isn't moving, only the blue dot (so I guess the map below is moving), it can be rotated tho, denied"*. Fog static during pan; rotation transforms apply correctly; only the blue dot translates with the camera. Per Plan 03-08 falsification clause Criterion B's failure alone delivers `denied` (Criterion A unmeasured-and-moot — walk aborted on visual grounds before quantitative probe rollups recorded). MirkFall recommendation: **DO NOT PORT BACK as-implemented**. Three diagnostic possibilities for Phase 3.1 gap-closure: (1) FogPainter draws in screen-space without consuming the camera's translation transform; (2) MobileLayerTransformer applies translation at the widget layer (Transform widget) but rotation via Canvas matrix that the painter's local Canvas does not inherit; (3) MapCamera updates do not propagate to FogLayer.build() between pan-driven repaint cycles, so the painter holds a stale build-time camera snapshot during the entire pan gesture. The diagnostic test that cleaves these three possibilities: log Canvas.getTransform() in FogPainter.paint() vs MapCamera.center in FogLayer.build() during a pan gesture. Phase 4 (wisp particles) and Phase 5 (decision gate) are **BLOCKED** until Phase 3.1 produces a fix or formally confirms the failure is unfixable. Phase 3 closes with a falsified hypothesis — a valid scientific result; the POC's deliverable was binary discrimination, not direction.
- [Phase 03-fog-of-war-the-hypothesis]: Lesson learned (Plan 03-08): **Structural widget-tree-containment tests are necessary-but-not-sufficient for same-Canvas hypothesis tests.** Plan 03-05's FOG-04 GREEN test (`find.descendant(of: FogLayer, matching: MobileLayerTransformer)` returns one match) was treated as a same-Canvas keystone but only confirmed widget-tree containment, NOT Canvas-transform sharing. Future port-back work to MirkFall, AND any Phase 3.1 fix attempt, must add a behavioural-transform-equality test alongside the structural test: trigger a real pan in a real flutter_map context, log the painter's `Canvas.getTransform()` matrix in `paint()`, assert it matches the tile layer's transform at the same paint frame. The structural-only test should be retained but augmented, not replaced.
- [Phase 03.1-fix-fog-pan-translation]: Plan 03.1-01: FogTransformLogger structurally mirrors SdfRebuildLogger verbatim — same constructor seam (rollupInterval), same start/stop/_emitRollup shape, same Logger.info JSONL emission. min/median/max instead of median/p95/max because p95 is meaningless for spatial coordinates; toStringAsFixed(6) string values in JSONL aligned with kPocCanvasTransformEpsilon (1e-6) for grep-readable transform-equality. computeStats helper exposed @visibleForTesting (static) for Plan 03.1-02 unit-level reuse. 1 Rule 1 deviation: removed redundant dart:typed_data import (Float64List re-exported via package:flutter/foundation.dart). 4/4 GREEN tests; 130 full-suite GREEN; project-wide analyze + format clean. FogTransformLogger is import-ready for Plan 03.1-02 to wire into _FogPainter.paint() alongside the 3-line uOffset derivation fix.
- [Phase 03.1-fix-fog-pan-translation]: Plan 03.1-03: **VERDICT ITERATING-WITH-MAJOR-PROGRESS** per CONTEXT §Iteration policy. Post-fix iPhone 17 Pro sideload session 2026-05-02 22:10:39Z..22:11:15Z against CI run 25260475395 (SHA 5c63197). Developer's verbatim verdict: *"iterating with major progress — fix is partial"*. **PERF-07 GREEN** — 36 1-Hz frame_delta rollups: median-of-medians 0.347 ms (≤16 ✓; 7.3× headroom), max-of-p95s 5.598 ms (≤32 ✓; 5.7× headroom), max-of-maxes 10.564 ms (≤48 ✓; 4.5× headroom). The unmeasured-and-moot Criterion A from Plan 03-08 is now properly captured. **Criterion B PARTIAL** — Plan 03-08 constant-zero failure mode structurally addressed (uOffset DOES reach the shader per fog_transform Finding 2; FOG-09 catches simple-pan failure mode mechanically in CI) BUT post-fix walk surfaced three new failure modes invisible to FOG-09: per-paint modulo-1.0 wrap producing visible noise-seam shimmer ("seed changing" obs 2 — corroborated by Finding 3: uOffsetX sweeps 0.005..0.99 5-10× per 1-Hz rollup during gestures); intermittent partial pan tracking (obs 3 — secondary effect of obs 2); reveal-hole offset from blue dot during gesture (obs 4 — corroborated by Finding 1: canvasTx/Ty jumps to (5.035, -44.198) mid-session and holds; RESEARCH §Pitfall D's "Canvas at identity" claim invalidated). FOG-04..07 retained at Falsified-in-production (NOT flipped). Two specific Plan 03.1-04 fix axes identified: (a) SHADER-MODULO-WRAP — pass full-precision pixelOrigin to the shader as vec2 uPixelOrigin, apply modulo per-pixel inside the fragment shader; (b) CANVAS-FRAME-ALIGNMENT — read canvas.getTransform() in _FogPainter.paint() and apply inverse to SDF coordinates (or render blue dot inside FogLayer's Canvas). Side anomaly recorded for Plan 03.1-04 RESEARCH evaluation: SDF rebuilds 12-115/sec during gestures (viewport-in-cache-key invalidation thrash) — per-rebuild stays sub-13ms so frame budget preserved, but candidate residual contributor to obs 2. Out-of-scope follow-up captured: ShaderSanityScreen no back button — single-line fix deferred. FOG-09 scope-clarified in REQUIREMENTS: it's a binary "moved at all?" gate, not a "looks correct?" gate; Plan 03.1-04 will add FOG-11 + FOG-12 as higher-fidelity behavioural gates. **Closure decision: iterate-with-major-progress; spawn Plan 03.1-04 via `/gsd:plan-phase 3.1 --gaps`**. Phase 4 + Phase 5 stay BLOCKED. MirkFall migration recommendation **DEFERRED** until subsequent Phase 3.1 plan delivers CONFIRMED-AFTER-FIX or DENIED-FINAL. Evidence: `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION.md` + `03.1-UAT.md`.
- [Phase 03.1-fix-fog-pan-translation]: Plan 03.1-02: 3-line uOffset derivation fix landed — `_FogPainter.paint()` derives `(uOffsetX, uOffsetY) = (camera.pixelOrigin / size) % 1.0` from the painter's existing `camera` field (FOG-07 single-snapshot lock preserved; identity uSdfRect UNCHANGED per Anti-Pattern 1). FogTransformLogger.recordPaint(...) instrumented AFTER the derivation but BEFORE the shader call so the JSONL captures the actual values forwarded. FOG-09 keystone test pumps real FlutterMap+FogLayer at Melun zoom 13, drives mapController.move(LatLng(48.5500, 2.6700), 13.0), asserts deltas exceed `kPocCanvasTransformEpsilon` on both axes — catches Plan 03-08 failure mode mechanically. Vacuous-test guard sub-test skip:true (manual unskip against pre-fix HEAD verifies guard). FOG-04 docstring augmented with forward-pointer (Plan 03-08 lesson made permanent). 5 deviations: 3 Rule 3 - Blocking (Path symbol collision dart:ui vs latlong2 — qualified as `ui.Path`; flutter_test 3.41 `skip:` is `bool?` not `String` — embedded rationale in description; SdfCache async resolution required `tester.runAsync(...)` 30 × 20 ms budget so the painter's null-sdfImage guard didn't short-circuit the test), 2 Rule 1 - Bug (unused `package:flutter/rendering.dart` import; cosmetic comment containing `const (0.0, 0.0)` literal violated strict done-grep). Task 1 deferred FogLayer mount-site arg until Task 2 (FogLayer constructor didn't accept the field yet) — the split kept each task's `<verify>` independently green. 131 GREEN + 1 SKIPPED full suite; project-wide analyze + format clean.
- [Phase 03.1-fix-fog-pan-translation]: Plan 03.1-05: CANVAS-FRAME-ALIGNMENT (FOG-12) + SDF-CACHE-VIEWPORT-THRASH (PERF-08) + SANITY-NO-BACK-BUTTON (UX-01) all software-complete in 14 min via 6 atomic commits (3 RED + 3 GREEN, TDD pair per task). `_FogPainter.paint()` reads `canvas.getTransform()` ONCE per paint at the top of paint() — single-snapshot at the matrix level mirrors FOG-07 single-MapCamera-snapshot; the same Float64List is reused for both the new clip-path correction (`computeFogClipPath(canvasOffset: Offset(matrix[12], matrix[13]))`) AND the existing fogTransformLogger emission. `computeFogClipPath` gains optional `Offset canvasOffset = Offset.zero` param; default collapses to original Plan 03-05 behaviour; pre-shifts BOTH the world rect AND every disc-hole center by `-canvasOffset`. `SdfCache._hash()` calls new `_quantiseBbox(viewport)` helper hashing 4 quantised bbox edges (× 1e4 rounded to int); cache rebuild trigger threshold (1e-4 lat/lon ≈ 11 m at equator) sized between per-paint micro-drift (~1e-7) and any real GPS-fix-driven viewport jump (~9e-6 per metre). PERF-08 test pattern reuses `Logger.root.onRecord` subscription verbatim from sdf_rebuild_logger_test.dart Test 1 — ZERO new seams added to SdfRebuildLogger; JSON-prefix filter (`'{'`) separates rollup lines from donor RevealedSdfBuilder's non-JSON status logs (both share logger name `'infrastructure.mirk.sdf'`). UX-01 chose `context.push` + explicit `IconButton(Icons.arrow_back, onPressed: () => context.pop())` over Material's `automaticallyImplyLeading` — Navigator.maybePop does NOT pop a GoRouter route reached via context.push (GoRouter requires context.pop). 5 deviations: 3 Rule 1 - Bug (FOG-12 sub-pixel tolerance 1e-3 instead of `kPocCanvasTransformEpsilon` 1e-6 because `Path.combine(PathOperation.difference, ...)` introduces ~1e-6 float noise on non-axis-aligned shapes; PERF-08 test removed unnecessary `as String`/`as int` casts after analyzer flagged them; UX-01 test replaced `(BuildContext _, GoRouterState __)` with Dart 3.7 wildcard `(_, _)` + wrapped router.push in unawaited()), 2 Rule 3 - Blocking (PERF-08 test JSON-prefix filter for log records to separate rollup JSON from donor builder status text; UX-01 test replaced pumpAndSettle with finite pump-with-duration sequence to avoid CircularProgressIndicator deadlock). FOG-04..07 retained at `Falsified-in-production` — Plan 03.1-06 walk on iPhone 17 Pro is the empirical gate. 138 GREEN / 1 SKIPPED / 0 RED full suite; analyze + format clean.
- [Phase 03.1-fix-fog-pan-translation]: Plan 03.1-04: SHADER-MODULO-WRAP fix shipped. Slot 3..4 uniform renamed end-to-end uOffset → uPixelOrigin (4 commits: 20e86c8 Task 1 rename, fd745b1 Task 2 FOG-11 test, 34b7ce5 Task 3 docs). Fragment shader applies fract(uPixelOrigin / uResolution) per-fragment — modulo wrap moved CPU → GPU; the Dart call site forwards camera.pixelOrigin verbatim with no . New FOG-11 behavioural smooth-noise-coordinate-evolution test (kPocFogSmoothCoordinateMaxDelta = 1e3 raw pixels + magnitude > 100 defence-in-depth) catches any future regression mechanically. JSONL field names (uOffsetXMin/Median/Max + appliedUOffset param) preserved for back-compat with 03.1-03 walk's session log extraction tooling. FOG-04..07 retained at Falsified-in-production — necessary-but-not-sufficient: Plan 03.1-05 (CANVAS-FRAME-ALIGNMENT) must also land before Plan 03.1-06 walk re-validates. 4 deviations (1 Rule 3 — FragmentShader is base/unmockable, replaced setAll-mock with RecordingFogShaderRenderer-equivalent verbatim-write test; 1 Rule 3 — ShaderSanityScreen also called setAll(offset:), rename propagated; 2 Rule 1 — unused import + uOffsetX docstring substring tripping zero-grep). 133 tests + 1 skipped GREEN; analyze 0; format clean.
- [Phase 03.1-fix-fog-pan-translation]: Plan 03.1-06: **VERDICT ITERATING-WITH-PARTIAL-PROGRESS** per CONTEXT §Iteration policy (third Phase 3.1 verdict; no hard cap on walk count). Walk #2 sideload session 2026-05-02 against CI run 25262881962 (SHA 9a87010) over central Melun, 296 seconds, 266 frame_delta + 110 SDF INFO + 267 fog_transform rollups. Developer's verbatim composite: *"Mirk is still flashing/regenerating during pan/zoom like if the seed was changing... rotating 90° very fast / translated very fast repetitively. To investigate"* + *"Mirk shader is somehow rotating and not covering the whole map when zooming in. Mirk shader is the one getting offseted now"* + *"sanity page is ok"* + *"the revealed area is staying where it should"* + *"med 1.3 ms / p95 1.4 ms / max 1.7 ms"*. **CONFIRMED-BY-WALK-2:** PERF-07 re-validated (med 1.495 / p95 6.340 / max 12.416 ms; 3.9-5.4× headroom; consistent with Walk #1's 4-7× — Plan 03.1-04 + 03.1-05 changes did not regress frame budget); FOG-12 reveal-hole alignment (Plan 03.1-05's `-(matrix[12], matrix[13])` clip-path shift works as designed); UX-01 back button (Plan 03.1-05's Icons.arrow_back). **FALSIFIED-BY-WALK-2:** FOG-11 (SHADER-MODULO-WRAP CI test went GREEN — fog_smooth_noise_test.dart asserts consecutive-paint pixelOrigin delta < 1e3 raw px — but user-observable shimmer persists with refined characterisation; **KEY LEARNING: CI-test-vs-walk-symptom mismatch** — the test caught the proximate failure mode Plan 03.1-04 was DESIGNED to catch, missing the deeper symptom space; Plan 03.1-07 must design CI tests aimed at user-observable noise-pattern visual stability, not just raw-uniform-delta-magnitude); PERF-08 (SDF rebuild rate empirically 1-121/sec median 68/sec — statistically indistinguishable from Walk #1's 12-115/sec; the 1e-4 lat/lon quantisation grid does not match the in-production gesture pattern's drift magnitude — unit test GREEN was an insufficient proxy). **NEW REGRESSION (P03.1-05 introduced):** fog-rect viewport-coverage — Plan 03.1-05 shifted the **clip path** by `-canvasOffset` but the **`canvas.drawRect(Offset.zero & size, fogPaint)` rect-cover** is ALSO subject to the canvas transform; when MobileLayerTransformer applies non-zero canvas translation (243/267 rollups; worst sustained `(canvasTx, canvasTy) = (+757.35, +319.46)` for ~50 sec — 20-25× larger than Walk #1's worst case), the rect paints in canvas-translated space leaving the OPPOSITE-edge of the viewport uncovered. Top-right + bottom-right diagonal strips of un-fogged street map visible at high zoom. Two new requirements opened: **FOG-13** (fog-rect symmetric compensation: Option (b) `canvas.translate(-canvasOffset)` at top of paint() OR Option (c) shift drawRect bounds by `-canvasOffset` symmetrically with clip path OR Option (d) over-draw rect by max(\|canvasTx\|, \|canvasTy\|)) + **FOG-14** (higher-fidelity user-observable noise-pattern stability CI test — three candidate root causes for SHADER-MODULO-WRAP refined: B-1 fp32 precision degradation in fract(uPixelOrigin / uResolution) at high zoom where ratio reaches 1e4-1e5; B-2 uResolution uniform incorrectly scaled; B-3 noise-tile-period mismatch causing fract() wraparound frequency to align with perceived "rotation"). FOG-04..07 stay at Falsified-in-production. PERF-07 re-measured. Phase 4 + Phase 5 stay BLOCKED. MirkFall migration recommendation **DEFERRED**. **Closure decision: iterate-with-partial-progress; spawn Plan 03.1-07 via `/gsd:plan-phase 3.1 --gaps` consuming `03.1-FALSIFICATION-2.md` sub-section D hypothesis triage table + `03.1-UAT-2.md` gaps YAML (FOG-13, FOG-14, PERF-08-WIDEN) as input.** Walk #1 records (`03.1-FALSIFICATION.md` + `03.1-UAT.md`) and Phase 3 record (`03-FALSIFICATION.md`) UNTOUCHED — historical integrity preserved across iteration cycles. Evidence: `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-2.md` + `03.1-UAT-2.md`.
- [Phase 03.1-fix-fog-pan-translation]: Plan 03.1-08: FOG-13 closure via Option (b) canvas.translate(-canvasOffset.dx, -canvasOffset.dy) at top of _FogPainter.paint() (after clipPath, before drawRect). Defense-in-depth retains the Plan 03.1-05 computeFogClipPath(canvasOffset:) shift; the two compensations compose correctly because clipPath geometry is established BEFORE the translate. Single-snapshot at the matrix level preserved (canvas.getTransform() called exactly once per paint; same Float64List flows to clipPath shift AND new canvas.translate). 3 atomic commits in ~9 min; 146 GREEN / 1 SKIPPED / 0 RED. FOG-13 status flips Pending → Complete; FOG-04..07 retain Falsified-in-production until Plan 03.1-09 walk validates the layered fix bundle.
- [Phase 03.1-fix-fog-pan-translation]: Plan 03.1-09: **VERDICT ITERATING-WITH-PARTIAL-PROGRESS-AND-ROTATION-REGRESSION** per CONTEXT §Iteration policy (4th Phase 3.1 verdict; no hard cap on walk count). Walk #3 sideload session 2026-05-03 against CI run 25275979318 (SHA 0625ba5) over central Melun on iPhone 17 Pro. **Walk #3 limitation:** developer did NOT Mail-share the session log; quantitative JSONL grep correlation unavailable for frame_delta, SDF, fog_transform streams — verbal evidence + mechanistic codebase interpretation only. **Findings:** (1) **Q1 PARTIAL — B-3 partial-fix outcome confirms Plan 03.1-07 SUMMARY framing.** Developer's verbatim: *"for the steppy 'translation' it still happens but diffrently, now when panning it is smooth, but every x amount of pan you get a hard step mobing the shader."* Plan 03.1-07 Branch B-3 reduced wrap frequency from viewport-width (~390 px) to noise-tile period (~16-65 px); panning smooth between wrap events but stepping discontinuity persists at wrap events. Wrap MAGNITUDE in cell-grid space stays same (`cellPx = spiralCoord * uResolution` is multiplied by full uResolution, not by the wrap divisor — Plan 03.1-07 Task 3 Rule 1 deviation), only wrap FREQUENCY changes. Developer's intent-correct iteration-path framing: *"if I pan right forever the shader should not be moved to be where I'm going, I should see a new area of the shader"* — exactly the world-coordinate-noise rewrite Plan 03.1-07 SUMMARY documented for Plan 03.1-10. (2) **Q2 NEW failure mode — rotation-correlated fog mis-coverage.** Developer's verbatim: *"so the shader going outside of the screen happens when you zoom and rotate (which always happend when zooming with human finger) I think we should disable rotation for now, only pan and zoom, north is up. simplier"*. Plan 03.1-08's `canvas.translate(-canvasOffset)` compensates ONLY translation (matrix[12], matrix[13]); rotation component (matrix[0,1,4,5]) is untouched. When MobileLayerTransformer applies non-zero rotation during pinch-zoom-rotate, the fog rect rotates with the canvas leaving wedges of un-fogged map at viewport corners. Walk #2 didn't trigger (likely because gesture pattern kept rotation near zero or fog-rect viewport-coverage gap masked it). Developer's preferred mitigation: disable map rotation (UX-02 NEW scope reduction). **Q3 + Q4 implicitly retained** (no developer complaints surfaced about reveal-hole or fog-static-during-pan). **Three new requirements:** **FOG-16** (rotation handling — disable per developer's choice OR full canvas-inverse-transform; Plan 03.1-10), **FOG-17** (world-coordinate noise sampling — replace `fract(uPixelOrigin / *)` with `worldPx = (fragUv * uResolution) + uPixelOrigin; noiseUv = worldPx / kNoiseTilePx`; pair with FOG-17a precision mitigation — CPU-side integer/fractional decomposition; Plan 03.1-10), **UX-02** (disable map rotation for POC; Plan 03.1-10). **Status updates:** FOG-13 demoted from Complete to Verified-by-test-only (Walk #3 surfaced rotation regression — pure-translation case still passes). FOG-15 demoted to Verified-by-test-only (partial-fix framing confirmed by walk; FOG-17 is iteration path). PERF-07 + PERF-08 retained at Walk #2 levels (no Walk #3 log Mail-shared). DEBUG-01 walk-time-validated implicitly. UX-01 walk-time-validated implicitly. FOG-04..07 + FOG-11 retain Falsified-in-production. **KEY LEARNING — PROGRESS WITHOUT CLOSURE:** each layered fix bundle moved the failure mode further down the symptom hierarchy (constant-zero → viewport-width-period shimmer → noise-tile-period stepping → rotation-correlated rect-cover) — measurable progress, no closure yet. Closure requires either Plan 03.1-10's world-coordinate-noise rewrite (D-1 + D-1a) so wraps don't happen at all + UX-02 rotation disable, OR Phase 3.2 architectural-counter-evidence pivot if D-1 also fails empirically. Phase 3.1 stays `In Progress (4th iteration)`; Phase 4 + Phase 5 STAY BLOCKED. MirkFall migration recommendation **DEFERRED**. **Closure decision: iterate-with-partial-progress-and-rotation-regression; spawn Plan 03.1-10 via `/gsd:plan-phase 3.1 --gaps`** consuming `03.1-FALSIFICATION-3.md` sub-section D hypothesis triage table + `03.1-UAT-3.md` `gaps:` YAML (FOG-16, FOG-17, FOG-17a, UX-02) as input. Walk #1 + Walk #2 + Phase 3 historical records UNTOUCHED. Evidence: `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-3.md` + `03.1-UAT-3.md`.
- [Phase 03.1-fix-fog-pan-translation]: Plan 03.1-07 Task 3 (Branch B-3 — noise-tile-period mismatch): selected per developer's verbatim iPhone 17 Pro Walk #2 PRODUCTION-conditions observation under the digit-spiral toggle on /map: *"the shader is being translated in x and y when I pan or zoom, it's clear with the numbered shader, with the mirk it seems like a 'seed' change because the mirk is animated. the translation isn't smooth, it's 'stepped', you pan a little bit it works fine, then there is a translation. while zooming it's being translated a lot."* Applied B-3 fix: `fract(uPixelOrigin / uResolution)` → `fract(uPixelOrigin / tilePeriodPixels)` where `tilePeriodPixels = uResolution / max(uScaleFar, uScaleMid, uScaleNear)`. Symmetric in production (`atmospheric_fog.frag`) and debug-spiral (`atmospheric_fog_debug_spiral.frag`) shaders. Derived in-shader; no new uniform; `totalFloatSlots = 41` preserved. **B-3 is a documented partial-fix:** wraps still happen but at noise-tile period (~16-65 px) instead of viewport-width (~390 px); within a single octave the wraps are sub-perceptible. If Walk #3 surfaces residual high-zoom stepping (where fp32 precision ALSO degrades — separate failure mode B-1), Plan 03.1-10 may need a world-coordinate-noise rewrite. FOG-14 closes via test pair: `fog_debug_spiral_continuity_test.dart` (3 sub-tests on a 60s @ 120Hz / 5 px/paint smooth-pan trajectory: pre-fix wrap count ~92, post-fix ~969, multiplier ratio = maxScale = 10.5) + `fog_tile_period_invariant_test.dart` (3 static-source assertions: production has tilePeriodPixels = uResolution/maxScale; pre-fix formulation absent from active code; debug shader mirrors B-3; totalFloatSlots = 41). FOG-15 closes via the structural fix landing GREEN. **Deviation [Rule 1 — Bug] FOG-14a invariant reframed:** plan body proposed cell-index delta ≤ 1 per paint, but every fract-wrap event causes a viewport-width jump in `cellPx = spiralCoord * uResolution` regardless of wrap period — B-3 changes wrap FREQUENCY, not per-event MAGNITUDE in cell-grid space. Reframed FOG-14a to wrap-frequency invariant (testable, distinguishes pre/post-fix mechanically). FOG-04..07 + FOG-11 retain Falsified-in-production. 5 atomic commits in ~16 min; full suite 158 GREEN / 1 SKIPPED / 0 RED.
- [Phase 03.1-fix-fog-pan-translation]: Plan 03.1-10: world-coordinate noise sampling (worldPx = fragUv * uResolution + uPixelOrigin; noiseUv = worldPx / kNoiseTilePx) + CPU-side integer/fractional decomposition (kPocFogIntegerWrapPeriodPx = 1536, integer multiple of kPocFogNoiseTilePx = 384) + UX-02 disable map rotation. kNoiseTilePx = 384.0 chosen for visual character continuity with pre-fix B-3 (on-screen cell ~36.6 raw px ~ ~37 raw px parity).
- [Phase 03.1-fix-fog-pan-translation]: 2026-05-04 MIRK-01 captured as DEFERRED architectural insight; Plan 03.1-11 scope = Walk #4 ONLY. Developer clarified in CLAUDE.md (`# MIRL solution`) that the MIRL fog/visual layer will compose multiple different shaders in the real MirkFall application; non-periodic shaders supported; explicit camera↔shader coupling natural. Conceptually MIRK-01 = standardized ABI between `_FogPainter` and any conforming shader. **However, formalization is DEFERRED** per developer's clarification: *"the architecture is malleable, I don't really care about the architecture for now, it can be made better later, once we find how to solve our issue, for now we don't even know how to make it work so defining an architecture seems too early"*. Empirical validation (Walk #4) comes first; architectural formalization picks up post-Walk-4 if/when the bundle confirms in production. **Decisions:** (1) MIRK-01 added to REQUIREMENTS.md `### Architecture` as deferred captured insight (NOT a Plan 03.1-11 deliverable); (2) Plan 03.1-10 implementation (commits `a276505` + `2b32513` + `dbc3e21`) retained as-is — no shader rollback, no rename, no formalization; (3) FOG-17 + FOG-17a + UX-02 all retain Complete; (4) Plan 03.1-11 scope locked at original 2026-05-03 framing = iPhone 17 Pro Walk #4 sideload UAT + verdict authoring (Tasks 1-3 of the existing 491-line PLAN.md); (5) MIRK-01 formal acceptance criteria revisited post-Walk-4 verdict if useful. **Two framing-iteration mistakes logged for transparency:** initial "shader receives no spatial input + map-locking via pure Canvas transforms" was over-strict (topologically impossible for non-periodic animated map-locked shaders); subsequent "Plan 03.1-11 = Tasks A+B+C+D MIRK-01 formalization" was premature optimization. Final scope: minimal Walk #4 only.
- [Phase 03.1-fix-fog-pan-translation]: 2026-05-04 `/gsd:discuss-phase 3.1` pass — 4 gray-area decisions captured in 03.1-CONTEXT.md `<decisions>` block: (1) **Walk #4 timing** = ALL tasks (pre-walk gates + sideload + verdict) in Plan 03.1-11 (single-plan, immediate verdict — matches existing 491-line PLAN.md structure); (2) **FOG-12 + FOG-13 Canvas-side compensation audit** = DEFERRED to post-Walk-4 (discuss-phase first answer was "auditer puis décider" but superseded by developer's "architecture is malleable; don't care for now" clarification — Walk #4's empirical outcome IS the answer; if fog covers viewport correctly, FOG-12/13 working; if surfaces gap, follow-up Plan 03.1-12 audit); (3) **MIRK-01 acceptance test trio** (static + behavioural + invariant) = DEFERRED with MIRK-01 itself; (4) **ABI uniform rename `uPixelOrigin` → `uWorldOffset`** = DEFERRED with MIRK-01. Net effect: planner re-run should produce a Plan 03.1-11 structurally identical to existing 2026-05-03 framing (Walk #4 mechanics only); discuss-phase decisions are captured for documentation/traceability without altering the plan body. — Plan-checker (gsd-plan-checker) ran 2026-05-04 against existing PLAN.md and returned VERIFICATION PASSED on all 8 dimensions; honest assessment surfaced that decisions #2/#3/#4 weren't literally referenced in the plan body, hence this STATE.md + CONTEXT.md propagation pass.
- [Phase 03.1-fix-fog-pan-translation]: Plan 03.1-11 EXECUTED 2026-05-04 — **VERDICT ITERATING-WITH-MAJOR-PROGRESS** per CONTEXT §Iteration policy (5th Phase 3.1 verdict; Walk #1 iterating-with-major-progress; Walk #2 iterating-with-partial-progress; Walk #3 iterating-with-partial-progress-and-rotation-regression; Walk #4 iterating-with-major-progress). Walk #4 sideload UAT iPhone 17 Pro against CI run `25305105125` (SHA `b31766f`) over central Melun. Mail-share discipline RE-ESTABLISHED (`WALK-3-NO-LOG-MAIL-SHARE` blocker resolved); session log `20260504T105024Z_logs.txt` (703 KB) extracted into 4 JSONL streams. **Findings:** (1) **Q1 99% RESOLVED at default zoom** — FOG-17 world-coordinate noise + FOG-17a Dart-side decomposition working as designed; zero dev_markers fired in default-zoom rollups 0-5 (vs Walk #3b's 8 markers at the same regime); developer's verbatim *"hard step non existent at default zoom level"*. (2) **Q1 PARTIAL at max zoom only** — 2 dev_markers fired at pxOriginX ~4.26M (Walk #2's precision-degradation regime); developer's verbatim *"once zoomed at maximum (closest to ground) hard step exists while panning, I pressed the bug button after each hard step to mark them in the log"*. Mechanism candidates: M1 FBM-rotated-octave continuity caveat / M2 sub-pixel jitter in FOG-17a modulo / M3 mod() semantics at GLSL-fp32 boundary. (3) **Q1b NEW failure mode surfaced** — zoom-gesture-correlated very-fast hard-step ("seed of procedural pattern fast"); mechanism: world-coordinate noise sampling gives continuity in pure-pan space but NOT in zoom-gesture space (uPixelOrigin jumps O(1M) raw px between zoom-snap boundaries; rollups 20-24 confirmed sweep 4.10M → 0.84M → 0.81M in ~5 sec). (4) **Q2 RESOLVED** — UX-02 walk-time-validated; canvasTx/Ty identically `0.000000` across all 40 fog_transform rollups; developer's verbatim *"rotation disable as wanted"*. (5) **Q3 + Q4 retained** — *"fog translate perfectly with map during pan (except for the hard steps)"*. (6) **Out-of-scope developer request captured** for Plan 03.1-12+: disable MapOptions.cameraConstraint to stress-test integer-wrap behaviour at extreme world coordinates (*"we should disable the bounding box that block us from going further to ensure that hard step do not reappear 100 km away"*). **PERF-07 re-measured:** medianMs 0.243 / p95Ms 5.020 / maxMs 9.514 across 40 rollups / 4761 samples; ~66× / ~6.4× / ~5.0× headroom (consistent with Walks #1 + #2 + Walk #3b). **FOG-17a signature CONFIRMED:** `uOffsetXMax/uOffsetYMax` bounded in [85.14, 1535.74] / [41.64, 1534.63] across ALL 40 rollups, even at pixelOriginX 4.26M (fp32 ULP at 1537 ≈ 2.4e-4 raw px vs ULP at 4.26M ≈ 0.51 raw px — three orders of magnitude better precision). **Status updates:** UX-02 + FOG-16 + FOG-17 flipped from `Complete — Verified-by-test` to `Complete — Verified-by-test + walk-time validated (P03.1-11 Walk #4)`. PERF-07 re-measured with ~66× headroom. FOG-04..07 + FOG-11 + FOG-15 retain `Falsified-in-production` with revision-clauses pointing at `03.1-FALSIFICATION-4.md`. **Three Plan 03.1-12+ candidate axes captured:** (1) Q1 max-zoom-only residual (M1/M2/M3 mechanism + C1/C2/C3 fix candidates); (2) Q1b zoom-gesture-correlated NEW failure (C-Q1b-1/2/3 fix candidates); (3) bounding-box-removal diagnostic. **THIS plan does NOT author Plan 03.1-12** per scope (per Plan 03.1-11 must_haves §11). Phase 3.1 stays `In Progress (5th iteration; Walk #5 pending in Plan 03.1-12+ follow-up plan-phase invocation)`. Phase 4 + Phase 5 STAY BLOCKED. MirkFall migration recommendation **DEFERRED until Walk #5**. Walks #1 + #2 + #3 + Walk #3b + Phase 3 historical records UNTOUCHED. Evidence: `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-4.md` + `03.1-UAT-4.md`. Next: `/gsd:plan-phase 3.1 --gaps` in follow-up plan-phase invocation.
- [Phase 03.1]: Plan 03.1-12: C1' eliminate FOG-17a integer-wrap-modulo (FOG-18) — Walk #4 debug-spiral positive control falsified FOG-17a's premise (noise function NOT truly periodic on kPocFogNoiseTilePx=384 in practice; the wrap event itself was the bug, not the precision penalty). _FogPainter.paint() now forwards camera.pixelOrigin directly (decomposed into intPx+fracPx for documentation continuity but with the modulo removed); kPocFogIntegerWrapPeriodPx constant deleted; kPocFogSmoothCoordinateMaxDelta updated to literal 2000.0. fp32 24-bit mantissa supports exact-integer values up to 16.7M raw-px, well above Walk #4's max observed 4.26M. ZERO shader edits (MIRL-compatible).
- [Phase 03.1]: Plan 03.1-12: C3' remove MapOptions.cameraConstraint stress-test diagnostic (DEBUG-02) — developer's verbatim Walk #4 request 'we should disable the bounding box that block us from going further to ensure that hard step do not reappear 100 km away'. Lets developer pan to extreme world coordinates (~100 km from Melun) during Walk #5 to verify FOG-18 doesn't introduce new precision-induced artefacts at high pixelOrigin magnitudes. Pure scope-extension diagnostic (not a fix axis). flutter_map 7.0.2 default CameraConstraint.unconstrained() applies; kPocBboxLat*/Lon* constants retained as stress-test-disabled (annotated docstrings); UX-02 InteractionOptions retained; minZoom/maxZoom retained. Made MapOptions(...) const-eligible (Rule 1 deviation - bug fix for prefer_const_constructors).
- [Phase 03.1]: Plan 03.1-12: C2' / FOG-19 zoom-invariant world-coordinate basis DEFERRED to follow-up plan after fresh /gsd:discuss-phase 3.1 round resolves the dimensional-mismatch design question (refZoom-px basis vs screen-px uResolution mixing in shader 'worldPx = fragUv * uResolution + uPixelOrigin' formula). Resolution requires choice between (a) change uResolution semantics in both shaders (MIRL violation), (b) introduce uZoomScale uniform (relaxes MIRL), (c) Dart-side interpolation across zoom-snap boundaries (originally rejected as C-Q1b-2; worth re-examining), (d) something else surfaced by discuss-phase. C2' deferral pattern — when a fix axis surfaces an architectural tension that needs design conversation, defer to a follow-up plan after fresh discuss-phase rather than ship a half-working fix. Walk #5 captures Q1b observation as input for the future C2' follow-up plan but Q1b residual does NOT block Walk #5 verdict from the new intermediate CONFIRMED-AFTER-FIX-PARTIAL outcome.

### Roadmap Evolution

- Phase 03.1 inserted after Phase 3: Fix Fog Pan-Translation (URGENT)

### Pending Todos

[From .planning/todos/pending/ — ideas captured during sessions]

None yet.

### Blockers/Concerns

[Issues that affect future work]

- ~~Phase 2 vector-tile FPS on iOS at zoom 13–15 has no published numbers; the Phase 2 walk IS the research, and is the highest-probability project-blocking risk.~~ **RESOLVED 2026-05-01 by Plan 02-06 verbal `approved`:** sustained ~120 fps on iPhone 17 Pro during pan / pinch / combined gestures at zoom 13–15 — 3× headroom over the ≥ 40 fps gate. Phase 3 fog hypothesis unblocked with massive frame-budget margin.
- AUTH-04 cross-restart auto-resume routing bug (deferred per Plan 01-07 SUMMARY + deferred-items.md). Not blocking Phase 1 closure (POC scope; revoking GPS perms during a GPS-needed POC is artificial). May resurface if a downstream phase exercises the cross-restart re-grant flow.
- ~~**PHASE-3.1-CAMERA-TRANSFORM (PARTIALLY-RESOLVED 2026-05-02 by Plan 03.1-03 walk; STILL OPEN — STILL A BLOCKER for Phase 4 + 5):** Plan 03.1-02 3-line uOffset fix DOES land in the IPA — fog_transform JSONL Finding 2 confirms uOffset reaches the shader and tracks pan trajectory. Plan 03-08 constant-zero failure mode (NEVER tracking) is structurally addressed and the new FOG-09 behavioural transform-equality test catches it mechanically in CI without a sideload walk. BUT the post-fix walk surfaced three new failure modes the diagnostic FOG-10 stream resolves to two specific Plan 03.1-04 fix axes: SHADER-MODULO-WRAP (per-paint modulo-1.0 wrap producing visible noise-seam shimmer, "seed changing" obs 2; intermittent partial tracking obs 3) and CANVAS-FRAME-ALIGNMENT (reveal-hole offset from blue dot during gesture caused by non-zero canvasTx/Ty translation that RESEARCH §Pitfall D claimed wouldn't occur, obs 4). Plan 03.1-04 follow-up required before Phase 3.1 closes. Phase 4 (wisp particles) and Phase 5 (decision gate) STAY BLOCKED. See `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION.md` for the diagnostic correlation + the Plan 03.1-04 fix-axis triage table.~~ **EVOLVED 2026-05-02 by Plan 03.1-06 Walk #2 — see PHASE-3.1-CAMERA-TRANSFORM-WALK2 below.** Original Walk #1 fix-axis hypothesis (CANVAS-FRAME-ALIGNMENT obs 4) RESOLVED for the reveal-hole case (FOG-12 confirmed by Walk #2); SHADER-MODULO-WRAP fix-axis still OPEN and refined.
- ~~**PHASE-3.1-CAMERA-TRANSFORM-WALK2 (NEW 2026-05-02 from P03.1-06 Walk #2; STILL A BLOCKER for Phase 4 + 5):** Plan 03.1-04 + 03.1-05 layered fix landed 3 of 4 fix-axes confirmed-by-walk; 1 fix-axis (FOG-11 SHADER-MODULO-WRAP) landed in CI but did NOT close the user-observable shimmer (KEY LEARNING: CI-test-vs-walk-symptom mismatch); 1 NEW regression introduced by Plan 03.1-05 (fog-rect viewport-coverage — clip-path-only compensation does not reach the `canvas.drawRect` rect-cover paint when canvas-translation reaches +757/+319 magnitudes). Plan 03.1-07 follow-up required: (a) refined SHADER-MODULO-WRAP per `03.1-FALSIFICATION-2.md` sub-section D triage (B-1 fp32 precision; B-2 uResolution scaling; B-3 noise-tile-period mismatch); (b) FOG-13 fog-rect symmetric compensation; (c) PERF-08 quantisation widen OR pivot to disc-set-only cache key; (d) FOG-14 higher-fidelity noise-pattern stability CI gate. Phase 4 + Phase 5 STAY BLOCKED until Plan 03.1-07 (or whichever subsequent Phase 3.1 plan) delivers `CONFIRMED-AFTER-FIX` or `DENIED-FINAL`. Per CONTEXT §Iteration policy: no hard cap on walk count.~~ **EVOLVED 2026-05-03 by Plan 03.1-09 Walk #3** — see PHASE-3.1-CAMERA-TRANSFORM-WALK3 below. Walk #2's SHADER-MODULO-WRAP fix-axis received Plan 03.1-07 Branch B-3 partial fix (confirmed by Walk #3 to degrade stepping but not eliminate); Walk #2's fog-rect viewport-coverage regression received Plan 03.1-08 fix (confirmed by Walk #3 for pure-translation case but rotation-correlated regression NEW).
- ~~**PHASE-3.1-WALK-4-CLOSURE (2026-05-04; ACTIVE BLOCKER for Phase 4 + 5):** Plan 03.1-10 software-fix bundle (`a276505` UX-02 + `2b32513` FOG-17a + `dbc3e21` FOG-17) landed at HEAD `b31766f`. Plan 03.1-11 = iPhone 17 Pro Walk #4 sideload UAT against this bundle + verdict authoring (`03.1-FALSIFICATION-4.md` + `03.1-UAT-4.md`).~~ **EVOLVED 2026-05-04 by Plan 03.1-11 Walk #4 verdict ITERATING-WITH-MAJOR-PROGRESS — see PHASE-3.1-WALK-5-PENDING below.** Walk #4 delivered Q1 99% resolved at default zoom + Q2 fully resolved + Q3/Q4 retained + PERF-07 re-measured + FOG-17a signature confirmed; residual Q1 max-zoom + NEW Q1b zoom-gesture failure mode = next iteration axes.
- **PHASE-3.1-WALK-5-PENDING (NEW 2026-05-04 from P03.1-11 Walk #4; ACTIVE BLOCKER for Phase 4 + 5):** Plan 03.1-11 Walk #4 verdict `ITERATING-WITH-MAJOR-PROGRESS` (5th Phase 3.1 verdict). Three Plan 03.1-12+ candidate axes: (1) **Q1 max-zoom-only residual** — 2 dev_markers fired at pxOriginX ~4.26M (Walk #2's precision-degradation regime); mechanism candidates M1 FBM-rotated-octave continuity caveat at integer-wrap boundary / M2 sub-pixel jitter in FOG-17a modulo at extreme zoom / M3 mod() semantics at GLSL-fp32 boundary; fix candidates C1 periodic Worley/explicit-period Perlin / C2 widen kPocFogIntegerWrapPeriodPx / C3 per-octave precision-aware decomposition. (2) **Q1b NEW zoom-gesture-correlated failure mode** — "seed of procedural pattern fast" during zoom in/out; mechanism: world-coordinate noise sampling gives noise-value continuity in pure-pan space but NOT in zoom-gesture space (uPixelOrigin jumps O(1M) raw px between zoom-snap boundaries); fix candidates C-Q1b-1 seed-stable lat-lon noise (project pixelOrigin → lat/lon, sample noise in lat/lon space) / C-Q1b-2 interpolate pixelOrigin across zoom-snap on the Dart side / C-Q1b-3 accept as POC-acceptable. (3) **Bounding-box-removal diagnostic** — developer-requested out-of-scope tool; disable MapOptions.cameraConstraint to stress-test integer-wrap behaviour at extreme world coordinates (~100 km from origin). Plan 03.1-12 to be authored in follow-up `/gsd:plan-phase 3.1 --gaps` invocation consuming Walk #4's diagnostic stream + `03.1-FALSIFICATION-4.md` Plan 03.1-12+ candidate axes section as input. Phase 4 + Phase 5 STAY BLOCKED until Walk #5 verdict. Per CONTEXT §Iteration policy no hard cap on walk count (currently 5 walks including Walk #3b sub-walk).
- ~~**PHASE-3.1-CAMERA-TRANSFORM-WALK3 (NEW 2026-05-03 from P03.1-09 Walk #3; STILL A BLOCKER for Phase 4 + 5):** Plan 03.1-07 Branch B-3 partial fix confirmed by Walk #3: stepping degraded from viewport-width to noise-tile period, panning smooth between wrap events but stepping persists at wrap events (developer's verbatim *"now when panning it is smooth, but every x amount of pan you get a hard step"*). Plan 03.1-07 SUMMARY explicitly anticipated this outcome — Wrap MAGNITUDE in cell-grid space stays same, only FREQUENCY changes. Plan 03.1-08 FOG-13 fix confirmed for pure-translation case but Walk #3 surfaced NEW rotation-correlated failure mode: `canvas.translate(-canvasOffset)` compensates ONLY translation (matrix[12], matrix[13]); rotation component (matrix[0,1,4,5]) is untouched, so MobileLayerTransformer rotation during pinch-zoom-rotate causes fog rect to rotate with canvas leaving wedges of un-fogged map at viewport corners. Plan 03.1-10 follow-up required: (a) **FOG-17** world-coordinate noise sampling (D-1 primary fix axis: `worldPx = (fragUv * uResolution) + uPixelOrigin; noiseUv = worldPx / kNoiseTilePx`); (b) **FOG-17a** precision mitigation pairing (CPU-side integer/fractional decomposition of pixelOrigin so shader input stays bounded at high zoom); (c) **FOG-16** rotation handling — DEVELOPER-ENDORSED path (a) disable rotation via `InteractionOptions(flags: InteractiveFlag.all & ~InteractiveFlag.rotate)` paired with NEW **UX-02** requirement; alternative path (b) full canvas-inverse-transform deferred to hypothetical post-POC. Walk #3 limitation: no log Mail-shared this walk; verbal evidence only — future walks should re-establish post-walk Mail-share discipline. Phase 4 + Phase 5 STAY BLOCKED until Plan 03.1-10 (or subsequent Phase 3.1 plan) delivers `CONFIRMED-AFTER-FIX` or `DENIED-FINAL`. Per CONTEXT §Iteration policy: no hard cap on walk count (currently 4 walks).~~ **SUPERSEDED 2026-05-04 by PHASE-3.1-MIRK-01-ABI-FORMALIZATION above** — the Plan 03.1-10 fix bundle (FOG-17 + FOG-17a + UX-02) landed at HEAD `b31766f` is architecturally correct under the new MIRK-01 ABI framing (no rollback). The "stepping at wrap events" + "rotation-correlated mis-coverage" symptom space is empirically resolved by the Plan 03.1-10 bundle (FOG-17 world-coord noise eliminates wraps; UX-02 disables rotation eliminating rotation-correlated case); Walk #4 is the empirical validation gate.
- ~~**SANITY-SCREEN-NO-BACK-BUTTON (NEW 2026-05-02 from P03.1-03 walk; LOW PRIORITY):** ShaderSanityScreen lacks a back button — developer had to force-close the app to return during the P03.1-03 walk. Single-line fix (`IconButton(icon: Icons.arrow_back, onPressed: () => context.pop())` in the AppBar leading slot). Captured for triage in 03.1-UAT.md `gaps:` section under id `SANITY-NO-BACK-BUTTON`. Not blocking Phase 3.1 closure; addressable in any downstream plan touching the sanity screen.~~ **RESOLVED 2026-05-02 by Plan 03.1-05 UX-01 fix; walk-time validated 2026-05-02 by Plan 03.1-06 Walk #2** (developer's *"sanity page is ok"*); **walk-3 implicit retention** (no force-close complaint surfaced).
- ~~**CI-TEST-VS-WALK-SYMPTOM-MISMATCH (NEW 2026-05-02 — META-LEARNING for Plan 03.1-07 + future falsification CI test design):** The FOG-11 CI test (`fog_smooth_noise_test.dart`) was designed to catch Walk #1 Finding 3's per-paint modulo-1.0-wrap discontinuity — and went GREEN at Walk #2's diagnostic-stream level (the modulo wrap moved from CPU to GPU per Plan 03.1-04; consecutive-paint pixelOrigin delta < 1e3 raw px holds). BUT the user-observable shimmer persists with refined characterisation ("rotating 90° very fast / translated fast repetitively") — the CI test caught the **proximate** failure mode without catching the **observable** one. Lesson: CI tests built around 1-Hz-rollup-derived metrics or raw-uniform-delta-magnitudes can be insufficient proxies for the user-observable visual symptom space. Plan 03.1-07's CI tests (FOG-14) must include higher-fidelity proxies — e.g. visual diff against reference frame, per-pixel noise-pattern stability across consecutive paints at high zoom, OR a precision-budget assertion (`uPixelOrigin / uResolution < 1024` to keep fp32 fract precision sane). Document this lesson permanently in the next phase's RESEARCH.md so future falsification-test design avoids the same trap.~~ **RESOLVED 2026-05-03 by Plan 03.1-07 Task 3 + Walk #3** — FOG-14 test pair (FOG-14a wrap-period regression gate + FOG-14b static-source engineering invariant) addresses the higher-fidelity proxy requirement: FOG-14a directly asserts the wrap-period divisor change (mechanically-verifiable engineering property aligned with what B-3 actually delivers, not 1-Hz-rollup-derived metric). Walk #3 confirms the meta-learning was correct — FOG-14 design avoided the CI-test-vs-walk-symptom mismatch trap; the failure mode surfaced at Walk #3 (residual stepping) is a genuine partial-fix outcome anticipated by Plan 03.1-07 SUMMARY, not a CI-blind-spot.
- ~~**WALK-3-NO-LOG-MAIL-SHARE (NEW 2026-05-03 — META-LEARNING for future Phase 3.1 walks):** Walk #3 evidence is verbal-only — developer did not Mail-share the session log post-walk. Quantitative JSONL grep correlation unavailable for frame_delta (PERF-07 numerical update), SDF (PERF-08 status), fog_transform (rotation-correlated diagnostic). Future walks should re-establish the post-walk Mail-share discipline (Plan 03.1-03 + 03.1-06 walks did Mail-share; Walk #3 broke the pattern). If Plan 03.1-10 delivers a verdict requiring quantitative confirmation (CONFIRMED-AFTER-FIX vs ITERATING vs DENIED-FINAL), the Mail-share is the gate to enabling diagnostic-stream pattern-matching. Document in Plan 03.1-10's pre-walk gate sequence as an explicit step.~~ **RESOLVED 2026-05-04 by Plan 03.1-11 Walk #4** — Mail-share discipline re-established; session log `20260504T105024Z_logs.txt` (703 KB) Mail-shared post-walk; 4 JSONL streams (frame_delta + fog_transform + SDF + dev_markers) extracted via grep into `.uat-tmp/walk4_*.jsonl`; quantitative diagnostic correlation drove the Walk #4 verdict (FOG-17a signature confirmation; marker-correlation analysis at max zoom; PERF-07 re-measurement).

## Session Continuity

Last session: 2026-05-04T12:58:09.602Z
Stopped at: Plan 03.1-12 EXECUTED 2026-05-04
Resume file: None
