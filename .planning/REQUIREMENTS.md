# Requirements: MirkFall Same-Canvas POC

**Defined:** 2026-04-30
**Core Value:** The fog-of-war stays perfectly locked to the map during pan, zoom, and combined gestures on a sideloaded iOS build.

Requirements are user-centric, testable, atomic. The POC's only question is the architectural hypothesis (single Canvas → no fog/map lag) — every requirement either proves that hypothesis or makes the iOS UAT walks investigable from a Windows dev box.

## v1 Requirements

### Bootstrap

- [x] **BOOT-01**: Flutter project initialised with SDK pin `3.41.7` and Dart `3.11.x`; `pubspec.yaml` strictly version-pinned (no `^`); `pubspec.lock` committed
- [x] **BOOT-02**: Every `.dart` file in `lib/` and `test/` starts with the GOSL v1.0 copyright header (`// Copyright (c) 2026 THONGVAN Alexis` / `// Licensed under the Good Old Software License v1.0` / `// See LICENSE file for details`)
- [x] **BOOT-03**: `LICENSE` file at repo root contains the GOSL v1.0 text
- [x] **BOOT-04**: `analysis_options.yaml` enforces `strict-casts: true`, `strict-inference: true`, `strict-raw-types: true`; uses `flutter_lints 6.0.0`
- [x] **BOOT-05**: `dart format --line-length 160 --set-exit-if-changed .` passes
- [x] **BOOT-06**: `flutter analyze` passes with no warnings
- [x] **BOOT-07**: `Fra_Melun.pmtile` (4 MB MVT vector) bundled as a Flutter asset under `assets/maps/`
- [x] **BOOT-08**: Battle-tested files ported verbatim from MirkFall: `atmospheric_fog.frag`, `revealed_sdf_builder.dart`, `reveal_disc.dart`, `mirk_viewport_bbox.dart`, `tile_cell_iteration.dart`, `mirk_projection.dart`, `fog_shader_uniforms.dart`, `animation_helpers.dart`, plus relevant `kMirkFog*` / `kMetersPerDegreeLat` / `kEarthRadiusMeters` constants

### Dependency Audit

- [x] **AUDIT-01**: `DEPENDENCIES.md` at repo root lists every direct dependency with: name, pinned version, license, telemetry audit (auto network egress: yes/no, what it does), transitive license summary, maintenance signal (last release, contributors), platform compatibility (iOS + Android), audit date
- [x] **AUDIT-02**: CI fails the build if any direct or transitive dependency carries a non-acceptable license (allow-list: MIT, BSD-2/3, Apache 2.0, ISC, zlib, CC0, Unlicense)
- [x] **AUDIT-03**: Zero packages perform automatic network egress on app launch (no analytics, no crash reporting, no attribution SDKs, no remote config, no update checks)

### CI

- [x] **CI-01**: GitHub Actions workflow on every push to `main` runs three jobs: lint (ubuntu-latest), build-android (ubuntu-latest), build-ios (macos-latest)
- [x] **CI-02**: Lint job runs `flutter analyze`, `dart format --line-length 160 --set-exit-if-changed`, `flutter test`
- [x] **CI-03**: Build-android job produces a debug APK downloadable as a workflow artifact
- [x] **CI-04**: Build-ios job produces an unsigned IPA (sideloadable via SideStore) downloadable as a workflow artifact
- [x] **CI-05**: Both APK and IPA artifacts are visible from the GitHub Actions run page on every push

### Permissions

- [x] **AUTH-01**: On launch, the app shows a permission rationale screen explaining why `locationWhenInUse` is required for the fog-of-war
- [x] **AUTH-02**: When the user accepts the rationale, the app requests `Permission.locationWhenInUse` via `permission_handler`
- [x] **AUTH-03**: On grant, the app navigates to the map screen via `context.go('/map')` (full pile reset — there's nowhere to go back to)
- [x] **AUTH-04**: On deny, the app shows a denied screen with a button that opens system settings via `permission_handler.openAppSettings()` _(software-complete per Plan 01-06; Plan 01-07 sideload UAT confirmed deny → /denied → Open Settings → iOS Settings page works correctly. Cross-restart auto-resume routing bug — toggle Location ON in iOS Settings + return to app should auto-nav from /denied to /map but stays on /denied — deferred per `.planning/phases/01-foundation/deferred-items.md` and reference pattern in `docs/flutter-ios-specifics.md` §5.6. Not blocking POC closure.)_
- [x] **AUTH-05**: `ios/Runner/Info.plist` contains `NSLocationWhenInUseUsageDescription` with a non-empty rationale string; no `NSLocationAlwaysAndWhenInUseUsageDescription` (out of scope)
- [x] **AUTH-06**: `ios/Runner/Info.plist` contains `ITSAppUsesNonExemptEncryption=false`

### Map

- [x] **MAP-01**: On first launch (after permission grant), `Fra_Melun.pmtile` is copied from `rootBundle` to `<getApplicationSupportDirectory()>/maps/Fra_Melun.pmtile` exactly once; subsequent launches detect the existing file and skip the copy
- [x] **MAP-02**: The map screen renders the bundled PMTiles via `flutter_map 7.0.2` + `vector_map_tiles 8.0.0` + `vector_map_tiles_pmtiles 1.5.0`, with the renderer's default style
- [x] **MAP-03**: Initial camera centred on Melun (lat `48.5397`, lon `2.6553`, zoom `13`)
- [x] **MAP-04**: User can pan the map with one-finger drag
- [x] **MAP-05**: User can zoom the map with pinch gesture
- [x] **MAP-06**: User can perform combined pan+zoom (pinch + drag simultaneously) and the map responds smoothly

### Location & Recenter

- [x] **LOC-01**: After permission grant, the app subscribes to `Geolocator.getPositionStream` with a sensible accuracy/distance filter
- [x] **LOC-02**: A blue dot (radius 7 px, fill `#2b7cd6`, white stroke 2 px) is rendered at the user's current position on the map and updates on each GPS fix
- [x] **LOC-03**: The most-recent GPS fix is cached in memory as `_lastFix`; the app does NOT call `Geolocator.getLastKnownPosition()` (unreliable on iOS — known plugin issue)
- [x] **LOC-04**: A floating action button on the map screen animates the camera to `_lastFix` at zoom 15
- [x] **LOC-05**: When `_lastFix` is null (no fix yet received), the recenter button is disabled (or shows a spinner)

### Fog of War

- [x] **FOG-01**: On each GPS fix, a `RevealDisc(lat, lon, 25 m)` is added to an in-memory disc list (no database) _(Plan 03-01 Wave 0 stub; Plan 03-02 RevealDiscRepository implementation; Plan 03-07 wires MapScreen.\_subscribeToPositions → discRepository.append on every fix with hand-rolled `rvd_<microsSinceEpoch>_<randomU32>_<counter>` ID per RESEARCH §Open Question 4. Verified-by-test: 5 GREEN unit tests pin RevealDiscRepository + distanceMetres surfaces.)_
- [x] **FOG-02**: A 256×256 R-channel midpoint-128 SDF (`ui.Image`) is built from the disc list via `RevealedSdfBuilder.buildFromDiscs`, with distance computed in **metres**, not pixels (so circles stay circular at all latitudes) _(Plan 03-01 Wave 0: distanceMetres stub landed; behaviour ships in Plan 03-02. Verified-by-test: degree-distance regression test asserts `distanceMetres((48.5,2.6), (48.5,3.6)) ≈ 73.7 km` not ~111 km.)_
- [x] **FOG-03**: The SDF is rebuilt when the disc list changes; the rebuild runs on the UI isolate (acceptable for `< 100` discs at `< 16 ms`); a debug log records each rebuild's duration _(Plan 03-01 Wave 0: SdfCache + SdfRebuildLogger stubs landed; behaviour ships in Plan 03-03. Verified-by-test: SdfCache hit/miss + SdfRebuildLogger 1-Hz JSONL rollup + stop()-flush-pending-samples tests all GREEN.)_
- [ ] **FOG-04**: A `FogLayer` widget is registered as a `flutter_map` custom layer that paints into the same Canvas as the tile layer _(Plan 03-01 Wave 0 stub; Plan 03-05 paint behaviour; Plan 03-07 wires FogLayer into MapScreen as a child of FlutterMap between VectorTileLayer and the blue-dot CircleLayer. **Plan 03-08 walk DENIED 2026-05-01** — structural FOG-04 test passes (`find.descendant(of: FogLayer, matching: MobileLayerTransformer)` returns one match), but production walk reveals fog does NOT translate with the map during pan (only rotation propagates). Same-Canvas hypothesis falsified as-implemented; widget-tree containment did not imply Canvas-transform sharing. Phase 3.1 gap-closure required before any port-back to MirkFall. See 03-FALSIFICATION.md. **Walk #4 update (P03.1-11 2026-05-04):** stays Falsified-in-production with major progress — Q1 99% resolved at default zoom + Q2 fully resolved by UX-02 + Q3/Q4 retained; residual Q1 PARTIAL at max zoom + Q1b NEW zoom-gesture failure mode deferred to Plan 03.1-12+. See `03.1-FALSIFICATION-4.md`.)_
- [ ] **FOG-05**: Inside `FogLayer.paint()`, the 41 float uniforms + 1 sampler of `atmospheric_fog.frag` are populated; identity sdfRect (`0, 0, 1, 1`) is passed because the SDF and the viewport share the same coordinate space _(Plan 03-01 Wave 0: slot-count gate test pinned at 41; population behaviour ships in Plan 03-05. Structural tests pass (FogShaderUniforms.totalFloatSlots == 41 + setAll populates the uniform list); production walk denied 2026-05-01 — the uniforms populate correctly but the painter's Canvas does not consume the tile-layer's translation transform. See 03-FALSIFICATION.md. **Walk #4 update (P03.1-11 2026-05-04):** stays Falsified-in-production with major progress; uOffsetXMax/YMax bounded under 1537 raw px across all 40 rollups confirms FOG-17a precision pairing in effect. See `03.1-FALSIFICATION-4.md`.)_
- [ ] **FOG-06**: The clip path (world rect minus disc circles, in screen coordinates) is computed and applied via `canvas.clipPath`; the shader is then drawn via `canvas.drawRect(viewport, Paint()..shader = fogShader)` _(Plan 03-01 Wave 0: computeFogClipPath stub landed; geometry ships in Plan 03-05. Structural tests pass (computeFogClipPath returns world-rect-minus-disc-circles geometry); production walk denied 2026-05-01 — the clip path is computed in the painter's local Canvas coordinate space which is NOT translated with the camera during pan, producing the static-fog-during-pan failure mode. See 03-FALSIFICATION.md. **Walk #4 update (P03.1-11 2026-05-04):** stays Falsified-in-production with major progress; canvasTx/Ty identically zero across all 40 rollups confirms UX-02 + rotation-disabled = MobileLayerTransformer accumulates no canvas-translation matrix elements. See `03.1-FALSIFICATION-4.md`.)_
- [ ] **FOG-07**: All inputs to the per-frame fog draw — SDF rect, clip path, viewport size, shader uniforms — derive from the **same `MapCamera` snapshot**, captured atomically at the start of paint (prevents BUG-014's combined-zoom-pan failure mode from re-emerging in the new pipeline) _(Plan 03-01 Wave 0: keystone test skeleton landed; single-snapshot enforcement ships in Plan 03-05 with KEYSTONE GREEN test asserting readCount==1 initial / +1 per forced rebuild over 3 rebuilds. Structural single-snapshot enforcement passes; **production walk denied 2026-05-01** — the snapshot is taken correctly per paint() invocation, but the painter is plausibly not invoked between pan-driven repaint cycles (Phase 3.1 diagnostic possibility #3) OR the snapshot is consumed in screen-space without applying the camera's translation transform (possibilities #1/#2). See 03-FALSIFICATION.md three diagnostic possibilities. **Walk #4 update (P03.1-11 2026-05-04):** stays Falsified-in-production with major progress; pixelOriginX/Y track gesture monotonically across both default-zoom (1.064M) and max-zoom (4.26M) regimes. See `03.1-FALSIFICATION-4.md`.)_
- [x] **FOG-08**: A frame-delta self-debug probe records, per frame: timestamp of the latest map camera update, timestamp of the fog uniform population, the delta between them; rolling median, p95, and max are exposed via the logger and an on-screen overlay _(Plan 03-01 Wave 0 stubs; Plan 03-04 ring buffer + 1-Hz JSONL rollup + dual-clock discipline; Plan 03-06 overlay rendering — 3-line colour-coded HUD subscribed to probe.rollups; Plan 03-07 wires FrameDeltaProbeOverlay into MapScreen Stack at top:104 right:8 + owns probe.start/dispose lifecycle in initState/dispose. Verified-by-test: 7 GREEN probe tests + 5 GREEN overlay/sanity tests + 12 GREEN MapScreen integration tests; quantitative walk evidence not captured per Plan 03-08 walk-aborted-on-visual-grounds (PERF-04 unmeasured) but the probe code itself is software-complete and shipping in the IPA.)_
- [x] **FOG-09**: A behavioural transform-equality regression test asserts `_FogPainter.paint()` calls `shaderRenderer.render(offset:)` with a different `offset` argument after `MapController.move(...)` than before, within `kPocCanvasTransformEpsilon`; the test is a CI-gating widget test that catches the Plan 03-08 static-fog-during-pan failure mode without a sideload walk. Existing Plan 03-05 FOG-04 structural test is augmented (docstring forward-pointer), not replaced. _(Phase 3.1 — Plan 03.1-02 GREEN)_ — Verified-by-test (P03.1-02 — `test/presentation/widgets/fog_pan_translation_test.dart`). **Scope clarification (P03.1-03 walk):** FOG-09 is a binary "moved at all?" gate — it asserts `offset` differs after `mapController.move()`. It catches the Plan 03-08 constant-zero failure mode mechanically, but does NOT catch the per-paint modulo-wrap shimmer (obs 2 + 3) or the Canvas-frame-misalignment reveal-hole offset (obs 4) that the post-fix walk surfaced. Plan 03.1-04 will add FOG-11 (smooth-noise-coordinate-evolution) and FOG-12 (canvas-frame-corrected reveal-vs-bluedot equality) as the higher-fidelity behavioural gates.
- [x] **FOG-10**: `FogTransformLogger` is a new sibling to `FrameDeltaProbe` + `SdfRebuildLogger`. Per-paint observation captures `(canvas.getTransform()[12..13], camera.pixelOrigin, camera.center, appliedUOffset)`; emits 1-Hz JSONL rollups via `Logger('infrastructure.mirk.fog_transform')` aligned to wall-clock seconds for grep-correlation; idle-second skip; FIFO drop on overflow at `kPocFogTransformBufferMaxSamples`; synchronous flush on `stop()`. Permanent in production code (debug-level always-on, NOT `--dart-define` gated, per CONTEXT decision). _(Phase 3.1 — Plan 03.1-01)_ _Complete — Verified-by-test (P03.1-01 — `fog_transform_logger_test.dart`)_
- [ ] **FOG-11**: A behavioural smooth-noise-coordinate-evolution regression test asserts the painter's `pixelOrigin` argument to `shaderRenderer.render(...)` evolves smoothly across consecutive `MapController.move(...)` calls — no consecutive-paint delta exceeding `kPocFogSmoothCoordinateMaxDelta` (1e3 raw pixels). Plus a defence-in-depth magnitude assertion that the captured value is in raw-pixel units (zoom 13 magnitude ~1e6), not normalised UV [0, 1). Catches the SHADER-MODULO-WRAP failure mode from `03.1-FALSIFICATION.md` observation 2 (developer's "seed of the mirk was changing"). _(Phase 3.1 — Plan 03.1-04)_ **Falsified-by-walk-2 (P03.1-06 walk 2026-05-02)** — CI test went GREEN (consecutive-paint pixelOrigin delta < 1e3 raw px is mechanically asserted) but user-observable shimmer persists with refined characterisation: *"the mirk pattern is switched to an other one, then an other one, then an other one, very fast (like changing the seed of a procedural pattern fast)... maybe it's just being rotated 90° very fast, or maybe it's translated very fast repetitively"*. The CI test was an insufficient proxy for the user-observable noise-pattern visual stability. Plan 03.1-07 will refine the CI test (FOG-14) and address the deeper root cause (fp32 precision degradation at high zoom OR uResolution logical-vs-device-px scaling OR noise-tile-period-vs-viewport-period mismatch — see `03.1-FALSIFICATION-2.md` sub-section D triage table). **Walk #4 update (P03.1-11 2026-05-04):** stays Falsified-in-production with revised symptom characterisation — at default zoom the user-observable shimmer is RESOLVED (Q1 99% closed; FOG-17 world-coord noise + FOG-17a precision pairing working as designed); at maximum zoom 2 dev_markers fired indicating residual stepping (different mechanism — FBM-rotated-octave continuity caveat at integer-wrap boundary); plus NEW Q1b zoom-gesture failure mode surfaced. See `03.1-FALSIFICATION-4.md`.
- [x] **FOG-12**: A behavioural canvas-frame-alignment regression test asserts `_FogPainter.paint()` reads `canvas.getTransform()` and pre-shifts the clip-path holes by `-(matrix[12], matrix[13])` so the SDF reveal frame stays co-located with sibling layers' UNTRANSLATED frames. Catches the CANVAS-FRAME-ALIGNMENT failure mode from `03.1-FALSIFICATION.md` observation 4 (developer's "revealed area is being offsetted from the blue dot during pan/zoom"). Test mounts a real FogLayer + injects a non-identity Canvas transform via `_RecordingMockCanvas` and asserts the clip-path bounds shift by exactly `-(canvasTx, canvasTy)`. Single-snapshot at the matrix level enforced via `getTransformCallCount == 1` per paint (mirrors FOG-07). _(Phase 3.1 — Plan 03.1-05)_ _Complete — Verified-by-test + walk-2 (P03.1-05 widget test + P03.1-06 walk-time confirmation; developer's *"the revealed area is staying where it should"* + screenshot showing blue dot at reveal-hole centre)_
- [x] **FOG-13**: A behavioural fog-rect-viewport-coverage regression test asserts `_FogPainter.paint()` shifts BOTH the clip path AND the `canvas.drawRect(rect, fogPaint)` rect-cover bounds symmetrically by `-(matrix[12], matrix[13])`, OR equivalently calls `canvas.translate(-canvasTx, -canvasTy)` at the top of `paint()` so all drawing ops occur in the world-aligned frame, OR over-draws the rect by `max(|canvasTx|, |canvasTy|)`. Catches the fog-rect viewport-coverage failure mode introduced by Plan 03.1-05's clip-path-only compensation: the `canvas.drawRect(Offset.zero & size, fogPaint)` paints in canvas-translated space leaving viewport-edge strips of un-fogged map visible at high zoom. Surfaced by `03.1-FALSIFICATION-2.md` Walk #2 sub-section C (developer's *"Mirk shader is somehow rotating and not covering the whole map when zooming in... mirk made worse (mirk offset itself leaving part of the map revealed at the edges)"*; canvas-translation magnitudes up to (+757, +319) for ~50 seconds in 243/267 rollups). _(Phase 3.1 — Plan 03.1-08)_ _Verified-by-test-only — Walk #3 surfaced rotation-correlated regression (NEW row FOG-16); pure-translation case still passes._ Implementation: Option (b) — `canvas.translate(-canvasOffset.dx, -canvasOffset.dy)` at top of `_FogPainter.paint()` (after `clipPath`, before the rect-cover `drawRect`). Defense-in-depth retains the Plan 03.1-05 `computeFogClipPath(canvasOffset: ...)` shift. FOG-12 stays Complete (clip-path geometry is established BEFORE the translate; the two compensations compose correctly because `clipPath` is not affected by subsequent `canvas.translate` calls). **Walk #3 finding (P03.1-09):** the compensation works correctly when canvas rotation is zero, but `canvasOffset = Offset(matrix[12], matrix[13])` only captures the translation component of the canvas transform; the rotation component (matrix[0,1,4,5]) is not compensated. When MobileLayerTransformer applies non-zero rotation during pinch-zoom-rotate, the fog rect rotates with the canvas, leaving wedges of un-fogged map at viewport corners. FOG-16 (NEW) opens to address this rotation-correlated case.
- [x] **FOG-14**: A higher-fidelity user-observable noise-pattern stability regression test that catches the symptom space FOG-11 missed. Test pair landed in two parts: FOG-14a (wrap-period regression gate — three sub-tests asserting the post-fix `fract` divisor produces ~10× more wraps than pre-fix on a smooth-pan trajectory; the wrap-frequency multiplier equals `maxScale` by B-3 design) + FOG-14b (static-source engineering invariant — production shader contains `tilePeriodPixels = uResolution / maxScale` AND `noiseUv = fragUv + fract(uPixelOrigin / tilePeriodPixels)`; pre-fix `fract(uPixelOrigin / uResolution)` does NOT appear in active code; debug-spiral mirrors the formulation; `totalFloatSlots == 41` preserved). Catches the persisting Walk #2 symptom that FOG-11's raw-uniform-delta-magnitude assertion missed. _(Phase 3.1 — Plan 03.1-07)_ _Complete — Verified-by-test (P03.1-07 Task 3 Branch B-3 — `fog_debug_spiral_continuity_test.dart` 3/3 GREEN + `fog_tile_period_invariant_test.dart` 3/3 GREEN)_
- [x] **FOG-15**: Mechanism-confirmed shader-coordinate-system fix landed in `assets/shaders/atmospheric_fog.frag` (and mirrored in `atmospheric_fog_debug_spiral.frag` so Walk #3 spiral observation reflects the post-fix coordinate system). **Mechanism: Branch B-3 (noise-tile-period mismatch).** Fix: `fract(uPixelOrigin / uResolution)` replaced by `fract(uPixelOrigin / tilePeriodPixels)` where `tilePeriodPixels = uResolution / max(uScaleFar, uScaleMid, uScaleNear)`. Derived in-shader from existing runtime uniform slots 20..22; no new uniform — `FogShaderUniforms.totalFloatSlots = 41` preserved. Documented partial-fix: wrap period moves from ~390 px (viewport-width) to ~16-65 px (tile-period). Wraps still happen but at sub-perceptible magnitudes within a single octave. _(Phase 3.1 — Plan 03.1-07 Task 3 Branch B-3)_ _Verified-by-test-only — Walk #3 (P03.1-09) confirms partial-fix framing per Plan 03.1-07 SUMMARY: developer's verbatim *"for the steppy 'translation' it still happens but diffrently, now when panning it is smooth, but every x amount of pan you get a hard step"* — B-3 reduced wrap frequency but didn't eliminate stepping. World-coordinate noise rewrite (FOG-17 NEW) is the iteration path Plan 03.1-10 takes. **Walk #4 update (P03.1-11 2026-05-04):** B-3 has been superseded by the FOG-17 world-coordinate formulation (Plan 03.1-10) which is now the active code path; Walk #4 confirms that FOG-17 + FOG-17a delivers Q1 99% closure at default zoom (the symptom B-3 was a partial fix for). FOG-15 stays Verified-by-test-only since the mechanism-confirmed fix it represents is now historical (the active formulation is FOG-17, not B-3). See `03.1-FALSIFICATION-4.md`._
- [x] **FOG-16**: Fog-rect viewport coverage during canvas rotation (matrix[0,1,4,5] non-identity). Surfaced by Walk #3 (Plan 03.1-09): Plan 03.1-08's `canvas.translate(-canvasOffset)` compensates ONLY the translation component of the canvas transform; the rotation component is untouched. When MobileLayerTransformer applies non-zero rotation during pinch-zoom-rotate (which always happens with human-finger pinch-zoom on iOS), the fog rect rotates with the canvas leaving wedges of un-fogged map at viewport corners. Acceptance options: **(a) DEVELOPER-ENDORSED** — disable map rotation via `InteractionOptions(flags: InteractiveFlag.all & ~InteractiveFlag.rotate)`; defers canvas-rotation-aware compensation until post-POC (paired with NEW UX-02 requirement). **(b) full canvas-inverse-transform** — `canvas.transform(matrix.inverted())` at top of `_FogPainter.paint()`; more invasive, risk of degenerate transforms, defer to hypothetical post-POC iteration. _(Phase 3.1 — Plan 03.1-10)_ _Complete — Verified-by-test + walk-time validated via UX-02 path (a) (P03.1-10 widget test + P03.1-11 Walk #4 walk-time validation 2026-05-04 — `canvasTxMin/Median/Max + canvasTyMin/Median/Max identically 0.000000 across ALL 40 fog_transform rollups`; developer's verbatim *"rotation disable as wanted"*; rotation-correlated mis-coverage failure mode structurally prevented). FOG-16 path (b) full canvas-inverse-transform deferred to hypothetical post-POC iteration._
- [x] **FOG-17**: World-coordinate noise sampling (eliminates fract-wrap stepping at all zoom levels). Replace `noiseUv = fragUv + fract(uPixelOrigin / tilePeriodPixels)` (Plan 03.1-07 Branch B-3 partial fix) with `worldPx = (fragUv * uResolution) + uPixelOrigin; noiseUv = worldPx / kNoiseTilePx`. Each fragment samples noise at its actual world position; as camera pans, NEW world coordinates enter the viewport, NEW noise appears at the edge — no `fract()`, no wraps, no stepping. Pair with FOG-17a (precision mitigation — CPU-side integer/fractional decomposition of `uPixelOrigin` so shader input stays bounded at high zoom; fp32 mantissa precision otherwise insufficient at zoom 16+ where pixelOrigin reaches 4.26M magnitudes). Acceptance: shader source contains `worldPx = (fragUv * uResolution) + uPixelOrigin` (or equivalent precision-aware formulation); pre-fix `fract(uPixelOrigin / *)` formulation excluded from active code; CI gate test on synthetic smooth-pan trajectory asserts no consecutive-paint per-pixel noise discontinuities. **Re-classified 2026-05-04 as MIRK-01 ABI implementation** — the slot 3..4 `uPixelOrigin` uniform is the standardized camera-derived input of the MIRK-01 ABI; FOG-17 is the production-fog-shader's implementation of that ABI; FOG-17a is the precision-aware decomposition the architecture (`_FogPainter`) computes before forwarding through the ABI. **NOT** a forbidden shader↔camera coupling — it IS the explicit, standardized coupling that MIRK-01 formalizes (per CLAUDE.md `# MIRL solution`: "il y a donc naturellement un couplage camera/shader"). _(Phase 3.1 — Plan 03.1-10)_ _Complete — Verified-by-test + walk-time validated AT DEFAULT ZOOM (P03.1-10 unit tests + P03.1-11 Walk #4 walk-time validation 2026-05-04). World-coordinate formulation `worldPx = fragUv * uResolution + uPixelOrigin; noiseUv = worldPx / kNoiseTilePx` lands in BOTH `atmospheric_fog.frag` AND `atmospheric_fog_debug_spiral.frag` (both implement the MIRK-01 ABI uniformly). FOG-17a precision pairing lands in `_FogPainter.paint()`. **Walk #4 evidence:** `uOffsetXMax/uOffsetYMax` values bounded in [85.14, 1535.74] / [41.64, 1534.63] across ALL 40 rollups (FOG-17a signature confirmed in effect at every paint, even at pixelOriginX 4.26M); developer's verbatim *"hard step non existent at default zoom level"* + *"fog translate perfectly with map during pan (except for the hard steps)"* + zero dev_markers fired in default-zoom rollups 0-5. **High-zoom Q1 residual** (2 dev_markers fired at pxOriginX ~4.26M) **+ Q1b NEW zoom-gesture failure mode** deferred to Plan 03.1-12+. FOG-04..07 + FOG-11 + FOG-15 retain `Falsified-in-production` with revision-clause pointing at `03.1-FALSIFICATION-4.md`._ **P03.1-12 (Plan 03.1-12 Task 1):** FOG-17a integer-wrap premise FALSIFIED by Walk #4 debug-spiral positive control; replaced by FOG-18 (modulo eliminated). FOG-17 world-coordinate noise sampling formulation in shaders UNCHANGED — still in effect.
- [x] **FOG-18**: Eliminate FOG-17a integer-wrap-modulo. Forward `camera.pixelOrigin` directly to the shader's slot 3..4 `uPixelOrigin` uniform (decomposed into `intPx + fracPx` for documentation continuity but with the modulo removed). Walk #4 debug-spiral positive control (zero steppiness during MAX-zoom pan with the digit-atlas cell pattern that IS truly periodic on 384) proved the noise function isn't actually periodic on `kPocFogNoiseTilePx`, so the wrap event at every `kPocFogIntegerWrapPeriodPx (=1536)` raw-px is itself the bug — not the precision penalty FOG-17a was designed to address. fp32 has 24 bits of exact-integer mantissa = 16.7M raw-px ceiling, well above Walk #4's max observed pixelOrigin magnitude of ~4.26M. Acceptance: Dart `_FogPainter.paint()` does NOT contain `% kPocFogIntegerWrapPeriodPx` (and does not reference the deleted `kPocFogIntegerWrapPeriodPx` constant at all); `appliedPixelOrigin` magnitude tracks `camera.pixelOrigin` magnitude monotonically (no wrap-induced discontinuities); fp32 precision verified sufficient up to Walk #4-observed pixelOrigin magnitudes (~4.26M). MIRL-compatible: ZERO shader source modifications; both `atmospheric_fog.frag` AND `atmospheric_fog_debug_spiral.frag` inherit the fix automatically via the unchanged slot 3..4 `uPixelOrigin` ABI. _(Phase 3.1 — Plan 03.1-12 Task 1)_ _Complete — Verified-by-test (P03.1-12 Task 1 — rewritten `fog_pixel_origin_decomposition_test.dart` as FOG-18 acceptance gate; updated `fog_smooth_noise_test.dart` + `fog_pan_translation_test.dart` reason-strings; updated `fog_world_coordinate_noise_test.dart` + `fog_debug_spiral_continuity_test.dart` to remove `kPocFogIntegerWrapPeriodPx` references — pending Walk #5 walk-time validation)._
- [x] **FOG-19**: Anchor fog noise samples to lat/lng via new `uZoomScale` uniform (slot 41). Walk #5 surfaced Q1b zoom-gesture residual ("numbers sliding / incorrect scaling" during deliberate zoom in/out transitions). Root cause: shader's `worldPx = fragUv * uResolution + uPixelOrigin` formula combines screen-px-relative basis with world-px-relative basis; `camera.pixelOrigin` jumps O(1M) raw px between zoom-snap boundaries, so the noise pattern's spatial frequency relative to map-features changes across zooms. Fix: forward `uZoomScale = pow(2, camera.zoom - kPocFogReferenceZoom)` (`kPocFogReferenceZoom = 13.0`) to BOTH `atmospheric_fog.frag` AND `atmospheric_fog_debug_spiral.frag`; shader divides `worldPx` by `(kNoiseTilePx * uZoomScale)` (production) or `cellPx` by `uZoomScale` (debug-spiral). Result: noise samples anchor to lat/lng — cells stay PUT during zoom (visible cell size grows with zoom-in, shrinks with zoom-out, the natural "zooming into a fixed-resolution texture" behavior). MIRL-compatible per CLAUDE.md `# MIRL solution` updated 2026-05-04: ABI uniform addition (consumed consistently by all conforming shaders); coordinate-plumbing modification anchors noise to world; visual identity preserved (at uZoomScale == 1.0 at reference zoom, sampling is bit-identical to pre-fix; at other zooms, the shader's character at any settled (zoom, position) post-fix matches pre-fix at SOME equivalent (zoom, position)). Acceptance: `FogShaderUniforms.totalFloatSlots == 42`; both shaders declare `uniform float uZoomScale`; both shaders divide noise sampling by uZoomScale; `_FogPainter.paint()` computes `math.pow(2, camera.zoom - kPocFogReferenceZoom).toDouble()` and forwards via `shaderRenderer.render(zoomScale: ...)`. _(Phase 3.1 — Plan 03.1-14 Task B)_ _Complete — Verified-by-test (P03.1-14 Task B — new `fog_zoom_invariant_basis_test.dart` + updated `fog_shader_uniforms_test.dart` slot-count gate + `fog_debug_spiral_continuity_test.dart` static-source assertions + updated `fog_tile_period_invariant_test.dart` post-FOG-19 formulation) — pending Walk #6 walk-time validation_

### Diagnostics

- [ ] **DEBUG-01**: A debug-spiral fragment shader (`assets/shaders/atmospheric_fog_debug_spiral.frag`) renders human-readable cell-index digits via a digit-atlas sampler in the same `uPixelOrigin / uResolution` coordinate system as production fog. Toggled on at `/sanity` via a runtime `Switch.adaptive` (default OFF; production fog rendering UNCHANGED). Pre-rasterized digit atlas built at runtime via `ui.PictureRecorder` + `TextPainter` — no font package or PNG asset dependency. Permanent diagnostic infrastructure (kept in codebase for future regression investigations — POC OR MirkFall after port-back). _(Phase 3.1 — Plan 03.1-07 Task 1)_ _Complete — Verified-by-test (P03.1-07 Task 1 — `digit_atlas_builder_test.dart` + augmented `shader_sanity_screen_test.dart` debug-spiral-toggle group)_
- [x] **DEBUG-02**: `MapOptions.cameraConstraint` is removed from `lib/presentation/screens/map_screen.dart` to enable the developer's Walk #4 stress-test request: *"we should disable the bounding box that block us from going further to ensure that hard step do not reappear 100 km away"*. Lets the developer pan to extreme world coordinates (~100 km from Melun) during Walk #5 to verify FOG-18 (Plan 03.1-12 Task 1 wrap elimination) doesn't introduce new precision-induced artefacts at high pixelOrigin magnitudes. Pure scope-extension diagnostic; not a fix axis. The flutter_map 7.0.2 default `CameraConstraint.unconstrained()` applies. The `kPocBboxLat*` + `kPocBboxLon*` + `kPocPanBoundsPadDegrees` constants in `lib/config/constants.dart` are RETAINED but currently unreferenced (annotated with DEBUG-02 stress-test-disabled docstrings — easy to re-enable by re-adding the `cameraConstraint: CameraConstraint.contain(...)` parameter to `MapOptions`). Acceptance: `MapOptions` does NOT include `cameraConstraint: CameraConstraint.contain(...)`; behavioural widget test asserts `flutterMap.options.cameraConstraint` is `UnconstrainedCamera`; static-source assertion verifies `CameraConstraint.contain` is absent from `lib/presentation/screens/map_screen.dart`. Re-enabling a sensible bbox constraint is a Phase 5 hardening concern. _(Phase 3.1 — Plan 03.1-12 Task 2)_ _Complete — Verified-by-test (P03.1-12 Task 2 — new DEBUG-02 group in `test/presentation/screens/map_screen_test.dart` with runtime + static-source assertions) — pending Walk #5 walk-time validation_
- [x] **DEBUG-03**: Replace repetitive 0..99 cell-cycling in `assets/shaders/atmospheric_fog_debug_spiral.frag` with unique 4-digit per-cell encoding for Walk #6 quantitative drift measurement during deliberate zoom transitions. Per developer's Walk #5 verbatim request: *"modifying the number to not have repetitive value would allow us to debug the amount of drift"*. Encoding: `cellId = clamp((cell.y + 50) * 100 + (cell.x + 50), 0, 9999)` rendered as 4 horizontal sub-cells (thousands / hundreds / tens / ones) sampled from the existing 10×10 digit atlas (atlas image unchanged; only the shader's per-cell sampling logic now reads 4 digit slots per cell instead of 2). Covers ±50 cells from world-origin = 10000 unique cells = ~8000 raw px in each axis at 80-px cell size — more than any Melun-anchored Walk #6 session reaches. Acceptance: shader source contains `(cell.y + 50) * 100 + (cell.x + 50)` AND does NOT contain `mod(float(rawCellIndex), 100.0)` (the previous repetitive formula is removed); behavioural debug-spiral continuity test updated. Pure debug-shader change — no production fog code touched. MIRL-compatible per the visual-identity-preservation rule (debug shader has full edit freedom; CLAUDE.md `# MIRL solution` updated 2026-05-04). _(Phase 3.1 — Plan 03.1-14 Task A)_ _Complete — Verified-by-test (P03.1-14 Task A — updated `fog_debug_spiral_continuity_test.dart` static-source assertions) — pending Walk #6 walk-time validation_

### Architecture

- [ ] **MIRK-01**: Shader-agnostic fog architecture via standardized ABI — **DEFERRED captured architectural insight**, not a Plan 03.1-11 deliverable. The MIRL fog/visual layer composes multiple different shader implementations (production atmospheric fog, debug-spiral, future drop-ins). Per-shader spatial-input plumbing in the painter is not maintainable across N shaders. **MIRK-01 conceptually = fixed contract (ABI) between `_FogPainter` and any conforming shader**: the painter computes ALL camera-derived inputs (camera.pixelOrigin decomposition, viewport size, time) and forwards them through standard uniforms; any shader implementing the ABI plugs in without painter-side changes. **Camera↔shader coupling is explicit and accepted** (per CLAUDE.md `# MIRL solution`: *"il y a donc naturellement un couplage camera/shader"*); the contract supports **non-periodic shaders** (per CLAUDE.md: *"on doit fonctionner avec des shader non periodique, n'importe quel shader"*). **Existing implementation seed** = Plan 03.1-10 (commits `2b32513` FOG-17a CPU integer/fractional decomposition + `dbc3e21` FOG-17 world-coord noise sampling) is architecturally aligned with MIRK-01 — `uPixelOrigin` slot 3..4 IS the standardized camera-derived ABI uniform; FOG-17a CPU-side decomposition IS the architectural precision computation. **Formal MIRK-01 acceptance is DEFERRED** because per the developer's 2026-05-04 clarification: *"the architecture is malleable, I don't really care about the architecture for now, it can be made better later, once we find how to solve our issue, for now we don't even know how to make it work so defining an architecture seems too early"*. The priority is empirical validation (Walk #4 sideload UAT against the Plan 03.1-10 fix bundle); architectural formalization can be picked up post-Walk-4 if/when the bundle confirms in production. _(Future / deferred — revisit post-Walk-4 verdict)_

### Wisp Particles

- [ ] **WISP-01**: `WispParticleSystem` is ported from MirkFall with particle positions refactored from `Offset` (screen pixels) to `LatLng` (world coordinates); positions are projected to screen at paint time using the SAME `MapCamera.of(context)` snapshot the fog uses (FOG-07 carry-over from Phase 3.1: a single snapshot per `FogLayer.build()` lifecycle — wisps must share THE snapshot, not just A snapshot — to avoid frame-mismatched projections that the parent project's BUG-014 exhibited)
- [ ] **WISP-02**: Wisps are spawned along disc perimeters as new discs appear; max 200 wisps active simultaneously; 8 m spacing; 2.5 s life; peak alpha 0.35. **Open questions for `/gsd:discuss-phase 4`** (do NOT pre-resolve in planning): kinematic units — speed (originally drafted as `18 px/s`), birth radius (`6 px`), death radius (`22 px`) — are these in screen-pixels (zoom-dependent visible size; verbatim-port-from-MirkFall = BUG-014 signature), world-pixels-at-`kPocFogReferenceZoom = 13.0` (zoom-invariant world-anchor), or meters (true ground-distance)? Drift direction (radial outward from spawn point? all outward from disc centre? tangential?) and acceleration profile (linear / eased / instantaneous-then-decelerate)? Phase 3.1 C2' / FOG-19 dimensional-mismatch trap is the cautionary precedent.
- [ ] **WISP-03**: A 5 s warm-up phase suppresses wisp spawning on app open
- [ ] **WISP-04**: Wisps render in the same Canvas as the fog and the tile layer (same paint pass, same `MapCamera` snapshot). **Open questions for `/gsd:discuss-phase 4`** (do NOT pre-resolve in planning): paint order relative to `_FogPainter.paint()`'s post-Phase-3.1 sequence (`canvas.translate(-canvasOffset)` FOG-13 → `canvas.clipPath(clipPath)` FOG-12 → `shader.render(pixelOrigin, zoomScale)` → `canvas.drawRect(Offset.zero & size, shaderPaint)`) — wisps drawn inside or outside the translated frame? inside or outside the clip path (clipped to reveal-hole or rendered over entire fog)? Render mechanism — dedicated wisp shader (MIRL applies per CLAUDE.md `# MIRL solution` visual-identity-preservation rule; ABI extension required; consistent with FOG-19 `uZoomScale` plumbing) OR Flutter Canvas API `drawCircle`/`drawRect` (MIRL doesn't apply; simpler; potentially worse perf at 200 particles)? DEBUG-01 toggle compatibility — wisps visible in `/sanity` debug-spiral mode or only in production?
- [ ] **WISP-05** *(NEW 2026-05-04 post-Phase-3.1 spec correction; per shader-fix-retrospective.md lesson #4 "ship the diagnostic before you need it" — Walk #4 supplementary debug-spiral asymmetric observation was the pivotal moment of Phase 3.1 closure)*: A `WispTransformLogger` (analogous to `FogTransformLogger` from Plan 03.1-01) emits per-paint diagnostic captures + 1-Hz JSONL rollups via `Logger('infrastructure.mirk.wisp')` carrying: active wisp count, mean particle age (ms), position bounds in `LatLng` (`latMin / latMax / lonMin / lonMax`), projected screen-Offset bounds (`screenXMin / screenXMax / screenYMin / screenYMax`), spawn rate per second. Cadence: 1-Hz wall-clock-aligned for grep-correlation against `infrastructure.mirk.fog_transform` + `infrastructure.mirk.sdf` + `infrastructure.mirk.frame_delta` streams. Enables Walk grep-correlation against fog/sdf streams + dev-marker timestamps if any wisp-anchoring failure mode surfaces (e.g., wisps drift relative to map during gesture; fp32 precision artefact at extreme distance; spawn-rate spike correlating with SDF rebuild; etc.). _(Phase 4 — implement before any walk attempt)_

### User Experience

- [x] **UX-01**: `/sanity` route's AppBar carries an `Icons.arrow_back` leading IconButton that calls `context.pop()`; the `/sanity` navigation from the main AppBar science-icon uses `context.push` (was `context.go`) so the pop has somewhere to return to. Catches the SANITY-NO-BACK-BUTTON failure mode from `03.1-FALSIFICATION.md` observation 5 (developer had to force-close the app to return from /sanity during the 03.1-03 walk). _(Phase 3.1 — Plan 03.1-05)_ _Complete — Verified-by-test + walk-2 (P03.1-05 widget test + P03.1-06 walk-time confirmation; developer's *"sanity page is ok"*)_
- [x] **UX-02**: Disable map rotation for the POC — `InteractionOptions(flags: InteractiveFlag.all & ~InteractiveFlag.rotate)` set on the FlutterMap widget; north-locked, only pan + pinch-zoom. Surfaced by Walk #3 (Plan 03.1-09): developer's preferred mitigation for the FOG-16 rotation-correlated fog mis-coverage failure mode — *"I think we should disable rotation for now, only pan and zoom, north is up. simplier"*. Sensible POC scope reduction; defers FOG-16 path (b) full canvas-inverse-transform until post-POC. Acceptance: `InteractionOptions` configured; widget test asserts a synthetic two-finger rotation gesture does NOT change `MapCamera.rotation`. MapCompass widget can be retained (always 0°) or removed as a separate UX decision. _(Phase 3.1 — Plan 03.1-10)_ _Complete — Verified-by-test + walk-time validated (P03.1-10 — `test/presentation/screens/map_screen_test.dart` rotation-disabled assertion: flags & InteractiveFlag.rotate == 0; flags & InteractiveFlag.drag != 0; flags & InteractiveFlag.pinchZoom != 0; P03.1-11 Walk #4 walk-time validation 2026-05-04 — `canvasTxMin/Median/Max + canvasTyMin/Median/Max identically 0.000000 across ALL 40 fog_transform rollups`; developer's verbatim *"rotation disable as wanted"*)._

### Logger

- [x] **LOG-01**: A logger configured via the `logging` package writes to `<getApplicationDocumentsDirectory()>/logs/yyyymmdd_hhmmss_logs.txt`, one file per app session
- [x] **LOG-02**: Log level for the POC is `Level.ALL` (verbose); each log line is timestamped to millisecond precision
- [x] **LOG-03**: The logger is initialised before any other module that might log (so initialisation failures are captured)
- [x] **LOG-04**: A button in the app (visible from any screen — likely an app-bar action) opens the system share sheet via `share_plus 12.0.2`, attaching the current session's log file
- [x] **LOG-05**: The share sheet works on a SideStore-sideloaded iOS build with the iOS Mail app as the share target. Phase 1 UAT exit gate: developer sideloads the IPA, taps share-logs, picks Mail, sends to themselves, verifies the email arrives with the gzipped log file as attachment. Verbal "approved" is the gate (no synthetic-log smoke test required per Phase 1 CONTEXT.md decision).

### Performance Instrumentation

- [x] **PERF-01**: An on-screen FPS counter overlay is rendered on the map screen (toggleable via a dev menu or always-on for the POC); shows current FPS rolling-averaged over 1 s
- [x] **PERF-02**: At Phase 2 UAT walk on iPhone 17 Pro: pan-FPS without fog ≥ 40 (gate before Phase 3 fog work begins) _(software-complete + sideload UAT verbal `approved` per Plan 02-06; FPS observed: sustained ~120 fps during pan / pinch / combined gestures at zoom 13–15 — 3× headroom over the gate. Idle-FPS ~4 fps is expected Flutter no-dirty-frames behaviour, not a regression. Evidence: `.planning/phases/02-map-no-fog/02-UAT.md`. Walked 2026-05-01 on iPhone 17 Pro against CI run 25212559648, SHA 46b8fcc.)_
- [ ] **PERF-03**: At Phase 3 UAT walk on iPhone 17 Pro: pan-FPS with fog active ≥ 30; idle-fog-animation FPS ≥ 50 _(Plan 03-08 walk on iPhone 17 Pro 2026-05-01: NOT MEASURED — walk aborted on visual grounds before deliberate FPS observation; PERF-03 is unmeasured-and-moot for this verdict because the failure mode is camera-lock correctness, not throughput. Even sustained 120 fps on a fog that doesn't translate with the camera would fail Criterion B. See `.planning/phases/03-fog-of-war-the-hypothesis/03-UAT.md`.)_
- [ ] **PERF-04**: At Phase 3 UAT walk: frame-delta probe (FOG-08) shows median ≤ 16 ms, p95 ≤ 32 ms, max ≤ 48 ms across ≥ 10 combined gestures _(Plan 03-08 walk on iPhone 17 Pro 2026-05-01: NOT CAPTURED — walk aborted on Criterion B's visual failure before the ≥ 10 combined-gesture seconds of probe rollup evidence could be recorded. Per the falsification clause, Criterion B's failure alone is sufficient for `denied`; Criterion A is unmeasured-and-moot. The probe code itself is software-complete and shipping in the IPA per FOG-08 verified-by-test entry. See `.planning/phases/03-fog-of-war-the-hypothesis/03-UAT.md`.)_
- [x] **PERF-05**: At Phase 3 UAT walk: developer's subjective verdict — no visible fog slip, no white-ellipse artefact during pan/zoom/combined gestures _(Plan 03-08 walk on iPhone 17 Pro 2026-05-01: **MEASURED — VERDICT DENIED.** Developer's verbatim words: *"mirk isn't moving, only the blue dot (so I guess the map below is moving), it can be rotated tho, denied"*. Three of four Criterion B sub-claims fail (slide-then-snap manifested as worse-than-slide-then-snap *static* fog; reveal-hole lag is permanent; inversion is geometrically forced). PERF-05 is checked-as-MEASURED (not as-passed) — the requirement was to capture the developer's subjective verdict, which was captured; the verdict itself is `denied`. See `.planning/phases/03-fog-of-war-the-hypothesis/03-FALSIFICATION.md` for the full Criterion B sub-claim breakdown and `03-UAT.md` for the walk evidence.)_
- [ ] **PERF-06**: Pixel 4a (Adreno 618) UAT walks at Phase 3 and Phase 5: app launches, fog renders, no crash; informational FPS recorded for cross-platform comparison (no hard pass criterion)
- [x] **PERF-07**: At Phase 3.1 UAT walk on iPhone 17 Pro: frame-delta probe (FOG-08) shows median ≤ 16 ms, p95 ≤ 32 ms, max ≤ 48 ms across ≥ 10 combined gestures (PERF-04 carry-over: Plan 03-08 walk left this unmeasured-and-moot). _(Phase 3.1 — Plan 03.1-03 Complete — Measured)_ Sideload session 2026-05-02 against CI run 25260475395 (SHA 5c63197): 36 1-Hz rollups captured; **median-of-medians 0.347 ms (≤ 16 ✓; 7.3× headroom), max-of-p95s 5.598 ms (≤ 32 ✓; 5.7× headroom), max-of-maxes 10.564 ms (≤ 48 ✓; 4.5× headroom)**. PERF-07 GREEN — the unmeasured-and-moot Criterion A from Plan 03-08 is now properly captured. **Re-validated at Walk #2 (P03.1-06 2026-05-02 against CI run 25262881962, SHA 9a87010):** 266 1-Hz rollups across 296-second session; **median-of-medians 1.495 ms (≤ 16 ✓; 5.4× headroom), max-of-p95s 6.340 ms (≤ 32 ✓; 5.0× headroom), max-of-maxes 12.416 ms (≤ 48 ✓; 3.9× headroom)**. Plan 03.1-04 (per-fragment `fract()`) + Plan 03.1-05 (`canvas.getTransform()` per-paint Float64List allocation) did NOT regress the frame budget. **Re-measured at Walk #4 (P03.1-11 2026-05-04 against CI run 25305105125, SHA b31766f):** 40 1-Hz rollups across 42-second session window (4761 total samples); **median-of-medians 0.243 ms (≤ 16 ✓; ~66× headroom), max-of-p95s 5.020 ms (≤ 32 ✓; ~6.4× headroom), max-of-maxes 9.514 ms (≤ 48 ✓; ~5.0× headroom)**. Plan 03.1-10 (Dart-side integer/fractional decomposition + per-fragment world-coordinate computation in BOTH production + debug-spiral shaders) did NOT regress the frame budget; Walk #4 numbers are consistent with Walks #1 + #2 + Walk #3b.
- [ ] **PERF-08**: `SdfCache._hash(...)` quantises the viewport bbox edges to 1e-4 lat/lon (~11 m at equator) so per-paint micro-drift during pan/zoom does not invalidate the cache. Pre-Plan-03.1-05 the raw bbox doubles produced 12-115 rebuilds/sec during gesture seconds despite constant disc count (per `03.1-FALSIFICATION.md` SDF Anomaly). Verified-by-test: same disc set + sub-quantisation viewport drift produces ONE rebuild (cache HIT); super-quantisation drift produces TWO rebuilds (cache MISS). _(Phase 3.1 — Plan 03.1-05 unit test GREEN)_ **Falsified-by-walk-2 (P03.1-06 walk 2026-05-02)** — empirical SDF rollups show 1-121 rebuilds/sec (median 68/sec) during the 296-second walk; statistically indistinguishable from Walk #1's 12-115/sec. The 1e-4 lat/lon quantisation grid does NOT match the in-production gesture pattern's drift magnitude. Per-rebuild cost stays sub-13 ms (frame budget preserved) but the unintended thrash remains a candidate residual contributor to the persisting FOG-11 shimmer symptom. Plan 03.1-07 will widen the quantisation OR pivot to a disc-set-only cache key (see `03.1-UAT-2.md` PERF-08-WIDEN gap entry).

## v2 Requirements

Deferred to a follow-up if the POC succeeds and ports back to MirkFall, or to a future iteration of the POC if the hypothesis needs more visual fidelity to be conclusive.

### Visual Fidelity

- **STYLE-01**: Custom basemap theme replicating MirkFall's neutral palette (`#f5f1e8` / `#a6c9df` / etc.)
- **STYLE-02**: Multiple mirk styles beyond `atmospheric` (faded, neon, etc.)

### Robustness

- **ROB-01**: SDF rebuild moved to a worker isolate when disc count grows beyond ~100 (POC stays on UI isolate per donor-file's documented `< 16 ms` cost)
- **ROB-02**: PMTiles file integrity check (hash verification) on first-launch copy
- **ROB-03**: Graceful handling of GPS-permission-revoked-mid-session

## Out of Scope

Explicitly excluded. Documented to prevent scope creep.

| Feature | Reason |
|---------|--------|
| Custom basemap styling | Not on the critical path of the same-Canvas hypothesis; default renderer style suffices |
| Multiple mirk styles | Atmospheric only; visual variety is a v2 concern |
| Database (Drift / SQLite) | Discs stored in memory; POC has no persistence requirement |
| Session management | No cross-session state; each launch is fresh |
| Offline compaction, country switching, mirk download infrastructure | Out of POC's hypothesis scope |
| Burger menu, settings screen, live tuner sheet | Convenience UI; defer until migration |
| `MapView` domain abstraction | Talk to `flutter_map` directly; abstraction is a migration concern |
| `MirkInitialRevealFade` | Visual polish; not on the hypothesis path |
| `Permission.locationAlways`, notification permission | Only `locationWhenInUse` needed for the POC |
| Telemetry / analytics / crash reporting | Forbidden by GOSL v1.0 in any case |
| Parent-FPS comparison (criterion C) | Dropped — POC stands on absolute FPS + lock-correctness alone |
| Per-phase Pixel 4a UAT walks | Pixel 4a walked only at Phase 3 (hypothesis test) and Phase 5 (decision); per-phase double-walks are too costly |
| State management framework (Riverpod / Bloc / Provider) | Plain `StatefulWidget` + `setState` + constructor-injected services suffices for a 3-screen POC; ports back to MirkFall's framework on migration |

## Traceability

Filled by the roadmap on 2026-04-30. Five phases:
1. **Foundation** — bootstrap, CI, logger, share, FPS counter, permission gate
2. **Map (no fog)** — PMTiles, gestures, blue dot, recenter; ≥ 40 fps no-fog gate
3. **Fog of War — THE HYPOTHESIS** — fog layer, SDF, frame-delta probe, falsification walk
4. **Wisp Particles** — world-locked wisps, cross-pipeline parity
5. **Decision Gate** — final hardening, Pixel 4a sanity walk, formal POC verdict

| Requirement | Phase | Status |
|-------------|-------|--------|
| BOOT-01 | Phase 1 | Complete |
| BOOT-02 | Phase 1 | Complete |
| BOOT-03 | Phase 1 | Complete |
| BOOT-04 | Phase 1 | Complete |
| BOOT-05 | Phase 1 | Complete |
| BOOT-06 | Phase 1 | Complete |
| BOOT-07 | Phase 1 | Complete |
| BOOT-08 | Phase 1 | Complete |
| AUDIT-01 | Phase 1 | Complete |
| AUDIT-02 | Phase 1 | Complete |
| AUDIT-03 | Phase 1 | Complete |
| CI-01 | Phase 1 | Complete |
| CI-02 | Phase 1 | Complete |
| CI-03 | Phase 1 | Complete |
| CI-04 | Phase 1 | Complete |
| CI-05 | Phase 1 | Complete |
| AUTH-01 | Phase 1 | Complete |
| AUTH-02 | Phase 1 | Complete |
| AUTH-03 | Phase 1 | Complete |
| AUTH-04 | Phase 1 | Complete |
| AUTH-05 | Phase 1 | Complete |
| AUTH-06 | Phase 1 | Complete |
| MAP-01 | Phase 2 | Complete |
| MAP-02 | Phase 2 | Complete |
| MAP-03 | Phase 2 | Complete |
| MAP-04 | Phase 2 | Complete |
| MAP-05 | Phase 2 | Complete |
| MAP-06 | Phase 2 | Complete |
| LOC-01 | Phase 2 | Complete |
| LOC-02 | Phase 2 | Complete |
| LOC-03 | Phase 2 | Complete |
| LOC-04 | Phase 2 | Complete |
| LOC-05 | Phase 2 | Complete |
| FOG-01 | Phase 3 | Complete — Verified-by-test (P03-01 stub; P03-02 RevealDiscRepository; P03-07 MapScreen wiring — append on every fix with hand-rolled rvd_ ID) |
| FOG-02 | Phase 3 | Complete — Verified-by-test (P03-01 stub; P03-02 distanceMetres degree-distance regression test GREEN) |
| FOG-03 | Phase 3 | Complete — Verified-by-test (P03-01 stubs; P03-03 SdfCache + SdfRebuildLogger; 1-Hz JSONL rollup + stop()-flush tests GREEN) |
| FOG-04 | Phase 3 | **Complete — Verified-by-walk (P03.1-15 Walk #6 CONFIRMED-AFTER-FIX FULL 2026-05-04 — Phase 3.1 closes after 6 iterations + 8 walks)** — Plan 03-08 DENIED verdict REVERSED. The same-Canvas fog hypothesis is REINSTATED; the layered Phase 3.1 fix bundle (Plans 03.1-02 + 03.1-04 + 03.1-05 + 03.1-07 + 03.1-08 + 03.1-10 + 03.1-12 + 03.1-14) delivers full pan-translation + zoom-anchoring + UX-02 rotation-absence + reveal-hole co-location + viewport-coverage. Developer's verbatim Walk #6 verdict: *"100% solved for panning and zoomin on mirk shader"*. See `03.1-FALSIFICATION-6.md`. |
| FOG-05 | Phase 3 | **Complete — Verified-by-walk (P03.1-15 Walk #6 CONFIRMED-AFTER-FIX FULL 2026-05-04 — Phase 3.1 closes)** — Plan 03-08 DENIED verdict REVERSED. Uniforms populate correctly per slot-gate test AND the painter's Canvas now correctly consumes the camera translation+zoom transforms via the layered Phase 3.1 fix bundle (FOG-09 + FOG-11 + FOG-12 + FOG-13 + FOG-14 + FOG-17 + FOG-18 + FOG-19); FogShaderUniforms.totalFloatSlots = 42 with slot 41 = uZoomScale. See `03.1-FALSIFICATION-6.md`. |
| FOG-06 | Phase 3 | **Complete — Verified-by-walk (P03.1-15 Walk #6 CONFIRMED-AFTER-FIX FULL 2026-05-04 — Phase 3.1 closes)** — Plan 03-08 DENIED verdict REVERSED. Clip path computed correctly per P03-05 tests AND now applied in the world-aligned coordinate frame via Plan 03.1-05's FOG-12 clip-path canvasOffset shift + Plan 03.1-08's FOG-13 symmetric `canvas.translate(-canvasOffset)`. Reveal hole co-located with blue dot retained (no Walk #6 complaint surfaced; UX-02 retained 3rd consecutive walk). See `03.1-FALSIFICATION-6.md`. |
| FOG-07 | Phase 3 | **Complete — Verified-by-walk (P03.1-15 Walk #6 CONFIRMED-AFTER-FIX FULL 2026-05-04 — Phase 3.1 closes)** — Plan 03-08 DENIED verdict REVERSED. Single-MapCamera-snapshot enforcement passes structurally (P03-05 KEYSTONE GREEN); the snapshot reaches the shader correctly via FOG-09 / FOG-17 / FOG-18 / FOG-19 layered fix bundle. pixelOrigin tracks gesture monotonically across all regimes; FOG-18 confirms uOffset == pxOrigin exactly; FOG-19 uZoomScale anchors noise to lat/lng during zoom. See `03.1-FALSIFICATION-6.md`. |
| FOG-08 | Phase 3 | Complete — Verified-by-test (P03-01 stubs; P03-04 ring buffer + 1-Hz JSONL rollup + dual-clock; P03-06 overlay; P03-07 wires probe.start in initState + overlay at top:104 right:8 in MapScreen Stack). Quantitative walk evidence not captured (P03-08 aborted on visual grounds before probe rollups recorded), but the probe code itself ships in the IPA. |
| FOG-09 | Phase 3.1 | Complete — Verified-by-test (P03.1-02) |
| FOG-10 | Phase 3.1 | Complete — Verified-by-test (P03.1-01) |
| FOG-11 | Phase 3.1 | **Complete — Verified-by-walk (P03.1-15 Walk #6 CONFIRMED-AFTER-FIX FULL 2026-05-04 — Phase 3.1 closes)** — Walk #5 closed Q1 max-zoom SNAP via FOG-18; **Walk #6 closes Q1b zoom-gesture residual via FOG-19 C-b uZoomScale uniform** (developer's *"100% solved for... zoomin on mirk shader"* directly resolves Walk #5's *"steppy displacement... numbers sliding... incorrect scaling?"*). FOG-11 superseded by the layered FOG-15 → FOG-17 → FOG-18 → FOG-19 evolution; CI test stays mechanically GREEN at HEAD. See `03.1-FALSIFICATION-6.md`. |
| FOG-12 | Phase 3.1 | Complete — Verified-by-test + walk-2 (P03.1-05 widget test + P03.1-06 walk-time confirmation; reveal hole co-located with blue dot) |
| FOG-13 | Phase 3.1 | Verified-by-test-only (P03.1-08 — `fog_rect_viewport_coverage_test.dart`); fog-rect viewport-coverage symmetric compensation via `canvas.translate(-canvasOffset)`. Walk #3 (P03.1-09) surfaced rotation-correlated regression — pure-translation case still passes; rotation case opens FOG-16. |
| FOG-14 | Phase 3.1 | Complete — Verified-by-test (P03.1-07 Task 3 Branch B-3 — `fog_debug_spiral_continuity_test.dart` wrap-period regression gate 3/3 GREEN + `fog_tile_period_invariant_test.dart` static-source invariant 3/3 GREEN) |
| FOG-15 | Phase 3.1 | **Complete — Verified-by-walk (P03.1-15 Walk #6 CONFIRMED-AFTER-FIX FULL 2026-05-04 — Phase 3.1 closes)** — historical mechanism-confirmed fix (Plan 03.1-07 Task 3 Branch B-3 tile-period-aware fract); superseded by FOG-17 + FOG-18 + FOG-19 layered fix bundle which delivers full closure. CI test stays mechanically GREEN at HEAD. See `03.1-FALSIFICATION-6.md`. |
| FOG-16 | Phase 3.1 | Complete — Verified-by-test + walk-time validated via UX-02 path (a) (P03.1-10 widget test + P03.1-11 Walk #4 walk-time validation 2026-05-04 — canvasTx/Ty identically 0.000000 across all 40 fog_transform rollups; developer's verbatim *"rotation disable as wanted"*). **P03.1-13 Walk #5 (2026-05-04) re-validates** — canvasTx/Ty identically 0.000000 across all 80 fog_transform rollups (2nd consecutive walk); UX-02 InteractionOptions retained. **P03.1-15 Walk #6 (2026-05-04, CONFIRMED-AFTER-FIX FULL) re-validates 3rd consecutive walk** — UX-02 retained by inheritance (Plan 03.1-14 made no rotation-related changes; no rotation complaint surfaced during Walk #6). Rotation-correlated mis-coverage failure mode structurally prevented; FOG-16 path (b) full canvas-inverse-transform deferred to hypothetical post-POC iteration. |
| FOG-17 | Phase 3.1 | Complete — Verified-by-test + walk-time validated (P03.1-10 unit tests + P03.1-11 Walk #4 + P03.1-13 Walk #5 + **P03.1-15 Walk #6 walk-time-validated 3rd consecutive walk 2026-05-04 CONFIRMED-AFTER-FIX FULL**). World-coordinate formulation lands in BOTH production + debug-spiral shaders; FOG-17a precision pairing (Dart-side integer/fractional decomposition) lands in `_FogPainter.paint()`. Re-classified 2026-05-04 as the production-fog-shader's implementation of the MIRK-01 ABI. **P03.1-12 (Plan 03.1-12 Task 1):** FOG-17a integer-wrap premise FALSIFIED by Walk #4 debug-spiral positive control; replaced by FOG-18 (modulo eliminated). FOG-17 world-coordinate noise sampling formulation in shaders UNCHANGED — still in effect. **P03.1-14 (Plan 03.1-14 Task B):** FOG-17 formulation EXTENDED with FOG-19 C-b uZoomScale division (`worldPx / (kNoiseTilePx * uZoomScale)`); anchors noise to lat/lng during zoom. **Walk #6 verbal verdict:** *"100% solved for panning and zoomin on mirk shader"* — FOG-17 world-coordinate formulation walk-time-validated at all zoom levels including zoom-gesture transitions. See `03.1-FALSIFICATION-6.md`. |
| FOG-18 | Phase 3.1 | Complete — Verified-by-test + walk-time validated 2nd consecutive walk (P03.1-12 Task 1 + **P03.1-13 Walk #5** Q1 SNAP closed at MAX zoom; **P03.1-15 Walk #6 walk-time-validated 2026-05-04 CONFIRMED-AFTER-FIX FULL** — retained by inheritance (Plan 03.1-14 made NO Dart-side changes to `_FogPainter.paint()`'s appliedUOffset path; FOG-19's zoom-scale lives shader-side, not Dart-side; FOG-18 modulo-elimination signature `uOffsetX == pxOriginX` retained); developer's verbatim *"100% solved for panning and zoomin on mirk shader"* implicitly confirms Q1 retention. FOG-17a-wrap-firing-visibly mechanism interpretation remains EMPIRICALLY CONFIRMED. See `03.1-FALSIFICATION-6.md`. |
| FOG-19 | Phase 3.1 | **Complete — Verified-by-test + walk-time validated (P03.1-14 Task B + P03.1-15 Walk #6 CONFIRMED-AFTER-FIX FULL 2026-05-04 — Phase 3.1 closes)** — C-b uZoomScale uniform (slot 41 added to BOTH production + debug shaders, consumed as `worldPx / (kNoiseTilePx * uZoomScale)` in production and `cellPx = worldPx / uZoomScale` in debug-spiral); Dart-side `_FogPainter.paint()` forwards `pow(2, camera.zoom - kPocFogReferenceZoom).toDouble()` with `kPocFogReferenceZoom = 13.0`. Test artefacts: `fog_zoom_invariant_basis_test.dart` 4 sub-tests; `fog_shader_uniforms_test.dart` totalFloatSlots == 42 + zoomScale verbatim test; `fog_debug_spiral_continuity_test.dart` FOG-19 static-source assertions; `fog_tile_period_invariant_test.dart` post-FOG-19 formulation. **Walk #6 walk-time validation:** developer's verbatim *"100% solved for... zoomin on mirk shader"* directly resolves Walk #5's *"steppy displacement... numbers sliding... incorrect scaling?"*. The DIVIDING form `worldPx / (kNoiseTilePx * uZoomScale)` is correct; the previous-iteration multiplicative form (algebraically equivalent to `pixelOrigin + size/2`) was wrong per the Plan-checker Blocker #1 hypothesis. MIRL visual-identity preserved at reference zoom 13.0 (uZoomScale = 1.0 → bit-identical to pre-fix). See `03.1-FALSIFICATION-6.md`. |
| DEBUG-01 | Phase 3.1 | Complete — Verified-by-test + walk-time validated (P03.1-07 Task 1 unit test — `digit_atlas_builder_test.dart` + augmented `shader_sanity_screen_test.dart` debug-spiral-toggle group; P03.1-09 Walk #3 implicitly walk-time-validated; **P03.1-15 Walk #6 walk-time-validated 2026-05-04 CONFIRMED-AFTER-FIX FULL** — developer accessed the debug-spiral toggle path during Walk #6 (per Walk #6 verbatim *"numbered shader is broken"* — DEBUG-01 toggle infrastructure functional, only the DEBUG-03 4-digit cell rendering output is regressed; see DEBUG-03 row + 03.1-FALSIFICATION-6.md §Known defects). Permanent diagnostic infrastructure retained in codebase. |
| DEBUG-02 | Phase 3.1 | Complete — Verified-by-test + walk-time validated (P03.1-12 Task 2 + **P03.1-15 Walk #6 walk-time-validated 2026-05-04 CONFIRMED-AFTER-FIX FULL** — structural fix mechanically present in IPA (CameraConstraint absent + CI gates GREEN). Walk #6 explicit ≥7M extreme-distance walk-time-validation outcome ambiguous (no Mail-shared JSONL): developer's verbatim *"100% solved for panning and zoomin on mirk shader"* implicitly covers any pan distance reached during the session AND/OR carries forward Walk #5's empirical baseline (NO precision-induced artefacts up to pxOriginX 4.26M, well within fp32 16.7M raw-px exact-integer mantissa ceiling). Phase 3.1 closure does NOT block on this — the FULL closure verdict was developer-driven and decisive. Explicit ≥7M validation deferred to ambient observation in future Phase 4/5 walks. See `03.1-FALSIFICATION-6.md` §Walk Evidence §Fog-transform diagnostic stream. |
| DEBUG-03 | Phase 3.1 | **Complete with known defect — debug-spiral 4-digit rendering regression explicitly waived by developer at Walk #6** ("numbered shader is broken : but I don't care"); DEBUG-03 served its diagnostic purpose during Plan 03.1-14 development and Walk #6 verbal verdict; cleanup deferred indefinitely (debug-shader-only; no production impact). The Plan 03.1-14 Task A unique-cell-numbers implementation introduced a regression in `atmospheric_fog_debug_spiral.frag` rendering output (CI gates `fog_debug_spiral_continuity_test.dart` static-source assertions still GREEN — `(cell.y + 50) * 100 + (cell.x + 50)` present AND `mod(float(rawCellIndex), 100.0)` absent AND `thousands` identifier present — but the runtime rendering of the 4-digit cells on iPhone 17 Pro is broken). Possible mechanisms (not investigated since waived): 4-glyph horizontal slot rendering, atlas UV mapping, encoding clamp at boundary, vec2 cell-coordinate computation interaction with FOG-19 `cellPx = worldPx / uZoomScale`. Developer assessed Q1b closure via subjective production-fog observation instead, which is the actual goal. Follow-up candidate IF Phase 4/5 surfaces a renewed need for quantitative drift measurement. See `03.1-FALSIFICATION-6.md` §Known defects / future cleanup candidates. |
| MIRK-01 | Phase 3.1 (deferred) | **Deferred captured architectural insight** — formal acceptance criteria deferred per developer's 2026-05-04 clarification (*"defining an architecture seems too early; once we find how to solve our issue, it can be made better later"*). Plan 03.1-10 implementation (FOG-17 + FOG-17a) is the seed aligned with MIRK-01; revisit formalization post-Walk-4 verdict. NOT a Plan 03.1-11 deliverable. |
| WISP-01 | Phase 4 | Pending |
| WISP-02 | Phase 4 | Pending |
| WISP-03 | Phase 4 | Pending |
| WISP-04 | Phase 4 | Pending |
| LOG-01 | Phase 1 | Complete |
| LOG-02 | Phase 1 | Complete |
| LOG-03 | Phase 1 | Complete |
| LOG-04 | Phase 1 | Complete |
| LOG-05 | Phase 1 | Complete |
| PERF-01 | Phase 1 | Complete |
| PERF-02 | Phase 2 | Complete |
| PERF-03 | Phase 3 | Not measured (P03-08 walk aborted on visual grounds; PERF-03 is unmeasured-and-moot for the `denied` verdict). |
| PERF-04 | Phase 3 | Not captured (P03-08 walk aborted before ≥ 10 combined-gesture seconds of probe rollups; per falsification clause Criterion B's failure alone delivers `denied`, so Criterion A is unmeasured-and-moot). |
| PERF-05 | Phase 3 | **Measured — VERDICT DENIED (P03-08 2026-05-01)**. Developer's verbatim words: *"mirk isn't moving, only the blue dot (so I guess the map below is moving), it can be rotated tho, denied"*. See 03-FALSIFICATION.md. |
| PERF-06 | Phase 5 | Pending |
| PERF-07 | Phase 3.1 | Complete — Measured (P03.1-03 Walk #1: medianMs 0.347 / p95Ms 5.598 / maxMs 10.564; 4-7× headroom). Re-validated (P03.1-06 Walk #2: medianMs 1.495 / p95Ms 6.340 / maxMs 12.416; 3.9-5.4× headroom). Walk #3 (P03.1-09) implicit retention — no log file Mail-shared. **Re-measured (P03.1-11 Walk #4 2026-05-04: medianMs 0.243 / p95Ms 5.020 / maxMs 9.514 across 40 rollups / 4761 samples; ~66× / ~6.4× / ~5.0× headroom).** **Re-measured (P03.1-13 Walk #5 2026-05-04: medianMs 1.228 / p95Ms 1.591 / maxMs 1.724 across 80 rollups; 13× / 20× / 28× headroom over the 92-sec active window with two ~14-21 sec sustained MAX-zoom pan windows).** **P03.1-15 Walk #6 (2026-05-04, CONFIRMED-AFTER-FIX FULL): Walks #4-#5 baseline retained; verbal verdict only (no Mail-shared session log; no JSONL re-measurement); no developer FPS regression complaint surfaced.** Plan 03.1-14's FOG-19 uZoomScale uniform adds minimal arithmetic (one float division per fragment in production shader; debug-only path for DEBUG-03 4-glyph encoding); MIRL visual-identity preserved at reference zoom. |
| PERF-08 | Phase 3.1 | **Falsified-by-walk-2 (P03.1-06)** — unit test GREEN but production rebuild rate 1-121/sec (median 68/sec) statistically indistinguishable from Walk #1 baseline. No Plan 03.1-07/08 work targeted PERF-08; status unchanged at Walk #3 (no log Mail-shared). Future-plan candidate (out-of-scope for Plan 03.1-10). |
| UX-01 | Phase 3.1 | Complete — Verified-by-test + walk-2 + walk-3-implicit (P03.1-05 widget test + P03.1-06 walk-time confirmation; P03.1-09 implicit retention — no force-close complaint surfaced) |
| UX-02 | Phase 3.1 | Complete — Verified-by-test + walk-time validated 3rd consecutive walk (P03.1-10 — `map_screen_test.dart` UX-02 rotation-disabled assertion; flags & InteractiveFlag.rotate == 0; flags & InteractiveFlag.drag != 0; flags & InteractiveFlag.pinchZoom != 0; **P03.1-11 Walk #4 walk-time validation 2026-05-04** — canvasTx/Ty identically 0.000000 across all 40 fog_transform rollups; developer's verbatim *"rotation disable as wanted"*; **P03.1-13 Walk #5 re-validation 2026-05-04** — canvasTx/Ty identically 0.000000 across all 80 fog_transform rollups; **P03.1-15 Walk #6 re-validation 2026-05-04 CONFIRMED-AFTER-FIX FULL** — UX-02 InteractionOptions retained by inheritance through Plan 03.1-12 + Plan 03.1-14 (no rotation-related changes); no rotation-related complaint surfaced during Walk #6). Phase 3.1 closure confirms UX-02 path (a) (disable rotation) is the empirical answer for the rotation-correlated mis-coverage failure mode. |

**Coverage:**
- v1 requirements: 76 total (71 + FOG-18 + DEBUG-02 + DEBUG-03 + FOG-19 + WISP-05)
- Mapped to phases: 76
- Unmapped: 0 ✓

**Per-phase counts:**
- Phase 1 (Foundation): 28 requirements (BOOT × 8, AUDIT × 3, CI × 5, AUTH × 6, LOG × 5, PERF-01)
- Phase 2 (Map, no fog): 12 requirements (MAP × 6, LOC × 5, PERF-02)
- Phase 3 (Fog — THE HYPOTHESIS): 11 requirements (FOG × 8, PERF-03/04/05)
- Phase 3.1 (Fix Fog Pan-Translation): 19 requirements (FOG-09, FOG-10, FOG-11, FOG-12, FOG-13, FOG-14, FOG-15, FOG-16, FOG-17, FOG-18, FOG-19, PERF-07, PERF-08, UX-01, UX-02, DEBUG-01, DEBUG-02, DEBUG-03, MIRK-01 [deferred — captured insight])
- Phase 4 (Wisps): 5 requirements (WISP × 5 — WISP-05 added 2026-05-04 post-Phase-3.1 spec correction; per `shader-fix-retrospective.md` lesson #4 "ship the diagnostic before you need it")
- Phase 5 (Decision Gate): 1 requirement (PERF-06)

## Revisions

- **2026-04-30 (Phase 1 planning):** LOG-05 wording softened — dropped the prior 50-megabyte synthetic-logfile smoke-test specification per CONTEXT.md `Phase 1 UAT exit gate` decision. The replacement gate is verbal "approved" after a single sideload + Mail round-trip walk.
- **2026-04-30 (Phase 1 planning, B-1 fix):** BOOT-01 SDK pin updated from `3.41.8` to `3.41.7` for parent code-donor parity per RESEARCH.md Open Question #1. The earlier 3.41.8 wording predated the planner's parent-parity lock; Plan 01 (`flutter create`) and Plan 03 Task 2 (CI workflow `flutter-version: '3.41.7'`) both reference 3.41.7. This wording change brings REQUIREMENTS.md into lockstep with both plans.
- **2026-05-01 (Phase 1 closure, Plan 01-07 SUMMARY):** LOG-03 marked Complete after source review of `lib/main.dart` confirmed `await FileLogger.bootstrap()` runs BEFORE `WidgetsBinding.instance.addObserver(FileLoggerLifecycleObserver())` and BEFORE `runApp(MirkPocApp)`. AUTH-04 carries a deferred-bug note (cross-restart auto-resume routing bug; software-complete per Plan 01-06, sideload UAT confirms manual deny + Open Settings flow works; cross-restart auto-nav from /denied to /map after iOS Settings toggle does not fire — deferred per user's POC-scope call). Phase 1 closes with all 28 requirement IDs addressed; AUTH-04 alone carries the documented limitation.
- **2026-05-01 (Phase 2 closure, Plan 02-06 SUMMARY):** PERF-02 marked Complete after sideload UAT walk on iPhone 17 Pro against CI run `25212559648` (SHA `46b8fcc`). Developer's verbatim verdict: *"everything works well, 120 fps when doing stuff, revert to 4 when not doing anything"*. Sustained ~120 fps observed during pan / pinch / combined gestures at zoom 13–15 — 3× headroom over the ≥ 40 fps gate. Idle-FPS ~4 fps documented as expected Flutter no-dirty-frames behaviour (same pattern observed in Phase 1 sideload UAT). Evidence: `.planning/phases/02-map-no-fog/02-UAT.md`. Phase 2 closes with all 12 requirement IDs Complete; Phase 3 (fog hypothesis) unblocked with massive frame-budget headroom for the fog shader.
- **2026-05-01 (Phase 3 closure, Plan 03-08 SUMMARY — HYPOTHESIS DENIED):** Plan 03-08 sideload UAT walk on iPhone 17 Pro against CI run `25224334312` (SHA `280dd04`) over central Melun delivered a **DENIED** verdict on the same-Canvas fog hypothesis. Developer's verbatim words: *"mirk isn't moving, only the blue dot (so I guess the map below is moving), it can be rotated tho, denied"*. Fog renders correctly on screen (the pre-walk `/sanity` shader-compile gate held — the developer saw "mirk" during the walk), and rotation gestures DO transform the fog surface, but **translation/pan does not**: the fog stays static relative to the screen while the underlying tile layer translates beneath it. The structural FOG-04 test (`find.descendant(of: FogLayer, matching: MobileLayerTransformer)`) passed but the behavioural consequence (Canvas-transform sharing) does not follow. **FOG-04..07 flipped to Falsified-in-production**; FOG-01..03 + FOG-08 retain Complete (verified-by-test: probe code, repository, distance helper, SDF cache + logger all software-functional). **PERF-05 marked Measured-with-DENIED-verdict**; PERF-03/04 marked Not-measured (walk aborted on visual grounds before quantitative evidence was collected — moot per falsification clause: Criterion B's failure alone delivers `denied`). MirkFall migration recommendation: **DO NOT PORT BACK as-implemented**. Three diagnostic possibilities documented in 03-FALSIFICATION.md (camera-staleness during pan-driven repaints; transform-bypass at painter Canvas; widget-tree-vs-Canvas-matrix transform application by `MobileLayerTransformer`). Phase 3.1 gap-closure investigation phase is the recommended next step to diagnose the camera-translation propagation path before any port-back attempt; Phase 4 (wisp particles) does NOT unblock until Phase 3.1 produces a fix or formally confirms the failure is unfixable. Evidence: `.planning/phases/03-fog-of-war-the-hypothesis/03-FALSIFICATION.md` + `03-UAT.md`.
- **2026-05-02 (Phase 3.1 planning, Plan 03.1-01):** Three new v1 requirement IDs added — FOG-09 (behavioural transform-equality regression test), FOG-10 (`FogTransformLogger` per-paint diagnostic instrumentation), PERF-07 (post-fix Criterion A measurement re-running PERF-04 methodology that Plan 03-08 left unmeasured-and-moot). Total v1 requirements grow from 56 to 59. Phase 3.1 is the gap-closure phase that either reverses the Plan 03-08 `DENIED` verdict to `CONFIRMED-AFTER-FIX` or strengthens it to `DENIED-FINAL`. The structural FOG-04 test is augmented (forward-pointer docstring) but not replaced; the new FOG-09 test catches the Plan 03-08 static-fog failure mode mechanically without a sideload walk.
- **2026-05-02 (Phase 3.1 iteration, Plan 03.1-04):** New v1 requirement `FOG-11` added — behavioural smooth-noise-coordinate-evolution regression test catching the SHADER-MODULO-WRAP failure mode (`03.1-FALSIFICATION.md` observation 2 — "seed of the mirk was changing"). Plan 03.1-04 moves the modulo wrap from the Dart call site (`(pixelOrigin / size) % 1.0`) into the fragment shader (`fract(uPixelOrigin / uResolution)` per-fragment). Slot 3..4 uniform renamed `uOffset` → `uPixelOrigin`; the Dart call site forwards full-precision pixelOrigin without transformation. The previously-falsified-in-production FOG-04..07 stay flagged (Plan 03.1-05 must also land before the iPhone walk re-validates them); FOG-09 + FOG-10 stay Complete. Total v1 requirements grow from 59 to 60.
- **2026-05-02 (Phase 3.1 iteration, Plan 03.1-05):** Three new v1 requirement IDs added — FOG-12 (canvas-frame-alignment behavioural test), PERF-08 (SdfCache bbox-quantisation no-thrash unit test), UX-01 (sanity-screen back-button widget test). Plan 03.1-05 layers two more fix axes onto Plan 03.1-04: (a) `_FogPainter.paint()` reads `canvas.getTransform()` and pre-shifts the clip-path holes by `-(matrix[12], matrix[13])` so the SDF reveal frame stays co-located with the sibling blue-dot CircleLayer's UNTRANSLATED frame (closing 03.1-FALSIFICATION.md observation 4); (b) `SdfCache._hash(...)` quantises the viewport bbox edges to 1e-4 lat/lon so per-paint micro-drift no longer triggers cache rebuilds (closing the SDF Anomaly residual hypothesis). Plus the UX-01 single-line fix for the sanity-screen back-button gap from observation 5. New `### User Experience` requirements section added. Total v1 requirements grow from 60 to 63. Phase 3.1 grows from 4 to 7 requirements. FOG-04..07 stay flagged at `Falsified-in-production` until Plan 03.1-06 walk validates the layered fix on iPhone 17 Pro.
- **2026-05-02 (Phase 3.1, Plan 03.1-03 SUMMARY — VERDICT ITERATING-WITH-MAJOR-PROGRESS):** Plan 03.1-03 sideload UAT session on iPhone 17 Pro 2026-05-02 22:10:39Z..22:11:15Z against CI run `25260475395` (SHA `5c63197`) delivered an `iterating:` verdict per CONTEXT §Iteration policy — fix is partial; major progress; hypothesis NOT yet `CONFIRMED-AFTER-FIX` and NOT yet `DENIED-FINAL`. Developer's verbatim verdict: *"iterating with major progress — fix is partial"*. Frame-delta probe captured 36 1-Hz rollups: medianMs max 2.204 ≤ 16 ✓; p95Ms max 5.598 ≤ 32 ✓; maxMs max 10.564 ≤ 48 ✓. **PERF-07 flipped to Complete — Measured** with 4-7× headroom across all three thresholds. **FOG-04..07 retained at `Falsified-in-production`** (NOT flipped to Complete) — the Plan 03-08 constant-zero failure mode is structurally addressed (FOG-09 catches it in CI mechanically) but the post-fix walk surfaced three new failure modes that FOG-04..07 don't yet capture: (1) per-paint modulo-1.0 wrap producing visible noise-seam shimmer ("seed changing" obs 2); (2) intermittent partial pan tracking (obs 3); (3) reveal-hole offset from blue dot during gesture caused by non-zero `canvasTx/Ty` Canvas-level translation that RESEARCH §Pitfall D claimed wouldn't occur (obs 4). Diagnostic FOG-10 stream resolves these to two specific Plan 03.1-04 fix axes: pass full-precision pixelOrigin to the shader (modulo-in-shader), and read `canvas.getTransform()` in `_FogPainter.paint()` to apply inverse to SDF coordinates (or render blue dot inside FogLayer's Canvas). FOG-09 scope-clarified: it's a binary "moved at all?" gate, not a "looks correct?" gate; Plan 03.1-04 will add FOG-11 (smooth noise coordinate evolution) and FOG-12 (canvas-frame-corrected reveal-vs-bluedot equality) as higher-fidelity behavioural gates. Out-of-scope follow-up captured: ShaderSanityScreen lacks a back button (developer had to force-close to return) — single-line fix deferred to a downstream plan. Phase 3.1 stays `In Progress` — Phase 4 + Phase 5 stay BLOCKED. MirkFall migration recommendation **DEFERRED** until a subsequent Phase 3.1 plan delivers `CONFIRMED-AFTER-FIX` or `DENIED-FINAL`. Next action: `/gsd:plan-phase 3.1 --gaps` to author Plan 03.1-04 with the diagnostic stream from THIS walk as input. Evidence: `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION.md` + `03.1-UAT.md`.
- **2026-05-03 (Phase 3.1 iteration, Plan 03.1-07 Task 1):** Two new v1 requirement IDs added — DEBUG-01 (debug-spiral shader infrastructure for mechanism investigation) under a new `### Diagnostics` section; FOG-15 (mechanism-confirmed shader-coordinate-system fix) under `### Fog of War` (companion to FOG-14 which gains a refined description). The B-0 gating step in `03.1-FALSIFICATION-2.md` sub-section D requires direct user observation BEFORE committing to a fix axis (the Walk-2 "rotating 90° / translated fast repetitively" framings were explicitly developer SPECULATION about a phenomenon they cannot visually resolve — NOT confirmed mechanisms). Plan 03.1-07 ships the debug-spiral shader as the cleavage tool: `assets/shaders/atmospheric_fog_debug_spiral.frag` renders human-readable cell-index digits in the SAME `uPixelOrigin / uResolution` coordinate system as production fog; toggled on via a runtime `Switch.adaptive` at `/sanity` (default OFF; production fog rendering UNCHANGED at both `/map` and `/sanity` when toggle is OFF). Digit atlas pre-rasterized at runtime via `DigitAtlasBuilder` (`ui.PictureRecorder` + `TextPainter`); process-cached so first toggle ON triggers a one-time ~30-50 ms async rasterization, subsequent toggles ON are instant. Phase 3.1 per-phase count grows from 9 to 11 requirements; v1 total grows from 65 to 67. Tasks 2 (iPhone observation checkpoint) and 3 (mechanism-specific fix selection: Branch A no-op / B-1 fp32 precision / B-2 DPR scaling / B-3 tile-period mismatch / Branch C unknown-escalate) follow.
- **2026-05-02 (Phase 3.1, Plan 03.1-06 SUMMARY — VERDICT ITERATING-WITH-PARTIAL-PROGRESS):** Plan 03.1-06 sideload UAT walk #2 on iPhone 17 Pro against CI run `25262881962` (SHA `9a87010`) over central Melun delivered the third Phase 3.1 `iterating:` verdict per CONTEXT §Iteration policy — 3 of 4 targeted fix-axes confirmed; 1 fix landed in CI but did NOT close the user-observable symptom; 1 fix introduced a NEW regression. Walk session 296 seconds; 266 frame_delta rollups + 110 SDF INFO rollups + 267 fog_transform rollups. **CONFIRMED:** PERF-07 re-validated (medianMs 1.495 / p95Ms 6.340 / maxMs 12.416; 3.9-5.4× headroom; consistent with Walk #1's 4-7× — Plan 03.1-04 + 03.1-05 changes did not regress frame budget). FOG-12 reveal-hole alignment confirmed (developer's *"the revealed area is staying where it should"* + screenshot showing blue dot at reveal-hole centre). UX-01 back button confirmed walk-time (developer's *"sanity page is ok"*). **FALSIFIED-BY-WALK-2:** FOG-11 (SHADER-MODULO-WRAP — CI test went GREEN but user-observable shimmer persists with refined characterisation: *"the mirk pattern is switched to an other one, then an other one, then an other one, very fast (like changing the seed of a procedural pattern fast)... maybe it's just being rotated 90° very fast, or maybe it's translated very fast repetitively"*; CI-test-vs-walk-symptom mismatch — KEY LEARNING). PERF-08 (SDF-CACHE-VIEWPORT-THRASH — unit test GREEN but production rebuild rate 1-121/sec median 68/sec is statistically indistinguishable from Walk #1; the 1e-4 quantisation grid does not match the in-production gesture pattern). **NEW REGRESSION (P03.1-05 introduced):** fog-rect viewport-coverage failure mode (developer's *"Mirk shader is somehow rotating and not covering the whole map when zooming in... mirk made worse (mirk offset itself leaving part of the map revealed at the edges)"* + screenshot showing top-right + bottom-right diagonal strips of un-fogged map). Diagnostic stream: 243/267 rollups have \|canvasTx\|>100 or \|canvasTy\|>100; worst sustained `(+757.35, +319.46)` for ~50 seconds. Plan 03.1-05's clip-path-only compensation does not reach the `canvas.drawRect(rect, fogPaint)` rect-cover paint — the rect IS subject to the canvas transform too. Two new requirements added: **FOG-13** (fog-rect viewport-coverage symmetric compensation — Plan 03.1-07 pending) and **FOG-14** (higher-fidelity user-observable noise-pattern stability test — Plan 03.1-07 pending). Phase 3.1 per-phase count grows from 7 to 9 requirements; v1 total grows from 63 to 65. Phase 3.1 stays `In Progress` — Phase 4 + Phase 5 stay BLOCKED. MirkFall migration recommendation **DEFERRED**. Next action: `/gsd:plan-phase 3.1 --gaps` to author Plan 03.1-07 with the FALSIFICATION-2.md sub-section D hypothesis triage table as input. Evidence: `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-2.md` + `03.1-UAT-2.md`. Walk #1 historical record `03.1-FALSIFICATION.md` + `03.1-UAT.md` AND Phase 3 record `03-FALSIFICATION.md` UNTOUCHED.
- **2026-05-03 (Phase 3.1 iteration, Plan 03.1-07 SUMMARY — FOG-14 + FOG-15 closure via Branch B-3):** Plan 03.1-07 Task 3 lands the Branch B-3 fix (noise-tile-period mismatch) selected per the developer's iPhone 17 Pro Walk #2 production-conditions observation: *"the shader is being translated in x and y when I pan or zoom, it's clear with the numbered shader, with the mirk it seems like a 'seed' change because the mirk is animated. the translation isn't smooth, it's 'stepped', you pan a little bit it works fine, then there is a translation. while zooming it's being translated a lot."* Mechanism confirmed: `fract(uPixelOrigin / uResolution)` wraps every viewport-width (~390 px), and the wrap mismatched the noise-tile period (~1/maxScale viewport-widths). Fix: `fract(uPixelOrigin / tilePeriodPixels)` where `tilePeriodPixels = uResolution / max(uScaleFar, uScaleMid, uScaleNear)`. Derived in-shader from existing runtime slots 20..22; no new uniform. `FogShaderUniforms.totalFloatSlots = 41` preserved. Applied symmetrically to `assets/shaders/atmospheric_fog.frag` (production) and `assets/shaders/atmospheric_fog_debug_spiral.frag` (debug-spiral; with constant-folded `DEBUG_SPIRAL_SCALE_*` mirrors of the production runtime constants). FOG-14 closes via test pair: FOG-14a (wrap-period regression gate — three sub-tests proving post-fix wraps ~10× more frequent than pre-fix on a 60s @ 120Hz / 5 px/paint smooth-pan trajectory; wrap-frequency multiplier equals `maxScale` = 10.5 by B-3 design); FOG-14b (static-source engineering invariant — production + debug shaders both use `tilePeriodPixels = uResolution / maxScale`; pre-fix formulation excluded from active code; 41-slot uniform layout intact). FOG-15 closes via the structural fix landing GREEN on both shaders. **B-3 is a documented PARTIAL fix:** wrap period moves from ~390 px to ~16-65 px depending on `maxScale`. Wraps still happen but at sub-perceptible magnitudes within a single octave. If Walk #3 surfaces residual stepping at very high zoom (where fp32 precision also degrades), Plan 03.1-10 may need a world-coordinate-noise rewrite. **FOG-11 remains Falsified-by-walk-2 — superseded by FOG-15.** The FOG-11 assertion (consecutive-paint pixelOrigin delta < 1e3 raw px) is mechanically still GREEN at HEAD; the user-observable shimmer is now addressed structurally by the FOG-15 mechanism-confirmed fix. FOG-04..07 retain `Falsified-in-production` until Plan 03.1-09 walk validates the layered fix bundle on iPhone 17 Pro. Phase 3.1 per-phase count unchanged (FOG-14 + FOG-15 were already counted in P03.1-07-Task1 revision); FOG-14 + FOG-15 status flips Pending → Complete. Phase 4 + Phase 5 stay BLOCKED until Walk #3 verdict.

**Plan 03.1-07 Task 3 — Deviations from plan:**
- **[Rule 1 — Bug] FOG-14a invariant reframed.** Plan body proposed cell-index delta ≤ 1 per paint as the FOG-14a invariant. That assertion is not mechanically attainable in either pre-fix or post-fix formulation: every fract-wrap event causes a viewport-width spatial jump in `cellPx = spiralCoord * uResolution` regardless of the wrap period. B-3 changes the wrap FREQUENCY (~10× more wraps), not the per-event MAGNITUDE in the spiral cell-grid space. Reframed FOG-14a to the testable engineering property: wrap frequency aligned with the noise-tile period. Three sub-tests guard the wrap-count regime pre/post-fix and the maxScale multiplier — collectively a stronger regression gate than the original cell-index continuity claim because they directly assert the divisor change.

- **2026-05-03 (Phase 3.1 iteration, Plan 03.1-08 SUMMARY — FOG-13 closure):** Plan 03.1-08 lands the FOG-13 fog-rect viewport-coverage symmetric compensation. `_FogPainter.paint()` now calls `canvas.translate(-canvasOffset.dx, -canvasOffset.dy)` immediately after `canvas.clipPath(clipPath)` and before the fog-rect cover `canvas.drawRect(Offset.zero & size, fogPaint)` — so all subsequent drawing operations occur in the world-aligned frame instead of the canvas-translated frame. Closes the Walk #2 sub-section C regression introduced by Plan 03.1-05's clip-path-only compensation (top-right + bottom-right diagonal strips of un-fogged street map at high zoom, when canvasTx/Ty reach (+757, +319) magnitudes). New behavioural test `fog_rect_viewport_coverage_test.dart` reproduces Walk #2's worst-case canvas-translation magnitude in CI: asserts the call sequence `getTransform → save → clipPath → translate → restore` with the translate args matching `(-canvasTx, -canvasTy)` within `kPocCanvasTransformEpsilon`, and asserts exactly ONE translate call per paint (single-snapshot discipline at the matrix level — mirrors FOG-07 single-MapCamera-snapshot and FOG-12 single-getTransform-call invariants). FOG-12 stays Complete (clip-path shift composes correctly with the new canvas.translate because clipPath geometry is established BEFORE the translate; clipPath is not affected by subsequent canvas.translate calls). FOG-09 + FOG-11 widget mocks extended with no-op `translate` overrides to keep the existing test seam Fake-`noSuchMethod` behaviour clean. FOG-04..07 retain `Falsified-in-production` until Plan 03.1-09 walk validates the layered fix bundle (P03.1-02 + P03.1-04 + P03.1-05 + P03.1-07 + P03.1-08). Phase 3.1 per-phase count unchanged (FOG-13 was already counted in P03.1-06 revision). FOG-13 row in traceability table flips from `Pending (P03.1-08) — fog-rect viewport-coverage symmetric compensation; surfaced by P03.1-06 Walk #2` to `Complete — Verified-by-test (P03.1-08 — fog_rect_viewport_coverage_test.dart); fog-rect viewport-coverage symmetric compensation via canvas.translate(-canvasOffset)`.

- **2026-05-03 (Phase 3.1 4th iteration, Plan 03.1-10):** Software-only landing — UX-02 (disable map rotation; FlutterMap.interactionOptions.flags clears the rotate bit) + FOG-16 path (a) closed via UX-02 + FOG-17 (world-coordinate noise sampling — `worldPx = fragUv * uResolution + uPixelOrigin; noiseUv = worldPx / kNoiseTilePx` in BOTH production + debug-spiral shaders) + FOG-17a (CPU-side integer/fractional decomposition of camera.pixelOrigin in `_FogPainter.paint()` keeping shader input bounded under ~1537 raw px regardless of zoom). New constants `kPocFogNoiseTilePx = 384.0` and `kPocFogIntegerWrapPeriodPx = 1536.0` (invariant: `kPocFogIntegerWrapPeriodPx % kPocFogNoiseTilePx == 0`; chosen so on-screen noise cell ≈ kNoiseTilePx / maxScale ≈ 36.6 raw px ≈ pre-fix ~37 raw px parity per plan-checker WARNING 1 fix). Updated `kPocFogSmoothCoordinateMaxDelta` from 1e3 to kPocFogIntegerWrapPeriodPx + 1 (= 1537) to defend against in-test integer-wrap event flake (per plan-checker INFO 5 fix). Noise-function inspection documented in `_FogPainter.paint()` decomposition block: hash3 lattice has period 1; integer-wrap event preserves visible noise pattern at exact noise-grid boundary (4 grid units shift) for the base octave, with documented continuity caveat for the FBM-rotated octaves. New tests: `fog_world_coordinate_noise_test.dart` (FOG-17 — 4 sub-tests across 1.064M / 4.26M / 17.04M pixelOrigin regimes asserting ZERO wraps), `fog_pixel_origin_decomposition_test.dart` (FOG-17a — 7 sub-tests asserting bounded shader input + decomposition math correctness). Rewritten tests: `fog_tile_period_invariant_test.dart` (static-source invariant flipped from Plan 03.1-07 B-3 to Plan 03.1-10 world-coordinate), `fog_debug_spiral_continuity_test.dart` (added post-Plan-03.1-10 ZERO-wraps sub-test; retained pre-fix + B-3 sub-tests as historical regression-defense). Touched tests: `fog_smooth_noise_test.dart` (line-115 magnitude assertion updated from `greaterThan(100)` to `inExclusiveRange(0, kPocFogIntegerWrapPeriodPx + 1)` per plan-checker WARNING 2 fix); `fog_pan_translation_test.dart` (FOG-09 reason string + docstring updated to acknowledge the post-FOG-17a bounded composite per plan-checker INFO 3 fix); `map_screen_test.dart` (replaced `equals(InteractiveFlag.all)` assertion with UX-02 contracts: rotate cleared, drag set, pinchZoom set). Phase 3.1 per-phase count unchanged (FOG-16 + FOG-17 + UX-02 already counted in P03.1-09 revision); UX-02 + FOG-16 + FOG-17 status flips Pending → Complete. FOG-04..07 + FOG-11 + FOG-15 retain `Falsified-in-production`; Plan 03.1-11 Walk #4 is the walk-time validation gate. PERF-08 deferred to Plan 03.1-12+ if Walk #4 surfaces it (Walk #3b confirmed PERF-08 cache-thrash persists at low pan magnitudes; not load-bearing on Walk #4 verdict per FOG-17 + UX-02 fix priority).

- **2026-05-03 (Phase 3.1, Plan 03.1-09 SUMMARY — VERDICT ITERATING-WITH-PARTIAL-PROGRESS-AND-ROTATION-REGRESSION):** Plan 03.1-09 sideload UAT Walk #3 on iPhone 17 Pro against CI run `25275979318` (SHA `0625ba5`) over central Melun delivered the **fourth** Phase 3.1 `iterating:` verdict per CONTEXT §Iteration policy (Walk #1 iterating-with-major-progress; Walk #2 iterating-with-partial-progress; Walk #3 iterating-with-partial-progress-and-rotation-regression). **Walk #3 limitation:** developer did NOT Mail-share the session log; quantitative JSONL grep correlation unavailable for frame_delta, SDF, fog_transform streams. Verbal evidence + mechanistic codebase interpretation only. **Findings:** (1) **Q1 PARTIAL — B-3 partial-fix outcome confirms Plan 03.1-07 SUMMARY framing.** Developer's verbatim *"for the steppy 'translation' it still happens but diffrently, now when panning it is smooth, but every x amount of pan you get a hard step mobing the shader."* The B-3 tile-period-aware fract reduced wrap frequency from viewport-width (~390 px) to noise-tile period (~16-65 px); panning is smooth between wrap events but stepping discontinuity persists at wrap events. Wrap MAGNITUDE in cell-grid space stays the same (`cellPx = spiralCoord * uResolution` is multiplied by full uResolution, not by the wrap divisor — Plan 03.1-07 Task 3 Rule 1 deviation), only wrap FREQUENCY changes. Developer's intent-correct iteration-path framing: *"if I pan right forever the shader should not be moved to be where I'm going, I should see a new area of the shader"* — exactly the world-coordinate-noise rewrite Plan 03.1-07 SUMMARY documented for Plan 03.1-10. (2) **Q2 NEW failure mode (rotation-correlated).** Developer's verbatim *"so the shader going outside of the screen happens when you zoom and rotate (which always happend when zooming with human finger)"*. Plan 03.1-08's `canvas.translate(-canvasOffset)` compensates ONLY translation (matrix[12], matrix[13]); rotation (matrix[0,1,4,5]) is untouched. When MobileLayerTransformer applies non-zero rotation during pinch-zoom-rotate, the fog rect rotates with the canvas leaving wedges of un-fogged map at viewport corners. Walk #2 didn't trigger this (likely the user's gestures kept rotation near zero or the larger fog-rect viewport-coverage gap masked it). Developer's preferred mitigation: *"I think we should disable rotation for now, only pan and zoom, north is up. simplier"* — UX-02 scope reduction. (3) **Q3 + Q4 implicitly retained** (no developer complaints surfaced). **Status updates:** FOG-13 demoted from Complete to **Verified-by-test-only** (Walk #3 surfaced rotation-correlated regression FOG-16 NEW; pure-translation case still passes). FOG-15 demoted to **Verified-by-test-only** (Walk #3 confirms partial-fix framing; FOG-17 NEW is the iteration path). PERF-07 implicit retention from Walk #2 baseline (no Walk #3 numerical update). PERF-08 unchanged (no Walk #3 SDF JSONL). DEBUG-01 walk-time-validated implicitly. UX-01 walk-time-validated implicitly. **Three new requirements:** **FOG-16** (fog-rect viewport coverage during canvas rotation; Plan 03.1-10), **FOG-17** (world-coordinate noise sampling + precision-mitigation pairing FOG-17a; Plan 03.1-10), **UX-02** (disable map rotation for POC; Plan 03.1-10). FOG-04..07 + FOG-11 retain `Falsified-in-production` until a subsequent Phase 3.1 walk validates the FOG-17 + FOG-16 layered fix. Phase 3.1 per-phase count grows from 11 to 14 requirements; v1 total grows from 67 to 70. **KEY LEARNING — PROGRESS WITHOUT CLOSURE:** each layered fix bundle moved the failure mode further down the symptom hierarchy (constant-zero → viewport-width-period shimmer → noise-tile-period stepping → rotation-correlated rect-cover) — measurable progress, no closure yet. Closure requires either (a) Plan 03.1-10's world-coordinate-noise rewrite (D-1 + D-1a) so wraps don't happen at all + UX-02 rotation disable, OR (b) Phase 3.2 architectural-counter-evidence pivot if D-1 also fails empirically. Phase 3.1 stays `In Progress` — Phase 4 + Phase 5 stay BLOCKED. MirkFall migration recommendation **DEFERRED**. Walk #1 + Walk #2 + Phase 3 historical records UNTOUCHED. Next action: `/gsd:plan-phase 3.1 --gaps` to author Plan 03.1-10 (world-coordinate noise + rotation handling). Evidence: `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-3.md` + `03.1-UAT-3.md`.

- **2026-05-04 (Phase 3.1 architectural insight captured — MIRK-01 deferred; Plan 03.1-11 = Walk #4 only):** Post-Walk-3 reflection clarifies the architectural intent for the MIRL fog/visual layer: it will compose multiple different shaders in the real (non-POC) MirkFall application, so per-shader spatial-input plumbing is not maintainable. **MIRK-01 = standardized ABI between `_FogPainter` and any conforming shader** (camera-derived inputs forwarded through standard uniforms; explicit camera↔shader coupling accepted; non-periodic shaders supported). **MIRK-01 is captured as a deferred future concern, NOT a Plan 03.1-11 deliverable** — per developer's clarification: *"the architecture is malleable, I don't really care about the architecture for now, it can be made better later, once we find how to solve our issue, for now we don't even know how to make it work so defining an architecture seems too early"*. **FOG-17 + FOG-17a retain `Complete` status** — re-classified as the seed implementation aligned with MIRK-01 (the existing `uPixelOrigin` uniform IS conceptually the standardized ABI input; FOG-17a CPU-side decomposition IS the architectural precision computation). **No shader rollback** — the Plan 03.1-10 implementation stays as-is. **UX-02 retains Complete**. Pre-Plan-03.1-10 main was renamed to `old` (preserves Plans 03.1-11 → 03.1-16 escalation history that converged on DENIED-final via further shader rewrites — archival reference only); current `main` reset to `b31766f`. New requirement MIRK-01 added under `### Architecture` section as **deferred captured insight**. **Plan 03.1-11 scope** = iPhone 17 Pro Walk #4 sideload UAT validating the existing Plan 03.1-10 fix bundle (per the original 2026-05-03 framing of `03.1-11-PLAN.md`). MIRK-01 formalization revisited post-Walk-4 verdict if/when empirical validation lands. v1 total grows from 70 to 71; Phase 3.1 count from 14 to 15.

  **Framing iteration log:** initially mis-framed MIRK-01 as "shader receives no spatial input; map-locking via pure Canvas transforms" — topologically impossible for non-periodic animated map-locked shaders without an explicit coupling. Then over-corrected to "MIRK-01 ABI formalization is a Plan 03.1-11 deliverable (Tasks A+B+C+D)" — premature optimization since the empirical validation (Walk #4) is still pending. Final framing: MIRK-01 is a captured architectural insight; Plan 03.1-11 stays narrowly focused on Walk #4 sideload UAT.

- **2026-05-04 (Phase 3.1, Plan 03.1-11 SUMMARY — VERDICT ITERATING-WITH-MAJOR-PROGRESS):** Plan 03.1-11 sideload UAT Walk #4 on iPhone 17 Pro against CI run `25305105125` (SHA `b31766f`) over central Melun delivered the **fifth** Phase 3.1 `iterating:` verdict per CONTEXT §Iteration policy (Walk #1 iterating-with-major-progress; Walk #2 iterating-with-partial-progress; Walk #3 iterating-with-partial-progress-and-rotation-regression; Walk #4 iterating-with-major-progress). Walk #4 session log Mail-shared (LOG-04 round-trip discipline RE-ESTABLISHED — `WALK-3-NO-LOG-MAIL-SHARE` blocker resolved); 42-second post-rebuild walk window covering both default-zoom (pxOriginX ~1.064M) and max-zoom (pxOriginX ~4.26M) regimes; 40 frame_delta + 28 SDF INFO + 40 fog_transform + 2 dev_marker rollups. **Findings:** (1) **Q1 99% RESOLVED at default zoom** — FOG-17 world-coordinate noise + FOG-17a Dart-side decomposition working as designed; zero dev_markers fired in default-zoom rollups 0-5 (vs Walk #3b's 8 markers at the same regime); developer's verbatim *"hard step non existent at default zoom level"*. (2) **Q1 PARTIAL at max zoom only** — 2 dev_markers fired at pxOriginX ~4.26M (Walk #2's precision-degradation regime); developer's verbatim *"once zoomed at maximum (closest to ground) hard step exists while panning"*. Mechanism candidates: M1 FBM-rotated-octave continuity caveat at integer-wrap boundary / M2 sub-pixel jitter in FOG-17a modulo at extreme zoom / M3 mod() semantics at GLSL-fp32 boundary. Candidate fixes: C1 periodic Worley/explicit-period Perlin / C2 widen kPocFogIntegerWrapPeriodPx / C3 per-octave precision-aware decomposition. (3) **Q1b NEW failure mode surfaced** — zoom-gesture-correlated very-fast hard-step ("seed of procedural pattern fast"). Mechanism: world-coordinate noise sampling gives noise-value continuity in pure-pan space but NOT in zoom-gesture space (uPixelOrigin jumps O(1M) raw px between zoom-snap boundaries; rollups 20-24 confirmed sweep 4.10M → 0.84M → 0.81M in ~5 sec). Candidate fixes: C-Q1b-1 seed-stable noise across zoom (project pixelOrigin → lat/lon, sample noise in lat/lon space) / C-Q1b-2 interpolate pixelOrigin across zoom-snap / C-Q1b-3 accept as POC-acceptable. (4) **Q2 RESOLVED** — UX-02 walk-time-validated; canvasTx/Ty identically 0.000000 across all 40 fog_transform rollups; developer's verbatim *"rotation disable as wanted"*. (5) **Q3 + Q4 retained** — *"fog translate perfectly with map during pan (except for the hard steps)"*. (6) **Out-of-scope developer request** captured: disable MapOptions.cameraConstraint to stress-test integer-wrap behaviour at extreme world coordinates (~100 km from origin); diagnostic-tool request for Plan 03.1-12+. **Status updates:** UX-02 + FOG-16 path (a) flip from `Complete — Verified-by-test` to `Complete — Verified-by-test + walk-time validated (P03.1-11 Walk #4)`. FOG-17 flips from `Complete — Verified-by-test` to `Complete — Verified-by-test + walk-time validated AT DEFAULT ZOOM (P03.1-11 Walk #4)`. PERF-07 re-measured (median-of-medians 0.243 ms / max-of-p95s 5.020 / max-of-maxes 9.514; ~66× / ~6.4× / ~5.0× headroom; consistent with Walks #1 + #2 + Walk #3b). FOG-04..07 + FOG-11 + FOG-15 retain `Falsified-in-production` with revision-clauses pointing at `03.1-FALSIFICATION-4.md` (residual high-zoom Q1 + Q1b zoom-gesture). **KEY FOG-17a SIGNATURE CONFIRMED:** uOffsetXMax/YMax bounded in [85.14, 1535.74] / [41.64, 1534.63] across ALL 40 rollups (FOG-17a precision pairing in effect at every paint, even at pixelOriginX 4.26M; fp32 ULP at 1537 ≈ 2.4e-4 raw px vs ULP at 4.26M ≈ 0.51 raw px). **No new requirement IDs added** (Q1 max-zoom residual + Q1b zoom-gesture surface as candidate Plan 03.1-12+ axes; this plan does NOT author 03.1-12). v1 total stays 71. Phase 3.1 stays `In Progress (5th iteration; Walk #5 pending in Plan 03.1-12+ follow-up)`. Phase 4 + Phase 5 STAY BLOCKED. MirkFall migration recommendation **DEFERRED until Walk #5**. Walks #1 + #2 + #3 + Walk #3b + Phase 3 historical records UNTOUCHED. Evidence: `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-4.md` + `03.1-UAT-4.md` + Mail-shared session log `20260504T105024Z_logs.txt` (703 KB). Next action: `/gsd:plan-phase 3.1 --gaps` in a follow-up plan-phase invocation to author Plan 03.1-12 with Walk #4's diagnostic stream as input.

- **2026-05-04 (Phase 3.1 CLOSURE, Plan 03.1-15 SUMMARY — VERDICT CONFIRMED-AFTER-FIX FULL — PHASE 3.1 CLOSES):** Plan 03.1-15 sideload UAT Walk #6 on iPhone 17 Pro against CI run `25338670208` (SHA `d753176`) delivered the **seventh** Phase 3.1 verdict (Walk #1 iterating-with-major-progress / Walk #2 iterating-with-partial-progress / Walk #3 iterating-with-partial-progress-and-rotation-regression / Walk #3b implicit marker analysis / Walk #4 iterating-with-major-progress / Walk #5 confirmed-after-fix-partial / **Walk #6 CONFIRMED-AFTER-FIX FULL — Phase 3.1 closes**). Developer's verbatim chat verdict: *"APPROVED / numbered shader is broken : but I don't care / 100% solved for panning and zoomin on mirk shader"*. **Findings:** **Q1 SNAP retained 100% from Walk #5** (Plan 03.1-14 made no changes to FOG-18 path). **Q1b CLOSED via FOG-19 C-b uZoomScale uniform** — developer's *"100% solved for... zoomin on mirk shader"* directly resolves Walk #5's *"steppy displacement... numbers sliding... incorrect scaling?"*; the DIVIDING form `worldPx / (kNoiseTilePx * uZoomScale)` (where `uZoomScale = pow(2, currentZoom - kPocFogReferenceZoom = 13.0)`) anchors noise samples to lat/lng during zoom; previous-iteration multiplicative form (algebraically equivalent to `pixelOrigin + size/2`) was wrong per Plan-checker Blocker #1. **Q2/Q3/Q4 retained** (UX-02 walk-time-validated 3rd consecutive walk; reveal-hole co-located; fog translates with map). **Q5 / DEBUG-02 explicit ≥7M:** implicitly validated via verbal verdict OR carried forward from Walk #5 baseline (does not block FULL closure). **PERF-07 retained** at Walk #5 levels (no Mail-shared re-measurement; minimal arithmetic added by FOG-19 hot-path division). **Mail-share NOT performed for Walk #6** (verbal verdict decisive). **Q6 NEW (DEBUG-03 quantitative cell-drift) IMPLICITLY ZERO via verbal verdict** — DEBUG-03 numbered-shader rendering is BROKEN (Plan 03.1-14 Task A regression); developer explicitly waived diagnostic with *"numbered shader is broken : but I don't care"*; assessed Q1b closure via subjective production-fog observation instead, which is the actual goal. **Status updates:** FOG-04 + FOG-05 + FOG-06 + FOG-07 + FOG-11 + FOG-15 flip from `Falsified-in-production` to `Complete — Verified-by-walk (P03.1-15 Walk #6 CONFIRMED-AFTER-FIX FULL)`. FOG-19 + FOG-18 + DEBUG-01 + DEBUG-02 flip to `Complete — Verified-by-test + walk-time validated`. UX-02 + FOG-16 + FOG-17 walk-time-validated 3rd consecutive walk. PERF-07 retained at Walk #5 levels. **DEBUG-03 status: Complete with known defect** (debug-spiral 4-digit rendering regression explicitly waived by developer; cleanup deferred indefinitely; debug-shader-only; no production impact). **Plan 03-08 DENIED verdict (2026-05-01) REVERSED.** The original Phase 3 same-Canvas fog hypothesis is REINSTATED. **MirkFall migration recommendation:** **PORT BACK** with the layered Plan 03.1-02 + 03.1-04 + 03.1-05 + 03.1-07 + 03.1-08 + 03.1-10 + 03.1-12 + 03.1-14 fix bundle. Phase 4 + Phase 5 UNBLOCK and transition to `Pending` status. v1 total stays 75. Phase 3.1: 15 of 15 plans complete. Walks #1-#5 + Walk #3b + Phase 3 historical records UNTOUCHED. CLAUDE.md `# current best version` section LEFT UNTOUCHED (developer-managed). Plan 03.1-16+ NOT authored — closure scope. Evidence: `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-6.md` + `03.1-UAT-6.md`. Next action: `/gsd:discuss-phase 4` to begin Phase 4 (Wisp Particles).

- **2026-05-04 (Phase 3.1, Plan 03.1-13 SUMMARY — VERDICT CONFIRMED-AFTER-FIX-PARTIAL):** Plan 03.1-13 sideload UAT Walk #5 on iPhone 17 Pro against CI run `25320729576` (SHA `960f5c2`) over central Melun delivered the **sixth** Phase 3.1 verdict (Walk #1 iterating-with-major-progress; Walk #2 iterating-with-partial-progress; Walk #3 iterating-with-partial-progress-and-rotation-regression; Walk #4 iterating-with-major-progress; Walk #5 **CONFIRMED-AFTER-FIX-PARTIAL** — NEW intermediate verdict introduced by Plan 03.1-13). 92-second active walk window (Mail-shared session log `20260504T171339Z_logs.txt` 1.95 MB; 4 JSONL streams extracted: 80 frame_delta + 80 fog_transform + 53 SDF INFO rollups + ZERO dev_markers). **Findings:** (1) **Q1 SNAP CLOSED at MAX zoom** — ~35 sec total sustained at pxOriginX 4.255M-4.258M (two windows: 14 sec rollups 9-22 + 21 sec rollups 35-55) with ZERO dev_markers fired; Walk #4 baseline was 2 markers in 2.36 sec at the same regime. Developer's verbatim: *"panning 100% solved, no hard step at any level of zoom"*. **FOG-17a-wrap-firing-visibly mechanism interpretation EMPIRICALLY CONFIRMED.** (2) **FOG-18 JSONL signature CONFIRMED:** `uOffsetXMedian == pixelOriginXMedian` 80/80 rollups (zero mismatches; modulo gone, basis derivation NOT introduced — C2' deferred). `uOffsetXMax` reaches **4258352.933 raw px** (Walk #4 was bounded under 1537 under FOG-17a). (3) **Q1b residual still present (EXPECTED — C2' / FOG-19 deferred):** developer's verbatim *"zoom still show same steppy displacement behavior in fog, in number we can see the numbers sliding"* + CLAUDE.md *"heavy hard steps while zooming / incorrect scaling"*. Captured as empirical baseline for the C2' follow-up plan. (4) **Q2 retained (UX-02 walk-time-validated 2nd consecutive walk)** — canvasTx/Ty identically 0.000000 across all 80 rollups. (5) **Q3 + Q4 retained** — *"panning 100% solved"* implicitly retains FOG-12 + FOG-13. (6) **C3' / DEBUG-02 walk-time-validation deferred to Walk #6** — max pxOriginX reached 4.26M (= MAX zoom at Melun); ≥7M extreme-distance threshold (~50-100 km from Melun) NOT crossed. Structural fix mechanically present in IPA. (7) **Diagnostic-tool feedback for C2' follow-up plan:** developer's verbatim *"modifying the number to not have repetitive value would allow us to debug the amount of drift"* — input for C2' plan's diagnostic-infrastructure axis (replace repetitive 0-9 digit-cycling in debug-spiral with unique per-cell values for quantitative drift measurement). **PERF-07 re-measured** (medianMs 1.228 / p95Ms 1.591 / maxMs 1.724 across 80 rollups; 13× / 20× / 28× headroom). **Status updates:** FOG-18 flips from `Complete — Verified-by-test` to **`Complete — Verified-by-test + walk-time validated (P03.1-13 Walk #5)`**; DEBUG-02 retains `Complete — Verified-by-test` with deferred-walk-time-validation note. UX-02 + FOG-16 + FOG-17 re-validated for the 2nd consecutive walk (Walk #5 confirms Walk #4). FOG-04..07 + FOG-11 + FOG-15 stay `Falsified-in-production` with revision-clauses pointing at `03.1-FALSIFICATION-5.md` (Q1b residual is the documented gap; resolves at Walk #6 after C2' follow-up plan lands). **No new requirement IDs added** (Q1b feeds the C2' follow-up plan via `gaps:` `Q1B-ZOOM-GESTURE-RESIDUAL` + `DEBUG-SPIRAL-UNIQUE-CELL-VALUES` + `C3-EXTREME-DISTANCE-WALK-TIME-VALIDATION` in `03.1-UAT-5.md`; this plan does NOT author Plan 03.1-14 per scope). v1 total stays 73. Phase 3.1 stays `In Progress (6th iteration; awaiting C2' plan + Walk #6)`. Phase 4 + Phase 5 STAY BLOCKED. MirkFall migration recommendation **DEFERRED until Walk #6**. Walks #1 + #2 + #3 + Walk #3b + Walk #4 + Phase 3 historical records UNTOUCHED. CLAUDE.md `# current best version` section LEFT UNTOUCHED (developer-managed). Evidence: `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md` + `03.1-UAT-5.md` + Mail-shared session log `20260504T171339Z_logs.txt` (1.95 MB). Next action: `/gsd:discuss-phase 3.1` (separate invocation) to resolve dimensional-mismatch design question (refZoom-px basis vs screen-px uResolution; options refZoom-px-uResolution-rename / uZoomScale uniform / Dart-zoom-snap-interpolation / lat-lng-anchored basis), then `/gsd:plan-phase 3.1 --gaps` to author the C2' follow-up plan.

---
*Requirements defined: 2026-04-30*
*Last updated: 2026-05-04 — FOG-19 added (Plan 03.1-14 Task B — C-b uZoomScale uniform at slot 41 + Dart-side `pow(2, camera.zoom - kPocFogReferenceZoom)` forwarding; anchors fog noise samples to lat/lng during zoom transitions per Walk #5 Q1b residual). DEBUG-03 added (Plan 03.1-14 Task A — unique 4-digit per-cell encoding in `atmospheric_fog_debug_spiral.frag` for Walk #6 quantitative drift measurement per developer's verbatim Walk #5 request *"modifying the number to not have repetitive value would allow us to debug the amount of drift"*). DEBUG-02 added (Plan 03.1-12 Task 2). FOG-18 added (Plan 03.1-12 Task 1). 75 v1 total.*
