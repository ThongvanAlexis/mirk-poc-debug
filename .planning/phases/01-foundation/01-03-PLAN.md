---
phase: 01-foundation
plan: 03
type: execute
wave: 0
depends_on: []
files_modified:
  - lib/domain/revealed/reveal_disc.dart
  - lib/domain/mirk/mirk_viewport_bbox.dart
  - lib/infrastructure/mirk/sdf/revealed_sdf_builder.dart
  - lib/infrastructure/mirk/tile_cell_iteration.dart
  - lib/infrastructure/mirk/mirk_projection.dart
  - lib/infrastructure/mirk/shader/fog_shader_uniforms.dart
  - lib/infrastructure/mirk/animation_helpers.dart
  - .github/workflows/ci.yml
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - BOOT-08
  - CI-01
  - CI-02
  - CI-03
  - CI-04
  - CI-05
  - LOG-05

must_haves:
  truths:
    - "Seven donor `.dart` files from MirkFall parent project land verbatim in the POC at the paths RESEARCH.md §Recommended Project Structure prescribes; each carries the GOSL 3-line header"
    - "All donor files compile cleanly (`flutter analyze` exits 0) — they are dormant in Phase 1 (not imported by `lib/main.dart` chain) but pass the analyzer"
    - "`.github/workflows/ci.yml` defines three jobs (`gates`, `android`, `ios`) per RESEARCH.md §Pattern 7 with `flutter-version: 3.41.7` (locked for parent parity) and `runs-on: macos-14` (locked for reproducibility)"
    - "REQUIREMENTS.md LOG-05 wording updated: drops the '50 MB synthetic-log smoke test' specification per CONTEXT.md `Phase 1 UAT exit gate` decision"
    - "After first push to `main`, GitHub Actions surfaces three jobs; the APK + IPA artifacts are downloadable from the run page (CI-03, CI-04, CI-05)"
  artifacts:
    - path: lib/domain/revealed/reveal_disc.dart
      provides: RevealDisc value class (Phase 3 fog input)
    - path: lib/domain/mirk/mirk_viewport_bbox.dart
      provides: MirkViewportBbox helper for fog SDF rect derivation
    - path: lib/infrastructure/mirk/sdf/revealed_sdf_builder.dart
      provides: RevealedSdfBuilder.buildFromDiscs (Phase 3 SDF generator)
    - path: lib/infrastructure/mirk/tile_cell_iteration.dart
      provides: Tile-cell iteration helpers (Phase 3 fog clip-path computation)
    - path: lib/infrastructure/mirk/mirk_projection.dart
      provides: MirkProjection — lat/lon ↔ pixel projection with the metres-per-degree-lat correction
    - path: lib/infrastructure/mirk/shader/fog_shader_uniforms.dart
      provides: FogShaderUniforms — populates the 41 floats + 1 sampler for atmospheric_fog.frag
    - path: lib/infrastructure/mirk/animation_helpers.dart
      provides: Easing/interpolation helpers used by Phase 3+ animation
    - path: .github/workflows/ci.yml
      provides: Three-job GitHub Actions workflow (gates → android + ios)
      contains: "gates:"
    - path: .planning/REQUIREMENTS.md
      provides: Updated LOG-05 wording dropping the 50 MB synthetic-log spec
  key_links:
    - from: lib/infrastructure/mirk/sdf/revealed_sdf_builder.dart
      to: lib/infrastructure/mirk/mirk_projection.dart
      via: "imports MirkProjection for pixel↔latlng conversion"
      pattern: "MirkProjection"
    - from: lib/infrastructure/mirk/sdf/revealed_sdf_builder.dart
      to: lib/domain/revealed/reveal_disc.dart
      via: "RevealedSdfBuilder.buildFromDiscs takes List<RevealDisc>"
      pattern: "RevealDisc"
    - from: .github/workflows/ci.yml
      to: tool/check_headers.dart + tool/check_licenses.dart + tool/check_dependencies_md.dart
      via: "gates job runs all three scripts"
      pattern: "tool/check_headers.dart"
---

<objective>
Land the seven BOOT-08 donor `.dart` files (verbatim from `C:\claude_checkouts\GOSL-MirkFall`) into the POC's `lib/` tree at the RESEARCH.md-prescribed paths. Each file passes `flutter analyze` despite being unreferenced by application code (analyzer doesn't flag unused FILES, only unused declarations within files — RESEARCH.md Pitfall G). Author the three-job GitHub Actions CI workflow (`.github/workflows/ci.yml`) per RESEARCH.md §Pattern 7 with the planner-locked decisions: Flutter 3.41.7 for parent parity, macOS-14 runner for reproducibility, JDK 21 for forward-compat. Update REQUIREMENTS.md LOG-05 to drop the 50 MB synthetic-log spec per CONTEXT.md.

Purpose: BOOT-08 is the largest port-from-parent surface (seven files, hundreds of lines). Landing them in Wave 0 stabilizes the donor-tree structure + GOSL-header check + analyzer pass before any active code wave starts touching them. The CI workflow lands now so that the first push to `main` from Plan 07 immediately exercises all three jobs (gates/android/ios) and surfaces CI-03/CI-04/CI-05 artifacts. The REQUIREMENTS.md LOG-05 update enacts the locked CONTEXT.md decision before Plan 04 ports the FileLogger.
Output: Seven donor files in place + analyzer green; CI workflow file present; REQUIREMENTS.md LOG-05 wording corrected.
</objective>

<execution_context>
@C:/Users/oliver/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oliver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/research/STACK.md
@CLAUDE.md

<interfaces>
<!-- Parent file paths (verified all present): -->
<!--   C:\claude_checkouts\GOSL-MirkFall\lib\domain\revealed\reveal_disc.dart -->
<!--   C:\claude_checkouts\GOSL-MirkFall\lib\domain\mirk\mirk_viewport_bbox.dart -->
<!--   C:\claude_checkouts\GOSL-MirkFall\lib\infrastructure\mirk\sdf\revealed_sdf_builder.dart -->
<!--   C:\claude_checkouts\GOSL-MirkFall\lib\infrastructure\mirk\tile_cell_iteration.dart -->
<!--   C:\claude_checkouts\GOSL-MirkFall\lib\infrastructure\mirk\mirk_projection.dart -->
<!--   C:\claude_checkouts\GOSL-MirkFall\lib\infrastructure\mirk\shader\fog_shader_uniforms.dart -->
<!--   C:\claude_checkouts\GOSL-MirkFall\lib\infrastructure\mirk\animation_helpers.dart -->
<!-- Adaptation guidance per RESEARCH.md Pitfall G: -->
<!--   - Each donor file MAY import packages NOT yet in Phase 1's pubspec (e.g. parent's MapView abstraction in lib/domain/map/map_view.dart) -->
<!--   - Phase 1 pubspec already declares flutter_map 7.0.2 + latlong2 0.9.1 (per Plan 01) — donor files importing these are fine -->
<!--   - If a donor file imports parent-specific code (lib/domain/map/map_view.dart, freezed-generated files, etc.), the executor MUST adapt: either stub the missing class in lib/config/constants.dart or refactor the import. The simplest donor file (reveal_disc.dart — value class) should be ported FIRST to flush out the adaptation pattern; subsequent files reuse the pattern. -->
<!-- CI workflow source: RESEARCH.md §Pattern 7 (lines 555-655) — POC-stripped from parent C:\claude_checkouts\GOSL-MirkFall\.github\workflows\ci.yml. -->
<!-- Planner-locked CI choices per RESEARCH.md §Open Questions: -->
<!--   - Flutter version: 3.41.7 (NOT 3.41.8) — parent parity, code-donor mandate -->
<!--   - macOS runner: macos-14 (NOT macos-latest, NOT macos-26) — reproducible Xcode 16 / iOS 18 SDK; POC has no device_info_plus forcing macos-26 -->
<!--   - JDK: 21 (forward-compat, parent uses 21 already) -->
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port the seven BOOT-08 donor files verbatim with import-path adaptations</name>
  <files>lib/domain/revealed/reveal_disc.dart, lib/domain/mirk/mirk_viewport_bbox.dart, lib/infrastructure/mirk/sdf/revealed_sdf_builder.dart, lib/infrastructure/mirk/tile_cell_iteration.dart, lib/infrastructure/mirk/mirk_projection.dart, lib/infrastructure/mirk/shader/fog_shader_uniforms.dart, lib/infrastructure/mirk/animation_helpers.dart</files>
  <action>
Port seven files from parent. **Order matters** to flush out adaptation patterns: simplest files first (value classes), then files that import them.

**Recommended port order (simplest → most coupled):**

1. **`lib/domain/revealed/reveal_disc.dart`** ← `C:\claude_checkouts\GOSL-MirkFall\lib\domain\revealed\reveal_disc.dart`
   - Value class `RevealDisc` (likely just lat, lon, radiusMetres + constructor + equality).
   - Likely imports: `package:latlong2/latlong.dart` for `LatLng`. Phase 1 pubspec has `latlong2: 0.9.1` declared — fine.
   - If it uses `freezed` (generated file: `reveal_disc.freezed.dart`), the POC does NOT have freezed in pubspec. **Adaptation:** rewrite as a hand-rolled immutable class with `==`, `hashCode`, `toString`. RESEARCH.md §Standard Stack §NOT included confirms freezed is dropped.

2. **`lib/infrastructure/mirk/animation_helpers.dart`** ← `C:\claude_checkouts\GOSL-MirkFall\lib\infrastructure\mirk\animation_helpers.dart`
   - Easing/interpolation helpers — pure Dart, no external deps. Should port verbatim.

3. **`lib/infrastructure/mirk/mirk_projection.dart`** ← parent same path
   - Lat/lon ↔ pixel projection with the metres-per-degree-lat correction (RESEARCH.md Pitfall 2 root defense).
   - Imports `kMetersPerDegreeLat`, `kEarthRadiusMeters` from `lib/config/constants.dart` — already landed in Plan 01.
   - Likely imports `package:latlong2/latlong.dart` — fine.

4. **`lib/infrastructure/mirk/tile_cell_iteration.dart`** ← parent same path
   - Tile-cell iteration helpers. May import `MirkProjection` from step 3.

5. **`lib/domain/mirk/mirk_viewport_bbox.dart`** ← parent same path
   - Viewport bounding box helper. If freezed-generated, hand-roll like step 1.
   - Imports `package:latlong2/latlong.dart`.

6. **`lib/infrastructure/mirk/shader/fog_shader_uniforms.dart`** ← parent same path
   - Populates the 41 float uniforms + 1 sampler for `atmospheric_fog.frag` (Plan 01 bundled the shader).
   - Imports `package:flutter/...` (likely `dart:ui` for `FragmentProgram`/`Image`). Phase 1 pubspec has Flutter SDK — fine.
   - May import `MirkProjection`, `MirkViewportBbox`, `RevealDisc`. All ported above.

7. **`lib/infrastructure/mirk/sdf/revealed_sdf_builder.dart`** ← parent same path
   - The `RevealedSdfBuilder.buildFromDiscs` SDF generator. Most coupled file: imports `RevealDisc`, `MirkProjection`, `MirkViewportBbox`, `tile_cell_iteration`, `kMirkFog*` constants from `lib/config/constants.dart`.
   - This is the file most likely to surface unmet imports — port LAST so the previous six are already in place.

**Adaptation rules (RESEARCH.md Pitfall G):**

a. **Missing parent imports.** If a donor file imports `package:gosl_mirkfall/...` or `lib/domain/map/map_view.dart` (parent's `MapView` abstraction layer not in POC), the executor must:
   - For type imports (`MapView`, `MapTheme`, etc.): add a minimal stub in `lib/domain/map/map_view.dart` ONLY if needed. POC's stance per RESEARCH.md is "talk to flutter_map directly; abstraction is a migration concern" — if a donor file uses `MapView`, replace the type with the underlying `MapCamera` from `flutter_map` package.
   - For freezed-generated `*.freezed.dart` imports: rewrite the donor class hand-rolled (no freezed in POC pubspec).

b. **Missing constants.** If a donor file references `kFooBar` not yet in `lib/config/constants.dart` (Plan 01 ported the listed subset only), add the constant to `lib/config/constants.dart` with the same value as parent.

c. **GOSL header preservation.** Each donor file ALREADY starts with the GOSL 3-line header. Preserve verbatim.

d. **Package name in imports.** Parent files use `package:gosl_mirkfall/...` for cross-file imports within `lib/`. POC's package name is `mirk_poc_debug` (from Plan 01's `pubspec.yaml`). Rewrite all `package:gosl_mirkfall/` → `package:mirk_poc_debug/`.

**Linter exceptions:** these donor files are dormant in Phase 1 (no `lib/main.dart` import path reaches them). `flutter analyze` does NOT flag unused FILES, only unused declarations within files — so passes. If analyzer surfaces "unused_import" warnings WITHIN a donor file (e.g. an import that the donor uses but only via dead code branches), DO NOT add `// ignore:` comments — investigate; the donor is supposed to be self-consistent. If it's truly dead code in the donor, that's a parent-project bug to file later, not a POC concern.

**Reading guidance:** for each donor file, READ the parent's full content first (use `Read` tool). Some donor files may be 200+ lines. Identify all imports, all referenced constants, all referenced types. Plan adaptations BEFORE writing.
  </action>
  <verify>
    <automated>cd "C:/claude_checkouts/mirk-poc-debug" &amp;&amp; flutter analyze --fatal-infos --fatal-warnings &amp;&amp; dart run tool/check_headers.dart 2>&amp;1 | grep -v "lib/main.dart"</automated>
  </verify>
  <done>
- All seven files exist at the prescribed paths
- Each starts with the GOSL 3-line header
- `flutter analyze --fatal-infos --fatal-warnings` exits 0 across the donor tree
- `dart run tool/check_headers.dart` passes for the donor files (only known violation: `lib/main.dart` from `flutter create`, fixed in Plan 07)
- Any adaptation made (parent → POC import path, freezed → hand-rolled, constant added) is documented in the commit message
  </done>
</task>

<task type="auto">
  <name>Task 2: Author .github/workflows/ci.yml — three-job CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Author the GitHub Actions workflow per RESEARCH.md §Pattern 7 lines 555-655, with the planner-locked CI choices.

**Locked planner decisions** (NOT discretion at execution):
- `flutter-version: '3.41.7'` (matches parent — code-donor mandate per RESEARCH.md §Open Questions #1)
- `runs-on: macos-14` for the iOS job (NOT `macos-latest`, NOT `macos-26` — reproducible Xcode 16 / iOS 18 SDK per RESEARCH.md §Open Questions #2; POC has no `device_info_plus` forcing macos-26)
- `java-version: '21'` for the Android job (forward-compat with future deps; matches parent)
- `concurrency:` block to cancel in-flight runs on rapid pushes (saves macos-14 minutes)

**File content** (write verbatim, modulo the version pins above):
```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  gates:
    name: Lint / License / Headers / Deps
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.41.7'
          channel: stable
          cache: true
      - run: flutter pub get
      - name: Format check
        run: dart format --line-length 160 --set-exit-if-changed .
      - name: Analyzer
        run: flutter analyze --fatal-infos --fatal-warnings
      - name: GOSL header check
        run: dart run tool/check_headers.dart
      - name: License allow-list check
        run: dart run tool/check_licenses.dart
      - name: DEPENDENCIES.md freshness check
        run: dart run tool/check_dependencies_md.dart
      - name: Tool tests
        run: dart test tool/test/
      - name: Flutter tests
        run: flutter test
      - name: LICENSE file present + GOSL v1.0
        run: |
          test -f LICENSE
          grep -q "Good Old Software License" LICENSE

  android:
    name: Build Android APK (debug)
    needs: gates
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.41.7'
          channel: stable
          cache: true
      - run: flutter pub get
      - run: flutter build apk --debug
      - uses: actions/upload-artifact@v4
        with:
          name: mirk-poc-debug-android-debug-apk
          path: build/app/outputs/flutter-apk/app-debug.apk
          if-no-files-found: error
          retention-days: 14

  ios:
    name: Build iOS (no-codesign, sideloadable)
    needs: gates
    runs-on: macos-14
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.41.7'
          channel: stable
          cache: true
      - run: flutter pub get
      - name: Remove placeholder Podfile.lock (Windows-dev bootstrap)
        working-directory: ios
        run: |
          if [ -f Podfile.lock ] && ! grep -q "^COCOAPODS:" Podfile.lock; then
            echo "::warning::Placeholder Podfile.lock detected — removing before pod install"
            rm -f Podfile.lock
          fi
      - name: CocoaPods install
        working-directory: ios
        run: pod install
      - name: Build iOS (no-codesign)
        run: flutter build ios --release --no-codesign
      - name: Package unsigned IPA for sideloading
        run: |
          set -euo pipefail
          BUILD_DIR="build/ios/iphoneos"
          if [ ! -d "$BUILD_DIR/Runner.app" ]; then
            echo "::error::Expected Runner.app under $BUILD_DIR after flutter build ios"
            exit 1
          fi
          WORK_DIR="$(mktemp -d)"
          mkdir "$WORK_DIR/Payload"
          cp -R "$BUILD_DIR/Runner.app" "$WORK_DIR/Payload/"
          (cd "$WORK_DIR" && zip -qr "$GITHUB_WORKSPACE/mirk-poc-debug-unsigned.ipa" Payload)
      - uses: actions/upload-artifact@v4
        with:
          name: mirk-poc-debug-ios-unsigned-ipa
          path: mirk-poc-debug-unsigned.ipa
          if-no-files-found: error
          retention-days: 14
```

**Notes:**
- The `gates` job depends on Plan 02 having landed `tool/check_headers.dart`, `tool/check_licenses.dart`, `tool/check_dependencies_md.dart`, and `tool/test/`. All landed.
- The `gates` job's `flutter test` step runs every test under `test/` — currently includes Plan 01's `test/assets/asset_bundle_test.dart` and Plan 02's `test/tooling/info_plist_keys_test.dart`. Plans 04-07 add more tests; the workflow auto-picks them up.
- The `gates` job WILL FAIL on first push until Plan 07 replaces `lib/main.dart` (`flutter create` boilerplate lacks GOSL header). This is an expected red-then-green progression. Document in commit message.
- `if-no-files-found: error` on artifact uploads — surfaces a regression as build failure rather than silent "artifact missing." Critical for CI-05 verification.
- The `macos-14` runner has Xcode 16 + iOS 18 SDK. Sufficient for `flutter build ios --release --no-codesign` with our deps (no `device_info_plus`).
- The `ios` job uses `--release` (NOT `--debug`) per parent ci.yml convention. The artifact name is `mirk-poc-debug-ios-unsigned-ipa` — Plan 07's UAT instructions reference this exact name.

**Do NOT add:**
- A `pull_request` artifact-publish step (artifacts only on push to main)
- Notification webhooks (CLAUDE.md telemetry policy)
- Caching beyond `subosito/flutter-action@v2`'s `cache: true` (no extra caches needed for Phase 1's small surface)
- Self-hosted runners (use GitHub-hosted only — reproducibility)
  </action>
  <verify>
    <automated>cd "C:/claude_checkouts/mirk-poc-debug" &amp;&amp; test -f .github/workflows/ci.yml &amp;&amp; grep -q "flutter-version: '3.41.7'" .github/workflows/ci.yml &amp;&amp; grep -q "runs-on: macos-14" .github/workflows/ci.yml &amp;&amp; grep -q "name: mirk-poc-debug-android-debug-apk" .github/workflows/ci.yml &amp;&amp; grep -q "name: mirk-poc-debug-ios-unsigned-ipa" .github/workflows/ci.yml</automated>
  </verify>
  <done>
- `.github/workflows/ci.yml` exists with three jobs: `gates`, `android`, `ios`
- `flutter-version: '3.41.7'` pinned across all three jobs
- `runs-on: macos-14` on the iOS job
- `java-version: '21'` on the Android job
- Concurrency block present (`cancel-in-progress: true`)
- Both artifact upload steps use `if-no-files-found: error`
- The artifact names match what Plan 07's UAT instructions reference: `mirk-poc-debug-android-debug-apk` and `mirk-poc-debug-ios-unsigned-ipa`
  </done>
</task>

<task type="auto">
  <name>Task 3: Update REQUIREMENTS.md LOG-05 wording — drop the 50 MB synthetic-log spec</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Per CONTEXT.md `Phase 1 UAT exit gate` decision: "Update REQUIREMENTS.md LOG-05 wording during Phase 1 planning to drop the '50 MB synthetic-log' specification".

1. **Read** `.planning/REQUIREMENTS.md`. Find the LOG-05 line (line 85 per the existing file structure):
```markdown
- [ ] **LOG-05**: The share sheet works on a SideStore-sideloaded iOS build with the iOS Mail app as the share target (Phase 1 smoke test confirms this)
```

2. **Replace with the softened wording** that aligns with CONTEXT.md decision:
```markdown
- [ ] **LOG-05**: The share sheet works on a SideStore-sideloaded iOS build with the iOS Mail app as the share target. Phase 1 UAT exit gate: developer sideloads the IPA, taps share-logs, picks Mail, sends to themselves, verifies the email arrives with the gzipped log file as attachment. Verbal "approved" is the gate (no synthetic-log smoke test required per Phase 1 CONTEXT.md decision).
```

3. **Add a brief footer note** below the requirements section (or in a new `## Revisions` section if one doesn't exist) documenting the change:
```markdown
## Revisions

- **2026-04-30 (Phase 1 planning):** LOG-05 wording softened — dropped the "50 MB synthetic-log smoke test" specification per CONTEXT.md `Phase 1 UAT exit gate` decision. The replacement gate is verbal "approved" after a single sideload + Mail round-trip walk.
```

4. **Verify** by re-reading the file: the LOG-05 row no longer mentions "50 MB" or "synthetic-log"; the Revisions section is present.

**Do NOT modify** any other requirement row, the traceability table, or the Out-of-Scope section. This is a surgical wording change.

**Do NOT update** `.planning/STATE.md`'s Blockers/Concerns section even though it references the 50 MB synthetic-log smoke test (line 76). STATE.md is updated by the orchestrator post-plan; the Plan 07 SUMMARY will reference this REQUIREMENTS.md change so the orchestrator can sync STATE.md.
  </action>
  <verify>
    <automated>cd "C:/claude_checkouts/mirk-poc-debug" &amp;&amp; ! grep -q "50 MB synthetic-log" .planning/REQUIREMENTS.md &amp;&amp; grep -q "Verbal" .planning/REQUIREMENTS.md &amp;&amp; grep -q "Revisions" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
- LOG-05 row in REQUIREMENTS.md no longer mentions "50 MB" or "synthetic-log"
- New wording captures the verbal-approval gate from CONTEXT.md
- A `## Revisions` section documents the change with date + rationale
- No other REQUIREMENTS.md content modified
  </done>
</task>

</tasks>

<verification>
Run from repo root after all 3 tasks complete:

```bash
flutter analyze --fatal-infos --fatal-warnings                                # donor files compile cleanly
dart run tool/check_headers.dart                                              # passes for all donor files (lib/main.dart still flagged — Plan 07 fixes)
test -f .github/workflows/ci.yml                                              # workflow file present
grep -c "flutter-version: '3.41.7'" .github/workflows/ci.yml                  # ≥3 (one per job)
grep -q "macos-14" .github/workflows/ci.yml                                   # iOS runner pinned
! grep -q "50 MB synthetic-log" .planning/REQUIREMENTS.md                     # spec dropped
```

**CI smoke (deferred to Plan 07):** the actual three-jobs-green run on `main` happens after Plan 07 has wired `lib/main.dart` and committed everything. Plan 07's verification includes the `gh run list --workflow=ci.yml --limit 1` check that surfaces the three jobs + downloadable artifacts (CI-01 through CI-05).
</verification>

<success_criteria>
- All seven BOOT-08 donor files present at prescribed paths with GOSL headers preserved
- `flutter analyze --fatal-infos --fatal-warnings` exits 0 across the donor tree (any analyzer warning on a donor file is a blocker — investigate adaptation, do NOT add `// ignore:` comments)
- `.github/workflows/ci.yml` defines `gates`, `android`, `ios` jobs with the planner-locked version pins (Flutter 3.41.7, macOS-14, JDK 21)
- REQUIREMENTS.md LOG-05 wording updated; `## Revisions` section documents the change
- Any adaptation made during donor-file porting (parent → POC import path, freezed → hand-rolled, constant added) is documented in commit messages and Plan 03 SUMMARY
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`:
- Bullet list of seven donor files ported (with adaptations made — e.g. "reveal_disc.dart: rewrote freezed-generated class as hand-rolled immutable")
- Confirmation that `flutter analyze` is green across the donor tree
- Confirmation of CI workflow shape (three jobs, version pins, artifact names)
- Note for Plan 07: this plan deliberately leaves `lib/main.dart` from `flutter create` in place; CI's `dart run tool/check_headers.dart` step WILL FAIL on the first push until Plan 07 replaces this file. This is the expected red-then-green progression.
- LOG-05 wording change confirmed; STATE.md Blockers/Concerns line 76 (50 MB synthetic-log) is now stale and should be updated by orchestrator at phase completion.
</output>
