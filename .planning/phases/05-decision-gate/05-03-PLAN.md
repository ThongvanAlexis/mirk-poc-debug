---
phase: 05-decision-gate
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - .planning/phases/05-decision-gate/05-PIXEL4A-FALSIFICATION-1.md
  - .planning/phases/05-decision-gate/05-PIXEL4A-UAT-1.md
  - .planning/phases/05-decision-gate/walk-evidence/pixel4a-walk-1/.gitkeep
autonomous: false
requirements: [PERF-06]

must_haves:
  truths:
    - "Pre-walk software gates pass on the closing SHA from Plan 01: full flutter test suite GREEN, flutter analyze 0 issues, dart format clean, GOSL header check OK, license allow-list OK, DEPENDENCIES.md freshness OK, tool tests GREEN."
    - "Pixel 4a APK pulled from CI artefact via `gh run download <run-id> --name mirk-poc-debug-android-debug-apk` (NOT a local build) — installed via `adb install -r <apk>`."
    - "Pixel 4a launch sanity gate clears: app cold-launches without SIGSEGV / Vulkan / Impeller crash within 30 s; `adb logcat` does NOT show `[ERROR:flutter/impeller]` or `SIGSEGV` lines (per Pitfall 1 — Adreno 618 + Vulkan-Impeller is documented risk territory)."
    - "If launch crashes — that IS a valid PERF-06 outcome (informational, soft criterion). Document the crash in `05-PIXEL4A-FALSIFICATION-1.md` as a known caveat to feed VERDICT.md (per CONTEXT §Caveat policy + Pitfall 1 — Adreno 6xx Impeller fallback is documented Flutter behavior)."
    - "If launch succeeds — proceed with WalkSimulator-driven walk mirroring iPhone walk regimes (default zoom + max zoom + C3' extreme-distance + UX-02 rotation no-op + idle); Mail-share session log post-walk via Android sharesheet (Gmail / Drive / Files target) per Pitfall 3 guard."
    - "All 5 JSONL streams extracted from Mail-shared log: `infrastructure.mirk.frame_delta`, `infrastructure.mirk.fog_transform`, `infrastructure.mirk.sdf`, `infrastructure.mirk.wisp`, `infrastructure.mirk.dev_marker`. Adreno 618 medianMs / p95Ms / maxMs computed for VERDICT.md Pixel 4a quantitative summary line."
    - "PERF-06 closure: app launches + fog renders + wisps render + 5-min walk completes without crash + informational FPS recorded — these are the four soft-criterion pass conditions; ANY of the four can fail and PERF-06 still resolves (it's informational), but ALL captured outcomes (success or failure) are documented."
    - "Per-walk-numbered doc shape `05-PIXEL4A-FALSIFICATION-1.md` + `05-PIXEL4A-UAT-1.md` (NOT unified with iPhone) — gives Plan 04 a distinct evidence anchor for the cross-platform reference + matches Phase 3.1 grep-tool compatibility precedent."
  artifacts:
    - path: ".planning/phases/05-decision-gate/05-PIXEL4A-FALSIFICATION-1.md"
      provides: "Pixel 4a (Adreno 618, Android 13) Walk #1 falsification document — Hypothesis (cross-platform sanity) + 4 PERF-06 soft criteria + per-criterion outcome + verbatim verdict line"
      min_lines: 80
    - path: ".planning/phases/05-decision-gate/05-PIXEL4A-UAT-1.md"
      provides: "Pixel 4a Walk #1 UAT log — pre-walk gate evidence + APK download + adb install + logcat sanity + walk steps + JSONL excerpts + Mail-share confirmation"
      min_lines: 70
    - path: ".planning/phases/05-decision-gate/walk-evidence/pixel4a-walk-1/"
      provides: "Directory holding the Mail-shared Android session log + extracted 5 JSONL streams + initial-launch logcat snippet"
  key_links:
    - from: ".planning/phases/05-decision-gate/05-PIXEL4A-FALSIFICATION-1.md"
      to: "REQUIREMENTS.md PERF-06 row"
      via: "Walk #1 closure: status flips from `Pending` to `Complete — Verified-by-walk` (success) OR `Complete — Caveat-documented` (launch crash / Impeller fallback)"
      pattern: "PERF-06"
    - from: ".planning/phases/05-decision-gate/05-PIXEL4A-FALSIFICATION-1.md"
      to: "walk-evidence/pixel4a-walk-1/frame_delta.jsonl"
      via: "Adreno 618 medianMs/p95Ms/maxMs cited in VERDICT.md Pixel 4a quantitative summary line"
      pattern: "infrastructure\\.mirk\\.frame_delta"
    - from: ".planning/phases/05-decision-gate/05-PIXEL4A-UAT-1.md"
      to: "walk-evidence/pixel4a-walk-1/initial-launch-logcat.txt"
      via: "30 s post-cold-launch logcat slice — captures Pitfall 1 Impeller crash signal OR confirms baseline-clean launch"
      pattern: "ERROR:flutter|Impeller|Vulkan|SIGSEGV"
---

<objective>
Pixel 4a (Adreno 618, Android 13) Walk #1 — cross-platform sanity reference for PERF-06 closure. Single sideload session covering: (a) app launch + Impeller-vs-Skia backend disposition (Pitfall 1 guard), (b) fog renders + wisps render + visual lock observable, (c) 5-min WalkSimulator-driven walk mirroring iPhone walk regimes (default + max zoom + C3' extreme-distance + UX-02 rotation no-op + idle), (d) Mail-share + 5-stream JSONL extraction.

PERF-06 is INFORMATIONAL (soft criterion per ROADMAP + REQUIREMENTS.md): "app launches, fog renders, no crash; informational FPS recorded for cross-platform comparison (no hard pass criterion)". Per CONTEXT §Pixel 4a walk shape — Phase 5 treats it with the same rigor as iPhone (Mail-share + same regimes including C3' + same metric capture) BUT the verdict-blocker semantics differ: an iPhone Criterion failure can trigger Phase 5.1 iteration; a Pixel 4a launch crash documents a CAVEAT in VERDICT.md (per Pitfall 1 — Adreno 6xx Impeller fallback is known Flutter behavior).

Walk source = synthetic via WalkSimulator AppBar control (commit `eec9087`); same listener body as live `Geolocator` per Phase 4 Walk #1 precedent. Free-form gesture regime — pans / zooms / extreme-distance / rotation gesture probe — at developer judgement.

Per-walk-numbered + Pixel-prefixed doc shape (`05-PIXEL4A-FALSIFICATION-1.md` + `05-PIXEL4A-UAT-1.md`) gives Plan 04 a distinct cross-platform evidence anchor.

Output: walk-evidence files + soft-criterion outcome statement. Plan 04 (VERDICT.md authoring) reads this for the Pixel 4a quantitative summary line + caveats list inheritance.
</objective>

<execution_context>
@C:/Users/oliver/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oliver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-decision-gate/05-CONTEXT.md
@.planning/phases/05-decision-gate/05-RESEARCH.md
@.planning/phases/05-decision-gate/05-VALIDATION.md
@.planning/phases/05-decision-gate/05-01-SUMMARY.md

# Walk-evidence templates — read for falsification + UAT doc shape:
@.planning/phases/04-wisp-particles/04-FALSIFICATION.md
@.planning/phases/04-wisp-particles/04-UAT.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-6.md

<interfaces>
<!-- Pre-walk gate sequence (defence-in-depth re-run after Plan 01): -->

```bash
flutter test                                                       # 211+ tests
flutter analyze --fatal-infos --fatal-warnings                     # 0 issues
dart format --line-length 160 --set-exit-if-changed lib/ test/     # exit 0
dart run tool/check_headers.dart                                   # OK
dart run tool/check_licenses.dart                                  # OK
dart run tool/check_dependencies_md.dart                           # OK
dart test tool/test/                                               # 18 tool tests
gh run watch <run-id-from-Plan-01>                                 # CI all-green
gh run download <run-id> --name mirk-poc-debug-android-debug-apk   # APK from CI artefact (NOT local build)
```

<!-- Pixel 4a APK install (existing Phase 1+ workflow): -->

```bash
adb devices                                                        # confirm Pixel 4a connected (USB or wireless)
adb install -r /tmp/p5-pixel-apk/app-debug.apk                     # actual filename per gh run download
adb shell pm list packages | grep mirk                             # confirm package installed
adb shell am start -n <package>/<MainActivity>                     # cold launch
```

<!-- Pitfall 1 — Pixel 4a / Adreno 618 launch sanity gate (30 s post-cold-launch logcat slice): -->

```bash
# Start logcat capture BEFORE launching the app:
adb logcat -c                                                      # clear buffer
adb logcat -v time > /tmp/pixel4a-launch.log 2>&1 &
LOGCAT_PID=$!

# Cold launch:
adb shell am start -n <package>/<MainActivity>

# Wait 30 s then snapshot:
sleep 30
kill $LOGCAT_PID
cp /tmp/pixel4a-launch.log .planning/phases/05-decision-gate/walk-evidence/pixel4a-walk-1/initial-launch-logcat.txt

# Crash signal grep:
grep -E "ERROR:flutter|Impeller|Vulkan|SIGSEGV|FATAL" .planning/phases/05-decision-gate/walk-evidence/pixel4a-walk-1/initial-launch-logcat.txt | head -20
```

Outcomes per Pitfall 1:
- **Clean launch:** zero matches → proceed to walk.
- **Impeller fallback to OpenGL ES (informational):** `[INFO:flutter/...impeller fallback...]` line present, no crash → proceed to walk; document the backend choice in `05-PIXEL4A-UAT-1.md`.
- **Crash:** `SIGSEGV` or `FATAL` → DO NOT proceed to walk; document the crash signature + the Pitfall 1 caveat ("Pixel 4a Adreno 618 Impeller crash on Flutter 3.41.7; mitigation = OpenGL ES fallback at MirkFall port-back time if needed"); skip Steps 4–8 of the walk; jump straight to Task 3 with the crash-evidence outcome.

<!-- Mail-share JSONL flow (Pitfall 3 — Mail-share fails silently on Pixel 4a): -->

Pre-walk verification: confirm Pixel 4a has Gmail OR another email client signed in (Settings → Accounts). At post-walk share-logs tap, the Android sharesheet presents Gmail / Drive / Files / Bluetooth / etc. Pick "Gmail" or "Drive" specifically — picking "Files" stores locally and the dev machine never sees the log.

Verify the email arrived BEFORE concluding the walk; if it didn't, re-trigger share-logs with a different target.

<!-- 5-stream JSONL extraction (same as iPhone walk; PERF-07 thresholds apply for informational comparison): -->

```bash
LOG=".planning/phases/05-decision-gate/walk-evidence/pixel4a-walk-1/yyyymmddTHHMMSSZ_logs.txt"
DIR=".planning/phases/05-decision-gate/walk-evidence/pixel4a-walk-1"
grep "infrastructure.mirk.frame_delta" "$LOG" > "$DIR/frame_delta.jsonl"
grep "infrastructure.mirk.fog_transform" "$LOG" > "$DIR/fog_transform.jsonl"
grep "infrastructure.mirk.sdf"          "$LOG" > "$DIR/sdf.jsonl"
grep "infrastructure.mirk.wisp"         "$LOG" > "$DIR/wisp.jsonl"
grep "infrastructure.mirk.dev_marker"   "$LOG" > "$DIR/dev_markers.jsonl" || true
```

<!-- VERDICT.md citation format (per CONTEXT §Pixel 4a walk shape): -->

A 5-line table or 1-paragraph quantitative summary referencing:
- Device: `Pixel 4a (Adreno 618, Android 13)`
- Backend: `Vulkan-Impeller | OpenGL ES fallback | Skia` (one of)
- PERF-07 medianMs / p95Ms / maxMs (informational, NOT thresholded — these are the cross-platform reference numbers)
- Visual lock observation (verbatim quote)
- Crash status (`none` | `Impeller-fallback only` | `<crash signature>`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-walk gates re-run + APK download + Pixel 4a launch sanity gate + author 05-PIXEL4A-FALSIFICATION-1.md + 05-PIXEL4A-UAT-1.md skeletons</name>
  <files>
    .planning/phases/05-decision-gate/05-PIXEL4A-FALSIFICATION-1.md,
    .planning/phases/05-decision-gate/05-PIXEL4A-UAT-1.md,
    .planning/phases/05-decision-gate/walk-evidence/pixel4a-walk-1/.gitkeep
  </files>
  <action>
**Step 1 — Read 05-01-SUMMARY.md to retrieve closing SHA + CI run-ID.**

**Step 2 — Re-run pre-walk software gates** (defence-in-depth):

```bash
flutter test                                                       # 211+ tests
flutter analyze --fatal-infos --fatal-warnings                     # 0 issues
dart format --line-length 160 --set-exit-if-changed lib/ test/     # clean
dart run tool/check_headers.dart                                   # OK
dart run tool/check_licenses.dart                                  # OK
dart run tool/check_dependencies_md.dart                           # OK
dart test tool/test/                                               # 18 tool tests
```

ALL must be GREEN. (Note: Plan 02 may run in parallel with this plan — both run in Wave 2 — but Plan 02's pre-walk gate run does NOT block Plan 03; both re-confirm the same closing SHA.)

**Step 3 — Confirm CI run + download Android debug APK:**

```bash
RUN_ID=<from-Plan-01-SUMMARY>
gh run view "$RUN_ID" --json conclusion --jq .conclusion           # success
mkdir -p .planning/phases/05-decision-gate/walk-evidence/pixel4a-walk-1
gh run download "$RUN_ID" --name mirk-poc-debug-android-debug-apk --dir /tmp/p5-pixel-apk
ls /tmp/p5-pixel-apk
```

If artefact name differs, use the actual name from `gh run view <run-id>`.

**Step 4 — Pixel 4a launch sanity gate (Pitfall 1):**

Verify Pixel 4a is connected:
```bash
adb devices
```

Should show one device line ending in `device` (not `unauthorized`). If `unauthorized`, accept the USB-debug prompt on the phone.

Install + capture 30 s logcat:
```bash
adb logcat -c                                                      # clear buffer
adb install -r /tmp/p5-pixel-apk/app-debug.apk                     # use actual filename

# Snapshot package + main activity name:
PKG=$(adb shell pm list packages | grep -i mirk | sed -e 's/^package://' | tr -d '\r')
echo "Pixel 4a package: $PKG"

# Capture logcat in background DURING cold launch:
adb logcat -v time > .planning/phases/05-decision-gate/walk-evidence/pixel4a-walk-1/initial-launch-logcat.txt 2>&1 &
LOGCAT_PID=$!

# Cold launch (the package's launcher activity is auto-resolved by `monkey`):
adb shell monkey -p "$PKG" -c android.intent.category.LAUNCHER 1

# Wait 30 s for app to fully start + first GPS permission flow:
sleep 30
kill $LOGCAT_PID 2>/dev/null
wait $LOGCAT_PID 2>/dev/null

# Grep for Pitfall 1 crash + Impeller-fallback signals:
echo "=== Crash + backend signals ==="
grep -E "ERROR:flutter|Impeller|Vulkan|SIGSEGV|FATAL|skia" .planning/phases/05-decision-gate/walk-evidence/pixel4a-walk-1/initial-launch-logcat.txt | head -30
```

Disposition (record in 05-PIXEL4A-UAT-1.md):
- **Clean launch (zero crash hits):** PERF-06 fog-renders + wisps-render gates can proceed — go to Step 5 + Task 2.
- **Impeller fallback only (`[INFO:...impeller fallback...]` lines but no crash):** OK; backend = OpenGL ES; document in UAT; proceed to Task 2.
- **Crash (`SIGSEGV` / `FATAL`):** STOP the walk — DO NOT proceed to Task 2's user-driven walk. Author the falsification doc with verdict `LAUNCH-CRASH-CAVEAT-DOCUMENTED`, capture the crash signature in `05-PIXEL4A-FALSIFICATION-1.md` Walk Evidence section, and skip Task 2 entirely. Task 3 then commits the documented crash + jumps to Plan 04.

**Step 5 — Author `05-PIXEL4A-FALSIFICATION-1.md` skeleton:**

```markdown
---
phase: 05-decision-gate
walk: 1
device: Pixel 4a (Adreno 618, Android 13)
date: 2026-05-DD
ci_run: <run-id from Plan 01>
sha: <closing SHA from Plan 01>
verdict: TBD                # filled in Task 3
backend: TBD                # Vulkan-Impeller | OpenGL ES fallback | Skia | crash-no-render
---

# Phase 5 Decision Gate — Pixel 4a Walk #1 — Falsification (PERF-06)

## Hypothesis (Phase 5 cross-platform sanity)

The layered Phase 3.1 + Phase 4 fix bundle (validated on iPhone 17 Pro under PERF-07 hard thresholds) renders fog + wisps under PERF-06's soft criteria on Pixel 4a (Adreno 618, Android 13). PERF-06 is informational — the bar is "app launches + fog renders + wisps render + 5-min walk no crash + informational FPS captured" with NO hard pass threshold.

If true: PERF-06 closes; VERDICT.md cites Adreno 618 quantitative numbers as the cross-platform reference.
If false (launch crash / fog-renders-but-wisps-don't / etc.): caveat documented in VERDICT.md per CONTEXT §Caveat policy. PERF-06 still resolves (it's informational) — the failure-mode is the deliverable.

## Falsification Criteria (PERF-06 soft criteria — captured outcome ≠ hard pass)

**Criterion P1 — App cold-launches without SIGSEGV / FATAL crash within 30 s** (Pitfall 1 guard).

**Criterion P2 — Fog renders correctly on the basemap** at default zoom; visible lock during gentle pan; no black-fog / never-rendered failure mode.

**Criterion P3 — Wisps render correctly** after 5-s warmup; spawn on disc perimeter as new GPS fixes arrive; drift + fade visible; remain anchored to map during pan.

**Criterion P4 — 5-min WalkSimulator-driven walk completes without crash** at default + max zoom + C3' extreme-distance regimes.

**Criterion P5 — Informational FPS captured:** `infrastructure.mirk.frame_delta` JSONL rollups Mail-shared post-walk. medianMs / p95Ms / maxMs values documented for VERDICT.md cross-platform reference summary line. NO hard threshold.

**Criterion P6 — UX-02 rotation no-op holds on Android** (cross-platform regression guard for FOG-16 path-(a) discipline).

**Criterion P7 — C3' extreme-distance behaviour matches iPhone:** Adreno 618 fp32 precision behaviour at ~50–100 km from Melun; observed visual lock + uOffsetXMax JSONL evidence.

## Walk Plan (Pixel 4a sideload session at desk per CONTEXT §Pixel 4a walk shape)

Sequence (mirror of iPhone walk):
  1. Pre-walk: `adb devices`; confirm Pixel 4a connected. `adb install -r` the closing-SHA APK. `adb logcat -c` then start logcat capture.
  2. Cold launch via `adb shell monkey ...`; observe 30 s with logcat running. Pitfall 1 disposition.
  3. (If launch clean) Grant location permission; observe FpsCounterOverlay + FrameDeltaProbeOverlay + initial blue dot.
  4. Default-zoom baseline: tap WalkSimulator AppBar action; pick a bearing. Observe Criterion P2 + P3 + P5.
  5. Pan + pinch-zoom + combined-gesture coverage at default zoom.
  6. Max-zoom regime (`kPocMaxZoom = 20`).
  7. C3' extreme-distance: pan to ~50–100 km from Melun (DEBUG-02 unconstrained). Observe Criterion P7.
  8. UX-02 rotation gesture probe (Criterion P6).
  9. 5-min sustained walk under WalkSimulator (Criterion P4).
  10. End walk: tap share-logs → Android sharesheet → Gmail (per Pitfall 3) → developer's address → send. Confirm email received.

## Pre-walk Gate Status

- [ ] flutter test full suite GREEN
- [ ] flutter analyze 0 issues
- [ ] dart format --set-exit-if-changed clean
- [ ] dart run tool/check_headers.dart GREEN
- [ ] dart run tool/check_dependencies_md.dart GREEN
- [ ] dart run tool/check_licenses.dart GREEN
- [ ] dart test tool/test/ GREEN
- [ ] CI run on closing SHA all-jobs GREEN
- [ ] Android debug APK artefact downloaded
- [ ] Pixel 4a connected via adb; package installed
- [ ] 30 s post-cold-launch logcat captured; Pitfall 1 disposition recorded

## Walk Evidence (filled by Task 3)

**Verdict:** TBD (CONFIRMED-PERF-06-CLEAN | CONFIRMED-PERF-06-IMPELLER-FALLBACK | LAUNCH-CRASH-CAVEAT | OTHER)
**Verbatim developer quote:** TBD
**Backend used:** TBD (Vulkan-Impeller | OpenGL ES | Skia | crash)
**JSONL excerpts (5 streams):** TBD — see `walk-evidence/pixel4a-walk-1/`
**Per-criterion outcome table:** TBD

## Carry-forward dispositions

- REQUIREMENTS.md PERF-06 row update: TBD (Plan 05 cascade)
- VERDICT.md Pixel 4a quantitative summary line: TBD (Plan 04 input)
- VERDICT.md caveats list inheritance: TBD (Plan 04 input — note any Pitfall 1 / 3 outcomes here)
```

**Step 6 — Author `05-PIXEL4A-UAT-1.md` skeleton:**

```markdown
# Phase 5 — Pixel 4a Walk #1 — UAT Log

**Phase:** 05-decision-gate
**Walk #:** 1
**Date:** TBD
**Tester:** Developer (solo)
**Device:** Pixel 4a (Adreno 618, Android 13)
**Sideload mechanism:** `adb install -r`
**CI Run:** <run-id>
**SHA:** <closing SHA>
**APK artefact:** mirk-poc-debug-android-debug-apk → /tmp/p5-pixel-apk/app-debug.apk
**Walk source:** synthetic via WalkSimulator AppBar control (commit `eec9087`)

## Pre-walk software gate evidence

```
$ flutter test
... XX tests passed, 1 skipped ...

$ flutter analyze --fatal-infos --fatal-warnings
No issues found!

$ dart format --line-length 160 --set-exit-if-changed lib/ test/
0 changed

$ dart run tool/check_headers.dart
OK (XX files)

$ dart run tool/check_dependencies_md.dart
OK (132 packages)

$ dart test tool/test/
18 GREEN

$ gh run view <run-id> --json conclusion --jq .conclusion
success

$ gh run download <run-id> --name mirk-poc-debug-android-debug-apk --dir /tmp/p5-pixel-apk
ls /tmp/p5-pixel-apk
app-debug.apk
```

## Pitfall 1 launch sanity gate (30 s post-cold-launch logcat)

```
$ adb devices
List of devices attached
<serial>    device

$ adb install -r /tmp/p5-pixel-apk/app-debug.apk
Performing Streamed Install
Success

$ adb logcat -c && adb logcat -v time > .planning/phases/05-decision-gate/walk-evidence/pixel4a-walk-1/initial-launch-logcat.txt 2>&1 &
$ adb shell monkey -p <pkg> -c android.intent.category.LAUNCHER 1
$ sleep 30 && kill $LOGCAT_PID

$ grep -E "ERROR:flutter|Impeller|Vulkan|SIGSEGV|FATAL" initial-launch-logcat.txt
TBD
```

**Disposition:** TBD (clean | Impeller-fallback | crash-with-caveat)

## Walk Steps (1–10 from 05-PIXEL4A-FALSIFICATION-1.md)

### Step 1: adb device + install + logcat capture
- TBD

### Step 2: Cold launch + Pitfall 1 disposition
- TBD

### Step 3: Permission grant + overlay sanity
- TBD

### Step 4: Default-zoom baseline + WalkSimulator start
- TBD

### Step 5: Pan + pinch-zoom + combined-gesture coverage
- TBD

### Step 6: Max-zoom regime
- TBD

### Step 7: C3' extreme-distance regime (~50–100 km from Melun)
- TBD; uOffsetXMax reach: TBD

### Step 8: UX-02 rotation gesture probe
- TBD

### Step 9: 5-min sustained walk
- TBD

### Step 10: Mail-share session log via Android sharesheet (Gmail target per Pitfall 3)
- Email received: TBD
- Log file: yyyymmddTHHMMSSZ_logs.txt
- 5 streams present: TBD

## Mail-share JSONL extracts

(Filled post-walk by Task 3.)

PERF-07 cross-platform reference numbers (informational): TBD.

## Verdict

TBD — filled by Task 3.
```

**Step 7 — Create walk-evidence dir + commit Task 1 outputs:**

```bash
mkdir -p .planning/phases/05-decision-gate/walk-evidence/pixel4a-walk-1
echo "# Walk-evidence directory for Phase 5 Pixel 4a Walk #1.\n# Populated post-walk by Task 3 with the Mail-shared session log + 5 extracted JSONL streams + initial-launch-logcat snapshot." > .planning/phases/05-decision-gate/walk-evidence/pixel4a-walk-1/.gitkeep

node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(05-03): pre-walk gates + Pixel 4a Walk #1 skeleton" --files \
  .planning/phases/05-decision-gate/05-PIXEL4A-FALSIFICATION-1.md \
  .planning/phases/05-decision-gate/05-PIXEL4A-UAT-1.md \
  .planning/phases/05-decision-gate/walk-evidence/pixel4a-walk-1/.gitkeep
```
  </action>
  <verify>
    <automated>flutter test &amp;&amp; flutter analyze --fatal-infos --fatal-warnings &amp;&amp; dart run tool/check_headers.dart &amp;&amp; dart run tool/check_dependencies_md.dart &amp;&amp; test -f .planning/phases/05-decision-gate/05-PIXEL4A-FALSIFICATION-1.md &amp;&amp; test -f .planning/phases/05-decision-gate/05-PIXEL4A-UAT-1.md &amp;&amp; test -d .planning/phases/05-decision-gate/walk-evidence/pixel4a-walk-1</automated>
  </verify>
  <done>
- Local hardening sweep GREEN.
- CI run from Plan 01 confirmed; Android debug APK downloaded; Pixel 4a connected via adb.
- 30 s post-cold-launch logcat captured; Pitfall 1 disposition recorded (clean | Impeller-fallback | crash).
- 05-PIXEL4A-FALSIFICATION-1.md + 05-PIXEL4A-UAT-1.md skeletons committed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Pixel 4a WalkSimulator-driven walk + Mail-share session log via Android sharesheet</name>
  <files>.planning/phases/05-decision-gate/walk-evidence/pixel4a-walk-1/yyyymmddTHHMMSSZ_logs.txt</files>
  <action>Branch on Pitfall 1 disposition recorded in Task 1: if launch crashed, SKIP this task (resume-signal ). Otherwise proceed with the WalkSimulator-driven walk on Pixel 4a mirroring the iPhone walk regimes (default zoom + max zoom + C3 extreme-distance + UX-02 rotation no-op probe + idle + 5-min sustained walk + Mail-share via Android sharesheet — pick Gmail or Drive per Pitfall 3, NOT Files). See full step-by-step in &lt;how-to-verify&gt; below. The developer s resume-signal IS the verification — Task 3 reads it + the saved Mail-shared log to extract JSONL evidence and author the falsification doc soft-criterion outcome.</action>
  <verify>
    <automated>MANUAL — see &lt;how-to-verify&gt; (PERF-06 is by definition manual-only sideload UAT walk on Pixel 4a per 05-VALIDATION.md; criteria P1..P7 are subjective + JSONL extraction is post-walk; the developer s resume-signal IS the verification).</automated>
  </verify>
  <done>Pixel 4a Walk #1 complete; developer s verbal verdict captured (CONFIRMED-PERF-06-CLEAN / -IMPELLER-FALLBACK / LAUNCH-CRASH-CAVEAT / PARTIAL-PERF-06 / paused); session log Mail-shared via Android sharesheet (Gmail target per Pitfall 3) and saved into walk-evidence/pixel4a-walk-1/ so Task 3 can extract the 5 JSONL streams + record backend disposition + author the falsification doc soft-criterion outcome. Adreno 618 informational PERF-07 medianMs / p95Ms / maxMs captured for the VERDICT.md cross-platform reference summary line; UX-02 rotation no-op + C3 extreme-distance regimes covered (or marked N/A for the launch-crash branch).</done>
  <what-built>
- Closing-SHA Android debug APK installed on Pixel 4a via `adb install -r`.
- 30 s post-cold-launch logcat captured; Pitfall 1 disposition recorded in 05-PIXEL4A-UAT-1.md (skipping Task 2 entirely if disposition was `crash` — proceed straight to Task 3 with crash evidence).
- WalkSimulator production-exposed in AppBar; same drive shape as iPhone Walk #1.
- 05-PIXEL4A-FALSIFICATION-1.md + 05-PIXEL4A-UAT-1.md skeletons in `.planning/phases/05-decision-gate/`.
- walk-evidence/pixel4a-walk-1/ directory ready to receive the Mail-shared log + 5 extracted JSONL streams.
  </what-built>
  <how-to-verify>
**Branch — Pitfall 1 launch disposition:**

  - **If launch CRASH (SIGSEGV / FATAL in 30 s logcat):** SKIP this task entirely. Type the resume signal `paused — pitfall1-crash — <crash-signature>`. Task 3 will read the captured logcat slice + author the falsification doc with `LAUNCH-CRASH-CAVEAT-DOCUMENTED` verdict + record the Adreno 618 Impeller fallback caveat for VERDICT.md inheritance.

  - **If launch CLEAN or Impeller-fallback only:** proceed with the walk below.

**Walk drive (free-form per CONTEXT — coverage targets, not strict order):**
  1. Confirm app launched + permission granted + overlays visible (FpsCounter + FrameDeltaProbe).
  2. Wait 5 s warmup; tap AppBar → `Icons.directions_walk` → bottom sheet → start WalkSimulator at default speed (1.4 m/s).
  3. Default-zoom baseline (1–2 min): wisps spawn on disc perimeter (P3); pan + pinch-zoom + combined-gesture; observe P2 + P5.
  4. Max-zoom regime (`kPocMaxZoom = 20`); pan; observe P2 + P5 at max zoom.
  5. C3' extreme-distance: increase WalkSimulator speed slider OR pan manually past Melun limits; sustain one-direction pan until basemap shows distinctly different geography (~50–100 km equivalent); observe P7 (fp32 precision sanity).
  6. UX-02 rotation gesture probe (P6): two-finger rotation gesture; confirm rotation is inert.
  7. 5-min sustained walk: leave WalkSimulator running; switch back to default zoom; let the walk drift continue (P4).
  8. Idle observation 30 s.

**End walk + Mail-share (Pitfall 3 — Android sharesheet target choice matters):**
  9. Tap share-logs button (always-visible AppBar action) → Android sharesheet appears → pick **Gmail** (NOT "Files" — Pitfall 3) → developer's address → send.
  10. Confirm email received within ~1 minute. Save the attachment `yyyymmddTHHMMSSZ_logs.txt` (or `.gz`) into `.planning/phases/05-decision-gate/walk-evidence/pixel4a-walk-1/`.
  11. If email did NOT arrive → re-trigger share-logs with a different sharesheet target (Drive, Bluetooth-to-laptop). Verify before exiting walk.

**Resume signal:** type one of:
  - `approved — verdict CONFIRMED-PERF-06-CLEAN — <verbatim quote>` (clean launch + walk + 5-min completes + email delivered + visual lock observed)
  - `approved — verdict CONFIRMED-PERF-06-IMPELLER-FALLBACK — <quote>` (clean walk but logcat shows OpenGL ES fallback — backend deviates from iPhone but app works)
  - `paused — pitfall1-crash — <crash-signature>` (launch crashed in 30 s logcat — Task 3 documents caveat instead)
  - `paused — pitfall3-mail-share — <reason>` (sharesheet didn't deliver email — re-trigger or document partial outcome)
  - `iterating — verdict OTHER — <observation>` (something unexpected — visual glitch, partial fog, wisps don't render, etc.)

If verdict ≠ CONFIRMED-PERF-06-* PERF-06 still RESOLVES (it's informational); the failure mode IS the deliverable for VERDICT.md caveat inheritance.
  </how-to-verify>
  <resume-signal>Type one of: `approved — verdict CONFIRMED-PERF-06-CLEAN — <quote>` | `approved — verdict CONFIRMED-PERF-06-IMPELLER-FALLBACK — <quote>` | `paused — pitfall1-crash — <signature>` | `paused — pitfall3-mail-share — <reason>` | `iterating — verdict OTHER — <observation>`</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Extract 5 JSONL streams + author Pixel 4a walk-evidence + fill verdict + commit</name>
  <files>
    .planning/phases/05-decision-gate/05-PIXEL4A-FALSIFICATION-1.md,
    .planning/phases/05-decision-gate/05-PIXEL4A-UAT-1.md,
    .planning/phases/05-decision-gate/walk-evidence/pixel4a-walk-1/frame_delta.jsonl,
    .planning/phases/05-decision-gate/walk-evidence/pixel4a-walk-1/fog_transform.jsonl,
    .planning/phases/05-decision-gate/walk-evidence/pixel4a-walk-1/sdf.jsonl,
    .planning/phases/05-decision-gate/walk-evidence/pixel4a-walk-1/wisp.jsonl,
    .planning/phases/05-decision-gate/walk-evidence/pixel4a-walk-1/dev_markers.jsonl
  </files>
  <action>
**Step 1 — Branch on Task 2 outcome:**

- **If Task 2 resume signal was `paused — pitfall1-crash — ...`:** SKIP Steps 2–4 (no Mail-shared log to extract). Go straight to Step 5 with verdict `LAUNCH-CRASH-CAVEAT-DOCUMENTED`; the falsification doc cites the captured `initial-launch-logcat.txt` + crash signature; the carry-forward dispositions document the Pitfall 1 Adreno 618 caveat for VERDICT.md inheritance. Task 3 still commits + closes Plan 03.

- **Otherwise (clean walk OR Impeller-fallback OR pitfall3-mail-share recovered):** proceed to Step 2.

**Step 2 — Locate the Mail-shared session log:**

```bash
ls -la .planning/phases/05-decision-gate/walk-evidence/pixel4a-walk-1/
LOG=$(ls .planning/phases/05-decision-gate/walk-evidence/pixel4a-walk-1/*_logs.txt 2>/dev/null | head -1)
test -f "$LOG" || { echo "ERROR: log file missing — Pitfall 3 not recovered"; exit 1; }
wc -l "$LOG"
```

If `.gz`: `gunzip -k <log>.gz`.

**Step 3 — Extract 5 JSONL streams** (same pattern as iPhone walk):

```bash
DIR=".planning/phases/05-decision-gate/walk-evidence/pixel4a-walk-1"
grep "infrastructure.mirk.frame_delta" "$LOG" > "$DIR/frame_delta.jsonl"
grep "infrastructure.mirk.fog_transform" "$LOG" > "$DIR/fog_transform.jsonl"
grep "infrastructure.mirk.sdf"          "$LOG" > "$DIR/sdf.jsonl"
grep "infrastructure.mirk.wisp"         "$LOG" > "$DIR/wisp.jsonl"
grep "infrastructure.mirk.dev_marker"   "$LOG" > "$DIR/dev_markers.jsonl" || true

for f in frame_delta fog_transform sdf wisp dev_markers; do
  count=$(wc -l < "$DIR/$f.jsonl")
  echo "$f.jsonl: $count rollups"
done
```

**Step 4 — Compute Pixel 4a quantitative summary** (PERF-07 informational + UX-02 + C3' + wisp activity):

```bash
# PERF-07 informational summary:
jq -s '
  map(.medianMs // empty) as $meds |
  map(.p95Ms    // empty) as $p95s |
  map(.maxMs    // empty) as $maxs |
  {
    rollups:         ($meds | length),
    medianOfMedians: ($meds | sort | .[length/2|floor]),
    maxOfP95s:       ($p95s | max),
    maxOfMaxes:      ($maxs | max)
  }
' "$DIR/frame_delta.jsonl"

# C3' uOffsetX reach:
jq -s 'map(.uOffsetXMax // empty) | max' "$DIR/fog_transform.jsonl"

# UX-02 canvasTx/Ty zero-check:
jq -s '
  map(select(.canvasTx != null)) |
  {
    rollups:           length,
    canvasTxAllZero:   all(.[]; .canvasTx == 0),
    canvasTyAllZero:   all(.[]; .canvasTy == 0)
  }
' "$DIR/fog_transform.jsonl"

# Wisp activity:
jq -s '
  map(.activeCount // empty) |
  {
    rollups:        length,
    medianActive:   (sort | .[length/2|floor]),
    maxActive:      max
  }
' "$DIR/wisp.jsonl"
```

**Step 5 — Apply per-criterion outcome (P1..P7) against captured numbers + verbatim resume signal:**

| Criterion | Soft-pass condition | Walk #1 captured | Outcome |
|-----------|--------------------|------------------|---------|
| P1 — App launches without SIGSEGV / FATAL | logcat clean | from initial-launch-logcat.txt | PASS / IMPELLER-FALLBACK / CRASH |
| P2 — Fog renders | verbatim verdict mentions fog visible | TBD | PASS / FAIL |
| P3 — Wisps render | verbatim + wisp.jsonl non-empty | TBD | PASS / FAIL |
| P4 — 5-min walk no crash | verbatim + log timestamps span ≥ 5 min | TBD | PASS / FAIL |
| P5 — Informational FPS captured | medianMs / p95Ms / maxMs from Step 4 | TBD | always-PASS-if-extracted (informational) |
| P6 — UX-02 rotation no-op | canvasTxAllZero == true && canvasTyAllZero == true | TBD | PASS / FAIL |
| P7 — C3' extreme-distance | uOffsetXMax >> 4M (cross-platform parity vs iPhone) | TBD | PASS / FAIL |

Compose verdict:
- **CONFIRMED-PERF-06-CLEAN:** P1=PASS, P2..P4=PASS, P5 captured, P6=PASS, P7=PASS — all clear.
- **CONFIRMED-PERF-06-IMPELLER-FALLBACK:** P1=IMPELLER-FALLBACK; rest PASS — note backend choice in falsification doc + VERDICT.md caveat.
- **LAUNCH-CRASH-CAVEAT-DOCUMENTED:** P1=CRASH; P2..P7=N/A — caveat-only outcome; VERDICT.md inherits the caveat per CONTEXT §Caveat policy.
- **PARTIAL-PERF-06:** mixed — document each PASS/FAIL row; PERF-06 still RESOLVES (informational); VERDICT.md inherits each non-PASS as a caveat row.

**Step 6 — Fill `05-PIXEL4A-FALSIFICATION-1.md`:**

- YAML front-matter: `verdict:` filled; `backend:` filled (Vulkan-Impeller | OpenGL ES fallback | Skia | crash-no-render).
- Pre-walk Gate Status checkboxes flipped.
- Walk Evidence section:
  - "Verbatim developer quote" = paste resume signal verbatim.
  - "Backend used" from logcat grep + Pitfall 1 disposition.
  - "JSONL excerpts (5 streams)" = paste Step 4 jq outputs (or "N/A — launch crashed before Mail-share" for crash branch).
  - "Per-criterion outcome table" = filled per Step 5.
- Carry-forward dispositions:
  - REQUIREMENTS.md PERF-06 row update — note for Plan 05 cascade: status flips to `Complete — Verified-by-walk` (clean) OR `Complete — Caveat-documented` (any non-clean outcome).
  - VERDICT.md Pixel 4a quantitative summary line — provide the EXACT 5-line table cell content for Plan 04 to lift verbatim.
  - VERDICT.md caveats list inheritance — list each soft-criterion fail (if any) so Plan 04 knows what to add to the caveats list.

**Step 7 — Fill `05-PIXEL4A-UAT-1.md`:**

- Replace placeholders with concrete values.
- Walk Steps 1–10 narrated with developer observations.
- Mail-share JSONL extracts: jq outputs verbatim (or "N/A" for crash branch).
- Verdict matches falsification doc.

**Step 8 — Commit:**

```bash
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(05-03): Pixel 4a Walk #1 evidence + verdict — <verdict-line>" --files \
  .planning/phases/05-decision-gate/05-PIXEL4A-FALSIFICATION-1.md \
  .planning/phases/05-decision-gate/05-PIXEL4A-UAT-1.md \
  .planning/phases/05-decision-gate/walk-evidence/pixel4a-walk-1/frame_delta.jsonl \
  .planning/phases/05-decision-gate/walk-evidence/pixel4a-walk-1/fog_transform.jsonl \
  .planning/phases/05-decision-gate/walk-evidence/pixel4a-walk-1/sdf.jsonl \
  .planning/phases/05-decision-gate/walk-evidence/pixel4a-walk-1/wisp.jsonl \
  .planning/phases/05-decision-gate/walk-evidence/pixel4a-walk-1/dev_markers.jsonl \
  .planning/phases/05-decision-gate/walk-evidence/pixel4a-walk-1/initial-launch-logcat.txt
```

Note: `initial-launch-logcat.txt` is committed as evidence regardless of branch (clean walk OR crash branch — its presence corroborates the Pitfall 1 disposition).
  </action>
  <verify>
    <automated>test -f .planning/phases/05-decision-gate/05-PIXEL4A-FALSIFICATION-1.md &amp;&amp; test -f .planning/phases/05-decision-gate/05-PIXEL4A-UAT-1.md &amp;&amp; test -f .planning/phases/05-decision-gate/walk-evidence/pixel4a-walk-1/initial-launch-logcat.txt &amp;&amp; grep -qE "verdict: (CONFIRMED-PERF-06|LAUNCH-CRASH-CAVEAT|PARTIAL-PERF-06|OTHER)" .planning/phases/05-decision-gate/05-PIXEL4A-FALSIFICATION-1.md</automated>
  </verify>
  <done>
- 05-PIXEL4A-FALSIFICATION-1.md verdict line filled (CONFIRMED-PERF-06-CLEAN | CONFIRMED-PERF-06-IMPELLER-FALLBACK | LAUNCH-CRASH-CAVEAT-DOCUMENTED | PARTIAL-PERF-06).
- 5 JSONL streams extracted into walk-evidence/pixel4a-walk-1/ (or marked N/A for crash branch).
- initial-launch-logcat.txt committed regardless of branch (Pitfall 1 disposition record).
- Per-criterion outcome table populated (P1..P7).
- Verbatim developer quote captured.
- VERDICT.md Pixel 4a quantitative summary line drafted in Carry-forward dispositions section (Plan 04 lifts verbatim).
- Commit landed on main; Plan 04 (VERDICT.md authoring) can now read this evidence + caveats list inheritance.
  </done>
</task>

</tasks>

<verification>
**Phase-level checks for Plan 03:**

1. `git ls-files .planning/phases/05-decision-gate/05-PIXEL4A-FALSIFICATION-1.md` returns the path.
2. `grep -qE "verdict: (CONFIRMED-PERF-06|LAUNCH-CRASH-CAVEAT|PARTIAL-PERF-06|OTHER)" .planning/phases/05-decision-gate/05-PIXEL4A-FALSIFICATION-1.md` succeeds.
3. `test -f .planning/phases/05-decision-gate/walk-evidence/pixel4a-walk-1/initial-launch-logcat.txt` succeeds (regardless of branch).
4. If verdict is `CONFIRMED-PERF-06-*`: `wc -l .planning/phases/05-decision-gate/walk-evidence/pixel4a-walk-1/frame_delta.jsonl` reports > 0.
5. Per-criterion outcome table in 05-PIXEL4A-FALSIFICATION-1.md has no `TBD` cells.
6. Verbatim developer quote present.
</verification>

<success_criteria>
- Pixel 4a (Adreno 618, Android 13) sideload session executed against Plan 01's closing-SHA APK.
- Pitfall 1 launch sanity disposition recorded via 30 s logcat slice.
- (Clean branch) 5 JSONL streams extracted; PERF-07 informational numbers computed; UX-02 + C3' parity captured.
- (Crash branch) Caveat documented for VERDICT.md inheritance per CONTEXT §Caveat policy.
- 05-PIXEL4A-FALSIFICATION-1.md + 05-PIXEL4A-UAT-1.md + initial-launch-logcat.txt committed.
- VERDICT.md Pixel 4a quantitative summary line drafted (Plan 04 lifts verbatim).
- Plan 04 has its cross-platform-reference evidence anchor.
- PERF-06 closes (informational; outcome captured regardless of pass/fail).
</success_criteria>

<output>
After completion, create `.planning/phases/05-decision-gate/05-03-SUMMARY.md` per the standard summary template. Include:
- The chosen verdict (CONFIRMED-PERF-06-CLEAN | -IMPELLER-FALLBACK | LAUNCH-CRASH-CAVEAT-DOCUMENTED | PARTIAL-PERF-06).
- Backend used (Vulkan-Impeller | OpenGL ES fallback | Skia | crash-no-render).
- Pixel 4a quantitative summary line (5-line table verbatim — Plan 04 lifts this).
- Caveats list contributions for VERDICT.md (Plan 04 inherits).
- Files created / modified.
- Pointer to walk-evidence directory for Plan 04 to consume.
</output>
