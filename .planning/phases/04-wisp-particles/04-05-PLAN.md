---
phase: 04-wisp-particles
plan: 05
type: execute
wave: 4
depends_on: ["04-04"]
files_modified:
  - .planning/phases/04-wisp-particles/04-FALSIFICATION.md
  - .planning/phases/04-wisp-particles/04-UAT.md
autonomous: false
requirements: [WISP-01, WISP-02, WISP-03, WISP-04, WISP-05, PERF-07, PERF-08, UX-02, DEBUG-02]

must_haves:
  truths:
    - "Pre-walk gates pass: full test suite GREEN; flutter analyze 0 issues; dart format clean; no GOSL header missing"
    - "CI run produces a sideloadable IPA artifact; SHA + run-ID captured in 04-UAT.md"
    - "Developer sideloads the IPA and runs an interactive Phase 4 sideload session at the desk (per Phase 3.1 D1 decision: walk = sideload session, not physical walking)"
    - "Walk #1 captures: (a) wisps spawn along disc perimeter on every new GPS-derived disc; (b) wisps drift outward over 2.5 s and fade; (c) wisps remain anchored to underlying map during pan + pinch-zoom; (d) PERF-07 frame_delta JSONL rollups show medianMs ≤ 16, p95Ms ≤ 32, maxMs ≤ 48; (e) PERF-08 SDF rebuild rate unchanged from Walk #5 baseline; (f) C3' extreme-distance pan (~50-100 km from Melun) renders wisps without fp32 jitter; (g) UX-02 rotation gestures are no-ops"
    - "WispTransformLogger JSONL stream Mail-shared post-walk; bounds-of-bounds for lat/lon/screenX/screenY captured for grep-correlation against fog_transform + sdf + frame_delta"
    - "Phase 4 verdict authored in 04-FALSIFICATION.md: CONFIRMED-AFTER-FIX | ITERATING-WITH-PARTIAL-PROGRESS | DENIED, with verbatim developer quotes + JSONL evidence"
    - "If verdict CONFIRMED: ROADMAP.md flips Phase 4 to Complete; Phase 5 transitions to Pending. If verdict NOT confirmed: Plan 04-XX gap-closure plan authored in follow-up /gsd:plan-phase 04 --gaps invocation."
  artifacts:
    - path: ".planning/phases/04-wisp-particles/04-FALSIFICATION.md"
      provides: "Phase 4 falsification document — Criteria + walk evidence + verdict"
      min_lines: 100
    - path: ".planning/phases/04-wisp-particles/04-UAT.md"
      provides: "Walk #1 UAT log — CI run ID + SHA + walk steps + JSONL excerpts + Mail-share confirmation"
      min_lines: 80
  key_links:
    - from: ".planning/phases/04-wisp-particles/04-FALSIFICATION.md"
      to: "WispTransformLogger JSONL evidence (infrastructure.mirk.wisp rollups)"
      via: "Walk #1 Mail-share + grep correlation against fog_transform/sdf/frame_delta"
      pattern: "infrastructure\\.mirk\\.wisp"
---

<objective>
Phase 4 verdict authoring. Pre-walk software gates (full suite GREEN + analyze + format + GOSL headers); produce + sideload an IPA via the existing CI workflow; run an interactive sideload session covering the 7 must-have axes (wisp spawn, drift, anchoring, PERF-07, PERF-08, C3' extreme-distance, UX-02 rotation no-op); Mail-share the JSONL session log; author the falsification + UAT documents; record verdict.

Per the Phase 3.1 closure pattern (Plan 03.1-15 Walk #6 closes Phase 3.1 with CONFIRMED-AFTER-FIX FULL), Walk #1 here is the FIRST Phase 4 walk — verdict can be CONFIRMED, PARTIAL (iterate), or DENIED. Iteration policy per CONTEXT §carry-forwards: NO HARD CAP on walk count; Mail-share discipline mandatory; if NOT confirmed, follow-up `/gsd:plan-phase 04 --gaps` authors the gap-closure plan.

Output: Walk evidence + verdict. If CONFIRMED, Phase 4 closes and Phase 5 unblocks.
</objective>

<execution_context>
@C:/Users/oliver/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oliver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-wisp-particles/04-CONTEXT.md
@.planning/phases/04-wisp-particles/04-RESEARCH.md
@.planning/phases/04-wisp-particles/04-VALIDATION.md
@.planning/phases/04-wisp-particles/04-01-SUMMARY.md
@.planning/phases/04-wisp-particles/04-02-SUMMARY.md
@.planning/phases/04-wisp-particles/04-03-SUMMARY.md
@.planning/phases/04-wisp-particles/04-04-SUMMARY.md

# Phase 3.1 walk-document templates — read for falsification doc shape:
@.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-6.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-6.md

# Phase 3 original falsification template (what NOT to repeat):
@.planning/phases/03-fog-of-war-the-hypothesis/03-FALSIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-walk gates + author 04-FALSIFICATION.md skeleton + push CI run + capture run-ID + SHA</name>
  <files>
    .planning/phases/04-wisp-particles/04-FALSIFICATION.md,
    .planning/phases/04-wisp-particles/04-UAT.md
  </files>
  <action>
**Step 1 — Pre-walk software gates (run + verify all GREEN):**

```bash
flutter test                                        # 180+ tests + Phase 4 additions; all GREEN
flutter analyze                                     # 0 issues
dart format --line-length 160 --set-exit-if-changed lib/ test/   # clean
dart run tool/check_headers.dart                    # GOSL headers present everywhere
dart run tool/check_dependencies_md.dart            # DEPENDENCIES.md fresh (no new deps in Phase 4)
```

If any gate fails, FIX before proceeding. CLAUDE.md "Solo dev / Claude is authorized to push directly on main when commits atomic + tests verts localement" — but ONLY when all gates pass.

**Step 2 — Author `04-FALSIFICATION.md` skeleton:**

Use the Phase 3.1 closure document `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-6.md` as the structural template. Pre-walk skeleton (verdict section left blank):

```markdown
# Phase 4: Wisp Particles — Falsification & Walk #1

**Phase:** 04-wisp-particles
**Walk #:** 1
**Date:** 2026-05-XX (TBD at execute time)
**CI Run:** TBD (filled at Step 3)
**SHA:** TBD (filled at Step 3)
**Verdict:** TBD (filled post-walk at Task 2)

## Hypothesis (Phase 4)

The Phase 3.1 cross-pipeline parity discipline (single MapCamera snapshot per build, single canvas-getTransform per paint, single canvas-translate-to-world-frame, single clipPath) generalises to a SECOND visual layer (wisp particles) WITHOUT regressing the Phase 3.1 fog lock and without surfacing new failure modes (fp32 precision at C3' extreme distance, PERF-07 budget overflow under fog + 200 wisps, PERF-08 SDF cache thrash regression).

If true: Phase 4 closes with CONFIRMED-AFTER-FIX; Phase 5 unblocks.
If false: capture failure mode + iterate per the Phase 3.1 model (Plans 03.1-04 through 03.1-14 — 8 walks total; iteration policy has no hard cap).

## Falsification Criteria (must all hold for CONFIRMED)

**Criterion A — Wisp spawn-and-decay visual:** wisps emerge on disc perimeter as new discs land; drift outward; fade over ~2.5 s. Captured by direct visual observation during the walk.

**Criterion B — Same-Canvas anchoring (cross-pipeline parity):** during pan + pinch-zoom + combined gesture, wisps stay locked to the underlying map exactly as the fog does. NO parallax between wisps and fog. NO wisp drift relative to the disc that spawned them.

**Criterion C — PERF-07 budget preserved under fog + 200 wisps:** infrastructure.mirk.frame_delta rollups show medianMs ≤ 16, p95Ms ≤ 32, maxMs ≤ 48 across ≥ 10 combined gestures. Carry-over from Phase 3.1 Walk #5 baseline (13×/20×/28× headroom on fog-only).

**Criterion D — PERF-08 SDF rebuild rate stable:** infrastructure.mirk.sdf rollups show no regression vs Walk #2 baseline (median 68/sec, max 121/sec). Wisp render path must NOT trigger additional SDF rebuilds (Pitfall 4 enforcement).

**Criterion E — C3' extreme-distance regime clean:** developer pans to ~50-100 km from Melun (DEBUG-02 cameraConstraint already removed in Phase 3.1 Plan 03.1-12); wisps render correctly with no fp32 precision artefacts (visible jitter on smooth pan); fog lock preserved.

**Criterion F — UX-02 rotation gestures are no-ops:** two-finger rotation gestures during pinch-zoom do NOT change MapCamera.rotation; canvasTx/Ty stay at 0 across all rollups (regression guard for the Phase 3.1 closure invariant).

**Criterion G — WispTransformLogger evidence captured + grep-correlatable:** infrastructure.mirk.wisp rollups Mail-shared; bounds (lat/lon/screenX/screenY) trackable across the walk; spawn rate matches expected ~20 wisps × N new discs / walk-duration cadence; epochSecond joins cleanly with fog_transform + sdf + frame_delta streams.

## Walk Plan (interactive sideload session at desk per Phase 3.1 D1 decision)

Sequence:
  1. Cold launch IPA on iPhone 17 Pro; wait for permission grant + first GPS fix; observe FpsCounterOverlay + FrameDeltaProbeOverlay.
  2. Default-zoom (z=13 Melun centre) baseline observation: wait 5 s for warmup; observe wisps spawning on subsequent fixes; visually validate Criterion A (spawn) + B (anchoring during gentle pan).
  3. Combined-gesture stress: ≥ 10 deliberate combined pinch-zoom-and-pan gestures; observe Criterion B (anchoring under combined load).
  4. Max-zoom regime: zoom to z=18-19; pan; observe Criterion B + Criterion C (frame_delta rollups via overlay).
  5. C3' extreme-distance: pan to ~50 km then ~100 km from Melun (DEBUG-02 unconstrained); observe Criterion E (wisp jitter? fog lock preserved?).
  6. UX-02 rotation gesture probe: deliberate two-finger rotation gesture; observe Criterion F (gesture is a no-op).
  7. Idle observation: leave the screen idle for 30 s; observe wisp drift continues (per-paint dt integration is fresh; not frozen).
  8. Mail-share session log; verify infrastructure.mirk.{fog_transform, sdf, frame_delta, wisp} rollups all present; verify infrastructure.mirk.wisp epochSecond aligns with siblings.

## Pre-walk Gate Status

- [ ] flutter test full suite GREEN (X tests passed)
- [ ] flutter analyze 0 issues
- [ ] dart format --set-exit-if-changed clean
- [ ] dart run tool/check_headers.dart GREEN
- [ ] dart run tool/check_dependencies_md.dart GREEN
- [ ] CI run pushed; run-ID + SHA captured

## Walk Evidence (filled post-walk by Task 2)

**Verdict:** TBD
**Verbatim developer quote:** TBD
**JSONL excerpts:** TBD
**Per-criterion verdict table:** TBD

## Carry-forward dispositions

- WISP-01..05 status updates (REQUIREMENTS.md): TBD post-verdict
- PERF-07 + PERF-08 walk-time validation: TBD
- UX-02 + DEBUG-02 carry-over re-validation: TBD
- ROADMAP.md Phase 4 status flip: TBD
```

**Step 3 — Author `04-UAT.md` skeleton:**

Same shape as `.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-6.md`:

```markdown
# Phase 4 — Walk #1 UAT Log

**Phase:** 04-wisp-particles
**Walk #:** 1
**Date:** TBD (filled at execute time)
**Tester:** Developer (solo)
**Device:** iPhone 17 Pro (ProMotion 120 Hz)
**Sideload mechanism:** SideStore
**CI Run:** TBD
**SHA:** TBD
**IPA artifact URL:** TBD (gh run view RUNID — captures the artifact link)

## Pre-walk software gate evidence

```
$ flutter test
... XX tests passed ...

$ flutter analyze
0 issues

$ dart format --line-length 160 --set-exit-if-changed lib/ test/
(no output, exit 0)

$ dart run tool/check_headers.dart
✓ ...
```

## Walk steps (1-8 from 04-FALSIFICATION.md)

### Step 1: Cold launch
- Observed: TBD
- FpsCounterOverlay reading: TBD
- FrameDeltaProbeOverlay reading: TBD

### Step 2: Default-zoom warmup observation
- Observed: TBD

(... steps 3-8 ...)

## Mail-shared session log

- Filename: yyyymmddTHHMMSSZ_logs.txt
- Size: TBD
- Mail received at: TBD
- Logger streams present: infrastructure.mirk.{fog_transform, sdf, frame_delta, wisp, dev_marker}
- Sample epochSecond join: TBD (proves grep-correlation works across all 5 streams)

## Verbatim developer quotes
- TBD

## Verdict
- TBD (CONFIRMED | PARTIAL | DENIED)
- Per-criterion table: TBD
```

**Step 4 — Push to main; capture CI run-ID + SHA.**

```bash
# Stage + commit pre-walk skeleton docs
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(04-05): pre-walk gate evidence + 04-FALSIFICATION.md skeleton" --files .planning/phases/04-wisp-particles/04-FALSIFICATION.md .planning/phases/04-wisp-particles/04-UAT.md

# Push (Phase 4 wave-3 commit triggers CI)
git push origin main

# Wait for CI; capture run-ID
gh run list --limit 5
gh run view <RUN-ID>  # confirm "build-ios" job GREEN; download IPA artifact

# Update both 04-FALSIFICATION.md and 04-UAT.md frontmatter with the captured CI Run + SHA
```

CRITICAL CONSTRAINTS:
- The pre-walk skeleton MUST be committed BEFORE the CI run is captured (the CI run-ID is included in the post-walk update — but the skeleton commits the framework first).
- DO NOT push without all software gates GREEN. CLAUDE.md mandate.
- The CI run validation MUST include `gh run view --log-failed` if any job fails — debug the failure (likely a Phase 4 test that didn't make it into the suite OR an analyzer warning), fix, recommit, re-push.
  </action>
  <verify>
    <automated>flutter test && flutter analyze && dart format --line-length 160 --set-exit-if-changed lib/ test/ && dart run tool/check_headers.dart && dart run tool/check_dependencies_md.dart</automated>
  </verify>
  <done>
    - All 5 pre-walk software gates GREEN
    - 04-FALSIFICATION.md skeleton committed with all 7 criteria + walk plan
    - 04-UAT.md skeleton committed with steps 1-8 placeholder
    - CI run pushed; run-ID + SHA captured + frontmatter populated in BOTH skeleton docs
    - Build-ios job GREEN; IPA artifact downloadable from CI run page
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Walk #1 sideload session — interactive UAT + Mail-share + verdict authoring</name>
  <files>.planning/phases/04-wisp-particles/04-FALSIFICATION.md, .planning/phases/04-wisp-particles/04-UAT.md</files>
  <action>
    Sideload the unsigned IPA on iPhone 17 Pro via SideStore; perform Walk #1 per the 8-step protocol enumerated in &lt;how-to-verify&gt;; capture verbatim verdict + Mail-share session log post-walk so Task 3 can grep-correlate the four infrastructure.mirk.{fog_transform, sdf, frame_delta, wisp} JSONL streams. The full walk protocol is in the &lt;how-to-verify&gt; block below; this &lt;action&gt; block exists for plan-checker schema completeness — execute the &lt;how-to-verify&gt; protocol verbatim.
  </action>
  <verify>
    <automated>MANUAL — see &lt;how-to-verify&gt; (PERF-07 + Criteria A/B/E/F subjective + WispTransformLogger JSONL pattern matching are inherently manual UAT walk gates per 04-VALIDATION.md Manual-Only Verifications table; the developer s resume-signal IS the verification).</automated>
  </verify>
  <done>
    Walk #1 complete + developer s verbal verdict captured ( /  /  / ); session log Mail-shared so Task 3 can extract the four mirk.* JSONL streams (including the NEW infrastructure.mirk.wisp stream from Plan 04-02). The verdict outcome drives Task 3 s branch (CONFIRMED-AFTER-FIX / ITERATING-WITH-PARTIAL-PROGRESS / DENIED). PERF-07 medianMs / p95Ms / maxMs values captured for the fog + 200-wisps regime; PERF-08 SDF rebuild rate captured for regression check vs Walk #2 baseline.
  </done>
  <what-built>
    - Wave 0-3 complete: WispParticle + WispParticleSystem + WispTransformLogger fully implemented
    - _FogPainter._renderWisps wired into the same canvas.save/restore as the fog draw, inside the same canvas-translate-to-world-frame, inside the same clipPath, consuming THE same MapCamera snapshot
    - MapScreen wires spawn (on new disc append) + WispTransformLogger lifecycle (start/stop)
    - All 7 falsification criteria (A-G) instrumented + observable
    - Pre-walk software gates GREEN; CI build-ios job produced sideloadable IPA
  </what-built>
  <how-to-verify>

    **Pre-walk setup (developer):**
    1. Locate the CI run captured in Task 1 frontmatter: `gh run view <RUN-ID>` → download `build-ios.zip` artifact
    2. Extract IPA; sideload via SideStore on iPhone 17 Pro
    3. Cold-launch the app; grant permission when prompted
    4. Land on /map; observe FpsCounterOverlay (top:8 right:8) + FrameDeltaProbeOverlay (top:104 right:8)

    **Walk #1 sequence (8 steps from 04-FALSIFICATION.md walk plan):**

    **Step 1 — Cold-launch baseline (~30 s):**
       - Wait for first GPS fix (or absence); observe FpsCounterOverlay reading at idle (expected ~4 fps Flutter no-dirty-frames behaviour — NOT a regression)
       - Observe FrameDeltaProbeOverlay readings (idle: medianMs near 0)
       - Confirm: zero wisps visible (warmup gate active for first 5 s + no fix yet)
       - Note startup time + any visible "no fog yet" frame at startup (per fog_layer.dart line 380-384)

    **Step 2 — Default-zoom warmup observation (~30 s):**
       - Wait until ≥ 5 s after first GPS fix (warmup window passes)
       - Observe wisps spawning on subsequent fixes — Criterion A
       - Visually validate: wisps emerge AT THE DISC PERIMETER (~25 m radius), not at the centre or randomly
       - Visually validate: wisps drift OUTWARD (radial from spawn point) and fade over ~2.5 s
       - Gentle pan: wisps stay anchored to the map (Criterion B partial)

    **Step 3 — Combined-gesture stress (~60 s):**
       - Perform ≥ 10 deliberate combined pinch-zoom-and-pan gestures
       - Observe Criterion B under load: NO parallax between wisps and fog; wisps + fog move TOGETHER as the user pans
       - Observe FrameDeltaProbeOverlay: medianMs should stay green-coded (≤ 16 ms)

    **Step 4 — Max-zoom regime (~60 s):**
       - Pinch-zoom to maximum (z=18-19); pan deliberately
       - Observe Criterion B at max zoom (where Phase 3.1 Walk #4 surfaced 2 dev_markers — the FOG-19 fix should hold for fog; wisps observed independently)
       - Observe Criterion C: FrameDeltaProbeOverlay readings under fog + ~200 wisps active
       - Note: do wisps spawn at max zoom? (The disc-perimeter spawn is world-anchored, so yes — same physical area = same wisps)

    **Step 5 — C3' extreme-distance pan (~90 s):**
       - Pan camera to ~50 km from Melun (any direction)
       - Pause + observe Criterion E: do wisps render? (depends on whether GPS fixes happened there — likely no; expect zero wisps but the existing wisps from earlier should still render correctly when panned back)
       - Pan to ~100 km from Melun
       - Pause + observe: any visible jitter on stationary wisps when camera is parked? (the fp32 hypothesis from Pitfall 5 + RESEARCH §Open Question 1)
       - Pan back to Melun centre to recover spawn activity

    **Step 6 — UX-02 rotation gesture probe (~15 s):**
       - Attempt deliberate two-finger rotation gesture
       - Observe Criterion F: rotation should be a NO-OP (MapCompass stays at 0°; map doesn't rotate)
       - Confirm: NO un-fogged wedges at viewport corners (the Walk #3 regression that UX-02 closed)

    **Step 7 — Idle observation (~30 s):**
       - Leave the screen idle (no gestures, no GPS movement) for 30 s
       - Observe: wisp drift CONTINUES even at idle (proves the per-paint dt integration is fresh, NOT frozen — Pitfall 6 prevention)
       - Observe: idle FrameDeltaProbeOverlay stays green

    **Step 8 — Mail-share session log:**
       - Tap the share-logs button in the AppBar
       - Choose Mail; send to self
       - Verify Mail received with attached `yyyymmddTHHMMSSZ_logs.txt`
       - On the desk machine: download attachment; grep for the 5 logger streams:
         ```bash
         grep "infrastructure.mirk.fog_transform" log.txt | head
         grep "infrastructure.mirk.sdf" log.txt | head
         grep "infrastructure.mirk.frame_delta" log.txt | head
         grep "infrastructure.mirk.wisp" log.txt | head             # NEW Phase 4
         grep "infrastructure.mirk.dev_marker" log.txt | head
         ```
       - Verify: the four streams (fog_transform, sdf, frame_delta, wisp) share epochSecond boundaries (pick one epochSecond value; assert all four streams have a rollup at that second)
       - Verify Criterion G: spawn rate matches expectation (~20 wisps × N new discs / walk duration)

    **Post-walk authoring (developer):**

    Update both 04-FALSIFICATION.md and 04-UAT.md with:
       1. Verbatim developer quote summarising the verdict ("100% solved" / "partial" / "denied" / etc.)
       2. Per-criterion table: A, B, C, D, E, F, G — each marked GREEN | PARTIAL | RED
       3. JSONL excerpts proving Criteria C/D/G (paste 5-10 representative rollup lines from each stream)
       4. Verdict block: CONFIRMED-AFTER-FIX | ITERATING-WITH-PARTIAL-PROGRESS | DENIED
       5. Carry-forward dispositions:
          - REQUIREMENTS.md updates (WISP-01..05 status flips)
          - PERF-07 walk-time validation outcome (medianMs / p95Ms / maxMs measured)
          - PERF-08 walk-time validation outcome (SDF rebuild rate vs Walk #2 baseline)
          - UX-02 + DEBUG-02 re-validation status (3rd / Nth consecutive walk)
          - ROADMAP.md Phase 4 status:
            - If CONFIRMED → flip to "Complete (HYPOTHESIS CONFIRMED 2026-XX-XX)"; Phase 5 unblocks
            - If PARTIAL → leave In Progress; capture iteration axes (similar to Phase 3.1 Walks #1-#5 partial verdicts)
            - If DENIED → flip to "Falsified-in-production"; iteration depends on root-cause analysis

    **Resume signal:** Provide one of:
       - **"approved"** — Walk #1 verdict CONFIRMED-AFTER-FIX; Phase 4 closes; Phase 5 unblocks
       - **"partial: <description>"** — iteration required; describe failure mode for `/gsd:plan-phase 04 --gaps` follow-up
       - **"denied: <description>"** — fundamental Phase 4 architectural failure; describe + we'll discuss next steps
       - **"abort: <reason>"** — pre-walk blockers prevent walk (e.g., IPA fails to launch, sideload broken, etc.)
  </how-to-verify>
  <resume-signal>Type "approved" / "partial: ..." / "denied: ..." / "abort: ..."</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Post-walk verdict commit + ROADMAP/REQUIREMENTS updates + (if CONFIRMED) Phase 5 unblock signal</name>
  <files>
    .planning/phases/04-wisp-particles/04-FALSIFICATION.md,
    .planning/phases/04-wisp-particles/04-UAT.md,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md,
    .planning/STATE.md
  </files>
  <action>
**Step 1 — Reflect Task 2's resume signal in the docs:**

Read the developer's resume-signal verbatim quote from Task 2; verbatim-include in 04-FALSIFICATION.md "Verbatim developer quote" + 04-UAT.md "Verbatim developer quotes" sections.

**Step 2 — Author the per-criterion table in 04-FALSIFICATION.md "Walk Evidence" section:**

```markdown
## Per-Criterion Verdict Table

| Criterion | Status | Evidence |
|-----------|--------|----------|
| A — Wisp spawn-and-decay visual | GREEN/PARTIAL/RED | TBD verbal + JSONL spawn rate |
| B — Same-Canvas anchoring (cross-pipeline parity) | GREEN/PARTIAL/RED | TBD verbal + WispTransformLogger lat/lon/screenX/Y bounds tracking |
| C — PERF-07 budget preserved (≤16/32/48 ms) | GREEN/PARTIAL/RED | TBD frame_delta rollup excerpt |
| D — PERF-08 SDF rebuild rate stable | GREEN/PARTIAL/RED | TBD sdf rollup excerpt vs Walk #2 baseline |
| E — C3' extreme-distance regime clean | GREEN/PARTIAL/RED | TBD verbal + wisp screenXMin/Max bounds at 50-100 km |
| F — UX-02 rotation gestures no-op | GREEN/PARTIAL/RED | TBD verbal + canvasTx/Ty values from fog_transform rollups |
| G — WispTransformLogger evidence captured + grep-correlatable | GREEN/PARTIAL/RED | TBD JSONL excerpts + epochSecond join example |
```

**Step 3 — Write the verdict block:**

Per the resume signal:

- If "approved" → CONFIRMED-AFTER-FIX (Phase 4 CLOSES). Mirror the structure of `03.1-FALSIFICATION-6.md` "Phase 3.1 verdict (P03.1-15 Walk #6 ...)" block.
- If "partial: ..." → ITERATING-WITH-PARTIAL-PROGRESS. Mirror the structure of `03.1-FALSIFICATION.md`'s partial verdicts (Walks #1, #3). Capture the specific iteration axis surfaced by the developer's quote.
- If "denied: ..." → DENIED. Mirror `03-FALSIFICATION.md` Plan 03-08 verdict shape. NOTE: Phase 4 DENIED is structurally unlikely (the architectural keystone is Phase 3.1's; Phase 4 is integration-level), but the framework supports it.
- If "abort: ..." → no verdict; document the blocker; the executor (this Task 3 logic) creates a `04-05-BLOCKER.md` and stops.

**Step 4 — Update ROADMAP.md based on verdict:**

If CONFIRMED:
- Phase 4 row in ## Phases section: `[ ]` → `[x]` and append `**Complete (HYPOTHESIS CONFIRMED 2026-XX-XX)**`.
- Phase 5 row: append `**UNBLOCKED 2026-XX-XX by Phase 4 Walk #1 CONFIRMED**`.
- ## Progress table: Phase 4 row Status → Complete; Plans Complete column → "5/5 (or however many landed; if iteration happened, count includes the gap-closure plans)".
- Add a Phase 4 verdict block (mirror Phase 3.1 verdict block style) to the ## Phases section.

If PARTIAL:
- Phase 4 row stays `[ ]`. Append `**ITERATING (Walk #1 PARTIAL: <iteration axis>)**`.
- Add Phase 4 verdict block capturing the partial outcome + iteration axes.
- Phase 5 stays Pending.

If DENIED (rare):
- Phase 4 row stays `[ ]`. Append `**DENIED Walk #1 — root-cause analysis required**`.
- Phase 5 stays Blocked.

**Step 5 — Update REQUIREMENTS.md based on verdict:**

For CONFIRMED:
- WISP-01..05: `[ ]` → `[x]` with `_Complete — Verified-by-test + walk-time validated (Plan 04-05 Walk #1 verbatim "...")_`.
- PERF-07 + PERF-08 carry-overs: append walk-time-validated 4th consecutive walk (or whatever the count is post-Phase-4).
- UX-02 + DEBUG-02 carry-overs: same — walk-time-validated Nth consecutive walk.

For PARTIAL: WISP-XX requirements that are walk-confirmed flip; the partial axes stay `[ ]` with iteration-clause pointing at 04-FALSIFICATION.md.

For DENIED: WISP-01..05 flip to `Falsified-in-production` per the Phase 3 / 03.1 precedent.

**Step 6 — Update STATE.md:**

Append a Phase 4 Walk #1 entry at the appropriate chronological position (top of recent activity log). Mirror the Phase 3.1 closure entry style. Capture: verdict, devices, JSONL evidence summary, ROADMAP transitions.

**Step 7 — Commit + push.**

```bash
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(04-05): Phase 4 Walk #1 verdict + ROADMAP/REQUIREMENTS/STATE updates" --files .planning/phases/04-wisp-particles/04-FALSIFICATION.md .planning/phases/04-wisp-particles/04-UAT.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md

git push origin main
```

**Step 8 — Surface next-action signal.**

If CONFIRMED → output: "Phase 4 closes. Phase 5 (Decision Gate) UNBLOCKS. Run `/gsd:discuss-phase 5`."

If PARTIAL → output: "Phase 4 iterates. Run `/gsd:plan-phase 04 --gaps` to author the gap-closure plan addressing: <iteration axes>."

If DENIED → output: "Phase 4 DENIED. Root-cause analysis required. Run `/gsd:discuss-phase 4` to triage; potentially `/gsd:research-phase 4` if architectural rework needed."

CRITICAL CONSTRAINTS:
- DO NOT auto-flip ROADMAP/REQUIREMENTS unless the resume signal is unambiguous. If the developer's signal was "partial: ..." with an unclear iteration axis, ASK for clarification before authoring; don't guess.
- The Phase 4 closure ROADMAP block MUST cite Walk #1's CI run-ID + SHA + verbatim developer quote (mirror 03.1-15 Walk #6 verdict block precision — 03.1-FALSIFICATION-6.md is the gold-standard template).
- Mail-share verification was REQUIRED for Walks #1-5 of Phase 3.1 but NOT for Walk #6 (verbal verdict was decisive). For Phase 4 Walk #1, Mail-share is REQUIRED (it's the FIRST walk; verbal verdict alone isn't sufficient evidence for closure).
  </action>
  <verify>
    <automated>echo "Manual verification — see done criteria"</automated>
  </verify>
  <done>
    - 04-FALSIFICATION.md verdict block authored per Task 2 resume signal
    - Per-criterion table populated with status + evidence
    - 04-UAT.md verbatim developer quote + JSONL excerpts present
    - ROADMAP.md Phase 4 row + verdict block reflect outcome
    - REQUIREMENTS.md WISP-01..05 + carry-over rows updated
    - STATE.md chronological entry appended
    - Single git commit + push
    - Next-action signal surfaced to user (Phase 5 unblock OR iteration plan invocation)
  </done>
</task>

</tasks>

<verification>
- 04-FALSIFICATION.md fully populated post-walk: criteria + walk plan + evidence + per-criterion table + verdict + carry-forward dispositions
- 04-UAT.md fully populated: pre-walk gate evidence + walk-step observations + Mail-share confirmation + verbatim developer quotes + verdict
- ROADMAP.md Phase 4 row reflects outcome (Complete | In Progress | Falsified)
- REQUIREMENTS.md WISP-01..05 status flips reflect verdict
- STATE.md captures the chronological entry
- All changes committed atomically + pushed to main
- Next-action signal clear (Phase 5 unblock OR `/gsd:plan-phase 04 --gaps` for iteration)
</verification>

<success_criteria>
- Pre-walk software gates ALL GREEN (test, analyze, format, headers, deps).
- CI build-ios job GREEN; sideloadable IPA in hand.
- Walk #1 executed; 7 falsification criteria observed; Mail-share session log captured + grep-correlated.
- Verdict authored in 04-FALSIFICATION.md + 04-UAT.md.
- ROADMAP + REQUIREMENTS + STATE updated to reflect verdict.
- Phase 4 either CLOSES (CONFIRMED → Phase 5 unblocks) or surfaces iteration axes (PARTIAL → `/gsd:plan-phase 04 --gaps`) or fails fundamentally (DENIED → triage).
</success_criteria>

<output>
After completion, create `.planning/phases/04-wisp-particles/04-05-SUMMARY.md`:
- Walk #1 verdict (CONFIRMED | PARTIAL | DENIED)
- CI run-ID + SHA
- Per-criterion outcome
- ROADMAP/REQUIREMENTS/STATE deltas
- Next-action signal
- (If CONFIRMED) Phase 4 closure timestamp + Phase 5 unblock notice
- (If PARTIAL) Iteration axes captured for follow-up plan-phase invocation
</output>
