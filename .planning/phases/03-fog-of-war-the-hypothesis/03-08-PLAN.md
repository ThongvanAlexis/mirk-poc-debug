---
phase: 03-fog-of-war-the-hypothesis
plan: 08
type: execute
wave: 5
depends_on:
  - 03-07
files_modified:
  - .planning/phases/03-fog-of-war-the-hypothesis/03-FALSIFICATION.md
  - .planning/phases/03-fog-of-war-the-hypothesis/03-UAT.md
autonomous: false
requirements:
  - PERF-03
  - PERF-04
  - PERF-05

must_haves:
  truths:
    - "Pre-walk gate sequence executed: full flutter test suite GREEN; flutter analyze clean; dart format clean; LOC-03/BOOT-02 CI gates green; CI run for the Plan 03-07 head commit produced a downloadable IPA"
    - "The sideloaded IPA opens /sanity successfully on iPhone 17 Pro: fog renders with circular reveal hole at viewport centre; no shader-compile exceptions in the FileLogger output"
    - "≥ 5 minute walk through Melun completed on iPhone 17 Pro with ≥ 10 deliberate combined pinch-zoom-and-pan gestures + ≥ 3 recenter taps"
    - "Pan-FPS with fog active observed ≥ 30 fps; idle-fog-animation FPS observed ≥ 50 fps (PERF-03)"
    - "Frame-delta probe rollups during ≥ 10 combined-gesture seconds show median ≤ 16 ms, p95 ≤ 32 ms, max ≤ 48 ms (PERF-04 — Criterion A)"
    - "Subjective verdict at end of walk: no fog slide-then-snap, no white-ellipse, no perceptible reveal-hole lag, no inversion (PERF-05 — Criterion B)"
    - "03-FALSIFICATION.md walk-evidence section appended with probe rollup lines + SDF rebuild lines + FPS observations + subjective notes; verdict written (confirmed / denied / confirmed-with-caveats); MirkFall migration recommendation appended"
    - "03-UAT.md exists summarising the walk (analogous to .planning/phases/02-map-no-fog/02-UAT.md) — date, device, CI run + SHA, gestures performed, frame-delta probe summary numbers, subjective verdict, link to 03-FALSIFICATION.md"
  artifacts:
    - path: ".planning/phases/03-fog-of-war-the-hypothesis/03-FALSIFICATION.md"
      provides: "Falsification document AMENDED with walk evidence + verdict; pre-walk skeleton from Plan 03-01 preserved verbatim"
      contains: "Walk Evidence"
    - path: ".planning/phases/03-fog-of-war-the-hypothesis/03-UAT.md"
      provides: "Phase 3 UAT walk summary — mirrors the shape of Phase 2's 02-UAT.md"
      contains: "PERF-03"
  key_links:
    - from: ".planning/phases/03-fog-of-war-the-hypothesis/03-FALSIFICATION.md"
      to: ".planning/phases/03-fog-of-war-the-hypothesis/03-UAT.md"
      via: "cross-reference between the formal falsification doc and the operational UAT summary"
      pattern: "03-UAT.md"
---

<objective>
Execute the Phase 3 UAT walk and produce the binary answer to the architectural hypothesis. This plan is the deliverable: walk Melun on iPhone 17 Pro, observe PERF-03 (pan-FPS ≥ 30, idle-fog-animation FPS ≥ 50), capture FOG-08 frame-delta probe rollups against PERF-04 thresholds (Criterion A: median ≤ 16 ms / p95 ≤ 32 ms / max ≤ 48 ms), record the PERF-05 subjective verdict (Criterion B: no fog slide-then-snap, no white-ellipse, no reveal-hole lag, no inversion), and write the verdict — confirmed / denied / confirmed-with-caveats — into 03-FALSIFICATION.md.

Purpose: This is the moment the POC's core question gets answered. A "confirmed" verdict unlocks Phase 4 (wisp particles). A "denied" verdict TERMINATES the project — the same-Canvas hypothesis is the formal counter-evidence and MirkFall does not migrate. Either outcome is a valid scientific result; the value of the POC is the discrimination, not the direction.

Output: 03-FALSIFICATION.md amended with walk evidence (probe rollup lines, SDF rebuild lines, FPS observations, subjective notes) AND the formal verdict. 03-UAT.md created mirroring 02-UAT.md's shape. Phase 3 is closed (all 11 requirement IDs marked Complete in REQUIREMENTS.md).

The walk itself is a `checkpoint:human-verify` task — Claude has run all the automatable gates (test suite, analyze, format, CI green) and assembled the IPA's prerequisites; the human-walking-with-the-iPhone is the only remaining step. After the walk, Claude amends the falsification document and writes 03-UAT.md from the developer's verbal/written notes.
</objective>

<execution_context>
@C:/Users/oliver/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oliver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-fog-of-war-the-hypothesis/03-CONTEXT.md
@.planning/phases/03-fog-of-war-the-hypothesis/03-RESEARCH.md
@.planning/phases/03-fog-of-war-the-hypothesis/03-FALSIFICATION.md
@.planning/phases/03-fog-of-war-the-hypothesis/03-07-SUMMARY.md
@.planning/phases/02-map-no-fog/02-UAT.md
@.planning/phases/02-map-no-fog/02-06-PLAN.md
@.planning/phases/02-map-no-fog/02-06-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-walk automation gates</name>
  <files>(no file mods — runs verification commands; outputs a pre-walk readiness summary into 03-UAT.md)</files>
  <action>
    Run every automatable gate that must pass BEFORE the human-walking step:

    1. `flutter test` — full suite GREEN. If anything fails, STOP and surface the failure to the developer.
    2. `flutter analyze --fatal-infos` — 0 warnings on lib/ + test/.
    3. `dart format --line-length 160 --set-exit-if-changed lib/ test/` — clean.
    4. `dart test tool/test/` — LOC-03 + BOOT-02 + license-check gates GREEN.
    5. `gh run list --branch main --limit 5` — confirm the CI run for the head commit (Plan 03-07's merge) is GREEN; capture the run ID and SHA.
    6. `gh run download <runId> --name ipa-unsigned` — fetch the IPA artifact path; print the path for the developer to sideload via SideStore.
    7. Confirm `.planning/phases/03-fog-of-war-the-hypothesis/03-FALSIFICATION.md` already exists with Criteria A + B written (Plan 03-01 wrote the skeleton).

    Create `.planning/phases/03-fog-of-war-the-hypothesis/03-UAT.md` skeleton (mirroring 02-UAT.md's structure):
    ```markdown
    # Phase 3 UAT Walk

    **Date:** _pending_
    **Device:** iPhone 17 Pro (ProMotion, A19 Pro)
    **iOS:** _pending_
    **CI run:** _pending — gh run ID + SHA filled by Task 1_
    **IPA artifact:** _pending — path filled by Task 1_

    ## Pre-walk Gates

    - [ ] flutter test full suite GREEN
    - [ ] flutter analyze 0 warnings
    - [ ] dart format clean
    - [ ] dart test tool/test/ (LOC-03 + BOOT-02 + license-check) GREEN
    - [ ] CI run for head commit GREEN (lint + android + ios)
    - [ ] IPA artifact downloaded
    - [ ] /sanity smoke test on sideloaded IPA — fog renders with circular reveal hole at viewport centre, no exceptions in FileLogger output

    ## Walk Protocol

    - Where: Central Melun (≈ lat 48.5397, lon 2.6553).
    - Duration target: ≥ 5 minutes continuous walk.
    - Gestures target: ≥ 10 deliberate combined pinch-zoom-and-pan, ≥ 3 recenter taps.
    - HUD visible during walk: FpsCounterOverlay (top:8) + MapCompass (top:56) + FrameDeltaProbeOverlay (top:104), all right:8.
    - Post-walk: share session log via Mail (LOG-04 / share_plus); paste the relevant `infrastructure.mirk.frame_delta` JSONL lines + `infrastructure.mirk.sdf` JSONL lines into 03-FALSIFICATION.md walk-evidence section.

    ## Walk Evidence

    _pending — filled after walk_

    ### Frame-delta probe rollups (PERF-04, Criterion A)

    _paste relevant 1-second JSONL lines from `infrastructure.mirk.frame_delta`_

    ### SDF rebuild rollups

    _paste relevant 1-second JSONL lines from `infrastructure.mirk.sdf`_

    ### FPS observations (PERF-03)

    _free-form notes from FpsCounterOverlay during pan / pinch / combined gestures / idle_

    ### Subjective verdict (PERF-05, Criterion B)

    _developer's free-form notes; one bullet per Criterion B sub-claim_

    ## Verdict

    _pending — written after walk_

    See `.planning/phases/03-fog-of-war-the-hypothesis/03-FALSIFICATION.md` for the formal verdict + MirkFall migration recommendation.
    ```

    Update 03-UAT.md with the pre-walk-gate results filled in (date, CI run ID, SHA, IPA path) so the developer has one document to glance at while sideloading.

    Verify: 03-UAT.md exists with the gates section populated; all gates green.
  </action>
  <verify>
    <automated>flutter test &amp;&amp; flutter analyze --fatal-infos &amp;&amp; dart format --line-length 160 --set-exit-if-changed lib/ test/</automated>
    <automated>test -f .planning/phases/03-fog-of-war-the-hypothesis/03-UAT.md &amp;&amp; grep -q "CI run" .planning/phases/03-fog-of-war-the-hypothesis/03-UAT.md</automated>
  </verify>
  <done>
    - All automated pre-walk gates pass
    - 03-UAT.md skeleton committed with pre-walk-gate section filled (CI run ID + SHA + IPA path)
    - Developer informed of the IPA path so they can sideload via SideStore
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Sideload IPA + execute Phase 3 falsification walk on iPhone 17 Pro</name>
  <files>(no file mods — manual UAT walk on physical device; outputs feed Task 3)</files>
  <action>
    Hand off to the developer for the manual UAT walk on iPhone 17 Pro. The full procedure is documented below in the checkpoint-specific `<what-built>`, `<how-to-verify>`, and `<resume-signal>` sections — those are the executor-facing instructions. Claude's `<action>` here: pause execution, present the checkpoint to the developer, wait for the resume signal containing the walk evidence + verdict.
  </action>
  <verify>
    <automated>MISSING — PERF-03/04/05 are inherently manual UAT walk gates per 03-VALIDATION.md "Manual-Only Verifications" table; no automated proxy exists. The developer's resume-signal IS the verification.</automated>
  </verify>
  <done>
    Developer returns with: median/p95/max numbers from the FrameDeltaProbe rollups during ≥ 10 combined-gesture seconds; FPS observations during pan / idle / pinch / combined; subjective verdict on each Criterion B sub-claim (slide-then-snap, white-ellipse, reveal-hole lag, inversion); final verdict — confirmed / denied / confirmed-with-caveats; MirkFall migration recommendation.
  </done>
  <what-built>
    Plans 03-01..03-07 shipped:
    - FOG-01: 25 m disc spawned on every GPS fix
    - FOG-02..03: SDF builder (donor) + hash cache + 1 Hz JSONL rollup
    - FOG-04..07: FogLayer custom flutter_map layer paints in same Canvas as VectorTileLayer; identity uSdfRect; single MapCamera snapshot lock; clip path + 41-uniform shader
    - FOG-08: frame-delta self-debug probe with 1 Hz rollup, broadcast stream, JSONL log; on-screen overlay (top:104, right:8) with green/yellow/red color coding against Criterion A thresholds
    - Pre-walk gate: /sanity route renders fog with synthetic 80 m disc at viewport centre using the SAME FogShaderUniforms.setAll path FogLayer uses

    The IPA from Task 1 carries all of the above. Pre-walk automation gates GREEN.
  </what-built>
  <how-to-verify>
    **Pre-walk shader-sanity gate (BEFORE the walk):**
    1. Sideload the IPA from Task 1's reported path via SideStore on iPhone 17 Pro.
    2. Open the app; grant location permission as in Phase 2 walk.
    3. Once on /map, tap the AppBar Icons.science button → navigate to /sanity.
    4. Confirm the screen renders: fog with a circular reveal hole visible somewhere centered on screen (the 80 m disc). The fog should look "atmospheric" (cool indigo palette per kMirkFog* constants).
    5. Tap share-logs (LOG-04). Mail yourself the session log. Open the gzipped log on a desktop and grep for `severe` or `Failed to load fog shader` — there should be NONE. If shader-compile errors are present, the walk is ABORTED — the IPA is broken; raise the issue and stop.
    6. Tap back from /sanity to /map (or context.go('/map') equivalent — TBD by /sanity screen UX; if no back button, manual exit is fine).

    **The walk:**
    7. Start in central Melun. Tap the recenter FAB once to center on your current GPS position.
    8. Walk ≥ 5 minutes continuous. During the walk, perform:
       - ≥ 10 deliberate combined pinch-zoom-and-pan gestures (rotate the camera while pinching while panning — the worst-case stress on the same-Canvas hypothesis).
       - ≥ 3 recenter taps (the FAB).
       - At least one full pan from one edge of Melun's PMTiles bbox to the other.
       - At least one zoom-out to z10 (min) and zoom back in to z15 (max).
    9. While walking, glance at the FrameDeltaProbeOverlay (top-right under MapCompass). Note any colour changes (green → yellow → red).
    10. Note FPS observations from the FpsCounterOverlay during pan / idle / pinch / combined gestures.
    11. Note any visual artifacts:
        - Fog slide-then-snap during pan (fog visibly LAGS the map and then jumps to catch up)
        - White-ellipse during fast pinch-zoom (a visible band where the fog is missing during the scale change)
        - Reveal-hole lag behind the blue dot (the clear circle around your position trails the dot instead of staying centred on it)
        - Inversion at any zoom level (fog appears where reveal should be, OR reveal where fog should be)

    **Post-walk:**
    12. Tap share-logs (LOG-04). Mail yourself the session log. Open the gzipped log file.
    13. grep `infrastructure.mirk.frame_delta` for the 1-second JSONL rollups during your walk. Note the median / p95 / max distributions across the gesture-heavy seconds.
    14. grep `infrastructure.mirk.sdf` for the SDF rebuild rollups.
    15. Report back to Claude:
        - **Quantitative:** the actual median / p95 / max numbers from the probe rollups during the ≥ 10 combined-gesture seconds. State whether Criterion A passed (median ≤ 16 ms, p95 ≤ 32 ms, max ≤ 48 ms).
        - **Qualitative (PERF-03):** the FPS observations during pan / idle / pinch / combined gestures.
        - **Subjective (PERF-05, Criterion B):** verbal "approved" or "denied" with notes on each of the four sub-claims (no fog slide-then-snap, no white-ellipse, no reveal-hole lag, no inversion).
        - **Final verdict:** _confirmed_ / _denied_ / _confirmed-with-caveats_, AND your MirkFall migration recommendation.
  </how-to-verify>
  <resume-signal>
    Type one of:
    - `approved median=Xms p95=Yms max=Zms PERF-03=passed PERF-05=passed verdict=confirmed migrate=yes` (one-liner shorthand)
    - Free-form text describing the walk evidence + verdict
    - `denied {reasons}` if the walk falsifies the hypothesis (Criterion A or Criterion B fails)
    - `aborted {reasons}` if the pre-walk shader-sanity gate fails or the IPA is unwalkable

    Claude then proceeds to Task 3 (amend the falsification document + write 03-UAT.md walk-evidence + update REQUIREMENTS.md).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Amend 03-FALSIFICATION.md + 03-UAT.md with walk evidence + verdict; update REQUIREMENTS.md</name>
  <files>
    .planning/phases/03-fog-of-war-the-hypothesis/03-FALSIFICATION.md,
    .planning/phases/03-fog-of-war-the-hypothesis/03-UAT.md,
    .planning/REQUIREMENTS.md,
    .planning/STATE.md,
    .planning/ROADMAP.md
  </files>
  <action>
    From the developer's resume-signal in Task 2:

    1. **Amend `.planning/phases/03-fog-of-war-the-hypothesis/03-FALSIFICATION.md`**:
       - Fill `Walked on:` with today's date.
       - Fill `Verdict:` with confirmed / denied / confirmed-with-caveats.
       - Paste the relevant `infrastructure.mirk.frame_delta` JSONL lines into the Walk Evidence → Probe rollup section (one line per active second during the ≥ 10 combined-gesture window — typically 10-20 lines).
       - Paste the relevant `infrastructure.mirk.sdf` JSONL lines into the SDF rebuild rollup section.
       - Write the FPS observations into the FPS observations section (free-form, the developer's words).
       - Write the subjective verdict into the Subjective verdict section as one bullet per Criterion B sub-claim (slide-then-snap / white-ellipse / reveal-hole lag / inversion — each gets a "yes/no — notes" line).
       - Fill the Verdict checklist:
         - [x] Criterion A passed? — yes/no, with the actual median/p95/max numbers from the rollups
         - [x] Criterion B passed? — yes/no, with notes from the subjective section
       - Fill the **Outcome:** line with the developer's verdict.
       - Fill the **MirkFall migration recommendation:** with the developer's words (e.g. "port back" / "do not port back" / "port back with caveats" + one paragraph rationale).

    2. **Amend `.planning/phases/03-fog-of-war-the-hypothesis/03-UAT.md`**:
       - Fill the per-section pending placeholders with the same content used for 03-FALSIFICATION.md (probe rollups, SDF rollups, FPS notes, subjective notes).
       - Tick the pre-walk gate checkboxes.
       - Add a final "Verdict" section linking to the falsification doc.

    3. **Update `.planning/REQUIREMENTS.md`**:
       - Mark FOG-01..08 as `[x]` (Complete) iff Task 2 reported `confirmed` OR `confirmed-with-caveats`. If `denied`, leave as `[ ]` and add a note that Phase 3 closed with a denied hypothesis (project terminates).
       - Mark PERF-03..05 as `[x]` iff confirmed/confirmed-with-caveats; otherwise document the failed observations alongside the requirement.
       - Add a Phase 3 closure entry to the Revisions section, mirroring Phase 2's pattern (date, plan, verbal verdict, sustained observations, evidence link).

    4. **Update `.planning/STATE.md`**:
       - Bump `status` to a Phase 3 closure summary.
       - Bump `last_activity` to today with a one-line summary.
       - Append a Decision row under "Accumulated Context" → "Decisions" mirroring the Phase 2 closure entry's shape (Plan 03-08 walk evidence + verdict + Phase 4 unblock OR project termination).
       - Update progress.completed_phases to 3 (if confirmed) and total plans counts.

    5. **Update `.planning/ROADMAP.md`**:
       - Mark Phase 3 `[x]` (if confirmed) with completion date.
       - Update the `## Progress` table.

    6. Verify all four .md files are valid markdown (no broken references) and committed.
  </action>
  <verify>
    <automated>node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" frontmatter validate ".planning/phases/03-fog-of-war-the-hypothesis/03-08-PLAN.md" --schema plan</automated>
  </verify>
  <done>
    - 03-FALSIFICATION.md amended with walk evidence + verdict + MirkFall recommendation; pre-walk content from Plan 03-01 preserved verbatim above the new sections
    - 03-UAT.md amended with full walk evidence; pre-walk gate checkboxes ticked
    - REQUIREMENTS.md FOG-01..08 + PERF-03..05 reflect verdict outcome
    - STATE.md + ROADMAP.md updated with Phase 3 closure
    - All 4 documents committed in a single commit (`docs(03): close Phase 3 — falsification walk evidence + verdict`)
  </done>
</task>

</tasks>

<verification>
- 03-FALSIFICATION.md contains: pre-walk Criteria A + B (from Plan 03-01); walk evidence (probe rollups + SDF rollups + FPS notes + subjective notes); formal verdict; MirkFall migration recommendation
- 03-UAT.md exists and parallels 03-FALSIFICATION.md's evidence section
- REQUIREMENTS.md updated with FOG + PERF requirement statuses based on verdict outcome
- STATE.md + ROADMAP.md reflect Phase 3 closure (or project termination if denied)
- All Phase 3 plan files committed; any code changes from Plans 03-01..07 unchanged by this plan
</verification>

<success_criteria>
- The Phase 3 deliverable (binary answer to the same-Canvas hypothesis) is committed to the repo at .planning/phases/03-fog-of-war-the-hypothesis/03-FALSIFICATION.md
- All 11 Phase 3 requirement IDs are addressed in REQUIREMENTS.md (Complete iff verdict is confirmed/confirmed-with-caveats; otherwise documented as failed observations)
- Phase 3 closes — either Phase 4 unblocks (if confirmed) or the project formally terminates (if denied) with the falsification document standing as the architectural counter-evidence
- Plan 03-08 produces the artefact Phase 5 will promote to repo root POC_VERDICT.md (Phase 5 work — deferred per CONTEXT.md)
</success_criteria>

<output>
After completion, create `.planning/phases/03-fog-of-war-the-hypothesis/03-08-SUMMARY.md` covering:
- Pre-walk gates run (which commands, all GREEN)
- CI run + SHA + IPA artifact path
- Walk evidence summary (median/p95/max numbers; FPS observations; subjective verdict)
- Verdict outcome — confirmed / denied / confirmed-with-caveats
- MirkFall migration recommendation (one paragraph)
- Phase 3 closure status — all 11 requirement IDs Complete? OR project terminates with denied hypothesis?
- Link to 03-FALSIFICATION.md (the formal artefact)
- Phase 4 readiness (if confirmed) OR Phase 5 promotion plan (if denied — Phase 5 still produces a POC_VERDICT.md citing the denied hypothesis as the formal recommendation)
</output>
