---
phase: 02-map-no-fog
plan: 06
type: execute
wave: 4
depends_on: [05]
files_modified:
  - .planning/phases/02-map-no-fog/02-UAT.md
  - .planning/phases/02-map-no-fog/deferred-items.md
autonomous: false
requirements:
  - PERF-02

must_haves:
  truths:
    - "Developer sideloads the IPA built from main on iPhone 17 Pro and the app launches without crash"
    - "Developer walks 200 m through central Melun at zoom 13–15"
    - "Developer performs ≥ 10 pure pans, ≥ 10 pure pinch-zooms, ≥ 10 combined pinch+pan gestures during the walk"
    - "FpsCounterOverlay sustains ≥ 40 fps during pan-FPS at zoom 13–15 (PERF-02 gate)"
    - "First-launch log records `Copied Fra_Melun.pmtile (~4 MB) in <500 ms` exactly once during the permission-grant flow"
    - "Subsequent launches skip the copy log line (idempotent)"
    - "Blue dot follows GPS fixes during the walk; recenter FAB animates camera to _lastFix at zoom 15 (LOC-04 sanity check)"
    - "Rapid zoom out (z=15 → 8 → 15) repaints tiles without persistent blank flashes (brief decode flicker on cold cache acceptable)"
    - "Verbal `approved` from developer is the exit gate (mirroring Phase 1 LOG-05 pattern)"
  artifacts:
    - path: ".planning/phases/02-map-no-fog/02-UAT.md"
      provides: "UAT walk record with pre-walk falsification thresholds, walk evidence (FPS samples, log excerpts), and the developer's verdict"
      contains: "verdict"
    - path: ".planning/phases/02-map-no-fog/deferred-items.md"
      provides: "Captures any deferred items surfaced during the walk (e.g. label-thinning if PERF-02 gates failed; UI quirks)"
      contains: "deferred"
  key_links:
    - from: ".planning/phases/02-map-no-fog/02-UAT.md"
      to: ".planning/REQUIREMENTS.md"
      via: "PERF-02 verbal approval recorded inline + REQUIREMENTS.md flips PERF-02 from Pending to Complete"
      pattern: "PERF-02"
---

<objective>
PERF-02 sideload UAT walk on iPhone 17 Pro through central Melun.
Decide whether `vector_map_tiles 8.0.0` at zoom 13–15 sustains ≥ 40 fps
on the bundled `Fra_Melun.pmtile` archive — the gate before Phase 3
fog work begins. STATE.md flagged this as the highest-probability
project-blocking risk.

Purpose: This is the only walk-evidence gate in Phase 2. Phases 1 and 5
also have walks, but Phase 2's gate decides whether the same-Canvas
hypothesis is even testable in Phase 3 (if pan-FPS without fog is
already sub-40, there's no headroom left for the fog shader). A FAIL
here forces a label-thinning mitigation plan (Phase 2.1 INSERTED) before
Phase 3 can start; a PASS unblocks Phase 3 immediately.

Output: `02-UAT.md` records pre-walk falsification thresholds, walk
evidence (FPS samples, log excerpts, gesture-count tally), and the
developer's verdict (verbal `approved` or detailed FAIL diagnosis with
mitigation route). REQUIREMENTS.md PERF-02 flips Pending → Complete.

This plan is NOT autonomous: the executor automates everything possible
(IPA build via CI, sideload prep docs reference, verdict template),
then yields a single `checkpoint:human-verify` task for the actual walk.
</objective>

<execution_context>
@C:/Users/oliver/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oliver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-map-no-fog/02-CONTEXT.md
@.planning/phases/02-map-no-fog/02-RESEARCH.md
@.planning/phases/02-map-no-fog/02-VALIDATION.md
@.planning/phases/02-map-no-fog/02-05-SUMMARY.md
@.planning/phases/01-foundation/01-07-PLAN.md
@.planning/phases/01-foundation/01-07-SUMMARY.md
@.planning/phases/01-foundation/deferred-items.md
@docs/flutter-ios-specifics.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build IPA via CI + author UAT walk template + verify pre-walk gates</name>
  <files>
    .planning/phases/02-map-no-fog/02-UAT.md
  </files>
  <action>
1. **Verify pre-walk gates pass on `main`:**

   ```bash
   gh run list --branch main --limit 5
   gh run watch <id-of-most-recent-push> --exit-status
   ```

   Confirm the latest CI run on `main` shows three GREEN jobs (gates +
   android + ios) and produces an unsigned IPA artifact downloadable
   from the run page.

   If the CI is RED, abort and route to bug-investigation per
   CLAUDE.md "Bug investigation workflow" — do NOT create a UAT
   template until the IPA build is recoverable.

2. **Author `.planning/phases/02-map-no-fog/02-UAT.md`** following the
   Phase 1 Plan 01-07 SUMMARY template adapted to PERF-02:

   ```markdown
   ---
   phase: 02-map-no-fog
   plan: 06
   type: uat
   status: pending
   created: <today>
   ---

   # Phase 2 UAT — PERF-02 Walk (no fog)

   **Gate:** Pan-FPS without fog ≥ 40 sustained on iPhone 17 Pro at zoom 13–15
   in central Melun. Verbal `approved` is the exit signal.

   ## Pre-walk Falsification Thresholds

   | Metric                                     | Threshold       | Source                         |
   |--------------------------------------------|-----------------|--------------------------------|
   | Pan-FPS (sustained, ≥ 5 s window)          | ≥ 40 fps        | REQUIREMENTS.md PERF-02        |
   | Refresh rate visible on FPS counter        | 120 Hz (ProMotion) | Phase 1 PERF-01             |
   | First-launch copy log                      | exactly one info line `Copied Fra_Melun.pmtile (~4 MB) in <N> ms` | ROADMAP.md Phase 2 SC #1 |
   | Subsequent-launch copy log                 | absent          | CONTEXT specifics §2 mandate   |
   | Blue dot following GPS                     | each fix updates dot position | LOC-02 + LOC-03  |
   | Recenter FAB                               | animates to _lastFix at z=15 over ~500 ms | LOC-04          |
   | Rapid zoom (z=15 → 8 → 15)                 | no sustained blank flashes | ROADMAP.md Phase 2 SC #4 |
   | Combined gestures (pinch+drag)             | smooth, no race-bias | MAP-06                  |
   | Pan inertia / fling                        | enabled           | InteractiveFlag.all default |
   | Rotation gesture                           | works; compass icon syncs glyph | CONTEXT decision  |
   | Compass tap                                | snap-to-north tween (250 ms) | Plan 04 LOC-04 cousin |

   ## Walk Plan (200 m, central Melun)

   - Cold-launch the app fresh-installed (delete prior install via SideStore first to exercise MAP-01 first-launch path).
   - Tap CTA → grant location → confirm gate screen briefly waits for PMTiles copy → land on /map.
   - Inspect logs (share-logs button in app bar): expect exactly ONE `Copied Fra_Melun.pmtile (~4 MB) in <N> ms` line.
   - Walk 200 m at zoom 13–15. During the walk:
     - At least 10 pure one-finger pans (drag without zoom change)
     - At least 10 pure pinch-zoom gestures (no concurrent drag)
     - At least 10 combined gestures (pinch + drag simultaneously)
     - At least 3 recenter-FAB taps to verify LOC-04 animation
     - At least 1 rotate gesture + compass-tap to verify the snap-to-north tween
   - Continuously read the FPS counter; sustained-pan-FPS over a ≥ 5 s window is the PERF-02 gate.
   - At the end of the walk, perform a rapid zoom-out (z=15 → 8) and zoom-in (z=8 → 15) cycle. Note any sustained blank flashes (brief decode flicker is acceptable).
   - Cold-restart the app. Confirm logs show NO copy log line (idempotent skip).

   ## Walk Evidence (filled during/after walk)

   - Pan-FPS observed (rolling 5 s windows): _<to fill>_
   - Combined-gesture-FPS observed: _<to fill>_
   - First-launch copy log: _<paste line>_
   - Subsequent-launch copy log present? _<yes/no — expected no>_
   - Blue dot tracking quality: _<smooth / jittery / lost>_
   - Recenter FAB animation observed at: _<lat,lon at end>_
   - Rapid-zoom flash behaviour: _<acceptable / sustained blanks>_
   - Compass snap-to-north observed: _<yes / no>_

   ## Verdict

   _<verbal `approved` to PASS, or detailed FAIL diagnosis below>_

   If FAIL — recommended mitigation:
   - [ ] Label thinning (Pitfall 5 + RESEARCH §Pitfall 1 fallback): wrap `ProtomapsThemes.lightV3()` in a custom theme builder that filters layers `places_*` / `roads_label_*` at zoom < 14
   - [ ] Insert Phase 2.1 (label-thinning mitigation) before Phase 3
   - [ ] Capture FPS numbers per zoom level so Phase 2.1 has a baseline

   ## Deviations / Surprises

   _<any unexpected behaviour worth flagging for downstream phases>_
   ```

3. **Print the sideload instructions** for the developer to copy from the
   docs (`docs/flutter-ios-specifics.md` §1 Podfile macros, §2 CFBundleName,
   §SideStore re-sign flow). Reference the file path; do NOT duplicate
   content.

4. **Print the IPA artifact URL** from the CI run page so the developer
   can download it directly:
   ```bash
   gh run view <run-id> --log
   # or
   gh run download <run-id> --name <ios-artifact-name> --dir .uat-tmp/
   ```

5. **Confirm** the next task gates the walk via `checkpoint:human-verify`.
  </action>
  <verify>
    <automated>node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" verify file-exists .planning/phases/02-map-no-fog/02-UAT.md</automated>
  </verify>
  <done>
- CI on `main` is GREEN (three jobs: gates, android, ios) with downloadable IPA artifact
- `.planning/phases/02-map-no-fog/02-UAT.md` exists with the pre-walk template
- Sideload instructions referenced from `docs/flutter-ios-specifics.md`
- Developer has the IPA artifact URL or local download path
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: PERF-02 sideload UAT walk — verbal approved or detailed FAIL</name>
  <what-built>
A walkable map screen on iPhone 17 Pro that:
1. Copies `Fra_Melun.pmtile` from `rootBundle` to `<getApplicationSupportDirectory()>/maps/` exactly once on first launch (with the exact log line `Copied Fra_Melun.pmtile (~4 MB) in <N> ms`)
2. Renders the bundled PMTiles via `vector_map_tiles 8.0.0` + `ProtomapsThemes.lightV3()` (V3 — V4 has remote sprites, AUDIT-03 forbidden)
3. Centres on Melun (48.5397, 2.6553) at zoom 13, locked to zoom range [10, 15]
4. Soft-pads pan bounds by ~0.02° around the Melun bbox (~2 km overpan)
5. Accepts one-finger drag pan, pinch zoom, and combined pinch+drag (race disabled)
6. Renders a blue dot (7 px / `#2B7CD6` / 2 px white stroke) at GPS fixes via `Geolocator.getPositionStream(LocationSettings(accuracy: best, distanceFilter: 5))`
7. Provides a recenter FAB (bottom-right) that animates the camera to `_lastFix` at zoom 15 over 500 ms with `Curves.easeInOut`; disabled when no fix
8. Provides a compass icon (top-right, under FpsCounterOverlay) that tracks bearing via `mapEventStream` and on tap snaps to north over 250 ms (shortest-path)
9. Shares logs via Phase 1's app-bar share button (LOG-04 reused)
10. Shows the FPS counter in the corner (Phase 1 PERF-01)
11. CI on `main` is GREEN with downloadable IPA artifact ready for sideload
  </what-built>
  <how-to-verify>
**Setup:**
1. Download the latest IPA from the GitHub Actions run page on `main`:
   `gh run list --branch main --limit 1` → grab the run ID → `gh run download <id>`
2. Sideload via SideStore on iPhone 17 Pro per `docs/flutter-ios-specifics.md` §2 (CFBundleName + re-sign flow)
3. **Delete any prior install first** to exercise the MAP-01 first-launch copy path

**Walk (~200 m, central Melun, zoom 13–15):**

1. **Cold launch.** Tap "Allow location" CTA. Permission grants. Brief 100–500 ms wait (PMTiles copy in flight). Land on `/map` with the Protomaps tile theme rendering.

2. **Inspect logs immediately** (app-bar share button → Mail → send to self). Expect exactly ONE log line matching:
   ```
   <UTC timestamp> INFO infrastructure.pmtiles: Copied Fra_Melun.pmtile (~4.0 MB) in <N> ms
   ```
   PASS if present + exactly once. FAIL if absent or duplicated.

3. **Walk 200 m through central Melun** while staying at zoom 13–15. During the walk perform:
   - **≥ 10 pure one-finger pans** — drag without changing zoom
   - **≥ 10 pure pinch-zoom gestures** — pinch in/out without dragging
   - **≥ 10 combined gestures** — pinch + drag simultaneously
   - **≥ 3 recenter-FAB taps** — verify the camera animates smoothly to your current position over ~500 ms
   - **≥ 1 rotate gesture** (two-finger twist) followed by a compass tap — verify the snap-to-north tween

   **Continuously read the FPS counter (top-right).** The PERF-02 gate is **sustained ≥ 40 fps over a ≥ 5 s pan window**. Brief dips (decode flicker) below 40 are acceptable; sustained sub-40 is FAIL.

4. **Verify blue dot tracking** — every GPS fix should update the dot's position smoothly. If the dot teleports erratically indoors / under tree cover, log it as a deferred item (no-accuracy-filter is the current decision; revisit if walk evidence shows noise).

5. **Rapid zoom test** at the walk's end: zoom out from z=15 → z=8 quickly (4–5 pinches), then zoom back in to z=15. Tiles should repaint without sustained blank flashes. Brief decode flicker on cold cache is OK.

6. **Cold-restart the app.** Inspect logs. Expect ZERO copy log lines (idempotent skip per CONTEXT mandate). PASS if absent.

7. **Fill in `02-UAT.md` Walk Evidence section** with observed FPS numbers, log excerpts, and any deviations.

**Verdict:**
- **PASS:** Verbal `approved`. PERF-02 satisfied. Phase 3 unblocked.
- **FAIL:** Detailed FAIL diagnosis in `02-UAT.md` Verdict section with FPS numbers per zoom level. Recommend Phase 2.1 (INSERTED) for label thinning before Phase 3 can begin. Capture under `deferred-items.md` and trigger `/gsd:plan-phase 02.1` (label-thinning mitigation).

**Edge cases that should NOT FAIL the gate:**
- One brief sub-40 dip during a hard pinch-zoom (decode bound — Pitfall 1 acknowledges this)
- One missed GPS fix (urban canyon)
- A 1–2 frame blank during cold-cache zoom transitions

**Edge cases that SHOULD FAIL the gate:**
- Sustained sub-40 fps during routine pan at z=14–15 (label collision is winning over the frame budget — needs Phase 2.1)
- Map renders empty grey at all zooms (Pitfall 3 — wrong source key in release build)
- App crashes or freezes during any gesture
- Blue dot never appears even after GPS fix is reported in logs
- Recenter FAB does not animate (instant snap or no movement)
- Compass icon does not rotate when the map rotates
- Copy log line emits on EVERY launch (idempotency broken — Plan 02 regression)
  </how-to-verify>
  <resume-signal>Verbal `approved` (PERF-02 PASS) OR detailed FAIL diagnosis with FPS numbers per zoom level + recommended mitigation. Either result, paste into `02-UAT.md` Verdict section before resuming.</resume-signal>
  <files>
    .planning/phases/02-map-no-fog/02-UAT.md
  </files>
  <action>
This is a checkpoint:human-verify task. The executor automates nothing
beyond Task 1 (which already authored the UAT walk template, verified
CI is GREEN, and located the IPA artifact). The actual work is the
developer walking 200 m through central Melun on iPhone 17 Pro per the
`<how-to-verify>` block above, then pasting their verdict into the
`02-UAT.md` Verdict section.

The executor MUST pause and wait for the developer's verbal
`approved` or detailed FAIL diagnosis before proceeding to Task 3.

No code changes in this task. All instructions for the developer are
in `<what-built>` (what they're testing) and `<how-to-verify>` (the
walk plan + edge-case rubric).
  </action>
  <verify>
    <automated>node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" verify file-exists .planning/phases/02-map-no-fog/02-UAT.md</automated>
  </verify>
  <done>
- Developer has performed the walk per `<how-to-verify>`
- Verdict pasted into `02-UAT.md` Verdict section: either verbal
  `approved` (PASS) or detailed FAIL diagnosis with FPS-per-zoom-level
  numbers and recommended mitigation
- Executor has resumed (`<resume-signal>` honoured)
  </done>
</task>

<task type="auto">
  <name>Task 3: Update REQUIREMENTS.md + STATE.md + roadmap on PASS, or trigger Phase 2.1 on FAIL</name>
  <files>
    .planning/REQUIREMENTS.md,
    .planning/ROADMAP.md,
    .planning/STATE.md,
    .planning/phases/02-map-no-fog/deferred-items.md
  </files>
  <action>
**Branch on the Task 2 verdict:**

**On PASS (verbal `approved`):**

1. **REQUIREMENTS.md** — flip `PERF-02` from `Pending` to `Complete` in
   the Traceability table. Update the requirement bullet to add a
   completion note matching Phase 1 LOG-05 phrasing:

   ```markdown
   - [x] **PERF-02**: At Phase 2 UAT walk on iPhone 17 Pro: pan-FPS without fog ≥ 40 (gate before Phase 3 fog work begins) _(software-complete + sideload UAT verbal `approved` per Plan 02-06; FPS observed: <numbers from 02-UAT.md>)_
   ```

2. **ROADMAP.md** — update the Phase 2 row in the Progress table:
   `2. Map (no fog) | TBD | Complete | <today>` (with actual plans count
   substituted for TBD — should be 6).

3. **STATE.md** — bump completed phases, update progress bar, log the
   Plan 02-06 decision (one-line entry under Decisions):

   ```markdown
   - [Phase 02-map-no-fog]: Plan 02-06: PERF-02 sideload UAT walk verbal `approved` on iPhone 17 Pro. Pan-FPS sustained <X> fps at z=13–15 over central Melun. Phase 3 fog work unblocked.
   ```

4. **deferred-items.md** — capture any walk-surfaced quirks that aren't
   blocking but worth tracking (e.g. compass tap-target near rotation
   gesture corner if it interfered, accuracy-degraded fix dancing, etc.).
   If nothing surfaces, write `## Deferred items: none`.

5. **Commit** the changes:
   ```bash
   node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(02): close Phase 2 — PERF-02 verbal approved" --files .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md .planning/phases/02-map-no-fog/02-UAT.md .planning/phases/02-map-no-fog/deferred-items.md
   ```

**On FAIL (detailed FPS numbers + mitigation requested):**

1. **02-UAT.md Verdict** — already filled in by the developer in Task 2.

2. **deferred-items.md** — record the FAIL with the recommended Phase 2.1
   label-thinning approach, including the FPS-per-zoom-level numbers
   from the walk:

   ```markdown
   ## Deferred items

   ### PERF-02 FAIL — Phase 2.1 (label thinning) required

   FPS observed during walk:
   - z=13: <N> fps sustained
   - z=14: <N> fps sustained (likely under 40 — POI density max)
   - z=15: <N> fps sustained

   Recommended mitigation: wrap `ProtomapsThemes.lightV3()` in a custom
   theme-layer-filter builder that drops `places_*` and `roads_label_*`
   layers at zoom < 14. RESEARCH §Pitfall 1 fallback path. Estimated
   one-plan effort.

   Trigger: `/gsd:plan-phase 02.1` to insert the mitigation phase
   between 2 and 3.
   ```

3. **ROADMAP.md** — insert a Phase 2.1 row marked INSERTED:
   ```markdown
   - [ ] **Phase 2.1: Label-Thinning Mitigation** (INSERTED) — drop high-density labels at zoom < 14 to recover PERF-02 ≥ 40 fps gate
   ```

4. **STATE.md** — log the FAIL + Phase 2.1 insertion under Blockers/Concerns.

5. **Commit** the changes:
   ```bash
   node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(02): PERF-02 FAIL — insert Phase 2.1 label-thinning" --files .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md .planning/phases/02-map-no-fog/02-UAT.md .planning/phases/02-map-no-fog/deferred-items.md
   ```

6. **Print** the recommendation: "Run `/gsd:plan-phase 02.1` to plan the label-thinning mitigation, then re-walk PERF-02 at the end of Phase 2.1."
  </action>
  <verify>
    <automated>node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" verify file-exists .planning/phases/02-map-no-fog/02-UAT.md .planning/phases/02-map-no-fog/deferred-items.md</automated>
  </verify>
  <done>
- `02-UAT.md` Verdict section is filled (verbal `approved` or detailed FAIL with FPS per zoom)
- On PASS: REQUIREMENTS.md PERF-02 flipped to Complete; ROADMAP.md Phase 2 row Complete; STATE.md updated; deferred-items.md committed (even if empty)
- On FAIL: ROADMAP.md has Phase 2.1 INSERTED row; STATE.md Blockers/Concerns updated; deferred-items.md carries the FAIL diagnosis + mitigation recommendation
- Single git commit covers all the docs changes
- LOC-03 gate still GREEN (no production-code changes in this plan)
  </done>
</task>

</tasks>

<verification>
1. `02-UAT.md` Verdict section filled with developer's verbal verdict (PASS or detailed FAIL)
2. On PASS: REQUIREMENTS.md PERF-02 flipped to Complete; ROADMAP.md Phase 2 row Complete; STATE.md updated; commit landed on `main`
3. On FAIL: ROADMAP.md has Phase 2.1 INSERTED row; deferred-items.md carries the diagnosis; commit landed on `main`
4. Phase 1 widget tests + Phase 2 unit tests still GREEN (no production changes in this plan, regression guard is automatic)
5. CI on `main` still GREEN
</verification>

<success_criteria>
1. PERF-02 either satisfied (verbal `approved` recorded) OR a Phase 2.1 mitigation is queued with concrete FPS-per-zoom-level evidence
2. The UAT walk evidence is preserved in `02-UAT.md` for future reference (Phase 5 retrospective will read this)
3. ROADMAP.md and STATE.md reflect the actual phase outcome
4. No production code changed in this plan — it's purely a UAT + documentation gate
</success_criteria>

<output>
After completion, create `.planning/phases/02-map-no-fog/02-06-SUMMARY.md`.
Highlight:
- The verdict (PASS or FAIL) verbatim
- FPS numbers observed per zoom level
- Whether Phase 3 is unblocked OR Phase 2.1 was inserted
- Any deferred items captured for Phase 5 retrospective
- Total walk duration vs Phase 1 Plan 01-07 (sideload-UAT velocity benchmark)
</output>
