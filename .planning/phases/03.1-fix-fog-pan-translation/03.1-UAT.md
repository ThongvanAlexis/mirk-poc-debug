---
phase: 03.1-fix-fog-pan-translation
plan: 03
type: uat
status: pending
source: [03.1-01-SUMMARY.md, 03.1-02-SUMMARY.md]
started: TBD
ci_run: TBD
ci_sha: TBD
device: iPhone 17 Pro (ProMotion 120 Hz, A19 Pro)
location: central Melun (48.5397, 2.6553)
exit_gate: developer's resume signal — verbal `approved` (CONFIRMED-AFTER-FIX) | `denied: <verbatim>` (DENIED-FINAL) | `iterating: <verbatim>` (open Plan 03.1-04)
verdict: TBD
walked: TBD
---

# Phase 3.1 UAT — Post-Fix Re-Test Walk (PERF-07 + Criterion B)

**Gate:** This is the empirical answer Phase 3.1 was created to produce. The Plan 03-08 walk closed Phase 3 `DENIED 2026-05-01` on the static-fog-during-pan failure mode. The Plan 03.1-02 fix derives `(uOffsetX, uOffsetY) = (camera.pixelOrigin / size) % 1.0` and forwards to the shader's slot-3..4 `uOffset` uniform. This walk either reverses the verdict to **CONFIRMED-AFTER-FIX** (unblocking Phase 4 + Phase 5 + the MirkFall port-back conversation) or strengthens it to **DENIED-FINAL** (Phase 3.2 alternative-renderer pivot becomes the only remaining option). The verbal `approved` / `denied:` / `iterating:` resume signal IS the verification.

This UAT mirrors the shape of `.planning/phases/03-fog-of-war-the-hypothesis/03-UAT.md` (Plan 03-08's walk). The new section is `### Diagnostic JSONL — fog_transform rollup` capturing the per-second canvasTransform / pixelOrigin / uOffset evidence stream from FOG-10.

## Build Under Test

| Field        | Value                                                                                               |
| ------------ | --------------------------------------------------------------------------------------------------- |
| CI run       | TBD                                                                                                 |
| Run URL      | TBD                                                                                                 |
| HEAD SHA     | TBD                                                                                                 |
| iOS artifact | `mirk-poc-debug-ios-unsigned-ipa` — downloaded to `.uat-tmp/mirk-poc-debug-unsigned.ipa`            |
| Gates job    | TBD                                                                                                 |
| Android job  | TBD                                                                                                 |
| iOS job      | TBD                                                                                                 |
| Tests        | TBD GREEN locally on the post-fix SHA (FOG-09 keystone + FOG-10 logger + Phase 1+2+3 retained)      |

## Pre-walk Gates

- [ ] `flutter test` full suite GREEN (FOG-09 + FOG-10 + all Phase 1+2+3 retained) — TBD locally on post-fix SHA
- [ ] `flutter analyze --fatal-infos` 0 warnings — TBD
- [ ] `dart format --line-length 160 --set-exit-if-changed .` clean — TBD
- [ ] `dart test tool/test/` GREEN — TBD
- [ ] CI run for head commit `<SHA>` GREEN (gates + android + ios) — TBD
- [ ] IPA artifact downloaded — TBD at `.uat-tmp/mirk-poc-debug-unsigned.ipa`
- [ ] `/sanity` smoke test on sideloaded IPA — TBD (fog renders on iPhone 17 Pro with circular reveal hole)

## Walk Protocol

- **Where:** Central Melun (48.5397, 2.6553) — same theatre as Plan 03-08.
- **Duration target:** ≥ 5 minutes continuous walk on iPhone 17 Pro.
- **Gestures target:**
  - **≥ 10 deliberate combined pinch-zoom-and-pan** (rotate + pinch + pan simultaneously; the worst-case stress on the same-Canvas hypothesis).
  - **≥ 3 recenter FAB taps**.
  - **≥ 1 full pan** from one edge of the Melun PMTiles bbox to the other.
  - **≥ 1 zoom-out to z10** (min) and back to z15 (max).
- **HUD visible during walk:**
  - `FpsCounterOverlay` at `top: 8 / right: 8` (Phase 1 PERF-01).
  - `MapCompass` at `top: 56 / right: 8` (Phase 2 LOC-04 cousin).
  - `FrameDeltaProbeOverlay` at `top: 104 / right: 8` (Phase 3 FOG-08 user-facing) — three colour-coded lines `med / p95 / max`, green/yellow/red against Criterion A.
- **Pre-walk shader-sanity gate (BEFORE the walk):**
  1. Sideload the IPA via SideStore on iPhone 17 Pro.
  2. Open the app; grant location permission.
  3. On `/map`, tap the AppBar `Icons.science` button → navigate to `/sanity`.
  4. Confirm the screen renders fog with a circular reveal hole visible somewhere centred on screen (the synthetic 80 m disc).
  5. **The KEY OBSERVATION new in Phase 3.1:** during the walk, does the fog VISUALLY TRANSLATE with the map during pan? (Plan 03-08's failure mode was that the fog stayed static while the tile layer translated beneath it — only the blue dot moved.)
  6. Return to `/map`.
- **Post-walk:**
  1. Share session log via Mail (LOG-04 / `share_plus`).
  2. Open `.txt.gz` on desktop, gunzip, inspect JSONL.
  3. `grep 'infrastructure.mirk.frame_delta'` → paste the 1-second JSONL rollups during the ≥ 10 combined-gesture seconds.
  4. `grep 'infrastructure.mirk.sdf'` → paste the SDF rebuild rollup lines.
  5. `grep 'infrastructure.mirk.fog_transform'` → paste the per-paint canvasTransform / pixelOrigin / center / appliedUOffset diagnostic JSONL lines.
  6. Note the FPS-counter readings observed during pan / pinch / combined / idle.
  7. Subjective verdict (Criterion B) — one sentence per Criterion B sub-claim.

## Walk Evidence

_TBD — filled post-walk by Plan 03.1-03 Task 3._

### Pre-walk shader-sanity gate (`/sanity` route)
_TBD._

### Frame-delta probe rollups (PERF-07, Criterion A — `infrastructure.mirk.frame_delta` JSONL)
_TBD — paste the per-second JSONL rollups during the ≥ 10 combined-gesture seconds._

### SDF rebuild rollups (`infrastructure.mirk.sdf` JSONL)
_TBD — paste the per-second JSONL rebuild lines._

### Diagnostic JSONL — fog_transform rollup (NEW for Phase 3.1 — `infrastructure.mirk.fog_transform`)
_TBD — paste the per-second canvasTransform / pixelOrigin / center / appliedUOffset rollup lines. The grep-correlation against frame_delta + sdf on per-second `epochSecond` boundaries is the load-bearing diagnostic evidence._

### FPS observations (PERF-03)
_TBD._

### Subjective verdict (Criterion B)
_TBD — developer's verbatim words._

## Verdict

**Verdict:** TBD.

**Walked at:** TBD on iPhone 17 Pro against CI run `<RUN_ID>` (SHA `<SHA>`) in central Melun.

**Developer's verbatim words:** _TBD._

**Interpretation:** _TBD._

See `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION.md` for the formal verdict and MirkFall migration recommendation.

## Deviations / Surprises

_TBD — filled post-walk._

## Summary

_TBD — filled post-walk._

## Gaps

<!-- APPEND only if walk reveals issues; YAML format for /gsd:plan-phase --gaps -->
_TBD — filled post-walk only if the walk reveals new gaps. On `CONFIRMED-AFTER-FIX`, no gaps expected; on `DENIED-FINAL` or `ITERATING`, append gap entries here for the follow-up phase planner._
