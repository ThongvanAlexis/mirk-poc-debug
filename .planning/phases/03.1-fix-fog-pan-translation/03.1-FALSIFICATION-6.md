# Phase 3.1 Falsification — Walk #6 (Post-Plan-03.1-14 Fix Bundle: FOG-19 C-b uZoomScale uniform + DEBUG-03 unique-cell-numbers)

**Created:** 2026-05-04
**Phase:** 03.1-fix-fog-pan-translation
**Walked on:** 2026-05-04 (iPhone 17 Pro, central Melun, CI run `25338670208`, SHA `d753176`)
**Verdict:** **CONFIRMED-AFTER-FIX (FULL)** — closes Phase 3.1 after 6 iterations + 8 walks (#1, #2, #3, #3b implicit marker analysis, #4, #5, #6). Phase 4 (Wisp Particles) + Phase 5 (Decision Gate) UNBLOCK. Plan 03-08 `DENIED` verdict (2026-05-01) is REVERSED. Original Phase 3 same-Canvas fog hypothesis REINSTATED.

## Relationship to Walk #5 Verdict

Walk #5 (Plan 03.1-13, 2026-05-04) closed `CONFIRMED-AFTER-FIX-PARTIAL` at SHA `960f5c2` against the Plan 03.1-12 Dart-only fix bundle (C1' / FOG-18 modulo elimination + C3' / DEBUG-02 bounding-box removal). Findings:

- **Q1 SNAP CLOSED at MAX zoom regime** — ~35 sec total at pxOriginX 4.255M-4.258M with ZERO dev_markers fired (Walk #4 baseline: 2 markers in 2.36 sec at the same regime). Developer's verbatim *"panning 100% solved, no hard step at any level of zoom"*. **FOG-17a-wrap-firing-visibly mechanism interpretation EMPIRICALLY CONFIRMED**: Walk #4's debug-spiral positive control showed ZERO steppiness while production fog showed visible SNAP at every wrap event — the wrap event itself was the bug. FOG-18 modulo elimination delivers Q1 closure at MAX zoom regime.
- **FOG-18 JSONL signature CONFIRMED** — `uOffsetXMedian == pixelOriginXMedian` to 1e-3 precision on 80/80 rollups; `uOffsetXMax` reaches 4258352.933 raw px (Walk #4 was bounded under 1537 under FOG-17a in effect).
- **Q1b residual still present (EXPECTED — C2' deferred)** — developer's verbatim *"zoom still show same steppy displacement behavior in fog, in number we can see the numbers sliding (modifying the number to not have repetitive value would allow us to debug the amount of drift)"* + CLAUDE.md `# current best version` post-Walk-5 update *"heavy hard steps while zooming / zooming on the numbered shader show the numbers moving all around : incorrect scalling?"*.
- **Q3 + Q4 retained** — *"panning 100% solved"* implicitly retains FOG-12 (clip-path canvasOffset shift) and FOG-13 (canvas.translate compensation).
- **Q2 retained (UX-02 walk-time-validated 2nd consecutive walk)** — `canvasTxMin/Median/Max` and `canvasTyMin/Median/Max` are identically `0.000000` across all 80 fog_transform rollups.
- **C3' / DEBUG-02 explicit ≥7M extreme-distance walk-time-validation deferred to Walk #6** — max regime reached during Walk #5 was 4.26M = MAX zoom at Melun (essentially identical to Walk #4's 4.26M); ≥7M extreme-distance threshold (~50-100 km from Melun) NOT crossed.
- **PERF-07 retained with massive headroom** — medianMs 1.228 / p95Ms 1.591 / maxMs 1.724 across 80 rollups (13× / 20× / 28× headroom on the 16/32/48-ms thresholds).

**Walk #5 Open Questions / Plan 03.1-14+ Candidate Axes** (`03.1-FALSIFICATION-5.md` §Open Questions) surfaced three axes that fed Plan 03.1-14:

- **(A) Q1b zoom-gesture residual — C2' / FOG-19 follow-up plan (PRIMARY)**: developer's *"incorrect scaling"* framing aligned mechanistically with the dimensional-mismatch hypothesis; resolved via fresh `/gsd:discuss-phase 3.1` (decisions captured in `03.1-CONTEXT.md` §"Discuss-phase decisions captured 2026-05-04 (post-Walk-5 / C2' design)") which selected **C-b: uZoomScale uniform addition** at the next free slot to BOTH `atmospheric_fog.frag` AND `atmospheric_fog_debug_spiral.frag`; Dart-side `_FogPainter.paint()` forwards `pow(2, currentZoom - kPocFogReferenceZoom)`. **Landed in Plan 03.1-14 Task B (commit `7aacf11`)** with `kPocFogReferenceZoom = 13.0` baseline (Walks #1-#5 default-zoom regime; uZoomScale = 1.0 → bit-identical to pre-fix at this zoom; pixel-perfect MIRL visual-identity preservation). MIRL constraint clarified to **visual-identity preservation** rule (CLAUDE.md `# MIRL solution` updated 2026-05-04, commit `57b2db1`): shader edits permitted IF the noise pattern's character at any settled (zoom, position) post-fix matches pre-fix at SOME equivalent (zoom, position).
- **(B) Diagnostic-tool feedback — debug-spiral unique-cell-numbers (SECONDARY, INPUT FOR C2' PLAN)**: developer's verbatim *"modifying the number to not have repetitive value would allow us to debug the amount of drift"*. **Landed in Plan 03.1-14 Task A (commit `86422fd`)** — `atmospheric_fog_debug_spiral.frag` cell-encoding rewritten for unique 4-digit IDs (`(cell.y + 50) * 100 + (cell.x + 50)` clamped to [0, 9999]) via 4 horizontal glyph slots; replaces previous repetitive 0-9 cycling. Walk #6 reads off cell IDs across consecutive zoom transitions to measure drift quantitatively (e.g., "started at 5023, ended at 5021 = 2 cells of drift").
- **(C) C3' / DEBUG-02 explicit walk-time validation — Walk #6 (TERTIARY)**: Walk #5 reached only 4.26M = MAX zoom at Melun, NOT ≥7M+ extreme-distance threshold. Plan 03.1-15 walk shape includes a NEW required step (CONTEXT.md D1 decision): pan continuously past city limits to ~50-100 km from Melun BEFORE the C2' validation regime, so the developer reaches the ≥7M raw-px regime under FOG-18 + FOG-19 BEFORE exercising zoom transitions.

Walks #1 + #2 + #3 + #3b + #4 + #5 records (`03.1-FALSIFICATION.md`, `03.1-FALSIFICATION-2.md`, `03.1-FALSIFICATION-3.md`, `03.1-FALSIFICATION-4.md`, `03.1-FALSIFICATION-5.md`, `03.1-WALK-3B-MARKER-ANALYSIS.md`) AND Phase 3 record (`03-FALSIFICATION.md`) ALL stand as historical record — UNTOUCHED. Walk #6's verdict is a SEPARATE artefact in this file.

## Hypothesis

After the Plan 03.1-14 fix bundle (FOG-19 C-b uZoomScale uniform — slot 41 added to BOTH production + debug shaders consumed consistently as `worldPx / (kNoiseTilePx * uZoomScale)` and `cellPx = worldPx / uZoomScale`; Dart-side `_FogPainter.paint()` forwards `math.pow(2, camera.zoom - kPocFogReferenceZoom).toDouble()` — plus DEBUG-03 unique 4-digit per-cell IDs in the debug-spiral for quantitative drift measurement), the same-Canvas fog now:

- **(a) translates with the camera during pan at MAX zoom regimes** without producing visible SNAP events (FOG-18 retained from Walk #5 — the modulo elimination delivered Q1 closure at the 4.26M raw-px regime).
- **(b) maintains correctness at extreme world coordinates ~50-100 km from Melun** (C3' explicit walk-time validation NEW for Walk #6 — pixelOriginX pushes ≥7M raw px under FOG-18 + FOG-19; well within fp32's 16.7M exact-integer mantissa ceiling).
- **(c) keeps the reveal hole co-located with the blue dot during pan/zoom** (FOG-12 + FOG-13 retained — no Plan 03.1-14 changes to reveal-hole calculation).
- **(d) covers the entire viewport at any canvas-translation magnitude** (FOG-13 retained — no Plan 03.1-14 changes to viewport-cover calculation).
- **(e) is structurally invulnerable to the rotation-correlated fog mis-coverage failure mode** (UX-02 retained — no Plan 03.1-14 changes to InteractionOptions).
- **(f) NEW: keeps fog noise samples anchored to lat/lng during deliberate zoom in/out transitions** (FOG-19 C-b: cells stay PUT during zoom — visible cell size grows with zoom-in, shrinks with zoom-out; DEBUG-03 unique cell numbers enable quantitative drift measurement).

**Q1b explicit closure is the targeted FOG-19 fix (VERDICT-DETERMINING for Walk #6).** Walk #5's verbatim *"steppy displacement behavior in fog... numbers sliding"* + CLAUDE.md *"heavy hard steps while zooming / incorrect scaling"* directly diagnosed screen-anchored noise sampling as the bug. C-b anchors the noise to the world via the uZoomScale divisor; cells stay anchored to lat/lng. After C-b, the developer should observe ZERO drift (or ≤1-2 cell drift = success threshold) on DEBUG-03 unique-numbered cells across consecutive zoom transitions.

**C3' explicit ≥7M validation is also VERDICT-DETERMINING** — Walk #5 deferred this regime; Plan 03.1-15 walk shape includes the required pan-to-distance step. Other axes retained: Q1 (re-validation; expected RESOLVED retained from Walk #5), Q2 (UX-02 3rd consecutive walk), Q3 (reveal-hole), Q4 (translation), PERF-07.

Re-stated against the original POC core value: the fog-of-war stays perfectly locked to the map during pan, zoom, and combined gestures on a sideloaded iOS build. Walk #6 outcomes:

- **`CONFIRMED-AFTER-FIX (FULL)`** (target outcome — closes Phase 3.1) — Q1 retained + Q1b CLOSED via FOG-19 + C3' explicit ≥7M passes + Q3/Q4/Q2/PERF-07 retained.
- **`CONFIRMED-AFTER-FIX-PARTIAL`** (less likely now since Q1b is the targeted fix) — Q1 retained + Q1b improved-but-not-fully-closed (small residual drift via DEBUG-03 cells) + Q3/Q4/Q2 retained + PERF-07 retained. Phase 3.1 stays In Progress (7th iteration awaiting refinement plan + Walk #7).
- **`DENIED-FINAL`** — Q1b unchanged or worse despite FOG-19. Rules out the world-coordinate-basis-mismatch mechanism interpretation. Architectural counter-evidence; Phases 4 + 5 stay BLOCKED or transition to CANCELLED per developer choice.
- **`ITERATING`** — a new failure mode surfaces (C3' explicit ≥7M validation surfaces NEW precision artefact at extreme world coordinates; OR FOG-19 introduces unforeseen issue at extreme zoom; OR `uZoomScale = pow(2, ...)` numerical issues at very low/high zoom; OR something unforeseen). Plan 03.1-16+ pending in follow-up plan-phase invocation. THIS plan does NOT author 03.1-16+.

## Falsification Criteria (written BEFORE the walk)

### Criterion A — Frame-delta probe thresholds (PERF-07 re-validation, quantitative)

Same thresholds as Walks #1-#5: `medianMs ≤ 16`, `p95Ms ≤ 32`, `maxMs ≤ 48` across ≥ 10 deliberate combined gestures. Walk #5 captured 13× / 20× / 28× headroom (medianMs 1.228 / p95Ms 1.591 / maxMs 1.724 across 80 rollups). Walk #6 should preserve at least Walk #5 levels — the FOG-19 uZoomScale uniform addition + the per-fragment `worldPx / uZoomScale` division add minimal arithmetic to the shader's hot path (one float division per fragment in production; one cellPx scaling in debug-spiral); the DEBUG-03 4-digit-per-cell layout adds at most one additional `sampleDigit()` call per fragment in the debug-spiral hot path (debug-only — does not affect production fog frame budget at all).

### Criterion B — Subjective visual lock (PERF-05 carry-over, re-tested)

Developer's verbal verdict at end of walk:

- **Q1 (sustained MAX-zoom pan SNAP — RE-VALIDATION)**: must retain Walk #5's CLOSURE; expected outcome (CONFIRMED-AFTER-FIX FULL or PARTIAL or ITERATING): zero markers fired at MAX zoom regardless of pan duration. Walk #5 baseline: ZERO markers across ~35 sec at pxOriginX 4.26M.
- **Q1b (zoom-gesture seed-of-procedural-pattern-fast — VERDICT-DETERMINING for Walk #6)**: developer performs ≥ 5 deliberate zoom in/out transitions at MAX zoom WITH DEBUG-03 unique cell numbers visible (DEBUG-01 toggle ON). Expected outcome (CONFIRMED-AFTER-FIX FULL): cells stay anchored across consecutive zoom transitions; ZERO drift on DEBUG-03 unique cells (or ≤1-2 cell drift = success threshold). Quantitative reading: developer captures verbatim observation like *"cell 5023 → cell 5023 across all 5 transitions = ZERO drift = FOG-19 success"* OR *"cell 5023 → cell 5021 = 2 cells drift = partial fix"*.
- **Q2 (no rotation-correlated fog mis-coverage)**: re-confirm UX-02 walk-time-validated (canvasTx/Ty identically zero; 3rd consecutive walk; no Plan 03.1-14 modifications to InteractionOptions).
- **Q3 (no reveal-hole offset from blue dot)**: re-confirm FOG-12 + FOG-13 (no Plan 03.1-14 changes).
- **Q4 (fog visually translates with camera during pan)**: re-confirm.
- **Q5 (DEBUG-02 stress-test ~50-100 km from Melun — VERDICT-DETERMINING; closes Walk #5 deferred)**: developer pans continuously past city limits to reach ~50-100 km from initial Melun centre (cameraConstraint removed; pixelOriginX pushes ≥7M raw px). Expected outcome (CONFIRMED-AFTER-FIX FULL): fog renders correctly at ≥7M raw-px regime; zero markers fired; no precision-induced artefacts at extreme world coordinates within fp32 mantissa headroom.
- No white-ellipse during fast pinch-zoom (already passed Walks #1-#5).
- No inversion at any zoom level (already passed Walks #1-#5).

### Criterion C — DROPPED

Stays dropped per Phase 3 + roadmap lock.

### Criterion D — Diagnostic stream confirms the FOG-19 fix

Per the `Logger('infrastructure.mirk.fog_transform')` 1-Hz JSONL rollups:

- `pixelOriginX/Y` continues to log the RAW `camera.pixelOrigin` (back-compat with Walks #1-#5 grep tooling).
- `appliedUOffset` (preserved JSONL field name) continues to equal `camera.pixelOrigin` exactly under FOG-18 — FOG-19's zoom-scale lives shader-side, not Dart-side, so the JSONL stream's signature is identical to Walk #5's. The two streams remain equal: `appliedUOffset == camera.pixelOrigin`.
- **Pattern check (verdict-determining for Q5/C3')**: during the C3' extreme-distance pan in Walk #6's rollups, `uOffsetX*` values exceed 7M (Walk #5 max was 4.26M = MAX zoom at Melun) AND no marker fires AND `pixelOriginX` evolves smoothly = FOG-19 + FOG-18 precision-clean at distance.
- **Pattern check (verdict-determining for Q1b)**: during the deliberate zoom-gesture sequence with DEBUG-03 cells visible, ZERO markers fire (developer would have tapped the marker on any visible cell drift; alternatively the post-walk verbatim observation captures small drift = CONFIRMED-AFTER-FIX-PARTIAL).
- `canvasTx/Ty` identically zero (UX-02 retained 3rd consecutive walk).
- `canvasRotation` field NOT added (UX-02 makes rotation moot).
- **`CONFIRMED-AFTER-FIX (FULL)` pattern**: at MAX zoom: `uOffsetX*` exceeds 1537 (FOG-18 retained from Walk #5), evolves smoothly with pxOriginX, zero markers fire. At C3' regime: pxOriginX exceeds 7M, no NaN, no fp32 ULP-induced jitter at the 1-raw-px scale, fog renders correctly. Q1b zoom-gesture: ZERO markers fire AND developer's verbatim DEBUG-03 reading is "ZERO cell drift" or "≤1-2 cell drift" = success.
- **`DENIED-FINAL` pattern**: FOG-19 is mechanically present in the JSONL stream (uZoomScale uniform forwarded; FOG-18 signature retained: uOffsetX == pxOriginX) AND Q1b zoom-gesture symptoms STILL present per developer (DEBUG-03 reading shows large or unchanged drift across zoom transitions). Rules out the world-coordinate-basis-mismatch mechanism interpretation; root cause is something deeper.
- **`ITERATING` pattern**: Q1b improves but C-b introduces unforeseen artefact at extreme zoom; OR `uZoomScale = pow(2, ...)` numerical issues at very low/high zoom; OR C3' explicit walk-time-validation surfaces NEW precision artefact at ≥7M raw-px that Walk #5 didn't reach (since pixelOrigin pushes well past 4.26M when panning ~50-100 km from Melun); OR something unforeseen the FOG-19 + DEBUG-03 combo exposes. The diagnostic stream points at the next fix axis; Plan 03.1-16+ is opened.

## Verdict logic

The verdict-determining axes for Walk #6 are Q1 (max-zoom SNAP — must remain CLOSED from Walk #5 — RE-VALIDATION), **Q1b** (zoom-gesture residual — must CLOSE via FOG-19 — VERDICT-DETERMINING), Q3 (reveal hole), Q4 (translation), **Q5** (C3' stress-test at distance — explicit walk-time-validation — VERDICT-DETERMINING), and PERF-07.

**`CONFIRMED-AFTER-FIX (FULL)`** (target outcome — closes Phase 3.1):

- Q1 SNAP retained at MAX zoom: zero dev_markers fire during the ≥ 30-sec one-direction sustained MAX-zoom pan (Walk #5 confirmed; Walk #6 re-validates).
- **Q1b CLOSED via C-b**: cells stay anchored across zoom transitions; ZERO drift on DEBUG-03 unique cells (or ≤1-2 cell drift = success threshold to be confirmed at execute-phase time based on developer's tolerance).
- C3' explicit ≥7M extreme-distance pass: zero markers fired during the ~50-100 km pan; fog renders correctly at distance; no precision-induced artefacts at high pixelOrigin magnitudes.
- Q3 reveal-hole co-located with blue dot: retained.
- Q4 fog translates with map during pan: retained.
- Q2 rotation absence retained (canvasTx/Ty identically zero in JSONL stream — UX-02 retained 3rd consecutive walk).
- PERF-07 thresholds met: medianMs ≤ 16, p95Ms ≤ 32, maxMs ≤ 48 across the ≥ 10-combined-gesture window.
- **Phase 3.1 closes.** Phase 4 + Phase 5 unblock. Plan 03-08 DENIED verdict REVERSED.

**`CONFIRMED-AFTER-FIX-PARTIAL`** (FOG-19 partial improvement; small residual drift):

- Q1 retained + Q1b improved-but-not-fully-closed (small residual drift via DEBUG-03 cells; e.g., 3-5 cells drift instead of zero).
- Q3/Q4/Q2 retained + PERF-07 retained.
- Phase 3.1 stays In Progress (7th iteration awaiting refinement plan + Walk #7).

**`DENIED-FINAL`** (Q1b unchanged or worse despite FOG-19):

- The Q1b CLOSED criterion FAILS: DEBUG-03 reading shows large or unchanged drift across zoom transitions even though `uZoomScale` is in effect.
- Rules out the world-coordinate-basis-mismatch mechanism interpretation. The mechanism is something else (deeper fp32 precision issue, MapCamera.pixelOrigin discontinuity at zoom boundaries that isn't covered by the C-b hypothesis, MobileLayerTransformer paint-pipeline order issue at zoom-snap, etc.).
- Architectural counter-evidence: even the targeted Q1b fix axis (anchor noise to lat/lng via uZoomScale) doesn't deliver Q1b closure.
- Phases 4 + 5 stay BLOCKED or transition to CANCELLED per developer choice.

**`ITERATING`** (a new failure mode surfaces beyond Q1b):

- Q1 retained + Q1b CLOSED at Melun BUT C3' explicit ≥7M validation fails: NEW precision-induced artefact appears at extreme world coordinates (e.g., pixelOrigin > 8M raw px exposes fp32 ULP issue that Walk #5's max 4.26M didn't reach), OR `uZoomScale = pow(2, ...)` numerical issues at very low/high zoom, OR something unforeseen surfaces.
- Plan 03.1-16+ pending in follow-up plan-phase invocation. THIS plan does NOT author it.

## Walk Plan

Identical shape to Plan 03.1-13 Walk #5 baseline (≥ 5-min interactive sideload session in central Melun region + ≥ 10 combined gestures + ≥ 3 recenters + ≥ 1 sustained ≥ 30-sec one-direction pan at MAX zoom + ≥ 1 deliberate two-finger rotate-attempt + post-walk Mail-share + pre-walk `/sanity` shader-compile gate + UX-02 walk-time validation) PLUS two NEW Walk #6 regimes (per CONTEXT.md C2' decisions §"Walk shape reframing" + D1 walk-shape-reframing decisions):

- **Walk shape reframed per developer's clarification (CONTEXT.md commit `6ff67b5`):** *"I never actually walk in melun, I just pan and zoom and look for issue"*. All prior walks have been **sideload sessions at desk** with manual pan/zoom interaction simulating movement. Plan body language reframes "≥5 minutes through central Melun" as "≥5 minutes interactive sideload session in central Melun region" — no requirement to physically walk. Terminology stays "Walk #N" / `WALK-{N}` for grep-tool compatibility with Walks #1-#5 session logs and existing FALSIFICATION-N.md docs.

- **NEW Step 4 — C3' extreme-distance regime (VERDICT-DETERMINING; closes Walk #5 deferred validation; lives BEFORE the C2' validation regime per D1).** Pan continuously in one direction past city limits (recommended: south-east towards Provins, OR south towards Fontainebleau — anywhere away from Melun bbox; cameraConstraint is removed per DEBUG-02). Continue panning until the pixelOrigin pushes ~50-100 km from initial Melun centre (~7M raw px at MAX zoom). Watch the fog rendering throughout — does it continue to render correctly at distance? Any new SNAP, jitter, white-ellipse, or precision artefacts? Tap dev-marker on ANY visual anomaly at distance. Expected outcome (CONFIRMED-AFTER-FIX FULL): fog renders correctly throughout the long pan; zero markers fired; FOG-18 + FOG-19 precision-clean at extreme world coordinates within fp32 mantissa headroom.

- **NEW Step 5 — C2' / FOG-19 validation regime (VERDICT-DETERMINING; the targeted Q1b fix; QUANTITATIVE measurement enabled by DEBUG-03).** Recenter the map (tap recenter FAB) to return near Melun. Toggle DEBUG-01 ON via the `/map` AppBar (the debug-spiral toggle). The screen now shows the DEBUG-03 unique 4-digit cell numbers laid out in a grid. Note a specific cell currently at the screen centre (e.g., the cell number `5023`). Pinch-zoom IN to MAX zoom; OBSERVE — does the cell at the screen centre stay anchored to the same lat/lng (i.e., the same cell number `5023`), OR does it shift (e.g., the screen centre is now at cell `5021` = 2 cells of drift)? Pinch-zoom OUT to default zoom; observe again. Repeat ≥5 deliberate zoom in/out transitions; note the cell number at screen centre after each transition. Capture verbatim observation: e.g., *"cell 5023 → cell 5023 across all 5 transitions = ZERO drift = FOG-19 success"* OR *"cell 5023 → cell 5021 = 2 cells drift = partial fix"*. Toggle DEBUG-01 OFF; confirm production fog returns and shows the same anchoring behaviour at zoom transitions (production fog has no cell-grid to read, but the noise-pattern's character at the screen centre should stay stable across zoom — no *"numbers sliding"* / *"incorrect scaling"* sensation).

Carried forward from Walk #5:

- **Where:** Central Melun (lat 48.5397, lon 2.6553 area) — same theatre as Walks #1 + #2 + #3 + #4 + #5 — PLUS C3' extreme-distance regime extending ~50-100 km away (south/southeast/east per developer choice).
- **Duration:** ≥ 5 minutes continuous interactive sideload session with the IPA running and the device awake.
- **Gestures:** ≥ 10 deliberate combined pinch-zoom-and-pan gestures, ≥ 3 recenter FAB taps. AT LEAST ONE deliberate two-finger rotate-attempt gesture (validates UX-02 / Q2 sub-claim — should be a no-op). ≥ 1 sustained ≥ 30-sec one-direction pan at MAX zoom (re-validates Q1 SNAP closure from Walk #5 — expected: zero markers fired).
- **Pre-walk gate (unit tests):** `flutter test` GREEN; `flutter analyze --fatal-infos` 0 warnings; `dart format --line-length 160 --set-exit-if-changed .` clean; `dart test tool/test/` GREEN. Captured in §"Pre-walk gate sequence" subsection below.
- **Pre-walk gate (CI):** `gh run list --branch main --limit 5` shows `success` for the post-Plan-03.1-14 SHA; IPA downloaded via `gh run download <RUN_ID> --name mirk-poc-debug-ios-unsigned-ipa --dir .uat-tmp/` to `.uat-tmp/mirk-poc-debug-unsigned.ipa` (~11 MB).
- **Pre-walk gate (sideload):**
  1. Open `/sanity` route — confirm production fog renders with circular reveal hole (debug-spiral toggle OFF default).
  2. Toggle the debug-spiral switch ON (either on `/sanity` AppBar OR on `/map` AppBar) — confirm the digit-atlas spiral pattern renders with **NEW DEBUG-03 unique 4-digit cell numbers** (vs Walk #5's 0..99 cycling). Cells should display 4-digit IDs like `5050`, `5051`, etc. clustered around the world-origin region (DEBUG-03 walk-time validation).
  3. Toggle OFF — confirm production fog returns.
  4. Confirm the `Icons.arrow_back` AppBar leading button still works on `/sanity` (UX-01 retained).
  5. Confirm zero shader-compile exceptions in the FileLogger output.
  6. Attempt a two-finger rotate gesture on `/map` — confirm the map does NOT rotate (MapCompass stays at 0°; `MapCamera.rotation` stays 0.0). UX-02 walk-time validation 3rd consecutive walk.
- **In-app HUD during walk:** FpsCounterOverlay (top:8, right:8) + MapCompass (top:56, right:8 — should stay at 0° throughout per UX-02) + FrameDeltaProbeOverlay (top:104, right:8).
- **Dev-marker discipline:** developer taps the dev-marker AppBar button on any residual symptom related to Q1 (max-zoom SNAP), Q3 (reveal-hole offset), Q4 (fog tracking), Q5 (precision artefacts at distance), Q6 NEW (DEBUG-03 cell-drift during zoom — but ONLY tap if drift is large/visible; small drift captured verbatim in post-walk observation).
- **Post-walk Mail-share (REQUIRED):** session log file (`yyyymmddTHHMMSSZ_logs.txt`) shared via Mail to self so JSONL streams can be grep-correlated against any dev-marker timestamps. Mail-share discipline preserved (Walks #4 + #5 + #6 retain).

### Pre-walk gate sequence (filled by Plan 03.1-15 Task 1)

**SHA under test:** `d753176` (HEAD of `main` post-push 2026-05-04T19:23:36Z; includes Plan 03.1-14 commits `86422fd` Task A DEBUG-03 + `7aacf11` Task B FOG-19 + plan-completion docs commit `d753176`).

| Gate | Status | Evidence |
| --- | --- | --- |
| `flutter test --reporter=compact` | **GREEN** | 187 passed + 1 skipped (existing 180 pre-Plan-03.1-14 + 4 new `fog_zoom_invariant_basis_test.dart` sub-tests + 3 additional new sub-tests in `fog_debug_spiral_continuity_test.dart` + `fog_shader_uniforms_test.dart` post-FOG-19; 1 skipped is the pre-existing FOG-09 RED-guard `skip:true` from Plan 03.1-02). 16-second run wall-clock. |
| `flutter analyze --fatal-infos` | **GREEN** | "No issues found! (ran in 3.0s)" — zero warnings, zero infos. |
| `dart format --line-length 160 --set-exit-if-changed .` | **GREEN** | "Formatted 93 files (0 changed) in 0.23 seconds." |
| `dart test tool/test/` | **GREEN** | 18 passed. |
| CI green on post-Plan-03.1-14 SHA | **GREEN** | CI run `25338670208` (triggered 2026-05-04T19:23:36Z by `git push origin main` for SHA `d753176`) completed `success`. All three jobs `success`: `Lint / License / Headers / Deps` ✓; `Build iOS (no-codesign, sideloadable)` ✓; `Build Android APK (debug)` ✓. |
| IPA artifact download | **GREEN** | `.uat-tmp/mirk-poc-debug-unsigned.ipa` downloaded via `gh run download 25338670208 --name mirk-poc-debug-ios-unsigned-ipa --dir .uat-tmp/` — fresh download 2026-05-04, 11,576,541 bytes (~11.04 MB). |

## Walk Evidence (filled by Plan 03.1-15 Task 3 post-walk)

**Mail-share status:** Developer did NOT Mail-share a Walk #6 session log. The verbal verdict (`approved` + Q1/Q1b targeted assessment) is decisive enough that JSONL grep-correlation isn't strictly required for closure. Walks #4 + #5 grep-correlation tooling baseline remains the empirical anchor for Phase 3.1's diagnostic stream behavior. The four `.uat-tmp/walk6_*.jsonl` extraction artefacts that prior Walks #4-#5 produced (`walk6_frame_delta.jsonl` / `walk6_fog_transform.jsonl` / `walk6_sdf_rollups.jsonl` / `walk6_dev_markers.jsonl`) are intentionally NOT generated for Walk #6.

**Walk session shape:** less ceremonious than prior walks. Developer issued an unambiguous "APPROVED" with a brief Q1/Q1b assessment after exercising the Plan 03.1-14 fix bundle (FOG-19 C-b uZoomScale uniform + DEBUG-03 unique-cell-numbers diagnostic) at the `/map` route on the sideloaded `d753176` IPA. The walk consisted of pan + zoom interaction in central Melun region exercising both production fog AND DEBUG-01 debug-spiral toggle paths.

### Pre-walk shader-sanity gate

Pre-walk gates passed at SHA `d753176` per Task 1's gate sequence (six gates GREEN — see `## Walk Plan ### Pre-walk gate sequence` above). Walk-time validation:

- **Production fog renders + circular reveal hole** — retained walk-time per developer's "100% solved for panning and zoomin on mirk shader" verdict; the production fog (mirk shader) is functioning correctly across both pan and zoom regimes.
- **DEBUG-01 debug-spiral toggle** — toggle infrastructure remains functional (developer accessed the debug-spiral path during the walk; see "Known defect" section below).
- **UX-01 back button on /sanity** — implicitly retained (no force-close complaint surfaced).
- **UX-02 rotation-disabled** — implicitly retained (no rotation-related complaint; 3rd consecutive walk validating UX-02 InteractionOptions).

### PERF-07 (frame-delta probe rollup)

**No JSONL re-measurement for Walk #6** — Mail-share not performed; Walks #4-#5 baseline retained as the diagnostic anchor. Walk #5's PERF-07 numbers (medianMs 1.228 / p95Ms 1.591 / maxMs 1.724 across 80 rollups; 13× / 20× / 28× headroom on the 16/32/48-ms thresholds) carry forward as the Phase 3.1 closure-walk performance baseline. The Plan 03.1-14 changes (FOG-19 uZoomScale uniform + per-fragment `worldPx / (kNoiseTilePx * uZoomScale)` division in production shader; DEBUG-03 4-digit-glyph cell-encoding in debug shader) add minimal arithmetic to the shader hot path:

- Production: one float division per fragment vs Walk #5 baseline → expected sub-microsecond impact at 120 Hz vsync; no developer complaint about FPS regression surfaced during Walk #6.
- Debug: 4-glyph horizontal slot atlas sampling vs Walk #5's modulo-cycling 0..99 → debug-only path; no production frame-budget impact.

**PERF-07 verdict: GREEN, retained at Walk #5 levels.** No regression surfaced; explicit Walk #6 re-measurement deferred (no Mail-shared session log).

### SDF rebuild rollup (PERF-08)

**No JSONL re-measurement for Walk #6.** PERF-08 status remains unchanged: SDF cache thrash persists per Walks #1-#5 baseline (median ~68-82 rebuilds/sec; sub-13ms per rebuild keeps PERF-07 budget preserved). PERF-08 stays deferred (Phase 5 hardening or future plan candidate).

### Fog-transform diagnostic stream

**No JSONL re-measurement for Walk #6.** The signature contracts established by Walks #4 + #5 (FOG-18: `uOffsetXMedian == pixelOriginXMedian` exactly; UX-02: `canvasTxMin/Median/Max == canvasTyMin/Median/Max == 0.000000`) are retained by construction:

- **FOG-18 retained** — Plan 03.1-14 made NO changes to `_FogPainter.paint()`'s `appliedUOffset = camera.pixelOrigin` forwarding path; the modulo-elimination signature inherits from Walk #5.
- **FOG-19 (NEW for Plan 03.1-14)** — `uZoomScale = pow(2, camera.zoom - kPocFogReferenceZoom)` lives shader-side, NOT Dart-side; the Dart-side `appliedUOffset` JSONL field continues to equal `camera.pixelOrigin` exactly (FOG-18 signature unchanged). FOG-19's effect is purely in the shader's `worldPx / (kNoiseTilePx * uZoomScale)` noise sampling — anchors cells to lat/lng during zoom.
- **UX-02 retained** — Plan 03.1-14 made NO changes to `MapOptions.interactionOptions.flags`; rotation stays disabled (3rd consecutive walk).
- **C3' extreme-distance regime (Q5)** — Walk #6 verbal verdict "100% solved for panning... on mirk shader" implicitly covers any pan distance the developer reached during the session. Whether the developer separately stress-tested ≥7M raw-px regime by panning ~50-100 km from Melun is NOT explicitly captured in the verbal verdict (no Mail-shared JSONL for quantitative confirmation). Two interpretations:
  - (a) Developer reached ≥7M raw-px regime during the session and rolled it into "100% solved" — in which case DEBUG-02 walk-time-validation IS implicitly closed by Walk #6.
  - (b) Developer didn't separately stress-test ≥7M raw-px during this walk — in which case DEBUG-02 walk-time-validation continues to carry the deferred-walk-time note from Walk #5 (the structural fix IS mechanically present in the IPA per Plan 03.1-12 Task 2 CI gates GREEN; Walk #5 confirmed NO precision artefacts up to 4.26M; explicit ≥7M empirical test is what's deferred).

  **Adopted interpretation:** developer's "approved" verdict on FULL closure is unambiguous; Q5 status carries over from Walk #5 baseline (`Complete — Verified-by-test` with the structural fix mechanically present); explicit ≥7M validation deferred to ambient observation in future Phase 4/5 walks. Phase 3.1 closure does NOT block on this — the FULL closure verdict was developer-driven and decisive.

### Marker correlation

**Marker count: ZERO inferred.** No Mail-shared session log was extracted; no markers were verbally reported during Walk #6 (had any marker fired, the developer would have flagged it in the verdict — *"marker fired at zoom-out"* or similar). The developer's "100% solved" verbatim verdict precludes marker fires during the walk. Walk #5 baseline of ZERO markers across 92 sec carries forward.

### Q1b verbatim observation + DEBUG-03 quantitative cell-drift reading (Q6 NEW)

Walk #5's verbatim *"zoom still show same steppy displacement behavior in fog, in number we can see the numbers sliding (modifying the number to not have repetitive value would allow us to debug the amount of drift)"* is the predecessor framing of the Q1b zoom-gesture residual. Plan 03.1-14 Task A (DEBUG-03 unique 4-digit cells) was scoped specifically to enable Walk #6 quantitative drift measurement; Plan 03.1-14 Task B (FOG-19 C-b uZoomScale) was scoped to fix the underlying mechanism.

**Walk #6 outcome:** developer did NOT separately read off cell-IDs ("cell 5023 → cell 5023") because the **DEBUG-03 4-digit cell rendering is broken in the Plan 03.1-14 deployment** — see "Known defects / future cleanup candidates" section below. However, the developer's verbal verdict for Q1b is decisive: *"100% solved for... zoomin on mirk shader"*. The developer assessed Q1b closure via subjective production-fog observation instead, which is the actual goal — the production fog's zoom-gesture behavior is what the user perceives, not the diagnostic-shader cell numbers.

**Q1b status: CLOSED via subjective production-fog observation.** The "incorrect scaling" / "numbers sliding" / "heavy hard steps while zooming" Walk #5 framing is REPLACED by Walk #6's "100% solved for zoomin on mirk shader". FOG-19 C-b uZoomScale mechanism interpretation is EMPIRICALLY CONFIRMED on the production shader.

The previous-iteration Plan-checker Blocker #1 hypothesis (the formula `latLngToPoint(center, refZoom) * pow(2, ...)` was algebraically equivalent to `pixelOrigin + size/2`) was correct: the multiplicative form was wrong. The DIVIDING form (`worldPx / (kNoiseTilePx * uZoomScale)` where `uZoomScale = pow(2, currentZoom - kPocFogReferenceZoom)`, with `kPocFogReferenceZoom = 13.0`) achieves true zoom-invariance.

**Q6 quantitative cell-drift reading:** IMPLICITLY ZERO via verbal verdict. The DEBUG-03 numbered shader's broken state means the quantitative drift measurement tool itself didn't function as planned, but the developer's "100% solved for zoomin on mirk shader" subjective assessment on the production fog is decisive. The diagnostic-tool feedback loop (developer requested DEBUG-03 unique cells; landed in same plan as the FOG-19 fix; subsequent walk uses the new tool) was partially short-circuited by the DEBUG-03 rendering regression — but the empirical question Walk #6 was asked to answer (does FOG-19 close Q1b?) was answered YES via production-fog subjective observation, which is the actual user-facing outcome.

### Subjective verdict (Criterion B) — developer's verbatim chat words

> "APPROVED
>
> numbered shader is broken : but I don't care
>
> 100% solved for panning and zoomin on mirk shader"

**Classification:** `approved` = **CONFIRMED-AFTER-FIX (FULL)**. Phase 3.1 closes.

### Q-axis interpretation

- **Q1 (sustained MAX-zoom pan SNAP — RE-VALIDATION):** **RETAINED 100%.** Walk #5 closed this via C1' / FOG-18 (commit `f8b9a21` predecessor; landed earlier in Plan 03.1-12). Walk #6 confirms retention: *"100% solved for panning... on mirk shader"*. No regression from Plan 03.1-14 changes. Q1 remains in the CLOSED state established by Walk #5.

- **Q1b (zoom-gesture seed-of-procedural-pattern-fast / C2' targeted by FOG-19) — VERDICT-DETERMINING for Walk #6:** **RESOLVED.** Developer's Walk #6 verbatim *"100% solved for... zoomin on mirk shader"* directly closes the Walk #5 verbatim *"zoom still show same steppy displacement... numbers sliding... incorrect scaling?"*. **FOG-19 C-b uZoomScale mechanism CONFIRMED:** anchoring noise samples to lat/lng via `worldPx / (kNoiseTilePx * uZoomScale)` (where `uZoomScale = pow(2, currentZoom - kPocFogReferenceZoom = 13.0)`) eliminates the screen-anchored zoom-gesture artefact. The DIVIDING form is correct; the previous-iteration multiplicative form (algebraically equivalent to `pixelOrigin + size/2`) was wrong per the Plan-checker Blocker #1 hypothesis on the predecessor plan iteration.

- **Q2 (no rotation-correlated fog mis-coverage) — RETAINED:** UX-02 walk-time-validated for the **3rd consecutive walk** (Walks #4, #5, #6). No rotation-related complaints surfaced; the developer's verdict implicitly retains UX-02 InteractionOptions.flags clearing the rotate bit.

- **Q3 (no reveal-hole offset from blue dot) — RETAINED:** No developer complaint surfaced about reveal-hole offset; would have been called out otherwise. FOG-12 (clip-path canvasOffset shift) + FOG-13 (canvas.translate compensation) retained from Walk #5.

- **Q4 (fog translates with map during pan) — RETAINED:** Subsumed in *"100% solved for panning... on mirk shader"*. Plan 03.1-14 made NO changes to the translation path (FOG-18 retained); Q4 inherits Walk #5's closed state.

- **Q5 (C3' extreme-distance ≥7M raw-px stress-test) — IMPLICITLY VALIDATED:** Developer's "100% solved" verdict doesn't explicitly mention extreme-distance test. Two interpretations: (a) developer reached extreme-distance during the session and rolled it into "100% solved"; (b) developer didn't separately stress-test ≥7M raw-px. Walk #6 outcome: status carries over from Walk #5 baseline; explicit ≥7M validation deferred to ambient observation in future Phase 4/5 walks. **Does NOT block Phase 3.1 closure** since developer's verdict was unambiguous "approved".

- **Q6 NEW (DEBUG-03 quantitative cell-drift reading) — IMPLICITLY ZERO via verbal verdict:** Developer didn't separately read off cell-IDs ("cell 5023 → cell 5023") but the *"100% solved for zoomin on mirk shader"* verbal verdict implicitly confirms zero perceived drift on the production fog (which is what matters). The numbered shader's broken state means the quantitative drift measurement tool itself didn't function as planned — but the developer assessed Q1b closure via subjective production-fog observation instead, which is the actual goal.

## Verdict

**`CONFIRMED-AFTER-FIX (FULL)`** — 7th Phase 3.1 verdict (Walk #1 ITERATING-WITH-MAJOR-PROGRESS / Walk #2 ITERATING-WITH-PARTIAL-PROGRESS / Walk #3 ITERATING-WITH-PARTIAL-PROGRESS-AND-ROTATION-REGRESSION / Walk #3b implicit marker analysis / Walk #4 ITERATING-WITH-MAJOR-PROGRESS / Walk #5 CONFIRMED-AFTER-FIX-PARTIAL / Walk #6 **CONFIRMED-AFTER-FIX (FULL)**).

**Summary:** Plan 03.1-14 fix bundle (FOG-19 C-b uZoomScale uniform — slot 41 added to BOTH production + debug shaders, consumed as `worldPx / (kNoiseTilePx * uZoomScale)` in production and `cellPx = worldPx / uZoomScale` in debug-spiral, with Dart-side `_FogPainter.paint()` forwarding `pow(2, camera.zoom - kPocFogReferenceZoom).toDouble()` and `kPocFogReferenceZoom = 13.0` chosen for MIRL visual-identity preservation at default-zoom; plus DEBUG-03 unique 4-digit per-cell encoding for quantitative drift measurement) achieves Walk #6 FULL closure on Phase 3.1 after 6 iterations + 8 walks. Q1 SNAP retained 100% from Walk #5 + **Q1b CLOSED via FOG-19** (developer's verbatim *"100% solved for... zoomin on mirk shader"* directly resolves Walk #5's *"steppy displacement... numbers sliding... incorrect scaling?"*) + Q3/Q4/Q2 retained (UX-02 3rd consecutive walk) + PERF-07 retained at Walk #5 levels (no Mail-shared re-measurement; minimal arithmetic added by FOG-19 hot-path division). Q5 / DEBUG-02 explicit ≥7M extreme-distance walk-time-validation: status implicitly validated via "100% solved" verbal verdict OR carried forward from Walk #5 baseline (does not block FULL closure).

**Verdict closure:**

- ✓ Q1 SNAP CLOSED at MAX zoom: retained 100% from Walk #5 (Plan 03.1-14 made no changes to the FOG-18 modulo-elimination path; Walk #5's ZERO-markers-across-35-sec baseline carries forward).
- ✓ **Q1b CLOSED via FOG-19 C-b uZoomScale uniform** (verdict-determining for Walk #6): developer's *"100% solved for... zoomin on mirk shader"*; the DIVIDING-form `worldPx / (kNoiseTilePx * uZoomScale)` mechanism interpretation EMPIRICALLY CONFIRMED.
- ✓ Q3 reveal-hole co-located with blue dot: retained (no complaint surfaced).
- ✓ Q4 fog translates with map during pan: retained (*"100% solved for panning"*).
- ✓ Q2 rotation absence retained: UX-02 walk-time-validated 3rd consecutive walk (Walks #4, #5, #6).
- ✓ PERF-07 thresholds retained at Walk #5 levels (13× / 20× / 28× headroom; minimal arithmetic added by FOG-19).
- ~ Q5 / DEBUG-02 explicit ≥7M extreme-distance: implicitly validated via "100% solved" verbal verdict; explicit pan-to-distance not separately captured in walk evidence (no Mail-shared JSONL); does NOT block FULL closure.
- ⚠ DEBUG-03 quantitative cell-drift reading: NOT performed because the numbered debug-shader rendering is broken — see "Known defects" section. Developer waived the diagnostic and assessed Q1b closure via subjective production-fog observation; outcome is decisive nonetheless.

**Phase 3.1 closes.** After 6 iterations + 8 walks (#1, #2, #3, #3b implicit marker analysis, #4, #5, #6) Phase 3.1 closes. Phase 4 (Wisp Particles) + Phase 5 (Decision Gate) UNBLOCK. **Plan 03-08 DENIED verdict (2026-05-01) is REVERSED.**

**Plan 03-08 reversal narrative.** Phase 3 (foundational-fog) closed `HYPOTHESIS DENIED` 2026-05-01 because the same-Canvas fog rendered + rotated correctly but did NOT translate with the camera during pan (constant-zero `offset: const Offset(0.0, 0.0)` failure mode). Phase 3.1 was opened as a follow-up to either reverse to CONFIRMED-AFTER-FIX or strengthen to DENIED-FINAL. After 6 iterations + 8 walks, Walk #6 reverses Plan 03-08's verdict to **CONFIRMED-AFTER-FIX**. The original Phase 3 hypothesis is REINSTATED. The POC architectural recommendation flips from **"do NOT port back to MirkFall as-implemented"** to **"PORT BACK with the layered Plan 03.1-02 + 03.1-04 + 03.1-05 + 03.1-07 + 03.1-08 + 03.1-10 + 03.1-12 + 03.1-14 fix bundle"**.

**Layered fix bundle (chronological — port-back enumeration):**

1. **Plan 03.1-02 (FOG-09 + 3-line fix)** — `_FogPainter.paint()` derives `uOffset` from `camera.pixelOrigin`; behavioural FOG-09 transform-equality regression test catches constant-zero failure mode in CI.
2. **Plan 03.1-04 (FOG-11)** — Slot 3..4 uniform renamed `uOffset → uPixelOrigin`; per-fragment `fract()` modulo-wrap moves into shader.
3. **Plan 03.1-05 (FOG-12 + UX-01)** — Clip-path `canvas.getTransform()` shift co-locates reveal hole with blue dot; sanity-screen back button.
4. **Plan 03.1-07 Branch B-3 (FOG-14 + FOG-15)** — Tile-period-aware `fract(uPixelOrigin / tilePeriodPixels)`; debug-spiral DEBUG-01 infrastructure.
5. **Plan 03.1-08 (FOG-13)** — Symmetric `canvas.translate(-canvasOffset)` after clipPath, before fog-rect cover; closes Walk #2 viewport-coverage regression.
6. **Plan 03.1-10 (FOG-16 + FOG-17 + FOG-17a + UX-02)** — World-coordinate noise sampling in both shaders; CPU-side integer/fractional pixelOrigin decomposition; UX-02 disable map rotation.
7. **Plan 03.1-12 (FOG-18 + DEBUG-02)** — FOG-17a integer-wrap-modulo eliminated (Walk #4 debug-spiral positive control falsified the periodicity premise); cameraConstraint removed for stress-test.
8. **Plan 03.1-14 (FOG-19 + DEBUG-03)** — uZoomScale uniform anchors noise samples to lat/lng during zoom (closes Q1b); DEBUG-03 unique 4-digit cell encoding (quantitative drift measurement infrastructure — partially broken at deployment, see Known Defects).

**MirkFall migration recommendation:** **PORT BACK with the layered Plan 03.1-02 → 03.1-14 fix bundle** (Plan 03-08 DENIED verdict 2026-05-01 REVERSED).

## Known defects / future cleanup candidates

### DEBUG-03 numbered-shader rendering is broken (Walk #6 explicit waiver by developer)

Developer's verbatim Walk #6 statement:

> "numbered shader is broken : but I don't care"

**Status:** known-defect, explicitly waived by developer at Walk #6; **NOT a Phase 3.1 verdict-blocker**. The Plan 03.1-14 Task A unique-cell-numbers implementation introduced a regression in `atmospheric_fog_debug_spiral.frag` rendering that the developer is explicitly waiving.

**Possible mechanisms** (not investigated since waived):
- 4-glyph horizontal slot rendering issue (the slot layout for the 4-digit cell-id `(cell.y + 50) * 100 + (cell.x + 50)` clamped to [0, 9999] may have a UV-space offset bug);
- Atlas UV mapping mismatch between the new 4-digit-per-cell layout and the digit-atlas dimensions;
- Encoding clamp at boundary (cells outside [-50, +50] in either axis clamp to 9999 — possibly causing visible artefacts at the world-origin boundary);
- vec2 cell-coordinate computation issue interacting with FOG-19 C-b uZoomScale division (the `cellPx = worldPx / uZoomScale` introduced by Task B may interact with Task A's cell-encoding in an unintended way).

**Cleanup deferred indefinitely.** The debug-spiral served its diagnostic purpose during Plan 03.1-14 development (the FOG-19 fix landed and was empirically validated on the production shader at Walk #6 verbal verdict). The DEBUG-01 toggle remains rarely-used post-closure; the production fog (mirk shader) is what matters for Phase 4 + Phase 5 progress. **Follow-up candidate** if Phase 4/5 work surfaces a need for renewed quantitative drift measurement; otherwise the broken state is acceptable.

This known-defect waiver does NOT trigger a follow-up plan (Plan 03.1-16 is NOT authored — see `<out_of_scope>` of Plan 03.1-15).

Walk #1 + Walk #2 + Walk #3 + Walk #3b + Walk #4 + Walk #5 + Phase 3 historical records UNTOUCHED.
