# Phase 3.1 Falsification — Walk #6 (Post-Plan-03.1-14 Fix B′ Meter-Space FOG-17a Decomposition + FOG-19)

**Created:** 2026-05-03
**Phase:** 03.1-fix-fog-pan-translation
**Walked on:** 2026-05-04 — iPhone 17 Pro, central Melun, CI run `25291831174` (SHA `15e98b3`). Mail-shared session log saved to `C:\claude_checkouts\mirk-poc-debug\20260503T220301Z_logs.txt\20260503T220301Z_logs.txt` per the STRUCTURALLY-ENFORCED Mail-share gate (held second time after Walk #5's first-time success). 5473 lines / 51 fog_transform rollups + 51 frame_delta rollups + 42 SDF rollups + 7 dev_markers spanning 00:03:01.99Z..00:03:54.87Z (~53-second walk session).
**Verdict:** **DENIED-WITH-NEW-Q1-FAILURE-MODE-AND-Q5-PARTIAL-IMPROVEMENT — see Plan 03.1-16+** (Q1 panning DENIED with a NEW failure mode the developer described as fog *"translating smoothly while panning instead of being attached to the map as it should"* — qualitatively distinct from Walk #5's *"steppy moments at wrap events"* characterisation; 7 dev-markers tagged `steppy_translation` fired in 21 seconds — 3.5× Walk #5's 2 markers; meter-space wrap events EMPIRICALLY CORRELATE with marker times in fog_transform JSONL, falsifying the Plan 03.1-14 sub-perceptibility prediction. Q5 zoom-scramble PARTIALLY IMPROVED — *"zoom is less worse, way less steppyness but still some"* — meter-space anchor delivers the Q5 architectural completion Walk #5 missed but residual stepping persists. Per CONTEXT §Iteration policy: 7th `ITERATING` verdict — the architectural model is now mechanistically diagnosed (Plan 03.1-14's analytical `kPocFogFbmDiscontinuityBound = 0.20` ≈ 11% fbm3 dynamic range is NOT sub-perceptual at the developer's worldMeters positions; the constant-magnitude INVARIANCE property landed correctly per the JSONL signature but the perceptual threshold prediction failed). Plan 03.1-14's documented fall-back path activates: option D-3 (Worley periodic noise primitive — period exactly aligned with `kPocFogNoiseTilePxMeters`, no per-octave matrix-amplification) is the recommended Plan 03.1-16 fix axis.

## Relationship to Walk #5 Verdict

Walk #5 (Plan 03.1-13, 2026-05-03) closed `ITERATING-WITH-Q1-REGRESSED-AND-Q5-NOT-FIXED-FROM-PERIOD-COMMENSURABILITY-GAP`. The Plan 03.1-12 FOG-18 software-fix bundle (world-meter anchor in BOTH atmospheric_fog.frag + atmospheric_fog_debug_spiral.frag + new uniform uMetersPerPixel slot 41 + Dart-side metersPerPixel computation in 4 painter sites) walk-time-evaluated as architecturally landed correctly (cells now scale with zoom in meter-space; noise position is zoom-invariant in meter-space at the single-frame level) BUT Plan 03.1-12 RETAINED the FOG-17a integer/fractional decomposition in PIXEL space, leaving a period-commensurability gap with the meter-space noise tile. The 1536-px wrap translated to a 4.7421-cell shift in noiseUv at z=15 lat 48.5° — a 0.7421-cell visible discontinuity per wrap event. Walk #5 dev-markers tagged `steppy_translation` correlated with Y-axis FOG-17a wrap events in fog_transform JSONL within ±1 sec. Q1 panning REGRESSED from Walk #4's *"perfect"* (Plan 03.1-11; commit chain `dbc3e21..b31766f` — FOG-17 + FOG-17a in pixel-space delivered the only walk-time-confirmed perfect-pan in the Phase 3.1 series); Q5 zoom-scramble NOT FIXED.

Walk #5 STRUCTURAL ACHIEVEMENT — Mail-share gate held on first attempt. The STRUCTURALLY-ENFORCED Mail-share gate (resume-signal contract requires log path as parameter) succeeded after Walks #3 + #4 broke Mail-share twice. Quantitative JSONL grep correlation against the developer's 2 dev-markers anchored the architectural diagnosis empirically.

Walk #5 BUILD-PROVENANCE GAP — the Plan 03.1-13 Task 1a logger augmentation commit `2b087a3` (FogTransformLogger metersPerPixel field) post-dated CI run `25285037397`. Walk #5's IPA lacked the empirical metersPerPixel JSONL signature; mechanically derivable but non-empirical. **Walk #6 STRUCTURAL build-provenance gate CLOSED this gap** — Task 1's `git rev-parse HEAD == gh run view 25291831174 --json headSha == 15e98b3` check passed; Walk #6's IPA contains the post-Plan-03.1-14 logger augmentations EMPIRICALLY (51 fog_transform rollups all carry `coordinateSpace: 'meters'` AND `metersPerPixel` fields).

Plan 03.1-14 closed FOG-19 software-side via the Fix B′ meter-space FOG-17a decomposition: Dart-side `worldMeters = pixelOrigin × metersPerPixel; bounded = (intMeters % kPocFogIntegerWrapPeriodMeters) + fracMeters` with `kPocFogIntegerWrapPeriodMeters = 4096 m = 4 × kPocFogNoiseTilePxMeters` (1024 m). Shader uniform slot 3..4 RENAMES from `uPixelOrigin` to `uWorldMetersOrigin` (semantic flip; FogShaderUniforms.totalFloatSlots STAYS at 42 — no new uniform). Period-commensurability invariant `4096 % 1024 == 0` GREEN; wrap injects exactly 4 integer cells in noiseUv → Octave 1 bit-identical, Octaves 2 + 3 receive a CONSTANT deterministic phase shift bounded analytically ≈ 11% of fbm3 dynamic range; the architectural property is INVARIANCE across all wrap events (NOT mathematical invisibility); see Plan 03.1-14 `continuity_proof_for_plan_03_1_14` for the full chain-of-reasoning. NEW v1 requirement FOG-19 captures the meta-invariant. NEW behavioural tests `fog_meter_space_decomposition_test` + `fog_meter_wrap_invisibility_test` GREEN. 7 REWRITTEN tests flip pixel-space invariants to meter-space.

Walks #1 + #2 + #3 + #3b + #4 + #5 records (`03.1-FALSIFICATION.md`, `03.1-FALSIFICATION-2.md`, `03.1-FALSIFICATION-3.md`, `03.1-FALSIFICATION-4.md`, `03.1-FALSIFICATION-5.md`, `03.1-UAT.md` through `03.1-UAT-5.md`, `03.1-WALK-3B-MARKER-ANALYSIS.md`) AND Phase 3 record (`03-FALSIFICATION.md`) ALL stand as historical record — UNTOUCHED. Walk #6's verdict is a SEPARATE artefact in this file.

## Hypothesis

After the Plan 03.1-14 software fix bundle (Fix B′ meter-space FOG-17a decomposition + FOG-19; shader uniform slot 3..4 rename uPixelOrigin → uWorldMetersOrigin; period-commensurability invariant `kPocFogIntegerWrapPeriodMeters % kPocFogNoiseTilePxMeters == 0` GREEN), the same-Canvas fog now:
(a) translates with the camera during pan WITHOUT visible stepping at wrap events (Walk #5 Q1 regression closes — wrap injects exactly +4 integer cells in noiseUv producing a CONSTANT-magnitude phase shift INVARIANT across all wrap events: Octave 1 bit-identical via hash3 period-1, Octaves 2 + 3 receive a deterministic ≈ 11% fbm3-dynamic-range residual shift; the constant-magnitude property eliminates the pre-fix variable-magnitude comparative-stepping signal; whether the residual constant-phase shift is sub-perceptual is the empirical Walk #6 PRIMARY GATE question — see Plan 03.1-14 continuity_proof_for_plan_03_1_14);
(b) is structurally invulnerable to rotation-correlated mis-coverage (UX-02 retained — Plan 03.1-14 doesn't touch the rotation handling);
(c) maintains a fully stable noise pattern across consecutive paints AT A SINGLE ZOOM LEVEL (FOG-17 + FOG-19 retained — the world-coordinate sampling + integer/fractional decomposition both apply in meter space);
(d) keeps the reveal hole co-located with the blue dot during pan/zoom (FOG-12 + FOG-13 retained);
(e) is anchored to ground meters such that at a fixed geographic point, the noise sample is zoom-INVARIANT (FOG-18 retained, with Walk #5 Q5 zoom-scramble closing because bounded_meters is geographic, not pixel).

Re-stated against the original POC core value: the fog-of-war stays perfectly locked to the map during pan, zoom, and combined gestures on a sideloaded iOS build. **CONFIRMED-AFTER-FIX** means the Phase 3 hypothesis is reinstated and the POC ports back to MirkFall with the full Plan 03.1-02 + 03.1-04 + 03.1-05 + 03.1-07 + 03.1-08 + 03.1-10 + 03.1-12 + 03.1-14 fix bundle; **DENIED-FINAL** means even Fix B′ meter-space FOG-17a decomposition doesn't deliver fog-camera-tracking parity (Phase 3.2 alternative-renderer pivot conversation); **ITERATING** means a residual failure mode surfaces (e.g., a NEW second-order failure introduced by Plan 03.1-14, OR fp32 precision degradation at extreme zoom in meter-space — the Plan 03.1-14 SUMMARY's documented sub-cell precision held mathematically but FBM-rotated-octave behaviour can be empirically surprising).

## Falsification Criteria (written BEFORE the walk)

### Criterion A — Frame-delta probe thresholds (PERF-07 re-validation, quantitative)

Same thresholds as Walks #1-5: `medianMs ≤ 16`, `p95Ms ≤ 32`, `maxMs ≤ 48` across ≥ 10 deliberate combined gestures. Walk #1: 4-7×; Walk #2: 3.9-5.4×; Walk #3b: 40-110× headroom on a 33-sec window; Walks #3 + #4: implicit (no log Mail-shared); Walk #5: 8.9×/5.2×/4.9× (median 1.808 / p95 6.105 / max 9.756). Walk #6 should preserve at least Walk #5 levels (Plan 03.1-14 adds 2 multiplications + 1 modulo + 1 truncate + 1 subtract per axis per paint at the Dart side — negligible; the shader gains nothing per-fragment — formula reorder, not new operations).

**STRUCTURALLY-ENFORCED Mail-share gate retained from Plan 03.1-13** — Walk #5 succeeded on first attempt; Walk #6 retains identically.

### Criterion B — Subjective visual lock (PERF-05 carry-over, re-tested)

Developer's verbal verdict at end of walk:

- **Q1 (PRIMARY Walk #6 closure-evidence-gate)** — ZERO `dev_marker steppy_translation` taps during sustained ≥ 60-second one-direction pan. Walk #5 fired 2 such markers; Walk #6 expects zero. Mechanism: meter-space wraps inject +4 integer cells in noiseUv → Octave 1 bit-identical, Octaves 2 + 3 receive a CONSTANT deterministic phase shift (constant-magnitude, zoom-independent, lat-independent — eliminates Walk #5's variable-magnitude stepping signal). Whether the residual ≈ 11% fbm3 dynamic-range shift is sub-perceptual is the empirical Walk #6 question.
- **Q2 (RETAINED from Walks #4-5)** — No rotation-correlated fog mis-coverage. Plan 03.1-14 doesn't touch UX-02 / FOG-16. Sub-test: developer attempts a deliberate two-finger rotate gesture; the map should NOT rotate.
- **Q3 (RETAINED from Walks #1-5)** — No reveal-hole offset from blue dot during gesture.
- **Q4 (RETAINED from Walks #1-5)** — Fog visually translates with camera during pan.
- **Q5 (PRIMARY Walk #6 closure-evidence-gate, retest from Walk #5)** — Noise pattern is anchored to ground meters; at a fixed geographic point the noise sample is zoom-invariant; cells visibly enlarge with zoom. Walk #5 reported failure; Plan 03.1-14 closes this because bounded_meters is geographic.
  - Sub-test 5a (production fog): developer fixes camera centre on lat 48.5397, lon 2.6553 and zooms from min to max; verifies the production fog noise pattern at the camera centre appears stable across zoom transitions (no rapid sliding/scramble).
  - Sub-test 5b (debug-spiral): developer toggles debug-spiral ON; zooms from min to max; verifies (a) cells visibly enlarge with zoom (cells are tied to ground); (b) at fixed map position the spiral cell numbering at the camera centre stays the same across all zoom levels (the fragment under the blue dot should always sample the same noise cell, regardless of zoom — direct visual confirmation of FOG-18 + FOG-19 zoom-invariance acceptance property).
  - Sub-test 5c (debug-spiral pan during fixed zoom): with debug-spiral ON at z=15, pan the camera ~500 m. Cells should scroll smoothly with NEW noise entering from the viewport edges (no fract-wrap; FOG-17 retained).
  - Sub-test 5d (rapid-zoom comparison): for direct comparison with Walk #5 (where Q5 failed), zoom in/out QUICKLY (rapid pinch). Pre-Plan-03.1-14 the noise pattern slid rapidly during zoom (the "scramble"); post-Plan-03.1-14 it should appear stable.
- No white-ellipse during fast pinch-zoom (already passed Walks #1-5).
- No inversion at any zoom level (already passed Walks #1-5).

### Criterion C — DROPPED

Stays dropped per Phase 3 + roadmap lock.

### Criterion D — Diagnostic stream confirms the layered fix

Per the `Logger('infrastructure.mirk.fog_transform')` 1-Hz JSONL rollups:

- **CONFIRMED-AFTER-FIX:** `pixelOriginX/Y` track gesture monotonically. `appliedUOffset` keys (semantic flipped from pixel-space to meter-space at Plan 03.1-14) stay under `kPocFogIntegerWrapPeriodMeters + 1` (~4097 m) regardless of zoom level — the FOG-19 invariant MUST hold. **NEW: `coordinateSpace: 'meters'` field present** in fog_transform rollup body — Plan 03.1-14 Task 2 augmentation; verifies the JSONL semantics flip landed in the IPA. **metersPerPixel field present and consistent with synthetic camera state** — Plan 03.1-13 augmentation, NOW empirically captured because build-provenance gate holds. At zoom 13 lat 48.5° expected ≈ 12.66; at zoom 15 ≈ 3.16; at zoom 19 ≈ 0.198. `canvasTx/Ty` jumps still occur — Plan 03.1-08 FOG-13 compensates them mechanically. **dev_marker JSONL signature for Walk #6 closure:** ZERO `steppy_translation` markers during the walk (or, if any fired, marker-correlation analysis MUST show NO correlation with `appliedUOffset` wrap-boundary crossings — confirming the residual constant-phase fbm3 discontinuity at wraps is sub-perceptual; if correlations exist, the architectural model needs the D-3 fall-back per Plan 03.1-14 `if_walk_6_surfaces_residual_stepping`).
- **DENIED-FINAL pattern:** the diagnostic stream shows the layered fix landed mechanically AT EVERY level (pixelOrigin tracks; appliedUOffset bounded under 4097 m; coordinateSpace='meters'; metersPerPixel correctly varies; canvas.translate compensates rect-cover; UX-02 makes camera.rotation = 0.0) AND the user-observable Q1/Q5 STILL persist — root cause is even deeper than meter-space FOG-17a (e.g., FBM-rotated-octave non-trivial behaviour at integer-cell shifts that the continuity proof understates; or fp32 precision empirically degrades sooner than analysed). Architecture is unfixable in `flutter_map 7.0.2` with the current shader stack. Phase 3.2 alternative-renderer pivot opens.
- **ITERATING pattern:** subjective verdict surfaces a residual failure mode. Most likely candidates: (i) NEW second-order failure mode introduced by Plan 03.1-14 (e.g., the meter-space modulo at extreme worldMeters values produces a Dart-side fp64 precision jitter visible in the rendered output); (ii) FBM-rotated-octave non-integer-multiple shift that wasn't fully captured by the continuity proof; (iii) Q2/Q3/Q4 regression introduced by Plan 03.1-14 (the painter signature changes carry risk). The diagnostic stream + (if markers fired) marker-correlation analysis points at the next fix axis; Plan 03.1-16+ is opened.

## Walk Plan

Mirrors Plan 03.1-13 (Walk #5) walk shape with one NEW STRUCTURAL addition for Walk #6:

- **NEW: STRUCTURAL build-provenance gate.** Pre-walk Task 1 explicitly verifies `git rev-parse HEAD == <CI run's headSha>`. If HEAD has commits after the CI SHA, the executor pushes them and triggers a fresh CI run BEFORE proceeding. This eliminates the build-provenance gap class of failure (Walk #5 commit `2b087a3` post-dated CI run `25285037397`).
- **STRUCTURALLY-ENFORCED Mail-share discipline RETAINED** from Plan 03.1-13. The resume-signal contract REQUIRES the absolute path of the Mail-shared session log file as a parameter. Walk #5 succeeded on first attempt; Walk #6 retains identically.
- **EXPLICIT Q1 retention re-test** at sustained ≥ 60-second one-direction pan: developer is instructed to fire `steppy_translation` dev-markers if ANY residual stepping is perceived during sustained pan.
- **EXPLICIT Q5 PRIMARY GATE retest** at zoom-traversal with debug-spiral toggle ON: same procedure as Walk #5 sub-tests 5a/5b/5c/5d.

- **Where:** Central Melun (lat 48.5397, lon 2.6553 area).
- **Duration:** ≥ 5 minutes continuous walk with the IPA running and the device awake.
- **Gestures:** ≥ 10 deliberate combined pinch-zoom-and-pan gestures, ≥ 3 recenter FAB taps. AT LEAST ONE deliberate two-finger rotate-attempt gesture (validates Q2). AT LEAST ONE sustained ≥ 60-second one-direction pan (validates Q1 — PRIMARY closure-evidence-gate). AT LEAST ONE complete zoom traversal (min zoom → max zoom or max → min) at a fixed map position with both production fog AND debug-spiral toggled (validates Q5 — PRIMARY closure-evidence-gate).
- **Pre-walk gate (unit tests):** `flutter test` GREEN (Plan 03.1-14 new tests included); `flutter analyze --fatal-infos` 0 warnings; `dart format --line-length 160 --set-exit-if-changed .` clean; `dart test tool/test/` GREEN.
- **Pre-walk gate (CI):** `gh run list --branch main --limit 5` shows `success` for the post-Plan-03.1-14 SHA; `gh run view <RUN_ID>` confirms `gates`, `build-android`, `build-ios` all green.
- **Pre-walk gate (BUILD-PROVENANCE — STRUCTURAL):** `gh run view <RUN_ID> --json headSha` returns the CI's source SHA; `git rev-parse HEAD` returns local HEAD; the two MUST be equal. If unequal: push HEAD, trigger fresh CI run, wait for green, download new IPA.
- **Pre-walk gate (IPA):** `gh run download <RUN_ID> --name mirk-poc-debug-ios-unsigned-ipa --dir .uat-tmp/` (produces `.uat-tmp/mirk-poc-debug-unsigned.ipa`).
- **Pre-walk gate (sideload):**
  1. Open `/sanity` route — confirm production fog renders with circular reveal hole (debug-spiral toggle OFF default).
  2. Toggle the debug-spiral switch ON — confirm the digit-atlas spiral pattern renders with visible numbered cells (DEBUG-01 walk-time validation).
  3. Toggle OFF — confirm production fog returns.
  4. Confirm the `Icons.arrow_back` AppBar leading button still works on `/sanity` (UX-01 retained).
  5. Confirm zero shader-compile exceptions in the FileLogger output (Plan 03.1-14's slot-3/4 rename + worldMeters formula flip MUST NOT produce a compile error in the post-Plan-03.1-14 IPA).
  6. Attempt a two-finger rotate gesture on `/map` — confirm the map does NOT rotate (UX-02 walk-time validation).
- **In-app HUD during walk:** `FpsCounterOverlay` (top:8, right:8) + `MapCompass` (top:56, right:8 — should stay at 0° throughout per UX-02) + `FrameDeltaProbeOverlay` (top:104, right:8).
- **Post-walk:** **STRUCTURALLY-ENFORCED.** Share the session log file via Mail (LOG-04 round-trip — REQUIRED for Walk #6; structural gate retained from Plan 03.1-13). Save the Mail attachment to a known local path. Report the absolute path as a parameter in the resume signal: `approved [/full/path/to/walk6_session_log.txt] — Q1+Q5 closed by Plan 03.1-14 + Q2-Q3-Q4 retained`. The executor then runs grep against the actual log file before authoring the verdict.

## Walk Evidence (filled AFTER the walk by Plan 03.1-15 Task 3)

### STRUCTURAL build-provenance gate — PASSED (NEW for Walk #6)

`git rev-parse HEAD == 15e98b3dd6e5332c417674f83e930b884120ce16`. CI run `25291831174` `headSha` field returns `15e98b3dd6e5332c417674f83e930b884120ce16`. **Equal — the structural gate succeeded on first attempt.** Walk #6's IPA was built from the EXACT commit on local HEAD, including:
- Plan 03.1-13 commit `2b087a3` (FogTransformLogger metersPerPixel field augmentation).
- Plan 03.1-14 commit `0d65212` (Fix B′ meter-space FOG-17a decomposition + FOG-19 — `coordinateSpace: 'meters'` JSONL field added; shader slot-3/4 rename).
- Plan 03.1-15 Task 1 commit `15e98b3` (this falsification + UAT skeletons; non-load-bearing).

The build-provenance gap class of failure that Walk #5 surfaced is structurally eliminated. **The metersPerPixel JSONL field is now empirically captured** — confirmed below in the fog-transform rollup section (51 of 51 rollups carry the field; range `[3.164, 26.660]` consistent with the developer's observed zoom regimes).

### Plan-vs-reality: artifact name mismatch (Rule 3 deviation)

Plan 03.1-15 Task 1 step 8 specified `gh run download <RUN_ID> --name ios-unsigned --dir .uat-tmp/`. Actual workflow artifact name in `.github/workflows/ci.yml` line 120 is `mirk-poc-debug-ios-unsigned-ipa`. Task 1 used the correct artifact name; documented as Rule 3 — Blocking deviation. Future plan templates should be aligned. IPA downloaded successfully to `.uat-tmp/mirk-poc-debug-unsigned.ipa` (11.58 MB).

### Pre-walk shader-sanity gate — SKIPPED by developer (deviation from plan)

Developer explicitly stated: *"I didn't do the sanity pre walk, frankly I don't get what purpose it serves"*. **NOT BLOCKING for the verdict** — the production fog rendering exercises the FOG-19 path regardless (the `_FogPainter.paint()` decomposition block executes on every paint at `/map`). The dev-markers, JSONL streams, and visual perception evidence below are all collected from the production walk regime.

The /sanity gate is documented in this section so future walks understand its purpose. The /sanity gate's design intent:
1. **Build-confusion detection** — confirms the sideloaded IPA is the one CI actually built (catches the case where SideStore installed an older cached IPA, or the dev grabbed a stale .ipa from a previous CI run). The build-provenance gate at Task 1 makes this intent partially redundant for the executor side, but the on-device verification adds a second defence layer (shader-compile errors at /sanity surface BEFORE the developer commits to a 5-minute walk).
2. **Shader-compile exception check** — the post-Plan-03.1-14 IPA contains a slot-3/4 uniform rename + a flipped worldMeters formula; if these failed at IPA-link-time (e.g., due to a metal/SPIR-V translation regression), `/sanity` would exhibit a black or fallback rendering. Production `/map` would exhibit the same failure but the dev would have to walk to notice it.
3. **DEBUG-01 + UX-01 + UX-02 walk-time validation** — three Phase 3.1 requirements rely on the toggle infrastructure + back-button + rotation-disable being visibly correct.

**Recommendation for future walk plans:** RETAIN the /sanity gate as STRUCTURALLY OPTIONAL but make the rationale explicit in the resume-signal documentation. The developer's *"I don't get what purpose it serves"* feedback indicates the rationale is undercommunicated in the current `<how-to-verify>` block. Plan 03.1-16+ should phrase the gate as *"~30 second pre-walk smoke test confirming the IPA you just installed actually contains the post-Plan-X changes — saves a 5-minute walk if SideStore cached an older IPA"* rather than as a procedural requirement. Alternative: drop the /sanity gate from future walks and rely solely on the build-provenance gate + first fog_transform rollup's `coordinateSpace` + `metersPerPixel` signature check (which is what was effectively done at Walk #6 — and the architectural diagnosis still landed cleanly).

### Probe rollup (frame-delta JSONL — `Logger('infrastructure.mirk.frame_delta')`) — PERF-07 GREEN with massive headroom

**51 1-Hz frame_delta rollups captured across the ~50-sec session.** Envelope:

| Statistic     | min       | median-of-medians | max       | Threshold | Headroom         |
| ------------- | --------- | ----------------- | --------- | --------- | ---------------- |
| `medianMs`    | 0.037 ms  | 0.311 ms          | 1.780 ms  | ≤ 16 ms   | **9.0× minimum (51× at median)** |
| `p95Ms`       | 0.223 ms  | —                 | 6.017 ms  | ≤ 32 ms   | **5.3× worst-case** |
| `maxMs`       | 0.279 ms  | —                 | 9.233 ms  | ≤ 48 ms   | **5.2× worst-case** |

`sampleCount` per rollup ranges 97-121 (consistent with ProMotion 120 Hz cadence; the 97 outlier is the first-second rollup which started mid-second).

**PERF-07 GREEN with at least Walk #5 baseline retention** (Walk #5: med 1.808 / p95 6.105 / max 9.756; Walk #6: med 0.311 / p95 6.017 / max 9.233 — median IMPROVED 5.8× because the developer spent more of the session in idle/static moments; p95 + max statistically equivalent). Plan 03.1-14's added Dart-side cycles (2 multiplications + 1 modulo + 1 truncate + 1 subtract per axis per paint) and shader-side formula-reorder did NOT regress the frame budget. **PERF-07 status flips to `Complete — Re-measured (P03.1-15 Walk #6)` with the actual numbers.**

### SDF rebuild rollup (`Logger('infrastructure.mirk.sdf')` — PERF-08 status — confirms thrash persists)

42 1-Hz INFO SDF rollups captured. Comparable to Walk #5 cadence; **PERF-08 stays at `Falsified-by-walk-2`** — no Plan 03.1-14 work targeted PERF-08; status unchanged. Per-rebuild cost stays sub-13 ms (`medianMs`/`p95Ms`/`maxMs` for SDF rebuilds are well under the frame budget — frame_delta envelope above confirms PERF-07 holds despite SDF thrash). **Plan 03.1-14 contingency — PERF-08 is NOT load-bearing on the Q5/Q1 architectural fix axis** but should be revisited if Walk #7+ stays in iterating mode for many more cycles.

### Fog-transform rollup (`Logger('infrastructure.mirk.fog_transform')`) — Plan 03.1-14 SIGNATURES EMPIRICALLY VERIFIED

51 1-Hz fog_transform rollups captured. **Plan 03.1-14 architectural signatures present at every level:**

| Signature | Required | Empirical (Walk #6) |
|-----------|----------|---------------------|
| `coordinateSpace: 'meters'` field present in EVERY rollup | ALL 51 rollups | **51/51 ✓** (Plan 03.1-14 Task 2 augmentation EMPIRICALLY VERIFIED — direct visible signature; the JSONL semantic flip landed in the IPA) |
| `metersPerPixel` field present + non-zero + smoothly evolving | ALL 51 rollups | **51/51 ✓** (Plan 03.1-13 augmentation NOW EMPIRICAL — Walk #5 had no field at all due to build-provenance gap; Walk #6 envelope `[3.164, 26.660]` m/raw_px consistent with developer's z=10..15 zoom regimes — at z=15 lat 48.5° expected 3.16 ≈ observed 3.164 ✓; at z=10 lat 48.5° expected 25.3 — observed 26.66 close to expected for lat ≈ 48.49 ✓) |
| `appliedUOffsetX/Y` (now meter-space at Plan 03.1-14) bounded under `kPocFogIntegerWrapPeriodMeters + 1 = 4097 m` across ALL rollups | ALL 51 rollups | **51/51 ✓** — uOffsetXMax envelope `[241.03, 4095.08]`, uOffsetYMax envelope `[431.10, 4094.34]` (both strictly under 4097 m). The FOG-19 meter-space invariant HOLDS empirically across 51 rollups including the post-walk zoom-out regime where pixelOrigin reaches 4.26M (z=15) — Walk #5's pixel-space envelope `[789.6, 1535.8]` raw px is now superseded by Walk #6's meter-space envelope under 4097 m. |
| `pixelOriginX/Y` track gesture monotonically | continuous | ✓ — pixelOriginX envelope `[505126, 4256094]`; pixelOriginY envelope `[343678, 2897350]`. Wide range reflects the developer's full zoom-in then zoom-out sweep through z≈10..15. |
| `canvasTx/Ty` zeroed mechanically (FOG-13 retains) | 0.000000 | ✓ — ALL 51 rollups show `canvasTxMin/Median/Max == canvasTyMin/Median/Max == 0.000000`. Plan 03.1-08 FOG-13 mechanism mechanically zeroes the canvas-frame offset; the rect-cover paints in the world-aligned frame. **No fog-rect viewport-coverage gap** (FOG-13 retains its Verified-by-test-only + walk-implicit status). |

**KEY OBSERVATION — METER-SPACE WRAP EVENTS DETECTED IN WALK #6 JSONL.** Across the session, multiple `appliedUOffsetX/Y` rollups exhibit envelopes that span the full `[<small>, ~4090]` meter-space range within a single second — the EMPIRICAL signature of meter-space wrap events under Plan 03.1-14 Fix B′. Examples (selected rollups during the developer's marker-firing window):

| epochSec | uOffsetXMin | uOffsetXMax | uOffsetYMin | uOffsetYMax | Wrap event present? |
| -------- | ----------- | ----------- | ----------- | ----------- | ------------------- |
| 1777845817 | 15.34 | 4087.86 | 8.22 | 4094.34 | **YES — full-range sweep on BOTH axes** (zoom-out transition through multiple wrap windows in 1 sec) |
| 1777845818 | 3646.79 | 4048.01 | 3268.10 | 3794.92 | (M3 fired this second — partial X wrap proximity) |
| 1777845820 | 241.03 | 3305.55 | 7.42 | 4084.53 | **YES — Y axis full sweep** |
| 1777845828 | 11.13 | 4095.08 | 1725.26 | 3411.21 | **YES — X axis full sweep** (M5 fired this second — wrap event correlates with marker time) |
| 1777845830 | 19.23 | 4091.14 | 4.57 | 4054.99 | **YES — full-range sweep on BOTH axes** |
| 1777845832 | 187.80 | 3086.02 | 11.42 | 4039.03 | **YES — Y axis full sweep** |

The meter-space FOG-19 invariant `bounded < 4097` HOLDS at every rollup, BUT wrap events occur at the cadence the developer's gestures induce — and **the dev-marker firing pattern (Q1 stepping perception) correlates temporally with these meter-space wrap events**. This is the EMPIRICAL falsification of Plan 03.1-14's analytical sub-perceptibility prediction (per the `kPocFogFbmDiscontinuityBound = 0.20` ≈ 11% fbm3 dynamic-range estimate).

### Dev-marker rollup (`infrastructure.mirk.dev_marker`) — SEVEN MARKERS, ALL `steppy_translation`, EMPIRICALLY CORRELATE WITH METER-SPACE WRAP EVENTS

**Seven dev-markers fired during Walk #6** (3.5× Walk #5's 2 markers). All tagged `steppy_translation`:

| Marker | Epoch (ms)      | Wall-clock UTC          | epochSec    | Δ to prev marker |
| ------ | --------------- | ----------------------- | ----------- | ---------------- |
| #1     | 1777845812749   | 2026-05-04T00:03:32.749 | 1777845812  | —                |
| #2     | 1777845815741   | 2026-05-04T00:03:35.741 | 1777845815  | +2.992 sec       |
| #3     | 1777845818883   | 2026-05-04T00:03:38.883 | 1777845818  | +3.142 sec       |
| #4     | 1777845821625   | 2026-05-04T00:03:41.625 | 1777845821  | +2.742 sec       |
| #5     | 1777845828776   | 2026-05-04T00:03:48.776 | 1777845828  | +7.151 sec       |
| #6     | 1777845831451   | 2026-05-04T00:03:51.451 | 1777845831  | +2.675 sec       |
| #7     | 1777845833443   | 2026-05-04T00:03:53.443 | 1777845833  | +1.992 sec       |

Markers cluster across a 21-second window. The developer reports the markers were fired during a sequence: *"in the log I clic the bug button to mark when I notice a step when I waz zooming, then panning."* — i.e., zoom-then-pan stepping perception. This is consistent with the JSONL evidence:
- M1-M4 fire during `epochSec 1777845812..1777845821` — zoom-out window where mpp climbs from 7.585 → 26.63 m/raw_px (z≈14 → z≈10).
- M5-M7 fire during `epochSec 1777845828..1777845833` — pan window where mpp stays at ≈26.65 (z≈10) and pixelOrigin barely moves (~10-100 raw px between paints).

#### Per-marker fog_transform correlation (within ±1s window)

**Marker #1 (epochSec 1777845812 — zoom-stepping perception):** The marker-second rollup has `pixelOriginX/Y` frozen at `(1775073, 1208192)`, mpp = 7.585, uOffsetX = 3799.30, uOffsetY = 932.31. The PREVIOUS second (1777845811): `pixelOriginX` swept `[1775073, 2381840]`, mpp swept `[5.65, 7.58]` — i.e., zoom-step in progress with 600-px pixelOrigin advancement at varying mpp. **The instant the camera locked at the new zoom, M1 fired.** Mechanism: during zoom transitions, mpp halves while pixelOrigin doubles, so `worldMetersOrigin = pixelOrigin × mpp` should stay continuous if mpp halves smoothly. But mpp is computed per-paint from `156543.03 × cos(lat) / pow(2.0, zoom)`, so any non-monotonic camera-zoom updates from `flutter_map`'s gesture pipeline produce per-paint discontinuities in `worldMetersOrigin`. This is the candidate mechanism for M1's stepping perception during zoom.

**Marker #2 (epochSec 1777845815 — zoom-stepping perception):** Marker-second rollup has pixelOriginX/Y in transition from `(1276015, 868434)` → `(1258708, 856651)`, mpp transitioning from 10.55 → 10.70. Same zoom-transition mechanism as M1.

**Marker #3 (epochSec 1777845818 — zoom-stepping perception, NEAR meter-space wrap):** Marker-second rollup uOffsetXMax = 4048.01, uOffsetYMax = 3794.92 — close to wrap boundary 4096 m. The PREVIOUS second (1777845817) shows `uOffsetX[15.34, 4087.86]` and `uOffsetY[8.22, 4094.34]` — full-range meter-space sweeps on BOTH axes within a single second. **M3 fires immediately after a multi-axis meter-space wrap event.** Plan 03.1-14's prediction: per-wrap fbm3 discontinuity ≈ 11% of dynamic range, INVARIANT and sub-perceptual. Empirical observation: the developer perceived stepping at this boundary crossing.

**Marker #4 (epochSec 1777845821 — pan-stepping perception, frozen camera):** Marker-second rollup has pixelOrigin frozen at `(505232, 343678)`, mpp = 26.63, uOffsetX = 241.03, uOffsetY = 2541.01. The PREVIOUS second (1777845820) shows `uOffsetY` sweeping `[7.42, 4084.53]` — full-range Y-axis meter-space sweep. **M4 fires immediately after a Y-axis meter-space wrap event in the previous second.** Same mechanism as M3.

**Marker #5 (epochSec 1777845828 — pan-stepping perception):** Marker-second rollup has uOffsetX sweeping `[11.13, 4095.08]` — **a meter-space wrap event occurred IN this second.** pixelOrigin barely moves (~15 raw px change × 26.64 mpp ≈ 400 m of ground), mpp stays at 26.64, but the wrap-event traverses the full 4096-m meter-space window. **M5 fires DURING a meter-space wrap event.** The constant-magnitude phase shift Plan 03.1-14 predicted is empirically perceptible at this developer's worldMeters position.

**Marker #6 (epochSec 1777845831 — pan-stepping perception):** Marker-second rollup has uOffsetX = 187.80 (post-wrap small value), pixelOrigin frozen. The PREVIOUS second (1777845830) shows `uOffsetX` sweeping `[19.23, 4091.14]` AND `uOffsetY` sweeping `[4.57, 4054.99]` — both axes wrapped. **M6 fires immediately after a multi-axis meter-space wrap event.** Same mechanism.

**Marker #7 (epochSec 1777845833 — pan-stepping perception):** Marker-second rollup has uOffsetX = 3086.02, uOffsetY = 1356.46. The PREVIOUS second (1777845832) shows `uOffsetY` sweeping `[11.42, 4039.03]` — Y-axis wrap. **M7 fires immediately after a Y-axis meter-space wrap event.**

#### Marker-correlation conclusion

**ALL 7 markers correlate with meter-space wrap events** (within ±1-second window). Plan 03.1-14's prediction was *the constant-magnitude phase shift bounded ≈ 11% of fbm3 dynamic range INVARIANT across all wrap events makes the residual perceptually invisible*. Walk #6 EMPIRICALLY FALSIFIES this prediction: the developer perceives stepping at meter-space wrap events at multiple zoom × lat combinations (M3 at z≈14 mpp ≈18, M4 at z≈10 mpp ≈26.6, M5 at z≈10 mpp ≈26.64, M6+M7 same z≈10 regime). The constant-magnitude property landed correctly in software (the JSONL signature confirms uOffset bounded under 4097 m), but the perceptual threshold prediction (≈11% fbm3 dynamic-range residual is sub-perceptual) is wrong at the developer's specific worldMeters positions and zoom regimes encountered during Walk #6.

This is precisely the failure mode Plan 03.1-14's `if_walk_6_surfaces_residual_stepping` contingency anticipates. Plan 03.1-14 PLAN.md `<continuity_proof_for_plan_03_1_14>` lines 444-518 documented option D-3 (Worley periodic noise primitive — period exactly aligned with `kPocFogNoiseTilePxMeters`, so wraps inject ZERO discontinuity, not constant-magnitude discontinuity) as the architectural fall-back if the analytical 11% bound proved perceptible. **Walk #6 activates this contingency.**

### Q1 NEW FAILURE MODE — "translates smoothly while panning instead of being attached to the map" (qualitatively distinct from Walk #5's "steppy moments at wrap events")

The developer's verbatim Q1 observation: *"panning still worse than the perfect result we had further commits away (I think we wrote somewhere which commit it was), still steppy and now translating smoothly while panning instead of being attached to the map as it should"*.

**Two qualitatively distinct components:**

1. **Persistent stepping (carry-over from Walk #5).** 7 dev-markers fired, all correlated with meter-space wrap events. The Plan 03.1-14 Fix B′ moved the wrap discontinuity from a non-integer-multiple shift (Walk #5 0.74-cell) to an integer-cell shift with constant-magnitude phase shift (Walk #6 ≈11% fbm3 dynamic range), but the residual constant-magnitude shift IS perceptually discernible to this developer at this gesture pattern. Mechanism documented above.

2. **NEW failure mode — fog appears to "translate smoothly with the viewport" instead of being attached to the map.** This is the developer's qualitative differentiation from Walk #5's *"steppy moment"* characterisation. Walk #4 (Plan 03.1-11, the *"perfect"* baseline) used the FOG-17 + FOG-17a pixel-space formulation `worldPx = fragUv * uResolution + uPixelOrigin; noiseUv = worldPx / kNoiseTilePx`. Walk #5 (Plan 03.1-12) added the FOG-18 meter-space anchor `worldMeters = (fragUv * uResolution + uPixelOrigin) * uMetersPerPixel; noiseUv = worldMeters / kNoiseTilePxMeters`. Walk #6 (Plan 03.1-14) reorders the formula to `(fragUv * uResolution) * uMetersPerPixel + uWorldMetersOrigin` where `uWorldMetersOrigin = bounded_meters` (Dart-decomposed under 4097 m).

The semantic difference matters: at Walk #4's *"perfect"* state, the shader computed `worldPx` as a continuous large-magnitude value across paints (within the FOG-17a wrap window of 1536 raw px). At Walk #6, the shader receives `uWorldMetersOrigin` as a small bounded composite (under 4097 m) that resets on every wrap event. **The `bounded_meters` value is INVARIANT across wraps under FOG-19 (Plan 03.1-14 Task 2 augmentation `coordinateSpace: 'meters'` confirms this), but it is NOT a continuous monotonic function of camera position over time.** It is a sawtooth: it grows linearly with camera pan UNTIL the wrap event, then resets. From the developer's perspective on screen, this looks like fog *"translating smoothly with the viewport"* between wrap events — because the camera-anchor term in the shader IS doing exactly that — followed by a discontinuity at each wrap event (the markers).

**Hypothesis for the NEW failure mode:** The developer's perception is anchored to between-wrap behaviour. Walk #4's smooth `worldPx` sampling produced fog where new noise entered from the viewport edges as the camera panned (the *"perfect"* state). Walk #6's `bounded_meters` is fully equivalent ARCHITECTURALLY (the `(fragUv * uResolution) * mpp` term in the new formula does the same edge-noise-entry function as Walk #4's `fragUv * uResolution + uPixelOrigin` term in the old formula), BUT the visual presentation of stepping at every meter-space wrap event RECOLOURS the developer's perception of the between-wrap regime: instead of *"fog smoothly tracking the map"*, they perceive *"fog smoothly translating, then snapping every few pans"*. The smooth-translation-during-pan IS correct per the math, but the contrast with the wrap snap creates the *"detached from the map"* gestalt.

**This NEW failure mode is INTERPRETIVE, not architectural.** The math is correct under FOG-19; the JSONL signatures all hold. What changed between Walk #4 (perfect) and Walk #6 (denied with new failure mode) is:
- Walk #4: pixel-space sampling, fp32-precision-bounded, no architectural wrap mechanism (FOG-17 produced fract-free smooth coordinate evolution within the integer-wrap window of 1536 raw px ≈ ~128 sec at 12 raw-px/s pan velocity);
- Walk #6: meter-space sampling, integer-cell-shift wrap mechanism, ≈11% fbm3 dynamic-range residual phase shift INVARIANT per wrap.

The wrap mechanism is the empirical perception trigger. Removing it (option D-3 Worley) restores Walk #4's smooth-pan experience PLUS Walk #5's zoom-axis closure (FOG-18 meter-space anchor retained).

### Q5 PARTIAL IMPROVEMENT — "less worse, way less steppyness but still some"

The developer's verbatim Q5 observation: *"zoom is less worse, way less steppyness but still some"*. **Q5 PARTIALLY IMPROVED from Walk #5's *"zooming still moves the mirl all around giving a noisy feeling"* to Walk #6's *"way less steppyness but still some"*.**

Mechanism: Plan 03.1-14 Fix B′ DELIVERS the architectural Q5 closure Walk #5 missed — `bounded_meters = (intMeters % 4096) + fracMeters` is geographic, not pixel-wrap-index, so at fixed map position the noise sample IS zoom-invariant within an integer-cell-shift tolerance. Cells visibly enlarge with zoom (the shader formula `(fragUv * uResolution) * uMetersPerPixel + uWorldMetersOrigin` produces correct meter-space cell scaling). Walk #5's *"scramble"* perception at zoom transitions has been substantially attenuated.

The residual stepping at zoom (markers M1-M3) corresponds to the same constant-magnitude phase-shift mechanism as Q1's pan stepping — every meter-space wrap event during a zoom transition produces a discernible discontinuity. At z=15 → z=10 zoom-out (mpp goes 3.16 → 26.66), the camera's worldMeters cycles through multiple wrap windows fast (a single second of zoom-out covers 12+ wraps per axis, per the multi-axis full-range sweeps in `epochSec 1777845817` and `1777845830`).

**Q5 closure is now WAITING ON the same fix that closes Q1 — option D-3 Worley** (zero-discontinuity wrap mechanism, replacing the integer-cell-shift mechanism). The architectural noise-anchor question is fully closed; the remaining residual is the per-wrap fbm3 amplitude discontinuity that's NOT sub-perceptual.

### Q2 — Rotation-disabled (PASSED ✓ implicit)

No developer complaint surfaced about rotation; UX-02 + FOG-16 path (a) closure walk-time-validated implicitly at Walk #6 (developer's verdict mentions only Q1 and Q5; rotation-disable holds). Walk #4-5's *"rotation disabled confirmed"* is RETAINED.

### Q3 — Reveal-hole-aligned (PASSED ✓ implicit)

No developer complaint surfaced about reveal-hole-vs-blue-dot misalignment. FOG-12 walk-time validation from Walk #4-5 (developer's *"revealed area working"*) is RETAINED. The fog_transform JSONL signature confirms `canvasTx/Ty == 0.000000` across all 51 rollups (FOG-13 mechanism mechanically zeroes the canvas-frame offset).

### Q4 — Fog translates with camera during pan (PASSED architecturally; SUBSUMED into Q1's NEW failure mode)

The fog DOES translate with the camera (the `(fragUv * uResolution) * mpp + uWorldMetersOrigin` formula tracks gestures correctly per the JSONL pixelOriginX/Y monotonic evolution + uWorldMetersOrigin sawtooth). However, the developer's NEW Q1 framing — fog *"translating smoothly while panning instead of being attached to the map"* — re-frames Q4's PASS as a perceptual anti-pattern. Architecturally Q4 holds; perceptually Q4's smooth-translation gets re-cast as "detached from map" because of the wrap-stepping context. Q4 stays PASSED in the technical/architectural sense; the perceptual issue is captured in Q1 NEW failure mode.

### FPS observations (PERF-03)

Per the frame_delta envelope above (sub-1-ms median, sub-7-ms p95, sub-10-ms max with sampleCount 97-121 per rollup), the device sustains ~120 fps during gestures. **PERF-03 implicitly passes.** The Q1 stepping + Q5 residual stepping are NOT frame-budget-related; they are noise-output-discontinuity architectural failures at the per-wrap fbm3 amplitude axis.

## Causal Analysis — Why Plan 03.1-14 Fix B′ Did Not Close Q1; New Failure Mode Hypothesis

### What the Plan 03.1-14 SUMMARY predicted

From `03.1-14-SUMMARY.md` lines 81 + 159-164:
> "the architectural property is INVARIANCE across all wrap events (NOT mathematical invisibility); see Plan 03.1-14 `continuity_proof_for_plan_03_1_14`"
> "The CRITICAL property is constant-magnitude INVARIANCE across wraps (eliminating the pre-fix Walk #5 variable-magnitude stepping signal), NOT mathematical invisibility. Walk #6 PRIMARY GATE empirically validates the perceptual threshold — if developer fires `steppy_translation` markers despite Plan 03.1-14 Fix B′, the architectural fall-back path is option D-3 (Worley periodic noise primitive — Plan 03.1-16+ scope)."

The Plan 03.1-14 author EXPLICITLY HEDGED the prediction: the analytical bound (`kPocFogFbmDiscontinuityBound = 0.20` ≈ 11% fbm3 dynamic range) was constant-magnitude, deterministic, zoom-independent, lat-independent, but the SUMMARY made no claim that 11% was sub-perceptual. The hedge is now activated.

### What Walk #6 confirmed empirically

| Plan 03.1-14 prediction | Walk #6 empirical verdict |
|--------------------------|---------------------------|
| `coordinateSpace: 'meters'` field present in JSONL | **VERIFIED ✓** (51/51 rollups) |
| `metersPerPixel` field present + non-zero | **VERIFIED ✓** (51/51 rollups; envelope `[3.164, 26.660]`) |
| `appliedUOffsetX/Y` bounded under 4097 m across all rollups | **VERIFIED ✓** (51/51 rollups; max observed 4095.08 just under 4096) |
| Period-commensurability invariant `4096 % 1024 == 0` GREEN at runtime | **VERIFIED ✓** (no out-of-bound JSONL values) |
| Wrap injects exactly 4 integer cells in noiseUv | **VERIFIED INDIRECTLY ✓** (the JSONL bounded composite jumps by approximately 4096 m at wrap events; the Dart-side decomposition formula is unit-tested at `fog_meter_space_decomposition_test`) |
| Octave 1 bit-identical at wrap (hash3 period-1) | **NOT WALK-OBSERVABLE** (single-octave noise is not the production rendering; production runs full 3-octave fbm3) |
| Octaves 2 + 3 receive ≈11% fbm3 dynamic-range CONSTANT-MAGNITUDE phase shift | **VERIFIED INDIRECTLY ✓** (no variable-magnitude stepping was reported by developer; the markers fire at a UNIFORM cadence of ~2-3 sec apart, consistent with constant-magnitude wrap events; not the Walk #5 variable-magnitude clustering signature) |
| **The constant-magnitude residual is sub-perceptual** | **FALSIFIED ✗** (7 markers fired at meter-space wrap events; markers correlate with wrap-event timestamps in fog_transform JSONL within ±1 sec) |

### Comparison with Walk #4's *"perfect"* baseline

Walk #4 (Plan 03.1-11, commit chain `dbc3e21..b31766f`) was the only Phase 3.1 walk to produce *"this works for panning : perfect"*. Walk #4 used:
- FOG-17 (world-coordinate noise) + FOG-17a (pixel-space integer/fractional decomposition; `kPocFogIntegerWrapPeriodPx = 1536 raw px`).
- Pixel-space anchor (cells were screen-pinned at all zoom levels — Walk #4's Q5 NEW failure surfaced this as a separate architectural axis).

Walk #4's pan was *"perfect"* because:
1. The `kPocFogIntegerWrapPeriodPx = 1536 raw px` wrap window was LARGE in meter-space at the developer's typical z=13-15 regime: at z=15 mpp = 3.16 → 1536 × 3.16 = 4855 m of ground per wrap. At Walk #3b's measured 12 raw-px/s pan velocity, that's `1536 / 12 = 128 seconds per wrap`. Sub-perceptual at typical walk velocities BECAUSE the wrap rarely fired during the walk.
2. The pixel-space FOG-17a decomposition + pixel-space FOG-17 sampling produced bit-identical noise across a single zoom level (no zoom transitions in Walk #4's primary pan window).

Walk #5 (Plan 03.1-12) added meter-space anchoring (FOG-18) on top of pixel-space FOG-17a, breaking the period commensurability — wrap shifted noise by 0.74 cells (non-integer-multiple). Walk #6 (Plan 03.1-14) restored period commensurability via meter-space FOG-17a (Fix B′) — wrap now shifts noise by 4 integer cells with ≈11% fbm3 dynamic-range residual.

**The architectural progression closed pan-axis (Walks #1-3 → #4) and zoom-axis (Walk #4 → #5 → #6) successively.** What's left is the per-wrap fbm3 amplitude discontinuity that the integer-cell-shift mechanism cannot eliminate (it's a property of the fbm3 octave summation, not the wrap discretisation). **Walk #4's *"perfect"* state was a happy accident of large-pixel-window + low-pan-velocity making wraps rare; Walk #6's denial is the empirical surfacing of what would have happened at Walk #4 had the developer panned for 128+ seconds in one direction.**

### Hypothesised mechanism for the NEW "translating smoothly with viewport" failure mode

The developer's NEW framing — *"translating smoothly while panning instead of being attached to the map"* — is qualitatively distinct from Walk #5's *"steppy moments"*. The hypothesised mechanism (HIGH confidence; cannot verify on-device without Plan 03.1-16 instrumentation):

The Plan 03.1-14 shader formula `(fragUv * uResolution) * uMetersPerPixel + uWorldMetersOrigin` decomposes the camera-anchor into a viewport-relative term (`fragUv * uResolution * mpp`) and a camera-position term (`uWorldMetersOrigin`). When `uWorldMetersOrigin = bounded_meters` resets at every wrap event, the visible noise pattern *shifts by 4 integer cells in noiseUv* relative to the previous frame. Between wrap events, `uWorldMetersOrigin` grows linearly with camera pan, and the noise pattern translates smoothly (correctly anchored to ground).

The developer's perceptual gestalt: they see SMOOTH translation (between wraps) PUNCTUATED BY DISCRETE 4-cell shifts (at wraps). The smooth-translation phases LOOK LIKE "fog moving with viewport" because the noise pattern in screen-space coordinates IS shifting smoothly between wraps (the smoothness is correct anchoring; the smoothness is what *"perfect"* looked like at Walk #4). The 4-cell-shift events at wraps create the gestalt "and then it snaps". The combined perception is *"smoothly translating then snapping"* — re-cast by the developer as *"translating smoothly with the viewport instead of being attached to the map"*.

**This is the same underlying architectural failure as Q1 stepping — just from a different perceptual angle.** Closing the per-wrap discontinuity (option D-3 Worley) closes BOTH the *"steppy"* and *"translating with viewport"* gestalt characterisations.

## Iteration Recommendation: OPEN PLAN 03.1-16 WITH FIX D-3 WORLEY (NOT DENIED-FINAL; NOT PHASE 3.2 PIVOT)

Per CONTEXT §Iteration policy (no hard cap on walk count), the verdict is **ITERATING-WITH-Q1-NEW-FAILURE-MODE-AND-Q5-PARTIAL — see Plan 03.1-16+**, NOT `DENIED-FINAL`.

### Rationale for ITERATING (not DENIED-FINAL)

Three conditions for `DENIED-FINAL` (per the Walk #6 falsification criteria § Criterion D):
1. The diagnostic stream shows the layered fix landed mechanically AT EVERY level — **YES, all signatures present** (coordinateSpace=meters, metersPerPixel populated, uOffset bounded under 4097, canvasTx/Ty zeroed).
2. The user-observable Q1/Q5 STILL persist after the architectural fix — **YES, Q1 has a NEW failure mode and Q5 has residual stepping**.
3. **The root cause is even deeper than meter-space FOG-17a** — **NO**. The root cause is now diagnosed at the per-wrap fbm3 amplitude discontinuity axis. Plan 03.1-14's continuity proof correctly bounded this at 11% but predicted sub-perceptibility. Walk #6 falsifies that prediction. **The fall-back path D-3 (Worley periodic noise) eliminates the discontinuity entirely** — wraps inject ZERO discontinuity (the noise primitive's period is exactly aligned with `kPocFogNoiseTilePxMeters`). Plan 03.1-14 PLAN.md `<continuity_proof_for_plan_03_1_14>` `if_walk_6_surfaces_residual_stepping` documented this contingency.

The architectural failure is NOT at the renderer-architecture level (`flutter_map 7.0.2` shader stack works correctly per all JSONL signatures); it's at the noise-primitive selection level (gradient-noise FBM has a per-octave-matrix-amplification mechanism that cannot reach mathematical invisibility under integer-cell shifts; periodic Worley/explicit-period-Perlin can).

### Rationale for NOT Phase 3.2 alternative-renderer pivot

Phase 3.2 alternatives (mapsforge_flutter, custom MVT-on-Canvas, flutter_gpu) would address renderer-architecture problems. Walk #6's diagnostic confirms the renderer architecture works correctly: same-Canvas paint path is fully functional, all uniforms are populated, the camera-tracking pipeline is sound. The remaining problem is *which noise function the shader uses*, which is a 1-shader-file change — orders of magnitude smaller than a renderer pivot.

### Recommended Plan 03.1-16 fix axis: Option D-3 Worley periodic noise primitive

From Plan 03.1-14 PLAN.md `<continuity_proof_for_plan_03_1_14>` `if_walk_6_surfaces_residual_stepping` block + Plan 03.1-13 FALSIFICATION-5 row D-3:

**Replace the gradient-noise FBM in `assets/shaders/atmospheric_fog.frag` (lines 152-207) with a periodic Worley/explicit-period-Perlin function with period exactly `kPocFogNoiseTilePxMeters = 1024 m`.**

Acceptance criteria for Plan 03.1-16:
1. `assets/shaders/atmospheric_fog.frag` `noise3` + `fbm3` functions replaced with a periodic primitive whose period is exactly `kPocFogNoiseTilePxMeters` in meter-space.
2. `assets/shaders/atmospheric_fog_debug_spiral.frag` symmetrically updated (debug-spiral observation must show ZERO discontinuity at wrap events).
3. NEW behavioural test `fog_periodic_noise_continuity_test.dart` asserts ZERO `noiseUv` discontinuity across multiple wrap events spanning ≥ 5 full wrap periods (vs Plan 03.1-14's `fog_meter_wrap_invisibility_test` which asserted bounded discontinuity).
4. CI gate test on synthetic smooth-pan trajectory at z=10 lat 0° (pathological case, mpp ≈ 156543) AND z=15 lat 48.5° (typical case, mpp ≈ 3.16) confirms no per-fragment noise discontinuity at any wrap event.
5. Aesthetic equivalence: visual A/B comparison between FBM and Worley aesthetics on `/sanity` to confirm the developer accepts the visual character change. RESEARCH.md `kMirkFog*` aesthetic constants may need re-tuning.

Plan 03.1-16 fix axis is HEAVIER surgery than Plan 03.1-14 (Plan 03.1-14 was a coordinate-space flip; Plan 03.1-16 is a noise-primitive replacement) but is well-scoped — single shader file change + symmetric debug-spiral mirror + 1 new test. Estimated effort: ~3 hours.

### Alternative: Option D-2 zoom-dependent FOG-17a wrap period (defer)

From Plan 03.1-14 PLAN.md row D-2: make `kPocFogIntegerWrapPeriodMeters` a runtime-computed value at the painter level. This trades the constant-magnitude per-wrap discontinuity for a zero-magnitude cosmetic-cell-perfect-alignment, but introduces zoom-step-correlated discontinuities (the bounded composite forwarded to shader changes its semantics across zoom transitions). Defer per Plan 03.1-14's recommendation — D-3 is architecturally cleaner.

### Alternative: ACCEPT Walk #4 Q1 perfection + accept current Q5 residual + revert Plan 03.1-14 (defer)

A pragmatic alternative: revert Plan 03.1-14, accepting Walk #4's *"perfect for panning"* + Walk #4's known Q5 zoom-scramble. Trade: Q1 perfect at typical pan velocity (the wrap fires only every ~128 seconds at 12 raw-px/s) + Q5 known-failure (the 5th iteration's Walk #4 Q5 NEW failure stays open). Defer this option — Plan 03.1-16 D-3 closes BOTH Q1 + Q5 simultaneously and is mechanically obvious.

## Verdict

- [x] Criterion A passed? — **YES.** Frame-delta probe rolling rollup over 51 1-Hz windows: median-of-medians 0.311 ms (≤ 16 ✓; 51× headroom), max-of-p95s 6.017 ms (≤ 32 ✓; 5.3× headroom), max-of-maxes 9.233 ms (≤ 48 ✓; 5.2× headroom). PERF-07 re-validated post-Plan-03.1-14; envelope statistically equivalent to or better than Walks #1+2+5.
- [x] Criterion B passed? — **PARTIAL.** Q2 + Q3 retained from Walks #4+5; **Q1 DENIED with NEW failure mode** (`translating smoothly while panning instead of being attached to the map` + 7 dev-markers fired correlating with meter-space wrap events; 3.5× Walk #5's marker count); **Q5 PARTIALLY IMPROVED** (`way less steppyness but still some` — meter-space anchor delivers the architectural Q5 closure but per-wrap fbm3 discontinuity persists at zoom transitions); Q4 architecturally retained but re-cast by Q1's NEW perceptual framing.
- [x] Criterion D passed? — **YES — DIAGNOSTIC COMPLETE.** All Plan 03.1-14 architectural signatures EMPIRICALLY VERIFIED in JSONL:
  - **`coordinateSpace: 'meters'` field present in 51/51 fog_transform rollups** (Plan 03.1-14 Task 2 augmentation EMPIRICALLY VERIFIED).
  - **`metersPerPixel` field present + non-zero in 51/51 rollups** (Plan 03.1-13 augmentation NOW EMPIRICAL — build-provenance gate held; envelope `[3.164, 26.660]` consistent with z=10..15 walk regimes).
  - **`appliedUOffsetX/Y` bounded under 4097 m across 51/51 rollups** (FOG-19 invariant HOLDS empirically; 7 dev-markers correlate with full-range meter-space sweeps within ±1 sec window).
  - **`canvasTx/Ty == 0.000000` across 51/51 rollups** (FOG-13 retained).
  - **STRUCTURAL build-provenance gate PASSED** (HEAD == CI headSha == `15e98b3`).
  - **STRUCTURALLY-ENFORCED Mail-share gate held second time** (after Walk #5 first time; pattern fully ratified).

**Outcome:** **ITERATING-WITH-Q1-NEW-FAILURE-MODE-AND-Q5-PARTIAL — see Plan 03.1-16+ for the next fix attempt (option D-3 Worley periodic noise primitive — Plan 03.1-14 contingency activates).**

The Plan 03.1-14 software fix bundle (Fix B′ meter-space FOG-17a decomposition + FOG-19; shader uniform slot 3..4 rename; period-commensurability invariant `kPocFogIntegerWrapPeriodMeters % kPocFogNoiseTilePxMeters == 0` GREEN; analytical bound `kPocFogFbmDiscontinuityBound = 0.20`) successfully landed the architectural completion endorsed at Walk #5 close-out. **Every JSONL signature confirms the meter-space decomposition + period-commensurability invariant operates correctly in the Walk #6 IPA.** But Plan 03.1-14's predicted sub-perceptibility of the residual ≈11% fbm3 dynamic-range constant-magnitude phase shift is FALSIFIED empirically: the developer perceives stepping at meter-space wrap events at multiple zoom × lat combinations (markers M1-M7 all correlate with wrap events in the JSONL). The architectural property is INVARIANCE (constant-magnitude across wraps), as predicted; the perceptual property of sub-perceptibility is wrong at the developer's specific worldMeters positions.

This is the **seventh Phase 3.1 verdict** (Walk #1: ITERATING-WITH-MAJOR-PROGRESS; Walk #2: ITERATING-WITH-PARTIAL-PROGRESS; Walk #3: ITERATING-WITH-PARTIAL-PROGRESS-AND-ROTATION-REGRESSION; Walk #4: ITERATING-WITH-Q1-Q4-CONFIRMED-Q5-ZOOM-SCRAMBLE-OPEN; Walk #5: ITERATING-WITH-Q1-REGRESSED-AND-Q5-NOT-FIXED-FROM-PERIOD-COMMENSURABILITY-GAP; Walk #6: **ITERATING-WITH-Q1-NEW-FAILURE-MODE-AND-Q5-PARTIAL**). Phase 3.1 does NOT close. Phase 4 (wisp particles) and Phase 5 (decision gate) STAY blocked.

**KEY LEARNING — NOISE-PRIMITIVE SELECTION AS THE FINAL ARCHITECTURAL AXIS:**
- Walk #1: noise didn't move with camera at all (pixelOrigin not forwarded to shader) — closed by Plan 03.1-02.
- Walk #2: noise moved with camera but had viewport-period shimmer (`fract(uPixelOrigin / uResolution)` wrap discontinuity) — closed by Plan 03.1-04.
- Walk #3: noise moved with camera but had noise-tile-period stepping (`fract(uPixelOrigin / tilePeriodPixels)` reduced wrap frequency, didn't eliminate it) + new rotation regression — closed by Plan 03.1-10 (FOG-17 + FOG-17a + UX-02).
- Walk #4: pan handled correctly (FOG-17 + FOG-17a eliminate fract wraps within the integer-wrap window) BUT zoom surfaces a deeper architectural gap: noise is pixel-space-anchored, not meter-space-anchored — closed by Plan 03.1-12 (FOG-18 world-meter anchor).
- Walk #5: zoom architecture corrected (FOG-18 world-meter anchor delivers zoom-invariant noise position in meter space + cells scale on screen with zoom) BUT a previously-deferred sub-architecture surfaces: FOG-17a decomposition lives in pixel-space while FOG-18 noise tile lives in meter-space, the two are non-commensurate at every zoom level, wraps produce sub-cell discontinuities — closed by Plan 03.1-14 (Fix B′ meter-space FOG-17a decomposition + FOG-19).
- **Walk #6: coordinate-space-alignment-of-decomposition closed (FOG-19 invariant `bounded < 4097` HOLDS in 51/51 rollups; period-commensurability invariant GREEN), BUT the noise-primitive selection axis surfaces:** gradient-noise FBM has per-octave-matrix-amplification (`M_1 = 2.03·I`, `M_2 = 2.05·I` applied to the integer-cell shift `V = (-4, 0, 0)`) producing ≈11% fbm3 dynamic-range constant-magnitude residual at every wrap event. The constant-magnitude property is correct (invariance); the perceptual threshold prediction (sub-perceptual) is wrong. Plan 03.1-16 D-3 (Worley periodic noise primitive — period exactly aligned with `kPocFogNoiseTilePxMeters`, no per-octave-matrix amplification, ZERO per-wrap discontinuity) closes this final architectural axis.

After Plan 03.1-16 lands, Phase 3.1 has no remaining architectural axes (the renderer architecture is sound, the coordinate system is meter-space-aligned, the decomposition is meter-space-aligned, the noise primitive is periodic). The only remaining open question would be aesthetic equivalence: does the Worley-based fog look enough like the FBM-based fog that the RESEARCH.md `kMirkFog*` constants port back cleanly to MirkFall? (likely yes — Worley + Worley-FBM aesthetics are well-established alternatives in the procedural-noise literature; aesthetic re-tune scope is hours, not days.)

**MirkFall migration recommendation:** **DEFERRED.** Plan 03.1-04 + 03.1-05 + 03.1-07 + 03.1-08 + 03.1-10 + 03.1-12 + 03.1-14 are necessary but not sufficient; Plan 03.1-16 D-3 Worley periodic noise primitive must land before any port-back conversation is entered. Re-evaluate at Walk #7 verdict.

**WALK #6 STRUCTURAL ACHIEVEMENTS:**

1. **STRUCTURAL build-provenance discipline pattern established and validated.** Walk #5 surfaced a build-provenance gap (Plan 03.1-13 commit `2b087a3` post-dated CI run `25285037397`). Walk #6's STRUCTURAL build-provenance gate (Task 1 `git rev-parse HEAD == gh run view <RUN_ID> --json headSha`) succeeded on first attempt. Pattern fully ratified — eliminate the build-provenance gap class of failure structurally.

2. **STRUCTURALLY-ENFORCED Mail-share discipline pattern fully ratified.** Walks #3 + #4 broke Mail-share twice; Walk #5 introduced the structural gate (resume signal requires log path) and held first time; Walk #6 held second time. Recurring discipline-break pattern resolved structurally.

3. **Plan 03.1-14 architectural signatures EMPIRICALLY VERIFIED in production IPA.** All four direct visible signatures (coordinateSpace=meters, metersPerPixel populated, uOffset bounded under 4097, canvasTx/Ty zeroed) hold across 51/51 fog_transform rollups. The Plan 03.1-14 software fix bundle landed correctly at the architectural level; the diagnosis points at the noise-primitive selection axis as the next (and likely final) architectural step.

4. **Diagnosis is complete for Plan 03.1-16 authoring.** The 7-marker correlation against meter-space wrap events anchors the Plan 03.1-14 perceptual-threshold falsification empirically. Plan 03.1-16 has a single fix axis (option D-3 Worley periodic noise primitive) with explicit acceptance criteria.

**The next action is `/gsd:plan-phase 3.1 --gaps` to author Plan 03.1-16** with: (a) primary fix axis option D-3 Worley periodic noise primitive (replace gradient-noise FBM in atmospheric_fog.frag + atmospheric_fog_debug_spiral.frag with a periodic primitive whose period is exactly `kPocFogNoiseTilePxMeters`); (b) NEW v1 requirement candidate **FOG-20** (noise primitive period exactly aligned with noise tile period; meta-requirement preventing recurrence of the per-wrap-discontinuity class of failure); (c) NEW behavioural test `fog_periodic_noise_continuity_test` asserting ZERO `noiseUv` discontinuity across multiple wrap events; (d) aesthetic A/B comparison checkpoint at `/sanity` before walk-time validation.

**MirkFall migration recommendation:** DEFERRED until Walk #7 (post-Plan-03.1-16) verdict.
