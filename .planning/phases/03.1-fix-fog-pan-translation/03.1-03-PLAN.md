---
phase: 03.1-fix-fog-pan-translation
plan: 03
type: execute
wave: 3
depends_on: ["03.1-02"]
files_modified:
  - .planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION.md
  - .planning/phases/03.1-fix-fog-pan-translation/03.1-UAT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [PERF-07]
user_setup:
  - service: github-actions
    why: "Wait for CI green on the post-fix SHA + download the IPA artifact for sideload"
    dashboard_config:
      - task: "Confirm CI run status (use `gh run list --branch main --limit 5` + `gh run watch` until green)"
        location: "GitHub Actions for the mirk-poc-debug repo"
      - task: "Download IPA artifact via `gh run download <RUN_ID> --name ios-unsigned --dir .uat-tmp/`"
        location: "GitHub Actions run detail page (or `gh` CLI)"
  - service: sidestore
    why: "Sideload the unsigned IPA onto iPhone 17 Pro for the walk"
    dashboard_config:
      - task: "Open SideStore on iPhone 17 Pro → install/refresh the .uat-tmp/mirk-poc-debug-unsigned.ipa"
        location: "SideStore iOS app + paired desktop pairing-file"
must_haves:
  truths:
    - "A new `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION.md` exists with the same structural shape as `03-FALSIFICATION.md`: Hypothesis, Falsification Criteria (A + B), Walk Plan, Walk Evidence (filled post-walk), Verdict (filled post-walk). Phase 3.1's hypothesis re-statement is `the fog renders, rotates, AND translates with the camera during pan after the Phase 3.1 fix`."
    - "The pre-walk gate sequence runs cleanly: `flutter test` GREEN; `flutter analyze --fatal-infos` 0 warnings; `dart format --line-length 160 --set-exit-if-changed .` clean; `dart test tool/test/` GREEN; CI green on the post-fix SHA on `main`; IPA downloaded via `gh run download` to `.uat-tmp/mirk-poc-debug-unsigned.ipa`."
    - "The iPhone 17 Pro UAT walk is performed by the user. Walk shape: ≥ 5 minutes through Melun + ≥ 10 deliberate combined pinch-zoom-and-pan gestures + ≥ 3 recenter taps + post-walk Mail-share of the session log."
    - "Post-walk: the developer's verbal verdict (`approved` / describe-failure-mode) is captured. If `approved`, the walk evidence section of `03.1-FALSIFICATION.md` is filled with the developer's verbatim words + interpretation + post-walk grep of `infrastructure.mirk.fog_transform` rollups + correlation against `infrastructure.mirk.frame_delta` + `infrastructure.mirk.sdf` rollups on per-second `epochSecond` boundaries."
    - "PERF-07 measurement is recorded: frame-delta probe rolling rollup over the ≥ 10-combined-gesture window with `medianMs ≤ 16`, `p95Ms ≤ 32`, `maxMs ≤ 48`. The Plan 03-08 walk left these unmeasured-and-moot; the Phase 3.1 walk produces them as the proper PERF-04 numbers carried over."
    - "Verdict written: `CONFIRMED-AFTER-FIX` if BOTH (a) developer's subjective verdict is `approved` AND (b) the PERF-07 frame-delta thresholds are satisfied. `DENIED-FINAL` if the developer's verdict describes a failure mode that the diagnostic JSONL stream cannot resolve via a follow-up 3.1 iteration. The iteration policy from CONTEXT (no hard cap on walk count, decide-after-diagnostic) means a first-walk failure may produce a follow-up plan rather than `DENIED-FINAL`; the verdict is only finalised when the developer explicitly chooses to terminate."
    - "On `CONFIRMED-AFTER-FIX`: ROADMAP.md Phase 03.1 row in the Progress table flips to `Complete (HYPOTHESIS CONFIRMED-AFTER-FIX)`. Phase 4 (`Wisp Particles`) status updates from `BLOCKED on Phase 3.1` to `Pending`. Phase 5 (`Decision Gate`) likewise. STATE.md `Current Position` updates to reflect Phase 3.1 closure. REQUIREMENTS.md FOG-04..07 status flips from `Falsified-in-production` to `Complete — Verified-by-walk (P03.1-03)`. PERF-07 status updates to `Complete — Measured (P03.1-03)` with the actual numbers."
    - "On `DENIED-FINAL`: ROADMAP.md Phase 03.1 row updates to `Complete (HYPOTHESIS DENIED-FINAL)`. Phases 4 + 5 stay `BLOCKED` (or transition to `Cancelled` per developer choice). REQUIREMENTS.md FOG-04..07 stay `Falsified-in-production` with an updated revision-clause that points at `03.1-FALSIFICATION.md`. STATE.md captures the formal POC architectural counter-evidence."
    - "Phase 3 `03-FALSIFICATION.md` (the historical `DENIED 2026-05-01` record) is LEFT UNTOUCHED — no edits, no overwrites. The historical record stands; the new verdict is a separate artefact at `03.1-FALSIFICATION.md`."
  artifacts:
    - path: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION.md"
      provides: "Phase 3.1 falsification document — pre-walk gate-pass evidence + post-walk verdict (`CONFIRMED-AFTER-FIX` | `DENIED-FINAL`)"
      min_lines: 80
      contains: "CONFIRMED-AFTER-FIX|DENIED-FINAL"
    - path: ".planning/phases/03.1-fix-fog-pan-translation/03.1-UAT.md"
      provides: "Walk evidence document — captures the developer's verbatim verdict + interpretation + JSONL grep correlation"
      min_lines: 30
      contains: "Walk Evidence"
  key_links:
    - from: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION.md"
      to: ".planning/phases/03-fog-of-war-the-hypothesis/03-FALSIFICATION.md"
      via: "explicit cross-reference in Phase 3.1's Hypothesis section + Verdict section ('Phase 3 closed DENIED 2026-05-01; Phase 3.1 either reverses to CONFIRMED-AFTER-FIX or strengthens to DENIED-FINAL')"
      pattern: "03-FALSIFICATION\\.md"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION.md"
      via: "FOG-04..07 + PERF-07 status clauses include `(P03.1-03)` cross-reference + the Revisions section appends a 2026-05-XX entry"
      pattern: "P03\\.1-03"
    - from: ".planning/STATE.md"
      to: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION.md"
      via: "Current Position + Decisions sections capture the verdict outcome"
      pattern: "Phase 3\\.1"
---

<objective>
Run the pre-walk gates, perform the iPhone 17 Pro sideload UAT walk, capture the developer's verdict, and produce `03.1-FALSIFICATION.md` — the Phase 3.1 falsification document. The walk verdict either reverses the Plan 03-08 `DENIED` outcome to `CONFIRMED-AFTER-FIX` (unblocking Phase 4 + Phase 5 of the original roadmap) or strengthens it to `DENIED-FINAL` (terminating the POC's same-Canvas hypothesis). The walk also produces the PERF-07 frame-delta probe quantitative evidence that Plan 03-08 left unmeasured-and-moot.

Purpose: this is the empirical answer Phase 3.1 was created to produce. The Plan 03.1-02 fix is software-complete + CI-gated; the walk is the architectural-evidence gate. Per CONTEXT §Iteration policy, there is no hard cap on walk count — a first-walk failure feeds the diagnostic JSONL stream back into a revised Plan 03.1-04 (or 03.1-05, etc.) and another walk follows. The `DENIED-FINAL` verdict is only written when the developer explicitly chooses termination based on diagnostic evidence.

Output: 1 new `03.1-FALSIFICATION.md` (the load-bearing artefact); 1 new `03.1-UAT.md` (the walk-evidence transcript); REQUIREMENTS.md status updates for FOG-04..07 + PERF-07; ROADMAP.md Phase 03.1 Progress-table row update; STATE.md Current Position + Decisions updates; Phase 3's `03-FALSIFICATION.md` left UNTOUCHED.
</objective>

<execution_context>
@C:/Users/oliver/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oliver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-CONTEXT.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-RESEARCH.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-VALIDATION.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-01-SUMMARY.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-02-SUMMARY.md
@.planning/phases/03-fog-of-war-the-hypothesis/03-FALSIFICATION.md
@.planning/phases/03-fog-of-war-the-hypothesis/03-UAT.md
@.planning/phases/03-fog-of-war-the-hypothesis/03-08-SUMMARY.md

<interfaces>
<!-- The two reference artefacts the new docs mirror. -->
<!-- Phase 3 set the structural template; Phase 3.1 follows it shape-for-shape. -->

`03-FALSIFICATION.md` structure (Phase 3 reference, lines 1-107):
1. Title + metadata (`Created`, `Phase`, `Walked on`, `Verdict`)
2. `## Hypothesis` (1 paragraph)
3. `## Falsification Criteria (written BEFORE the walk)` — `### Criterion A`, `### Criterion B`, `### Criterion C — DROPPED`
4. `## Walk Plan` — bullet list of where/duration/gestures/pre-walk-gate/in-app-HUD/post-walk
5. `## Walk Evidence (filled AFTER the walk)` — sub-sections for each evidence stream (`/sanity` gate, probe rollup, SDF rollup, FPS, subjective verdict)
6. `## Verdict` — checkbox list (Criterion A passed? Criterion B passed?) + `**Outcome:**` + `**MirkFall migration recommendation:**`

Phase 3.1's `03.1-FALSIFICATION.md` mirrors this exactly. Differences:
- The Hypothesis is re-stated for 3.1: `the fog translates with the camera during pan after the Phase 3.1 shader-uniform fix`.
- Criterion A is the SAME thresholds (median ≤ 16 ms / p95 ≤ 32 ms / max ≤ 48 ms — PERF-07 carries over the methodology) but is now MEASURABLE because the fix means the walk doesn't abort on visual grounds.
- Criterion B sub-claims are re-phrased for 3.1: "the fog visually translates with the camera during pan", "no white-ellipse during fast pinch-zoom", "no reveal-hole lag behind blue dot", "no inversion at any zoom".
- Criterion C stays DROPPED.
- New diagnostic stream: the `infrastructure.mirk.fog_transform` JSONL rollup (FOG-10) is added to the Walk Evidence sub-sections — the per-paint canvasTransform / pixelOrigin / appliedUOffset triple correlates against the existing `frame_delta` + `sdf` rollups on per-second `epochSecond` boundaries.

`03-UAT.md` structure (Phase 3 reference): the walk-evidence transcript — verbatim developer verdict, walk metadata (CI run ID, SHA, walk theatre, walk duration), grep extracts of the JSONL rollups. Phase 3.1's `03.1-UAT.md` mirrors this; the new section is `### Diagnostic JSONL — fog_transform rollup` capturing the per-second canvasTransform / pixelOrigin / uOffset evidence stream.

The pre-walk gate sequence is identical to Plan 03-08:
1. `flutter test` (full suite GREEN)
2. `flutter analyze --fatal-infos` (0 warnings)
3. `dart format --line-length 160 --set-exit-if-changed .` (clean)
4. `dart test tool/test/` (CI gate scripts GREEN — LOC-03, BOOT-02, etc.)
5. CI green on the post-fix SHA on `main` (`gh run list --branch main --limit 5` shows `success` for the latest run; `gh run view <RUN_ID>` confirms `gates`, `build-android`, `build-ios` all `success`)
6. IPA downloaded: `gh run download <RUN_ID> --name ios-unsigned --dir .uat-tmp/` produces `.uat-tmp/mirk-poc-debug-unsigned.ipa`

The walk shape is identical to Plan 03-08:
- ≥ 5 minutes continuous walk through central Melun (lat 48.5397, lon 2.6553 area)
- ≥ 10 deliberate combined pinch-zoom-and-pan gestures
- ≥ 3 recenter FAB taps
- HUD visible: FpsCounterOverlay (top:8, right:8), MapCompass (top:56, right:8), FrameDeltaProbeOverlay (top:104, right:8) — three colour-coded lines `med {N} ms / p95 {N} ms / max {N} ms`
- Post-walk: tap the AppBar share-logs button → pick Mail → send to self → save attachment → grep `infrastructure.mirk.fog_transform` + `infrastructure.mirk.frame_delta` + `infrastructure.mirk.sdf` lines for the walk timeframe (epochSecond range)

The post-walk grep correlation pattern (per CONTEXT §Implementation Decisions log-timeline-alignment):
- Three streams emit JSONL on `epochSecond` boundaries — `frame_delta`, `sdf`, `fog_transform`
- Pipe each stream through `jq` or grep+sort → align by `epochSecond` → produce a per-second composite row showing fog-paint frame-delta + SDF rebuild stats + fog-transform diagnostic stats together
- The composite row is the post-walk evidence the verdict is anchored on
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the pre-walk `03.1-FALSIFICATION.md` skeleton + run the pre-walk gate sequence</name>
  <files>.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION.md</files>
  <action>
    Create `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION.md`. Mirror the structural shape of `.planning/phases/03-fog-of-war-the-hypothesis/03-FALSIFICATION.md` exactly — same section headings, same metadata block at the top, same Falsification-Criteria-written-BEFORE-the-walk discipline. Phase 3.1 specialisations:

    1. Title + metadata block (top of the file):
       ```markdown
       # Phase 3.1 Falsification — Same-Canvas Fog Hypothesis (Post-Fix Re-Test)

       **Created:** 2026-05-XX  (executor: fill in real date)
       **Phase:** 03.1-fix-fog-pan-translation
       **Walked on:** TBD  (filled post-walk: iPhone 17 Pro, central Melun, CI run `<RUN_ID>`, SHA `<SHA>`)
       **Verdict:** TBD  (filled post-walk: `CONFIRMED-AFTER-FIX` | `DENIED-FINAL` | `ITERATING — see 03.1-04+`)

       ## Relationship to Phase 3 Verdict

       Phase 3 closed `DENIED 2026-05-01` (Plan 03-08 walk) with the static-fog-during-pan failure mode. The Phase 3 verdict (`.planning/phases/03-fog-of-war-the-hypothesis/03-FALSIFICATION.md`) stands as the historical record — UNTOUCHED. Phase 3.1 is the gap-closure investigation: research traced the bug to `_FogPainter.paint()` passing `offset: const (0.0, 0.0)` to the shader instead of deriving it from `camera.pixelOrigin`. The fix landed in Plan 03.1-02 (3-line code change at the painter call site). This document records the post-fix re-test verdict.
       ```

    2. `## Hypothesis` section:
       ```markdown
       ## Hypothesis

       After the Phase 3.1 fix in `_FogPainter.paint()` — derive `(uOffsetX, uOffsetY) = ((camera.pixelOrigin.x / size.width) % 1.0, (camera.pixelOrigin.y / size.height) % 1.0)` from the same `MapCamera` snapshot already captured in `FogLayer.build()` and forward to `shaderRenderer.render(offset: ...)` — the same-Canvas fog now translates with the camera during pan.

       Re-stated against the original POC core value: the fog-of-war stays perfectly locked to the map during pan, zoom, and combined gestures on a sideloaded iOS build. **Confirmed-after-fix** means the Phase 3 hypothesis is reinstated and the POC ports back to MirkFall with the Plan 03.1-02 fix included; **denied-final** means the same-Canvas architecture is unfixable in `flutter_map 7.0.2` and the POC produces the formal architectural counter-evidence (the Phase 3.2 alternative-renderer pivot becomes the only remaining option, and is itself a deferred-decision per CONTEXT §Fix-failure contingency).
       ```

    3. `## Falsification Criteria (written BEFORE the walk)` section. Mirror Phase 3's Criterion A + Criterion B; drop Criterion C the same way:

       ```markdown
       ## Falsification Criteria (written BEFORE the walk)

       ### Criterion A — Frame-delta probe thresholds (PERF-07, quantitative)

       The FOG-08 frame-delta probe rolling rollups across ≥ 10 deliberate combined pinch-zoom-and-pan gestures over a ≥ 5 minute walk on iPhone 17 Pro must satisfy:

       - **Median camera-to-fog-paint delta ≤ 16 ms** (1 frame at 60 Hz; 2 frames at 120 Hz)
       - **p95 ≤ 32 ms**
       - **max ≤ 48 ms**

       Persisted to the session log as 1-second JSONL rollups via `Logger('infrastructure.mirk.frame_delta')`. **NEW vs Plan 03-08:** the walk is no longer expected to abort on visual grounds, so Criterion A is genuinely measurable for the first time. PERF-07 captures the measurement.

       ### Criterion B — Subjective visual lock (PERF-05 carry-over, re-tested post-fix)

       Developer's verbal verdict at end of walk:

       - **Fog visually translates with the camera during pan** (the Plan 03-08 failure mode is reversed)
       - No fog slide-then-snap behind the map during pan
       - No white-ellipse artefact during fast pinch-zoom
       - No perceptible reveal-hole lag behind the blue dot
       - No inversion (fog appearing where reveal should be) at any zoom level

       ### Criterion C — DROPPED

       Stays dropped per Phase 3 + roadmap lock. Recorded for traceability — do not reinstate.

       ### NEW: Phase 3.1 diagnostic stream (FOG-10) — `infrastructure.mirk.fog_transform`

       The Plan 03.1-01 `FogTransformLogger` emits 1-Hz JSONL rollups capturing per-paint `(canvas.getTransform()[12..13], camera.pixelOrigin, camera.center, appliedUOffset)`. Post-walk grep-correlates this stream against `frame_delta` + `sdf` rollups on per-second `epochSecond` boundaries. Three correlation patterns expected:

       - **CONFIRMED-AFTER-FIX:** `pixelOriginX/Y` track the user's pan trajectory monotonically; `uOffsetX/Y` modulo-1.0 mirror that movement (small jumps as the modulo wraps); `canvasTx/Ty` flat at 0 (per RESEARCH §Pitfall D — Canvas at identity inside `MobileLayerTransformer` at rotation=0). The diff between `pixelOrigin` and `appliedUOffset * size` confirms the conversion math is correct in the binary that was just walked.
       - **DENIED-FINAL pattern #1:** `pixelOriginX/Y` track but `appliedUOffset` is `(0,0)` — fix did not land in the IPA (CI/sideload regression).
       - **DENIED-FINAL pattern #2:** `pixelOriginX/Y` track AND `appliedUOffset` tracks AND fog still appears static — root cause is downstream of the shader uniform (e.g. shader code precision-loss at very large pixelOrigin magnitudes; possibility #1's underlying mechanism is deeper than the shader-uniform-bypass research diagnosed). This pattern triggers Phase 3.2 conversation.
       ```

    4. `## Walk Plan` section. Mirror Phase 3's bullet list shape; specialise the pre-walk-gate enumeration to include the FOG-09 keystone test as a green-CI gate:
       ```markdown
       ## Walk Plan

       - **Where:** Central Melun (lat 48.5397, lon 2.6553 area; same theatre as Plan 03-08).
       - **Duration:** ≥ 5 minutes continuous walk with the IPA running and the device awake.
       - **Gestures:** ≥ 10 deliberate combined pinch-zoom-and-pan gestures, ≥ 3 recenter FAB taps.
       - **Pre-walk gate (unit tests):** `flutter test` GREEN (FOG-09 + FOG-10 + all Phase 1+2+3 retained); `flutter analyze --fatal-infos` 0 warnings; `dart format --line-length 160 --set-exit-if-changed .` clean; `dart test tool/test/` GREEN.
       - **Pre-walk gate (CI):** `gh run list --branch main --limit 5` shows `success` for the post-fix SHA; `gh run view <RUN_ID>` confirms `gates`, `build-android`, `build-ios` all green; IPA downloaded via `gh run download <RUN_ID> --name ios-unsigned --dir .uat-tmp/`.
       - **Pre-walk gate (sideload):** open `/sanity` route on the sideloaded build first; confirm the fog renders with a circular reveal hole visible (proves the SDF→shader path); confirm zero shader-compile exceptions in the FileLogger output.
       - **In-app HUD during walk:** FpsCounterOverlay (top:8, right:8) + MapCompass (top:56, right:8) + FrameDeltaProbeOverlay (top:104, right:8) — three lines: `med {N} ms / p95 {N} ms / max {N} ms`, colour-coded green/yellow/red against Criterion A.
       - **Post-walk:** share the session log file via Mail (LOG-04 round-trip); paste the relevant frame-delta + SDF + fog-transform JSONL lines into the "Walk Evidence" section below; write the subjective verdict (Criterion B) by hand; verbal `approved` is the gate (or describe-failure-mode for plan-revise iteration).
       ```

    5. `## Walk Evidence (filled AFTER the walk by Plan 03.1-03)` — placeholder sections for each evidence stream. Initially the body of each subsection reads `_TBD — filled post-walk._`. Sub-sections required:
       ```markdown
       ## Walk Evidence (filled AFTER the walk by Plan 03.1-03 Task 2)

       ### Pre-walk shader-sanity gate (`/sanity` route)
       _TBD — filled post-walk._

       ### Probe rollup (frame-delta JSONL — `Logger('infrastructure.mirk.frame_delta')`)
       _TBD — filled post-walk. Expected: ≥ 10-combined-gesture seconds of JSONL lines; per-second `medianMs ≤ 16`, `p95Ms ≤ 32`, `maxMs ≤ 48`._

       ### SDF rebuild rollup (`Logger('infrastructure.mirk.sdf')`)
       _TBD — filled post-walk. Expected: rebuilds correlated with new GPS fixes; idle seconds emit nothing._

       ### Fog-transform rollup (NEW — `Logger('infrastructure.mirk.fog_transform')`)
       _TBD — filled post-walk. Expected: `pixelOriginX/Y` track the pan trajectory; `appliedUOffset` modulo-1.0 mirror that movement; `canvasTx/Ty` flat at 0._

       ### FPS observations (PERF-03)
       _TBD — filled post-walk. Coarse-grained reading off the FpsCounterOverlay (gate: pan-FPS ≥ 30 with fog active; idle-fog-animation FPS ≥ 50)._

       ### Subjective verdict (Criterion B)
       _TBD — filled post-walk. Developer's verbatim words + interpretation._
       ```

    6. `## Verdict` section — placeholder until the walk runs:
       ```markdown
       ## Verdict

       - [ ] Criterion A passed? — TBD post-walk.
       - [ ] Criterion B passed? — TBD post-walk.

       **Outcome:** TBD.

       **MirkFall migration recommendation:** TBD.
       ```

    NOW execute the pre-walk gate sequence. Run each command and capture the exit status:
    1. `flutter test` (must report 0 failures, 0 reds)
    2. `flutter analyze --fatal-infos` (must exit 0)
    3. `dart format --line-length 160 --set-exit-if-changed .` (must exit 0)
    4. `dart test tool/test/` (must report all GREEN)
    5. `gh run list --branch main --limit 5` (capture the most recent SHA + run ID; the run must be `success`)
    6. `gh run view <RUN_ID>` (confirm `gates`, `build-android`, `build-ios` all `success`)
    7. `gh run download <RUN_ID> --name ios-unsigned --dir .uat-tmp/` (produces `.uat-tmp/mirk-poc-debug-unsigned.ipa`)

    If ANY pre-walk gate fails, STOP and report the failure. Do NOT proceed to Task 2 (the walk) until every gate is green. Re-running the gates after a fix-and-push is acceptable.

    Also create `.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT.md` as a thin walk-evidence transcript file (mirrors Phase 3's `03-UAT.md`) — initially containing the metadata block + an empty `## Walk Evidence` section with the same sub-section structure as `03.1-FALSIFICATION.md`'s walk-evidence section. Most of the actual transcript content lives in `03.1-FALSIFICATION.md`'s Walk Evidence section; `03.1-UAT.md` is the more-verbose secondary artefact (raw JSONL extracts, screenshots if any).
  </action>
  <verify>
    <automated>flutter test 2>&amp;1 | tail -3 &amp;&amp; flutter analyze --fatal-infos 2>&amp;1 | tail -3 &amp;&amp; dart format --line-length 160 --set-exit-if-changed . 2>&amp;1 | tail -3 &amp;&amp; dart test tool/test/ 2>&amp;1 | tail -3 &amp;&amp; gh run list --branch main --limit 1 2>&amp;1 | tail -3</automated>
    <file_must_exist>.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION.md</file_must_exist>
    <file_must_exist>.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT.md</file_must_exist>
    <file_must_exist>.uat-tmp/mirk-poc-debug-unsigned.ipa</file_must_exist>
  </verify>
  <done>
    - All 4 local pre-walk gates exit 0 (test / analyze / format / tool-tests).
    - CI run on the post-fix SHA on `main` is `success` (gates + build-android + build-ios all green).
    - `.uat-tmp/mirk-poc-debug-unsigned.ipa` exists and is non-empty (≥ 5 MB — typical IPA size).
    - `03.1-FALSIFICATION.md` exists with all six top-level sections (Hypothesis, Criteria, Walk Plan, Walk Evidence, Verdict, Relationship-to-Phase-3-Verdict). Walk Evidence + Verdict bodies read `TBD`.
    - `03.1-UAT.md` exists as a thin transcript skeleton.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: iPhone 17 Pro sideload UAT walk + post-walk verbal verdict capture</name>
  <files>.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION.md, .planning/phases/03.1-fix-fog-pan-translation/03.1-UAT.md</files>
  <action>
    Hand off to the developer for the manual UAT walk on iPhone 17 Pro. The full procedure is documented below in the checkpoint-specific <what-built>, <how-to-verify>, and <resume-signal> sections — those are the executor-facing instructions. Claude pauses execution, presents the checkpoint to the developer, and waits for the resume signal containing the walk evidence + verdict. After the resume signal is received, Claude proceeds to Task 3 (verdict authoring + REQUIREMENTS/ROADMAP/STATE updates).
  </action>
  <verify>
    <automated>MISSING — PERF-07 + Criterion B (subjective) are inherently manual UAT walk gates per 03.1-VALIDATION.md Manual-Only Verifications table; no automated proxy exists. The developer''s resume-signal IS the verification.</automated>
  </verify>
  <done>
    Developer returns with one of: (a) verbal &#96;approved&#96; — fog tracks camera during pan; CONFIRMED-AFTER-FIX. (b) &#96;denied: &lt;verbatim description&gt;&#96; — describe the failure mode for the diagnostic JSONL correlation step. (c) &#96;iterating: &lt;verbatim description&gt;&#96; — same as denied: but the developer chooses to iterate (Plan 03.1-04 follow-up). Plus the post-walk session log shared via Mail (LOG-04 round-trip) so the executor can grep for the three rollup streams.
  </done>
  <what-built>
    The Plan 03.1-02 fix is software-complete:
    - `_FogPainter.paint()` derives `uOffset = ((pixelOrigin.x / size.width) % 1.0, (pixelOrigin.y / size.height) % 1.0)` and forwards to `shaderRenderer.render(offset: ...)` (3-line code change at the call site).
    - `FogTransformLogger` (sibling to FrameDeltaProbe + SdfRebuildLogger) emits 1-Hz JSONL rollups capturing per-paint canvasTransform / pixelOrigin / center / appliedUOffset diagnostics.
    - FOG-09 behavioural transform-equality regression test is GREEN in CI.
    - FOG-10 logger correctness tests (4) are GREEN in CI.
    - FOG-04 + FOG-07 retained tests stay GREEN through the fix.
    - Pre-walk gates (flutter test / analyze / format / tool tests / CI / IPA download) are all green per Task 1.

    The IPA at `.uat-tmp/mirk-poc-debug-unsigned.ipa` is ready for sideload.
  </what-built>
  <how-to-verify>
    1. Open SideStore on iPhone 17 Pro. Refresh the app's certificate if necessary. Install or refresh `.uat-tmp/mirk-poc-debug-unsigned.ipa`.
    2. Launch `mirk-poc-debug` on iPhone 17 Pro. Allow GPS permission via the rationale screen → "Allow While Using App".
    3. **Pre-walk sanity gate:** tap the science (`Icons.science`) button in the AppBar to navigate to `/sanity`. Confirm the fog renders with a circular reveal hole visible — proves the shader compiles and the SDF→shader path executes. Tap back to `/map`.
    4. **Walk the route:** ≥ 5 minutes continuous through central Melun (lat 48.5397, lon 2.6553 area; same theatre as Plan 03-08). During the walk:
       - Perform ≥ 10 deliberate combined pinch-zoom-and-pan gestures.
       - Perform ≥ 3 recenter FAB taps.
       - Watch the FrameDeltaProbeOverlay (top:104, right:8) — note approximate `med` / `p95` / `max` values during combined gestures (rough mental record is fine; the JSONL log captures the exact numbers).
       - **The KEY observation:** does the fog visually translate with the map during pan? (the Plan 03-08 failure mode was that the fog stayed static while the tile layer translated beneath it — only the blue dot moved.)
    5. **Post-walk:** tap the share button in the AppBar → pick Mail → send the session log to your own email → save the attached `yyyymmdd_hhmmss_logs.txt` from your inbox.
    6. **Capture the verdict:** at the end of the walk, describe in your own words whether the fog tracked the camera during pan. Single-word `approved` is the gate for `CONFIRMED-AFTER-FIX`. Any failure mode (static fog, partial translation, jitter, white-ellipse, reveal-hole lag, inversion, etc.) should be described — that description feeds the diagnostic JSONL grep step.
    7. **Post-walk grep correlation** (executor runs after the verbal verdict is captured):
       - `grep "infrastructure.mirk.frame_delta" <log-file> > .uat-tmp/frame_delta.jsonl`
       - `grep "infrastructure.mirk.sdf" <log-file> > .uat-tmp/sdf.jsonl`
       - `grep "infrastructure.mirk.fog_transform" <log-file> > .uat-tmp/fog_transform.jsonl`
       - For each line, parse the JSON, extract `epochSecond`, and produce a per-second composite row. Pipe into `03.1-FALSIFICATION.md`'s Walk Evidence subsections.

    **The expected `CONFIRMED-AFTER-FIX` evidence pattern:**
    - Walk evidence: developer says `approved` (or equivalent — fog visually tracks the camera; no static fog).
    - `frame_delta` rollup: `medianMs ≤ 16` / `p95Ms ≤ 32` / `maxMs ≤ 48` across the ≥ 10 combined-gesture seconds (PERF-07 satisfied).
    - `fog_transform` rollup: `pixelOriginXMin/Median/Max` track the pan trajectory; `uOffsetXMin/Median/Max` track in modulo-1.0 wrapping; `canvasTxMin/Median/Max` flat at 0 (Canvas at identity inside MobileLayerTransformer).

    **The DENIED-FINAL evidence patterns** are documented in `03.1-FALSIFICATION.md` §Criteria — pattern #1 (fix did not land in the IPA) or pattern #2 (fix landed but fog still static — root cause downstream of the shader uniform).
  </how-to-verify>
  <resume-signal>
    Type ONE of:
    - `approved` — fog tracks camera during pan; CONFIRMED-AFTER-FIX. Executor proceeds to Task 3.
    - `denied: <verbatim description of the failure mode>` — describe what went wrong in your own words. Executor opens the post-walk JSONL grep results, correlates against the failure description, and reports back: either (a) recommend a Plan 03.1-04 follow-up fix attempt with a specific diagnostic-anchored hypothesis, or (b) recommend `DENIED-FINAL` if the diagnostic evidence shows the failure is structural (Phase 3.2 alternative-renderer pivot conversation).
    - `iterating: <verbatim description>` — same as `denied:` shape, but the developer chooses to iterate (CONTEXT §Iteration policy: no hard cap on walk count). Executor opens a Plan 03.1-04 in a follow-up plan-phase invocation.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Author the post-walk verdict — fill `03.1-FALSIFICATION.md` Walk Evidence + Verdict; update REQUIREMENTS.md / ROADMAP.md / STATE.md</name>
  <files>.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION.md, .planning/phases/03.1-fix-fog-pan-translation/03.1-UAT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
    Branch on the developer's verdict from Task 2:

    **Branch A — `approved` → CONFIRMED-AFTER-FIX:**

    1. Fill `03.1-FALSIFICATION.md` Walk Evidence subsections with the post-walk grep correlation from Task 2:
       - `### Pre-walk shader-sanity gate`: `PASSED. Developer confirmed the fog rendered at /sanity with the circular reveal hole visible. No shader-compile exceptions in the FileLogger output (grep -E 'severe|Failed to load fog shader' <log-file> returned 0 matches).`
       - `### Probe rollup (frame-delta JSONL)`: paste the per-second composite extract for the ≥ 10-combined-gesture window. Compute the rolling rollup across the window (median-of-medians, max-of-p95s, max-of-maxes). Compare against `medianMs ≤ 16` / `p95Ms ≤ 32` / `maxMs ≤ 48` thresholds.
       - `### SDF rebuild rollup`: paste the per-second extract; correlate `rebuildCount` against the GPS fix log (every fix should produce one rebuild via `discRepository.append → SdfCache.busts → next paint rebuilds`).
       - `### Fog-transform rollup (NEW)`: paste the per-second extract; confirm `pixelOriginX/Y` track the pan trajectory; `appliedUOffsetX/Y` track modulo-1.0; `canvasTx/Ty` flat at 0 (per RESEARCH §Pitfall D).
       - `### FPS observations (PERF-03 carry-over)`: coarse reading from the FpsCounterOverlay (e.g., "sustained ~120 fps during pan; ~50 fps idle with fog animation").
       - `### Subjective verdict (Criterion B)`: developer's verbatim words from Task 2; interpretation paragraph mapping each Criterion B sub-claim to PASS/FAIL.

    2. Fill `03.1-FALSIFICATION.md` Verdict section:
       ```markdown
       ## Verdict

       - [x] Criterion A passed? — **YES.** Frame-delta probe rolling rollup over ≥ 10 combined gestures: median = {N} ms (≤ 16 ✓), p95 = {N} ms (≤ 32 ✓), max = {N} ms (≤ 48 ✓). PERF-07 satisfied — the unmeasured-and-moot Criterion A from Plan 03-08 is now properly captured.
       - [x] Criterion B passed? — **YES.** Developer's verbatim verdict: *"<verbatim>"*. All four Criterion B sub-claims pass: fog translates with camera during pan ✓; no slide-then-snap ✓; no white-ellipse during pinch-zoom ✓; no reveal-hole lag ✓; no inversion ✓.

       **Outcome:** **CONFIRMED-AFTER-FIX.**

       The same-Canvas fog hypothesis is **reinstated** with the Plan 03.1-02 fix. The Phase 3 `DENIED 2026-05-01` verdict is reversed by the Phase 3.1 evidence: deriving `(uOffsetX, uOffsetY)` from `camera.pixelOrigin / size` (modulo-1.0) and forwarding to the shader's slot-3..4 `uOffset` uniform was the missing 3-line link. The structural FOG-04 test (Plan 03-05) was correct but insufficient; the new FOG-09 behavioural transform-equality test (Plan 03.1-02) catches the failure mode mechanically and prevents regression in CI.

       **MirkFall migration recommendation:** **PORT BACK** with the Plan 03.1-02 fix included. The shader-uniform call-site is the load-bearing change; everything else (single-MapCamera-snapshot lock, identity uSdfRect, no RepaintBoundary, FogShaderUniforms 41-slot SSOT) was already correct and stays untouched. The MirkFall donor `_FogPainter.paint()` call site MUST be reviewed against this fix at port-back time — if the parent project's painter passes a hard-coded zero for `offset`, the same regression would re-emerge.
       ```

    3. Update `.planning/REQUIREMENTS.md`:
       - FOG-04, FOG-05, FOG-06, FOG-07: change checkbox from `[ ]` to `[x]`. Update the inline status from `Falsified-in-production (P03-08 walk DENIED 2026-05-01)` to `Complete — Verified-by-walk (P03.1-03 CONFIRMED-AFTER-FIX 2026-05-XX)`. Append a status-clause referencing both Plan 03-08 (the DENIED) and Plan 03.1-03 (the CONFIRMED-AFTER-FIX reversal) for traceability.
       - PERF-07: change checkbox from `[ ]` to `[x]`; status `Complete — Measured (P03.1-03)`. Include the actual numbers: `median = {N} ms, p95 = {N} ms, max = {N} ms`.
       - PERF-03: change status from `Not measured` to `Measured — coarse FpsCounterOverlay reading: sustained ~{N} fps pan; ~{N} fps idle with fog animation. (P03.1-03)`.
       - PERF-04: change status from `Not captured` to `Captured at Plan 03.1-03 walk (re-run after Phase 3.1 fix). See PERF-07 row for actual numbers — same methodology.`.
       - PERF-05: change status note from `MEASURED — VERDICT DENIED` to `Re-MEASURED at P03.1-03 — VERDICT CONFIRMED-AFTER-FIX. Plan 03-08 verdict (DENIED) stands as historical record; Plan 03.1-03 verdict reverses it. See `03.1-FALSIFICATION.md`.`
       - Append a new entry to the `## Revisions` section:
         ```
         - **2026-05-XX (Phase 3.1 closure, Plan 03.1-03 SUMMARY — HYPOTHESIS CONFIRMED-AFTER-FIX):** Plan 03.1-03 sideload UAT walk on iPhone 17 Pro against CI run `<RUN_ID>` (SHA `<SHA>`) over central Melun delivered a **CONFIRMED-AFTER-FIX** verdict. Developer's verbatim words: *"<verbatim>"*. Fog visually translates with the camera during pan; the Plan 03-08 static-fog failure mode is reversed by the Plan 03.1-02 3-line fix in `_FogPainter.paint()` (derive `uOffset` from `camera.pixelOrigin / size` modulo-1.0; forward to shader's slot-3..4 `uOffset` uniform). FOG-04, FOG-05, FOG-06, FOG-07 flip from Falsified-in-production back to Complete — Verified-by-walk. PERF-07 marked Measured with actual numbers (median = {N} ms, p95 = {N} ms, max = {N} ms across ≥ 10 combined gestures — comfortably inside the 16/32/48 ms thresholds). PERF-03/04/05 statuses updated to reflect the post-fix re-measurement. MirkFall migration recommendation: **PORT BACK** with the Plan 03.1-02 fix; the parent project's painter call site must be reviewed for the same shader-uniform regression at port-back time. Phase 4 (wisp particles) and Phase 5 (decision gate) **UNBLOCKED**. The new FOG-09 behavioural transform-equality test in CI prevents future regression to the static-fog failure mode without a sideload walk. Evidence: `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION.md` + `03.1-UAT.md`.
         ```

    4. Update `.planning/ROADMAP.md`:
       - In the `## Progress` table, change Phase 03.1's status to `Complete (HYPOTHESIS CONFIRMED-AFTER-FIX)`, plans `3/3`, completed `2026-05-XX`.
       - In Phase 4's row: change status from `Blocked on Phase 3.1` to `Pending`.
       - In Phase 5's row: change status from `Blocked on Phase 3.1` to `Pending`.
       - In Phase 3's overview line (line 17 region): append a final sentence — `Phase 3.1 reversed the verdict to CONFIRMED-AFTER-FIX 2026-05-XX after a 3-line shader-uniform fix; see 03.1-FALSIFICATION.md.`
       - In Phase 4 + Phase 5 overview lines: remove the `**BLOCKED on Phase 3.1 gap-closure outcome**` clause.

    5. Update `.planning/STATE.md`:
       - Frontmatter: `stopped_at: Phase 3.1 CONFIRMED-AFTER-FIX walk complete; Phase 4 unblocked.`
       - `last_activity: 2026-05-XX — Plan 03.1-03 walk CONFIRMED-AFTER-FIX. Phase 4 unblocked.`
       - Bump `progress.completed_phases` from `3` to `4` (Phase 3.1 is the 4th completed phase) and `total_phases` to `6` (Phase 3.1 inserted; Phase 4 + Phase 5 still pending).
       - In the `## Current Position` section: update `Phase: 3 of 5 (...)` to `Phase: 3.1 of 6 (Fix Fog Pan-Translation) — CLOSED with HYPOTHESIS CONFIRMED-AFTER-FIX`. Update the `Status:` paragraph to reflect Phase 3.1 closure + Phase 4 unblock.
       - In the `### Decisions` subsection: append a new entry summarising the Phase 3.1 closure (mirror the Plan 03-08 entry shape).
       - In the `### Blockers/Concerns` section: change the `PHASE-3.1-CAMERA-TRANSFORM (NEW 2026-05-01, BLOCKER)` entry to `PHASE-3.1-CAMERA-TRANSFORM (RESOLVED 2026-05-XX by Plan 03.1-03 CONFIRMED-AFTER-FIX walk):` and strike-through the original wording.

    6. **DO NOT TOUCH** `.planning/phases/03-fog-of-war-the-hypothesis/03-FALSIFICATION.md`. The Phase 3 historical record is preserved as-is. Confirm via `git status .planning/phases/03-fog-of-war-the-hypothesis/03-FALSIFICATION.md` that the file is unchanged.

    **Branch B — `denied:` → DENIED-FINAL:**

    1. Same `03.1-FALSIFICATION.md` Walk Evidence subsection fills as Branch A, but the data shows the failure mode (e.g., uOffset stuck at 0 in the rollup → fix did not land in the IPA → DENIED-FINAL pattern #1; or uOffset tracks but fog still static → DENIED-FINAL pattern #2). The diagnostic stream is the load-bearing evidence: post-walk grep reveals which pattern.

    2. Verdict section:
       ```markdown
       **Outcome:** **DENIED-FINAL.**

       The same-Canvas fog hypothesis is **falsified beyond the Phase 3.1 fix scope**. {Pattern description from the diagnostic stream.} The Phase 3 + Phase 3.1 verdicts together form the formal architectural counter-evidence: same-Canvas in `flutter_map 7.0.2` does not deliver fog-camera-tracking parity even with the Plan 03.1-02 fix.

       **MirkFall migration recommendation:** **DO NOT PORT BACK.** {Rationale.} The Phase 3.2 alternative-renderer pivot (mapsforge_flutter, custom MVT-on-Canvas, flutter_gpu) becomes the only remaining option, and is itself a deferred-decision per CONTEXT §Fix-failure contingency.
       ```

    3. REQUIREMENTS.md updates: FOG-04..07 stay `Falsified-in-production` with an updated revision-clause appending Plan 03.1-03 evidence. PERF-07 marked `Measured-with-DENIED-FINAL-verdict` with actual numbers (whatever the rollup captured).

    4. ROADMAP.md updates: Phase 03.1 row → `Complete (HYPOTHESIS DENIED-FINAL)`. Phase 4 + Phase 5 stay `BLOCKED` (or transition to `Cancelled` per developer choice — flag this in the SUMMARY for the user to decide).

    5. STATE.md updates: capture the architectural counter-evidence + the Phase 3.2 conversation hand-off.

    **Branch C — `iterating:` → defer Plan 03.1-04 to a follow-up plan-phase invocation:**

    1. Fill `03.1-FALSIFICATION.md` Walk Evidence with the post-walk grep correlation (same as Branches A + B).

    2. Verdict section: `**Outcome:** ITERATING — see Plan 03.1-04+ for the next fix attempt. Diagnostic stream from this walk: <description>.`

    3. Do NOT update REQUIREMENTS.md / ROADMAP.md / STATE.md to a closure state. Mark Phase 3.1 as `In Progress (iterating)` in the Progress table.

    4. The Plan 03.1-04 plan is created via a follow-up `/gsd:plan-phase 03.1` (or analogous) invocation, with the diagnostic stream from THIS walk as input. THIS task does not author Plan 03.1-04.

    For all three branches: commit all updates via the standard plan-execution commit pattern. Cross-reference Plan 03.1-03 in commit messages. Do NOT commit `.uat-tmp/` (gitignored) or the post-walk `.jsonl` extracts (transient walk artefacts).
  </action>
  <verify>
    <automated>flutter test 2>&amp;1 | tail -3 &amp;&amp; flutter analyze --fatal-infos 2>&amp;1 | tail -3 &amp;&amp; node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" frontmatter validate .planning/phases/03.1-fix-fog-pan-translation/03.1-03-PLAN.md --schema plan</automated>
    <file_must_exist>.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION.md</file_must_exist>
    <file_must_exist>.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT.md</file_must_exist>
  </verify>
  <done>
    - `03.1-FALSIFICATION.md` Walk Evidence + Verdict sections are filled (no `TBD` strings remaining for the chosen branch — Branches A or B; Branch C explicitly leaves them flagged for Plan 03.1-04).
    - `03.1-UAT.md` carries the walk transcript.
    - REQUIREMENTS.md FOG-04..07 + PERF-03/04/05/07 status fields reflect the verdict outcome (Branch A: Complete; Branch B: Falsified-in-production with updated clause; Branch C: in-progress).
    - ROADMAP.md Progress table Phase 03.1 row reflects the verdict; Phase 4 + Phase 5 statuses updated per branch.
    - STATE.md Current Position + Decisions + Blockers/Concerns updated.
    - `git status .planning/phases/03-fog-of-war-the-hypothesis/03-FALSIFICATION.md` shows the file is UNCHANGED (Phase 3 historical record preserved).
    - All test gates remain green (the closure docs do not touch any production code).
  </done>
</task>

</tasks>

<verification>
After all three tasks complete:
- `03.1-FALSIFICATION.md` is filled top-to-bottom with the verdict and walk evidence (or marked `iterating` per Branch C).
- The Phase 3 historical record (`03-FALSIFICATION.md`) is BYTE-IDENTICAL to its pre-Phase-3.1 state.
- REQUIREMENTS.md, ROADMAP.md, STATE.md, all reflect the verdict outcome consistently.
- `flutter test` (full suite) is GREEN — Phase 3.1 closure docs do not touch any production code.
- `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" frontmatter validate .planning/phases/03.1-fix-fog-pan-translation/03.1-03-PLAN.md --schema plan` returns `{ valid: true }`.
</verification>

<success_criteria>
- The iPhone 17 Pro UAT walk produces a verdict (verbal `approved` or `denied:` or `iterating:`) — the empirical answer Phase 3.1 was created to produce.
- `03.1-FALSIFICATION.md` is the load-bearing artefact capturing the verdict + the diagnostic-JSONL evidence stream.
- On `CONFIRMED-AFTER-FIX`: Phase 4 + Phase 5 unblock; the MirkFall migration recommendation flips to PORT BACK with the Plan 03.1-02 fix; the formal POC closure conversation can proceed at Phase 5.
- On `DENIED-FINAL`: the formal POC architectural counter-evidence is complete; the Phase 3.2 alternative-renderer conversation is the only remaining option.
- On `iterating:`: Plan 03.1-04 is opened in a follow-up invocation with the diagnostic stream from THIS walk as input.
- The Phase 3 historical record stays untouched — historical integrity preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-fix-fog-pan-translation/03.1-03-SUMMARY.md` capturing:
- The pre-walk gate sequence outcome (4 local + CI + IPA download)
- The walk metadata (CI run ID, SHA, walk theatre, walk duration, gesture count)
- The developer's verbatim verdict
- The post-walk JSONL grep correlation (per-second composite extract for the ≥ 10-combined-gesture window)
- The Verdict outcome (`CONFIRMED-AFTER-FIX` | `DENIED-FINAL` | `ITERATING`)
- Files modified across all three tasks (the 4-5 planning docs)
- Any deviations from the plan with Rule classification
- Cross-reference to Phase 4 (the unblocked-or-still-blocked downstream phase) + the migration recommendation hand-off to Phase 5
</output>
