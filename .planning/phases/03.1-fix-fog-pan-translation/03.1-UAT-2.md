---
phase: 03.1-fix-fog-pan-translation
plan: 06
type: uat
status: complete
source: [03.1-04-SUMMARY.md, 03.1-05-SUMMARY.md]
started: 2026-05-02T22:43:47Z
ci_run: "25262881962"
ci_sha: "9a87010"
device: iPhone 17 Pro (ProMotion 120 Hz, A19 Pro)
location: central Melun (initial GPS fix at 48.5397, 2.6553 area)
exit_gate: developer's resume signal — verbal `iterating: <verbatim>` (received)
verdict: ITERATING-WITH-PARTIAL-PROGRESS
walked: 2026-05-02
gaps:
  - id: FOG-13
    title: fog-rect viewport-coverage symmetric compensation
    summary: |
      Plan 03.1-05's clip-path shift compensation worked for the reveal-hole
      geometry (FOG-12 confirmed) but the underlying `canvas.drawRect(Offset.zero & size, fogPaint)`
      rect-cover paints in canvas-translated space when MobileLayerTransformer
      applies non-zero canvas translation. Walk #2 captured 243/267 rollups
      with |canvasTx|>100 or |canvasTy|>100; worst sustained
      (canvasTx, canvasTy) = (+757.35, +319.46) for the final ~50 seconds.
      Symptom: top-right + bottom-right diagonal strips of un-fogged street
      map visible at high zoom. Hypothesis: shift the rect bounds
      symmetrically with the clip path, OR call `canvas.translate(-canvasOffset)`
      at the top of paint() so the entire painter draws in the
      world-aligned frame, OR over-draw the rect by max(|canvasTx|, |canvasTy|).
    severity: blocker
    introduced_by: P03.1-05
    surfaced_by: walk-2
    proposed_fix_options:
      - "(b) canvas.translate(-canvasTx, -canvasTy) at top of paint(); remove redundant clip-path shift"
      - "(c) shift drawRect bounds by -canvasOffset symmetrically with clip path"
      - "(d) over-draw rect by max(|canvasTx|, |canvasTy|) — extra paint clipped by device screen"
  - id: FOG-14
    title: noise-pattern visual stability at high zoom (refined SHADER-MODULO-WRAP)
    summary: |
      FOG-11 CI test (`fog_smooth_noise_test.dart`) asserts consecutive-paint
      pixelOrigin delta < 1e3 raw px and went GREEN. Walk #2 confirms the
      raw-uniform-discontinuity Walk #1 Finding 3 captured IS closed
      mechanically. BUT the user-observable noise-pattern instability
      persists with refined characterisation: "rotating 90° very fast /
      translated very fast repetitively". CI-test-vs-walk-symptom mismatch.
      Three candidate root causes (from FALSIFICATION-2.md sub-section D):
      (B-1) fp32 precision degradation in fract(uPixelOrigin / uResolution)
      at high zoom where the ratio reaches 1e4-1e5; (B-2) uResolution
      uniform incorrectly scaled (logical-px vs device-px confusion on
      iOS 3x DPR); (B-3) noise-tile-period mismatch causing fract()
      wraparound frequency to align with perceived "rotation".
    severity: blocker
    introduced_by: ""  # not introduced by P03.1-04/05 — was always there; P03.1-04 closed proximate, missed observable
    surfaced_by: walk-2
    proposed_fix_options:
      - "(B-1) decompose pixelOrigin into integer + fractional parts CPU-side; forward only fractional"
      - "(B-2) audit FogShaderUniforms.setAll(resolution: ...) for logical-vs-device-px scaling; multiply or divide by devicePixelRatio as needed"
      - "(B-3) replace fract(uPixelOrigin / uResolution) with fract(uPixelOrigin / uTilePeriodPixels) where uTilePeriodPixels = uResolution / max(uScaleFar, uScaleMid, uScaleNear)"
      - "Plus: add a higher-fidelity CI test (FOG-14a or augmented FOG-11) that catches user-observable noise-pattern instability — visual diff against reference frame OR precision-budget assertion (ratio < 1024 at any zoom)"
  - id: PERF-08-WIDEN
    title: SDF cache quantisation 1e-4 not closing per-paint thrash empirically
    summary: |
      Plan 03.1-05's PERF-08 unit test asserted sub-quantisation viewport drift
      produces ONE rebuild and went GREEN. Walk #2 captures 110 SDF rollups
      with rebuildCount distribution: <=5: 4 rollups; 6-30: 12; 31-100: 69;
      >100: 25. Median 68/sec, max 121/sec — statistically indistinguishable
      from Walk #1's 12-115/sec. The 1e-4 lat/lon (~11 m) quantisation grid
      apparently does not match the in-production gesture pattern's drift
      magnitude. Per-rebuild cost stays sub-13 ms so frame budget preserved,
      but the unintended thrash remains a candidate residual contributor to
      FOG-14 symptoms.
    severity: medium
    introduced_by: ""  # P03.1-05 attempted closure; partial
    surfaced_by: walk-2
    proposed_fix_options:
      - "Widen quantisation to 1e-3 lat/lon (~110 m) and re-walk"
      - "Pivot to disc-set-only cache key (viewport ignored unless leaving the world-rect the SDF was built for)"
      - "Move SDF rebuild off the UI isolate (RESEARCH §Open Question deferred to v2 ROB-01)"
---

# Phase 3.1 UAT — Walk #2 (Post-Plan-03.1-04 + Plan-03.1-05 Layered Fix)

**Gate:** Walk #2 is the empirical answer to the layered Plan 03.1-04 (SHADER-MODULO-WRAP) + Plan 03.1-05 (CANVAS-FRAME-ALIGNMENT + PERF-08 + UX-01) fix bundle. Walk #1 closed `ITERATING-WITH-MAJOR-PROGRESS` 2026-05-02; Walk #2 either flips the verdict to `CONFIRMED-AFTER-FIX` (unblocking Phase 4 + Phase 5) or strengthens it to `DENIED-FINAL` (terminating the same-Canvas hypothesis) or — per CONTEXT §Iteration policy — captures a third `ITERATING` verdict that opens Plan 03.1-07.

This UAT mirrors the shape of `.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT.md` (Walk #1's transcript). The new section is `### Diagnostic JSONL — Walk #2 fog_transform rollup` capturing the per-second canvasTransform / pixelOrigin / uOffset evidence stream after the layered fix.

## Build Under Test

| Field        | Value |
| ------------ | ----- |
| CI run       | 25262881962 |
| Run URL      | https://github.com/saibashirudo/mirk-poc-debug/actions/runs/25262881962 |
| HEAD SHA     | 9a87010 |
| iOS artifact | `mirk-poc-debug-ios-unsigned-ipa` — downloaded to `.uat-tmp/mirk-poc-debug-unsigned.ipa` |
| Gates job    | success |
| Android job  | success |
| iOS job      | success |
| Tests        | 138 GREEN / 1 SKIPPED / 0 RED |

## Pre-walk Gates

- [x] `flutter test` full suite GREEN — 138/138 + 1 SKIPPED (Plan 03.1-06 Task 1 — pre-existing FOG-09 vacuous-test guard)
- [x] `flutter analyze --fatal-infos` 0 warnings — Plan 03.1-06 Task 1
- [x] `dart format --line-length 160 --set-exit-if-changed .` clean — 81 files, Plan 03.1-06 Task 1
- [x] `dart test tool/test/` GREEN — 18/18, Plan 03.1-06 Task 1
- [x] CI run 25262881962 GREEN (gates + android + ios) — SHA 9a87010
- [x] IPA artifact downloaded — `.uat-tmp/mirk-poc-debug-unsigned.ipa` — ~11 MB
- [x] `/sanity` smoke test on sideloaded IPA — developer's verbatim *"sanity page is ok"*; UX-01 back-button confirmed working at walk-time

## Walk Protocol

- **Where:** Central Melun (initial GPS fix at 48.5285, 2.6551 area — same theatre as Walk #1).
- **Duration:** ≥ 5 minutes continuous walk with the IPA running and the device awake.
- **Gestures:** ≥ 10 deliberate combined pinch-zoom-and-pan gestures, ≥ 3 recenter FAB taps.
- **HUD visible during walk:** `FpsCounterOverlay` + `MapCompass` + `FrameDeltaProbeOverlay`.
- **Pre-walk shader-sanity gate:** `/sanity` route — confirm fog renders with circular reveal hole; confirm new `Icons.arrow_back` leading button works (UX-01 walk-time validation); back-pop returns to `/map`.
- **Post-walk:** session log shared via Mail (LOG-04 round-trip) → saved attachment, three JSONL streams extracted via grep into `.uat-tmp/walk2_{frame_delta,sdf,fog_transform}.jsonl`.

## Walk Evidence

The full evidence + Walk Evidence sections A/B/C/D + Verdict are in the falsification document
(`03.1-FALSIFICATION-2.md`). This UAT transcript holds the lighter-weight per-stream summary +
the developer's verbatim verdict; the falsification doc is the load-bearing artefact.

### Pre-walk shader-sanity gate (`/sanity` route + UX-01 back-button validation)

PASSED. Developer's verbatim *"sanity page is ok"*. The `Icons.arrow_back` AppBar leading button (Plan 03.1-05) returns to `/map` cleanly without force-close. UX-01 confirmed walk-time.

### Frame-delta probe rollups (PERF-07, Criterion A — `infrastructure.mirk.frame_delta` JSONL)

**266 rollups** captured over the 296-second walk. Aggregate:

| Metric    | Min   | Median-of-rollups | Max-of-rollups | Budget   | Result |
| --------- | ----- | ----------------- | -------------- | -------- | ------ |
| medianMs  | 0.133 | 1.495             | 2.965          | ≤ 16 ms  | PASS   |
| p95Ms     | 0.242 | 2.029             | 6.340          | ≤ 32 ms  | PASS   |
| maxMs     | 0.460 | 2.162             | 12.416         | ≤ 48 ms  | PASS   |

**Headroom:** 3.9-5.4× across all three thresholds. PERF-07 re-validated post-Plan-03.1-04 + 03.1-05 layered fix. Single-Float64List per-paint allocation from Plan 03.1-05's `canvas.getTransform()` did NOT regress frame budget at POC scale.

### SDF rebuild rollups (`infrastructure.mirk.sdf` JSONL — PERF-08 validation)

**110 INFO rollups** captured. Aggregate:

| Metric                 | Walk #1 | Walk #2 |
| ---------------------- | ------- | ------- |
| `rebuildCount` min     | 12      | **1**   |
| `rebuildCount` median  | ~50-70  | **68**  |
| `rebuildCount` max     | 115     | **121** |
| Total rebuilds (window) | ~1500   | **7481** |
| Distribution           | (high in gesture seconds) | <=5: 4; 6-30: 12; 31-100: 69; >100: 25 |

**PERF-08 falsified-by-walk-2.** Plan 03.1-05's 1e-4 lat/lon quantisation does NOT close the per-paint cache thrash empirically. The unit test went GREEN with sub-quantisation drift producing ONE rebuild — but the in-production gesture pattern produces drift outside the quantisation grid more often than the test pattern anticipated. See gaps section above for Plan 03.1-07 fix options.

### Diagnostic JSONL — Walk #2 fog_transform rollup (`infrastructure.mirk.fog_transform`)

**267 rollups** captured. Key findings:

- **pixelOriginX/Y** range: 888,176 .. 4,256,820 (X) / 604,481 .. 2,897,579 (Y). Eleven zoom-step discontinuities of 0.5M-3.3M pixels each between consecutive seconds (zoom 13 ↔ zoom 16 transitions).
- **canvasTx/Ty** non-zero on **252 of 267 rollups**. Worst sustained: `(canvasTx, canvasTy) = (+757.35, +319.46)` for the final ~50 seconds. Walk #1 saw worst `(-31.36, -46.33)` — Walk #2's canvas-translation magnitudes are **20-25× larger**, exposing the rect-cover compensation gap that Walk #1 didn't surface.
- **uOffsetX/Y** (now raw-pixel units per Plan 03.1-04) track pixelOrigin verbatim — Walk #1 Finding 3's multi-wrap-per-second sweeping is NO LONGER REPRODUCED at the diagnostic-stream level. The proximate uniform-discontinuity Plan 03.1-04 was designed to close IS closed mechanically.

But the user-observable shimmer persists, indicating the diagnostic-stream metric was an **insufficient proxy** for the symptom space.

Representative excerpt (single-second high-zoom pan):

```
epochSecond=1777761913: pixelOriginXMed=4255798.97 pixelOriginYMed=2897155.91 canvasTxMax=193.42 canvasTyMax=-196.47
epochSecond=1777761953: pixelOriginXMed=4255867.48 pixelOriginYMed=2897135.87 canvasTxMin=544.94 canvasTxMax=642.23
epochSecond=1777762083: pixelOriginXMed=4255787.83 pixelOriginYMed=2897177.75 canvasTxMin=757.35 canvasTxMax=757.35
```

Full extracts in `.uat-tmp/walk2_{frame_delta,sdf,fog_transform}.jsonl` (gitignored — transient walk artefacts).

### FPS observations (PERF-03)

No discrete FPS counter readings recorded in the transcript. Inferred from frame_delta median 1.5 ms (≈ 670 fps theoretical), per-paint cost is utterly negligible — same finding as Walk #1. The user-observable failure modes are NOT frame-budget issues.

### Subjective verdict (Criterion B)

**Verdict: `iterating: <verbatim>`** per CONTEXT §Iteration policy. Three of four targeted fix-axes confirmed; one fix-axis (FOG-11 SHADER-MODULO-WRAP) landed in CI but did NOT close the user-observable symptom; one fix-axis (Plan 03.1-05 CANVAS-FRAME-ALIGNMENT) introduced a NEW symptom (fog-rect viewport-coverage).

Mapped to Walk #1's 5 observations:

1. **PERF-07 GREEN — re-validated.** *"med 1.3 ms / p95 1.4 ms / max 1.7 ms"* on the FrameDeltaProbeOverlay; rolling rollup confirms 1.5/2.0/2.2 ms median-of-medians/p95s/maxes across 266 rollups.
2. **FOG-11 (SHADER-MODULO-WRAP) — FALSIFIED-BY-WALK-2.** *"Mirk is still flashing/regenerating during pan/zoom like if the seed was changing... rotating 90° very fast / translated very fast repetitively. To investigate."* CI test went GREEN; user-observable symptom persists with refined characterisation.
3. **FOG-12 (CANVAS-FRAME-ALIGNMENT, reveal-hole) — CONFIRMED.** *"the revealed area is staying where it should"*. Screenshot shows blue dot at reveal-hole centre.
4. **NEW failure mode (P03.1-05 introduced) — fog-rect viewport-coverage.** *"Mirk shader is somehow rotating and not covering the whole map when zooming in... mirk made worse (mirk offset itself leaving part of the map revealed at the edges)."* Screenshot shows top-right + bottom-right diagonal strips of un-fogged street map. New gap FOG-13.
5. **UX-01 (sanity back button) — CONFIRMED.** *"sanity page is ok"*.

## Verdict

**Verdict:** ITERATING-WITH-PARTIAL-PROGRESS.

**Walked at:** 2026-05-02, central Melun, iPhone 17 Pro sideloaded against CI run 25262881962 (SHA 9a87010).

**Developer's verbatim words (composite):**
- *"Mirk is still flashing/regenerating during pan/zoom like if the seed was changing"*
- *"the mirk pattern is switched to an other one, then an other one, then an other one, very fast (like changing the seed of a procedural pattern fast)... maybe it's just being rotated 90° very fast, or maybe it's translated very fast repetitively. To investigate."*
- *"Mirk shader is somehow rotating and not covering the whole map when zooming in. Mirk shader is the one getting offseted now"*
- *"sanity page is ok"*
- *"the revealed area is staying where it should"*
- *"med 1.3 ms / p95 1.4 ms / max 1.7 ms"* (approximate FrameDeltaProbeOverlay readings)

**Interpretation:** Three of four Plan 03.1-04 + 03.1-05 fix-axes confirmed (PERF-07 re-validated, FOG-12 reveal-hole alignment, UX-01 back button). FOG-11 SHADER-MODULO-WRAP fix landed in CI but the user-observable shimmer persists — the CI test was an insufficient proxy for the symptom (key learning). Plan 03.1-05 CANVAS-FRAME-ALIGNMENT fix correctly compensated the clip path but did NOT compensate the fog rect-cover paint, introducing a new viewport-edge un-fogged-strips failure mode. PERF-08 SDF-CACHE-VIEWPORT-THRASH fix landed in unit test but does NOT close the per-paint thrash empirically (1e-4 quantisation grid does not match the in-production gesture pattern).

See `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-2.md` for the formal verdict + sub-section A/B/C/D walk evidence + hypothesis triage table for Plan 03.1-07.

## Deviations / Surprises

- **Walk-time diagnostic-stream proxy mismatch (KEY LEARNING):** the FOG-11 CI test asserted the proximate uniform-discontinuity failure mode (`fog_smooth_noise_test.dart` consecutive-paint pixelOrigin delta < 1e3 raw px) and went GREEN. Empirical walk: the user-observable symptom persists. Plan 03.1-04's fix landed mechanically; the diagnostic-stream metric the test was built around was the wrong proxy for "noise pattern looks stable across paints". Plan 03.1-07's CI tests must aim higher up the symptom-space hierarchy.
- **PERF-08 1e-4 quantisation grid did not close the per-paint thrash empirically.** The unit test exercised sub-quantisation drift; the in-production gesture pattern produces drift OUTSIDE that quantisation grid more often than expected. PERF-08 flips to falsified-by-walk-2.
- **canvasTx/Ty Walk #2 magnitudes are 20-25× larger than Walk #1 worst case.** Walk #1: worst `(-31.36, -46.33)` for ~28 seconds. Walk #2: sustained `(+757.35, +319.46)` for ~50 seconds. Gesture pattern difference (more aggressive zoom-out / re-centre cycles in Walk #2) exposes the rect-cover compensation gap that Walk #1 didn't surface. Plan 03.1-05's clip-path-only compensation pattern, written against Walk #1's worst-case 46-px-magnitude evidence, is empirically insufficient when the magnitude grows to 757-px.

## Summary

This is the **third Phase 3.1 verdict** — Walk #1 closed `iterating-with-major-progress`, Walk #2 closes `iterating-with-partial-progress`. Per CONTEXT §Iteration policy there is no hard cap on walk count. Plan 03.1-07 will be authored via `/gsd:plan-phase 3.1 --gaps` consuming the FALSIFICATION-2.md hypothesis triage table + the `gaps:` entries in this transcript (FOG-13, FOG-14, PERF-08-WIDEN). Phase 4 + Phase 5 STAY blocked. MirkFall migration recommendation **DEFERRED**.
