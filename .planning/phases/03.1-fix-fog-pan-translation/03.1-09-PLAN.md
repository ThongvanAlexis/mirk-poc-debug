---
phase: 03.1-fix-fog-pan-translation
plan: 09
type: execute
wave: 2
depends_on: ["03.1-07", "03.1-08"]
files_modified:
  - .planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-3.md
  - .planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-3.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
gap_closure: true
requirements: [FOG-04, FOG-05, FOG-06, FOG-07, FOG-11, FOG-13, FOG-14, FOG-15, DEBUG-01, PERF-07]
user_setup:
  - service: github-actions
    why: "Wait for CI green on the post-Plan-03.1-07 + Plan-03.1-08 SHA + download the IPA artifact for the Walk #3 sideload"
    dashboard_config:
      - task: "Confirm CI run status (`gh run list --branch main --limit 5` + `gh run watch`)"
        location: "GitHub Actions for the mirk-poc-debug repo"
      - task: "Download IPA artifact via `gh run download <RUN_ID> --name ios-unsigned --dir .uat-tmp/`"
        location: "GitHub Actions run detail page (or `gh` CLI)"
  - service: sidestore
    why: "Sideload the unsigned IPA onto iPhone 17 Pro for Walk #3"
    dashboard_config:
      - task: "Open SideStore on iPhone 17 Pro → install/refresh the .uat-tmp/mirk-poc-debug-unsigned.ipa"
        location: "SideStore iOS app + paired desktop pairing-file"
must_haves:
  truths:
    - "A new `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-3.md` exists capturing the Walk #3 verdict. The existing `03.1-FALSIFICATION.md` (Walk #1) AND `03.1-FALSIFICATION-2.md` (Walk #2) are LEFT UNTOUCHED — historical record preserved as Walk #1 + Walk #2."
    - "A new `.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-3.md` exists capturing the Walk #3 transcript. The existing `03.1-UAT.md` + `03.1-UAT-2.md` are LEFT UNTOUCHED."
    - "Pre-walk gate sequence runs cleanly: `flutter test` GREEN (138+ existing + new FOG-13 + FOG-14 + DEBUG-01 tests); `flutter analyze --fatal-infos` 0 warnings; `dart format --line-length 160 --set-exit-if-changed .` clean; `dart test tool/test/` GREEN; CI green on the post-Plan-03.1-07 + 03.1-08 SHA on `main`; IPA downloaded via `gh run download` to `.uat-tmp/mirk-poc-debug-unsigned.ipa`."
    - "The iPhone 17 Pro UAT Walk #3 is performed by the user. Walk shape matches Plan 03.1-06 + Plan 03.1-03 patterns: ≥ 5 minutes through Melun + ≥ 10 deliberate combined pinch-zoom-and-pan gestures + ≥ 3 recenter taps + post-walk Mail-share of the session log + pre-walk `/sanity` shader-compile gate (now also tests the Plan 03.1-07 debug-spiral toggle works on the sideloaded build — the toggle was lab-verified in Task 1; Walk #3 confirms it functions on real iOS)."
    - "Post-walk: developer's verbal verdict (`approved` / `denied:` / `iterating:`) is captured. The walk evidence section of `03.1-FALSIFICATION-3.md` is filled with: developer's verbatim words + interpretation + post-walk grep of `infrastructure.mirk.fog_transform` + `infrastructure.mirk.frame_delta` + `infrastructure.mirk.sdf` rollups + correlation on per-second `epochSecond` boundary."
    - "PERF-07 is re-measured for Walk #3: frame-delta probe rolling rollup over the ≥ 10-combined-gesture window with `medianMs ≤ 16`, `p95Ms ≤ 32`, `maxMs ≤ 48`. Walk #1 captured 4-7× headroom; Walk #2 captured 3.9-5.4× headroom; Walk #3 should preserve similar headroom (the Plan 03.1-08 `canvas.translate` adds ~1 cycle per paint — negligible against the budget)."
    - "Verdict written: `CONFIRMED-AFTER-FIX` if BOTH (a) developer's subjective verdict is `approved` AND (b) PERF-07 thresholds satisfied AND (c) FOG-13 walk-time validation passes (no top-right / bottom-right un-fogged-strips visible at high zoom AT ANY canvas-translation magnitude) AND (d) FOG-14/FOG-15 walk-time validation passes (no shimmer / pattern-instability — depends on Plan 03.1-07 Task 3's chosen branch). `DENIED-FINAL` if the diagnostic JSONL stream cannot resolve the failure mode via a follow-up 3.1 iteration. `ITERATING` if a residual failure mode surfaces that has a clear next-iteration fix axis (Plan 03.1-10+)."
    - "On `CONFIRMED-AFTER-FIX`: ROADMAP.md Phase 03.1 row in the Progress table flips to `Complete (HYPOTHESIS CONFIRMED-AFTER-FIX)`. Phase 4 (`Wisp Particles`) status updates from `Blocked on Phase 3.1` to `Pending`. Phase 5 (`Decision Gate`) likewise. STATE.md `Current Position` updates to reflect Phase 3.1 closure. REQUIREMENTS.md FOG-04..07 + FOG-11 + FOG-13 + FOG-14 + FOG-15 + DEBUG-01 statuses flip to `Complete — Verified-by-walk (P03.1-09)`. PERF-07 status updates to `Complete — Re-measured (P03.1-09)` with the actual numbers."
    - "On `DENIED-FINAL`: ROADMAP.md Phase 03.1 row updates to `Complete (HYPOTHESIS DENIED-FINAL)`. Phases 4 + 5 stay `Blocked` (or transition to `Cancelled` per developer choice — flag for user decision). REQUIREMENTS.md FOG-04..07 stay `Falsified-in-production` with updated revision-clause that points at `03.1-FALSIFICATION-3.md`. FOG-13 + FOG-14 + FOG-15 + DEBUG-01 still flip to `Complete — Verified-by-test` (their CI gates ARE GREEN by Plan 03.1-07/08 construction). STATE.md captures the formal POC architectural counter-evidence."
    - "On `ITERATING`: a Plan 03.1-10+ is opened in a follow-up plan-phase invocation, with Walk #3's diagnostic stream as input. THIS plan does NOT author 03.1-10. FOG-13 + FOG-14 + FOG-15 + DEBUG-01 still flip to `Complete — Verified-by-test`. PERF-07 updated with Walk #3 numbers regardless. FOG-04..07 + FOG-11 stay `Falsified-in-production`. Phase 3.1 stays `In Progress (iterating, walk #4 pending)`."
    - "Phase 3 `03-FALSIFICATION.md` AND Phase 3.1 Walk #1 `03.1-FALSIFICATION.md` AND Walk #2 `03.1-FALSIFICATION-2.md` are LEFT UNTOUCHED — no edits, no overwrites. Historical records preserved across iteration cycles."
  artifacts:
    - path: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-3.md"
      provides: "Phase 3.1 Walk #3 falsification document — pre-walk gate-pass evidence + post-walk verdict (`CONFIRMED-AFTER-FIX` | `DENIED-FINAL` | `ITERATING`)"
      min_lines: 80
      contains: "Walk #3"
    - path: ".planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-3.md"
      provides: "Walk #3 evidence transcript — developer's verbatim verdict + interpretation + JSONL grep correlation"
      min_lines: 30
      contains: "Walk Evidence"
  key_links:
    - from: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-3.md"
      to: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-2.md"
      via: "explicit cross-reference in Walk #3's Hypothesis section + Verdict section ('Walk #2 closed iterating-with-partial-progress 2026-05-02; Walk #3 either confirms or denies the layered Plan 03.1-07 + 03.1-08 fix')"
      pattern: "03\\.1-FALSIFICATION-2\\.md"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-3.md"
      via: "FOG-04..07 + FOG-11 + FOG-13 + FOG-14 + FOG-15 + DEBUG-01 + PERF-07 status clauses include `(P03.1-09)` cross-reference; Revisions section appends a 2026-05-XX Walk #3 entry"
      pattern: "P03\\.1-09"
    - from: ".planning/STATE.md"
      to: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-3.md"
      via: "Current Position + Decisions sections capture the Walk #3 verdict outcome"
      pattern: "Phase 3\\.1"
---

<objective>
Run the pre-walk gates, perform the iPhone 17 Pro sideload UAT Walk #3, capture the developer's verdict, and produce `03.1-FALSIFICATION-3.md` — the Phase 3.1 Walk #3 falsification document. Walk #3 is the empirical re-test for the layered Plan 03.1-07 (mechanism-confirmed shader-coordinate-system fix) + Plan 03.1-08 (FOG-13 fog-rect viewport-coverage symmetric compensation) fix bundle. The Walk #3 verdict either reverses the Walk #2 `ITERATING-WITH-PARTIAL-PROGRESS` outcome to `CONFIRMED-AFTER-FIX` (unblocking Phase 4 + Phase 5 of the original roadmap) or strengthens it to `DENIED-FINAL` (terminating the POC's same-Canvas hypothesis) or — per CONTEXT §Iteration policy — captures a fourth `ITERATING` verdict that opens Plan 03.1-10.

Purpose: this is the empirical gate Phase 3.1 needs to close after the Plan 03.1-07 + 03.1-08 layered fix bundle. Plan 03.1-07 closes the B-0 / FOG-14 / FOG-15 mechanism-confirmation gap (whichever branch of the hypothesis triage table Task 2's observation selected). Plan 03.1-08 closes the FOG-13 fog-rect viewport-coverage regression introduced by Plan 03.1-05. Walk #3 is the architectural-evidence gate that flips FOG-04..07 + FOG-11 to Complete (or strengthens the verdict). Per CONTEXT §Iteration policy, there is no hard cap on walk count.

Output: 1 new `03.1-FALSIFICATION-3.md` (the load-bearing artefact); 1 new `03.1-UAT-3.md` (the walk-evidence transcript); REQUIREMENTS.md status updates for FOG-04..07 + FOG-11 + FOG-13 + FOG-14 + FOG-15 + DEBUG-01 + PERF-07; ROADMAP.md Phase 03.1 Progress-table row update; STATE.md Current Position + Decisions updates. Phase 3's `03-FALSIFICATION.md` AND Phase 3.1 Walk #1's `03.1-FALSIFICATION.md` AND Walk #2's `03.1-FALSIFICATION-2.md` ALL left UNTOUCHED.
</objective>

<execution_context>
@C:/Users/oliver/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oliver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-CONTEXT.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-2.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-2.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-VALIDATION.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-06-PLAN.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-07-SUMMARY.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-08-SUMMARY.md

<interfaces>
<!-- Walk #3 mirrors Walk #2 (Plan 03.1-06) shape-for-shape with adjusted hypothesis re-statements. -->

`03.1-FALSIFICATION-2.md` Walk #2 structure (the immediate predecessor template):
1. Title + metadata (`Created`, `Phase`, `Walked on`, `Verdict`)
2. `## Relationship to Walk #2 Verdict` (NEW for Walk #3 — points at 03.1-FALSIFICATION-2.md)
3. `## Hypothesis`
4. `## Falsification Criteria (written BEFORE the walk)` — `### Criterion A`, `### Criterion B`, `### Criterion C — DROPPED`, `### Criterion D — Diagnostic stream`
5. `## Walk Plan`
6. `## Walk Evidence (filled AFTER the walk)`
7. `## Verdict`

Walk #3's `03.1-FALSIFICATION-3.md` mirrors this exactly with these specialisations:
- Title: `# Phase 3.1 Falsification — Walk #3 (Post-Plan-03.1-07 + Plan-03.1-08 Layered Fix)`
- Add `## Relationship to Walk #2 Verdict` section pointing at `03.1-FALSIFICATION-2.md`; summarise the iterating-with-partial-progress outcome and the gap entries (FOG-13, FOG-14, FOG-15, DEBUG-01) closed by Plans 03.1-07 + 03.1-08.
- Hypothesis re-statement: "After the Plan 03.1-07 mechanism-confirmed fix (Branch <X> from the FALSIFICATION-2 sub-section D triage table) AND the Plan 03.1-08 FOG-13 fog-rect viewport-coverage symmetric compensation, the same-Canvas fog now (a) translates with the camera during pan, (b) maintains a stable noise pattern across consecutive paints, (c) keeps the reveal hole co-located with the blue dot, AND (d) covers the entire viewport at any canvas-translation magnitude."
- Criterion A: same thresholds (PERF-07 carry-over): `medianMs ≤ 16`, `p95Ms ≤ 32`, `maxMs ≤ 48`. Walk #1: 4-7×; Walk #2: 3.9-5.4×; Walk #3 should preserve similar headroom.
- Criterion B sub-claims:
  - "Fog visually translates with the camera during pan" (Walk #1 obs 3 / Walk #2 — fixed by Plan 03.1-02 + 03.1-04 + 03.1-05; Walk #3 confirms)
  - "No 'seed changing' noise-seam shimmer during gesture" (Walk #1 obs 2 / Walk #2 — Plan 03.1-07's Branch <X> fix targets this directly)
  - "No reveal-hole offset from blue dot" (Walk #1 obs 4 / Walk #2 — fixed by Plan 03.1-05 FOG-12; Walk #3 re-confirms post-FOG-13)
  - "Fog covers the entire viewport at any zoom — no top-right / bottom-right un-fogged strips" (Walk #2 sub-section C — fixed by Plan 03.1-08 FOG-13)
  - "No white-ellipse during fast pinch-zoom" (already passed Walk #1)
  - "No inversion at any zoom level" (already passed Walk #1)
- Criterion C stays DROPPED.
- Criterion D (Diagnostic stream): `pixelOriginX/Y` track gesture monotonically; if Plan 03.1-07 Branch B-1 was selected, `appliedUOffset` keys are now in [0, 1] fractional range (the FOG-14 precision-budget assertion). `canvasTx/Ty` jumps still occur — and Plan 03.1-08's `canvas.translate(-canvasOffset)` compensates them mechanically (FOG-13 CI gate validates the compensation; the walk's role is corroborating that no viewport-edge un-fogged strips appear at any canvas-translation magnitude).

`03.1-UAT-3.md` mirrors `03.1-UAT-2.md` shape-for-shape: metadata block + walk protocol + walk evidence (frame-delta + SDF + fog-transform rollups) + subjective verdict + verdict + deviations + summary + (optional) gaps section if Walk #3 surfaces residual failure modes.

The pre-walk gate sequence is identical to Plan 03.1-06 Task 1 (six gates: flutter test / analyze / format / tool tests / CI green / IPA download).

The walk shape is identical to Plan 03.1-06: ≥ 5 minutes Melun + ≥ 10 combined gestures + ≥ 3 recenter taps + LOG-04 Mail share. Pre-walk shader-sanity gate now also includes a Plan 03.1-07 debug-spiral toggle live-test on iPhone (toggle ON, observe spiral renders correctly; toggle OFF, observe production fog returns).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the pre-walk `03.1-FALSIFICATION-3.md` skeleton + run the pre-walk gate sequence</name>
  <files>.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-3.md, .planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-3.md</files>
  <action>
    Create `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-3.md`. Mirror the structural shape of `03.1-FALSIFICATION-2.md` with the specialisations described in the §Interfaces block above. Use the executor's actual current date for the `Created:` field; leave `Walked on:` and `Verdict:` as `TBD` until Task 2 fills them.

    Top-of-file metadata block:
    ```markdown
    # Phase 3.1 Falsification — Walk #3 (Post-Plan-03.1-07 + Plan-03.1-08 Layered Fix)

    **Created:** 2026-05-XX  (executor: fill in real date)
    **Phase:** 03.1-fix-fog-pan-translation
    **Walked on:** TBD  (filled post-walk: iPhone 17 Pro, central Melun, CI run `<RUN_ID>`, SHA `<SHA>`)
    **Verdict:** TBD  (filled post-walk: `CONFIRMED-AFTER-FIX` | `DENIED-FINAL` | `ITERATING — see 03.1-10+`)

    ## Relationship to Walk #2 Verdict

    Walk #2 (Plan 03.1-06, 2026-05-02) closed `ITERATING-WITH-PARTIAL-PROGRESS`. Three of four targeted fix-axes confirmed (PERF-07 re-validated, FOG-12 reveal-hole alignment confirmed, UX-01 back button confirmed); one fix-axis (FOG-11 SHADER-MODULO-WRAP) landed in CI but did NOT close the user-observable shimmer; one fix introduced a NEW regression (Plan 03.1-05's clip-path-only compensation left the fog-rect viewport-coverage gap). Three new requirements opened: FOG-13 (fog-rect symmetric compensation), FOG-14 (higher-fidelity noise-pattern stability test), FOG-15 (mechanism-confirmed shader-coordinate-system fix); plus DEBUG-01 (debug-spiral shader infrastructure) added by Plan 03.1-07 as the cleavage tool for Walk #2's UNKNOWN-mechanism shimmer (developer's "rotating 90° / translated fast repetitively" was speculation about an unresolvable visual phenomenon).

    Plan 03.1-07 closed FOG-14 + FOG-15 + DEBUG-01 via the debug-spiral observation step (Task 2 verbal `observation: <Branch>`) + the mechanism-specific fix (Task 3 Branch <A/B-1/B-2/B-3/C>).
    Plan 03.1-08 closed FOG-13 via the `canvas.translate(-canvasOffset)` symmetric compensation in `_FogPainter.paint()`.

    Walk #1 + Walk #2 records (`03.1-FALSIFICATION.md`, `03.1-FALSIFICATION-2.md`, `03.1-UAT.md`, `03.1-UAT-2.md`) AND Phase 3 record (`03-FALSIFICATION.md`) ALL stand as historical record — UNTOUCHED. Walk #3's verdict is a SEPARATE artefact in this file.

    ## Hypothesis

    After the Plan 03.1-07 mechanism-confirmed fix (Branch <X> per Task 2 observation — fill in actual branch from 03.1-07-SUMMARY.md) AND the Plan 03.1-08 FOG-13 fog-rect viewport-coverage symmetric compensation (`canvas.translate(-canvasOffset)` at top of `_FogPainter.paint()`), the same-Canvas fog now: (a) translates with the camera during pan; (b) maintains a stable noise pattern across consecutive paints (FOG-14 CI gate validates); (c) keeps the reveal hole co-located with the blue dot during pan/zoom (FOG-12 retained); (d) covers the entire viewport at any canvas-translation magnitude (FOG-13 CI gate validates).

    Re-stated against the original POC core value: the fog-of-war stays perfectly locked to the map during pan, zoom, and combined gestures on a sideloaded iOS build. **CONFIRMED-AFTER-FIX** means the Phase 3 hypothesis is reinstated and the POC ports back to MirkFall with all of Plan 03.1-02 + 03.1-04 + 03.1-05 + 03.1-07 + 03.1-08 fixes included; **DENIED-FINAL** means even the layered fix bundle doesn't deliver fog-camera-tracking parity; **ITERATING** means a residual failure mode surfaced that has a clear next-iteration fix axis (Plan 03.1-10+).

    ## Falsification Criteria (written BEFORE the walk)

    ### Criterion A — Frame-delta probe thresholds (PERF-07 re-validation, quantitative)

    Same thresholds as Walk #1 + #2: `medianMs ≤ 16`, `p95Ms ≤ 32`, `maxMs ≤ 48` across ≥ 10 deliberate combined gestures. Walk #1: 4-7×; Walk #2: 3.9-5.4×; Walk #3 should preserve similar headroom (Plan 03.1-08's `canvas.translate` adds ~1 cycle per paint — negligible against the budget).

    ### Criterion B — Subjective visual lock (PERF-05 carry-over, re-tested)

    Developer's verbal verdict at end of walk:
    - **Fog visually translates with the camera during pan** (re-confirmed)
    - **No 'seed changing' noise-seam shimmer during gesture** (Walk #2 fail — Plan 03.1-07 Branch <X> targets this)
    - **No reveal-hole offset from blue dot during gesture** (re-confirmed)
    - **Fog covers the entire viewport at any zoom — no top-right / bottom-right un-fogged strips** (Walk #2 fail — Plan 03.1-08 FOG-13 targets this)
    - No white-ellipse during fast pinch-zoom (already passed Walks #1 + #2)
    - No inversion at any zoom level (already passed Walks #1 + #2)

    ### Criterion C — DROPPED

    Stays dropped per Phase 3 + roadmap lock.

    ### Criterion D — Diagnostic stream confirms the layered fix

    Per the `Logger('infrastructure.mirk.fog_transform')` 1-Hz JSONL rollups:
    - **CONFIRMED-AFTER-FIX:** `pixelOriginX/Y` track gesture monotonically. If Plan 03.1-07 Branch B-1 selected: `appliedUOffset` keys (which preserve the JSONL field name for back-compat) are now in [0, 1] fractional range (FOG-14 precision-budget assertion mechanically validated). `canvasTx/Ty` jumps still occur — Plan 03.1-08's `canvas.translate` compensates them mechanically (FOG-13 CI gate validates).
    - **DENIED-FINAL pattern:** the diagnostic stream shows the layered fix landed mechanically AT EVERY level (pixelOrigin tracks, uOffset within precision budget, canvas.translate compensates rect-cover) AND the user-observable shimmer / coverage-gap STILL persists — root cause is even deeper than the coordinate-system + rect-compensation; architecture is unfixable in `flutter_map 7.0.2`.
    - **ITERATING pattern:** subjective verdict surfaces a residual failure mode (e.g., shimmer reduced but not eliminated; new failure mode surfaces at extreme zoom). The diagnostic stream points at the next fix axis; Plan 03.1-10 is opened.

    ## Walk Plan

    Identical to Plan 03.1-06 (Walk #2) — same theatre, duration, gestures, HUD, post-walk Mail-share. Only the post-fix SHA + CI run number differ. Pre-walk shader-sanity gate now ALSO includes a debug-spiral toggle live-test on the sideloaded build (Plan 03.1-07 DEBUG-01 walk-time validation).

    - **Where:** Central Melun (lat 48.5397, lon 2.6553 area).
    - **Duration:** ≥ 5 minutes continuous walk with the IPA running and the device awake.
    - **Gestures:** ≥ 10 deliberate combined pinch-zoom-and-pan gestures, ≥ 3 recenter FAB taps.
    - **Pre-walk gate (unit tests):** `flutter test` GREEN (FOG-09, FOG-10, FOG-11, FOG-12, FOG-13, FOG-14, PERF-08, UX-01, DEBUG-01, plus Plan 03.1-07 chosen-branch test, plus all Phase 1+2+3 retained); `flutter analyze --fatal-infos` 0 warnings; `dart format --line-length 160 --set-exit-if-changed .` clean; `dart test tool/test/` GREEN.
    - **Pre-walk gate (CI):** `gh run list --branch main --limit 5` shows `success` for the post-Plan-03.1-07 + 03.1-08 SHA; `gh run view <RUN_ID>` confirms `gates`, `build-android`, `build-ios` all green; IPA downloaded via `gh run download <RUN_ID> --name ios-unsigned --dir .uat-tmp/`.
    - **Pre-walk gate (sideload):**
      1. Open `/sanity` route — confirm production fog renders with circular reveal hole (debug-spiral toggle OFF default).
      2. Toggle the debug-spiral switch ON — confirm the spiral pattern renders (DEBUG-01 walk-time validation).
      3. Toggle OFF — confirm production fog returns.
      4. Confirm the `Icons.arrow_back` AppBar leading button still works (UX-01 retained).
      5. Confirm zero shader-compile exceptions in the FileLogger output.
    - **In-app HUD during walk:** FpsCounterOverlay (top:8, right:8) + MapCompass (top:56, right:8) + FrameDeltaProbeOverlay (top:104, right:8).
    - **Post-walk:** share the session log file via Mail (LOG-04 round-trip); paste the relevant frame-delta + SDF + fog-transform JSONL lines into the Walk Evidence section below; write the subjective verdict (Criterion B) by hand; verbal `approved` is the gate (or `denied:` / `iterating:` for failure / next-iteration paths).

    ## Walk Evidence (filled AFTER the walk by Plan 03.1-09 Task 2)

    ### Pre-walk shader-sanity gate (`/sanity` route + UX-01 back-button validation + DEBUG-01 toggle live-test)
    _TBD — filled post-walk._

    ### Probe rollup (frame-delta JSONL — `Logger('infrastructure.mirk.frame_delta')`)
    _TBD — filled post-walk. Expected: median ≤ 16 ms, p95 ≤ 32 ms, max ≤ 48 ms across ≥ 10 combined gestures (PERF-07 re-validation; Walk #1 4-7×; Walk #2 3.9-5.4×; Walk #3 should preserve similar headroom)._

    ### SDF rebuild rollup (`Logger('infrastructure.mirk.sdf')` — PERF-08 status)
    _TBD — filled post-walk. PERF-08 was falsified-by-walk-2 (rebuild rate 1-121/sec median 68/sec); Walk #3 re-measures whether the rate changed (no Plan 03.1-07/08 work targets PERF-08 — likely unchanged; the question is whether PERF-08 stays falsified or whether the layered fix incidentally shifts the gesture pattern's hash trajectory)._

    ### Fog-transform rollup (`Logger('infrastructure.mirk.fog_transform')`)
    _TBD — filled post-walk. Expected: `pixelOriginX/Y` monotonic; if Plan 03.1-07 Branch B-1 selected, `appliedUOffset` (raw-pixel back-compat key) values now in [0, 1] fractional range (FOG-14 precision-budget); `canvasTx/Ty` may still jump non-zero but the FOG-13 + FOG-12 CI gates validate the painter's compensation_

    ### FPS observations (PERF-03 — coarse FpsCounterOverlay reading)
    _TBD — filled post-walk._

    ### Subjective verdict (Criterion B)
    _TBD — filled post-walk. Developer's verbatim words + interpretation paragraph mapping each Criterion B sub-claim to PASS/FAIL._

    ## Verdict

    - [ ] Criterion A passed? — TBD post-walk.
    - [ ] Criterion B passed? — TBD post-walk.
    - [ ] Criterion D passed? — TBD post-walk.

    **Outcome:** TBD.

    **MirkFall migration recommendation:** TBD.
    ```

    Then create `.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-3.md` as the thin Walk #3 transcript skeleton — mirror `03.1-UAT-2.md`'s structure with metadata block + Walk Protocol + empty `## Walk Evidence` section + `## Verdict` placeholder + `## Deviations / Surprises` placeholder + `## Summary` placeholder.

    NOW execute the pre-walk gate sequence:
    1. `flutter test` (must report 0 failures, 0 reds — Phase 1+2+3 retained tests + new FOG-09, FOG-10, FOG-11, FOG-12, FOG-13, FOG-14, PERF-08, UX-01, DEBUG-01, plus Plan 03.1-07 chosen-branch test all GREEN; the FOG-09 vacuous-test guard stays SKIPPED)
    2. `flutter analyze --fatal-infos` (must exit 0)
    3. `dart format --line-length 160 --set-exit-if-changed .` (must exit 0)
    4. `dart test tool/test/` (must report all GREEN)
    5. `gh run list --branch main --limit 5` (capture the post-Plan-03.1-07 + 03.1-08 SHA + run ID; the run must be `success`)
    6. `gh run view <RUN_ID>` (confirm `gates`, `build-android`, `build-ios` all `success`)
    7. `gh run download <RUN_ID> --name ios-unsigned --dir .uat-tmp/` (produces `.uat-tmp/mirk-poc-debug-unsigned.ipa`)

    If ANY pre-walk gate fails, STOP and report. Do NOT proceed to Task 2 until every gate is green.
  </action>
  <verify>
    <automated>flutter test 2>&amp;1 | tail -3 &amp;&amp; flutter analyze --fatal-infos 2>&amp;1 | tail -3 &amp;&amp; dart format --line-length 160 --set-exit-if-changed . 2>&amp;1 | tail -3 &amp;&amp; dart test tool/test/ 2>&amp;1 | tail -3 &amp;&amp; gh run list --branch main --limit 1 2>&amp;1 | tail -3</automated>
    <file_must_exist>.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-3.md</file_must_exist>
    <file_must_exist>.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-3.md</file_must_exist>
    <file_must_exist>.uat-tmp/mirk-poc-debug-unsigned.ipa</file_must_exist>
  </verify>
  <done>
    - All 4 local pre-walk gates exit 0 (test / analyze / format / tool-tests).
    - CI run on the post-Plan-03.1-07 + 03.1-08 SHA on `main` is `success` (gates + build-android + build-ios all green).
    - `.uat-tmp/mirk-poc-debug-unsigned.ipa` exists and is non-empty (≥ 5 MB).
    - `03.1-FALSIFICATION-3.md` exists with all top-level sections + the Relationship-to-Walk-2 section. Walk Evidence + Verdict bodies read `TBD`.
    - `03.1-UAT-3.md` exists as a thin transcript skeleton.
    - `git status` shows the existing `03.1-FALSIFICATION.md` + `03.1-FALSIFICATION-2.md` + `03.1-UAT.md` + `03.1-UAT-2.md` UNCHANGED (Walk #1 + Walk #2 records preserved).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: iPhone 17 Pro sideload UAT Walk #3 + post-walk verbal verdict capture</name>
  <files>.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-3.md, .planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-3.md</files>
  <action>
    Hand off to the developer for the manual UAT Walk #3 on iPhone 17 Pro. The full procedure is documented in the checkpoint-specific `<what-built>`, `<how-to-verify>`, and `<resume-signal>` sections — those are the executor-facing instructions. Claude pauses execution, presents the checkpoint to the developer, and waits for the resume signal containing the walk evidence + verdict. After the resume signal is received, Claude proceeds to Task 3 (verdict authoring + REQUIREMENTS/ROADMAP/STATE updates).
  </action>
  <verify>
    <automated>MISSING — PERF-07 + Criterion B + Criterion D (subjective + diagnostic-stream pattern matching) are inherently manual UAT walk gates per 03.1-VALIDATION.md Manual-Only Verifications table; no automated proxy exists. The developer&#39;s resume-signal IS the verification.</automated>
  </verify>
  <done>
    Developer returns with one of: (a) verbal `approved` — fog tracks camera during pan + no shimmer + no reveal-hole offset + no viewport-edge un-fogged strips; CONFIRMED-AFTER-FIX. (b) `denied: <verbatim>` — describe the failure mode for diagnostic JSONL correlation. (c) `iterating: <verbatim>` — same as denied: but the developer chooses to iterate (Plan 03.1-10+). Plus the post-walk session log shared via Mail (LOG-04 round-trip).
  </done>
  <what-built>
    The Plan 03.1-07 + 03.1-08 layered fix is software-complete:

    - **Plan 03.1-07 (DEBUG-01 + FOG-14 + FOG-15):** A debug-spiral shader infrastructure ships at `/sanity` (toggle default OFF; permanent diagnostic tool). Plan 03.1-07 Task 2's user observation identified the shimmer mechanism (Branch <X> from `03.1-07-SUMMARY.md`); Plan 03.1-07 Task 3 applied the mechanism-specific fix:
      - Branch B-1 (fp32 precision): `_FogPainter.paint()` decomposes pixelOrigin into integer + fractional CPU-side; forwards only fractional to shader. Shader's `fract(uPixelOrigin / uResolution)` becomes a no-op pass-through.
      - Branch B-2 (DPR scaling): `_FogPainter.paint()` multiplies `uResolution` by `MediaQuery.devicePixelRatio` so the shader's coordinate system matches device-pixel `FlutterFragCoord`.
      - Branch B-3 (tile-period mismatch): shader's `fract()` period derives from `uResolution / max(uScaleFar, uScaleMid, uScaleNear)` — aligned with noise-tile period, not viewport.
      - (Branches A or C document the path forward without modifying production.)
    - **Plan 03.1-08 (FOG-13):** `_FogPainter.paint()` calls `canvas.translate(-canvasOffset.dx, -canvasOffset.dy)` immediately after `canvas.clipPath(clipPath)` — symmetric canvas-translation compensation that closes the Walk #2 sub-section C regression (top-right + bottom-right un-fogged-strips at high zoom, when canvasTx/Ty reach 757/319 px magnitudes).

    CI gates green for new tests:
    - DEBUG-01 (`shader_sanity_screen_test.dart` augmented) — debug-spiral toggle behaviour
    - FOG-13 (`fog_rect_viewport_coverage_test.dart`) — canvas.translate compensation at extreme canvasTx/Ty
    - FOG-14 (test name depends on Plan 03.1-07 Branch — fp32 precision OR DPR-aware uResolution OR tile-period-aware fract-period)
    - FOG-15 (test name depends on Plan 03.1-07 Branch — chosen-axis assertion)

    Existing tests still GREEN: FOG-04 structural, FOG-07 KEYSTONE, FOG-09 transform-equality, FOG-10 fog_transform_logger, FOG-11 smooth-noise, FOG-12 canvas-frame-alignment, PERF-08 bbox-quantisation, UX-01 sanity back button.

    Pre-walk gates (Task 1): all 6 GREEN. The IPA at `.uat-tmp/mirk-poc-debug-unsigned.ipa` is ready for sideload.
  </what-built>
  <how-to-verify>
    1. Open SideStore on iPhone 17 Pro. Refresh certificate if needed. Install or refresh `.uat-tmp/mirk-poc-debug-unsigned.ipa`.
    2. Launch `mirk-poc-debug` on iPhone 17 Pro. Allow GPS via the rationale screen → "Allow While Using App".
    3. **Pre-walk sanity gate (production fog + UX-01 + DEBUG-01 walk-time validation):** tap the science (`Icons.science`) button in the AppBar to navigate to `/sanity`.
       - Confirm the production fog renders with circular reveal hole.
       - Confirm an `Icons.arrow_back` button appears in the leading slot (UX-01 retained).
       - **NEW:** confirm a `Switch.adaptive` toggle for "Debug Spiral" appears in the AppBar actions (DEBUG-01).
       - Toggle the debug spiral ON. Confirm the digit-atlas spiral pattern renders (DEBUG-01 walk-time validation — the lab-verified toggle works on real iOS).
       - Toggle OFF. Confirm production fog returns (Plan 03.1-07 Task 1 + 03.1-08 fix didn't regress production rendering).
       - Tap the back button. Confirm it returns to `/map`.
    4. **Walk the route:** ≥ 5 minutes continuous through central Melun (same theatre as Walks #1 + #2). During the walk:
       - Perform ≥ 10 deliberate combined pinch-zoom-and-pan gestures.
       - Perform ≥ 3 recenter FAB taps.
       - Watch the FrameDeltaProbeOverlay (top:104, right:8) — note approximate `med` / `p95` / `max` values during combined gestures.
       - **The KEY observations** (the four Walk #2 failure modes the layered fix targets):
         - **Q1: Does the noise pattern stay STABLE during gesture, or does it shimmer / "seed change"?** (Walk #2 obs 2 — Plan 03.1-07's Branch <X> targets this)
         - **Q2: Does the fog cover the ENTIRE viewport at high zoom — no top-right / bottom-right un-fogged strips?** (Walk #2 obs 4 — Plan 03.1-08 FOG-13 targets this)
         - **Q3: Is the reveal hole co-located with the blue dot during pan/zoom?** (Walk #2 obs 3 — re-confirmation; Plan 03.1-05 retained)
         - **Q4: Does the fog visually translate with the map during pan?** (Walk #2 obs 1 — re-confirmation; Plans 03.1-02 + 03.1-04 retained)
    5. **Post-walk:** tap the share button in the AppBar → pick Mail → send the session log to your own email → save the attached `yyyymmdd_hhmmss_logs.txt` from your inbox.
    6. **Capture the verdict:** describe in your own words whether each of the four Walk #2 failure modes is now resolved. Single-word `approved` is the gate for `CONFIRMED-AFTER-FIX`. Any residual failure mode (partial shimmer, partial un-fogged-strip at extreme magnitudes, new failure mode) should be described — that description feeds the diagnostic JSONL grep step.
    7. **Post-walk grep correlation** (executor runs after the verbal verdict is captured):
       - `grep "infrastructure.mirk.frame_delta" <log-file> > .uat-tmp/walk3_frame_delta.jsonl`
       - `grep "infrastructure.mirk.sdf" <log-file> > .uat-tmp/walk3_sdf.jsonl`
       - `grep "infrastructure.mirk.fog_transform" <log-file> > .uat-tmp/walk3_fog_transform.jsonl`
       - For each line, parse the JSON, extract `epochSecond`, produce a per-second composite row. Pipe into `03.1-FALSIFICATION-3.md`'s Walk Evidence subsections.

    **Expected `CONFIRMED-AFTER-FIX` evidence pattern:**
    - Walk evidence: developer says `approved` — fog tracks camera; no shimmer; reveal hole co-located with blue dot; viewport entirely fogged at any zoom.
    - `frame_delta` rollup: PERF-07 thresholds satisfied with similar headroom to Walks #1 + #2.
    - `fog_transform` rollup: `pixelOriginX/Y` monotonic; if Plan 03.1-07 Branch B-1, `appliedUOffset` keys in [0, 1] fractional range; `canvasTx/Ty` jumps still occur but FOG-13 CI gate validates the canvas.translate compensation.
    - `sdf` rollup: PERF-08 status unchanged from Walk #2 (no Plan 03.1-07/08 work targets it; remains a candidate for a future plan if the developer chooses).

    **DENIED-FINAL or ITERATING patterns** are documented in `03.1-FALSIFICATION-3.md` §Criteria — pattern matching uses the diagnostic stream to identify residual fix axes (or, in DENIED case, the architectural-counter-evidence finding).
  </how-to-verify>
  <resume-signal>
    Type ONE of:
    - `approved — pattern-stable + viewport-fully-covered + reveal-hole-aligned + fog-translates` — all four Q1-Q4 sub-claims confirmed by developer (Q1 noise pattern stable / no shimmer; Q2 viewport entirely fogged at any zoom — no top-right/bottom-right un-fogged strips; Q3 reveal hole co-located with blue dot during pan/zoom; Q4 fog visually translates with map during pan); CONFIRMED-AFTER-FIX. Executor proceeds to Task 3 (Branch A) and flips FOG-04..07 + FOG-11 + PERF-07 + FOG-13/14/15 + DEBUG-01 to Complete. If any single sub-claim fails, use `denied: <which Qx failed + verbatim>` or `iterating: <which Qx failed + verbatim>` instead.
    - `denied: <verbatim description of the residual failure mode>` — describe what went wrong. Executor opens the post-walk JSONL grep results, correlates against the failure description, and reports back: either (a) recommend a Plan 03.1-10+ follow-up fix attempt with a specific diagnostic-anchored hypothesis, or (b) recommend `DENIED-FINAL` if the diagnostic evidence shows the failure is structural (Phase 3.2 alternative-renderer pivot conversation).
    - `iterating: <verbatim description>` — same as `denied:` shape but the developer chooses to iterate per CONTEXT §Iteration policy. Executor opens a Plan 03.1-10 in a follow-up plan-phase invocation.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Author the Walk #3 verdict — fill `03.1-FALSIFICATION-3.md` Walk Evidence + Verdict; update REQUIREMENTS.md / ROADMAP.md / STATE.md</name>
  <files>.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-3.md, .planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-3.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
    Branch on the developer's verdict from Task 2:

    **Branch A — `approved` → CONFIRMED-AFTER-FIX:**

    1. Fill `03.1-FALSIFICATION-3.md` Walk Evidence subsections with post-walk grep correlation. Mirror Plan 03.1-06 Task 3 Branch A (which never executed — Walk #2 was iterating-with-partial-progress; use the structure from `03.1-06-PLAN.md` Branch A action body verbatim with Walk #3 specifics).

    2. Fill `03.1-FALSIFICATION-3.md` Verdict section:
       ```markdown
       ## Verdict

       - [x] Criterion A passed? — **YES.** Frame-delta probe rolling rollup over ≥ 10 combined gestures: median = {N} ms (≤ 16 ✓), p95 = {N} ms (≤ 32 ✓), max = {N} ms (≤ 48 ✓). PERF-07 re-validated post-Plan-03.1-07+08.
       - [x] Criterion B passed? — **YES.** Developer's verbatim verdict: *"<verbatim>"*. All four Walk #2 failure modes resolved: fog translates with camera ✓; no shimmer ✓; reveal hole co-located with blue dot ✓; viewport entirely fogged at any zoom (no top-right / bottom-right un-fogged strips) ✓.
       - [x] Criterion D passed? — **YES.** Diagnostic stream confirms the layered fix.

       **Outcome:** **CONFIRMED-AFTER-FIX.**

       The same-Canvas fog hypothesis is **reinstated** with the layered Plan 03.1-02 + 03.1-04 + 03.1-05 + 03.1-07 + 03.1-08 fix bundle. Phase 3's `DENIED 2026-05-01` verdict and Phase 3.1 Walks #1 + #2's `ITERATING-WITH-MAJOR/PARTIAL-PROGRESS` verdicts are converted to `CONFIRMED-AFTER-FIX` after Walk #3.

       **MirkFall migration recommendation:** **PORT BACK** with the full layered fix bundle:
       1. `_FogPainter.paint()` derives `pixelOrigin` from `camera.pixelOrigin` (Plan 03.1-02).
       2. `atmospheric_fog.frag` slot 3..4 uniform `uPixelOrigin` (renamed from `uOffset`) with per-fragment `fract()` modulo (Plan 03.1-04).
       3. `_FogPainter.paint()` reads `canvas.getTransform()` and forwards `Offset(matrix[12], matrix[13])` to `computeFogClipPath(canvasOffset: ...)` (Plan 03.1-05).
       4. `SdfCache._hash(...)` quantises viewport bbox to 1e-4 lat/lon (Plan 03.1-05; performance-only).
       5. Plan 03.1-07 Branch <X> mechanism-specific fix (per `03.1-07-SUMMARY.md`).
       6. `_FogPainter.paint()` calls `canvas.translate(-canvasOffset)` immediately after `canvas.clipPath(clipPath)` (Plan 03.1-08).
       7. (Optional) The DEBUG-01 debug-spiral infrastructure is OPTIONAL for port-back — it is permanent diagnostic tooling, useful but not required for production fog correctness.

       The MirkFall donor `_FogPainter.paint()` call site MUST be reviewed against ALL six production-impacting changes at port-back time.
       ```

    3. Update `.planning/REQUIREMENTS.md`:
       - FOG-04, FOG-05, FOG-06, FOG-07: change `[ ]` → `[x]`. Update inline status from `Falsified-in-production` to `Complete — Verified-by-walk (P03.1-09 CONFIRMED-AFTER-FIX 2026-05-XX after layered Plan 03.1-02+04+05+07+08 fix bundle)`.
       - FOG-11: change `[ ]` → `[x]`; status `Complete — Verified-by-walk (P03.1-09 — Plan 03.1-07 Branch <X> mechanism fix landed)`.
       - FOG-13: status augmented `Complete — Verified-by-test + walk (P03.1-08 unit + P03.1-09 walk-time confirmation)`.
       - FOG-14: status augmented `Complete — Verified-by-test + walk (P03.1-07 Task 3 unit + P03.1-09 walk-time confirmation)`.
       - FOG-15: status augmented `Complete — Verified-by-test + walk (P03.1-07 Task 3 unit + P03.1-09 walk-time confirmation; Branch <X>)`.
       - DEBUG-01: status augmented `Complete — Verified-by-test + walk (P03.1-07 Task 1 unit + P03.1-09 walk-time validation; permanent diagnostic infrastructure retained in codebase)`.
       - PERF-07: status `Complete — Re-measured (P03.1-09)`. Include actual Walk #3 numbers.
       - PERF-08: stays falsified-by-walk-2; or update to "stays falsified" with a note that no Plan 03.1-07/08 work targeted it; future-plan candidate.
       - PERF-03 / PERF-04 / PERF-05: update statuses to reflect re-measurement at Walk #3.
       - Append a new entry to `## Revisions`:
         ```
         - **2026-05-XX (Phase 3.1 closure, Plan 03.1-09 SUMMARY — HYPOTHESIS CONFIRMED-AFTER-FIX):** Plan 03.1-09 sideload UAT Walk #3 on iPhone 17 Pro against CI run `<RUN_ID>` (SHA `<SHA>`) over central Melun delivered a **CONFIRMED-AFTER-FIX** verdict after the layered Plan 03.1-02 + 03.1-04 + 03.1-05 + 03.1-07 + 03.1-08 fix bundle. Developer's verbatim words: *"<verbatim>"*. All four Walk #2 failure modes resolved: fog visually translates with camera; no 'seed changing' noise-seam shimmer (Plan 03.1-07 Branch <X> fix); reveal hole co-located with blue dot (Plan 03.1-05 retained); viewport entirely fogged at any zoom (Plan 03.1-08 FOG-13 fix). FOG-04..07 + FOG-11 flip from Falsified-in-production back to Complete — Verified-by-walk. FOG-13 + FOG-14 + FOG-15 + DEBUG-01 verified-by-walk in addition to verified-by-test. PERF-07 re-measured. MirkFall migration recommendation: **PORT BACK** with the full 6-change layered fix bundle (5 architectural + 1 perf change documented in `03.1-FALSIFICATION-3.md`). Phase 4 (wisp particles) and Phase 5 (decision gate) **UNBLOCKED**. The new FOG-09..15 + DEBUG-01 + PERF-08 + UX-01 CI gates prevent regression to ANY of the documented Walk #1/2 failure modes without a sideload walk. Evidence: `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-3.md` + `03.1-UAT-3.md`. Phase 3.1 closes with all requirement IDs Complete (FOG-09..15, PERF-07, PERF-08-or-deferred, UX-01, DEBUG-01) plus the 4 retroactively-Complete (FOG-04..07).
         ```

    4. Update `.planning/ROADMAP.md`:
       - In `## Progress` table: Phase 03.1 row → `Complete (HYPOTHESIS CONFIRMED-AFTER-FIX)`, plans `9/9`, completed `2026-05-XX`.
       - In Phase 4 + Phase 5 rows: change status from `Blocked on Phase 3.1` to `Pending`.
       - In Phase 3 overview line: append `Phase 3.1 reversed the verdict to CONFIRMED-AFTER-FIX 2026-05-XX after a layered Plan 03.1-02+04+05+07+08 fix bundle (uniform rename + per-fragment modulo + clip-path shift compensation + mechanism-confirmed fix + canvas.translate symmetric compensation).`
       - In Phase 4 + Phase 5 overview lines: remove the `**BLOCKED on Phase 3.1**` clause.

    5. Update `.planning/STATE.md`:
       - Frontmatter: `stopped_at: Phase 3.1 CLOSED with HYPOTHESIS CONFIRMED-AFTER-FIX (Walk #3 2026-05-XX); Phase 4 + 5 UNBLOCKED.`; `last_activity` accordingly.
       - Bump `progress.completed_phases` from 3 to 4.
       - `## Current Position`: update to reflect Phase 3.1 closure + Phase 4 unblock.
       - `### Decisions`: append a Phase 3.1 closure entry (mirror the Plan 03.1-06 entry shape).
       - `### Blockers/Concerns`: change the `PHASE-3.1-CAMERA-TRANSFORM-WALK2 (NEW ...)` entry to `PHASE-3.1-CAMERA-TRANSFORM-WALK2 (RESOLVED 2026-05-XX by Plan 03.1-09 Walk #3 CONFIRMED-AFTER-FIX after layered Plan 03.1-07+08 fix bundle):` and strike-through the original wording. Also resolve `CI-TEST-VS-WALK-SYMPTOM-MISMATCH` with a note pointing at FOG-14's higher-fidelity proxy (Plan 03.1-07 Task 3 fix).

    6. **DO NOT TOUCH** `.planning/phases/03-fog-of-war-the-hypothesis/03-FALSIFICATION.md` (Phase 3 historical record) OR `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION.md` (Walk #1) OR `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-2.md` (Walk #2). Verify via `git status` that all three files are unchanged.

    **Branch B — `denied:` → DENIED-FINAL:**

    Mirror Plan 03.1-06 Task 3 Branch B treatment with Walk #3 specifics. Walk Evidence filled with failure-mode evidence + diagnostic-stream pattern matching. Verdict captures DENIED-FINAL outcome with MirkFall recommendation `DO NOT PORT BACK` and Phase 3.2 conversation hand-off. REQUIREMENTS.md FOG-04..07 + FOG-11 stay `Falsified-in-production` with updated revision-clause appending Walk #3 evidence. ROADMAP.md Phase 03.1 row → `Complete (HYPOTHESIS DENIED-FINAL)`. Phases 4 + 5 stay `Blocked` (or transition to `Cancelled` per developer choice — flag in SUMMARY for user decision). FOG-13, FOG-14, FOG-15, DEBUG-01 stay GREEN at the test level.

    **Branch C — `iterating:` → defer Plan 03.1-10 to follow-up:**

    Mirror Plan 03.1-06 Task 3 Branch C treatment with Walk #3 specifics. Walk Evidence filled with post-walk grep correlation. Verdict: `**Outcome:** ITERATING — see Plan 03.1-10+ for the next fix attempt.`. Do NOT update REQUIREMENTS.md / ROADMAP.md / STATE.md to closure state. Mark Phase 3.1 as `In Progress (iterating, walk #4 pending)` in Progress table. FOG-13 + FOG-14 + FOG-15 + DEBUG-01 still flip to Complete — Verified-by-test (their CI gates ARE GREEN). FOG-04..07 + FOG-11 stay Falsified-in-production. PERF-07 updated with Walk #3 numbers regardless. Plan 03.1-10 is created via a follow-up `/gsd:plan-phase 3.1 --gaps` invocation.

    For all three branches: commit all updates via the standard plan-execution commit pattern. Cross-reference Plan 03.1-09 in commit messages. Do NOT commit `.uat-tmp/` (gitignored) or post-walk `.jsonl` extracts (transient walk artefacts).
  </action>
  <verify>
    <automated>flutter test 2>&amp;1 | tail -3 &amp;&amp; flutter analyze --fatal-infos 2>&amp;1 | tail -3 &amp;&amp; node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" frontmatter validate .planning/phases/03.1-fix-fog-pan-translation/03.1-09-PLAN.md --schema plan</automated>
    <file_must_exist>.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-3.md</file_must_exist>
    <file_must_exist>.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-3.md</file_must_exist>
  </verify>
  <done>
    - `03.1-FALSIFICATION-3.md` Walk Evidence + Verdict sections are filled (no `TBD` strings remaining for the chosen branch — Branches A or B; Branch C explicitly leaves them flagged for Plan 03.1-10).
    - `03.1-UAT-3.md` carries the Walk #3 transcript.
    - REQUIREMENTS.md statuses reflect the verdict outcome (Branch A: all Complete; Branch B: FOG-04..07 + FOG-11 Falsified-final + new IDs Complete; Branch C: in-progress for FOG-04..07 + FOG-11 + new IDs Complete).
    - ROADMAP.md Progress table Phase 03.1 row reflects the verdict; Phase 4 + Phase 5 statuses updated per branch.
    - STATE.md Current Position + Decisions + Blockers/Concerns updated.
    - Walk #1 + Walk #2 + Phase 3 historical records UNCHANGED (verified via `git status`).
    - `flutter test` (full suite) GREEN — closure docs do not touch any production code.
    - Plan frontmatter validation: `{ valid: true }`.
  </done>
</task>

</tasks>

<verification>
After all three tasks complete:
- `03.1-FALSIFICATION-3.md` is filled top-to-bottom with the Walk #3 verdict and walk evidence (or marked `iterating` per Branch C).
- The Phase 3 historical record (`03-FALSIFICATION.md`) AND Walks #1 + #2 historical records (`03.1-FALSIFICATION.md`, `03.1-FALSIFICATION-2.md`) are ALL byte-identical to their pre-Plan-03.1-09 states.
- REQUIREMENTS.md, ROADMAP.md, STATE.md all reflect the Walk #3 verdict outcome consistently.
- `flutter test` (full suite) is GREEN — Plan 03.1-09 closure docs do not touch any production code.
- Plan frontmatter validation returns `{ valid: true }`.
</verification>

<success_criteria>
- The iPhone 17 Pro Walk #3 produces a verdict (verbal `approved` / `denied:` / `iterating:`) — the empirical answer to the layered Plan 03.1-07 + 03.1-08 fix bundle.
- `03.1-FALSIFICATION-3.md` is the load-bearing artefact capturing the Walk #3 verdict + diagnostic-JSONL evidence stream.
- On `CONFIRMED-AFTER-FIX`: Phase 4 + Phase 5 unblock; MirkFall migration recommendation flips to PORT BACK with the full 5-architectural + 1-perf change layered fix bundle; Phase 5 closure conversation can proceed; FOG-04..07 + FOG-11 + FOG-13 + FOG-14 + FOG-15 + DEBUG-01 + PERF-07 all flip to Complete.
- On `DENIED-FINAL`: the formal POC architectural counter-evidence is complete; Phase 3.2 alternative-renderer conversation is the only remaining option. New tests stay Complete (the fix DID land in software).
- On `ITERATING`: Plan 03.1-10 is opened in a follow-up invocation with Walk #3's diagnostic stream as input.
- The Phase 3 + Phase 3.1 Walks #1 + #2 historical records stay untouched — historical integrity preserved across iteration cycles.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-fix-fog-pan-translation/03.1-09-SUMMARY.md` capturing:
- Pre-walk gate sequence outcome (4 local + CI + IPA download)
- Walk #3 metadata (CI run ID, SHA, walk theatre, walk duration, gesture count)
- Developer's verbatim verdict
- Post-walk JSONL grep correlation (per-second composite extract for the ≥ 10-combined-gesture window)
- Walk #3 Verdict outcome (`CONFIRMED-AFTER-FIX` | `DENIED-FINAL` | `ITERATING`)
- Files modified across all three tasks (5 planning docs)
- Any deviations from the plan with Rule classification
- Cross-reference to Phase 4 (the unblocked-or-still-blocked downstream phase) + the migration recommendation hand-off to Phase 5
- Comparison vs Walks #1 + #2 numbers — frame_delta headroom preservation, SDF rebuild rate change (PERF-08 status), failure-mode resolution status per Walk #1/2 observation tables
</output>
