---
phase: 03.1-fix-fog-pan-translation
plan: 12
subsystem: rendering
tags: [fog-shader, dart-only-fix, mirl-compatible, gap-closure, walk-4-follow-up, c2-deferred, pre-walk-5]

# Dependency graph
requires:
  - phase: 03.1-fix-fog-pan-translation
    provides: Plan 03.1-11 Walk #4 verdict ITERATING-WITH-MAJOR-PROGRESS at HEAD `b31766f`; FOG-17 + FOG-17a precision pairing in production code; Walk #4's Mail-shared session log + 4 JSONL streams + dev-marker corroboration; 03.1-FALSIFICATION-4.md Supplementary Walk Evidence (Plan 03.1-12+ Dart-only fix axes corrected — C1' / C2' / C3') as gap input
provides:
  - "FOG-18 (REQUIREMENTS.md `### Fog of War` section + traceability table row): eliminate FOG-17a integer-wrap-modulo per Walk #4 falsification of FOG-17a's premise"
  - "DEBUG-02 (REQUIREMENTS.md `### Diagnostics` section + traceability table row): MapOptions.cameraConstraint removed for Walk #5 stress-test diagnostic"
  - "_FogPainter.paint() FOG-18 forwarding (intPx + fracPx, no modulo)"
  - "MapOptions without cameraConstraint (CameraConstraint.unconstrained() default applies)"
  - "kPocFogSmoothCoordinateMaxDelta = 2000.0 literal (pre-FOG-18 was kPocFogIntegerWrapPeriodPx + 1)"
  - "Test files updated/rewritten: fog_pixel_origin_decomposition_test.dart (rewritten as FOG-18 acceptance gate); map_screen_test.dart (DEBUG-02 group); fog_smooth_noise_test.dart + fog_pan_translation_test.dart + fog_world_coordinate_noise_test.dart + fog_debug_spiral_continuity_test.dart (FOG-18-aware updates)"
  - "Plan 03.1-13 (Walk #5) input: this Dart-only fix bundle delivered as two atomic commits (post-walk attribution clean)"
affects: [03.1-13-PLAN.md, future C2'/FOG-19 follow-up plan after /gsd:discuss-phase 3.1]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "C2' deferral pattern — when a fix axis surfaces an architectural tension that needs design conversation (here: refZoom-px basis vs screen-px uResolution dimensional-mismatch; relaxing MIRL via uZoomScale uniform vs other approaches), defer to a follow-up plan after fresh /gsd:discuss-phase rather than ship a half-working fix."
    - "Atomic-commit-per-axis pattern for post-walk attribution — Walk #5 will be able to grep commit-message prefix (`refactor(03.1-12): C1' ...` / `feat(03.1-12): C3' ...`) + dev-markers + appliedUOffset signature to attribute symptom→axis cleanly even if a hybrid outcome surfaces."
    - "MIRL-constraint validation pattern — `git diff <task-1-commit>~..HEAD -- assets/shaders/` empty across both task commits proves zero shader edits; documentation-continuity comments in fog_layer.dart preserve the FOG-17a → FOG-18 evolution narrative without referencing the deleted constant."

key-files:
  created:
    - ".planning/phases/03.1-fix-fog-pan-translation/03.1-12-SUMMARY.md (this file)"
  modified:
    - "lib/config/constants.dart (deleted kPocFogIntegerWrapPeriodPx; updated kPocFogSmoothCoordinateMaxDelta to literal 2000.0; annotated kPocFogNoiseTilePx docstring; annotated kPocBboxLat*/Lon* + kPocPanBoundsPadDegrees as DEBUG-02 stress-test-disabled)"
    - "lib/presentation/widgets/fog_layer.dart (_FogPainter.paint() forwards camera.pixelOrigin directly; truncateToDouble decomposition retained for documentation continuity but the modulo is removed; FOG-18 docstring documents Walk #4 falsification of FOG-17a's premise)"
    - "lib/presentation/screens/map_screen.dart (MapOptions.cameraConstraint parameter REMOVED; MapOptions(...) made const-eligible; UX-02 InteractionOptions retained; minZoom/maxZoom retained; DEBUG-02 docstring documents stress-test diagnostic intent)"
    - "test/presentation/widgets/fog_pixel_origin_decomposition_test.dart (REWRITTEN as FOG-18 acceptance gate — replaces the FOG-17a bounded-magnitude assertions)"
    - "test/presentation/widgets/fog_smooth_noise_test.dart (line-119 magnitude assertion replaced; reason-strings reference FOG-18; threshold inherits new kPocFogSmoothCoordinateMaxDelta = 2000.0)"
    - "test/presentation/widgets/fog_pan_translation_test.dart (FOG-09 reason-strings updated to reference FOG-18 post-FOG-17a-elimination)"
    - "test/presentation/widgets/fog_world_coordinate_noise_test.dart (Rule 3 - Blocking deviation: removed kPocFogIntegerWrapPeriodPx references that blocked compile; the integer-wrap-event sub-test repurposed as historical reference documentation)"
    - "test/presentation/widgets/fog_debug_spiral_continuity_test.dart (Rule 3 - Blocking deviation: removed kPocFogIntegerWrapPeriodPx references that blocked compile; world-coordinate sub-test now FOG-18-aware)"
    - "test/presentation/screens/map_screen_test.dart (NEW DEBUG-02 group with runtime UnconstrainedCamera assertion + static-source isNot(contains('CameraConstraint.contain')) assertion; UX-02 group retained)"
    - ".planning/REQUIREMENTS.md (FOG-18 + DEBUG-02 entries added; FOG-17 row appended with FOG-17a-falsification clause; traceability rows added; Phase 3.1 count 15 → 17; v1 total 71 → 73; FOG-19 NOT added — deferred per scope reduction; Last updated footer bumped 2026-05-04)"
    - ".planning/ROADMAP.md (Plan 03.1-12 row flipped from `- [ ]` to `- [x]` with execution summary; gsd-tools roadmap update-plan-progress recalculated phase 03.1 plan-progress block)"
    - ".planning/STATE.md (frontmatter `stopped_at` + `last_activity` updated; Current focus + Current Position narrative updated to reflect 12-of-13 plans complete; Progress bar 32/34 = 94%; gsd-tools state add-decision appended three Phase 03.1 decision entries; gsd-tools state record-metric appended `Phase 03.1 P12` row to Performance Metrics table)"

key-decisions:
  - "C1' / FOG-18 — eliminate FOG-17a integer-wrap-modulo. Walk #4 debug-spiral positive control (digit-atlas cells truly periodic on 384) showed ZERO steppiness during max-zoom pan, while production fog showed visible SNAP at every wrap event — falsifying FOG-17a's premise that the noise function was truly periodic on `kPocFogNoiseTilePx (=384)`. The wrap event ITSELF was the bug, not the precision penalty FOG-17a was designed to address. fp32 24-bit mantissa supports exact-integer values up to 16.7M raw-px, well above Walk #4's max observed pixelOriginX of ~4.26M."
  - "C3' / DEBUG-02 — `MapOptions.cameraConstraint` removed for Walk #5 stress-test diagnostic per developer's verbatim Walk #4 request *\"we should disable the bounding box that block us from going further to ensure that hard step do not reappear 100 km away\"*. Pure scope-extension diagnostic; not a fix axis itself; verifies FOG-18 doesn't introduce new precision-induced artefacts at extreme world coordinates within fp32's 16.7M ceiling. UX-02 InteractionOptions + minZoom/maxZoom retained."
  - "C2' / FOG-19 DEFERRED to follow-up plan after fresh /gsd:discuss-phase 3.1 round resolves the dimensional-mismatch design question. The genuinely zoom-invariant basis `crs.latLngToPoint(camera.center, kPocFogReferenceZoom)` produces refZoom-px coordinates that mix incoherently with the shader's `worldPx = fragUv * uResolution + uPixelOrigin` formula (uResolution is in screen-px). Resolving requires choice between (a) change uResolution semantics in both shaders (MIRL violation — touches shader source); (b) introduce uZoomScale uniform (relaxes MIRL — adds one ABI slot); (c) Dart-side interpolation across zoom-snap boundaries (originally rejected as C-Q1b-2 in 03.1-FALSIFICATION-4.md, worth re-examining); (d) something else surfaced by discuss-phase. Walk #5 captures Q1b observation as input for the future C2' follow-up plan; Q1b residual does NOT block Walk #5 verdict from new intermediate `CONFIRMED-AFTER-FIX-PARTIAL` outcome."
  - "MIRL constraint preserved — ZERO shader source modifications across both task commits. Both `assets/shaders/atmospheric_fog.frag` AND `assets/shaders/atmospheric_fog_debug_spiral.frag` are untouched. The slot 3..4 `uPixelOrigin` uniform retains its semantics (a `vec2` interpreted as world-pixel-coordinate basis at current zoom); only the Dart-side computation that produces it changes (Task 1 removes the modulo wrap; both shaders inherit the fix automatically via the unchanged ABI)."
  - "Plan-checker iteration 2/3 acceptance criteria honoured — the FOG-18 BEHAVIOURAL test asserts `forwarded ≈ camera.pixelOrigin within < 1 raw px` (replacing the pre-FOG-17a `bounded under 1537` assertion); Issue #4 (plan-checker correctness verification) auto-resolved by C2' deferral since no Task 2 interposes a basis transformation between `camera.pixelOrigin` and the forwarded value; Issue #5 (gentle-pan threshold headroom) verified — `fog_smooth_noise_test.dart` exercises 10 small Melun-bounded `MapController.move()` calls with ~3 raw-px deltas each at zoom 13, providing ~3 orders of magnitude headroom under the new 2000-raw-px threshold."

patterns-established:
  - "C2' deferral pattern — when a fix axis surfaces an architectural tension that needs design conversation, defer to a follow-up plan after fresh /gsd:discuss-phase rather than ship a half-working fix. Documented rationale: the genuinely zoom-invariant basis `crs.latLngToPoint(camera.center, kPocFogReferenceZoom)` produces refZoom-px coordinates that mix incoherently with the shader's `worldPx = fragUv * uResolution + uPixelOrigin` formula; resolving requires either changing uResolution semantics across both shaders (MIRL violation), introducing a uZoomScale uniform (relaxes MIRL), or a different Dart-side approach surfaced by the discuss-phase."
  - "Atomic-commit-per-axis pattern for post-walk attribution — Walk #5 will be able to attribute symptom→axis cleanly via the conventional-commit prefix (`refactor(03.1-12): C1' ...` / `feat(03.1-12): C3' ...`) + dev-markers + appliedUOffset signature, even if a hybrid outcome surfaces."
  - "Documentation-continuity-without-deleted-symbol pattern — when deleting a constant, _both_ the production code and any docstring/comment that referenced it must be cleaned (the FOG-18 static-source test asserts `isNot(contains('kPocFogIntegerWrapPeriodPx'))` against the entire `lib/presentation/widgets/fog_layer.dart` file, including comments)."

deferred-followup-needed:
  - "C2' / FOG-19 zoom-invariant world-coordinate basis — schedule `/gsd:discuss-phase 3.1` after Walk #5 verdict to resolve the dimensional-mismatch design question (refZoom-px basis vs screen-px uResolution); new follow-up plan opens after that decision picks among options (a)/(b)/(c)/(d). Walk #5 captures Q1b observation as input."
  - "FOG-12 + FOG-13 Canvas-side compensation audit — STAYS DEFERRED per 03.1-CONTEXT.md `<deferred>` block. Walk #5 outcome IS the empirical answer (if fog covers viewport correctly under all gestures including extreme-distance pans ⇒ FOG-12/13 working; viewport gap surfaces ⇒ open audit + redesign in Plan 03.1-14+)."
  - "MIRK-01 ABI formalization — STAYS DEFERRED per 03.1-CONTEXT.md `<deferred>` block. The slot 3..4 `uPixelOrigin` uniform IS conceptually the standardized ABI input; FOG-17a (replaced by FOG-18 in this plan) was the architectural precision computation; formalize ABI structure post-Walk-5 verdict if/when empirical validation lands."
  - "PERF-08 SDF-cache-thrash addressing — same status as Plans 03.1-10/11/12 — deferred. Walk #4 confirmed the rebuild rate is not user-observable as a performance issue (PERF-07 has ~66× headroom). Revisit only if a future walk surfaces it."
  - "Re-enabling cameraConstraint after Walk #5 — DEBUG-02 removes it permanently for the POC stress-test diagnostic. If Walk #5 closes Phase 3.1 with `CONFIRMED-AFTER-FIX-PARTIAL` (or eventually full `CONFIRMED-AFTER-FIX` after the C2' follow-up), the Phase 5 hardening pass can revisit whether to re-add a sensible bbox constraint for production UX. Constants `kPocBboxLat*` + `kPocBboxLon*` + `kPocPanBoundsPadDegrees` retained in `lib/config/constants.dart` (annotated as stress-test-disabled) so re-enabling is a one-line MapOptions edit."

requirements-completed: [FOG-18, DEBUG-02]

# Metrics
duration: ~25 min
completed: 2026-05-04
---

# Phase 03.1 Plan 12: C1' + C3' Dart-Only Fix Bundle Summary

**Walk #4 follow-up Dart-only fix bundle EXECUTED 2026-05-04 — C1' eliminate FOG-17a integer-wrap-modulo (introduce FOG-18) + C3' remove `MapOptions.cameraConstraint` stress-test diagnostic (introduce DEBUG-02). Two atomic commits per plan-execution discipline; ZERO shader source modifications (MIRL constraint preserved). C2' / FOG-19 zoom-invariant world-coordinate basis DEFERRED to follow-up plan after fresh `/gsd:discuss-phase 3.1` round resolves the dimensional-mismatch design question.**

## Commits

| # | Hash | Message |
|---|------|---------|
| 1 | `f8b9a21` | `refactor(03.1-12): C1' eliminate FOG-17a integer-wrap-modulo (introduce FOG-18)` |
| 2 | `50fa14e` | `feat(03.1-12): C3' remove MapOptions.cameraConstraint stress-test diagnostic (introduce DEBUG-02)` |

Pre-plan baseline: `6a50478`. Post-plan HEAD: `50fa14e`. Two-commit delta cleanly attributable to the two axes (one task = one commit = one axis).

## What was done

### Task 1 — C1' / FOG-18

**File-by-file changes:**

- **`lib/presentation/widgets/fog_layer.dart`** — `_FogPainter.paint()` lines 455-512: the FOG-17a docstring block rewritten as the FOG-18 docstring (Walk #4 falsification narrative + fp32 mantissa ceiling rationale + C2' deferral cross-reference); the modulo wrap `final boundedX = (intPxX % kPocFogIntegerWrapPeriodPx) + fracPxX;` collapsed to `final boundedX = intPxX + fracPxX; // semantically == pxOrigin.x`. The `truncateToDouble()` decomposition is RETAINED for documentation continuity (the structure is preserved in case the future C2' follow-up re-introduces a basis derivation that uses the decomposition). All other FOG-07 / FOG-12 / FOG-13 / FOG-17 logic in `_FogPainter.paint()` UNCHANGED.

- **`lib/config/constants.dart`** — `kPocFogIntegerWrapPeriodPx` constant DELETED (lines ~387-422 of pre-plan source). `kPocFogSmoothCoordinateMaxDelta` updated from `= kPocFogIntegerWrapPeriodPx + 1` to literal `= 2000.0` with refreshed docstring documenting the FOG-18 regime. `kPocFogNoiseTilePx` docstring annotated with FOG-18 cross-reference (the previously-paired constant `kPocFogIntegerWrapPeriodPx` is gone; `kPocFogNoiseTilePx` continues to live on its own as the noise-tile period for FOG-17 world-coordinate sampling).

- **`test/presentation/widgets/fog_pixel_origin_decomposition_test.dart`** — REWRITTEN as the FOG-18 acceptance gate. The pre-fix file's group `'FOG-17a (Plan 03.1-10) — CPU-side integer/fractional decomposition'` becomes `'FOG-18 (Plan 03.1-12) — direct pixelOrigin forwarding (FOG-17a wrap eliminated)'`. Sub-tests:
  1. STATIC source — asserts `_FogPainter.paint()` source string `isNot(contains('% kPocFogIntegerWrapPeriodPx'))` AND `isNot(contains('kPocFogIntegerWrapPeriodPx'))` (the second clause forced cleanup of all references in `lib/presentation/widgets/fog_layer.dart`, including in comments).
  2. NUMERICAL Walk #4 marker #0 magnitude (4_256_182.0, 2_896_819.0) forwards exactly.
  3. NUMERICAL extrapolated zoom-19 magnitude (17_040_000.0, 4_260_000.0) forwards within fp32 mantissa ceiling.
  4. NUMERICAL small-magnitude boundary (1536.5, 384.25) forwards exactly (no wrap).
  5. NUMERICAL negative magnitudes (-100.0, -1536.5) forward exactly (sign preserved by `truncateToDouble`).
  6. BEHAVIOURAL — mounts FogLayer at zoom 15 + Melun (camera.pixelOrigin ≈ 2.1M raw px); asserts `(forwarded.x - camera.pixelOrigin.x).abs() < 1.0` AND same for y. **Issue #4 (plan-checker iter 2/3) auto-resolved by C2' deferral** — no Task 2 interposes a basis transformation between `camera.pixelOrigin` and the forwarded value, so the assertion stays correct.

- **`test/presentation/widgets/fog_smooth_noise_test.dart`** — line-119 magnitude assertion changed from `inExclusiveRange(0, kPocFogIntegerWrapPeriodPx + 1)` to `greaterThan(1e3)` (the deleted constant no longer compiles; the new assertion defends against the pre-Plan-03.1-04 normalised-UV regime where forwarded values would be < 1). Threshold `kPocFogSmoothCoordinateMaxDelta` automatically picks up the new literal 2000.0 value (3 orders of magnitude headroom for the gentle ~3 raw-px-per-paint test scenarios — Issue #5 plan-checker verification GREEN).

- **`test/presentation/widgets/fog_pan_translation_test.dart`** — FOG-09 reason-strings updated to reflect post-FOG-18 semantics (post-FOG-17a was bounded composite; post-FOG-18 is raw camera.pixelOrigin again). The mechanical `delta > epsilon` assertion still passes — a programmatic pan still produces a non-zero delta in the forwarded value, only now the delta tracks the raw camera.pixelOrigin shift directly.

- **`test/presentation/widgets/fog_world_coordinate_noise_test.dart`** + **`test/presentation/widgets/fog_debug_spiral_continuity_test.dart`** — **Rule 3 — Blocking deviation:** these test files were not in the plan's `<files>` for Task 1, but their use of the deleted `kPocFogIntegerWrapPeriodPx` constant blocked compile. Both updated to remove the reference (now FOG-18-aware; `_decompose(...)` and friends collapse to no-modulo recomposition). The integer-wrap-event sub-test in `fog_world_coordinate_noise_test.dart` was repurposed as historical reference documentation (1536.0 / 384.0 = 4.0 sanity check + FBM-rotated octave shifts pinned at 11.6 / 20.4 / 42.0 noise-grid units for archival reference).

- **`.planning/REQUIREMENTS.md`** — FOG-18 entry added in `### Fog of War` section (after FOG-17); FOG-17 row appended with FOG-17a-falsification clause + FOG-18-replacement reference; FOG-18 row added in traceability table (Phase 3.1, Complete — Verified-by-test); Phase 3.1 count 15 → 16 (FOG-18 added); v1 total 71 → 72; Last updated footer bumped 2026-05-04.

**Atomic commit:** `refactor(03.1-12): C1' eliminate FOG-17a integer-wrap-modulo (introduce FOG-18)` — `f8b9a21`. 8 files changed, 371 insertions, 335 deletions.

### Task 2 — C3' / DEBUG-02

**File-by-file changes:**

- **`lib/presentation/screens/map_screen.dart`** — `MapOptions(...)` constructor (lines ~328-351 of pre-plan source): the `cameraConstraint:` parameter block REMOVED (5 lines deleted; the flutter_map 7.0.2 default `CameraConstraint.unconstrained()` applies). New 14-line DEBUG-02 docstring captures the developer's verbatim Walk #4 request as the design rationale + cross-references the kPocBboxLat*/Lon* constants. UX-02 `interactionOptions: InteractionOptions(flags: InteractiveFlag.all & ~InteractiveFlag.rotate)` retained verbatim. minZoom/maxZoom retained. **Rule 1 — Bug deviation:** removing the `cameraConstraint` parameter made `MapOptions(...)` const-eligible — analyzer flagged `prefer_const_constructors` + `unnecessary_const`; fixed by making the constructor const (`options: const MapOptions(...)`) and dropping the now-unnecessary `const` on `interactionOptions`.

- **`lib/config/constants.dart`** — `kPocBboxLat*` + `kPocBboxLon*` + `kPocPanBoundsPadDegrees` constants retained (still defined as compile-time constants) but each gets a one-line `DEBUG-02: stress-test disabled` annotation on its docstring. New 11-line DEBUG-02 header comment block above the bbox-constants section explains the stress-test diagnostic intent + how to re-enable (just re-add the `cameraConstraint: CameraConstraint.contain(...)` parameter to `MapOptions`).

- **`test/presentation/screens/map_screen_test.dart`** — pre-plan `'CameraConstraint contains the padded Melun bbox'` testWidget REPLACED with two new DEBUG-02 testWidgets:
  1. `'DEBUG-02 (Plan 03.1-12 Task 2) — cameraConstraint REMOVED for Walk #5 stress-test diagnostic'` — pumps `MapScreen.fromServices(...)`; finds the `FlutterMap` widget; asserts `flutterMap.options.cameraConstraint` is `isA<UnconstrainedCamera>()`.
  2. `'DEBUG-02: static-source — map_screen.dart does NOT contain CameraConstraint.contain'` — reads `lib/presentation/screens/map_screen.dart` as a string; asserts `isNot(contains('CameraConstraint.contain'))`. Mechanical regression defense.

- **`.planning/REQUIREMENTS.md`** — DEBUG-02 entry added in `### Diagnostics` section (after DEBUG-01); DEBUG-02 row added in traceability table (Phase 3.1, Complete — Verified-by-test); Phase 3.1 count 16 → 17 (DEBUG-02 added); v1 total 72 → 73; Last updated footer bumped to mention DEBUG-02 alongside FOG-18.

**Atomic commit:** `feat(03.1-12): C3' remove MapOptions.cameraConstraint stress-test diagnostic (introduce DEBUG-02)` — `50fa14e`. 4 files changed, 83 insertions, 26 deletions.

## Test gates — all GREEN

- `flutter test`: 180 passed + 1 skipped (the existing FOG-09 RED-guard `skip:true` from Plan 03.1-02; same count as pre-plan baseline since we rewrote-not-added the FOG-18 test file)
- `flutter analyze --fatal-infos`: 0 issues (No issues found)
- `dart format --line-length 160 --set-exit-if-changed .`: clean (Formatted 92 files, 0 changed)
- `dart test tool/test/`: 18 passed

## MIRL constraint verification

```bash
$ git diff HEAD~2..HEAD -- assets/shaders/atmospheric_fog.frag assets/shaders/atmospheric_fog_debug_spiral.frag | wc -l
0
```

Empty diff. Both shaders are untouched bit-for-bit across the two task commits. The slot 3..4 `uPixelOrigin` uniform retains its semantics; only the Dart-side computation that produces it changes.

## Deviations from Plan

### Rule 3 — Blocking — additional test files

**[Rule 3 — Blocking] Updated `fog_world_coordinate_noise_test.dart` + `fog_debug_spiral_continuity_test.dart` for compile-clean delete**

- **Found during:** Task 1 GREEN phase (after deleting `kPocFogIntegerWrapPeriodPx` from `constants.dart`).
- **Issue:** Plan's `<files>` for Task 1 lists 6 paths (`constants.dart`, `fog_layer.dart`, 3 fog_*_test.dart files, REQUIREMENTS.md). The deleted constant was ALSO referenced in two additional test files: `fog_world_coordinate_noise_test.dart` (used as a scaling magnitude in synthetic-pan trajectory setup + as a divisor in the simulated post-FOG-17a decomposition) and `fog_debug_spiral_continuity_test.dart` (used as a divisor in the `_Formulation.worldCoordinate` simulated decomposition). Both files would FAIL TO COMPILE post-deletion, blocking Task 1's `flutter test` from passing.
- **Fix:** Both files updated to remove the `kPocFogIntegerWrapPeriodPx` reference (substituted with the literal `1536.0` for historical-reference documentation; the post-FOG-18 simulated decomposition collapses to `intPx + fracPx == pxOrigin`). The integer-wrap-event sub-test in `fog_world_coordinate_noise_test.dart` Test 4 was repurposed as historical reference documentation (FBM-rotated octave shifts at 11.6 / 20.4 / 42.0 noise-grid units pinned for archival interest).
- **Files modified:** `test/presentation/widgets/fog_world_coordinate_noise_test.dart`, `test/presentation/widgets/fog_debug_spiral_continuity_test.dart`.
- **Commit:** `f8b9a21` (Task 1 atomic commit).

### Rule 1 — Bug — analyzer warnings post-cameraConstraint-removal

**[Rule 1 — Bug] Made `MapOptions(...)` const-eligible after Task 2's parameter removal**

- **Found during:** Task 2 GREEN phase (after removing the `cameraConstraint:` parameter from `MapOptions(...)`).
- **Issue:** `flutter analyze --fatal-infos` flagged `prefer_const_constructors` on `MapOptions(...)` line 328 (after removal of the non-const `cameraConstraint` parameter, all remaining parameters were const-compatible) AND `unnecessary_const` on the `const InteractionOptions(...)` inside the now-const-context.
- **Fix:** Made `options: const MapOptions(...)` (added the const keyword on the constructor); dropped the redundant inner `const` from `interactionOptions: InteractionOptions(...)` (no longer needed inside a const context).
- **Files modified:** `lib/presentation/screens/map_screen.dart`.
- **Commit:** `50fa14e` (Task 2 atomic commit).

## Self-Check: PASSED

Verified:
- Files modified all exist (production + test files staged in their respective task commits).
- Commits `f8b9a21` (Task 1 — `refactor(03.1-12): C1' ...`) and `50fa14e` (Task 2 — `feat(03.1-12): C3' ...`) both present in `git log --oneline --all`.
- `git diff HEAD~2..HEAD -- assets/shaders/` returns EMPTY (MIRL constraint preserved).
- `kPocFogIntegerWrapPeriodPx` no longer appears as a Dart symbol in any production code path (only inside docstrings/string-literals in test reason-strings + the deleted-historical narrative comment in lib/config/constants.dart's `kPocFogNoiseTilePx` cross-reference).
- `lib/presentation/screens/map_screen.dart` does not contain `CameraConstraint.contain` (mechanical static-source assertion in `map_screen_test.dart`).
- `.planning/REQUIREMENTS.md` contains `**FOG-18**` and `**DEBUG-02**` entries with traceability table rows; FOG-19 has no requirement entry / traceability row (only a footer mention narrating the deferral, per success criteria).
