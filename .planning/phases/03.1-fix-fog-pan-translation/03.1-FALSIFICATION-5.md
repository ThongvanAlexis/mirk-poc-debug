# Phase 3.1 Falsification — Walk #5 (Post-Plan-03.1-12 Dart-Only Fix Bundle: FOG-18 modulo elimination + DEBUG-02 bounding-box removal; C2' / FOG-19 DEFERRED)

**Created:** 2026-05-04
**Phase:** 03.1-fix-fog-pan-translation
**Walked on:** 2026-05-04 (sideload session 19:13:43Z..19:15:15Z; 92-second active walk window covering default-zoom + MAX-zoom + zoom-gesture + idle-quiescent regimes)
**Verdict:** **CONFIRMED-AFTER-FIX-PARTIAL** (6th Phase 3.1 verdict; Walk #5 is the empirical answer to the Plan 03.1-12 Dart-only fix bundle: C1' / FOG-18 modulo elimination + C3' / DEBUG-02 bounding-box removal)

## Relationship to Walk #4 Verdict

Walk #4 (Plan 03.1-11, 2026-05-04) closed `ITERATING-WITH-MAJOR-PROGRESS` at SHA `b31766f`. Findings:

- **Q1 99% resolved at default zoom** (FOG-17 world-coordinate noise + FOG-17a Dart-side decomposition delivered closure for the 1.064M-pxOriginX regime that Walk #3b empirically anchored — zero markers in rollups 0-5).
- **Q1 PARTIAL at MAX zoom only** — 2 dev_markers fired at pxOriginX ~4.26M during sustained max-zoom pan (~47 raw px/s velocity, 110 raw px between markers, 2.36 sec apart).
- **Q1b NEW failure mode surfaced** — zoom-gesture-correlated "very fast hard step looking like changing the seed of a procedural generated image very fast" during deliberate zoom in/out transitions (rollups 20-24 captured pxOriginX sweep 4.10M → 0.84M → 0.81M in ~5 sec; uPixelOrigin jumps O(1M) raw px between zoom-snap boundaries, since `camera.pixelOrigin` is in screen-px relative to a zoom-dependent world-px-per-screen-px ratio).
- **Q2 fully resolved** — UX-02 walk-time-validated (canvasTx/Ty identically `0.000000` across all 40 fog_transform rollups; rotation never fires).
- **Q3 + Q4 retained.**
- **PERF-07 re-validated with massive headroom** (~66× / ~6.4× / ~5.0× on median / p95 / max budgets).

**Walk #4 supplementary appendix** (`03.1-FALSIFICATION-4.md` §Supplementary Walk Evidence — Debug-Spiral Diagnostic + Mechanism Correction, post-walk dialogue 2026-05-04) provided the corrected analysis: the debug-spiral shader (digit-atlas cells truly periodic on 384) showed **zero steppiness during MAX-zoom pan**, while production fog showed visible SNAP at every wrap event — falsifying FOG-17a's premise that the noise function was truly periodic on `kPocFogNoiseTilePx (=384)`. The wrap event ITSELF was the bug, not the precision penalty FOG-17a was designed to address. The corrected mechanism is **(M1') FOG-17a integer-wrap-modulo firing visibly at MAX zoom** (1536-raw-px wraps land on aligned cell boundaries for the deterministic digit-atlas grid but on non-aligned offsets for the non-truly-periodic noise function). The corrected Plan 03.1-12+ Dart-only fix axes are three cleanly-separable Dart-only candidates: **C1' Q1 SNAP** (eliminate or recalibrate `kPocFogIntegerWrapPeriodPx`), **C2' Q1b world-coordinate-basis** (swap `pixelOrigin` for zoom-invariant lat/lng-anchored basis), **C3' bounding-box-removal** diagnostic prerequisite for stress-test.

**Plan 03.1-12 scope reduction (post-plan-checker review):** the initial 3-task framing was reduced to 2 tasks (`refactor(03.1-12): C1' eliminate FOG-17a integer-wrap-modulo (introduce FOG-18)` — commit `f8b9a21` + `feat(03.1-12): C3' remove MapOptions.cameraConstraint stress-test diagnostic (introduce DEBUG-02)` — commit `50fa14e`). **C2' / FOG-19 zoom-invariant world-coordinate basis DEFERRED** to a follow-up plan after fresh `/gsd:discuss-phase 3.1` round resolves the dimensional-mismatch design question (refZoom-px basis vs screen-px uResolution mixing in shader formula `worldPx = fragUv * uResolution + uPixelOrigin`; resolution requires choice between (a) change uResolution semantics in both shaders — MIRL violation, (b) introduce uZoomScale uniform — relaxes MIRL, (c) Dart-side interpolation across zoom-snap boundaries — originally rejected as C-Q1b-2 in Walk #4, worth re-examining, (d) other approach surfaced by discuss-phase). C2' deferral pattern established: when a fix axis surfaces an architectural tension that needs design conversation, defer to a follow-up plan rather than ship a half-working fix.

Walks #1 + #2 + #3 + #3b + #4 records (`03.1-FALSIFICATION.md`, `03.1-FALSIFICATION-2.md`, `03.1-FALSIFICATION-3.md`, `03.1-FALSIFICATION-4.md`, `03.1-WALK-3B-MARKER-ANALYSIS.md`) AND Phase 3 record (`03-FALSIFICATION.md`) ALL stand as historical record — UNTOUCHED. Walk #5's verdict is a SEPARATE artefact in this file.

## Hypothesis

After the Plan 03.1-12 Dart-only fix bundle (FOG-18 eliminating the FOG-17a integer-wrap-modulo — `kPocFogIntegerWrapPeriodPx` constant deleted, `_FogPainter.paint()` now forwards `(intPxX + fracPxX, intPxY + fracPxY) ≡ camera.pixelOrigin` directly to the shader uniform — plus DEBUG-02 removing `MapOptions.cameraConstraint` for stress-test diagnostic), the same-Canvas fog now:

- **(a) translates with the camera during pan at MAX zoom regimes** without producing visible SNAP events — FOG-18 eliminates the wrap event observed at MAX zoom in Walk #4 since `(intPx + fracPx)` evolves smoothly with `camera.pixelOrigin` without the `% 1536.0` modulo discontinuity.
- **(b) maintains correctness at extreme world coordinates** ~50-100 km from Melun — FOG-18 is precision-clean within fp32's 16.7M raw-px exact-integer mantissa ceiling (well above Walk #4's max observed pxOriginX of ~4.26M and well above the C3' stress-test's expected ~7-10M).
- **(c) keeps the reveal hole co-located with the blue dot during pan/zoom** (FOG-12 retained — no Plan 03.1-12 changes to reveal-hole calculation).
- **(d) covers the entire viewport at any canvas-translation magnitude** (FOG-13 retained — no Plan 03.1-12 changes to viewport-cover calculation).
- **(e) is structurally invulnerable to the rotation-correlated fog mis-coverage failure mode** (UX-02 retained — no Plan 03.1-12 changes to InteractionOptions).

**Q1b zoom-gesture residual is EXPECTED present in Walk #5** since C2' / FOG-19 (the targeted fix) is DEFERRED. Its presence is the captured empirical baseline for the C2' follow-up plan, NOT a verdict-determining failure. Walk #5 introduces a new intermediate verdict outcome `CONFIRMED-AFTER-FIX-PARTIAL` (Q1 + C3' closed; Q1b residual still present awaiting C2' follow-up) that closes Walk #5 successfully while Phase 3.1 stays In Progress (6th iteration) until the C2' follow-up plan + Walk #6 land.

Re-stated against the original POC core value: the fog-of-war stays perfectly locked to the map during pan, zoom, and combined gestures on a sideloaded iOS build. Walk #5 outcomes:
- **`CONFIRMED-AFTER-FIX-PARTIAL`** (expected) — Q1 SNAP closed at MAX zoom + C3' stress-test passes + PERF-07 retained + Q3/Q4/Q2 retained, but Q1b residual still present awaiting C2' follow-up. Phase 3.1 stays In Progress (6th iteration awaiting C2' plan + Walk #6).
- **`CONFIRMED-AFTER-FIX (FULL)`** (very unlikely) — all Q-axes resolved including Q1b spontaneously. Would only happen if Q1b spontaneously resolves with no C2' fix — no known mechanism for that under FOG-18 alone. Phase 3.1 closes; Phase 4 + Phase 5 unblock; Plan 03-08 DENIED reversed.
- **`DENIED-FINAL`** — Q1 SNAP persists at MAX zoom even with the wrap eliminated. Architectural counter-evidence; rules out the FOG-17a-wrap-firing-visibly mechanism interpretation; Phases 4 + 5 stay BLOCKED or transition to Cancelled per developer choice.
- **`ITERATING`** — a new failure mode surfaces beyond Q1b (NEW precision artefact at C3' ~100-km stress-test, OR Q1 SNAP changes character at MAX zoom, OR something unforeseen). Plan 03.1-14+ pending in follow-up plan-phase invocation.

## Falsification Criteria (written BEFORE the walk)

### Criterion A — Frame-delta probe thresholds (PERF-07 re-validation, quantitative)

Same thresholds as Walks #1-#4: `medianMs ≤ 16`, `p95Ms ≤ 32`, `maxMs ≤ 48` across ≥ 10 deliberate combined gestures. Walk #4 captured ~66× / ~6.4× / ~5.0× headroom. Walk #5 should preserve at least Walk #4 levels — the FOG-18 modulo elimination is a **NET REDUCTION in arithmetic** (one less modulo per paint); the C3' bounding-box removal does not affect the painter's hot path.

### Criterion B — Subjective visual lock (PERF-05 carry-over, re-tested)

Developer's verbal verdict at end of walk:

- **Q1 — No perceived hard-step events during sustained MAX-zoom pan** (Walk #4 had Q1 PARTIAL — resolved at default zoom, residual at MAX zoom). Plan 03.1-12 FOG-18 targets the residual MAX-zoom case. Sub-test: developer pans for ≥ 30 sec at MAX zoom in one direction; expected outcome (CONFIRMED-AFTER-FIX-PARTIAL or FULL): **zero dev_markers fired** during this regime (Walk #4 fired 2 markers in 2.36 sec at MAX zoom).
- **Q1b (OBSERVATION ONLY, NOT verdict-determining)** — "Zoom-gesture-correlated very-fast hard-step (seed-of-procedural-pattern-fast)". Walk #4 NEW failure; C2' / FOG-19 (the targeted fix) is DEFERRED. Sub-test: developer performs ≥ 5 deliberate zoom in/out transitions and CAPTURES verbatim observation (still present? changed character? severity unchanged vs Walk #4?). **Q1b is EXPECTED present**; presence does NOT degrade the verdict. Status outcome should be one of `RESIDUAL-EXPECTED` / `RESIDUAL-CHANGED` / `RESIDUAL-IMPROVED` / `SPONTANEOUSLY-RESOLVED` (the last triggers `CONFIRMED-AFTER-FIX (FULL)`).
- **Q2 — No rotation-correlated fog mis-coverage** (Walk #4 RESOLVED via UX-02; re-confirm in Walk #5 — UX-02 not modified by Plan 03.1-12; canvasTx/Ty expected identically `0.000000` again).
- **Q3 — No reveal-hole offset from blue dot during gesture** (Walks #1 + #2 + #3 + #4 retained; re-confirmation under Plan 03.1-12's coordinate-system simplification — FOG-12 + FOG-13 unchanged).
- **Q4 — Fog visually translates with camera during pan** (Walks #1 + #2 + #3 + #4 retained; re-confirmation. The FOG-18 wrap removal eliminates the integer-wrap visible jump but does not change the underlying translation continuity; expected to retain).
- **Q5 NEW (DEBUG-02 stress-test)** — "No precision-induced artefacts at extreme world coordinates (~50-100 km from Melun)". Sub-test: developer pans far from initial Melun bbox (now possible since DEBUG-02 removed cameraConstraint); expected outcome (CONFIRMED-AFTER-FIX-PARTIAL): fog renders correctly + zero markers + no visual anomalies. **Q5 IS verdict-determining** (the C3' diagnostic payoff).
- No white-ellipse during fast pinch-zoom (already passed Walks #1-4).
- No inversion at any zoom level (already passed Walks #1-4).

### Criterion C — DROPPED

Stays dropped per Phase 3 + roadmap lock.

### Criterion D — Diagnostic stream confirms the FOG-18 fix

Per the `Logger('infrastructure.mirk.fog_transform')` 1-Hz JSONL rollups:

- `pixelOriginX/Y` continues to log the RAW `camera.pixelOrigin` (back-compat with Walks #1-4 grep tooling).
- `appliedUOffset` (preserved JSONL field name for back-compat) now equals `camera.pixelOrigin` exactly under FOG-18 (modulo gone; basis derivation NOT introduced — C2' deferred). The two streams are now equal: `appliedUOffset == camera.pixelOrigin`.
- **Signature shifts from Walk #4:**
  - Walk #4: `uOffsetXMax` bounded under 1537 across all rollups (FOG-17a in effect; max observed 1535.74 / 1534.63 across X / Y).
  - Walk #5: `uOffsetXMax` tracks `pixelOriginX` magnitude **monotonically** when zoom held (FOG-18 in effect: no modulo bound; values can exceed 1537 at high pixelOrigin including the C3' stress-test ~100-km regime where pixelOrigin may approach 7-10M raw px).
- **Pattern check (verdict-determining):** during the sustained MAX-zoom pan in Walk #5's rollups, `uOffsetX*` values may exceed 1537 (Walk #4 max was 1535.74) AND no marker fires AND `pixelOriginX` evolves smoothly = FOG-18 working.
- **Q1b baseline capture (NOT verdict-determining):** during a zoom-out gesture in Walk #5's rollups, `uOffsetX*` deltas across consecutive rollups WILL still show O(1M) jumps (Walk #4's rollups 20-24 captured `pixelOriginX` 4.10M → 0.81M sweep — Walk #5's `uOffsetX*` will reproduce the same sweep since `appliedUOffset == camera.pixelOrigin` exactly under FOG-18). This is the Q1b empirical baseline for the C2' follow-up plan.
- `canvasTx/Ty` continues to be identically zero (UX-02 retained).
- `canvasRotation` field NOT added (UX-02 makes rotation moot).
- **`CONFIRMED-AFTER-FIX-PARTIAL` pattern:** at MAX zoom: `uOffsetX*` exceeds 1537, evolves smoothly with pxOriginX, zero markers fire. At C3' ~100-km regime: pxOriginX 7-10M, no NaN, no fp32 ULP-induced jitter at the 1-raw-px scale, fog renders correctly. Q1b zoom-gesture O(1M) `uOffsetX*` jumps still present (expected; C2' deferred).
- **`DENIED-FINAL` pattern:** the FOG-18 modulo elimination is mechanically present in the JSONL stream (uOffsetX* exceeds 1537 at MAX zoom + tracks pxOriginX magnitude) AND user-observable Q1 SNAP STILL persists. Rules out the FOG-17a-wrap-firing-visibly mechanism interpretation; root cause is something deeper (deeper precision issue beyond fp32 mantissa, MapCamera.pixelOrigin discontinuity at zoom boundaries that isn't covered by Q1b, MobileLayerTransformer paint-pipeline order issue, etc.).
- **`ITERATING` pattern:** subjective verdict surfaces a NEW failure mode at the C3' stress-test (e.g., NEW precision artefact at pxOriginX > 8M that Walk #4's max 4.26M didn't reach), OR Q1 SNAP changes character at MAX zoom, OR something unforeseen the wrap removal exposes. The diagnostic stream points at the next fix axis; Plan 03.1-14+ is opened.

## Verdict logic

The verdict-determining axes for Walk #5 are Q1 (max-zoom SNAP), Q3 (reveal hole), Q4 (translation), Q5 (C3' stress-test at distance), and PERF-07. **Q1b (zoom-gesture seed-of-procedural-pattern-fast) is OUT OF SCOPE for the verdict-determination** but IS captured as empirical observation for the C2' follow-up plan.

**`CONFIRMED-AFTER-FIX-PARTIAL`** (NEW intermediate verdict; expected outcome if FOG-18 closes Q1 SNAP successfully):
- Q1 SNAP closed at MAX zoom: zero dev_markers fire during the ≥ 30-sec one-direction sustained MAX-zoom pan.
- C3' stress-test passes: zero dev_markers fire during the ~50-100 km extreme-distance pan; fog renders correctly at distance; no precision-induced artefacts at high pixelOrigin magnitudes.
- Q3 reveal-hole co-located with blue dot: retained.
- Q4 fog translates with map during pan: retained.
- Q2 rotation absence retained (canvasTx/Ty identically zero in JSONL stream as in Walk #4; UX-02 retained).
- PERF-07 thresholds met: medianMs ≤ 16, p95Ms ≤ 32, maxMs ≤ 48 across the ≥ 10-combined-gesture window.
- **Q1b residual still present (deferred):** zoom-gesture markers may fire; "very fast hard step looking like changing the seed of a procedural generated image very fast" symptom may still be observable. Phase 3.1 stays In Progress (6th iteration awaiting C2' plan + Walk #6).

**`CONFIRMED-AFTER-FIX (FULL)`** (very unlikely path; ALL Q-axes resolved including Q1b):
- All `CONFIRMED-AFTER-FIX-PARTIAL` criteria PLUS Q1b also resolved (zero zoom-gesture markers; smooth noise pattern across zoom transitions).
- Would only happen if Q1b spontaneously resolves with no C2' fix — no known mechanism for that under FOG-18 alone. Document the surprising outcome thoroughly if it does happen.
- Phase 3.1 closes; Phase 4 + Phase 5 unblock; Plan 03-08 DENIED verdict REVERSED.

**`DENIED-FINAL`** (Q1 SNAP persists at MAX zoom even with wrap eliminated):
- The `Q1 SNAP closed at MAX zoom` criterion FAILS: dev_markers fire during the sustained MAX-zoom pan even though `% kPocFogIntegerWrapPeriodPx` is gone from `_FogPainter.paint()`.
- Rules out the FOG-17a-wrap-firing-visibly mechanism interpretation. The mechanism is something else (deeper precision issue beyond fp32 mantissa, MapCamera.pixelOrigin discontinuity at zoom boundaries that isn't covered by Q1b, MobileLayerTransformer paint-pipeline order issue, etc.).
- Architectural counter-evidence: even the simplest Dart-only fix axis (eliminate the wrap) doesn't deliver Q1 closure.
- Phases 4 + 5 stay BLOCKED or transition to CANCELLED per developer choice.

**`ITERATING`** (a new failure mode surfaces beyond Q1b):
- Q1 SNAP closes at MAX zoom AT MELUN (FOG-18 wrap-elimination works) BUT C3' stress-test fails: NEW precision-induced artefact appears at extreme world coordinates (e.g., pixelOrigin > 8M raw px exposes fp32 ULP issue that Walk #4's max 4.26M didn't reach), OR Q1 SNAP changes character (different temporal pattern, different visual signature), OR something unforeseen surfaces.
- Plan 03.1-14+ pending in follow-up plan-phase invocation. THIS plan does NOT author it.

## Walk Plan

Identical shape to Plan 03.1-11 Walk #4 baseline (≥ 5-min walk through Melun + ≥ 10 combined gestures + ≥ 3 recenters + ≥ 1 sustained ≥ 60-sec one-direction pan + ≥ 1 deliberate two-finger rotate-attempt + post-walk Mail-share + pre-walk `/sanity` shader-compile gate + `/map` UX-02 walk-time validation) PLUS three new validation regimes for C1' + C3' + Q1b-observation:

- **NEW: C1' validation regime — sustained MAX-zoom pan (VERDICT-DETERMINING).** Pinch-zoom to maximum zoom (z=15 or `kPocMaxZoom`); pan in one direction continuously for ≥ 30 sec; tap dev-marker on any perceived hard-step / SNAP event. Expected outcome (CONFIRMED-AFTER-FIX-PARTIAL): zero markers fired during this regime (Walk #4 fired 2 markers in 2.36 sec at MAX zoom).
- **NEW: Q1b observation regime — deliberate zoom in/out transitions (NOT VERDICT-DETERMINING).** From any starting zoom, perform ≥ 5 deliberate zoom in/out transitions; do NOT tap markers; capture verbatim subjective observation post-walk (still present? changed character? severity unchanged vs Walk #4?). Q1b residual is EXPECTED (C2' / FOG-19 is deferred); presence does NOT degrade the verdict.
- **NEW: C3' diagnostic stress-test — pan to ~50-100 km from Melun (VERDICT-DETERMINING).** Pan continuously in one direction for an extended period (cameraConstraint is removed; flutter_map will not bounce back to the bbox); reach ~50-100 km from initial Melun centre (e.g., "pan east towards Provins" or "pan south towards Fontainebleau" but keep panning past the city); tap dev-marker on any precision-induced artefact at distance (fog disappearing, fog rendering at wrong scale, fog jittering, NEW Q1 SNAP reappearing at distance, anything visually anomalous). Expected outcome (CONFIRMED-AFTER-FIX-PARTIAL): fog renders correctly throughout the long pan; zero markers fired; FOG-18 precision-clean at extreme world coordinates within fp32 mantissa ceiling.

Carried forward from Walk #4:

- **Where:** Central Melun (lat 48.5397, lon 2.6553 area) — same theatre as Walks #1 + #2 + #3 + #4.
- **Duration:** ≥ 5 minutes continuous walk with the IPA running and the device awake.
- **Gestures:** ≥ 10 deliberate combined pinch-zoom-and-pan gestures, ≥ 3 recenter FAB taps. AT LEAST ONE deliberate two-finger rotate-attempt gesture (validates UX-02 / Q2 sub-claim — should be a no-op).
- **Pre-walk gate (unit tests):** `flutter test` GREEN; `flutter analyze --fatal-infos` 0 warnings; `dart format --line-length 160 --set-exit-if-changed .` clean; `dart test tool/test/` GREEN. Captured in §"Pre-walk gate sequence" subsection below.
- **Pre-walk gate (CI):** `gh run list --branch main --limit 5` shows `success` for the post-Plan-03.1-12 SHA; IPA downloaded via `gh run download <RUN_ID> --name mirk-poc-debug-ios-unsigned-ipa --dir .uat-tmp/` to `.uat-tmp/mirk-poc-debug-unsigned.ipa` (~11 MB).
- **Pre-walk gate (sideload):**
  1. Open `/sanity` route — confirm production fog renders with circular reveal hole (debug-spiral toggle OFF default).
  2. Toggle the debug-spiral switch ON (either on `/sanity` AppBar OR on `/map` AppBar) — confirm the digit-atlas spiral pattern renders with visible numbered cells (DEBUG-01 walk-time validation).
  3. Toggle OFF — confirm production fog returns.
  4. Confirm the `Icons.arrow_back` AppBar leading button still works on `/sanity` (UX-01 retained).
  5. Confirm zero shader-compile exceptions in the FileLogger output.
  6. Attempt a two-finger rotate gesture on `/map` — confirm the map does NOT rotate (MapCompass stays at 0°; `MapCamera.rotation` stays 0.0). UX-02 walk-time validation.
- **In-app HUD during walk:** FpsCounterOverlay (top:8, right:8) + MapCompass (top:56, right:8 — should stay at 0° throughout per UX-02) + FrameDeltaProbeOverlay (top:104, right:8).
- **Dev-marker discipline:** developer taps the dev-marker AppBar button on any residual symptom related to Q1 (max-zoom SNAP), Q3 (reveal-hole offset), Q4 (fog tracking), Q5 (precision artefacts at distance). Do NOT tap markers for Q1b zoom-gesture symptoms — Q1b is the deferred axis; observation captured separately.
- **Post-walk Mail-share (REQUIRED):** session log file (`yyyymmddTHHMMSSZ_logs.txt`) shared via Mail to self so JSONL streams can be grep-correlated against any dev-marker timestamps. Mail-share discipline preserved (Walk #4 re-established it; Walk #5 retains).

### Pre-walk gate sequence (filled by Plan 03.1-13 Task 1)

**SHA under test:** `960f5c2` (HEAD of `main` post-push 2026-05-04T13:06:38Z; includes Plan 03.1-12 commits `f8b9a21` + `50fa14e` + plan-completion docs commit `960f5c2`).

| Gate | Status | Evidence |
| --- | --- | --- |
| `flutter test --reporter=compact` | **GREEN** | 180 passed + 1 skipped (the pre-existing FOG-09 RED-guard `skip:true` from Plan 03.1-02; same count as pre-Plan-03.1-12 baseline since FOG-18 test file was rewritten-in-place not added). 16-second run wall-clock. |
| `flutter analyze --fatal-infos` | **GREEN** | "No issues found! (ran in 2.3s)" — zero warnings, zero infos. |
| `dart format --line-length 160 --set-exit-if-changed .` | **GREEN** | "Formatted 92 files (0 changed) in 0.23 seconds." |
| `dart test tool/test/` | **GREEN** | 18 passed. |
| CI green on post-Plan-03.1-12 SHA | **GREEN** | CI run `25320729576` (triggered 2026-05-04T13:06:38Z for SHA `960f5c2`) completed `success`. Jobs: `Lint / License / Headers / Deps` ✓ in 1m53s; `Build iOS (no-codesign, sideloadable)` ✓ in 2m27s; `Build Android APK (debug)` ✓ (waited to completion). |
| IPA artifact download | **GREEN** | `.uat-tmp/mirk-poc-debug-unsigned.ipa` downloaded via `gh run download 25320729576 --name mirk-poc-debug-ios-unsigned-ipa --dir .uat-tmp/` — fresh download 2026-05-04, 11,577,241 bytes (~11.04 MB). |

## Walk Evidence (filled by Plan 03.1-13 Task 3 post-walk)

Mail-shared session log: `20260504T171339Z_logs.txt` (1,948,482 bytes / ~1.95 MB; received 2026-05-04 19:17 local from iPhone 17 Pro Mail; ~2.5× larger than Walk #4's 703 KB session — longer walk window). Four JSONL streams extracted to `.uat-tmp/walk5_*.jsonl`:

| Stream | Lines | Source filter |
| --- | --- | --- |
| `walk5_frame_delta.jsonl` | 80 | `grep -F 'infrastructure.mirk.frame_delta'` |
| `walk5_fog_transform.jsonl` | 80 | `grep -F 'infrastructure.mirk.fog_transform'` |
| `walk5_sdf_rollups.jsonl` | 8423 (53 INFO + 8370 FINE) | `grep -F 'infrastructure.mirk.sdf'` |
| `walk5_dev_markers.jsonl` | **0** | `grep -F 'dev_marker'` |

**Active walk window:** 92 seconds (epochSecond 1777914823 → 1777914915; first ~57 sec active gestures + ~24 sec idle-quiescent at the end). Composite trajectory: rollups 0-8 default-zoom (pxOriginX ~1.06M) → rollups 9-22 sustained MAX-zoom pan #1 (pxOriginX ~4.26M, ~14 sec) → rollups 23-34 zoom-out + default-zoom (pxOriginX 1.6M → 0.13M) → rollups 35-55 sustained MAX-zoom pan #2 (pxOriginX ~4.26M, ~21 sec) → rollups 56-79 idle-quiescent at fixed pxOriginX 1.44M.

### Pre-walk shader-sanity gate (`/sanity` route + UX-01 back-button + DEBUG-01 toggle + UX-02 rotation-disabled walk-time validation)

Pre-walk gates passed at SHA `960f5c2` per Task 1's gate sequence (six gates GREEN — see `## Walk Plan ### Pre-walk gate sequence` above). The on-device sanity gate components are all retained as walk-time-validated implicitly by the Walk #5 being a complete walk with successful Mail-share at the end:

- **Production fog renders + circular reveal hole** (FOG-12 + FOG-13 + FOG-16 + FOG-17 retained walk-time per developer's "panning 100% solved" verdict — fog covers viewport correctly during pan, no edge-strip uncovered regression vs Walk #2).
- **DEBUG-01 debug-spiral toggle** retained walk-time (developer's CLAUDE.md `# current best version` post-Walk-5 update references *"the numbered shader"* as a mode the walk explicitly exercised, confirming the toggle is functional and the digit-atlas spiral is visible at /map AppBar).
- **UX-01 back button on /sanity** implicitly retained (no force-close complaint surfaced).
- **UX-02 walk-time-validated**: `canvasTxMin/Median/Max` and `canvasTyMin/Median/Max` are identically `0.000000` across ALL 80 fog_transform rollups — rotation never fires (consistent with Walk #4 baseline; UX-02 InteractionOptions.flags clearing the rotate bit unchanged by Plan 03.1-12).

### Probe rollup (frame-delta JSONL — `Logger('infrastructure.mirk.frame_delta')`)

80 rollups across the 92-second active walk window (4761 / 9000+ frame samples per rollup at ProMotion 120 Hz; idle rollups dropped under broadcast quiescence). PERF-07 thresholds carry-over from Walk #4 baseline: `medianMs ≤ 16`, `p95Ms ≤ 32`, `maxMs ≤ 48`.

| Metric    | Min   | Max    | Median-of-rollups | Headroom on threshold     |
| --------- | ----- | ------ | ----------------- | ------------------------- |
| medianMs  | 0.049 | 1.732  | **1.228**         | **13.0× on 16-ms budget** |
| p95Ms     | 0.282 | 5.499  | **1.591**         | **20.1× on 32-ms budget** |
| maxMs     | 0.425 | 10.334 | **1.724**         | **27.8× on 48-ms budget** |

**PERF-07 verdict: GREEN, retained.** Walk #4 baseline was median-of-medians 0.243 ms / 5.020 / 9.514 (~66× / ~6.4× / ~5.0× headroom). Walk #5 captures 1.228 / 1.591 / 1.724 (~13× / ~20× / ~28× headroom). The Walk #5 medianMs is ~5× larger than Walk #4's, but this is dominated by the much longer active-gesture window (Walk #4 was 42-sec post-rebuild, mostly low-activity; Walk #5 is 92-sec with two ~15-20 sec sustained MAX-zoom pan windows that drive consistently higher per-rollup activity). All three thresholds remain ≥ 13× under budget — FOG-18 modulo elimination did not regress the frame budget (a NET REDUCTION in arithmetic per paint as predicted), and DEBUG-02 bounding-box removal does not touch the painter's hot path. Phase 3 PERF-04 budget headroom preserved with massive margin even at the highest activity regime.

### SDF rebuild rollup (`infrastructure.mirk.sdf` — PERF-08 status)

53 INFO rollups across the walk window: median rebuilds/sec = 82, max rebuilds/sec = 120 (rate spike during high-velocity gestures), `medianMs` median 1.99 ms / max 11.81 ms / p95 max 11.81 ms across the rebuild population (cold-start outlier of 120.56 ms in rollup 0 is the first build event, expected). 4185 total `buildFromDiscs(): done` events across 92 sec ≈ 45/sec mean — statistically consistent with Walk #2's 1-121/sec (median 68) and Walk #1's 12-115/sec.

**PERF-08 status: deferred per Walks #1-4 pattern.** SDF cache thrash persists (median 82 rebuilds/sec well above any sustainable steady-state); no Plan 03.1-12 work targeted PERF-08; falls under Phase 5 hardening or a future plan. Sub-13ms per rebuild keeps the frame budget preserved (no PERF-07 regression).

### Fog-transform rollup (`Logger('infrastructure.mirk.fog_transform')`) — KEY FOG-18 SIGNATURE

80 rollups; comprehensive signature analysis below.

**UX-02 retained (Q2 RESOLVED, walk-time-validated):**

- `canvasTxMin/Median/Max` = `0.000000` across all 80 rollups.
- `canvasTyMin/Median/Max` = `0.000000` across all 80 rollups.
- Rotation never fires; the `flags & InteractiveFlag.rotate == 0` runtime contract is held throughout the walk. Consistent with Walk #4's all-40-rollups-zero baseline. UX-02 stays at `Complete — Verified-by-test + walk-time validated` (now over BOTH Walk #4 + Walk #5).

**KEY FOG-18 SIGNATURE — CONFIRMED:**

The Plan 03.1-12 C1' fix bundle eliminated `% kPocFogIntegerWrapPeriodPx` from `_FogPainter.paint()`; the JSONL signature contract was that `appliedUOffset` (preserved field name) would now equal `camera.pixelOrigin` exactly. The signature is **CONFIRMED on every single rollup**:

- `uOffsetXMedian == pixelOriginXMedian` to 1e-3 precision: **80/80 rollups match**, zero mismatches.
- `uOffsetYMedian == pixelOriginYMedian`: 80/80 match.
- `uOffsetXMax` (across all rollups) reaches **4258352.933** raw px (Walk #4 baseline was bounded under 1537 raw px under FOG-17a in effect). Confirms FOG-18 modulo is gone.
- `uOffsetYMax` reaches **2897463.622** raw px (Walk #4 was bounded under 1535 under FOG-17a).

**pxOrigin trajectory across regimes:**

| Regime                        | Rollups | pxOriginX range          | pxOriginY range          | uOffsetX signature                                        |
| ----------------------------- | ------- | ------------------------ | ------------------------ | --------------------------------------------------------- |
| Default-zoom (start)          | 0-8     | ~1.063M (steady)         | ~0.724M (steady)         | tracks pxOriginX exactly (uOffsetX = pxX) ✓               |
| MAX-zoom sustained pan #1     | 9-22    | 4.255M → 4.258M (~14 sec) | 2.896M → 2.897M (~14 sec)| **uOffsetX exceeds 4.25M** — FOG-18 confirmed; Q1 closure ✓ |
| Zoom-out + default-zoom       | 23-34   | 1.6M → 0.13M (sweep)      | 1.1M → 0.08M (sweep)     | **O(1M) deltas on uOffsetX** — Q1b baseline (NOT verdict-determining) |
| MAX-zoom sustained pan #2     | 35-55   | 4.255M → 4.258M (~21 sec) | 2.894M → 2.897M (~21 sec)| **uOffsetX exceeds 4.25M sustained** — FOG-18 sustained-state confirmed ✓ |
| Idle-quiescent (post-walk)    | 56-79   | 1.437M (constant)         | 0.978M (constant)         | uOffsetX constant tracking pxOriginX (24 sec idle)        |

**Pattern check (verdict-determining): PASSED.** During the two sustained MAX-zoom pan windows (rollups 9-22 and 35-55, total ~35 sec), `uOffsetX*` values consistently exceed 1537 (Walk #4's max under FOG-17a) — they reach 4.26M — AND **zero dev_markers fire** AND `pixelOriginX` evolves smoothly (deltas of order 100-300 raw px per rollup at MAX-zoom pan velocity). FOG-18 is mechanically present in the JSONL stream AND the user-observable Q1 SNAP at MAX zoom is gone. **Q1 closure CONFIRMED at MAX zoom regime (4.26M raw-px world coordinates).**

**Q1b baseline capture (NOT verdict-determining):** zoom-out gestures appear at rollups 23-26 (4.26M → 1.63M → 0.51M → 0.17M → 0.13M sweep in ~4 sec; uOffsetX = pxOriginX exactly under FOG-18 since `appliedUOffset == camera.pixelOrigin`) and rollups 53-56 (4.26M → 4.26M → 4.26M → 1.44M sweep in ~4 sec). These O(1M) raw-px uOffsetX jumps between consecutive rollups are the JSONL signature of the Q1b zoom-gesture failure mode — `MapCamera.pixelOrigin` jumps between zoom-snap boundaries, and the world-coordinate noise sampling has continuity in pure-pan space but NOT in zoom-gesture space (deferred for C2' follow-up plan; zoom-snap interpolation or zoom-invariant basis is the open design question).

**C3' regime — extreme-distance walk-time validation (DEBUG-02 stress-test): NOT REACHED.** Max `|pixelOriginX|` across all 80 rollups is **4,258,353 raw px** (essentially identical to Walk #4's 4,260,000 — this is the MAX zoom regime at Melun central anchor, NOT the ~50-100 km extreme-distance regime). The C3' threshold of ~7M raw px (corresponding to ~50 km from Melun's pxOrigin baseline of ~1.06M at zoom 13) was not crossed during the walk. Two interpretations:
- (a) Developer panned to MAX zoom (z=15/16) at Melun and used the full ~14-sec + ~21-sec sustained-pan windows there to test FOG-18; did not separately pan ~50-100 km laterally to exercise the C3' bbox-removal regime explicitly.
- (b) Developer's verbatim verdict *"panning 100% solved, no hard step at any level of zoom"* IMPLICITLY rolls a quick pan-to-distance into the global verdict (no markers fired across the entire 92-sec window — including any moments the developer may have briefly panned outside the original ~5×7 km Melun bbox).

**DEBUG-02 walk-time-validation status: deferred to Walk #6.** The structural fix is mechanically present (CameraConstraint.contain absent from `MapOptions(...)` per Plan 03.1-12 Task 2 — `flutter_map` 7.0.2 default `CameraConstraint.unconstrained()` applies); the JSONL stream confirms NO precision-induced artefacts in the regime actually reached (4.26M raw-px world coordinates, well within fp32's 16.7M exact-integer mantissa ceiling); but explicit ~50-100 km extreme-distance pan was not exercised in the JSONL trajectory. DEBUG-02 retains `Complete — Verified-by-test` with a Walk #5 walk-time-validation note that the bbox-removal IS active in the IPA (the absence of bbox-snap-back during the walk is implicit confirmation) but the explicit ≥7M pxOrigin threshold check is deferred. Per resume instructions: *"Walk-time validation deferred to Walk #6"* if extreme-distance not reached — adopted.

### FPS observations (PERF-03)

Coarse FpsCounterOverlay reading not separately recorded by developer. Walk #5's frame-delta probe rollups indicate `medianMs` median-of-rollups 1.228 ms ≈ ~814 fps frame-budget — the device's 120 Hz ProMotion display is paint-throttled by hardware vsync, so the frame budget headroom (PERF-07's 13× / 20× / 28× margins above the 16/32/48 ms thresholds) is the substantive measurement; FpsCounterOverlay sustained 120 fps during gesture activity per Walk #4 pattern (no developer complaint surfaced about FPS regression).

### Marker correlation (Walk #4 template applied)

**Marker count: ZERO across the entire 92-second walk window** (`walk5_dev_markers.jsonl` is empty after `grep -F 'dev_marker'` against the session log).

**Walk #4 baseline:** 2 dev_markers fired during sustained MAX-zoom pan at pxOriginX ~4.26M (47 raw px/s velocity, 110 raw px between markers, 2.36 sec apart). Walk #5's MAX-zoom sustained pan was longer (~14 sec window #1 + ~21 sec window #2 = ~35 sec total at MAX zoom across both windows, vs Walk #4's ~5 sec at MAX zoom) AND covered the same pxOriginX magnitudes (4.255M-4.258M, vs Walk #4's 4.26M). Walk #5 reached the SAME world-coordinate regime at MAX zoom for ~7× LONGER and **fired ZERO markers**.

**Marker-correlation interpretation: Q1 RESOLVED at MAX zoom under FOG-18.** The Walk #4 markers were the empirical evidence of the FOG-17a integer-wrap-modulo firing visibly at MAX zoom (the 1536-raw-px wrap landed on aligned cell boundaries for the deterministic digit-atlas grid — Walk #4 debug-spiral positive control showed zero steppiness — but on non-aligned offsets for the non-truly-periodic noise function — Walk #4 production fog showed visible SNAP at every wrap event). Plan 03.1-12 C1' eliminated the modulo. Walk #5 holds the corrected regime for ~35 sec at the SAME pxOriginX magnitude with ZERO marker fires. **The corrected mechanism interpretation from `03.1-FALSIFICATION-4.md` §Supplementary Walk Evidence (M1' FOG-17a wrap-firing-visibly) is empirically validated by Walk #5's FOG-18 verdict.**

### Q1b verbatim observation (NOT verdict-determining; feeds C2' follow-up plan)

Developer's verbatim post-Walk-5 statement (Q1b portion):

> "zoom still show same steppy displacement behavior in fog, in number we can see the numbers sliding (modifying the number to not have repetitive value would allow us to debug the amount of drift)"

Developer's verbatim CLAUDE.md `# current best version` post-Walk-5 update (zoom-related portion):

> "heavy hard steps while zooming
> perfect panning on the numbered shader and the mirk shader
> zooming on the numbered shader show the numbers moving all around : incorrect scalling?"

(⚠ CLAUDE.md `# current best version` section is developer-managed; quoted verbatim here as evidence; this falsification document does NOT modify CLAUDE.md.)

**Q1b status: RESIDUAL-EXPECTED (with character-refinement vs Walk #4).** Walk #4 framed Q1b as *"very fast hard step looking like changing the seed of a procedural generated image very fast"*. Walk #5 reframes it as *"steppy displacement behavior"* + *"the numbers sliding"* on the debug-spiral. The mechanism interpretation aligns with developer's CLAUDE.md *"incorrect scaling"* observation: the C2' / FOG-19 dimensional-mismatch hypothesis (refZoom-px vs screen-px in shader's `worldPx = fragUv * uResolution + uPixelOrigin` formula) predicts exactly this — the noise pattern's spatial frequency relative to screen-px changes across zoom-snap boundaries because the basis is in screen-px (uResolution) but should be in a zoom-invariant world-px reference. Q1b is EXPECTED present (C2' / FOG-19 is deferred); presence does NOT degrade the Walk #5 verdict.

**KEY DIAGNOSTIC-TOOL INSIGHT for the C2' follow-up plan:** developer's verbatim *"modifying the number to not have repetitive value would allow us to debug the amount of drift"* is actionable feedback for the debug-spiral shader (`atmospheric_fog_debug_spiral.frag`). Currently the digit-atlas cells render with repetitive 0-9 cycling. If cells encoded unique per-cell values — e.g., row-major integer like `row*10+col` rendered as a multi-digit string, OR row+col encoded as two distinct digit-glyphs — the developer could **measure the precise pixel-amount of drift between consecutive zoom snaps**, providing quantitative input for the C2' follow-up plan's design. Captured in `## Open Questions / Plan 03.1-14+ Candidate Axes` below as input for the C2' follow-up plan's diagnostic infrastructure axis. Note: implementing this requires a shader edit to `atmospheric_fog_debug_spiral.frag`, which falls under the diagnostic-instrumentation scope (the `# MIRL solution` MIRL constraint is for production code; debug instrumentation can be more flexible — but ratify with developer at C2' plan time).

### Subjective verdict (Criterion B)

Developer's verbatim post-Walk-5 statement (composite):

> "panning 100% solved, no hard step at any level of zoom
> zoom still show same steppy displacement behavior in fog, in number we can see the numbers sliding (modifying the number to not have repetitive value would allow us to debug the amount of drift)"

Developer's verbatim CLAUDE.md `# current best version` post-Walk-5 update:

> "docs(03.1-12): complete C1' + C3' Dart-only fix bundle plan #31
> 960f5c2
>
> heavy hard steps while zooming
> perfect panning on the numbered shader and the mirk shader
> zooming on the numbered shader show the numbers moving all around : incorrect scalling?"

**Q-axis interpretation:**

- **Q1 (sustained MAX-zoom pan SNAP):** **RESOLVED.** Developer's verbatim *"panning 100% solved, no hard step at any level of zoom"* + CLAUDE.md *"perfect panning on the numbered shader and the mirk shader"*. JSONL evidence: ~35 sec total at MAX-zoom pxOriginX 4.25M-4.258M with zero markers (Walk #4 baseline: 2 markers in 2.36 sec at MAX zoom). The FOG-17a-wrap-firing-visibly mechanism interpretation is CONFIRMED by Walk #5 (Walk #4's debug-spiral positive control showed zero steppiness because the digit-atlas IS truly periodic on 384, while production fog showed SNAP because noise function is NOT truly periodic on 384 → the wrap event itself was the bug, not the precision penalty FOG-17a was designed to address). FOG-18 modulo elimination delivers Q1 closure at MAX zoom regime.

- **Q1b (zoom-gesture seed-of-procedural-pattern-fast — OBSERVATION ONLY, NOT verdict-determining):** **RESIDUAL-EXPECTED.** Developer's verbatim *"zoom still show same steppy displacement behavior in fog... numbers sliding"* + CLAUDE.md *"heavy hard steps while zooming"* + *"incorrect scaling"*. C2' / FOG-19 (the targeted fix) is DEFERRED per Plan 03.1-12 scope_note + plan-checker review; Q1b residual is EXPECTED present and the verbatim observation feeds the C2' follow-up plan as empirical baseline. Mechanism interpretation: dimensional mismatch in shader formula `worldPx = fragUv * uResolution + uPixelOrigin` (refZoom-px basis vs screen-px uResolution) — under zoom-snap, uResolution (screen-px) jumps by 2× but uPixelOrigin scales differently, leaving the noise pattern's spatial frequency relative to map-features inconsistent across zoom-snap. Resolution requires fresh `/gsd:discuss-phase 3.1` round.

- **Q2 (rotation-correlated fog mis-coverage):** **RETAINED.** UX-02 walk-time-validated for the second walk in a row — `canvasTxMin/Median/Max` and `canvasTyMin/Median/Max` are identically `0.000000` across all 80 rollups. Rotation never fires. UX-02 InteractionOptions retained from Walk #4; no Plan 03.1-12 modifications.

- **Q3 (reveal-hole offset from blue dot):** **RETAINED.** No developer complaint surfaced about reveal-hole offset; the *"panning 100% solved"* verdict implicitly retains FOG-12 (clip-path canvasOffset shift) and FOG-13 (canvas.translate compensation). Plan 03.1-12 made no changes to these.

- **Q4 (fog visually translates with camera during pan):** **RETAINED.** *"panning 100% solved"* directly confirms Q4 — fog tracks the camera during pan at every zoom level. The previously-observed SNAP discontinuities at integer-wrap events (Walk #4 at MAX zoom) are now eliminated by FOG-18.

- **Q5 NEW (DEBUG-02 stress-test at ~50-100 km from Melun):** **PASSED-WITHIN-REACHED-REGIME / DEFERRED-EXPLICIT-WALK-TIME-VALIDATION.** The structural fix (cameraConstraint removal) is mechanically present in the IPA per Plan 03.1-12 Task 2's static-source assertion + runtime UnconstrainedCamera assertion. Walk #5's JSONL trajectory confirms NO precision-induced artefacts up to pxOriginX 4.26M (well within fp32 mantissa headroom). Explicit ≥7M pxOrigin extreme-distance regime was not crossed during the walk; developer's *"no hard step at any level of zoom"* implicitly rolls into Q5 PASSED but the explicit ~50-100 km pan-to-distance was not exercised in the JSONL stream. DEBUG-02 walk-time-validation deferred to Walk #6 (or rolls into the next Walk #6 verdict naturally if developer pans further then).

## Verdict

**`CONFIRMED-AFTER-FIX-PARTIAL`** — 6th Phase 3.1 verdict (Walk #1 ITERATING-WITH-MAJOR-PROGRESS / Walk #2 ITERATING-WITH-PARTIAL-PROGRESS / Walk #3 ITERATING-WITH-PARTIAL-PROGRESS-AND-ROTATION-REGRESSION / Walk #3b implicit marker-analysis / Walk #4 ITERATING-WITH-MAJOR-PROGRESS / Walk #5 CONFIRMED-AFTER-FIX-PARTIAL).

**Summary:** Plan 03.1-12 Dart-only fix bundle (C1' / FOG-18 modulo elimination + C3' / DEBUG-02 bounding-box removal) delivers Q1 closure at MAX zoom regime (zero dev_markers across ~35 sec at pxOriginX ~4.26M; Walk #4 baseline was 2 markers in 2.36 sec at the same regime). Q3 + Q4 + Q2 retained. PERF-07 retained with massive headroom (13×/20×/28×). FOG-18 JSONL signature confirmed (`uOffsetX*` exceeds 1537 — reaches 4.26M — and tracks `pixelOriginX` magnitude monotonically; Walk #4 was bounded under 1537). Q1b residual still present (developer's verbatim *"steppy displacement behavior"* during zoom; CLAUDE.md *"heavy hard steps while zooming"* + *"incorrect scaling"*) — EXPECTED since C2' / FOG-19 is DEFERRED to a follow-up plan after fresh `/gsd:discuss-phase 3.1` round resolves the dimensional-mismatch design question. Q5 / DEBUG-02 explicit ≥7M extreme-distance walk-time validation deferred to Walk #6 (max regime reached during Walk #5 was 4.26M = MAX zoom at Melun, NOT the ~50-100 km pan-to-distance regime).

**Verdict closure:**

- ✓ Q1 SNAP closed at MAX zoom: zero dev_markers across the ~35-sec total at pxOriginX 4.25M-4.258M (Walk #4 fired 2 markers in 2.36 sec at the same pxOrigin magnitude).
- ✓ Q3 reveal-hole co-located with blue dot: retained (no complaint surfaced).
- ✓ Q4 fog translates with map during pan: retained (*"panning 100% solved"*).
- ✓ Q2 rotation absence retained: canvasTx/Ty identically `0.000000` across all 80 rollups; UX-02 InteractionOptions retained.
- ✓ PERF-07 thresholds met: medianMs 1.228 ≤ 16, p95Ms 1.591 ≤ 32, maxMs 1.724 ≤ 48 (median-of-rollups across 80 rollups).
- — Q1b residual still present (deferred): *"steppy displacement behavior in fog... numbers sliding"* during zoom transitions. Phase 3.1 stays In Progress (6th iteration awaiting C2' plan + Walk #6).
- — Q5 / DEBUG-02 explicit extreme-distance walk-time validation: deferred to Walk #6 (max pxOrigin reached 4.26M; ~7M threshold not crossed).

**FOG-17a-wrap-firing-visibly mechanism interpretation EMPIRICALLY CONFIRMED:** Walk #4 supplementary appendix (post-walk dialogue 2026-05-04) documented that the debug-spiral shader (digit-atlas truly periodic on 384) showed ZERO steppiness during MAX-zoom pan, while production fog showed visible SNAP at every wrap event — falsifying FOG-17a's premise that the noise function was truly periodic on `kPocFogNoiseTilePx (=384)`. The wrap event ITSELF was the bug. Walk #5 confirms: with the modulo eliminated (FOG-18), MAX-zoom pan is "100% solved" per developer.

**Closure decision:** Walk #5 closes Phase 3.1's 6th iteration with **CONFIRMED-AFTER-FIX-PARTIAL**. Phase 3.1 stays `In Progress (6th iteration; awaiting C2' plan + Walk #6)`. Phase 4 + Phase 5 STAY BLOCKED. **THIS plan does NOT author the C2' follow-up plan** per scope (per Plan 03.1-13 must_haves §11). Next: `/gsd:discuss-phase 3.1` to resolve the dimensional-mismatch design question (refZoom-px basis vs screen-px uResolution; C-Q1b-1 lat/lon-anchored basis vs C-Q1b-2 Dart-side zoom-snap interpolation vs uZoomScale uniform vs other approach surfaced by discuss-phase), followed by `/gsd:plan-phase 3.1 --gaps` to author the C2' follow-up plan; subsequent Walk #6 closes Phase 3.1 with full CONFIRMED-AFTER-FIX (or further iteration if Q1b's mechanism turns out to be different from C2' / FOG-19's hypothesis).

**MirkFall migration recommendation:** **DEFERRED until Walk #6.** The Plan 03.1-12 fix bundle (FOG-18 + DEBUG-02) IS port-back-ready in isolation (delivers Q1 + Q4 closure at all zoom levels), but the full Phase 3.1 closure requires Q1b resolution via the C2' follow-up plan + Walk #6 confirmation before recommending PORT-BACK on the layered Phase 3.1 bundle.

## Open Questions / Plan 03.1-14+ Candidate Axes

This section captures the input for the upcoming C2' follow-up plan + any additional axes surfaced by Walk #5.

### (A) Q1b zoom-gesture residual — C2' / FOG-19 follow-up plan (PRIMARY)

Developer's verbatim *"zoom still show same steppy displacement behavior in fog, in number we can see the numbers sliding"* + CLAUDE.md *"heavy hard steps while zooming"* + *"zooming on the numbered shader show the numbers moving all around : incorrect scalling?"*.

The C2' follow-up plan needs `/gsd:discuss-phase 3.1` to resolve the dimensional-mismatch design question:
- **Option (a):** change `uResolution` semantics in both shaders from screen-px to refZoom-px — MIRL violation (changes ABI semantics; multi-shader future requires consistent uResolution meaning).
- **Option (b):** introduce `uZoomScale` uniform (zoom-snap normalised to a reference zoom level) — relaxes MIRL by adding a uniform but preserves uResolution screen-px semantics; consistent across all shaders.
- **Option (c):** Dart-side interpolation across zoom-snap boundaries — originally rejected as C-Q1b-2 in Walk #4 (paint-frame discontinuity at zoom-snap not gracefully recoverable Dart-side without per-frame Dart→shader uniform updates that defeat the single-snapshot discipline); worth re-examining post-Walk-5 with the developer's *"incorrect scaling"* framing.
- **Option (d):** other approach surfaced by discuss-phase (e.g., lat/lon-anchored noise basis projecting `pixelOrigin → lat/lng → noise sample`, which sidesteps the screen-px-vs-refZoom-px mismatch entirely; requires evaluating fp32 precision of the lat/lng-to-noise projection at extreme zoom).

### (B) Diagnostic-tool feedback from Walk #5 — debug-spiral unique-cell-numbers (SECONDARY, INPUT FOR C2' PLAN)

Developer's verbatim *"modifying the number to not have repetitive value would allow us to debug the amount of drift"*. The current `atmospheric_fog_debug_spiral.frag` renders cells with repetitive 0-9 digit-cycling. Replacing with unique per-cell values (e.g., row-major integer `row*10+col` rendered as multi-digit, OR two-digit-glyph encoding of (row, col)) would let the developer **measure the precise pixel-amount of drift between consecutive zoom snaps** — providing quantitative empirical input for the C2' follow-up plan's design choice between options (a)/(b)/(c)/(d).

The shader edit IS allowed under the `# MIRL solution` constraint scope analysis: MIRL is for production code; debug instrumentation has more flexibility (the digit-atlas + DigitAtlasBuilder + debug-spiral shader are all pure diagnostic infrastructure landed by Plan 03.1-07 Task 1). Capture in the C2' follow-up plan's diagnostic-infrastructure axis; ratify with developer at plan-authoring time.

### (C) C3' / DEBUG-02 explicit walk-time validation — Walk #6 (TERTIARY)

Walk #5 reached pxOriginX max 4.26M (= MAX zoom at Melun), NOT the ~7M+ extreme-distance threshold. The structural fix is mechanically present (cameraConstraint removed; static-source + runtime CI gates GREEN) but the explicit ~50-100 km pan-to-distance regime was not exercised in the JSONL trajectory. Walk #6 (which closes Phase 3.1 after C2' follow-up plan lands) naturally covers this if the developer panitellingly chooses to test extreme-distance during that walk. DEBUG-02 retains `Complete — Verified-by-test` until Walk #6 elevates it to `walk-time validated`.

Walk #1 + Walk #2 + Walk #3 + Walk #3b + Walk #4 + Phase 3 historical records UNTOUCHED.
