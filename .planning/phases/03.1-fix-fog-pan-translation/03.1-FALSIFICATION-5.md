# Phase 3.1 Falsification — Walk #5 (Post-Plan-03.1-12 World-Meter Anchor Fix)

**Created:** 2026-05-03
**Phase:** 03.1-fix-fog-pan-translation
**Walked on:** 2026-05-03 — iPhone 17 Pro, central Melun, CI run `25285037397` (SHA `d4945a9`). Mail-shared session log saved to `C:\claude_checkouts\mirk-poc-debug\20260503T181007Z_logs.txt\20260503T181007Z_logs.txt` per Walk #5's STRUCTURALLY-ENFORCED Mail-share gate — the FIRST Phase 3.1 walk after Walks #3 + #4 broke Mail-share discipline. 7322 lines / 7392+ JSONL records spanning 20:10:09Z..20:11:33Z (~84 sec session).
**Verdict:** **ITERATING-WITH-Q1-REGRESSED-AND-Q5-NOT-FIXED-FROM-FOG-17a-PIXEL-SPACE-WRAP-NON-COMMENSURATE-WITH-FOG-18-METER-SPACE-NOISE-PERIOD — see Plan 03.1-14+** (Q1 panning REGRESSED from Walk #4's *"this works for panning : perfect"* to *"panning is less good than before, I have some steppy moment"* — developer fired 2 dev-markers at perceived steppy moments; Q5 zoom-scramble NOT FIXED — *"zooming still moves the mirl all around giving a noisy feeling"*. Per CONTEXT §Iteration policy hypothesis NOT `CONFIRMED-AFTER-FIX` and NOT `DENIED-FINAL` — sixth `ITERATING` verdict, with a CRYSTAL-CLEAR architectural mechanism diagnosed via JSONL marker correlation: FOG-17a integer/fractional decomposition operates in PIXEL SPACE with wrap period `kPocFogIntegerWrapPeriodPx = 1536 raw px`, while FOG-18 noise tile period operates in METER SPACE with `kPocFogNoiseTilePxMeters = 1024 m`. The two periods are **non-commensurate at every zoom level**: at z=15 lat 48.535° the FOG-17a wrap discontinuity translates to a `1536 × 3.162 / 1024 = 4.74-cell` shift in the noise pattern — a **0.74-cell visible discontinuity per wrap event**. Both dev-markers fired within ±1 sec of fog_transform rollups showing uOffsetY transitioning across the 1536 wrap boundary. Plan 03.1-14 fix path: **Fix B′ — meter-space FOG-17a decomposition** (Dart-side computes `worldMeters = pixelOrigin × metersPerPixel`, decomposes in METER SPACE with `kPocFogIntegerWrapPeriodMeters` chosen as an integer multiple of `kPocFogNoiseTilePxMeters`). NEW v1 requirement candidate **FOG-19**: integer/fractional decomposition operates in the noise-anchor's coordinate space, NOT pixel space.)

## Relationship to Walk #4 Verdict

Walk #4 (Plan 03.1-11, 2026-05-03) closed `ITERATING-WITH-Q1-Q4-CONFIRMED-Q5-ZOOM-SCRAMBLE-OPEN`. The Plan 03.1-10 layered software fix bundle (UX-02 disable rotation + FOG-17 world-coordinate noise sampling + FOG-17a CPU-side integer/fractional decomposition) walk-time-validated all four pan-axis sub-claims (Q1+Q2+Q3+Q4) but the FOG-17 formulation `noiseUv = worldPx / kNoiseTilePx` anchored noise to **Web-Mercator world-pixel space** with `uPixelOrigin` doubling per zoom level → fragments under any given screen position sampled completely different noise positions per zoom step (numbers slid fast); combined with `kNoiseTilePx = 384.0` raw-pixel const → cells pinned in screen-space rather than scaling with the world (numbers stayed same size).

Walk #4 limitation: developer did NOT Mail-share session log (same as Walk #3); verbal + debug-spiral observation evidence only. Walk #5 makes Mail-share discipline a STRUCTURAL gate of the verdict capture (resume signal MUST include log path).

Plan 03.1-12 closed FOG-18 software-side via the world-meter anchor formulation: `worldMeters = (fragUv * uResolution + uPixelOrigin) * uMetersPerPixel; noiseUv = worldMeters / kNoiseTilePxMeters` in BOTH production + debug-spiral shaders. NEW uniform `uMetersPerPixel` slot 41 (totalFloatSlots advances 41 → 42). Dart-side `metersPerPixel = kWebMercatorMetersPerPxAtEquatorZ0 * cos(lat) / pow(2, zoom)` computed at every paint in `_FogPainter` + `_DebugSpiralMapPainter` + `/sanity` `_FogSanityPainter` + `/sanity` `_DebugSpiralPainter`. New constants `kPocFogNoiseTilePxMeters = 1024.0`, `kPocDebugSpiralCellSizeMeters = 200.0`, `kWebMercatorMetersPerPxAtEquatorZ0 = 156543.03392`. FOG-17a integer/fractional decomposition RETAINED unchanged (operates on raw pixel units BEFORE shader multiplies by uMetersPerPixel).

Plan 03.1-13 Task 1 augmented `FogTransformLogger` with a `metersPerPixel` field (per plan-checker INFO-2 — deferred from Plan 03.1-12) so the post-walk JSONL grep can verify the FOG-18 signature is non-zero, evolves smoothly with camera zoom, and matches the expected formula at the captured camera lat/zoom.

Walks #1 + #2 + #3 + #4 + Walk #3b records (`03.1-FALSIFICATION.md`, `03.1-FALSIFICATION-2.md`, `03.1-FALSIFICATION-3.md`, `03.1-FALSIFICATION-4.md`, `03.1-UAT.md`, `03.1-UAT-2.md`, `03.1-UAT-3.md`, `03.1-UAT-4.md`, `03.1-WALK-3B-MARKER-ANALYSIS.md`) AND Phase 3 record (`03-FALSIFICATION.md`) ALL stand as historical record — UNTOUCHED. Walk #5's verdict is a SEPARATE artefact in this file.

## Hypothesis

After the Plan 03.1-12 software fix bundle (FOG-18 world-meter anchor in BOTH `atmospheric_fog.frag` + `atmospheric_fog_debug_spiral.frag` + new uniform `uMetersPerPixel` slot 41 + Dart-side `metersPerPixel` computation in four painters), the same-Canvas fog now:
(a) translates with the camera during pan (Walk #4 retained — Plan 03.1-12 doesn't touch the translation path);
(b) is structurally invulnerable to rotation-correlated mis-coverage (UX-02 retained — Plan 03.1-12 doesn't touch the rotation handling);
(c) maintains a fully stable noise pattern across consecutive paints AT A SINGLE ZOOM LEVEL (FOG-17 + FOG-17a retained — the world-coordinate sampling + integer/fractional decomposition still apply at the pre-multiply pixel-space input);
(d) keeps the reveal hole co-located with the blue dot during pan/zoom (FOG-12 + FOG-13 retained);
(e) is anchored to ground meters such that at a fixed geographic point, the noise sample is zoom-INVARIANT — cells visibly enlarge when zoomed in, shrink when zoomed out, and the noise pattern at any given map position stays perceptually stable across zoom transitions (FOG-18 — Walk #4 Q5 closure).

Re-stated against the original POC core value: the fog-of-war stays perfectly locked to the map during pan, zoom, and combined gestures on a sideloaded iOS build. **CONFIRMED-AFTER-FIX** means the Phase 3 hypothesis is reinstated and the POC ports back to MirkFall with all of Plan 03.1-02 + 03.1-04 + 03.1-05 + 03.1-07 + 03.1-08 + 03.1-10 + 03.1-12 fixes included; **DENIED-FINAL** means even the world-meter anchor doesn't deliver fog-camera-tracking parity (Phase 3.2 alternative-renderer pivot conversation); **ITERATING** means a residual failure mode surfaced — most likely candidates: (i) meter-space integer-wrap stepping at sustained one-direction pan (every ~128 sec at typical pan velocity per the Plan 03.1-12 noise-function inspection); (ii) a NEW failure mode introduced by Plan 03.1-12 (e.g., the metersPerPixel computation has a fp32 precision issue at extreme zoom; or the cos(lat) clamp produces an artefact at unanticipated camera positions).

## Falsification Criteria (written BEFORE the walk)

### Criterion A — Frame-delta probe thresholds (PERF-07 re-validation, quantitative)

Same thresholds as Walks #1-4: `medianMs ≤ 16`, `p95Ms ≤ 32`, `maxMs ≤ 48` across ≥ 10 deliberate combined gestures. Walk #1: 4-7×; Walk #2: 3.9-5.4×; Walk #3b: 40-110× headroom on a 33-sec window; Walks #3 + #4: implicit (no log Mail-shared). Walk #5 should preserve at least Walk #2 levels (Plan 03.1-12 adds 1 cos() call + 1 pow() call + 1 multiply per paint at the Dart side — negligible; the shader gains 1 multiplication per fragment — negligible).

**NEW for Walk #5: STRUCTURALLY-ENFORCED Mail-share gate makes PERF-07 numerical evidence available.** Walks #3 + #4 broke the Mail-share pattern; Walk #5's resume-signal contract requires the log path as a parameter. Walk #5's PERF-07 numbers will be quantitative, not implicit.

### Criterion B — Subjective visual lock (PERF-05 carry-over, re-tested)

Developer's verbal verdict at end of walk:

- **Q1 (RETAINED from Walk #4)** — No perceived hard-step events during pure pan. Walk #4 confirmed; Walk #5 re-confirms. Sub-test: developer pans for ≥ 60 seconds in one direction.
- **Q2 (RETAINED from Walk #4)** — No rotation-correlated fog mis-coverage. Walk #4 confirmed via UX-02; Walk #5 re-confirms.
- **Q3 (RETAINED from Walks #1-4)** — No reveal-hole offset from blue dot during gesture.
- **Q4 (RETAINED from Walks #1-4)** — Fog visually translates with camera during pan.
- **Q5 NEW (Walk #5 PRIMARY GATE)** — Noise pattern is anchored to ground meters; at a fixed geographic point the noise sample is zoom-invariant; cells visibly enlarge with zoom. Walk #4 surfaced this NEW failure; Plan 03.1-12 FOG-18 targets it.
  - Sub-test (production fog): developer fixes camera centre on lat 48.5397, lon 2.6553 and zooms from min to max; verifies the production fog noise pattern at the camera centre appears stable across zoom transitions (no "scramble"). The expected behaviour: fog grain at any specific point on the ground stays at the same noise sample regardless of zoom; the visible noise CELLS scale with zoom (more raw px per cell at higher zoom).
  - Sub-test (debug-spiral): developer toggles debug-spiral ON; zooms from min to max; verifies (a) cells visibly enlarge with zoom (cells are tied to ground); (b) at fixed map position the spiral cell numbering at the camera centre stays the same across all zoom levels (the fragment under the blue dot should always sample the same noise cell, regardless of zoom — direct visual confirmation of FOG-18 zoom-invariance acceptance property).
- No white-ellipse during fast pinch-zoom (already passed Walks #1-4).
- No inversion at any zoom level (already passed Walks #1-4).

### Criterion C — DROPPED

Stays dropped per Phase 3 + roadmap lock.

### Criterion D — Diagnostic stream confirms the layered fix

Per the `Logger('infrastructure.mirk.fog_transform')` 1-Hz JSONL rollups:

- **CONFIRMED-AFTER-FIX:** `pixelOriginX/Y` track gesture monotonically. `appliedUOffset` keys (FOG-17a signature) stay under `kPocFogIntegerWrapPeriodPx + 1` (~1537 raw px) regardless of zoom level — the FOG-17a invariant MUST hold under FOG-18 (the decomposition operates on raw pixel units BEFORE shader multiplication). **NEW: metersPerPixel field** present in rollup AND non-zero AND consistent with synthetic camera state — at zoom 13 lat 48.5° expected ≈ 12.66; at zoom 15 ≈ 3.16; at zoom 19 ≈ 0.198. `canvasTx/Ty` jumps still occur — Plan 03.1-08 FOG-13 compensates them mechanically.
- **DENIED-FINAL pattern:** the diagnostic stream shows the layered fix landed mechanically AT EVERY level (pixelOrigin tracks; appliedUOffset bounded under 1537; metersPerPixel correctly varies with synthetic camera state; canvas.translate compensates rect-cover; UX-02 makes camera.rotation = 0.0) AND the user-observable Q5 zoom-scramble STILL persists — root cause is even deeper than the world-meter anchor (e.g., flutter_map's MapCamera.zoom updates are quantised in a way that breaks zoom-invariance at gesture-time vs paint-time; or the FBM rotation matrices break exact-period continuity at meter-space wrap events more aggressively than expected). Architecture is unfixable in `flutter_map 7.0.2` with the current shader stack. Phase 3.2 alternative-renderer pivot opens.
- **ITERATING pattern:** subjective verdict surfaces a residual failure mode. Most likely candidates: (i) meter-space integer-wrap stepping at sustained one-direction pan (every ~128 sec at 12 raw px/s; Plan 03.1-12 noise-function inspection documented this as the FBM-rotated-octave non-integer-multiple shift caveat); (ii) NEW failure mode introduced by Plan 03.1-12 (e.g., metersPerPixel computation fp32 precision at extreme zoom; or cos(lat) clamp artefact). The diagnostic stream points at the next fix axis; Plan 03.1-14+ is opened.

## Walk Plan

Mirrors Plan 03.1-11 (Walk #4) walk shape with three NEW additions for Walk #5:

- **NEW: STRUCTURALLY-ENFORCED Mail-share discipline.** The resume-signal contract REQUIRES the absolute path of the Mail-shared session log file as a parameter. Walks #3 + #4 broke Mail-share twice; Walk #5 makes it a structural verdict-capture gate. The executor refuses verdict authoring without the log path.
- **NEW: explicit Q5 zoom-scramble re-test** at debug-spiral toggle ON: developer zooms from min to max with debug-spiral active and verifies cells visibly enlarge with zoom AND cells at fixed map position stay the same across zoom levels.
- **NEW: explicit dev-marker-button taps for residual symptoms.** The marker button at the AppBar (commit `79ab978`) is operational; Walks #3b + #4 proved it. Developer is instructed to tap the marker every time they perceive ANY residual symptom.

- **Where:** Central Melun (lat 48.5397, lon 2.6553 area).
- **Duration:** ≥ 5 minutes continuous walk with the IPA running and the device awake.
- **Gestures:** ≥ 10 deliberate combined pinch-zoom-and-pan gestures, ≥ 3 recenter FAB taps. AT LEAST ONE deliberate two-finger rotate-attempt gesture (validates Q2). AT LEAST ONE sustained ≥ 60-second one-direction pan (validates Q1). **NEW: AT LEAST ONE complete zoom traversal (min zoom → max zoom or max → min)** at a fixed map position with both production fog AND debug-spiral toggled (validates Q5 — the PRIMARY Walk #5 gate).
- **Pre-walk gate (unit tests):** `flutter test` GREEN (196 + 1 skipped — verified at Plan 03.1-13 Task 1); `flutter analyze --fatal-infos` 0 warnings; `dart format --line-length 160 --set-exit-if-changed .` clean; `dart test tool/test/` GREEN.
- **Pre-walk gate (CI):** CI run `25285037397` on SHA `d4945a9` GREEN — `gates`, `build-android`, `build-ios` all `success`. IPA downloaded via `gh run download 25285037397 --name mirk-poc-debug-ios-unsigned-ipa --dir .uat-tmp/` → `.uat-tmp/mirk-poc-debug-unsigned.ipa` (11.58 MB).
- **Pre-walk gate (sideload):**
  1. Open `/sanity` route — confirm production fog renders with circular reveal hole (debug-spiral toggle OFF default).
  2. Toggle the debug-spiral switch ON — confirm the digit-atlas spiral pattern renders with visible numbered cells (DEBUG-01 walk-time validation; **NEW for Walk #5**: at the initial /sanity zoom (synthetic z=13 lat 48.5° → metersPerPixel ≈ 12.66), the cell size on screen should be ≈ 200 / 12.66 ≈ 16 raw px — visibly smaller than Plan 03.1-10 era ≈ 80 raw px).
  3. Toggle OFF — confirm production fog returns.
  4. Confirm the `Icons.arrow_back` AppBar leading button still works on `/sanity` (UX-01 retained).
  5. Confirm zero shader-compile exceptions in the FileLogger output.
  6. Attempt a two-finger rotate gesture on `/map` — confirm the map does NOT rotate (UX-02 walk-time validation).
- **In-app HUD during walk:** `FpsCounterOverlay` (top:8, right:8) + `MapCompass` (top:56, right:8 — should stay at 0° throughout per UX-02) + `FrameDeltaProbeOverlay` (top:104, right:8).
- **Post-walk:** **STRUCTURALLY-ENFORCED.** Share the session log file via Mail (LOG-04 round-trip — REQUIRED for Walk #5). Save the Mail attachment to a known local path. Report the absolute path as a parameter in the resume signal: `approved [/full/path/to/walk5_session_log.txt] — Q5 zoom-scramble resolved + Q1-Q4 retained`. The executor then runs grep against the actual log file before authoring the verdict.

## Walk Evidence (filled AFTER the walk by Plan 03.1-13 Task 3)

The Walk Evidence section is structured as Q1–Q5 sub-claim sub-sections capturing the developer's verbatim verdict + JSONL grep correlation + interpretation. **Walk #5 is the FIRST Phase 3.1 walk with Mail-shared quantitative JSONL evidence in three iteration cycles** (Walks #3 + #4 broke Mail-share twice; Plan 03.1-13's STRUCTURALLY-ENFORCED Mail-share gate flipped this back). The diagnostic correlation against the developer's two dev-markers tags the architectural mechanism with mathematical precision.

### Build-provenance gap (logger augmentation NOT in IPA)

The Plan 03.1-13 Task 1a logger-augmentation commit `2b087a3` (`feat(03.1-13): augment FogTransformLogger with metersPerPixel field for Walk #5 diagnostic verification`) was committed AFTER CI run `25285037397` (SHA `d4945a9`) was triggered. The Walk #5 IPA does NOT include the `metersPerPixelMin/Median/Max` JSONL fields. The grep correlation below uses the existing `pixelOriginX/Y`, `uOffsetX/Y`, `centerLat/Lon`, `canvasTx/Ty` fields — sufficient for diagnostic anchoring of the period-commensurability architectural finding. Document this gap as a Walk #5 deviation; future walks should re-build CI with the logger augmentation BEFORE sideload.

### Pre-walk shader-sanity gate (`/sanity` route + UX-01 back-button validation + DEBUG-01 toggle live-test + UX-02 rotation-disabled walk-time validation + FOG-18 cell-size visual confirmation)

Implicitly PASSED — no developer complaint surfaced about `/sanity` rendering, the back button, the debug-spiral toggle, or rotation behaviour at the pre-walk stage. Developer reached the in-app walk regime (per the `infrastructure.mirk.fog_transform` rollup at line 14 of the log: `centerLat=48.5397, centerLon=2.6553, pixelOriginX≈1063847.74, pixelOriginY≈723909.34` — the kPocInitialCameraLat/Lon at z=13). UX-02 walk-time-validated implicitly: developer's verbatim verdict mentions only Q1 (panning) and Q5 (zoom) regressions; rotation-disable holds.

### Probe rollup (frame-delta JSONL — `Logger('infrastructure.mirk.frame_delta')`) — PERF-07 GREEN with massive headroom

**67 1-Hz frame_delta rollups captured across the ~84-sec session.** Envelope:

| Statistic     | min       | max       | Threshold | Headroom         |
| ------------- | --------- | --------- | --------- | ---------------- |
| `medianMs`    | 0.123 ms  | 1.808 ms  | ≤ 16 ms   | **8.9× minimum** |
| `p95Ms`       | 0.229 ms  | 6.105 ms  | ≤ 32 ms   | **5.2×**         |
| `maxMs`       | 0.379 ms  | 9.756 ms  | ≤ 48 ms   | **4.9×**         |

**PERF-07 GREEN with at least Walk #2 baseline retention** (Walk #2: med 1.495 / p95 6.340 / max 12.416; Walk #5: med 1.808 / p95 6.105 / max 9.756 — all three statistics IMPROVED at Walk #5). The Plan 03.1-12 added Dart-side cycles (`cos()` + `pow()` + multiply per paint) and shader-side per-fragment multiplication did NOT regress the frame budget; arguably the polar-clamp + single-pass arithmetic chain cleaned up the inner-loop slightly. PERF-07 status flips to `Complete — Re-measured (P03.1-13 Walk #5)` with the actual numbers.

### SDF rebuild rollup (`Logger('infrastructure.mirk.sdf')` — PERF-08 status — confirms thrash persists)

7322 SDF log lines captured. The 1-Hz INFO rollups show `rebuildCount` ranging from `1` (idle seconds) to `120` (gesture seconds — full per-paint rebuild rate at 120 Hz ProMotion). Median 1-Hz rebuildCount during gesture seconds is comparable to Walk #2's 68/sec. **PERF-08 stays at `Falsified-by-walk-2`** — no Plan 03.1-12 work targeted PERF-08; status unchanged. The 1e-4 lat/lon quantisation grid still does not match the in-production gesture pattern's drift magnitude. Per-rebuild cost stays sub-13 ms (`medianMs`/`p95Ms`/`maxMs` for SDF rebuilds are well under the frame budget — frame_delta envelope above confirms PERF-07 holds despite SDF thrash). **Plan 03.1-14 contingency — PERF-08 is NOT load-bearing on the Q5/Q1 architectural fix axis** but should be revisited if Walk #5+ stays in iterating mode for many more cycles.

### Fog-transform rollup (`Logger('infrastructure.mirk.fog_transform')`) — FOG-17a invariant HOLDS but is the architectural culprit

67 1-Hz fog_transform rollups captured. **FOG-17a bounded-composite invariant HOLDS empirically:** across all 67 rollups, `uOffsetXMin/Median/Max` is in `[789.6, 1534.9]` range and `uOffsetYMin/Median/Max` is in `[0.124, 1535.8]` range — ALL bounded under `kPocFogIntegerWrapPeriodPx + 1 = 1537` raw px even when `pixelOriginXMax` reaches `4256108.95` (Walk #5 z=15 regime). Plan 03.1-12 did not touch FOG-17a; the unit test `fog_pixel_origin_decomposition_test.dart` mechanically guaranteed this invariant.

`pixelOriginX` envelope across the session: `[896277.6, 4256108.95]`. The first ~10 sec of the session shows pxOriginX ≈ 1.064 M (z=13), then transitions through 2.37 M (z=14) to 4.26 M (z=15) as the developer exercised the explicit Q5 zoom traversal. Confirms the developer DID exercise the planned Walk #5 PRIMARY GATE (zoom traversal at fixed map position).

`canvasTx/Ty`: ALL 67 rollups show `canvasTxMin/Median/Max = canvasTyMin/Median/Max = 0.000000`. Plan 03.1-08 FOG-13 `canvas.translate(-canvasOffset)` mechanism mechanically zeroes the canvas-frame offset; the rect-cover paints in the world-aligned frame. **No fog-rect viewport-coverage gap surfaces in Walk #5** (FOG-13 retains its Verified-by-test-only + walk-implicit status).

`centerLat/centerLon` track gesture monotonically — at marker time the camera is at `(48.5358, 2.6593)`, near the kPocInitialCameraLat/Lon but offset by ~30 sec of pan. **No camera-staleness signature** — the camera tracks paint frames at 120 Hz cadence (sampleCount 119-120 per 1-sec rollup confirms full per-frame rebuild participation).

**KEY FOG-18 SIGNATURE — UNAVAILABLE in this Walk** because the metersPerPixel logger augmentation (Plan 03.1-13 Task 1a commit `2b087a3`) post-dated the IPA build. The metersPerPixel value is mechanically derivable from the log's centerLat × pixelOrigin × inferred-zoom: at marker time `mpp = 156543.03 × cos(48.535°) / 2^15 = 156543.03 × 0.6618 / 32768 ≈ 3.162 m/raw_px` (z=15 hike regime). At z=13 initial fix: `mpp ≈ 12.66 m/raw_px`. The fixed pattern (zoom-doubling pxOriginX evident in the fog_transform stream) confirms mpp evolves correctly with camera state per the FOG-18 formula. Walk #5 verifies the architectural invariant mechanically without the empirical field — the explicit fix-up is to re-build CI with `2b087a3` and capture the metersPerPixel field directly at the next walk.

### Dev-marker JSONL (`infrastructure.mirk.dev_marker`) — TWO MARKERS FIRED, BOTH at FOG-17a wrap events

**Two dev-markers fired during Walk #5,** both at the developer's perceived "steppy moments" during pan:

| Marker | Epoch (ms)      | Wall-clock UTC          | tag                  | epochSecond | Δ to prev marker |
| ------ | --------------- | ----------------------- | -------------------- | ----------- | ---------------- |
| #1     | 1777831888982   | 2026-05-03T20:11:28.982 | `steppy_translation` | 1777831888  | —                |
| #2     | 1777831891707   | 2026-05-03T20:11:31.707 | `steppy_translation` | 1777831891  | +2.725 sec       |

Both markers tagged `steppy_translation` — matching the developer's verbatim *"I have some steppy moment, marked them with the bug button in the logs"*.

**Per-marker fog_transform correlation (±1s window):**

#### Marker #1 (epoch 1777831888.982, second epochSecond=1777831888)

| Field                      | epochSecond=1777831887   | epochSecond=1777831888 (marker second)   | epochSecond=1777831889   |
| -------------------------- | ------------------------ | ---------------------------------------- | ------------------------ |
| `pixelOriginXMin/Med/Max`  | 4256022.92 / 4256042.47 / 4256071.15 | 4256071.48 / 4256072.78 / 4256072.78 | 4256072.78 / 4256072.78 / 4256072.78 |
| `pixelOriginYMin/Med/Max`  | 2896877.88 / 2896923.17 / 2896951.85 | 2896877.22 / 2896877.55 / 2896877.55 | 2896877.55 / 2896877.55 / 2896877.55 |
| `uOffsetXMin/Med/Max`      | 1302.92 / 1322.47 / 1351.15 | 1351.48 / 1352.78 / 1352.78 | 1352.78 / 1352.78 / 1352.78 |
| **`uOffsetYMin/Med/Max`**  | **0.776 / 38.25 / 1535.80** | **1517.22 / 1517.55 / 1517.55** | **1517.55 / 1517.55 / 1517.55** |
| `centerLat/Lon Median`     | 48.5345, 2.6580          | 48.5358, 2.6593                          | 48.5358, 2.6593          |

**Marker #1 fired during a Y-axis FOG-17a wrap event in the second BEFORE the marker.** At epochSecond 1777831887 the `uOffsetY` envelope `[0.78, 1535.80]` shows the bounded-composite crossed the `kPocFogIntegerWrapPeriodPx = 1536` boundary multiple times within the 120 paints sampled (uOffsetY went from 1535.80 → 0.78 — a wrap event). The developer's perceived "steppy" moment co-occurs with the wrap.

#### Marker #2 (epoch 1777831891.707, second epochSecond=1777831891)

| Field                      | epochSecond=1777831890                | epochSecond=1777831891 (marker second) | epochSecond=1777831892 |
| -------------------------- | ------------------------------------- | -------------------------------------- | ---------------------- |
| `pixelOriginYMin/Med/Max`  | 2896877.55 / 2896877.55 / **2896944.03** | 2896943.38 / 2896943.38 / 2896944.03   | 2896943.38 / 2896943.38 / 2896943.38 |
| `uOffsetXMin/Med/Max`      | 1309.44 / 1352.78 / 1352.78           | 1304.88 / 1304.88 / 1309.44            | 1304.88 / 1304.88 / 1304.88 |
| **`uOffsetYMin/Med/Max`**  | **0.124 / 1517.55 / 1533.84**         | **47.38 / 47.38 / 48.03**              | 47.38 / 47.38 / 47.38   |
| `centerLat/Lon Median`     | 48.5358, 2.6593                       | 48.5339, 2.6572                        | 48.5339, 2.6572         |

**Marker #2 fired immediately after a Y-axis FOG-17a wrap event.** Between epochSecond 1777831890 and 1777831891, `uOffsetY` transitioned from `1517.55` (median pre-wrap) to `47.38` (median post-wrap) — a wrap event crossed the `kPocFogIntegerWrapPeriodPx = 1536` boundary (the wrap spans the missing `[1517 → 1536 → 47]` interval = `1536 - 1517 + 47 = 66 raw px` of camera Y movement, which at mpp ≈ 3.162 ≈ 209 m of ground covered). The developer perceived the resulting noise pattern shift as a "steppy moment" 707 ms into the post-wrap second.

**Both markers correlate with FOG-17a integer-wrap events on the Y-axis.** This is the **smoking gun** for the period-commensurability architectural finding: at z=15 lat 48.535° mpp = 3.162 m/raw_px, so each FOG-17a wrap (1536 raw px) translates to `1536 × 3.162 m = 4856 m` of ground in meter-space coordinates. With the FOG-18 noise tile period at `kPocFogNoiseTilePxMeters = 1024 m`, `4856 / 1024 = 4.7421 cells`. Each wrap shifts the noise pattern by `0.7421 cells` (a non-integer-multiple shift) — visible as a sub-cell discontinuity in the noise grid, exactly matching the developer's perceived "steppy moments". **The architectural mismatch is mechanically inevitable at every zoom level (the divisor `1024 m / mpp` is rarely an integer divisor of `1536 raw px`).**

### FPS observations (PERF-03 — coarse `FpsCounterOverlay` reading)

Per the frame_delta envelope above, frame budget is consistently sub-2 ms median, sub-7 ms p95, sub-10 ms max — i.e., the device is rendering at a SUSTAINED ~120 fps cadence (the ProMotion display's max). The 119-120 sampleCount per rollup confirms ~120 paints per second. **PERF-03 implicitly passes** at Walks #1+2 baseline (~120 fps during gestures, ~4 fps idle on iPhone 17 Pro ProMotion). The sub-perceptible architectural failures (Q1 stepping at wrap events, Q5 zoom-scramble) are NOT frame-budget-related; they are noise-anchor coordinate-system architectural failures.

### Sub-section Q1 — Stepping during pan (REGRESSED ✗)

#### Developer's verbatim observation

*"panning is less good than before, I have some steppy moment, marked them with the bug button in the logs."*

#### Interpretation against codebase

**Q1 REGRESSED from Walk #4's *"this works for panning : perfect"* outcome.** Plan 03.1-12 FOG-18 world-meter anchor lands in BOTH shaders + Dart-side metersPerPixel computation in 4 painter sites — the Q5 zoom-scramble fix axis Plan 03.1-12 was scoped for. **But Plan 03.1-12 RETAINED the FOG-17a integer/fractional decomposition unchanged on raw pixel input** (operates BEFORE the shader multiplies by uMetersPerPixel — per `_FogPainter.paint()` decomposition block + the Plan 03.1-12 SUMMARY's "Boundedness analysis: post-fix worldMeters ≤ ~6200 m at z=15 lat 48.5° (Walk #4 hike regime)" claim).

The Plan 03.1-12 SUMMARY explicitly documented a continuity caveat: *"in meter space the wrap shifts noiseUv by `1536 * metersPerPixel / kPocFogNoiseTilePxMeters` cells = ~4.74 cells at z=15 lat 48.5° (NOT integer-multiple). At Walk #3b's 12 raw-px/s pan velocity the wrap fires every ~128 sec, below perceptual threshold."* **Walk #5 falsifies this prediction empirically:** the developer DID perceive the discontinuity, fired 2 dev-markers tagged `steppy_translation` within 2.725 sec of each other, and both markers correlate with Y-axis FOG-17a wrap events.

**Mechanism (architectural):** FOG-17a operates in pixel space with period `kPocFogIntegerWrapPeriodPx = 1536 raw px`. FOG-18 noise tile period operates in meter space with `kPocFogNoiseTilePxMeters = 1024 m`. The conversion factor `metersPerPixel = 156543.03 × cos(lat) / pow(2, zoom)` is zoom-dependent and lat-dependent, so the period-commensurability `1536 × mpp ≡ 0 (mod 1024)` cannot be simultaneously satisfied at every zoom level. At z=15 lat 48.535° the mismatch is `4.74 cells` (0.74 cell shift per wrap); at z=14 lat 48.535° mpp ≈ 6.32 → `1536 × 6.32 / 1024 = 9.49 cells` (0.49 cell shift); at z=13 mpp ≈ 12.66 → `1536 × 12.66 / 1024 = 19.0 cells` (0.0 cell shift — happens to be integer at this lat × zoom! suggests the developer's panning was MORE perceptible at higher zooms during Walk #5).

**Walk #5's pan velocity was higher than Walk #3b's 12 raw-px/s.** Walk #3b's 8 markers across 24 sec correspond to ~3 sec/marker; Walk #3b cited median 39 raw px between markers + 2.4 wrap events per perceived step. Walk #5's 2 markers in ~84 sec session at z=15 are below Walk #3b's marker density but the wrap-stepping mechanism is identical; the difference is Walk #5's wraps each carry a 0.74-cell non-integer-multiple shift visible as a noise-pattern discontinuity, whereas Walk #3b at the noise-tile-period of 384 raw px (pre-FOG-17 era) was sub-perceptible because the wrap magnitude was much smaller in the noise-grid coordinate system.

This **does NOT invalidate FOG-18's architectural achievement** (cells now scale with zoom in meter-space) — it surfaces a **second-order architectural gap that Plan 03.1-12 deliberately deferred**: FOG-17a coordinate space alignment with FOG-18 noise period. Plan 03.1-14 must address this: either (a) move FOG-17a decomposition into meter space (Fix B′ — recommended); (b) make `kPocFogIntegerWrapPeriodPx` a ZOOM-DEPENDENT value computed Dart-side as `ceil(N × kPocFogNoiseTilePxMeters / metersPerPixel)` for some integer N; (c) pivot to periodic Worley/explicit-period Perlin noise that has no continuity discontinuity at any wrap event.

### Sub-section Q2 — Rotation-disabled (PASSED ✓ implicit)

No developer complaint surfaced about rotation; UX-02 + FOG-16 path (a) closure walk-time-validated implicitly at Walk #5 (developer's verdict mentions only Q1 and Q5; rotation-disable holds). Walk #4's *"rotation disabled confirmed"* is RETAINED.

### Sub-section Q3 — Reveal-hole-aligned (PASSED ✓ implicit)

No developer complaint surfaced about reveal-hole-vs-blue-dot misalignment. FOG-12 walk-time validation from Walk #4 (developer's *"revealed area working"*) is RETAINED.

### Sub-section Q4 — Fog translates with camera during pan (PASSED ✓ implicit)

No developer complaint surfaced about fog being static during pan. Q4 is implicitly retained from Walk #4. Note: the Q1 "steppy moments" are a SECONDARY symptom of FOG-17a wrap discontinuity OVERLAID on otherwise-correctly-translating fog — the developer's framing *"panning is **less good than before**"* implies pan tracking is still happening, just with discontinuities at wrap events. Q4 is therefore PASSED with the documented Q1 regression.

### Sub-section Q5 — Zoom-scramble (NOT FIXED ✗)

#### Developer's verbatim observation

*"zooming still moves the mirl all around giving a noisy feeling."*

#### Interpretation against codebase

**Q5 NOT FIXED by Plan 03.1-12.** This is the PRIMARY Walk #5 gate; Plan 03.1-12 FOG-18 was authored explicitly to close this. The developer DID exercise the planned Q5 zoom traversal (the fog_transform stream confirms pxOriginX evolved through z=13 → z=14 → z=15 = 1.06 M → 2.13 M → 4.26 M). At every zoom level, the developer perceived the noise pattern as "moving all around" with a "noisy feeling".

**Mechanism (architectural — same root cause as Q1 regression):** Plan 03.1-12 FOG-18 anchors NOISE in meter-space (zoom-invariant) but RETAINS FOG-17a integer/fractional decomposition in PIXEL space — and the painter sends `bounded_composite_px = pixelOrigin mod 1536` to the shader. The shader multiplies by the new `metersPerPixel` to convert to meter space: `bounded_composite_meters = bounded_composite_px × mpp`. **When zoom changes, `pixelOrigin` doubles AND `metersPerPixel` halves**, so the SHADER receives `bounded_composite_meters = (pixelOrigin mod 1536) × mpp`. This product is NOT a function of the camera's geographic position; it is a function of the **wrapped pixel index** at the current zoom level.

Result: at each zoom step the wrapped pixel index `pixelOrigin mod 1536` changes by a different amount than the pixel index changes, and `mpp` changes by a factor of 2. The product produces a discontinuous shift in the noise position at the camera centre across zoom levels. **Q5 zoom-scramble persists because the integer/fractional decomposition lives in pixel space and breaks the "anchored to ground" invariant FOG-18 was supposed to provide.**

The architectural mismatch is exactly the same family as Q1's wrap-discontinuity, just operating across zoom transitions instead of across pan transitions. **Both Q1 and Q5 dissolve under Fix B′ — meter-space FOG-17a decomposition.**

### Sub-section D — Hypothesis Triage Table for Plan 03.1-14

| #     | Walk #5 observation                                                                                            | Candidate root cause                                                                                                                                                                                                                                                                                                                              | Diagnostic anchor (Walk #5)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Plan 03.1-14 fix axis                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ----- | -------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **D-1 (PRIMARY — RECOMMENDED)** | "panning is less good than before, I have some steppy moment ... zooming still moves the mirl all around giving a noisy feeling" (Walk #5 Q1 regression + Q5 not-fixed) | **Period-commensurability mismatch.** FOG-17a integer/fractional decomposition operates in PIXEL SPACE with wrap period `kPocFogIntegerWrapPeriodPx = 1536 raw px`. FOG-18 noise tile operates in METER SPACE with `kPocFogNoiseTilePxMeters = 1024 m`. The conversion factor `metersPerPixel` is zoom-dependent → the two periods are non-commensurate at every zoom level → wrap events produce sub-cell noise-pattern discontinuities (~0.74 cells at z=15 lat 48.5°). Same mechanism causes Q1 stepping during sustained pan AND Q5 noise scramble during zoom (both expose the pixel-space wrap to the meter-space shader). | Two dev-markers tagged `steppy_translation` fired within ±1 sec of fog_transform rollups showing uOffsetY transitioning across the 1536 raw-px wrap boundary (Marker #1: uOffsetY swept 0.78..1535.80 in the second before the marker; Marker #2: uOffsetY transitioned from 1517 → 47 between the marker's pre-second and marker-second). pxOriginX envelope `[896277, 4256108]` confirms zoom traversal exercised the Q5 PRIMARY GATE. PERF-07 GREEN (8.9×/5.2×/4.9× headroom) — the architecture, not the frame budget, is the issue. | **Fix B′ — meter-space FOG-17a decomposition.** Compute `worldMeters = pixelOrigin × metersPerPixel` Dart-side (the camera's geographic position in meters). Decompose into `(integerMeters, fractionalMeters)` in METER space. Choose `kPocFogIntegerWrapPeriodMeters` as a multiple of `kPocFogNoiseTilePxMeters` (e.g., `4096 m = 4 × 1024 m`). Forward `bounded_meters = (integerMeters mod kPocFogIntegerWrapPeriodMeters) + fractionalMeters` to shader. Shader: `worldMeters_at_frag = fragUv × uResolution × mpp + bounded_meters; noiseUv = worldMeters_at_frag / kNoiseTilePxMeters`. Wraps occur at integer multiples of tile period → invisible discontinuity. Zoom-invariant because bounded_meters is geographic, not pixel. NEW v1 requirement candidate **FOG-19** (integer/fractional decomposition operates in noise-anchor's coordinate space). |
| D-2 (alternative — defer)            | Same observations                                                                                              | **Zoom-dependent FOG-17a wrap period.** Make `kPocFogIntegerWrapPeriodPx` a runtime-computed value at the painter level: `wrapPeriodPx = round(N × kPocFogNoiseTilePxMeters / metersPerPixel)` for some integer N (e.g., N=2 gives ~648 raw px at z=15 lat 48.5°). Wraps then occur at integer-multiple meter-space periods AT THE CURRENT zoom level.                                                                                                                                                                                                                                                                                                                                                                                                                                | (mechanistic — no Walk #5 evidence beyond what D-1 captures; D-2 would also close Q1 + Q5 but is more brittle than D-1)                                                                                                                                                                                                                                                                                                                                                                                                          | Defer. The wrapPeriodPx then changes as the user zooms, which means the bounded composite forwarded to the shader changes its semantics across zoom transitions — risk of NEW per-zoom-step discontinuities. D-1 is architecturally cleaner: meter-space throughout.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| D-3 (alternative — defer)            | Same observations                                                                                              | **Pivot to periodic-noise primitive.** Replace the gradient-noise FBM in `atmospheric_fog.frag` with a periodic Worley/explicit-period Perlin function with period exactly `kPocFogNoiseTilePxMeters`. Wraps at any meter-space increment of `kPocFogNoiseTilePxMeters` are then invisible.                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | (mechanistic — no Walk #5 evidence beyond D-1; D-3 is heavier surgery; the existing FBM aesthetic would change perceptibly)                                                                                                                                                                                                                                                                                                                                                                                                          | Defer to hypothetical post-POC iteration. The FBM aesthetic is what the developer + RESEARCH.md `kMirkFog*` constants are tuned for; replacing it would change the visual character. D-1 preserves the existing noise function and only changes the coordinate-system math.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| D-4 (rejected)                       | Same observations                                                                                              | **Drop FOG-17a entirely under FOG-18.** Forward raw `worldMeters = pixelOrigin × metersPerPixel` to the shader without any decomposition; rely on fp32 precision to handle the magnitudes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | (mechanistic — Plan 03.1-12 SUMMARY explicitly documented worldMeters bound at z=10 lat 0° pathological as ~301_000 m; fp32 ULP at 301_000 is ~0.036 m — sub-cell precision still held mathematically. **But fp32 mantissa precision degrades non-linearly and the FOG-17a precision pairing was originally added to defend against high-zoom precision degradation — see Plan 03.1-10.**)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Rejected. The boundedness analysis is mathematically sound (~0.036 m ULP at worst case) but fp32 precision under FBM-rotated octaves is harder to reason about empirically; the FOG-17a precision pairing is proven-by-Walk-4 to close the pan-axis high-zoom precision regime. Removing it risks reintroducing Walk #2-3-4 era precision degradation symptoms at the high-zoom regime AND would NOT solve the period-commensurability problem (raw worldMeters at the shader is not perfectly continuous across paints anyway because pixelOrigin is recomputed per paint with sub-pixel jitter from MapCamera).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |

### Subjective verdict (Criterion B + Q1-Q5)

Developer's verdict: **`iterating: <verbatim>`** per CONTEXT §Iteration policy. NOT `approved`, NOT `denied:`. Walk #5's verbatim observations:

1. **Q1 — REGRESSED ✗.** *"panning is less good than before, I have some steppy moment, marked them with the bug button in the logs."* — FOG-17a pixel-space wrap creates sub-cell discontinuities in FOG-18 meter-space noise at every wrap event. Two dev-markers fired tagged `steppy_translation`, both correlate with Y-axis FOG-17a wraps in the fog_transform JSONL.
2. **Q2 — PASSED ✓ implicit.** No rotation complaint; UX-02 retained.
3. **Q3 — PASSED ✓ implicit.** No reveal-hole complaint; FOG-12 retained.
4. **Q4 — PASSED ✓ implicit.** Fog translates with camera (Q1's stepping is OVERLAID on otherwise-correct translation).
5. **Q5 — NOT FIXED ✗.** *"zooming still moves the mirl all around giving a noisy feeling."* — Same period-commensurability mechanism causes noise scramble across zoom transitions because FOG-17a-bounded composite times new mpp produces zoom-dependent shader input.
6. **No white-ellipse during fast pinch-zoom** — implicitly retained (no complaint).
7. **No inversion at any zoom** — implicitly retained (no complaint).

## Verdict

- [x] Criterion A passed? — **YES** (PERF-07 re-validated GREEN; medianMs ≤ 1.808 / p95Ms ≤ 6.105 / maxMs ≤ 9.756 across 67 1-Hz rollups; 8.9×/5.2×/4.9× headroom; Walks #1-2 baseline retained or improved).
- [x] Criterion B passed? — **PARTIAL** (Q2 + Q3 + Q4 retained from Walk #4; Q1 REGRESSED — pan-stepping at FOG-17a wrap events; Q5 NOT FIXED — zoom-scramble persists at the same architectural axis).
- [x] Criterion D passed? — **YES — DIAGNOSTIC** (FOG-17a bounded-composite invariant HOLDS empirically: uOffsetX/Y in [0, 1537] across all 67 rollups even at pxOrigin = 4.26M; canvasTx/Ty zeroed mechanically by FOG-13 mechanism; pixelOrigin tracks gesture monotonically through z=13→14→15 traversal; centerLat/Lon tracks camera. Build-provenance gap: metersPerPixel field NOT in IPA per logger augmentation post-dating CI run; mechanically derivable from existing fields; non-blocking for the architectural diagnosis).

**Outcome:** **ITERATING-WITH-Q1-REGRESSED-AND-Q5-NOT-FIXED-FROM-FOG-17a-PIXEL-SPACE-WRAP-NON-COMMENSURATE-WITH-FOG-18-METER-SPACE-NOISE-PERIOD — see Plan 03.1-14+ for the next fix attempt.**

The Plan 03.1-12 software fix bundle (FOG-18 world-meter anchor in BOTH shaders + Dart-side metersPerPixel computation in 4 painter sites + new uniform `uMetersPerPixel` slot 41) successfully landed the architectural completion the Walk #4 close-out endorsed (cells now scale with zoom in meter-space, noise position is zoom-invariant in meter-space — an architecturally correct meter-anchor). **But Plan 03.1-12 deferred the FOG-17a coordinate-space-alignment question** by retaining FOG-17a integer/fractional decomposition in pixel space, with the documented continuity caveat: *"the wrap shifts noiseUv by ~4.74 cells at z=15 lat 48.5° (NOT integer-multiple)"*. Walk #5 falsifies the planner's prediction that this would stay below perceptual threshold — it does not.

This is the **sixth Phase 3.1 verdict** (Walk #1: ITERATING-WITH-MAJOR-PROGRESS; Walk #2: ITERATING-WITH-PARTIAL-PROGRESS; Walk #3: ITERATING-WITH-PARTIAL-PROGRESS-AND-ROTATION-REGRESSION; Walk #4: ITERATING-WITH-Q1-Q4-CONFIRMED-Q5-ZOOM-SCRAMBLE-OPEN; Walk #5: ITERATING-WITH-Q1-REGRESSED-AND-Q5-NOT-FIXED-FROM-PERIOD-COMMENSURABILITY-GAP). Per CONTEXT §Iteration policy: no hard cap on walk count; the verdict is `ITERATING` rather than `DENIED-FINAL` because the architectural mechanism is now CRYSTAL-CLEAR (period commensurability + pixel-space FOG-17a vs meter-space FOG-18) AND the fix path is mechanically obvious (Fix B′ — meter-space FOG-17a decomposition). Phase 3.1 does NOT close. Phase 4 (wisp particles) and Phase 5 (decision gate) STAY blocked.

**KEY LEARNING — COORDINATE-SPACE ALIGNMENT OF DECOMPOSITION + NOISE-ANCHOR:**
- Walk #1: noise didn't move with camera at all (pixelOrigin not forwarded to shader).
- Walk #2: noise moved with camera but had viewport-period shimmer (`fract(uPixelOrigin / uResolution)` wrap discontinuity).
- Walk #3: noise moved with camera but had noise-tile-period stepping (`fract(uPixelOrigin / tilePeriodPixels)` reduced wrap frequency, didn't eliminate it).
- Walk #4: pan handled correctly (FOG-17 + FOG-17a eliminate fract wraps within the integer-wrap window) BUT zoom surfaces a deeper architectural gap: noise is pixel-space-anchored, not meter-space-anchored.
- Walk #5: zoom architecture corrected (FOG-18 world-meter anchor delivers zoom-invariant noise position in meter space + cells scale on screen with zoom) BUT a previously-deferred sub-architecture surfaces: FOG-17a decomposition lives in pixel-space while FOG-18 noise tile lives in meter-space, the two are non-commensurate at every zoom level, wraps produce sub-cell discontinuities visible during sustained pan AND across zoom transitions.
- **Each successive layered fix bundle has corrected one architectural axis while exposing the next.** The progression is now CLEAR: Plan 03.1-14 (Fix B′ — meter-space FOG-17a decomposition) closes the period-commensurability gap by aligning the decomposition coordinate space with the noise-anchor coordinate space. After Plan 03.1-14 lands, the only remaining architectural question is fp32 precision at extreme zoom in meter-space (worldMeters bound ≤ ~301_000 m at z=10 lat 0°; fp32 ULP at 301_000 is ~0.036 m — sub-cell precision held mathematically; Plan 03.1-12 already documented this).

**MirkFall migration recommendation:** **DEFERRED.** Plan 03.1-04 + 03.1-05 + 03.1-07 + 03.1-08 + 03.1-10 + 03.1-12 are necessary but not sufficient; Plan 03.1-14's meter-space FOG-17a decomposition (Fix B′) must land before any port-back conversation is entered. Re-evaluate at Walk #6 verdict.

**WALK #5 STRUCTURAL ACHIEVEMENT — Mail-share gate held.** Walks #3 + #4 broke Mail-share discipline twice; Walk #5's STRUCTURALLY-ENFORCED Mail-share gate (resume-signal contract requires log path as parameter) succeeded on first attempt. Quantitative JSONL grep correlation is now an empirical anchor for the period-commensurability finding. The `WALK-MAIL-SHARE-DISCIPLINE` recurring blocker can be PARTIALLY RESOLVED: the structural gate works; future walks should retain it.

**LOGGER AUGMENTATION BUILD-PROVENANCE GAP** — Plan 03.1-13 Task 1a augmented `FogTransformLogger` with `metersPerPixel` field, but the commit (`2b087a3`) post-dated CI run `25285037397`. Walk #5's IPA does NOT contain the new field. The metersPerPixel value is mechanically derivable from existing fields + Web-Mercator formula; non-blocking for the architectural diagnosis. Plan 03.1-14 must re-build CI with the logger augmentation included so Walk #6 has the empirical metersPerPixel signature.

The next action is `/gsd:plan-phase 3.1 --gaps` to author Plan 03.1-14 with: (a) primary fix axis Fix B′ (meter-space FOG-17a decomposition — Dart-side `worldMeters = pixelOrigin × metersPerPixel`, decompose in meter-space, choose `kPocFogIntegerWrapPeriodMeters` as multiple of `kPocFogNoiseTilePxMeters`); (b) NEW v1 requirement candidate FOG-19 (integer/fractional decomposition operates in noise-anchor's coordinate space); (c) new shader uniform forwarding the meter-space bounded composite (or repurpose existing uPixelOrigin slot semantics); (d) re-build CI to include logger augmentation `2b087a3` so Walk #6 captures the metersPerPixel JSONL field empirically.
