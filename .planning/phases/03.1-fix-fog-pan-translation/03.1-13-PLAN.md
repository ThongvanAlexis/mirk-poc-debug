---
phase: 03.1-fix-fog-pan-translation
plan: 13
type: execute
wave: 2
depends_on: ["03.1-12"]
files_modified:
  - .planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md
  - .planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-5.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
gap_closure: true
scope_note: |
  Plan 03.1-13 = iPhone 17 Pro Walk #5 sideload UAT against the Plan 03.1-12
  fix bundle (FOG-18 modulo elimination + DEBUG-02 bounding-box removal).
  5th walk in Phase 3.1, 6th iteration overall.

  ## Scope reduction synced with Plan 03.1-12 (revised 2026-05-04)

  Plan 03.1-12's initial 3-task framing was reduced to 2 tasks post-plan-checker
  review (C2' / FOG-19 zoom-invariant world-coordinate basis DEFERRED to a
  follow-up plan after `/gsd:discuss-phase 3.1` resolves the dimensional-
  mismatch design question). Walk #5 therefore validates **two axes only**:

  - **C1' / FOG-18** — eliminate FOG-17a integer-wrap-modulo. Walk #4
    debug-spiral positive-control proved the wrap was the bug; Walk #5
    expected outcome at MAX zoom = zero markers fired (Walk #4 fired 2 markers
    in 2.36 sec at pxOriginX ~4.26M).
  - **C3' / DEBUG-02** — remove `MapOptions.cameraConstraint`. Walk #5
    diagnostic stress-test: pan ~50-100 km from Melun under removed constraint
    to verify Q1 SNAP doesn't reappear at distance (FOG-18 robustness at
    extreme world coordinates within fp32's 16.7M raw-px exact-integer
    mantissa ceiling).

  **Q1b zoom-gesture failure mode is OUT OF SCOPE for Walk #5's verdict** since
  C2' / FOG-19 is deferred. The walk SHOULD CAPTURE Q1b observation (still
  present? changed character? developer's verbatim words) as input for the
  upcoming C2' follow-up plan, but **Q1b residual does NOT block Walk #5
  verdict** from a new intermediate `CONFIRMED-AFTER-FIX-PARTIAL` outcome
  (defined under `## Verdict logic` below).

  ## Why a SEPARATE walk plan (mirrors Plans 03.1-03 / 03.1-06 / 03.1-09 / 03.1-11)

  Every Phase 3.1 fix bundle has been followed by a dedicated walk plan that
  authors the next-numbered FALSIFICATION + UAT documents. This separation:
  - Keeps the fix-axis attribution clean (commits in 03.1-12 are pure code;
    commits here are pure verdict authoring).
  - Lets the walk plan run with its own checkpoint:human-verify task without
    blocking the autonomous fix bundle plan.
  - Preserves the CONTEXT §Iteration policy structure: each walk is a
    falsifiable empirical event with a written-before-walk hypothesis +
    written-after-walk verdict, captured as a load-bearing artefact.

  ## Three-task structure (identical to Plan 03.1-11)

  - Task 1 (auto): pre-walk gate sequence + `03.1-FALSIFICATION-5.md` skeleton + `03.1-UAT-5.md` skeleton authoring.
  - Task 2 (checkpoint:human-verify): iPhone 17 Pro sideload UAT Walk #5.
  - Task 3 (auto): post-walk verdict authoring + REQUIREMENTS/ROADMAP/STATE updates + atomic commit.

  ## NEW for Walk #5 (vs Walks #1-#4)

  - **C1' validation regime:** developer pans deliberately at MAX zoom for ≥ 30
    sec to verify Q1 SNAP is gone (Walk #4's 2 markers fired in 2.36 sec at
    47 raw px/s; Walk #5's expected outcome with FOG-18 = zero markers at MAX
    zoom regardless of pan duration).
  - **C3' diagnostic stress-test (the C3' DIAGNOSTIC PAYOFF):** developer pans
    to extreme world coordinates (~50-100 km from Melun starting point) under
    the removed `cameraConstraint` and verifies Q1 SNAP does NOT reappear at
    distance. This is the empirical proof that FOG-18's no-modulo regime is
    precision-clean within fp32's 16.7M raw-px exact-integer mantissa ceiling.
    If Q1 SNAP DOES reappear at distance, FOG-18 alone is insufficient and the
    follow-up plan needs to consider precision-aware decomposition variants.
  - **Q1b zoom-gesture observation (NOT verdict-determining):** developer
    performs ≥ 5 deliberate zoom in/out transitions and CAPTURES verbatim
    observation (Q1b still present? changed signature? severity unchanged?).
    Q1b is EXPECTED to remain present since C2' is deferred — its presence
    does NOT degrade the Walk #5 verdict. The capture feeds the upcoming C2'
    follow-up plan as empirical baseline.
  - **JSONL appliedUOffset semantics shift:** Walk #4 stream showed `uOffsetXMax`
    bounded under 1537 (FOG-17a signature). Walk #5 stream will show
    `uOffsetXMax` tracking `pixelOriginX` magnitude monotonically (FOG-18
    signature: no modulo; values can exceed 1537 at high pixelOrigin including
    the C3' stress-test ~100-km regime where pixelOrigin may approach 7-10M).
    The grep-correlation pattern is INVERTED from Walk #4: bounded magnitude
    was "good" (FOG-17a in effect); now monotonic magnitude is "good" (FOG-18
    in effect). **NOTE:** zoom-gesture continuity in `uOffsetX*` deltas is
    NOT expected (would require FOG-19 / C2' which is deferred); zoom-gesture
    O(1M) raw-px snaps will still be visible in the JSONL stream under
    Walk #5's data — that's the captured Q1b baseline for the C2' follow-up.
  - **Two-finger rotate gesture validation:** preserved from Walk #4
    (UX-02 walk-time-validation).
  - **Mail-share discipline preserved:** post-walk session log Mail-shared
    so JSONL streams (`infrastructure.mirk.frame_delta` +
    `infrastructure.mirk.sdf` + `infrastructure.mirk.fog_transform`) can be
    grep-correlated against any dev-marker timestamps.

requirements: [FOG-04, FOG-05, FOG-06, FOG-07, FOG-11, FOG-15, FOG-18, DEBUG-02, UX-02, PERF-07]
user_setup:
  - service: github-actions
    why: "Wait for CI green on the post-Plan-03.1-12 SHA + download the IPA artifact for the Walk #5 sideload"
    dashboard_config:
      - task: "Confirm CI run status (`gh run list --branch main --limit 5` + `gh run watch <RUN_ID>`)"
        location: "GitHub Actions for the mirk-poc-debug repo"
      - task: "Download IPA artifact via `gh run download <RUN_ID> --name mirk-poc-debug-ios-unsigned-ipa --dir .uat-tmp/`"
        location: "GitHub Actions run detail page (or `gh` CLI)"
  - service: sidestore
    why: "Sideload the unsigned IPA onto iPhone 17 Pro for Walk #5"
    dashboard_config:
      - task: "Open SideStore on iPhone 17 Pro → install/refresh the .uat-tmp/mirk-poc-debug-unsigned.ipa"
        location: "SideStore iOS app + paired desktop pairing-file"
must_haves:
  truths:
    - "A new `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md` exists capturing the Walk #5 verdict. Walks #1 + #2 + #3 + #4 falsification documents AND the Walk #3b marker analysis are LEFT UNTOUCHED — historical record preserved across iteration cycles."
    - "A new `.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-5.md` exists capturing the Walk #5 transcript. Walks #1 + #2 + #3 + #4 UAT transcripts are LEFT UNTOUCHED."
    - "Pre-walk gate sequence runs cleanly: `flutter test` GREEN (180+ existing + Plan 03.1-12's rewritten tests: FOG-18 rewritten + DEBUG-02 group); `flutter analyze --fatal-infos` 0 warnings; `dart format --line-length 160 --set-exit-if-changed .` clean; `dart test tool/test/` GREEN; CI green on the post-Plan-03.1-12 SHA on `main`; IPA downloaded via `gh run download` to `.uat-tmp/mirk-poc-debug-unsigned.ipa`."
    - "The iPhone 17 Pro UAT Walk #5 is performed by the user. Walk shape matches Plan 03.1-11 (Walk #4) baseline PLUS three new validation regimes: (a) C1' validation — sustained ≥ 30-sec MAX-zoom pan with the dev-marker tapped on any residual SNAP; expected outcome: zero markers (Walk #4 fired 2 in 2.36 sec at MAX zoom). (b) C3' diagnostic stress-test — pan ~50-100 km from Melun starting point under the removed `cameraConstraint` with the dev-marker tapped on any reappearing SNAP at distance; expected outcome: zero markers (verifies FOG-18 is precision-clean at extreme world coordinates). (c) Q1b observation capture — ≥ 5 deliberate zoom in/out transitions; developer reports verbatim whether Q1b is unchanged / changed / improved relative to Walk #4 (Q1b is EXPECTED present since C2' is deferred — observation feeds the C2' follow-up plan). PLUS the Walk #4 baseline regimes preserved: ≥ 5 minutes through Melun + ≥ 10 deliberate combined pinch-zoom-and-pan gestures + ≥ 3 recenter taps + post-walk Mail-share + pre-walk `/sanity` shader-compile gate (production fog renders + DEBUG-01 toggle works on /sanity AppBar AND /map AppBar) + at least one two-finger rotate gesture attempt (UX-02 walk-time validation: rotation should NOT fire)."
    - "Mail-share discipline preserved — Walk #4 re-established post-walk Mail-share; Walk #5 retains. Session log Mail-shared so JSONL streams (`infrastructure.mirk.frame_delta` + `infrastructure.mirk.sdf` + `infrastructure.mirk.fog_transform`) can be grep-correlated against any dev-marker timestamps. The Walk #3b marker-correlation analysis (`03.1-WALK-3B-MARKER-ANALYSIS.md`) + Walk #4 marker-correlation pattern (`03.1-FALSIFICATION-4.md` §Marker correlation) are the templates."
    - "Post-walk: developer's verbal verdict (`approved` / `denied:` / `iterating:`) is captured. The walk evidence section of `03.1-FALSIFICATION-5.md` is filled with: developer's verbatim words + Q-axis interpretation (Q1 / Q1b-observation / Q2 / Q3 / Q4 / Q5-stress-test) + post-walk grep of `infrastructure.mirk.fog_transform` + `infrastructure.mirk.frame_delta` + `infrastructure.mirk.sdf` rollups + per-second composite extract + (if markers fired) marker-correlation analysis mirroring the Walk #4 template."
    - "PERF-07 is re-measured for Walk #5 (expected at Walk #4 levels): frame-delta probe rolling rollup over the ≥ 10-combined-gesture window with `medianMs ≤ 16`, `p95Ms ≤ 32`, `maxMs ≤ 48`. Walk #4 captured ~66× / ~6.4× / ~5.0× headroom. Walk #5 should preserve at least Walk #4 levels — the FOG-18 modulo elimination is a NET REDUCTION in arithmetic (one less modulo per paint); the C3' bounding-box removal does not affect the painter."
    - "JSONL diagnostic-stream FOG-18 signature confirmed: Walk #5's `infrastructure.mirk.fog_transform` rollups show `uOffsetXMax/uOffsetYMax` values that track `pixelOriginX/Y` magnitudes monotonically when zoom is held constant (FOG-18 signature: no modulo bound; values can exceed 1537 at high pixelOrigin including the C3' stress-test ~100-km regime). The `pixelOriginX/Y` JSONL field still logs the RAW `camera.pixelOrigin` for back-compat with Walks #1-4 grep tooling — under FOG-18 alone (without FOG-19), `appliedUOffset == camera.pixelOrigin` exactly so the two streams are now equal. **Q1b observation captured:** zoom-gesture rollups will still show `pixelOriginX/Y` (and consequently `uOffsetX*`) snapping O(1M) raw px between zoom integer levels — this is EXPECTED (Q1b is the deferred fix axis; the JSONL signature is the empirical baseline for the C2' follow-up plan)."
    - "Verdict written per the verdict logic in `## Verdict logic` below."
    - "On `CONFIRMED-AFTER-FIX-PARTIAL` (NEW intermediate verdict — see `## Verdict logic`): ROADMAP.md Phase 03.1 row in the Progress table stays `In Progress (6th iteration; awaiting C2' plan + Walk #6)`. Phase 4 + Phase 5 STAY blocked. STATE.md captures the partial closure outcome + the C2' follow-up trigger. REQUIREMENTS.md FOG-18 + DEBUG-02 flip from `Complete — Verified-by-test` to `Complete — Verified-by-test + walk-time validated (P03.1-13)`. UX-02 + FOG-16 + FOG-17 retain Walk #4's walk-time-validated status (assuming they pass Walk #5 too — re-validate). PERF-07 status updates to `Complete — Re-measured (P03.1-13)` with the actual numbers. FOG-04..07 + FOG-11 + FOG-15 stay `Falsified-in-production` with revision-clauses pointing at `03.1-FALSIFICATION-5.md` (Q1b residual is the documented gap; resolved when the C2' follow-up plan lands + Walk #6 confirms)."
    - "On `CONFIRMED-AFTER-FIX (FULL)` (very unlikely path; would only happen if Q1b spontaneously resolves with no C2' fix — no known mechanism for that under FOG-18 alone, but if it happens): ROADMAP.md Phase 03.1 row flips to `Complete (HYPOTHESIS CONFIRMED-AFTER-FIX)`. Phase 4 + Phase 5 unblock. REQUIREMENTS.md FOG-04..07 + FOG-11 + FOG-15 flip from `Falsified-in-production` to `Complete — Verified-by-walk (P03.1-13 CONFIRMED-AFTER-FIX 2026-05-XX after Plan 03.1-12 Dart-only fix bundle FOG-18 + DEBUG-02)`. Plan 03-08 DENIED verdict is REVERSED in the Phase 3 row note."
    - "On `DENIED-FINAL`: Q1 SNAP persists at MAX zoom even with the wrap eliminated. This rules out the FOG-17a-wrap-firing-visibly mechanism interpretation; suggests deeper architectural issue. ROADMAP.md Phase 03.1 row updates to `Complete (HYPOTHESIS DENIED-FINAL)`. Phases 4 + 5 stay `Blocked` (or transition to `Cancelled` per developer choice — flag for user decision; mirror Plan 03.1-11 pattern). REQUIREMENTS.md FOG-04..07 + FOG-11 + FOG-15 stay `Falsified-in-production` with updated revision-clause that points at `03.1-FALSIFICATION-5.md`. FOG-18 + DEBUG-02 still flip to `Complete — Verified-by-test + walk-time validated` (their CI gates ARE GREEN by Plan 03.1-12 construction; the structural fixes ARE in the codebase; the open question is whether they're SUFFICIENT). UX-02 + FOG-16 + FOG-17 retain Walk #4's walk-time-validated status. STATE.md captures the formal POC architectural counter-evidence."
    - "On `ITERATING` (a new failure mode surfaces beyond Q1b — e.g., a NEW precision-induced artefact at the C3' ~100-km stress-test that FOG-18 didn't anticipate; OR Q1 SNAP changes character at MAX zoom; OR a NEW failure mode the wrap removal exposes): a Plan 03.1-14+ is opened in a follow-up plan-phase invocation, with Walk #5's diagnostic stream as input. THIS plan does NOT author 03.1-14. FOG-18 + DEBUG-02 still flip to `Complete — Verified-by-test + walk-time validated` (the structural fixes ARE in the codebase; the open question is whether they're SUFFICIENT). PERF-07 updated with Walk #5 numbers regardless. FOG-04..07 + FOG-11 + FOG-15 stay `Falsified-in-production`. Phase 3.1 stays `In Progress (6th iteration)`."
    - "Phase 3 `03-FALSIFICATION.md` AND Phase 3.1 Walks #1 + #2 + #3 + #4 falsification documents AND `03.1-WALK-3B-MARKER-ANALYSIS.md` are LEFT UNTOUCHED — no edits, no overwrites. Historical records preserved across iteration cycles."
  artifacts:
    - path: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md"
      provides: "Phase 3.1 Walk #5 falsification document — pre-walk gate-pass evidence + post-walk verdict (`CONFIRMED-AFTER-FIX-PARTIAL` | `CONFIRMED-AFTER-FIX (FULL)` | `DENIED-FINAL` | `ITERATING`)"
      min_lines: 100
      contains: "Walk #5"
    - path: ".planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-5.md"
      provides: "Walk #5 evidence transcript — developer's verbatim verdict + Q-axis interpretation + JSONL grep correlation + (if markers fired) marker-correlation analysis"
      min_lines: 40
      contains: "Walk Evidence"
  key_links:
    - from: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md"
      to: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-4.md"
      via: "explicit cross-reference in Walk #5's `## Relationship to Walk #4 Verdict` section ('Walk #4 closed iterating-with-major-progress 2026-05-04; Walk #5 either confirms-partial or denies the Plan 03.1-12 Dart-only fix bundle: FOG-18 modulo elimination + DEBUG-02 bounding-box removal; C2' / FOG-19 deferred')"
      pattern: "03\\.1-FALSIFICATION-4\\.md"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md"
      via: "FOG-04..07 + FOG-11 + FOG-15 + FOG-18 + DEBUG-02 + UX-02 + PERF-07 status clauses include `(P03.1-13)` cross-reference; Revisions section appends a 2026-05-XX Walk #5 entry"
      pattern: "P03\\.1-13"
    - from: ".planning/STATE.md"
      to: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md"
      via: "Current Position + Decisions sections capture the Walk #5 verdict outcome"
      pattern: "Phase 3\\.1"
    - from: ".planning/ROADMAP.md"
      to: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md"
      via: "Phase 03.1 row + Plan 03.1-13 row updated with Walk #5 verdict; on CONFIRMED-AFTER-FIX (FULL), Phase 3 status note updated to reflect verdict reversal"
      pattern: "Walk #5"
---

<objective>
Run the pre-walk gates, perform the iPhone 17 Pro sideload UAT Walk #5, capture the developer's verdict, and produce `03.1-FALSIFICATION-5.md` — the Phase 3.1 Walk #5 falsification document. Walk #5 is the empirical re-test for the Plan 03.1-12 Dart-only fix bundle:

- **C1' / FOG-18** — eliminate FOG-17a integer-wrap-modulo (forward `camera.pixelOrigin` directly; Walk #4 debug-spiral positive-control proved the wrap was the bug, not the precision penalty it was designed to address).
- **C3' / DEBUG-02** — remove `MapOptions.cameraConstraint`; lets developer pan to extreme world coordinates (~50-100 km from Melun) for stress-test diagnostic verifying FOG-18 precision-cleanness at distance.

**C2' / FOG-19 is DEFERRED** to a follow-up plan after `/gsd:discuss-phase 3.1` resolves the dimensional-mismatch design question. **Q1b zoom-gesture residual is EXPECTED present in Walk #5** and does NOT block the verdict — it is captured as empirical baseline for the C2' follow-up plan.

The Walk #5 verdict either:
- Closes Walk #5 with `CONFIRMED-AFTER-FIX-PARTIAL` (NEW intermediate verdict — Q1 SNAP closed at MAX zoom + C3' stress-test passes + PERF-07 retained + Q3/Q4 retained, but Q1b residual still present awaiting C2' follow-up). Phase 3.1 stays `In Progress (6th iteration awaiting C2' plan + Walk #6)`.
- Closes Walk #5 with `CONFIRMED-AFTER-FIX (FULL)` (very unlikely — would only happen if Q1b spontaneously resolves with no C2' fix; no known mechanism for that under FOG-18 alone).
- Strengthens to `DENIED-FINAL` if Q1 SNAP persists at MAX zoom even with the wrap eliminated (rules out FOG-17a-wrap-firing-visibly mechanism interpretation; suggests deeper architectural issue).
- Captures a sixth `ITERATING` verdict if a new failure mode surfaces (e.g., NEW precision-induced artefact at C3' ~100-km stress-test; OR Q1 SNAP changes character; OR something unforeseen the wrap removal exposes). Plan 03.1-14+ pending in follow-up `/gsd:plan-phase 3.1 --gaps` invocation.

Purpose: this is the empirical gate Phase 3.1 needs after the Plan 03.1-12 Dart-only fix bundle. Plan 03.1-12 software-completed the C1' + C3' axes (C2' deferred); Walk #5 is the architectural-evidence gate that flips FOG-18 + DEBUG-02 from `Verified-by-test` to `walk-time validated` AND captures Q1b empirical baseline for the C2' follow-up. Walk #5 does NOT close FOG-04..07 + FOG-11 + FOG-15 — those wait for the eventual fully-CONFIRMED-AFTER-FIX walk after the C2' follow-up plan lands.

Output: 1 new `03.1-FALSIFICATION-5.md` (the load-bearing artefact); 1 new `03.1-UAT-5.md` (the walk-evidence transcript); REQUIREMENTS.md status updates for FOG-04..07 + FOG-11 + FOG-15 + FOG-18 + DEBUG-02 + UX-02 + PERF-07; ROADMAP.md Phase 03.1 Progress-table row update; STATE.md Current Position + Decisions updates. Phase 3's `03-FALSIFICATION.md` AND Phase 3.1 Walks #1 + #2 + #3 + #4 records AND `03.1-WALK-3B-MARKER-ANALYSIS.md` ALL left UNTOUCHED.

### MIRL constraint compliance (HARD — see CLAUDE.md `# MIRL solution`)

The Plan 03.1-12 fix bundle that Walk #5 validates is Dart-only — zero shader source modifications. The shader ABI (41 float slots; slot 3..4 = `uPixelOrigin` semantics) is preserved bit-for-bit. Both `atmospheric_fog.frag` AND `atmospheric_fog_debug_spiral.frag` inherit the fix automatically.

### Iteration policy (carried forward — unchanged)

Plan-revise-walk loop. **No hard cap on walk count.** Walk #5 is the 5th attempt under Phase 3.1 (Walk #1 P03.1-03 + Walk #2 P03.1-06 + Walk #3 P03.1-09 + Walk #3b implicit marker analysis + Walk #4 P03.1-11 + Walk #5 here). Walk #5 closes against the Plan 03.1-12 bundle (C1' + C3'); Walk #6 is reserved for the C2' follow-up plan that lands after `/gsd:discuss-phase 3.1` resolves the FOG-19 dimensional-mismatch design question.

## Verdict logic

The verdict-determining axes for Walk #5 are Q1 (max-zoom SNAP), Q3 (reveal hole), Q4 (translation), Q5 (C3' stress-test at distance), and PERF-07. Q1b (zoom-gesture seed-of-procedural-pattern-fast) is OUT OF SCOPE for the verdict-determination but IS captured as empirical observation for the C2' follow-up plan.

**`CONFIRMED-AFTER-FIX-PARTIAL`** (NEW intermediate verdict; expected outcome if FOG-18 closes Q1 SNAP successfully):
- Q1 SNAP closed at MAX zoom: zero dev_markers fire during the ≥ 30-sec one-direction sustained MAX-zoom pan.
- C3' stress-test passes: zero dev_markers fire during the ~50-100 km extreme-distance pan; fog renders correctly at distance; no precision-induced artefacts at high pixelOrigin magnitudes.
- Q3 reveal-hole co-located with blue dot: retained.
- Q4 fog translates with map during pan: retained.
- Q2 rotation absence retained (canvasTx/Ty identically zero in JSONL stream as in Walk #4; UX-02 retained).
- PERF-07 thresholds met: medianMs ≤ 16, p95Ms ≤ 32, maxMs ≤ 48 across the ≥ 10-combined-gesture window.
- **Q1b residual still present (deferred):** zoom-gesture markers may fire; "very fast hard step looking like changing the seed of a procedural generated image very fast" symptom may still be observable. Phase 3.1 stays In Progress (6th iteration awaiting C2' plan + Walk #6).

**`CONFIRMED-AFTER-FIX (FULL)`** (very unlikely path; ALL Q-axes resolved including Q1b):
- All `CONFIRMED-AFTER-FIX-PARTIAL` criteria PLUS Q1b also resolved (zero zoom-gesture markers; smooth noise pattern across zoom transitions).
- Would only happen if Q1b spontaneously resolves with no C2' fix — no known mechanism for that under FOG-18 alone. Document the surprising outcome thoroughly if it does happen.
- Phase 3.1 closes; Phase 4 + Phase 5 unblock; Plan 03-08 DENIED verdict REVERSED.

**`DENIED-FINAL`** (Q1 SNAP persists at MAX zoom even with wrap eliminated):
- The `Q1 SNAP closed at MAX zoom` criterion FAILS: dev_markers fire during the sustained MAX-zoom pan even though `% kPocFogIntegerWrapPeriodPx` is gone from `_FogPainter.paint()`.
- Rules out the FOG-17a-wrap-firing-visibly mechanism interpretation. The mechanism is something else (deeper precision issue beyond fp32 mantissa, MapCamera.pixelOrigin discontinuity at zoom boundaries that isn't covered by Q1b, MobileLayerTransformer paint-pipeline order issue, etc.).
- Architectural counter-evidence: even the simplest Dart-only fix axis (eliminate the wrap) doesn't deliver Q1 closure.
- Phases 4 + 5 stay BLOCKED or transition to CANCELLED per developer choice.

**`ITERATING`** (a new failure mode surfaces beyond Q1b):
- Q1 SNAP closes at MAX zoom AT MELUN (FOG-18 wrap-elimination works) BUT C3' stress-test fails: NEW precision-induced artefact appears at extreme world coordinates (e.g., pixelOrigin > 8M raw px exposes fp32 ULP issue that Walk #4's max 4.26M didn't reach), OR Q1 SNAP changes character (different temporal pattern, different visual signature), OR something unforeseen surfaces.
- Plan 03.1-14+ pending in follow-up plan-phase invocation. THIS plan does NOT author it.

The verdict tree is documented as a decision flow in `03.1-FALSIFICATION-5.md` §Verdict, populated by Task 3 post-walk.
</objective>

<execution_context>
@C:/Users/oliver/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oliver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-CONTEXT.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-2.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-3.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-4.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-2.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-3.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-4.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-WALK-3B-MARKER-ANALYSIS.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-VALIDATION.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-11-PLAN.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-11-SUMMARY.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-12-PLAN.md

<interfaces>
<!-- Walk #5 mirrors Walk #4 (Plan 03.1-11) shape-for-shape with adjusted hypothesis re-statements. -->

`03.1-FALSIFICATION-4.md` Walk #4 structure (the immediate predecessor template):
1. Title + metadata (`Created`, `Phase`, `Walked on`, `Verdict`)
2. `## Relationship to Walk #3 Verdict` (Walk #5 inserts `## Relationship to Walk #4 Verdict`)
3. `## Hypothesis`
4. `## Falsification Criteria (written BEFORE the walk)` — `### Criterion A`, `### Criterion B`, `### Criterion C — DROPPED`, `### Criterion D — Diagnostic stream`
5. `## Walk Plan`
6. `## Walk Evidence (filled AFTER the walk)`
7. `## Verdict`
8. (Walk #4 specific) `## Supplementary Walk Evidence` — only included if Walk #5 surfaces a debug-spiral correction or post-walk dialogue clarification

Walk #5's `03.1-FALSIFICATION-5.md` mirrors Walk #4's structure exactly with these specialisations:
- Title: `# Phase 3.1 Falsification — Walk #5 (Post-Plan-03.1-12 Dart-Only Fix Bundle: FOG-18 modulo elimination + DEBUG-02 bounding-box removal; C2' / FOG-19 DEFERRED)`
- Add `## Relationship to Walk #4 Verdict` section pointing at `03.1-FALSIFICATION-4.md`; summarise the Walk #4 ITERATING-WITH-MAJOR-PROGRESS outcome and the Plan 03.1-12 scope reduction (3-task framing → 2-task; C2' / FOG-19 deferred to follow-up after `/gsd:discuss-phase 3.1` resolves dimensional-mismatch design question).
- Hypothesis re-statement: "After the Plan 03.1-12 Dart-only fix bundle (FOG-18 eliminating the FOG-17a integer-wrap-modulo + DEBUG-02 removing `MapOptions.cameraConstraint` for stress-test diagnostic), the same-Canvas fog now: (a) translates with the camera during pan at MAX zoom regimes (FOG-18 eliminates the SNAP wrap event observed at MAX zoom in Walk #4); (b) maintains correctness at extreme world coordinates ~50-100 km from Melun (FOG-18 precision-clean within fp32's 16.7M raw-px exact-integer mantissa ceiling — well above Walk #4's max 4.26M and well above the C3' stress-test's expected ~7-10M); (c) keeps the reveal hole co-located with the blue dot during pan/zoom (FOG-12 retained); (d) covers the entire viewport at any canvas-translation magnitude (FOG-13 retained); (e) is structurally invulnerable to the rotation-correlated fog mis-coverage failure mode (UX-02 retained). **Q1b zoom-gesture residual is EXPECTED present** since FOG-19 is deferred — its presence is the captured empirical baseline for the C2' follow-up plan, NOT a verdict-determining failure."
- Criterion A: same thresholds (PERF-07 carry-over): `medianMs ≤ 16`, `p95Ms ≤ 32`, `maxMs ≤ 48`. Walk #4: ~66× / ~6.4× / ~5.0× headroom; Walk #5 should preserve at least Walk #4 levels (FOG-18 reduces arithmetic by one modulo; C3' bounding-box removal does not touch the painter).
- Criterion B sub-claims:
  - **Q1**: "No perceived hard-step events during sustained MAX-zoom pan" — Walk #4 had Q1 PARTIAL (resolved at default zoom, residual at MAX zoom). Plan 03.1-12 FOG-18 targets the residual MAX-zoom case. Sub-test: developer pans for ≥ 30 sec at MAX zoom in one direction; expected outcome (CONFIRMED-AFTER-FIX-PARTIAL or FULL): zero dev_markers fired (Walk #4 fired 2 markers in 2.36 sec at MAX zoom).
  - **Q1b (OBSERVATION ONLY, not verdict-determining)**: "Zoom-gesture-correlated very-fast hard-step (seed-of-procedural-pattern-fast)" — Walk #4 NEW failure; C2' / FOG-19 (the targeted fix) is DEFERRED. Sub-test: developer performs ≥ 5 deliberate zoom in/out transitions and CAPTURES verbatim observation (still present? changed character? severity unchanged vs Walk #4?). Q1b is EXPECTED present; presence does NOT degrade the verdict.
  - **Q2**: "No rotation-correlated fog mis-coverage" — Walk #4 RESOLVED via UX-02; re-confirm in Walk #5 (UX-02 not modified by Plan 03.1-12).
  - **Q3**: "No reveal-hole offset from blue dot during gesture" — Walks #1 + #2 + #3 + #4 retained; re-confirmation under Plan 03.1-12's coordinate-system simplification (FOG-12 + FOG-13 unchanged).
  - **Q4**: "Fog visually translates with camera during pan" — Walks #1 + #2 + #3 + #4 retained; re-confirmation. The FOG-18 wrap removal eliminates the integer-wrap visible jump but does not change the underlying translation continuity; expected to retain.
  - **Q5 NEW (DEBUG-02 stress-test)**: "No precision-induced artefacts at extreme world coordinates (~50-100 km from Melun)". Sub-test: developer pans far from initial Melun bbox (now possible since DEBUG-02 removed cameraConstraint); expected outcome (CONFIRMED-AFTER-FIX-PARTIAL): fog renders correctly + zero markers + no visual anomalies. **Q5 IS verdict-determining** (the C3' diagnostic payoff).
  - No white-ellipse during fast pinch-zoom (already passed Walks #1-4).
  - No inversion at any zoom level (already passed Walks #1-4).
- Criterion C stays DROPPED.
- Criterion D (Diagnostic stream): `pixelOriginX/Y` continues to log the RAW `camera.pixelOrigin` (back-compat with Walks #1-4 grep tooling). `appliedUOffset` (preserved JSONL field name) now equals `camera.pixelOrigin` exactly under FOG-18 (modulo gone; basis derivation NOT introduced — C2' deferred). **Signature shifts from Walk #4:**
  - Walk #4: `uOffsetXMax` bounded under 1537 across all rollups (FOG-17a in effect).
  - Walk #5: `uOffsetXMax` tracks `pixelOriginX` magnitude monotonically when zoom held (FOG-18 in effect: no modulo bound; values can exceed 1537 at high pixelOrigin including the C3' stress-test ~100-km regime where pixelOrigin may approach 7-10M raw px).
  - **Pattern check (verdict-determining):** during the sustained MAX-zoom pan in Walk #5's rollups, `uOffsetX*` values may exceed 1537 (Walk #4 max was 1535.74) AND no marker fires AND `pixelOriginX` evolves smoothly = FOG-18 working.
  - **Q1b baseline capture (NOT verdict-determining):** during a zoom-out gesture in Walk #5's rollups, `uOffsetX*` deltas across consecutive rollups WILL still show O(1M) jumps (Walk #4's rollups 20-24 captured `pixelOriginX` 4.10M → 0.81M sweep — Walk #5's `uOffsetX*` will show the same sweep since `appliedUOffset == camera.pixelOrigin` under FOG-18). This is the Q1b empirical baseline for the C2' follow-up plan.
  - `canvasTx/Ty` continues to be identically zero (UX-02 retained).
  - `canvasRotation` field NOT added (UX-02 makes rotation moot).

`03.1-UAT-5.md` mirrors `03.1-UAT-4.md` shape-for-shape: metadata block + walk protocol + walk evidence (frame-delta + SDF + fog-transform rollups) + subjective verdict + verdict + deviations + summary + (optional) gaps section if Walk #5 surfaces residual failure modes + `gaps:` YAML in frontmatter for any opened-by-Walk-#5 gap entries.

The pre-walk gate sequence is identical to Plan 03.1-11 Task 1 (six gates: flutter test / analyze / format / tool tests / CI green / IPA download). Plan 03.1-11 Task 1 ran the gate sequence at SHA `b31766f`; Plan 03.1-13 Task 1 runs against the post-Plan-03.1-12 SHA (let `gh run list --branch main --limit 5` resolve at execute-phase time).

The walk shape is identical to Plan 03.1-11 PLUS the C1' + C3' validation regimes (and Q1b OBSERVATION capture) enumerated in `<scope_note>` §"NEW for Walk #5". Dev-marker workflow unchanged: tap on any residual symptom; tag `"steppy_translation"` (hardcoded since Plan 03.1-09); post-hoc disambiguation via timing.

</interfaces>

<out_of_scope>
The following items are EXPLICITLY out of scope for Plan 03.1-13:

1. **Authoring Plan 03.1-14+.** If Walk #5 returns `ITERATING`, the next plan is opened in a follow-up `/gsd:plan-phase 3.1 --gaps` invocation that consumes Walk #5's diagnostic stream as input. THIS plan does NOT author 03.1-14+.

2. **Authoring the C2' follow-up plan.** Walk #5 captures Q1b empirical baseline; the C2' follow-up plan is authored in a SEPARATE invocation that begins with `/gsd:discuss-phase 3.1` to resolve the dimensional-mismatch design question (refZoom-px basis vs screen-px uResolution; relax MIRL via uZoomScale uniform vs Dart-side interpolation across zoom-snap vs other approach). THIS plan does NOT author the C2' follow-up.

3. **Re-enabling cameraConstraint.** DEBUG-02 (Plan 03.1-12 Task 2) removed it; Walk #5 validates it stays out for the stress-test diagnostic. Re-enabling for production UX is a Phase 5 hardening concern.

4. **MIRK-01 ABI formalization, MIRK-01 acceptance test trio, ABI uniform rename `uPixelOrigin` → `uWorldOffset`, JSONL field rename `uOffsetX*` → `uWorldOffsetX*`.** All DEFERRED per 03.1-CONTEXT.md `<deferred>` block. Walk #5 outcome may inform whether to revisit (CONFIRMED-AFTER-FIX-PARTIAL → can revisit at leisure during/after Phase 5 hardening; ITERATING → defer further).

5. **PERF-08 SDF-cache-thrash addressing.** Same status as Walks #1-4 — deferred. Re-measured for context only.

6. **Pixel 4a parity walk.** Stays in Phase 5 per the original roadmap.

7. **Modifying shader source.** MIRL constraint. The Plan 03.1-12 fix bundle is Dart-only; Walk #5 validates that bundle without touching shaders.

8. **Re-running Walks #1-#4.** Historical records preserved across iteration cycles; this plan does NOT re-author or modify any prior walk's FALSIFICATION or UAT documents.
</out_of_scope>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run pre-walk gate sequence + author 03.1-FALSIFICATION-5.md skeleton + 03.1-UAT-5.md skeleton</name>
  <files>
    .planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md,
    .planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-5.md
  </files>
  <action>
    Pre-walk gate sequence + skeleton authoring. Mirror Plan 03.1-11 Task 1 structure verbatim.

    **A) Pre-walk gates (six gates, all must pass):**

    1. `flutter test` — full suite GREEN (180+ tests; expected post-Plan-03.1-12: rewritten `fog_pixel_origin_decomposition_test.dart` as FOG-18 + extended `map_screen_test.dart` for DEBUG-02; no new test FILE added since C2' deferred). Capture `flutter test --reporter=compact` output; record total tests passed in the FALSIFICATION-5 skeleton.
    2. `flutter analyze --fatal-infos` — 0 warnings.
    3. `dart format --line-length 160 --set-exit-if-changed .` — 0 files changed.
    4. `dart test tool/test/` — 18 passed.
    5. CI green check via `gh run list --branch main --limit 5`. The post-Plan-03.1-12 SHA must show `success` for the iOS build job (`Build iOS (no-codesign, sideloadable)`). Capture the run ID + SHA in the skeleton metadata.
    6. IPA artifact download: `gh run download <RUN_ID> --name mirk-poc-debug-ios-unsigned-ipa --dir .uat-tmp/`. Verify `.uat-tmp/mirk-poc-debug-unsigned.ipa` exists and is ~11 MB.

    Each gate's pass/fail status + relevant output captured in `03.1-FALSIFICATION-5.md` §"Pre-walk gate sequence" subsection.

    **B) `03.1-FALSIFICATION-5.md` skeleton authoring:**

    Mirror `03.1-FALSIFICATION-4.md` structure:
    - Title: `# Phase 3.1 Falsification — Walk #5 (Post-Plan-03.1-12 Dart-Only Fix Bundle: FOG-18 modulo elimination + DEBUG-02 bounding-box removal; C2' / FOG-19 DEFERRED)`
    - `**Created:**` (today's date)
    - `**Phase:**` 03.1-fix-fog-pan-translation
    - `**Walked on:**` (PLACEHOLDER — filled by Task 3 post-walk)
    - `**Verdict:**` PENDING
    - `## Relationship to Walk #4 Verdict` — narrative summary of Walk #4's ITERATING-WITH-MAJOR-PROGRESS outcome + the Plan 03.1-12 scope reduction (initially 3-task → 2-task post-plan-checker; C2' / FOG-19 deferred to follow-up after `/gsd:discuss-phase 3.1`). Cite `03.1-FALSIFICATION-4.md` §Supplementary Walk Evidence — Debug-Spiral Diagnostic + Mechanism Correction as the corrected analysis input.
    - `## Hypothesis` — full re-statement per `<interfaces>` `Hypothesis re-statement` paragraph (note Q1b expected present; not verdict-determining).
    - `## Falsification Criteria (written BEFORE the walk)` — write Criteria A / B / C-DROPPED / D in full per `<interfaces>` enumeration. Q1 / Q1b-OBSERVATION / Q2 / Q3 / Q4 / Q5 NEW sub-claims under Criterion B with explicit sub-tests. Mark Q1b explicitly as OBSERVATION-ONLY, not verdict-determining.
    - `## Verdict logic` — verbatim copy of the verdict logic from this plan's `<objective>` section (CONFIRMED-AFTER-FIX-PARTIAL / CONFIRMED-AFTER-FIX (FULL) / DENIED-FINAL / ITERATING criteria).
    - `## Walk Plan` — mirror Walk #4's walk plan with the C1' validation regime + C3' diagnostic stress-test + Q1b observation capture added. Pre-walk gate evidence captured in `### Pre-walk gate sequence` subsection (filled by this Task 1).
    - `## Walk Evidence (filled AFTER the walk by Plan 03.1-13 Task 3)` — empty placeholder section.
    - `## Verdict` — empty placeholder section.

    **C) `03.1-UAT-5.md` skeleton authoring:**

    Mirror `03.1-UAT-4.md` structure:
    - Frontmatter with `gaps:` YAML block (initially empty — populated by Task 3 if Walk #5 surfaces residual failure modes).
    - Walk #5 metadata block (placeholder for CI run, SHA, walk theatre, duration, gestures — filled by Task 3).
    - Walk Evidence section (placeholder).
    - Subjective verdict block (placeholder for developer's verbatim words).
    - Verdict + deviations + summary placeholders.

    **D) Atomic commit:**

    Single atomic commit at the end of Task 1:
    ```
    docs(03.1-13): pre-walk gates green + Walk #5 falsification + UAT skeletons (Task 1)

    Six pre-walk gates GREEN at SHA <sha>: flutter test (N tests passed),
    analyze 0 warnings, dart format clean, tool tests 18 passed, CI run
    <run_id> success, IPA downloaded.

    Walk #5 falsification skeleton authored at
    .planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md
    with hypothesis + criteria + verdict-logic written BEFORE the walk per
    Phase 3.1 falsification discipline. Q1b zoom-gesture residual marked as
    OBSERVATION-ONLY (C2' / FOG-19 deferred). UAT skeleton at 03.1-UAT-5.md.

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    ```
  </action>
  <verify>
    <automated>flutter test &amp;&amp; flutter analyze --fatal-infos &amp;&amp; dart format --line-length 160 --set-exit-if-changed . &amp;&amp; dart test tool/test/</automated>
  </verify>
  <done>
    - All six pre-walk gates pass (test + analyze + format + tool tests + CI + IPA download).
    - `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md` exists with hypothesis + falsification criteria + verdict logic written; ≥ 100 lines.
    - `.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-5.md` exists as an empty-but-structured skeleton with `gaps:` frontmatter block.
    - One atomic commit `docs(03.1-13): pre-walk gates green + Walk #5 ...` lands on `main`.
    - `.uat-tmp/mirk-poc-debug-unsigned.ipa` exists and is ~11 MB.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: iPhone 17 Pro sideload UAT Walk #5</name>
  <files>.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md, .planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-5.md</files>
  <action>
    Sideload the unsigned IPA on iPhone 17 Pro via SideStore; perform Walk #5 per the protocol below; capture verbatim verdict + Mail-share session log post-walk so Task 3 can grep-correlate JSONL streams. The full walk protocol with C1' validation + C3' stress-test + Q1b observation regimes is enumerated in the <how-to-verify> block below; this <action> block exists for plan-checker schema completeness — execute the <how-to-verify> protocol verbatim.
  </action>
  <verify>
    <automated>MANUAL — see &lt;how-to-verify&gt; (PERF-07 + Criterion B + Criterion D subjective + diagnostic-stream pattern matching + dev-marker correlation are inherently manual UAT walk gates per 03.1-VALIDATION.md Manual-Only Verifications table; the developer's resume-signal IS the verification).</automated>
  </verify>
  <done>
    Walk #5 complete + developer's verbal verdict captured (`approved` / `denied:` / `iterating:`); session log Mail-shared so Task 3 can extract JSONL streams. The verdict outcome drives Task 3's branch (CONFIRMED-AFTER-FIX-PARTIAL / CONFIRMED-AFTER-FIX (FULL) / DENIED-FINAL / ITERATING).
  </done>
  <what-built>
    Plan 03.1-12 Dart-only fix bundle landed on `main` and CI-green-gated:
    - **C1' / FOG-18** — FOG-17a integer-wrap-modulo eliminated; `camera.pixelOrigin` forwarded directly (no modulo bound).
    - **C3' / DEBUG-02** — `MapOptions.cameraConstraint` removed; developer can now pan to extreme world coordinates (~50-100 km from Melun) for stress-test diagnostic.
    - **C2' / FOG-19 DEFERRED** — zoom-invariant world-coordinate basis NOT implemented; Q1b zoom-gesture residual EXPECTED present in Walk #5 as captured empirical baseline for the C2' follow-up plan.
    - All landed fixes are Dart-only (MIRL constraint satisfied).
    - Pre-walk gate sequence GREEN at the post-Plan-03.1-12 SHA.
    - IPA downloaded to `.uat-tmp/mirk-poc-debug-unsigned.ipa`.
  </what-built>
  <how-to-verify>
    Walk shape mirrors Walks #1-4 with three new validation regimes for C1' + C3' + Q1b-observation.

    1. **Sideload + boot:**
       - Open SideStore on iPhone 17 Pro → install/refresh `.uat-tmp/mirk-poc-debug-unsigned.ipa`.
       - Launch the app. Permission gate → grant location.
    2. **Pre-walk shader-sanity gate (`/sanity` route):**
       - Navigate to `/sanity` via the app bar's debug menu (or the implicit route per Plan 03.1-08).
       - Confirm production fog renders with circular reveal hole (debug-spiral toggle OFF default).
       - Toggle the debug-spiral switch ON. Confirm the digit-atlas spiral pattern renders with visible numbered cells (DEBUG-01 walk-time validation).
       - Toggle OFF — confirm production fog returns.
       - Confirm the AppBar back button still works on `/sanity` (UX-01 retained).
       - Confirm zero shader-compile exceptions in the FileLogger output (check via `Share Logs` → Mail to self if needed for direct inspection).
       - Attempt a two-finger rotate gesture. Confirm map does NOT rotate (UX-02 retained).
    3. **Walk #5 main protocol (≥ 5 minutes through Melun):**
       - Walk through Melun for ≥ 5 minutes with the IPA running and the device awake.
       - Perform ≥ 10 deliberate combined pinch-zoom-and-pan gestures.
       - Perform ≥ 3 recenter FAB taps.
       - At least one deliberate two-finger rotate-attempt gesture (UX-02 walk-time validation).
       - **NEW: tap the dev-marker button every time you observe ANY residual symptom related to Q1 (max-zoom SNAP), Q3 (reveal-hole offset), Q4 (fog tracking), Q5 (precision artefacts at distance). Do NOT tap markers for Q1b zoom-gesture symptoms — Q1b is the deferred axis; observation is captured separately in step 5.**
    4. **C1' validation regime — sustained MAX-zoom pan (VERDICT-DETERMINING):**
       - Pinch-zoom to maximum zoom (z=15 or `kPocMaxZoom`).
       - Pan in one direction continuously for ≥ 30 sec.
       - Tap the dev-marker on any perceived hard-step / SNAP event.
       - Expected outcome (CONFIRMED-AFTER-FIX-PARTIAL): zero markers fired during this regime (Walk #4 fired 2 markers in 2.36 sec at MAX zoom).
    5. **Q1b observation regime — deliberate zoom in/out transitions (NOT VERDICT-DETERMINING):**
       - From any starting zoom, perform ≥ 5 deliberate zoom in/out transitions.
       - Do NOT tap markers — instead, capture verbatim subjective observation post-walk:
         - Is Q1b "very fast hard step looking like changing the seed of a procedural generated image very fast" still present?
         - Has its character / severity changed vs Walk #4?
         - Any other zoom-gesture observations (frequency, dependence on zoom range, dependence on gesture velocity)?
       - This observation feeds the C2' follow-up plan as empirical baseline.
       - Q1b residual is EXPECTED (C2' / FOG-19 is deferred); presence does NOT degrade the verdict.
    6. **C3' diagnostic stress-test — pan to ~50-100 km from Melun (VERDICT-DETERMINING):**
       - Pan continuously in one direction for an extended period (the cameraConstraint is removed — flutter_map will not bounce you back to the bbox).
       - Goal: reach a position roughly 50-100 km from initial Melun centre. Use approximate landmark / direction (e.g., "pan east towards Provins" or "pan south towards Fontainebleau" but keep panning past the city).
       - Tap the dev-marker on any precision-induced artefact at distance (fog disappearing, fog rendering at wrong scale, fog jittering, NEW Q1 SNAP reappearing at distance, anything visually anomalous).
       - Expected outcome (CONFIRMED-AFTER-FIX-PARTIAL): fog renders correctly throughout the long pan; zero markers fired; FOG-18 precision-clean at extreme world coordinates within fp32 mantissa ceiling.
    7. **Post-walk Mail-share:**
       - Tap the AppBar `Share Logs` button.
       - Pick Mail → send to yourself.
       - Confirm the session log file (`yyyymmdd_hhmmss_logs.txt`) arrives in inbox.
    8. **Verbal verdict:**
       - Report verdict to Claude in plain English. Use one of four verdict shapes:
         - `approved-partial` (or `confirmed-after-fix-partial`) — Q1 SNAP closed at MAX zoom + C3' stress-test passes + PERF-07 retained + Q3/Q4 retained; Q1b residual still present (expected). The likely path; closes Walk #5 successfully but Phase 3.1 stays In Progress awaiting C2' follow-up + Walk #6.
         - `approved` (or `confirmed-after-fix`) — All Q-axes resolved INCLUDING Q1b spontaneously. Very unlikely; document the surprising outcome thoroughly.
         - `denied: <reasoning>` — Q1 SNAP persists at MAX zoom even with the wrap eliminated. Architectural counter-evidence; Phases 4 + 5 stay BLOCKED or transition to Cancelled per developer choice.
         - `iterating: <reasoning>` — A NEW failure mode surfaces beyond Q1b (e.g., NEW precision artefact at C3' stress-test, OR Q1 SNAP changes character at MAX zoom, OR something unforeseen). Plan 03.1-14+ pending in follow-up plan-phase invocation.
       - Include verbatim words wherever possible (these become Walk #5's Q-axis interpretation source material). Q1b verbatim observation is required regardless of verdict (feeds C2' follow-up).
  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - `approved-partial` (with optional verbatim verdict comments + Q1b verbatim observation) — Walk #5 closes with `CONFIRMED-AFTER-FIX-PARTIAL`; Plan 03.1-12 bundle delivers Q1 + C3' closure; Phase 3.1 stays In Progress awaiting C2' follow-up + Walk #6; Task 3 authors `CONFIRMED-AFTER-FIX-PARTIAL` verdict.
    - `approved` (with optional verbatim verdict comments + Q1b verbatim observation including any spontaneous-Q1b-resolution explanation) — Walk #5 closes with `CONFIRMED-AFTER-FIX (FULL)`; Phase 3.1 closes; Phase 4 + Phase 5 unblock; Plan 03-08 DENIED reversed.
    - `denied: <reasoning>` — Walk #5 fails at the architectural level (Q1 SNAP persists at MAX zoom even with wrap eliminated); Task 3 authors `DENIED-FINAL` verdict.
    - `iterating: <reasoning>` — Walk #5 surfaces NEW failure mode beyond Q1b; Task 3 authors `ITERATING` verdict with the surfaced gaps captured in `gaps:` frontmatter; Plan 03.1-14+ pending in follow-up plan-phase invocation.

    Mail-share the session log to yourself and reference the timestamp + filename so Task 3 can extract JSONL streams for the post-walk grep correlation.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Walk #5 verdict authoring + REQUIREMENTS / ROADMAP / STATE updates</name>
  <files>
    .planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md,
    .planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-5.md,
    .planning/REQUIREMENTS.md,
    .planning/ROADMAP.md,
    .planning/STATE.md
  </files>
  <action>
    Author the Walk #5 verdict + propagate to REQUIREMENTS/ROADMAP/STATE. Mirror Plan 03.1-11 Task 3 verbatim with verdict-branch specialization for the new CONFIRMED-AFTER-FIX-PARTIAL branch.

    **A) Extract JSONL streams from Mail-shared session log:**

    Mail-shared session log filename: `<yyyymmddTHHMMSSZ>_logs.txt` (per FileLogger naming).

    Extract the four JSONL streams to `.uat-tmp/walk5_*.jsonl`:
    - `grep -F 'infrastructure.mirk.frame_delta' <log_file> > .uat-tmp/walk5_frame_delta.jsonl`
    - `grep -F 'infrastructure.mirk.fog_transform' <log_file> > .uat-tmp/walk5_fog_transform.jsonl`
    - `grep -F 'infrastructure.mirk.sdf' <log_file> > .uat-tmp/walk5_sdf_rollups.jsonl`
    - `grep -F '"event":"dev_marker"' <log_file> > .uat-tmp/walk5_dev_markers.jsonl` (or whatever the dev-marker event tag is — check Plan 03.1-09 marker implementation)

    `.uat-tmp/walk5_*.jsonl` are transient (not committed; .gitignore'd already per Plan 03.1-09 pattern).

    **B) `03.1-FALSIFICATION-5.md` Walk Evidence section:**

    Fill the empty placeholders from Task 1. Mirror `03.1-FALSIFICATION-4.md` §Walk Evidence structure:
    - `### Pre-walk shader-sanity gate (...)` — confirm pre-walk gates passed (carry from Task 1) + UX-02 walk-time-validation (canvasTx/Ty identically zero in Walk #5 fog_transform rollups).
    - `### Probe rollup (frame-delta JSONL)` — per-rollup distribution table with `medianMs / p95Ms / maxMs` Min / Max / Median-of-rollups; PERF-07 verdict + headroom comparison vs Walk #4.
    - `### SDF rebuild rollup` — `rebuildCount/sec` distribution; PERF-08 status (deferred per Walks #1-4 pattern).
    - `### Fog-transform rollup` — KEY FOG-18 SIGNATURE:
      - `canvasTx/Ty` identically zero (UX-02 retained).
      - `pixelOriginX/Y` trajectory across regimes (default-zoom + max-zoom + zoom-gesture + ~50-100 km stress-test).
      - **FOG-18 SIGNATURE**: `uOffsetXMax/uOffsetYMax` track `pixelOriginX/Y` magnitude monotonically (no longer bounded under 1537 — values can exceed 1537 at high pixelOrigin). Pattern check: at MAX zoom + sustained pan, `uOffsetX*` grows linearly with raw px panned (Walk #4: bounded under 1537; Walk #5: tracks raw magnitude). At C3' stress-test ~100-km regime: `uOffsetX*` may approach 7-10M raw px and stays precise (no NaN, no fp32 ULP-induced jitter visible at the 1 raw-px scale).
      - **Q1b BASELINE CAPTURE (NOT verdict-determining)**: `uOffsetX*` deltas across consecutive rollups during zoom gestures WILL show O(1M) jumps (Walk #4 rollups 20-24 captured `pixelOriginX` 4.10M → 0.81M sweep — Walk #5's `uOffsetX*` will reproduce this since `appliedUOffset == camera.pixelOrigin` exactly under FOG-18). Document the JSONL signature as input for the upcoming C2' follow-up plan.
    - `### FPS observations (PERF-03)` — coarse FpsCounterOverlay reading.
    - `### Marker correlation (Walk #4 template applied)` — IF markers fired:
      - Marker count + epochSec timestamps + correlation against fog_transform rollup at the same epochSec.
      - Per-marker zoom regime classification: was it MAX-zoom-pan (Q1 residual)? extreme-distance (Q5 stress-test failure)? other?
      - Mechanism analysis: which Q-axis sub-claim falsifies if markers fired in a specific regime.
    - `### Subjective verdict (Criterion B)` — developer's verbatim words (from Task 2 verbal verdict + any Mail-shared annotations) + Q-axis interpretation:
      - Q1: status (RESOLVED / PARTIAL / FAILED / NEW)
      - **Q1b: OBSERVATION** (verbatim observation; status as RESIDUAL-EXPECTED / RESIDUAL-CHANGED / RESIDUAL-IMPROVED / SPONTANEOUSLY-RESOLVED — not verdict-determining)
      - Q2: status (RETAINED / FAILED — should be RETAINED unless something exotic surfaces)
      - Q3: status (RETAINED / FAILED)
      - Q4: status (RETAINED / FAILED)
      - Q5 NEW (DEBUG-02 stress-test at distance): status (PASSED / FAILED / NEW failure mode at distance)

    **C) `03.1-FALSIFICATION-5.md` Verdict section — branch on subjective verdict:**

    Four branches per verdict signal from Task 2:

    **Branch A1 — `approved-partial` / CONFIRMED-AFTER-FIX-PARTIAL (expected):**
    - Verdict: `**CONFIRMED-AFTER-FIX-PARTIAL**` (6th Phase 3.1 verdict; Q1 + C3' closed; Q1b residual deferred awaiting C2' plan + Walk #6).
    - Q1 RESOLVED, Q3/Q4/Q2 RETAINED, Q5 PASSED, Q1b OBSERVATION-CAPTURED.
    - Plan 03.1-12 Dart-only fix bundle empirically validated (the C1' + C3' axes).
    - Phase 3.1 stays `In Progress (6th iteration; awaiting C2' plan + Walk #6)`.
    - Phase 4 + Phase 5 STAY blocked.
    - C2' follow-up trigger: `/gsd:discuss-phase 3.1` to resolve dimensional-mismatch design question; subsequent `/gsd:plan-phase 3.1 --gaps` to author the C2' plan.

    **Branch A2 — `approved` / CONFIRMED-AFTER-FIX (FULL) (very unlikely):**
    - Verdict: `**CONFIRMED-AFTER-FIX**` (Q1b spontaneously resolved with no C2' fix — document mechanism if known).
    - All Q-axes RESOLVED including Q1b.
    - Phase 3.1 closes; Phase 4 + Phase 5 unblock.
    - MirkFall migration recommendation: **PORT BACK** (with all Phase 3.1 fixes included: 03.1-02 + 03.1-04 + 03.1-05 + 03.1-07 + 03.1-08 + 03.1-10 + 03.1-12).

    **Branch B — `denied:` / DENIED-FINAL:**
    - Verdict: `**DENIED-FINAL**` (Q1 SNAP persists at MAX zoom even with wrap eliminated).
    - Architectural counter-evidence: even the simplest Dart-only fix axis (eliminate the wrap) doesn't deliver Q1 closure at MAX zoom.
    - Capture the residual mechanism + suggest pivot direction (Phase 3.2 alternative renderer? port-back-with-different-architecture? POC closure?).
    - MirkFall migration recommendation: **DO NOT PORT BACK as-implemented.**

    **Branch C — `iterating:` / ITERATING:**
    - Verdict: `**ITERATING-WITH-...`.
    - Capture residual failure mode(s) + Plan 03.1-14+ candidate axes.
    - Per CONTEXT §Iteration policy: no hard cap on walk count.
    - Plan 03.1-14 pending in follow-up plan-phase invocation.

    **D) `03.1-UAT-5.md` finalisation:**

    - Walk metadata block: CI run ID, SHA, walk theatre, duration, gestures performed.
    - Verbatim verdict block-quoted.
    - Per-stream summary table.
    - Walk-#4-template comparison table (medianMs, headroom, canvasTx/Ty, pixelOriginX range, dev_markers fired, SDF rebuild rate, verdict).
    - **Q1b verbatim observation** (separate subsection under Subjective verdict — feeds the C2' follow-up plan).
    - `gaps:` frontmatter populated with any opened-by-Walk-#5 gap entries (Branch A1 captures Q1b as captured-baseline; Branch B / C captures fresh failure modes).

    **E) REQUIREMENTS.md updates:**

    Branch-specialized:

    **On CONFIRMED-AFTER-FIX-PARTIAL:**
    - FOG-04..07: stay `Falsified-in-production` with revision-clause pointing at `03.1-FALSIFICATION-5.md` (closed at MAX zoom + at distance; Q1b zoom-gesture residual still pending C2' follow-up).
    - FOG-11: stay `Falsified-in-production` (similar reasoning).
    - FOG-15: stay `Verified-by-test-only (historical mechanism-confirmed fix; superseded by FOG-17 + FOG-18)`.
    - FOG-18 + DEBUG-02: `Complete — Verified-by-test (P03.1-12)` → `Complete — Verified-by-test + walk-time validated (P03.1-13 Walk #5)`.
    - UX-02 + FOG-16 + FOG-17: re-validate at Walk #5 levels (annotation: `+ walk-time validated (P03.1-13 Walk #5 re-confirms Walk #4)`).
    - PERF-07: re-measured per Walk #5 numbers.
    - Walk #5 Revisions entry appended at the bottom + C2' follow-up trigger noted.

    **On CONFIRMED-AFTER-FIX (FULL):**
    - FOG-04..07: `Falsified-in-production` → `Complete — Verified-by-walk (P03.1-13 CONFIRMED-AFTER-FIX 2026-05-XX after layered Plan 03.1-02 + 03.1-04 + 03.1-05 + 03.1-07 + 03.1-08 + 03.1-10 + 03.1-12 fix bundle; Q1b spontaneously resolved)`.
    - FOG-11: `Falsified-in-production` → `Complete — Verified-by-walk (P03.1-13 CONFIRMED-AFTER-FIX)`.
    - FOG-15: `Verified-by-test-only` retained as historical record.
    - FOG-18 + DEBUG-02: `Complete — Verified-by-test + walk-time validated (P03.1-13)`.
    - UX-02 + FOG-16 + FOG-17: re-validate.
    - PERF-07: re-measured.
    - Walk #5 Revisions entry.

    **On DENIED-FINAL:**
    - FOG-04..07 + FOG-11 + FOG-15: stay `Falsified-in-production` with revision-clause pointing at `03.1-FALSIFICATION-5.md`.
    - FOG-18 + DEBUG-02: `Complete — Verified-by-test + walk-time validated (P03.1-13)` (the structural fixes are in the codebase + observed in JSONL stream; the open question is whether they're SUFFICIENT — the answer is no, but the fixes themselves are mechanically present).
    - UX-02 + FOG-16 + FOG-17: re-validate.
    - PERF-07: re-measured.
    - Add Walk #5 DENIED-FINAL Revisions entry.

    **On ITERATING:**
    - FOG-04..07 + FOG-11 + FOG-15: stay `Falsified-in-production` with revision-clause pointing at `03.1-FALSIFICATION-5.md`.
    - FOG-18 + DEBUG-02: `Complete — Verified-by-test + walk-time validated (P03.1-13)`.
    - UX-02 + FOG-16 + FOG-17: re-validate.
    - PERF-07: re-measured.
    - Add Walk #5 ITERATING Revisions entry with surfaced gaps + Plan 03.1-14+ candidate axes.

    **F) ROADMAP.md updates:**

    - Phase 03.1 row in Progress table: update plan count + status:
      - CONFIRMED-AFTER-FIX-PARTIAL: `13/13 — In Progress (6th iteration; Q1 + C3' closed; Q1b residual awaiting C2' follow-up plan + Walk #6)`.
      - CONFIRMED-AFTER-FIX (FULL): `13/13 — Complete (HYPOTHESIS CONFIRMED-AFTER-FIX 2026-05-XX)`.
      - DENIED-FINAL: `13/13 — Complete (HYPOTHESIS DENIED-FINAL 2026-05-XX)`.
      - ITERATING: `13/13 — In Progress (6th iteration; new failure mode surfaced; Plan 03.1-14+ pending)`.
    - On CONFIRMED-AFTER-FIX (FULL):
      - Phase 4 (`Wisp Particles`) status: `Blocked on Phase 3.1` → `Pending`.
      - Phase 5 (`Decision Gate`) status: `Blocked on Phase 3.1` → `Pending`.
      - Phase 3 row note: prepend `**REVERSED 2026-05-XX:** Phase 3 hypothesis ORIGINALLY DENIED 2026-05-01; Phase 3.1 closure at Walk #5 reverses to CONFIRMED-AFTER-FIX with the layered fix bundle.`
    - Phase 03.1 narrative: append the Walk #5 verdict line.
    - Plan 03.1-13 row: marked as landed with verdict summary.

    **G) STATE.md updates:**

    - `Current Position`: reflect Walk #5 outcome.
    - `Decisions log`: append Walk #5 entry.
    - `Blockers`: 
      - On CONFIRMED-AFTER-FIX-PARTIAL: resolve `PHASE-3.1-WALK-5-PENDING`; open `PHASE-3.1-C2-FOLLOWUP-DISCUSS-PHASE-PENDING`; `PHASE-3.1-WALK-6-PENDING` (after C2' plan lands).
      - On CONFIRMED-AFTER-FIX (FULL): resolve `PHASE-3.1-WALK-5-PENDING`; resolve all Phase 4 + 5 BLOCKED entries.
      - On DENIED-FINAL: resolve `PHASE-3.1-WALK-5-PENDING`; transition Phase 4 + 5 to `BLOCKED-FINAL` or `CANCELLED` per developer choice.
      - On ITERATING: resolve `PHASE-3.1-WALK-5-PENDING`; open `PHASE-3.1-WALK-6-PENDING` against Plan 03.1-14+.
    - `Session Continuity`: updated.

    **H) Atomic commit:**

    Single atomic commit at the end of Task 3:
    ```
    docs(03.1-13): Walk #5 verdict <VERDICT> + REQUIREMENTS/ROADMAP/STATE updates (Task 3)

    Walk #5 sideload UAT against CI run <run_id> (SHA <sha>) over central
    Melun delivered the 6th Phase 3.1 verdict: <VERDICT>.

    <one-paragraph-summary of verdict + Q-axis outcomes + JSONL signature
    confirmation + Q1b verbatim observation summary>

    Files modified:
    - 03.1-FALSIFICATION-5.md (load-bearing verdict analysis)
    - 03.1-UAT-5.md (transcript + per-stream summary + verbatim verdict + Q1b observation)
    - REQUIREMENTS.md (status updates per verdict branch)
    - ROADMAP.md (Phase 03.1 row + (CONFIRMED-FULL only) Phase 3 reversal note + Phase 4/5 unblock)
    - STATE.md (Current Position + Decisions log + Blockers)

    Walks #1 + #2 + #3 + #4 + Walk #3b + Phase 3 historical records UNTOUCHED.

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    ```
  </action>
  <verify>
    <automated>node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" verify must-haves --plan .planning/phases/03.1-fix-fog-pan-translation/03.1-13-PLAN.md</automated>
  </verify>
  <done>
    - `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md` Walk Evidence + Verdict sections filled (≥ 100 lines total).
    - `.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-5.md` complete with verbatim verdict block-quoted + per-stream summary + Q1b verbatim observation + `gaps:` frontmatter populated per verdict branch.
    - REQUIREMENTS.md updated per verdict branch (FOG-04..07 + FOG-11 + FOG-15 + FOG-18 + DEBUG-02 + UX-02 + FOG-16 + FOG-17 + PERF-07 statuses + Walk #5 Revisions entry).
    - ROADMAP.md Phase 03.1 + (CONFIRMED-FULL only) Phase 3 reversal + Phase 4/5 unblock per verdict branch.
    - STATE.md Current Position + Decisions + Blockers + Session Continuity per verdict branch.
    - One atomic commit `docs(03.1-13): Walk #5 verdict <VERDICT> ...` lands on `main`.
    - Walks #1-#4 + Walk #3b + Phase 3 historical records UNTOUCHED (verify via `git diff --name-only HEAD~..HEAD | grep -E '03\\.1-FALSIFICATION-(1|2|3|4)\\.md|03-FALSIFICATION\\.md|WALK-3B'` returning empty output).
  </done>
</task>

</tasks>

<verification>
After all three tasks:

1. Walks #1 / #2 / #3 / #4 + Walk #3b + Phase 3 historical records UNTOUCHED:
   ```bash
   git diff --name-only HEAD~..HEAD | grep -E '03\.1-FALSIFICATION-(1|2|3|4)\.md|03-FALSIFICATION\.md|WALK-3B'
   ```
   should return EMPTY output (no commits in this plan's scope touch those files).

2. New artefacts exist:
   - `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md`
   - `.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-5.md`

3. Two atomic commits land on `main`:
   - `docs(03.1-13): pre-walk gates green + Walk #5 ...` (Task 1)
   - `docs(03.1-13): Walk #5 verdict <VERDICT> ...` (Task 3)
   - Task 2 = checkpoint:human-verify, no commit.

4. Pre-walk gates GREEN at Task 1 (test + analyze + format + tool tests + CI + IPA download).

5. Walk #5 verdict captured + propagated through REQUIREMENTS / ROADMAP / STATE per the appropriate branch (CONFIRMED-AFTER-FIX-PARTIAL / CONFIRMED-AFTER-FIX (FULL) / DENIED-FINAL / ITERATING).

6. Q1b verbatim observation captured in `03.1-UAT-5.md` regardless of verdict (feeds the C2' follow-up plan).
</verification>

<success_criteria>
- Two atomic commits land on `main` (pre-walk gates + skeleton; verdict authoring).
- Walk #5 sideload UAT performed by developer (Task 2 checkpoint resolved).
- `03.1-FALSIFICATION-5.md` + `03.1-UAT-5.md` exist as load-bearing artefacts.
- REQUIREMENTS / ROADMAP / STATE updated per verdict branch.
- On CONFIRMED-AFTER-FIX-PARTIAL: Phase 3.1 stays In Progress; C2' follow-up trigger captured in STATE.md; Q1b empirical baseline documented as input for C2' plan.
- On CONFIRMED-AFTER-FIX (FULL): Phase 3.1 closes, Phase 4 + Phase 5 unblock, Plan 03-08 DENIED reversed, MirkFall port-back recommendation issued.
- On DENIED-FINAL: formal POC architectural counter-evidence captured; Phases 4 + 5 status updated per developer choice.
- On ITERATING: Plan 03.1-14+ pending in follow-up plan-phase invocation; Phase 3.1 stays In Progress.
- Walks #1-#4 + Walk #3b + Phase 3 historical records UNTOUCHED.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-fix-fog-pan-translation/03.1-13-SUMMARY.md` per the standard summary template.

Notable artefacts to capture in the summary:
- `requirements-completed`: branch-dependent
  - CONFIRMED-AFTER-FIX-PARTIAL: `[FOG-18, DEBUG-02, UX-02, PERF-07]` (Q1 + C3' closure walk-time validated; FOG-04..07 + FOG-11 + FOG-15 stay Falsified-in-production awaiting C2' follow-up).
  - CONFIRMED-AFTER-FIX (FULL): `[FOG-04, FOG-05, FOG-06, FOG-07, FOG-11, FOG-15, FOG-18, DEBUG-02, UX-02, PERF-07]` (Phase 3.1 closure).
  - DENIED-FINAL: `[FOG-18, DEBUG-02, UX-02, PERF-07]` (only the structurally-landed-and-walk-validated; FOG-04..07 + FOG-11 + FOG-15 stay Falsified-in-production).
  - ITERATING: `[FOG-18, DEBUG-02, UX-02, PERF-07]`.
- `key-files.created`: `03.1-FALSIFICATION-5.md`, `03.1-UAT-5.md`, `03.1-13-SUMMARY.md`.
- `key-files.modified`: REQUIREMENTS.md, ROADMAP.md, STATE.md.
- `key-decisions`: enumerate the verdict + Q-axis outcomes + JSONL signature confirmations + Q1b verbatim observation + Phase 4/5 status changes per verdict branch.
- `patterns-established`: "Walk-#5 FOG-18 signature confirmation pattern" (uOffsetX* tracks pixelOriginX magnitude monotonically); "CONFIRMED-AFTER-FIX-PARTIAL intermediate-verdict pattern" (closes a fix bundle while keeping a deferred axis open for follow-up); on CONFIRMED-AFTER-FIX (FULL): "Plan 03-08 verdict reversal pattern".
- `deferred-followup-needed` (CONFIRMED-AFTER-FIX-PARTIAL only): "C2' / FOG-19 zoom-invariant basis — schedule `/gsd:discuss-phase 3.1` to resolve dimensional-mismatch design question; subsequent `/gsd:plan-phase 3.1 --gaps` opens the C2' follow-up plan; Walk #6 closes Phase 3.1 with full CONFIRMED-AFTER-FIX after C2' lands."
</output>
</content>
</invoke>