---
phase: 03.1-fix-fog-pan-translation
plan: 13
type: execute
wave: 2
depends_on: ["03.1-12"]
files_modified:
  - .planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md
  - .planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-5.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
gap_closure: true
requirements: [FOG-04, FOG-05, FOG-06, FOG-07, FOG-11, FOG-15, FOG-18, PERF-07]
user_setup:
  - service: github-actions
    why: "Wait for CI green on the post-Plan-03.1-12 SHA + download the IPA artifact for the Walk #5 sideload"
    dashboard_config:
      - task: "Confirm CI run status (`gh run list --branch main --limit 5` + `gh run watch`)"
        location: "GitHub Actions for the mirk-poc-debug repo"
      - task: "Download IPA artifact via `gh run download <RUN_ID> --name ios-unsigned --dir .uat-tmp/`"
        location: "GitHub Actions run detail page (or `gh` CLI)"
  - service: sidestore
    why: "Sideload the unsigned IPA onto iPhone 17 Pro for Walk #5"
    dashboard_config:
      - task: "Open SideStore on iPhone 17 Pro → install/refresh the .uat-tmp/mirk-poc-debug-unsigned.ipa"
        location: "SideStore iOS app + paired desktop pairing-file"
must_haves:
  truths:
    - "A new `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md` exists capturing the Walk #5 verdict. Walks #1 + #2 + #3 + #4 falsification documents AND the Walk #3b marker analysis are LEFT UNTOUCHED — historical record preserved across iteration cycles."
    - "A new `.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-5.md` exists capturing the Walk #5 transcript. Walks #1 + #2 + #3 + #4 UAT transcripts are LEFT UNTOUCHED."
    - "Pre-walk gate sequence runs cleanly: `flutter test` GREEN (180+ existing + Plan 03.1-12's new tests: FOG-18 fog_world_meter_anchor 3 sub-tests + fog_meter_decomposition 5+ sub-tests + REWRITTEN fog_world_coordinate_noise + fog_pixel_origin_decomposition + fog_tile_period_invariant + fog_debug_spiral_continuity + UPDATED fog_shader_uniforms_test totalFloatSlots == 42); `flutter analyze --fatal-infos` 0 warnings; `dart format --line-length 160 --set-exit-if-changed .` clean; `dart test tool/test/` GREEN; CI green on the post-Plan-03.1-12 SHA on `main`; IPA downloaded via `gh run download` to `.uat-tmp/mirk-poc-debug-unsigned.ipa`."
    - "**STRUCTURALLY-ENFORCED MAIL-SHARE GATE (NEW for Walk #5):** Walks #3 + #4 broke the post-walk Mail-share pattern (verbal-only evidence). The Plan 03.1-11 pre-walk gate explicitly required Mail-share discipline to be re-established but Walk #4 broke it again. Walk #5 makes Mail-share discipline a STRUCTURAL gate of the verdict capture: the developer's resume signal MUST include the absolute path of the Mail-shared session log file as a parameter (`approved [path/to/log.txt] — ...` / `denied [path/to/log.txt]: ...` / `iterating [path/to/log.txt]: ...`). The log path must point to a file accessible to the executor (i.e., the developer has saved the Mail attachment to the local filesystem before reporting the verdict). The executor then runs grep against the actual log file before authoring the verdict. If no log path is provided, the executor MUST refuse to proceed to verdict authoring and re-prompt the developer. This structural gate eliminates the verbal-only evidence pattern."
    - "The iPhone 17 Pro UAT Walk #5 is performed by the user. Walk shape matches Plan 03.1-11 (Walk #4) pattern: ≥ 5 minutes through Melun + ≥ 10 deliberate combined pinch-zoom-and-pan gestures + ≥ 3 recenter taps + post-walk Mail-share of the session log (NOW STRUCTURALLY ENFORCED) + pre-walk `/sanity` shader-compile gate. **NEW for Walk #5: explicit Q5 zoom-scramble re-test** with debug-spiral toggle ON: developer zooms from min to max with debug-spiral active and verifies (a) cells visibly enlarge with zoom (cells are 'tied to ground'); (b) at fixed map position (e.g., always centre on lat 48.5397, lon 2.6553), the spiral cell numbering at the camera centre stays the same across all zoom levels (the fragment under the blue dot should always sample the same noise cell, regardless of zoom — direct visual confirmation of FOG-18 zoom-invariance acceptance property)."
    - "**Walk #5-specific Q1-Q4 retention re-test:** Walk #4's Q1 (no perceived hard-step events during pure pan) + Q2 (no rotation-correlated fog mis-coverage during rotate-attempt gestures) + Q3 (no reveal-hole offset from blue dot during gesture) + Q4 (fog visually translates with camera during pan) MUST all hold. If any regress, that's a different failure mode (Plan 03.1-14 territory; the Plan 03.1-12 metersPerPixel computation may have introduced a regression at the painter level). Plan 03.1-12 doesn't touch the pan-axis fix bundle; Q1-Q4 should retain Walk #4 status."
    - "Post-walk: developer's verbal verdict (`approved [log_path]` / `denied [log_path]:` / `iterating [log_path]:`) is captured. The walk evidence section of `03.1-FALSIFICATION-5.md` is filled with: developer's verbatim words + interpretation + post-walk grep of `infrastructure.mirk.fog_transform` + `infrastructure.mirk.frame_delta` + `infrastructure.mirk.sdf` rollups + per-second composite extract + (if markers fired) marker-correlation analysis mirroring the Walk #3b template. **NEW for Walk #5: fog_transform JSONL grep MUST verify the FOG-17a signature (appliedUOffset values bounded under 1537 raw px even at high zoom — the FOG-18 implementation MUST preserve FOG-17a) AND the new metersPerPixel signature (per-rollup metersPerPixel field present and consistent with synthetic camera state).**"
    - "PERF-07 is re-measured for Walk #5: frame-delta probe rolling rollup over the ≥ 10-combined-gesture window with `medianMs ≤ 16`, `p95Ms ≤ 32`, `maxMs ≤ 48`. Walk #1 captured 4-7× headroom; Walk #2 captured 3.9-5.4× headroom; Walk #3 captured implicitly (no log Mail-shared); Walk #3b's 33-sec window showed median-of-medians 0.405 ms (40-110× headroom); Walk #4 captured implicitly (no log Mail-shared). Walk #5 should preserve at least Walk #2 levels (Plan 03.1-12 adds 1 cos() call + 1 pow() call + 1 multiply per paint at the Dart side — negligible; the shader gains 1 multiplication per fragment — negligible)."
    - "Verdict written: `CONFIRMED-AFTER-FIX` if BOTH (a) developer's subjective verdict is `approved` AND (b) PERF-07 thresholds satisfied AND (c) **Q5 zoom-scramble RESOLVED** — debug-spiral cells visibly enlarge with zoom AND cells at fixed map position stay the same across zoom levels AND production fog noise pattern at fixed map position appears stable across zoom transitions AND (d) Q1-Q4 retained from Walk #4. `DENIED-FINAL` if even FOG-18 meter-space anchor fails empirically (the diagnostic JSONL stream cannot resolve a residual failure mode via a follow-up Phase 3.1 iteration; Phase 3.2 alternative-renderer pivot conversation). `ITERATING` if a residual failure mode surfaces (e.g., meter-space integer-wrap stepping at sustained one-direction pan every ~128 sec; OR a NEW failure mode the meter-space anchor didn't address)."
    - "On `CONFIRMED-AFTER-FIX`: ROADMAP.md Phase 03.1 row in the Progress table flips to `Complete (HYPOTHESIS CONFIRMED-AFTER-FIX)`. Phase 4 (`Wisp Particles`) status updates from `Blocked on Phase 3.1` to `Pending`. Phase 5 (`Decision Gate`) likewise. STATE.md `Current Position` updates to reflect Phase 3.1 closure. REQUIREMENTS.md FOG-04..07 + FOG-11 + FOG-15 + FOG-18 statuses flip to `Complete — Verified-by-walk (P03.1-13 CONFIRMED-AFTER-FIX 2026-05-XX after layered Plan 03.1-02+04+05+07+08+10+12 fix bundle)`. PERF-07 status updates to `Complete — Re-measured (P03.1-13)` with the actual numbers."
    - "On `DENIED-FINAL`: ROADMAP.md Phase 03.1 row updates to `Complete (HYPOTHESIS DENIED-FINAL)`. Phases 4 + 5 stay `Blocked` (or transition to `Cancelled` per developer choice — flag for user decision). REQUIREMENTS.md FOG-04..07 + FOG-11 + FOG-15 stay `Falsified-in-production` with updated revision-clause that points at `03.1-FALSIFICATION-5.md`. UX-02 + FOG-16 + FOG-17 + FOG-18 still flip to `Complete — Verified-by-test + walk-time validated structurally` (their CI gates ARE GREEN by Plan 03.1-10 + Plan 03.1-12 construction; the structural fixes ARE in the codebase; the open question is whether they're SUFFICIENT for fog-camera-tracking parity at all coordinate-system axes). STATE.md captures the formal POC architectural counter-evidence. The Phase 3.2 alternative-renderer pivot conversation opens (`mapsforge_flutter`, custom MVT-on-Canvas, `flutter_gpu`)."
    - "On `ITERATING`: a Plan 03.1-14+ is opened in a follow-up plan-phase invocation, with Walk #5's diagnostic stream as input. THIS plan does NOT author 03.1-14. UX-02 + FOG-16 + FOG-17 + FOG-18 still flip to `Complete — Verified-by-test + walk-time validated structurally` (the structural fixes ARE in the codebase; the open question is whether they're SUFFICIENT). PERF-07 updated with Walk #5 numbers regardless. FOG-04..07 + FOG-11 + FOG-15 stay `Falsified-in-production`. Phase 3.1 stays `In Progress (6th iteration, walk #6 pending)`."
    - "Phase 3 `03-FALSIFICATION.md` AND Phase 3.1 Walks #1 + #2 + #3 + #4 falsification documents AND `03.1-WALK-3B-MARKER-ANALYSIS.md` are LEFT UNTOUCHED — no edits, no overwrites. Historical records preserved across iteration cycles."
  artifacts:
    - path: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md"
      provides: "Phase 3.1 Walk #5 falsification document — pre-walk gate-pass evidence + post-walk verdict (`CONFIRMED-AFTER-FIX` | `DENIED-FINAL` | `ITERATING`)"
      min_lines: 100
      contains: "Walk #5"
    - path: ".planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-5.md"
      provides: "Walk #5 evidence transcript — developer's verbatim verdict + interpretation + JSONL grep correlation + (if markers fired) marker-correlation analysis"
      min_lines: 40
      contains: "Walk Evidence"
  key_links:
    - from: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md"
      to: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-4.md"
      via: "explicit cross-reference in Walk #5's `## Relationship to Walk #4 Verdict` section ('Walk #4 closed iterating-with-Q1-Q4-confirmed-Q5-zoom-scramble-open 2026-05-03; Walk #5 either confirms or denies the layered Plan 03.1-12 software fix bundle')"
      pattern: "03\\.1-FALSIFICATION-4\\.md"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md"
      via: "FOG-04..07 + FOG-11 + FOG-15 + FOG-18 + UX-02 + FOG-16 + FOG-17 + PERF-07 status clauses include `(P03.1-13)` cross-reference; Revisions section appends a 2026-05-XX Walk #5 entry"
      pattern: "P03\\.1-13"
    - from: ".planning/STATE.md"
      to: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md"
      via: "Current Position + Decisions sections capture the Walk #5 verdict outcome; WALK-MAIL-SHARE-DISCIPLINE blocker resolution"
      pattern: "Phase 3\\.1"
---

<objective>
Run the pre-walk gates, perform the iPhone 17 Pro sideload UAT Walk #5, capture the developer's verdict, and produce `03.1-FALSIFICATION-5.md` — the Phase 3.1 Walk #5 falsification document. Walk #5 is the empirical re-test for the Plan 03.1-12 software fix bundle: FOG-18 world-meter anchor (Fix B — developer-endorsed during Walk #4 close-out) in BOTH `atmospheric_fog.frag` AND `atmospheric_fog_debug_spiral.frag` + new uniform `uMetersPerPixel` slot 41 + Dart-side `metersPerPixel = kWebMercatorMetersPerPxAtEquatorZ0 * cos(lat) / pow(2, zoom)` computation per paint. The Walk #5 verdict either reverses Walk #4's `ITERATING-WITH-Q1-Q4-CONFIRMED-Q5-ZOOM-SCRAMBLE-OPEN` outcome to `CONFIRMED-AFTER-FIX` (unblocking Phase 4 + Phase 5 of the original roadmap) or strengthens it to `DENIED-FINAL` (terminating the POC's same-Canvas hypothesis at the architectural noise-anchor axis; Phase 3.2 alternative-renderer pivot conversation) or — per CONTEXT §Iteration policy — captures a sixth `ITERATING` verdict that opens Plan 03.1-14+.

Purpose: this is the empirical gate Phase 3.1 needs to close after the Plan 03.1-12 software fix bundle. Plan 03.1-12 software-completed FOG-18 in BOTH shaders + Dart-side metersPerPixel computation in three painters (`_FogPainter`, `_DebugSpiralMapPainter`, `/sanity` `_DebugSpiralPainter`). Walk #5 is the architectural-evidence gate that flips FOG-04..07 + FOG-11 + FOG-15 + FOG-18 to Complete (or strengthens the verdict). Per CONTEXT §Iteration policy, there is no hard cap on walk count.

**WALK #5 KEY DIFFERENTIATORS vs Walks #1-4:**

1. **STRUCTURALLY-ENFORCED MAIL-SHARE GATE** — Walks #3 + #4 broke Mail-share discipline twice in a row (verbal-only evidence; no quantitative JSONL grep correlation possible). Walk #5 makes Mail-share STRUCTURAL: the developer's resume signal MUST include the absolute path of the Mail-shared session log file as a parameter. The executor refuses verdict authoring without the log path. This eliminates the verbal-only-evidence pattern.

2. **EXPLICIT Q5 ZOOM-SCRAMBLE RE-TEST** at debug-spiral toggle ON: developer zooms from min to max with debug-spiral active and verifies (a) cells visibly enlarge with zoom (visual confirmation of meter-space anchor); (b) at fixed map position the spiral cell numbering at camera centre stays the same across zoom levels (visual confirmation of FOG-18 zoom-invariance). Walk #4 closed early on debug-spiral observation grounds; Walk #5 explicitly re-tests with the FOG-18 fix in place.

3. **NEW JSONL signature: metersPerPixel field** in fog_transform rollups. The post-Plan-03.1-12 `FogTransformLogger` should expose the per-paint metersPerPixel value AS a new field in the rollup. Post-walk grep verifies the metersPerPixel is non-zero, varies with synthetic camera state, AND matches the Web-Mercator formula at the captured camera lat/zoom. (Note: if the FogTransformLogger doesn't yet expose metersPerPixel, this is a documented gap to capture in Plan 03.1-12 SUMMARY's Issues Encountered section; the executor at Plan 03.1-13 may need to add the logger-side field as a Walk #5 sub-task — flag in Task 1.)

Output: 1 new `03.1-FALSIFICATION-5.md` (the load-bearing artefact); 1 new `03.1-UAT-5.md` (the walk-evidence transcript); REQUIREMENTS.md status updates for FOG-04..07 + FOG-11 + FOG-15 + FOG-16 + FOG-17 + FOG-18 + UX-02 + PERF-07; ROADMAP.md Phase 03.1 Progress-table row update; STATE.md Current Position + Decisions + Blockers updates (resolve WALK-MAIL-SHARE-DISCIPLINE if Walk #5's structural gate succeeds). Phase 3's `03-FALSIFICATION.md` AND Phase 3.1 Walks #1 + #2 + #3 + #4 records AND `03.1-WALK-3B-MARKER-ANALYSIS.md` ALL left UNTOUCHED.
</objective>

<execution_context>
@C:/Users/oliver/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oliver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-CONTEXT.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-2.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-3.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-4.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-2.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-3.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-4.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-WALK-3B-MARKER-ANALYSIS.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-VALIDATION.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-11-PLAN.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-11-SUMMARY.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-12-PLAN.md

<interfaces>
<!-- Walk #5 mirrors Walk #4 (Plan 03.1-11) shape-for-shape with FOG-18-specific specialisations + Mail-share structural gate. -->

`03.1-FALSIFICATION-4.md` Walk #4 structure (the immediate predecessor template):
1. Title + metadata (`Created`, `Phase`, `Walked on`, `Verdict`)
2. `## Relationship to Walk #3 Verdict` (Walk #5 inserts `## Relationship to Walk #4 Verdict`)
3. `## Hypothesis`
4. `## Falsification Criteria (written BEFORE the walk)` — `### Criterion A`, `### Criterion B` (Q1-Q5 sub-claims), `### Criterion C — DROPPED`, `### Criterion D — Diagnostic stream`
5. `## Walk Plan`
6. `## Walk Evidence (filled AFTER the walk)`
7. `## Verdict`

Walk #5's `03.1-FALSIFICATION-5.md` mirrors this exactly with these specialisations:
- Title: `# Phase 3.1 Falsification — Walk #5 (Post-Plan-03.1-12 World-Meter Anchor Fix)`
- Add `## Relationship to Walk #4 Verdict` section pointing at `03.1-FALSIFICATION-4.md`; summarise the iterating-with-Q1-Q4-confirmed-Q5-zoom-scramble-open outcome and the gap entries (FOG-18) closed by Plan 03.1-12.
- Hypothesis re-statement: "After the Plan 03.1-12 software fix bundle (FOG-18 world-meter anchor in BOTH atmospheric_fog.frag + atmospheric_fog_debug_spiral.frag + new uniform uMetersPerPixel slot 41 + Dart-side metersPerPixel computation in _FogPainter + _DebugSpiralMapPainter + /sanity _DebugSpiralPainter), the same-Canvas fog now: (a) translates with the camera during pan (Walk #4 retained); (b) is structurally invulnerable to rotation-correlated mis-coverage (UX-02 retained); (c) maintains a fully stable noise pattern across consecutive paints AT A SINGLE ZOOM LEVEL (FOG-17 retained); (d) keeps the reveal hole co-located with the blue dot during pan/zoom (FOG-12 retained); (e) is anchored to ground meters such that at a fixed geographic point, the noise sample is zoom-INVARIANT — cells visibly enlarge when zoomed in, shrink when zoomed out, and the noise pattern at any given map position stays perceptually stable across zoom transitions (FOG-18 — Walk #4 Q5 closure)."
- Criterion A: same thresholds (PERF-07 carry-over): `medianMs ≤ 16`, `p95Ms ≤ 32`, `maxMs ≤ 48`. Walk #1: 4-7×; Walk #2: 3.9-5.4×; Walk #3b: 40-110× (33-sec window); Walks #3 + #4: implicit (no log Mail-shared). Walk #5 should preserve at least Walk #2 levels.
- Criterion B sub-claims:
  - **Q1 (RETAINED from Walk #4):** "No perceived hard-step events during pure pan" — Walk #4 confirmed; Walk #5 re-confirms. Sub-test: developer pans for ≥ 60 seconds in one direction. Plan 03.1-12 doesn't touch the pan-axis fix bundle; Q1 should retain Walk #4 status.
  - **Q2 (RETAINED from Walk #4):** "No rotation-correlated fog mis-coverage" — Walk #4 confirmed via UX-02; Walk #5 re-confirms. Sub-test: developer attempts a deliberate two-finger rotate gesture; the map should NOT rotate.
  - **Q3 (RETAINED from Walks #1-4):** "No reveal-hole offset from blue dot during gesture" — re-confirmation under Plan 03.1-12's coordinate-system change.
  - **Q4 (RETAINED from Walks #1-4):** "Fog visually translates with camera during pan" — re-confirmation; Plan 03.1-12 doesn't touch the translation path.
  - **Q5 (NEW — Walk #5 PRIMARY GATE):** "Noise pattern is anchored to ground meters; at a fixed geographic point the noise sample is zoom-invariant; cells visibly enlarge with zoom" — Walk #4 surfaced this NEW failure; Plan 03.1-12 FOG-18 targets it. Sub-test (production fog): developer fixes camera centre on lat 48.5397, lon 2.6553 and zooms from min to max; verifies the production fog noise pattern at the camera centre appears stable across zoom transitions (no "scramble"). Sub-test (debug-spiral): developer toggles debug-spiral ON; zooms from min to max; verifies (a) cells visibly enlarge with zoom (cells are tied to ground); (b) at fixed map position the spiral cell numbering at the camera centre stays the same across all zoom levels (the fragment under the blue dot should always sample the same noise cell, regardless of zoom — direct visual confirmation of FOG-18 zoom-invariance).
  - No white-ellipse during fast pinch-zoom (already passed Walks #1-4)
  - No inversion at any zoom level (already passed Walks #1-4)
- Criterion C stays DROPPED.
- Criterion D (Diagnostic stream): `pixelOriginX/Y` track gesture monotonically; `appliedUOffset` keys (FOG-17a signature — values bounded under ~1537 raw px regardless of zoom) MUST hold; **NEW for Walk #5: metersPerPixel field present in fog_transform rollup AND non-zero AND consistent with synthetic camera state.** Per-rollup metersPerPixel should evolve smoothly with camera zoom (no discontinuities); at zoom 13 lat 48.5° expected ≈ 12.66; at zoom 15 ≈ 3.16; at zoom 19 ≈ 0.198. `canvasTx/Ty` jumps still occur — Plan 03.1-08 FOG-13 compensates them mechanically.

`03.1-UAT-5.md` mirrors `03.1-UAT-4.md` shape-for-shape: metadata block + walk protocol + walk evidence (frame-delta + SDF + fog-transform rollups) + subjective verdict + verdict + deviations + summary + (optional) gaps section if Walk #5 surfaces residual failure modes + `gaps:` YAML in frontmatter for any opened-by-Walk-#5 gap entries.

The pre-walk gate sequence is identical to Plan 03.1-11 Task 1 (six gates: flutter test / analyze / format / tool tests / CI green / IPA download).

The walk shape extends Plan 03.1-11 with: (1) STRUCTURAL Mail-share gate at the resume-signal contract; (2) explicit Q5 zoom-scramble re-test at debug-spiral toggle ON; (3) NEW metersPerPixel JSONL signature verification.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the pre-walk `03.1-FALSIFICATION-5.md` skeleton + run the pre-walk gate sequence + verify FogTransformLogger metersPerPixel field</name>
  <files>.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md, .planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-5.md</files>
  <action>
    Create `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md`. Mirror the structural shape of `03.1-FALSIFICATION-4.md` with the specialisations described in the §Interfaces block above. Use the executor's actual current date for the `Created:` field; leave `Walked on:` and `Verdict:` as `TBD` until Task 2 fills them.

    Top-of-file metadata block:
    ```markdown
    # Phase 3.1 Falsification — Walk #5 (Post-Plan-03.1-12 World-Meter Anchor Fix)

    **Created:** 2026-05-XX  (executor: fill in real date)
    **Phase:** 03.1-fix-fog-pan-translation
    **Walked on:** TBD  (filled post-walk: iPhone 17 Pro, central Melun, CI run `<RUN_ID>`, SHA `<SHA>`)
    **Verdict:** TBD  (filled post-walk: `CONFIRMED-AFTER-FIX` | `DENIED-FINAL` | `ITERATING — see 03.1-14+`)

    ## Relationship to Walk #4 Verdict

    Walk #4 (Plan 03.1-11, 2026-05-03) closed `ITERATING-WITH-Q1-Q4-CONFIRMED-Q5-ZOOM-SCRAMBLE-OPEN`. The Plan 03.1-10 layered software fix bundle (UX-02 disable rotation + FOG-17 world-coordinate noise sampling + FOG-17a CPU-side integer/fractional decomposition) walk-time-validated all four pan-axis sub-claims (Q1+Q2+Q3+Q4) but the FOG-17 formulation `noiseUv = worldPx / kNoiseTilePx` anchored noise to **Web-Mercator world-pixel space** with `uPixelOrigin` doubling per zoom level → fragments under any given screen position sampled completely different noise positions per zoom step (numbers slid fast); combined with `kNoiseTilePx = 384.0` raw-pixel const → cells pinned in screen-space rather than scaling with the world (numbers stayed same size).

    Walk #4 limitation: developer did NOT Mail-share session log (same as Walk #3); verbal + debug-spiral observation evidence only. Walk #5 makes Mail-share discipline a STRUCTURAL gate of the verdict capture (resume signal MUST include log path).

    Plan 03.1-12 closed FOG-18 software-side via the world-meter anchor formulation: `worldMeters = (fragUv * uResolution + uPixelOrigin) * uMetersPerPixel; noiseUv = worldMeters / kNoiseTilePxMeters` in BOTH production + debug-spiral shaders. NEW uniform `uMetersPerPixel` slot 41 (totalFloatSlots advances 41 → 42). Dart-side `metersPerPixel = kWebMercatorMetersPerPxAtEquatorZ0 * cos(lat) / pow(2, zoom)` computed at every paint in `_FogPainter` + `_DebugSpiralMapPainter` + `/sanity` `_DebugSpiralPainter`. New constants `kPocFogNoiseTilePxMeters = 1024.0`, `kPocDebugSpiralCellSizeMeters = 200.0`, `kWebMercatorMetersPerPxAtEquatorZ0 = 156543.03392`. FOG-17a integer/fractional decomposition RETAINED unchanged (operates on raw pixel units BEFORE shader multiplies by uMetersPerPixel).

    Walks #1 + #2 + #3 + #4 + Walk #3b records (`03.1-FALSIFICATION.md`, `03.1-FALSIFICATION-2.md`, `03.1-FALSIFICATION-3.md`, `03.1-FALSIFICATION-4.md`, `03.1-UAT.md`, `03.1-UAT-2.md`, `03.1-UAT-3.md`, `03.1-UAT-4.md`, `03.1-WALK-3B-MARKER-ANALYSIS.md`) AND Phase 3 record (`03-FALSIFICATION.md`) ALL stand as historical record — UNTOUCHED. Walk #5's verdict is a SEPARATE artefact in this file.

    ## Hypothesis

    After the Plan 03.1-12 software fix bundle (FOG-18 world-meter anchor in BOTH `atmospheric_fog.frag` + `atmospheric_fog_debug_spiral.frag` + new uniform `uMetersPerPixel` slot 41 + Dart-side `metersPerPixel` computation in three painters), the same-Canvas fog now:
    (a) translates with the camera during pan (Walk #4 retained — Plan 03.1-12 doesn't touch the translation path);
    (b) is structurally invulnerable to rotation-correlated mis-coverage (UX-02 retained — Plan 03.1-12 doesn't touch the rotation handling);
    (c) maintains a fully stable noise pattern across consecutive paints AT A SINGLE ZOOM LEVEL (FOG-17 + FOG-17a retained — the world-coordinate sampling + integer/fractional decomposition still apply at the pre-multiply pixel-space input);
    (d) keeps the reveal hole co-located with the blue dot during pan/zoom (FOG-12 + FOG-13 retained);
    (e) is anchored to ground meters such that at a fixed geographic point, the noise sample is zoom-INVARIANT — cells visibly enlarge when zoomed in, shrink when zoomed out, and the noise pattern at any given map position stays perceptually stable across zoom transitions (FOG-18 — Walk #4 Q5 closure).

    Re-stated against the original POC core value: the fog-of-war stays perfectly locked to the map during pan, zoom, and combined gestures on a sideloaded iOS build. **CONFIRMED-AFTER-FIX** means the Phase 3 hypothesis is reinstated and the POC ports back to MirkFall with all of Plan 03.1-02 + 03.1-04 + 03.1-05 + 03.1-07 + 03.1-08 + 03.1-10 + 03.1-12 fixes included; **DENIED-FINAL** means even the world-meter anchor doesn't deliver fog-camera-tracking parity (Phase 3.2 alternative-renderer pivot conversation); **ITERATING** means a residual failure mode surfaced — most likely candidates: (i) meter-space integer-wrap stepping at sustained one-direction pan (every ~128 sec at typical pan velocity per the Plan 03.1-12 noise-function inspection); (ii) a NEW failure mode introduced by Plan 03.1-12 (e.g., the metersPerPixel computation has a fp32 precision issue at extreme zoom; or the cos(lat) clamp produces an artefact at unanticipated camera positions).

    ## Falsification Criteria (written BEFORE the walk)

    ### Criterion A — Frame-delta probe thresholds (PERF-07 re-validation, quantitative)

    Same thresholds as Walks #1-4: `medianMs ≤ 16`, `p95Ms ≤ 32`, `maxMs ≤ 48` across ≥ 10 deliberate combined gestures. Walk #1: 4-7×; Walk #2: 3.9-5.4×; Walk #3b: 40-110× headroom on a 33-sec window; Walks #3 + #4: implicit (no log Mail-shared). Walk #5 should preserve at least Walk #2 levels (Plan 03.1-12 adds 1 cos() call + 1 pow() call + 1 multiply per paint at the Dart side — negligible; the shader gains 1 multiplication per fragment — negligible).

    **NEW for Walk #5: STRUCTURALLY-ENFORCED Mail-share gate makes PERF-07 numerical evidence available.** Walks #3 + #4 broke the Mail-share pattern; Walk #5's resume-signal contract requires the log path as a parameter. Walk #5's PERF-07 numbers will be quantitative, not implicit.

    ### Criterion B — Subjective visual lock (PERF-05 carry-over, re-tested)

    Developer's verbal verdict at end of walk:

    - **Q1 (RETAINED from Walk #4)** — No perceived hard-step events during pure pan. Walk #4 confirmed; Walk #5 re-confirms. Sub-test: developer pans for ≥ 60 seconds in one direction.
    - **Q2 (RETAINED from Walk #4)** — No rotation-correlated fog mis-coverage. Walk #4 confirmed via UX-02; Walk #5 re-confirms.
    - **Q3 (RETAINED from Walks #1-4)** — No reveal-hole offset from blue dot during gesture.
    - **Q4 (RETAINED from Walks #1-4)** — Fog visually translates with camera during pan.
    - **Q5 NEW (Walk #5 PRIMARY GATE)** — Noise pattern is anchored to ground meters; at a fixed geographic point the noise sample is zoom-invariant; cells visibly enlarge with zoom. Walk #4 surfaced this NEW failure; Plan 03.1-12 FOG-18 targets it.
      - Sub-test (production fog): developer fixes camera centre on lat 48.5397, lon 2.6553 and zooms from min to max; verifies the production fog noise pattern at the camera centre appears stable across zoom transitions (no "scramble"). The expected behaviour: fog grain at any specific point on the ground stays at the same noise sample regardless of zoom; the visible noise CELLS scale with zoom (more raw px per cell at higher zoom).
      - Sub-test (debug-spiral): developer toggles debug-spiral ON; zooms from min to max; verifies (a) cells visibly enlarge with zoom (cells are tied to ground); (b) at fixed map position the spiral cell numbering at the camera centre stays the same across all zoom levels (the fragment under the blue dot should always sample the same noise cell, regardless of zoom — direct visual confirmation of FOG-18 zoom-invariance acceptance property).
    - No white-ellipse during fast pinch-zoom (already passed Walks #1-4).
    - No inversion at any zoom level (already passed Walks #1-4).

    ### Criterion C — DROPPED

    Stays dropped per Phase 3 + roadmap lock.

    ### Criterion D — Diagnostic stream confirms the layered fix

    Per the `Logger('infrastructure.mirk.fog_transform')` 1-Hz JSONL rollups:

    - **CONFIRMED-AFTER-FIX:** `pixelOriginX/Y` track gesture monotonically. `appliedUOffset` keys (FOG-17a signature) stay under `kPocFogIntegerWrapPeriodPx + 1` (~1537 raw px) regardless of zoom level — the FOG-17a invariant MUST hold under FOG-18 (the decomposition operates on raw pixel units BEFORE shader multiplication). **NEW: metersPerPixel field** present in rollup AND non-zero AND consistent with synthetic camera state — at zoom 13 lat 48.5° expected ≈ 12.66; at zoom 15 ≈ 3.16; at zoom 19 ≈ 0.198. `canvasTx/Ty` jumps still occur — Plan 03.1-08 FOG-13 compensates them mechanically.
    - **DENIED-FINAL pattern:** the diagnostic stream shows the layered fix landed mechanically AT EVERY level (pixelOrigin tracks; appliedUOffset bounded under 1537; metersPerPixel correctly varies with synthetic camera state; canvas.translate compensates rect-cover; UX-02 makes camera.rotation = 0.0) AND the user-observable Q5 zoom-scramble STILL persists — root cause is even deeper than the world-meter anchor (e.g., flutter_map's MapCamera.zoom updates are quantised in a way that breaks zoom-invariance at gesture-time vs paint-time; or the FBM rotation matrices break exact-period continuity at meter-space wrap events more aggressively than expected). Architecture is unfixable in `flutter_map 7.0.2` with the current shader stack. Phase 3.2 alternative-renderer pivot opens.
    - **ITERATING pattern:** subjective verdict surfaces a residual failure mode. Most likely candidates: (i) meter-space integer-wrap stepping at sustained one-direction pan (every ~128 sec at 12 raw px/s; Plan 03.1-12 noise-function inspection documented this as the FBM-rotated-octave non-integer-multiple shift caveat); (ii) NEW failure mode introduced by Plan 03.1-12 (e.g., metersPerPixel computation fp32 precision at extreme zoom; or cos(lat) clamp artefact). The diagnostic stream points at the next fix axis; Plan 03.1-14+ is opened.

    ## Walk Plan

    Mirrors Plan 03.1-11 (Walk #4) walk shape with three NEW additions for Walk #5:

    - **NEW: STRUCTURALLY-ENFORCED Mail-share discipline.** The resume-signal contract REQUIRES the absolute path of the Mail-shared session log file as a parameter. Walks #3 + #4 broke Mail-share twice; Walk #5 makes it a structural verdict-capture gate. The executor refuses verdict authoring without the log path.
    - **NEW: explicit Q5 zoom-scramble re-test** at debug-spiral toggle ON: developer zooms from min to max with debug-spiral active and verifies cells visibly enlarge with zoom AND cells at fixed map position stay the same across zoom levels.
    - **NEW: explicit dev-marker-button taps for residual symptoms.** The marker button at the AppBar (commit `79ab978`) is operational; Walks #3b + #4 proved it. Developer is instructed to tap the marker every time they perceive ANY residual symptom.

    - **Where:** Central Melun (lat 48.5397, lon 2.6553 area).
    - **Duration:** ≥ 5 minutes continuous walk with the IPA running and the device awake.
    - **Gestures:** ≥ 10 deliberate combined pinch-zoom-and-pan gestures, ≥ 3 recenter FAB taps. AT LEAST ONE deliberate two-finger rotate-attempt gesture (validates Q2). AT LEAST ONE sustained ≥ 60-second one-direction pan (validates Q1). **NEW: AT LEAST ONE complete zoom traversal (min zoom → max zoom or max → min)** at a fixed map position with both production fog AND debug-spiral toggled (validates Q5 — the PRIMARY Walk #5 gate).
    - **Pre-walk gate (unit tests):** `flutter test` GREEN (180+ existing + Plan 03.1-12 new tests: FOG-18 fog_world_meter_anchor 3 sub-tests + fog_meter_decomposition 5+ sub-tests + REWRITTEN fog_world_coordinate_noise + fog_pixel_origin_decomposition + fog_tile_period_invariant + fog_debug_spiral_continuity + UPDATED fog_shader_uniforms_test totalFloatSlots == 42); `flutter analyze --fatal-infos` 0 warnings; `dart format --line-length 160 --set-exit-if-changed .` clean; `dart test tool/test/` GREEN.
    - **Pre-walk gate (CI):** `gh run list --branch main --limit 5` shows `success` for the post-Plan-03.1-12 SHA; `gh run view <RUN_ID>` confirms `gates`, `build-android`, `build-ios` all green; IPA downloaded via `gh run download <RUN_ID> --name ios-unsigned --dir .uat-tmp/`.
    - **Pre-walk gate (sideload):**
      1. Open `/sanity` route — confirm production fog renders with circular reveal hole (debug-spiral toggle OFF default).
      2. Toggle the debug-spiral switch ON (either on `/sanity` AppBar OR on `/map` AppBar — global toggle landed in Plan 03.1-08 mid-checkpoint corrective bundle commit `f707226`) — confirm the digit-atlas spiral pattern renders with visible numbered cells (DEBUG-01 walk-time validation; **NEW for Walk #5**: at the initial zoom 13 the cell size on screen should be ≈ 200 / 12.66 ≈ 16 raw px — visibly smaller than Plan 03.1-10 era ≈ 80 raw px; at extreme zoom-in ≈ 200 / 0.2 ≈ 1000 raw px — visibly LARGE; this is the visual confirmation that FOG-18 cell-size meter-anchoring landed).
      3. Toggle OFF — confirm production fog returns.
      4. Confirm the `Icons.arrow_back` AppBar leading button still works on `/sanity` (UX-01 retained).
      5. Confirm zero shader-compile exceptions in the FileLogger output.
      6. Attempt a two-finger rotate gesture on `/map` — confirm the map does NOT rotate (UX-02 walk-time validation).
    - **In-app HUD during walk:** `FpsCounterOverlay` (top:8, right:8) + `MapCompass` (top:56, right:8 — should stay at 0° throughout per UX-02) + `FrameDeltaProbeOverlay` (top:104, right:8).
    - **Post-walk:** **STRUCTURALLY-ENFORCED.** Share the session log file via Mail (LOG-04 round-trip — REQUIRED for Walk #5; Walks #3 + #4 broke this; Walk #5 makes it a structural verdict-capture gate). Save the Mail attachment to a known local path (e.g., `~/Downloads/walk5_session_log.txt`). Report the absolute path as a parameter in the resume signal: `approved [/full/path/to/walk5_session_log.txt] — Q5 zoom-scramble resolved + Q1-Q4 retained`. The executor then runs grep against the actual log file before authoring the verdict.

    ## Walk Evidence (filled AFTER the walk by Plan 03.1-13 Task 3)

    ### Pre-walk shader-sanity gate (`/sanity` route + UX-01 back-button validation + DEBUG-01 toggle live-test + UX-02 rotation-disabled walk-time validation + FOG-18 cell-size visual confirmation)
    _TBD — filled post-walk._

    ### Probe rollup (frame-delta JSONL — `Logger('infrastructure.mirk.frame_delta')`)
    _TBD — filled post-walk. Expected: median ≤ 16 ms, p95 ≤ 32 ms, max ≤ 48 ms across ≥ 10 combined gestures (PERF-07 re-validation; Walk #1 4-7×; Walk #2 3.9-5.4×; Walk #3b 40-110×; Walks #3 + #4 implicit; Walk #5 should preserve at least Walk #2 levels)._

    ### SDF rebuild rollup (`Logger('infrastructure.mirk.sdf')` — PERF-08 status)
    _TBD — filled post-walk. PERF-08 was falsified-by-walk-2 (rebuild rate 1-121/sec); Walk #3b confirmed thrash persists at low pan (max 48/sec). Walks #3 + #4 implicit. Walk #5 re-measures._

    ### Fog-transform rollup (`Logger('infrastructure.mirk.fog_transform')`)
    _TBD — filled post-walk._
    _**KEY FOG-17a SIGNATURE (RETAINED under FOG-18):** `appliedUOffsetXMin/Median/Max` values should be in [0, kPocFogIntegerWrapPeriodPx + 1] = [0, 1537] range across ALL rollups even when pixelOriginXMin/Median/Max reaches millions. Plan 03.1-12 doesn't touch FOG-17a; the bounded-composite invariant MUST hold._
    _**NEW Walk #5 FOG-18 SIGNATURE: metersPerPixel field** present in fog_transform rollup AND non-zero AND consistent with synthetic camera state. At zoom 13 lat 48.5° expected ≈ 12.66; at zoom 15 ≈ 3.16; at zoom 19 ≈ 0.198. The metersPerPixel field should evolve smoothly with camera zoom across the walk (no discontinuities)._

    ### FPS observations (PERF-03 — coarse `FpsCounterOverlay` reading)
    _TBD — filled post-walk._

    ### Marker correlation (Walk #3b template applied if dev-marker fired)
    _TBD — filled post-walk. If the dev-marker fired during Walk #5, mirror the Walk #3b template: per-marker pixelOrigin delta, marker-to-marker pan magnitude, correlation against the integer-wrap-period boundary (was the marker tap on or near a meter-space integer-wrap event?). If no marker fired, this section reads "No dev-markers fired — developer perceived no residual stepping or zoom-scramble symptoms during the walk."_

    ### Subjective verdict (Criterion B — Q1-Q5)
    _TBD — filled post-walk. Developer's verbatim words + interpretation paragraph mapping each Criterion B sub-claim (Q1-Q5) to PASS/FAIL. **Q5 is the PRIMARY Walk #5 gate** — the new sub-claim that Plan 03.1-12 FOG-18 was authored to close._

    ## Verdict

    - [ ] Criterion A passed? — TBD post-walk.
    - [ ] Criterion B passed? — TBD post-walk.
    - [ ] Criterion D passed? — TBD post-walk.

    **Outcome:** TBD.

    **MirkFall migration recommendation:** TBD.
    ```

    Then create `.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-5.md` as the thin Walk #5 transcript skeleton — mirror `03.1-UAT-4.md`'s structure with metadata block + Walk Protocol + empty `## Walk Evidence` section + `## Verdict` placeholder + `## Deviations / Surprises` placeholder + `## Summary` placeholder. The frontmatter `gaps:` YAML stays empty until Task 3 fills it (or stays empty on `approved` / `denied` outcomes).

    **Pre-walk verification: confirm FogTransformLogger metersPerPixel field exists.** Read `lib/infrastructure/mirk/fog_transform_logger.dart` (the existing logger from Plan 03.1-01 era) and check whether the rollup body includes a `metersPerPixel` field. If NOT, Plan 03.1-12 should have added it as part of Task 2 (the painter forwarding); if Plan 03.1-12 SUMMARY noted this as a gap (Issues Encountered section), fix it as a Walk #5 sub-task before sideload:
    1. Read `lib/infrastructure/mirk/fog_transform_logger.dart` source.
    2. If `metersPerPixel` is NOT in the recordPaint method's named params and rollup body: add it. Mirror the existing pattern (e.g., `final double metersPerPixel`); compute min/median/max across the buffered samples per second; emit in JSON rollup body alongside the existing pixelOriginX/Y + appliedUOffsetX/Y fields.
    3. Update `_FogPainter.paint()` to pass `metersPerPixel: metersPerPixel` to `fogTransformLogger.recordPaint(...)`.
    4. Run `flutter test` to confirm GREEN.
    5. Commit: `feat(03.1-13): augment FogTransformLogger with metersPerPixel field for Walk #5 diagnostic verification`.

    NOW execute the pre-walk gate sequence:
    1. `flutter test` (must report 0 failures, 0 reds — Phase 1+2+3 retained tests + Plan 03.1-12 new tests all GREEN; the FOG-09 vacuous-test guard stays SKIPPED)
    2. `flutter analyze --fatal-infos` (must exit 0)
    3. `dart format --line-length 160 --set-exit-if-changed .` (must exit 0)
    4. `dart test tool/test/` (must report all GREEN)
    5. `gh run list --branch main --limit 5` (capture the post-Plan-03.1-12 SHA + run ID; the run must be `success`)
    6. `gh run view <RUN_ID>` (confirm `gates`, `build-android`, `build-ios` all `success`)
    7. `gh run download <RUN_ID> --name ios-unsigned --dir .uat-tmp/` (produces `.uat-tmp/mirk-poc-debug-unsigned.ipa`)

    If ANY pre-walk gate fails, STOP and report. Do NOT proceed to Task 2 until every gate is green.
  </action>
  <verify>
    <automated>flutter test 2>&amp;1 | tail -3 &amp;&amp; flutter analyze --fatal-infos 2>&amp;1 | tail -3 &amp;&amp; dart format --line-length 160 --set-exit-if-changed . 2>&amp;1 | tail -3 &amp;&amp; dart test tool/test/ 2>&amp;1 | tail -3 &amp;&amp; gh run list --branch main --limit 1 2>&amp;1 | tail -3</automated>
    <file_must_exist>.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md</file_must_exist>
    <file_must_exist>.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-5.md</file_must_exist>
    <file_must_exist>.uat-tmp/mirk-poc-debug-unsigned.ipa</file_must_exist>
  </verify>
  <done>
    - All 4 local pre-walk gates exit 0 (test / analyze / format / tool-tests).
    - CI run on the post-Plan-03.1-12 SHA on `main` is `success` (gates + build-android + build-ios all green).
    - `.uat-tmp/mirk-poc-debug-unsigned.ipa` exists and is non-empty (≥ 5 MB).
    - `03.1-FALSIFICATION-5.md` exists with all top-level sections + the Relationship-to-Walk-4 section. Walk Evidence + Verdict bodies read `TBD`.
    - `03.1-UAT-5.md` exists as a thin transcript skeleton.
    - FogTransformLogger emits a `metersPerPixel` field in its 1-Hz JSONL rollup (verified by reading the source OR by augmenting the logger as a Walk #5 sub-task if Plan 03.1-12 did not add it).
    - `git status` shows the existing `03.1-FALSIFICATION.md` + `03.1-FALSIFICATION-2.md` + `03.1-FALSIFICATION-3.md` + `03.1-FALSIFICATION-4.md` + `03.1-UAT.md` + `03.1-UAT-2.md` + `03.1-UAT-3.md` + `03.1-UAT-4.md` + `03.1-WALK-3B-MARKER-ANALYSIS.md` UNCHANGED (Walks #1 + #2 + #3 + #4 + Walk #3b records preserved).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: iPhone 17 Pro sideload UAT Walk #5 + STRUCTURALLY-ENFORCED Mail-share discipline + post-walk verbal verdict capture</name>
  <files>.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md, .planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-5.md</files>
  <action>
    Hand off to the developer for the manual UAT Walk #5 on iPhone 17 Pro. The full procedure is documented in the checkpoint-specific `<what-built>`, `<how-to-verify>`, and `<resume-signal>` sections — those are the executor-facing instructions. **CRITICAL:** the resume-signal contract REQUIRES the absolute path of the Mail-shared session log file as a parameter; the executor MUST refuse verdict authoring without the log path. After the resume signal is received with a valid log path, Claude proceeds to Task 3 (verdict authoring + REQUIREMENTS/ROADMAP/STATE updates).
  </action>
  <verify>
    <automated>MISSING — PERF-07 + Criterion B (Q1-Q5 sub-claims) + Criterion D (subjective + diagnostic-stream pattern matching + dev-marker correlation if markers fire + metersPerPixel JSONL signature verification) are inherently manual UAT walk gates per 03.1-VALIDATION.md Manual-Only Verifications table; no automated proxy exists. The developer&#39;s resume-signal IS the verification.</automated>
  </verify>
  <done>
    Developer returns with one of (each requires log_path parameter):
    (a) `approved [/full/path/to/walk5_session_log.txt] — Q5-zoom-scramble-resolved + Q1-Q4-retained` — all five Q1-Q5 sub-claims confirmed; CONFIRMED-AFTER-FIX.
    (b) `denied [/full/path/to/walk5_session_log.txt]: <verbatim description>` — describe what went wrong; the executor opens post-walk JSONL grep against the log file.
    (c) `iterating [/full/path/to/walk5_session_log.txt]: <verbatim description>` — same as denied: but developer chooses to iterate (Plan 03.1-14+).
    Plus the post-walk session log shared via Mail (LOG-04 round-trip) AND saved to a local path the executor can read. **The log_path parameter is REQUIRED** — the executor refuses verdict authoring without it.
  </done>
  <what-built>
    The Plan 03.1-12 software fix bundle is complete:

    - **FOG-18 (Fix B world-meter anchor):** `assets/shaders/atmospheric_fog.frag` AND `assets/shaders/atmospheric_fog_debug_spiral.frag` BOTH adopt world-meter noise sampling: `vec2 worldMeters = (fragUv * uResolution + uPixelOrigin) * uMetersPerPixel; vec2 noiseUv = worldMeters / kNoiseTilePxMeters;`. Each fragment samples noise at its own world-meter position; at a fixed geographic point worldMeters is zoom-INVARIANT (the noise sample at that point is therefore zoom-invariant). Cells visibly enlarge when zoomed in, shrink when zoomed out.
    - **NEW uniform `uMetersPerPixel`** at slot 41 (`FogShaderUniforms.totalFloatSlots` advances 41 → 42; under shader budget cap of 64).
    - **Dart-side `metersPerPixel = kWebMercatorMetersPerPxAtEquatorZ0 * cos(camera.center.latitude * π/180) / pow(2.0, camera.zoom)`** computed at every paint in three painters: `_FogPainter.paint()` (production fog), `_DebugSpiralMapPainter.paint()` (`/map` debug-spiral), `/sanity` `_DebugSpiralPainter` (synthetic — uses `kPocInitialCameraLat` + `kPocInitialZoom`).
    - **FOG-17a integer/fractional decomposition RETAINED unchanged** — operates on raw pixel units BEFORE the shader multiplies by uMetersPerPixel. Boundedness analysis: worldMeters ≤ ~6200 m at z=15 lat 48.5°; ≤ ~387 m at z=19; noiseUv ≤ ~6.05 worst case — well within fp32 precision.
    - **New constants:** `kWebMercatorMetersPerPxAtEquatorZ0 = 156543.03392`, `kPocFogNoiseTilePxMeters = 1024.0`, `kPocDebugSpiralCellSizeMeters = 200.0`.

    CI gates green for new + rewritten tests:
    - `test/presentation/widgets/fog_world_meter_anchor_test.dart` — 3 sub-tests asserting zoom-invariance at fixed lat/lng across z=10..15 (Melun + equator + high-lat).
    - `test/presentation/widgets/fog_meter_decomposition_test.dart` — 5+ sub-tests covering metersPerPixel formula at z=10..19 lat 0°..80°, FOG-17a bounded-composite preserved, worldMeters magnitude bound, painter-forwards-metersPerPixel, per-paint re-derivation.
    - REWRITTEN `test/presentation/widgets/fog_world_coordinate_noise_test.dart` — simulator flipped pixel→meter; ZERO wraps at three pixelOrigin magnitudes.
    - REWRITTEN `test/presentation/widgets/fog_pixel_origin_decomposition_test.dart` — FOG-17a invariants retained; NEW metersPerPixel + worldMeters bound assertions.
    - REWRITTEN `test/presentation/widgets/fog_tile_period_invariant_test.dart` — flipped FOG-17 → FOG-18 invariants; totalFloatSlots == 42.
    - REWRITTEN `test/presentation/widgets/fog_debug_spiral_continuity_test.dart` — added post-FOG-18 sub-test; retained historical sub-tests.
    - UPDATED `test/infrastructure/mirk/shader/fog_shader_uniforms_test.dart` — totalFloatSlots == 42 + slot-layout source-grep.
    - TOUCHED `test/presentation/widgets/fog_smooth_noise_test.dart` (NEW metersPerPixel range assertion) + `fog_pan_translation_test.dart` (NEW per-paint metersPerPixel re-derivation assertion).

    Existing tests still GREEN: FOG-04 structural, FOG-07 KEYSTONE, FOG-09 transform-equality, FOG-10 fog_transform_logger, FOG-11 smooth-noise, FOG-12 canvas-frame-alignment, FOG-13 rect-viewport-coverage, PERF-08 bbox-quantisation, UX-01 sanity back button, UX-02 rotation-disabled.

    Pre-walk gates (Task 1): all 6 GREEN. The IPA at `.uat-tmp/mirk-poc-debug-unsigned.ipa` is ready for sideload.
  </what-built>
  <how-to-verify>
    1. Open SideStore on iPhone 17 Pro. Refresh certificate if needed. Install or refresh `.uat-tmp/mirk-poc-debug-unsigned.ipa`.
    2. Launch `mirk-poc-debug` on iPhone 17 Pro. Allow GPS via the rationale screen → "Allow While Using App".
    3. **Pre-walk sanity gate (production fog + UX-01 + DEBUG-01 walk-time validation + UX-02 rotation-disabled walk-time validation + FOG-18 cell-size visual confirmation):** tap the science (`Icons.science`) button in the AppBar to navigate to `/sanity`.
       - Confirm the production fog renders with circular reveal hole.
       - Confirm an `Icons.arrow_back` button appears in the leading slot (UX-01 retained).
       - Confirm a `Switch.adaptive` toggle for "Debug Spiral" appears in the AppBar actions (DEBUG-01).
       - Toggle the debug spiral ON. Confirm the digit-atlas spiral pattern renders. **NEW for Walk #5: FOG-18 visual confirmation.** At the initial /sanity zoom (synthetic z=13 lat 48.5° → metersPerPixel ≈ 12.66), the cell size on screen should be ≈ 200 / 12.66 ≈ 16 raw px — visibly smaller than Plan 03.1-10 era ≈ 80 raw px. (The /sanity uses synthetic camera so this is a fixed observation; the live zoom-variance test happens on /map.)
       - Toggle OFF. Confirm production fog returns.
       - Tap the back button. Confirm it returns to `/map`.
       - Attempt a deliberate two-finger rotate gesture on `/map`. Confirm:
         - The map does NOT rotate.
         - The MapCompass widget stays at 0°.
    4. **Walk the route:** ≥ 5 minutes continuous through central Melun. During the walk:
       - Perform ≥ 10 deliberate combined pinch-zoom-and-pan gestures.
       - Perform ≥ 3 recenter FAB taps.
       - Perform AT LEAST ONE sustained ≥ 60-second one-direction pan (validates Q1).
       - Perform AT LEAST ONE deliberate two-finger rotate-attempt gesture during normal walk (validates Q2).
       - **NEW for Walk #5 — Q5 PRIMARY GATE: perform AT LEAST ONE complete zoom traversal at a fixed map position with both production fog AND debug-spiral toggled.**
         - **Sub-test 5a (production fog):** centre the camera on a recognisable feature (e.g., a road intersection or building corner). Tap recenter FAB if needed. Slowly zoom from min zoom (z=10 — country-level view) to max zoom (z=18 or higher — building-level). Watch the fog noise pattern AT THE FIXED FEATURE: it should appear stable across zoom transitions (no rapid sliding/scramble). Cells should visibly enlarge when zoomed in, shrink when zoomed out.
         - **Sub-test 5b (debug-spiral):** toggle debug-spiral ON via the `/map` AppBar global toggle. Note the cell number at the camera centre (e.g., "84"). Slowly zoom from min to max while keeping the camera centred on the same feature. Watch the cell number AT THE CAMERA CENTRE: it should stay the same across zoom levels (e.g., always "84" — the fragment under the blue dot always samples the same noise cell). Watch the cell SIZE on screen: it should visibly enlarge when zoomed in (cells "tied to ground").
         - **Sub-test 5c (debug-spiral pan during fixed zoom):** with debug-spiral ON at z=15, pan the camera ~500 m. Cells should scroll smoothly with NEW noise entering from the viewport edges (no fract-wrap; FOG-17 retained).
         - **Optional comparison sub-test 5d:** for direct comparison with Walk #4 (where Q5 failed), zoom in/out QUICKLY (rapid pinch). Pre-Plan-03.1-12 the noise pattern slid rapidly during zoom (the "scramble"); post-Plan-03.1-12 it should appear stable.
       - Watch the FrameDeltaProbeOverlay (top:104, right:8) — note approximate `med` / `p95` / `max` values during combined gestures.
       - **NEW: Tap the dev-marker button** every time the developer perceives ANY residual symptom (steppy translation, fog mis-coverage, zoom-scramble residual, anything visually anomalous).
       - **The KEY observations** (Q1-Q5 sub-claims):
         - **Q1: No perceived hard-step events during pure pan?** (Walk #4 confirmed; re-confirmation).
         - **Q2: No rotation-correlated fog mis-coverage during rotate-attempt gestures?** (Walk #4 confirmed; re-confirmation).
         - **Q3: Reveal hole co-located with the blue dot during pan/zoom?** (Walks #1-4 retained; re-confirmation).
         - **Q4: Fog visually translates with the map during pan?** (Walks #1-4 retained; re-confirmation).
         - **Q5 (PRIMARY): Noise pattern is anchored to ground meters; cells visibly enlarge with zoom; at fixed map position the noise sample is zoom-invariant?** (Walk #4 NEW failure; Plan 03.1-12 closes this — PRIMARY Walk #5 gate).
    5. **Post-walk: STRUCTURALLY-ENFORCED Mail-share.** Tap the share button in the AppBar → pick Mail → send the session log to your own email → save the attached `yyyymmdd_hhmmss_logs.txt` from your inbox to a known local path (e.g., `~/Downloads/walk5_session_log.txt` on macOS, or `C:\Users\oliver\Downloads\walk5_session_log.txt` on Windows). **The absolute path is REQUIRED in the resume signal.** Walks #3 + #4 broke this discipline; Walk #5 makes it structural.
    6. **Capture the verdict:** describe in your own words whether each of the five Q1-Q5 sub-claims is now resolved. Single-word `approved [path]` is the gate for `CONFIRMED-AFTER-FIX`. Any residual failure mode should be described — that description feeds the diagnostic JSONL grep step.
    7. **Post-walk grep correlation** (executor runs after the verbal verdict + log path are captured):
       - `grep "infrastructure.mirk.frame_delta" <log-file> > .uat-tmp/walk5_frame_delta.jsonl`
       - `grep "infrastructure.mirk.sdf" <log-file> > .uat-tmp/walk5_sdf.jsonl`
       - `grep "infrastructure.mirk.fog_transform" <log-file> > .uat-tmp/walk5_fog_transform.jsonl`
       - `grep "dev_marker" <log-file> > .uat-tmp/walk5_dev_markers.jsonl` (if any markers fired)
       - For each line, parse the JSON, extract `epochSecond`, produce a per-second composite row.
       - **NEW for Walk #5: verify metersPerPixel JSONL signature** — the fog_transform rollups should each include a `metersPerPixel` field. Verify the value is non-zero AND consistent with the synthetic camera state (e.g., at zoom 15 lat 48.5° expected ≈ 3.16). The metersPerPixel field should evolve smoothly with camera zoom across the walk.
       - **Verify FOG-17a signature RETAINED under FOG-18:** appliedUOffsetXMin/Median/Max values bounded under 1537 raw px across ALL rollups (FOG-17a invariant must hold; the decomposition operates on raw pixels BEFORE shader multiplication).
       - Pipe extracts into `03.1-FALSIFICATION-5.md`'s Walk Evidence subsections.

    **Expected `CONFIRMED-AFTER-FIX` evidence pattern:**
    - Walk evidence: developer says `approved [path]` — no perceived steps; rotate gestures are no-ops; reveal hole co-located with blue dot; fog tracks camera; **Q5 zoom-scramble RESOLVED** — cells visibly enlarge with zoom; cells at fixed map position stay the same across zoom levels.
    - `frame_delta` rollup: PERF-07 thresholds satisfied with similar headroom to Walks #1-2.
    - `fog_transform` rollup: `pixelOriginX/Y` monotonic; `appliedUOffsetXMin/Median/Max` bounded under 1537 raw px (FOG-17a retained); **NEW: metersPerPixel field present, non-zero, evolving smoothly with camera zoom** (FOG-18 signature).
    - `dev_markers` JSONL: empty or contains only sub-perceptible-cadence markers (~128-sec wrap events).

    **DENIED-FINAL or ITERATING patterns** are documented in `03.1-FALSIFICATION-5.md` §Criteria — pattern matching uses the diagnostic stream + (if markers fired) marker-correlation analysis to identify residual fix axes (or, in DENIED case, the architectural-counter-evidence finding pointing at Phase 3.2 alternative-renderer pivot).
  </how-to-verify>
  <resume-signal>
    Type ONE of (the absolute log path is REQUIRED — without it the executor refuses verdict authoring):

    - `approved [/full/path/to/walk5_session_log.txt] — Q5-zoom-scramble-resolved + Q1-Q4-retained` — all five Q1-Q5 sub-claims confirmed by developer. CONFIRMED-AFTER-FIX. Executor proceeds to Task 3 (Branch A) and flips FOG-04..07 + FOG-11 + FOG-15 + FOG-18 + FOG-16 + FOG-17 + UX-02 + PERF-07 to Complete.

    - `denied [/full/path/to/walk5_session_log.txt]: <verbatim description of the residual failure mode>` — describe what went wrong. Executor opens the post-walk JSONL grep results from the log file, correlates against the failure description, and reports back: either (a) recommend a Plan 03.1-14+ follow-up fix attempt with a specific diagnostic-anchored hypothesis (e.g., FBM-rotated-octave non-integer-multiple shift at meter-space wrap; pivot to periodic Worley/explicit-period Perlin), or (b) recommend `DENIED-FINAL` if the diagnostic evidence shows the failure is structural at the renderer-architecture level (Phase 3.2 alternative-renderer pivot conversation: `mapsforge_flutter`, custom MVT-on-Canvas, `flutter_gpu`).

    - `iterating [/full/path/to/walk5_session_log.txt]: <verbatim description>` — same as `denied:` shape but the developer chooses to iterate per CONTEXT §Iteration policy. Executor opens a Plan 03.1-14 in a follow-up plan-phase invocation.

    **WITHOUT a log path:** the executor refuses verdict authoring and re-prompts the developer. The structural Mail-share gate is the load-bearing addition for Walk #5.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Author the Walk #5 verdict — fill `03.1-FALSIFICATION-5.md` Walk Evidence + Verdict; update REQUIREMENTS.md / ROADMAP.md / STATE.md</name>
  <files>.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md, .planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-5.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
    **PRE-CONDITION CHECK:** Verify the developer's resume signal includes a log path parameter. If not, REFUSE to proceed and re-prompt. Read the log file at the provided path; confirm it contains the expected JSONL streams (`infrastructure.mirk.frame_delta`, `infrastructure.mirk.sdf`, `infrastructure.mirk.fog_transform`, optionally `dev_marker`). If the log file is missing or malformed, REFUSE and re-prompt.

    Branch on the developer's verdict from Task 2:

    **Branch A — `approved [log_path]` → CONFIRMED-AFTER-FIX:**

    1. Run post-walk grep correlation against the log file at `log_path`:
       ```
       grep "infrastructure.mirk.frame_delta" <log_path> > .uat-tmp/walk5_frame_delta.jsonl
       grep "infrastructure.mirk.sdf" <log_path> > .uat-tmp/walk5_sdf.jsonl
       grep "infrastructure.mirk.fog_transform" <log_path> > .uat-tmp/walk5_fog_transform.jsonl
       grep "dev_marker" <log_path> > .uat-tmp/walk5_dev_markers.jsonl
       ```
       For each, parse JSON, extract `epochSecond`, produce per-second composite row.

    2. Fill `03.1-FALSIFICATION-5.md` Walk Evidence subsections with the post-walk grep correlation. **NEW Walk #5 verifications:**
       - **fog_transform metersPerPixel field present + consistent with camera state.** Each rollup line has a `metersPerPixel` field (or `metersPerPixelMin/Median/Max` for the per-second aggregation pattern that mirrors pixelOriginX/Y). Values are non-zero and evolve smoothly with camera zoom (no discontinuities). At zoom 15 lat 48.5° expected ≈ 3.16; at zoom 19 ≈ 0.198. The walk's actual zoom range can be inferred from pixelOriginX/Y magnitude (zoom 13 → ~1M; zoom 15 → ~4M; zoom 19 → ~17M) — metersPerPixel should track inversely.
       - **FOG-17a signature RETAINED under FOG-18.** appliedUOffsetXMin/Median/Max values bounded under 1537 raw px across ALL rollups, even when pixelOriginXMin/Median/Max reaches millions. Direct visible signature of FOG-17a in effect post-FOG-18.

    3. Fill `03.1-FALSIFICATION-5.md` Verdict section:
       ```markdown
       ## Verdict

       - [x] Criterion A passed? — **YES.** Frame-delta probe rolling rollup over ≥ 10 combined gestures: median = {N} ms (≤ 16 ✓), p95 = {N} ms (≤ 32 ✓), max = {N} ms (≤ 48 ✓). PERF-07 re-validated post-Plan-03.1-12.
       - [x] Criterion B passed? — **YES.** Developer's verbatim verdict: *"<verbatim>"*. All five Q1-Q5 sub-claims confirmed:
         - Q1 ✓ (Walk #4 retained): no perceived hard-step events during pure pan;
         - Q2 ✓ (Walk #4 retained): rotate-attempt gestures are no-ops;
         - Q3 ✓ (Walks #1-4 retained): reveal hole co-located with blue dot;
         - Q4 ✓ (Walks #1-4 retained): fog translates with camera;
         - **Q5 ✓ (NEW — Walk #5 closure): noise pattern anchored to ground meters; cells visibly enlarge with zoom; at fixed map position the noise sample is zoom-invariant.**
       - [x] Criterion D passed? — **YES.** Diagnostic stream confirms the layered fix at every level. `appliedUOffset` values bounded under 1537 raw px across ALL rollups (FOG-17a signature RETAINED under FOG-18). `metersPerPixel` field present in fog_transform rollup, non-zero, evolving smoothly with camera zoom (FOG-18 signature). No dev-markers fired (or markers fired only at sub-perceptible cadence — Walk #5b candidate analysis if cadence ≥ 128 sec).

       **Outcome:** **CONFIRMED-AFTER-FIX.**

       The same-Canvas fog hypothesis is **reinstated** with the layered Plan 03.1-02 + 03.1-04 + 03.1-05 + 03.1-07 + 03.1-08 + 03.1-10 + 03.1-12 fix bundle. Phase 3's `DENIED 2026-05-01` verdict and Phase 3.1 Walks #1-4's `ITERATING-WITH-*` verdicts are converted to `CONFIRMED-AFTER-FIX` after Walk #5.

       **MirkFall migration recommendation:** **PORT BACK** with the full layered fix bundle:
       1. `_FogPainter.paint()` derives `pixelOrigin` from `camera.pixelOrigin` (Plan 03.1-02).
       2. `atmospheric_fog.frag` slot 3..4 uniform `uPixelOrigin` (renamed from `uOffset`) (Plan 03.1-04).
       3. `_FogPainter.paint()` reads `canvas.getTransform()` and forwards `Offset(matrix[12], matrix[13])` to `computeFogClipPath(canvasOffset: ...)` (Plan 03.1-05).
       4. `SdfCache._hash(...)` quantises viewport bbox to 1e-4 lat/lon (Plan 03.1-05; performance-only).
       5. `_FogPainter.paint()` calls `canvas.translate(-canvasOffset)` immediately after `canvas.clipPath(clipPath)` (Plan 03.1-08).
       6. `atmospheric_fog.frag` AND `atmospheric_fog_debug_spiral.frag` both adopt the FOG-17 world-coordinate sampling formulation followed by the FOG-18 world-meter anchor (Plan 03.1-10 + Plan 03.1-12).
       7. `_FogPainter.paint()` decomposes `camera.pixelOrigin` into integer + fractional and forwards bounded composite (Plan 03.1-10 FOG-17a).
       8. `_FogPainter.paint()` AND `_DebugSpiralMapPainter.paint()` AND `/sanity` `_DebugSpiralPainter` all compute `metersPerPixel = kWebMercatorMetersPerPxAtEquatorZ0 * cos(lat) / pow(2, zoom)` and forward via the new uniform `uMetersPerPixel` slot 41 (Plan 03.1-12 FOG-18).
       9. `FlutterMap.options.interactionOptions` clears the rotate flag (Plan 03.1-10 UX-02).
       10. (Optional) The DEBUG-01 debug-spiral infrastructure is OPTIONAL for port-back — permanent diagnostic tooling, useful but not required for production fog correctness.

       The MirkFall donor `_FogPainter.paint()` call site MUST be reviewed against ALL nine production-impacting changes at port-back time.

       **POC scope reduction note (UX-02 + FOG-16):** The POC ships with rotation disabled. If MirkFall after port-back wants to re-enable rotation, the canvas-rotation-aware compensation (FOG-16 path (b) — read full canvas matrix; apply inverse via `canvas.transform(matrix.inverted())`) must be implemented before re-enabling.
       ```

    4. Update `.planning/REQUIREMENTS.md`:
       - FOG-04, FOG-05, FOG-06, FOG-07: change `[ ]` → `[x]`. Update inline status from `Falsified-in-production` to `Complete — Verified-by-walk (P03.1-13 CONFIRMED-AFTER-FIX 2026-05-XX after layered Plan 03.1-02+04+05+07+08+10+12 fix bundle)`.
       - FOG-11: `[ ]` → `[x]`; status `Complete — Verified-by-walk (P03.1-13 — world-meter anchor closes the architectural noise-anchor axis FOG-11 CI test was inadequate proxy for; the user-observable noise-pattern stability across zoom is now delivered by FOG-18)`.
       - FOG-15: status augmented `Complete — Verified-by-test + walk (P03.1-07 Task 3 unit + P03.1-13 walk-time confirmation post-Plan-03.1-12 world-meter rewrite)`.
       - FOG-16 + FOG-17: status augmented `Complete — Verified-by-test + walk-4 + walk-5 (P03.1-10 unit + P03.1-11 + P03.1-13 walk-time confirmation)`.
       - FOG-18: change `[ ]` → `[x]`; status `Complete — Verified-by-walk (P03.1-12 unit + P03.1-13 walk-time confirmation — Q5 zoom-scramble resolved; cells visibly enlarge with zoom; appliedUOffset JSONL signatures bounded under 1537 raw px confirming FOG-17a in effect; metersPerPixel JSONL signatures present and consistent with camera state confirming FOG-18 in effect)`.
       - UX-02: status augmented `Complete — Verified-by-test + walk-4 + walk-5 (P03.1-10 widget test + P03.1-11 + P03.1-13 walk-time validation)`.
       - PERF-07: `Complete — Re-measured (P03.1-13)`. Include actual Walk #5 numbers.
       - PERF-08: stays falsified-by-walk-2 (no Plan 03.1-12 work targeted it; deferred to post-POC if it becomes a higher-tier concern).
       - PERF-03 / PERF-04 / PERF-05: update statuses to reflect re-measurement at Walk #5.
       - Append a new entry to `## Revisions`:
         ```
         - **2026-05-XX (Phase 3.1 closure, Plan 03.1-13 SUMMARY — HYPOTHESIS CONFIRMED-AFTER-FIX):** Plan 03.1-13 sideload UAT Walk #5 on iPhone 17 Pro against CI run `<RUN_ID>` (SHA `<SHA>`) over central Melun delivered a **CONFIRMED-AFTER-FIX** verdict after the layered Plan 03.1-02+04+05+07+08+10+12 fix bundle. Developer's verbatim words: *"<verbatim>"*. All five Q1-Q5 sub-claims confirmed (Q1-Q4 retained from Walk #4; Q5 NEW closure: noise pattern anchored to ground meters via Plan 03.1-12 FOG-18 world-meter anchor — cells visibly enlarge with zoom; at fixed map position the noise sample is zoom-invariant). FOG-04..07 + FOG-11 + FOG-15 flip from Falsified-in-production back to Complete — Verified-by-walk. FOG-16 + FOG-17 + FOG-18 + UX-02 verified-by-walk in addition to verified-by-test. PERF-07 re-measured. **STRUCTURALLY-ENFORCED Mail-share discipline succeeded** — Walk #5 resume-signal contract required log path parameter; quantitative JSONL grep correlation available (vs Walks #3 + #4 verbal-only). MirkFall migration recommendation: **PORT BACK** with the full 9-architectural-change layered fix bundle. Phase 4 (wisp particles) and Phase 5 (decision gate) **UNBLOCKED**. Evidence: `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md` + `03.1-UAT-5.md`. Phase 3.1 closes with all requirement IDs Complete (FOG-09..18, PERF-07, PERF-08-deferred-post-POC, UX-01, UX-02, DEBUG-01) plus the 4 retroactively-Complete (FOG-04..07).
         ```

    5. Update `.planning/ROADMAP.md`:
       - In `## Progress` table: Phase 03.1 row → `Complete (HYPOTHESIS CONFIRMED-AFTER-FIX)`, plans `13/13`, completed `2026-05-XX`.
       - In Phase 4 + Phase 5 rows: change status from `Blocked on Phase 3.1` to `Pending`.
       - In Phase 3 overview line: append `Phase 3.1 reversed the verdict to CONFIRMED-AFTER-FIX 2026-05-XX after the layered Plan 03.1-02+04+05+07+08+10+12 fix bundle (uniform rename + per-fragment modulo + clip-path shift compensation + B-3 noise-tile-period intermediate fix + canvas.translate symmetric compensation + world-coordinate noise sampling + CPU-side integer/fractional decomposition + rotation disable + WORLD-METER ANCHOR).`
       - In Phase 4 + Phase 5 overview lines: remove the `**BLOCKED on Phase 3.1**` clause.
       - Mark `03.1-13-PLAN.md` row in Phase 03.1 plans list as `[x]` landed.

    6. Update `.planning/STATE.md`:
       - Frontmatter: `stopped_at: Phase 3.1 CLOSED with HYPOTHESIS CONFIRMED-AFTER-FIX (Walk #5 2026-05-XX); Phase 4 + 5 UNBLOCKED.`; `last_activity` accordingly.
       - Bump `progress.completed_phases` from 3 to 4.
       - `## Current Position`: update to reflect Phase 3.1 closure + Phase 4 unblock.
       - `### Decisions`: append a Phase 3.1 closure entry.
       - `### Blockers/Concerns`: change all Phase-3.1-related blockers to `(RESOLVED 2026-05-XX by Plan 03.1-13 Walk #5 CONFIRMED-AFTER-FIX after layered Plan 03.1-12 software fix bundle)`. **Resolve `WALK-MAIL-SHARE-DISCIPLINE`** (Walk #5's structural-enforcement gate succeeded — pattern established for any future POC walks).

    7. **DO NOT TOUCH** Phase 3 + Phase 3.1 Walks #1 + #2 + #3 + #4 + Walk #3b historical records. Verify via `git status` that all are unchanged.

    **Branch B — `denied [log_path]:` → DENIED-FINAL:**

    Mirror Plan 03.1-11 Task 3 Branch B treatment with Walk #5 specifics. Walk Evidence filled with failure-mode evidence + diagnostic-stream pattern matching (including metersPerPixel signature analysis) + (if markers fired) marker-correlation analysis. Verdict captures DENIED-FINAL outcome with MirkFall recommendation `DO NOT PORT BACK` and Phase 3.2 conversation hand-off. REQUIREMENTS.md FOG-04..07 + FOG-11 + FOG-15 stay `Falsified-in-production` with updated revision-clause appending Walk #5 evidence. ROADMAP.md Phase 03.1 row → `Complete (HYPOTHESIS DENIED-FINAL)`. Phases 4 + 5 stay `Blocked` (or transition to `Cancelled` per developer choice). FOG-16 + FOG-17 + FOG-18 + UX-02 still flip to `Complete — Verified-by-test + walk-time validated structurally` (the structural fixes ARE in the codebase and walk-time validated structurally — UX-02 disabled rotation is observable; FOG-18 cell-size enlargement-with-zoom is observable; the open question is whether they're SUFFICIENT for fog-camera-tracking parity at the deepest coordinate-system axis). The Phase 3.2 alternative-renderer pivot conversation opens.

    **Branch C — `iterating [log_path]:` → defer Plan 03.1-14 to follow-up:**

    Mirror Plan 03.1-11 Task 3 Branch C treatment with Walk #5 specifics. Walk Evidence filled with post-walk grep correlation (including metersPerPixel signature analysis) + (if markers fired) marker-correlation analysis. Verdict: `**Outcome:** ITERATING — see Plan 03.1-14+ for the next fix attempt.`. Do NOT update REQUIREMENTS.md / ROADMAP.md / STATE.md to closure state. Mark Phase 3.1 as `In Progress (6th iteration, walk #6 pending)` in Progress table. UX-02 + FOG-16 + FOG-17 + FOG-18 still flip to `Complete — Verified-by-test + walk-time validated structurally` (their CI gates ARE GREEN; structural prevention is observable). FOG-04..07 + FOG-11 + FOG-15 stay `Falsified-in-production`. PERF-07 updated with Walk #5 numbers regardless. Plan 03.1-14 is created via a follow-up `/gsd:plan-phase 3.1 --gaps` invocation. Populate `03.1-UAT-5.md` frontmatter `gaps:` YAML with any opened-by-Walk-#5 gap entries.

    For all three branches: commit all updates via the standard plan-execution commit pattern. Cross-reference Plan 03.1-13 in commit messages. Do NOT commit `.uat-tmp/` (gitignored) or post-walk `.jsonl` extracts (transient walk artefacts).
  </action>
  <verify>
    <automated>flutter test 2>&amp;1 | tail -3 &amp;&amp; flutter analyze --fatal-infos 2>&amp;1 | tail -3 &amp;&amp; node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" frontmatter validate .planning/phases/03.1-fix-fog-pan-translation/03.1-13-PLAN.md --schema plan</automated>
    <file_must_exist>.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md</file_must_exist>
    <file_must_exist>.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-5.md</file_must_exist>
  </verify>
  <done>
    - Resume-signal log path parameter verified — log file readable and contains expected JSONL streams.
    - `03.1-FALSIFICATION-5.md` Walk Evidence + Verdict sections are filled (no `TBD` strings remaining for the chosen branch — Branches A or B; Branch C explicitly leaves them flagged for Plan 03.1-14).
    - `03.1-UAT-5.md` carries the Walk #5 transcript.
    - REQUIREMENTS.md statuses reflect the verdict outcome.
    - ROADMAP.md Progress table Phase 03.1 row reflects the verdict; Phase 4 + Phase 5 statuses updated per branch.
    - STATE.md Current Position + Decisions + Blockers/Concerns updated; WALK-MAIL-SHARE-DISCIPLINE resolved (Branch A) or carried forward (Branch C).
    - Walks #1 + #2 + #3 + #4 + Walk #3b + Phase 3 historical records UNCHANGED (verified via `git status`).
    - `flutter test` (full suite) GREEN — closure docs do not touch any production code.
    - Plan frontmatter validation: `{ valid: true }`.
  </done>
</task>

</tasks>

<verification>
After all three tasks complete:
- `03.1-FALSIFICATION-5.md` is filled top-to-bottom with the Walk #5 verdict and walk evidence (or marked `iterating` per Branch C).
- The Phase 3 historical record (`03-FALSIFICATION.md`) AND Walks #1 + #2 + #3 + #4 historical records (`03.1-FALSIFICATION.md`, `03.1-FALSIFICATION-2.md`, `03.1-FALSIFICATION-3.md`, `03.1-FALSIFICATION-4.md`) AND Walk #3b marker analysis (`03.1-WALK-3B-MARKER-ANALYSIS.md`) are ALL byte-identical to their pre-Plan-03.1-13 states.
- REQUIREMENTS.md, ROADMAP.md, STATE.md all reflect the Walk #5 verdict outcome consistently.
- **STRUCTURALLY-ENFORCED Mail-share discipline succeeded** — Walk #5 resume-signal log path parameter was provided by developer; quantitative JSONL grep correlation available (vs Walks #3 + #4 verbal-only).
- `flutter test` (full suite) is GREEN — Plan 03.1-13 closure docs do not touch any production code (modulo the Task 1 logger augmentation if needed; that's a small one-field addition).
- Plan frontmatter validation returns `{ valid: true }`.
</verification>

<success_criteria>
- The iPhone 17 Pro Walk #5 produces a verdict (verbal `approved [log_path]` / `denied [log_path]:` / `iterating [log_path]:`) — the empirical answer to the Plan 03.1-12 software fix bundle.
- `03.1-FALSIFICATION-5.md` is the load-bearing artefact capturing the Walk #5 verdict + diagnostic-JSONL evidence stream (including metersPerPixel FOG-18 signature) + (if markers fired) marker-correlation analysis.
- On `CONFIRMED-AFTER-FIX`: Phase 4 + Phase 5 unblock; MirkFall migration recommendation flips to PORT BACK with the full 9-change layered fix bundle (including rotation-disabled UX + world-meter anchor); Phase 5 closure conversation can proceed; FOG-04..07 + FOG-11 + FOG-15 + FOG-16 + FOG-17 + FOG-18 + UX-02 + PERF-07 all flip to Complete.
- On `DENIED-FINAL`: the formal POC architectural counter-evidence is complete at the deepest coordinate-system axis (meter-space anchor doesn't deliver fog-camera-tracking parity); Phase 3.2 alternative-renderer conversation is the only remaining option. UX-02 + FOG-16 + FOG-17 + FOG-18 + Plan 03.1-10 + Plan 03.1-12 tests stay Complete (the fix DID land in software); the failure is downstream of the world-meter sampling.
- On `ITERATING`: Plan 03.1-14 is opened in a follow-up invocation with Walk #5's diagnostic stream as input.
- The Phase 3 + Phase 3.1 Walks #1 + #2 + #3 + #4 + Walk #3b historical records stay untouched — historical integrity preserved across iteration cycles.
- **STRUCTURALLY-ENFORCED Mail-share discipline pattern established** — Walk #5's resume-signal log-path-parameter contract eliminates the verbal-only-evidence pattern that Walks #3 + #4 fell into; pattern carries forward to any future POC walks.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-fix-fog-pan-translation/03.1-13-SUMMARY.md` capturing:
- Pre-walk gate sequence outcome (4 local + CI + IPA download)
- Walk #5 metadata (CI run ID, SHA, walk theatre, walk duration, gesture count, ≥ 60-sec one-direction pan duration, two-finger rotate-attempt observation, complete-zoom-traversal observation at fixed map position)
- Developer's verbatim verdict (with log_path parameter)
- Post-walk JSONL grep correlation (per-second composite extract for the ≥ 10-combined-gesture window)
- (If markers fired) marker-correlation analysis mirroring the Walk #3b template
- Walk #5 Verdict outcome (`CONFIRMED-AFTER-FIX` | `DENIED-FINAL` | `ITERATING`)
- Files modified across all three tasks (5 planning docs + optionally 1 production file if FogTransformLogger augmented in Task 1)
- Any deviations from the plan with Rule classification
- Cross-reference to Phase 4 (the unblocked-or-still-blocked downstream phase) + the migration recommendation hand-off to Phase 5
- Comparison vs Walks #1 + #2 + #3 + Walk #3b + #4 numbers — frame_delta headroom preservation (NOW QUANTITATIVE, not implicit), SDF rebuild rate change (PERF-08 status), failure-mode resolution status per Q1-Q5 sub-claim, marker firing count delta vs Walk #3b
- FOG-17a signature confirmation: `appliedUOffsetXMin/Median/Max` JSONL values bounded under 1537 raw px across ALL rollups (RETAINED under FOG-18)
- **NEW: FOG-18 metersPerPixel signature confirmation:** `metersPerPixel` field present in fog_transform rollup AND non-zero AND consistent with synthetic camera state (metersPerPixel ≈ 12.66 at z=13 lat 48.5°; ≈ 3.16 at z=15; ≈ 0.198 at z=19)
- **STRUCTURALLY-ENFORCED Mail-share discipline outcome:** Walk #5 resume-signal log-path-parameter contract succeeded → quantitative JSONL evidence available; pattern established for any future POC walks (recommendation in Phase 5 closure)
</output>
