---
phase: 03.1-fix-fog-pan-translation
plan: 13
type: execute
wave: 2
depends_on: ["03.1-12"]
files_modified:
  - .planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md
  - .planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-5.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
gap_closure: true
scope_note: |
  Plan 03.1-13 = iPhone 17 Pro Walk #5 sideload UAT against the Plan 03.1-12
  fix bundle (FOG-18 modulo elimination + FOG-19 zoom-invariant basis + DEBUG-02
  bounding-box removal). 5th walk in Phase 3.1, 6th iteration overall.

  ## Why a SEPARATE walk plan (mirrors Plans 03.1-03 / 03.1-06 / 03.1-09 / 03.1-11)

  Every Phase 3.1 fix bundle has been followed by a dedicated walk plan that
  authors the next-numbered FALSIFICATION + UAT documents. This separation:
  - Keeps the fix-axis attribution clean (commits in 03.1-12 are pure code;
    commits here are pure verdict authoring).
  - Lets the walk plan run with its own checkpoint:human-verify task without
    blocking the autonomous fix bundle plan.
  - Preserves the CONTEXT §Iteration policy structure: each walk is a
    falsifiable empirical event with a written-before-walk hypothesis +
    written-after-walk verdict, captured as a load-bearing artefact.

  ## Three-task structure (identical to Plan 03.1-11)

  - Task 1 (auto): pre-walk gate sequence + `03.1-FALSIFICATION-5.md` skeleton + `03.1-UAT-5.md` skeleton authoring.
  - Task 2 (checkpoint:human-verify): iPhone 17 Pro sideload UAT Walk #5.
  - Task 3 (auto): post-walk verdict authoring + REQUIREMENTS/ROADMAP/STATE updates + atomic commit.

  ## NEW for Walk #5 (vs Walks #1-#4)

  - **C1' validation regime:** developer pans deliberately at MAX zoom for ≥ 30
    sec to verify Q1 SNAP is gone (Walk #4's 2 markers fired in 2.36 sec at
    47 raw px/s; Walk #5's expected outcome with FOG-18 = zero markers at MAX
    zoom regardless of pan duration).
  - **C2' validation regime:** developer performs ≥ 5 deliberate zoom in/out
    transitions to verify Q1b is gone (Walk #4's "very fast hard step looking
    like changing the seed of a procedural generated image very fast" during
    zoom in/out; Walk #5's expected outcome with FOG-19 = continuous noise
    pattern across zoom transitions).
  - **C3' diagnostic stress-test:** developer pans to extreme world coordinates
    (~100 km from Melun starting point — using the iOS Maps app or a known
    landmark for direction) to verify FOG-18 + FOG-19 don't introduce new
    precision-induced artefacts at high pixelOrigin magnitudes. The
    `cameraConstraint` is removed (DEBUG-02) so this is now possible.
  - **JSONL appliedUOffset semantics shift:** Walk #4 stream showed `uOffsetXMax`
    bounded under 1537 (FOG-17a signature). Walk #5 stream will show
    `uOffsetXMax` tracking `pixelOriginX` magnitude monotonically (FOG-18
    signature: no modulo) AND continuously across zoom transitions
    (FOG-19 signature: no zoom-snap discontinuity). The grep-correlation
    pattern is INVERTED from Walk #4: bounded magnitude was "good" (FOG-17a
    in effect); now monotonic magnitude is "good" (FOG-18 + FOG-19 in effect).
  - **Two-finger rotate gesture validation:** preserved from Walk #4
    (UX-02 walk-time-validation).
  - **Mail-share discipline preserved:** post-walk session log Mail-shared
    so JSONL streams (`infrastructure.mirk.frame_delta` +
    `infrastructure.mirk.sdf` + `infrastructure.mirk.fog_transform`) can be
    grep-correlated against any dev-marker timestamps.

requirements: [FOG-04, FOG-05, FOG-06, FOG-07, FOG-11, FOG-15, FOG-18, FOG-19, DEBUG-02, UX-02, PERF-07]
user_setup:
  - service: github-actions
    why: "Wait for CI green on the post-Plan-03.1-12 SHA + download the IPA artifact for the Walk #5 sideload"
    dashboard_config:
      - task: "Confirm CI run status (`gh run list --branch main --limit 5` + `gh run watch <RUN_ID>`)"
        location: "GitHub Actions for the mirk-poc-debug repo"
      - task: "Download IPA artifact via `gh run download <RUN_ID> --name mirk-poc-debug-ios-unsigned-ipa --dir .uat-tmp/`"
        location: "GitHub Actions run detail page (or `gh` CLI)"
  - service: sidestore
    why: "Sideload the unsigned IPA onto iPhone 17 Pro for Walk #5"
    dashboard_config:
      - task: "Open SideStore on iPhone 17 Pro → install/refresh the .uat-tmp/mirk-poc-debug-unsigned.ipa"
        location: "SideStore iOS app + paired desktop pairing-file"
must_haves:
  truths:
    - "A new `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md` exists capturing the Walk #5 verdict. Walks #1 + #2 + #3 + #4 falsification documents AND the Walk #3b marker analysis are LEFT UNTOUCHED — historical record preserved across iteration cycles."
    - "A new `.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-5.md` exists capturing the Walk #5 transcript. Walks #1 + #2 + #3 + #4 UAT transcripts are LEFT UNTOUCHED."
    - "Pre-walk gate sequence runs cleanly: `flutter test` GREEN (180+ existing + Plan 03.1-12's new tests: FOG-18 rewritten + FOG-19 new file + DEBUG-02 group); `flutter analyze --fatal-infos` 0 warnings; `dart format --line-length 160 --set-exit-if-changed .` clean; `dart test tool/test/` GREEN; CI green on the post-Plan-03.1-12 SHA on `main`; IPA downloaded via `gh run download` to `.uat-tmp/mirk-poc-debug-unsigned.ipa`."
    - "The iPhone 17 Pro UAT Walk #5 is performed by the user. Walk shape matches Plan 03.1-11 (Walk #4) pattern PLUS three new validation regimes: (a) C1' validation — sustained ≥ 30-sec MAX-zoom pan with the dev-marker tapped on any residual SNAP; expected outcome: zero markers (Walk #4 fired 2 in 2.36 sec at MAX zoom). (b) C2' validation — ≥ 5 deliberate zoom in/out transitions with the dev-marker tapped on any residual seed-change-fast symptom; expected outcome: zero markers (Walk #4 surfaced this Q1b symptom on every zoom gesture). (c) C3' diagnostic stress-test — pan ~100 km from Melun starting point (now possible because cameraConstraint is removed) with the dev-marker tapped on any precision-induced artefact at distance; expected outcome: zero markers (verifies FOG-18 + FOG-19 are precision-clean at extreme world coordinates). PLUS the Walk #4 baseline regimes preserved: ≥ 5 minutes through Melun + ≥ 10 deliberate combined pinch-zoom-and-pan gestures + ≥ 3 recenter taps + post-walk Mail-share + pre-walk `/sanity` shader-compile gate (production fog renders + DEBUG-01 toggle works on /sanity AppBar AND /map AppBar) + at least one two-finger rotate gesture attempt (UX-02 walk-time validation: rotation should NOT fire)."
    - "Mail-share discipline preserved — Walk #4 re-established post-walk Mail-share; Walk #5 retains. Session log Mail-shared so JSONL streams (`infrastructure.mirk.frame_delta` + `infrastructure.mirk.sdf` + `infrastructure.mirk.fog_transform`) can be grep-correlated against any dev-marker timestamps. The Walk #3b marker-correlation analysis (`03.1-WALK-3B-MARKER-ANALYSIS.md`) + Walk #4 marker-correlation pattern (`03.1-FALSIFICATION-4.md` §Marker correlation) are the templates."
    - "Post-walk: developer's verbal verdict (`approved` / `denied:` / `iterating:`) is captured. The walk evidence section of `03.1-FALSIFICATION-5.md` is filled with: developer's verbatim words + Q-axis interpretation (Q1 / Q1b / Q2 / Q3 / Q4) + post-walk grep of `infrastructure.mirk.fog_transform` + `infrastructure.mirk.frame_delta` + `infrastructure.mirk.sdf` rollups + per-second composite extract + (if markers fired) marker-correlation analysis mirroring the Walk #4 template."
    - "PERF-07 is re-measured for Walk #5: frame-delta probe rolling rollup over the ≥ 10-combined-gesture window with `medianMs ≤ 16`, `p95Ms ≤ 32`, `maxMs ≤ 48`. Walk #4 captured ~66× / ~6.4× / ~5.0× headroom. Walk #5 should preserve at least Walk #4 levels — the FOG-18 modulo elimination is a NET REDUCTION in arithmetic (one less modulo per paint); the FOG-19 basis derivation adds one `crs.latLngToPoint` call + one `pow` per paint (negligible at painter-level frequency vs the 41-uniform shader population)."
    - "JSONL diagnostic-stream signature shift confirmed: Walk #5's `infrastructure.mirk.fog_transform` rollups show `uOffsetXMax/uOffsetYMax` values that (a) track `pixelOriginX/Y` magnitudes monotonically when zoom is held constant (FOG-18 signature: no modulo bound; values can exceed 1537 at high pixelOrigin) AND (b) evolve continuously across zoom transitions (FOG-19 signature: no O(1M)-raw-px snap at zoom integer boundaries; consecutive-rollup `uOffsetX*` deltas during zoom gesture < `kPocFogSmoothCoordinateMaxDelta` = 2000). The `pixelOriginX/Y` JSONL field still logs the RAW `camera.pixelOrigin` for back-compat with Walks #1-4 grep tooling — the diagnostic-stream pattern is `uOffsetX*` continuous WHILE `pixelOriginX/Y` discontinuous = FOG-19 mechanically working."
    - "Verdict written: `CONFIRMED-AFTER-FIX` if BOTH (a) developer's subjective verdict is `approved` AND (b) PERF-07 thresholds satisfied AND (c) Q1 max-zoom SNAP resolved (zero dev_markers fired during sustained MAX-zoom pan; FOG-18 walk-time-validated) AND (d) Q1b zoom-gesture continuity resolved (zero dev_markers fired during deliberate zoom in/out transitions; FOG-19 walk-time-validated) AND (e) C3' stress-test passes (zero precision-induced artefacts at ~100 km from origin; DEBUG-02 walk-time-validated; FOG-18 + FOG-19 robustness at extreme world coordinates confirmed) AND (f) Q2 rotation absence retained (canvasTx/Ty identically zero in JSONL stream as in Walk #4; UX-02 retained) AND (g) Q3 reveal-hole co-located with blue dot retained AND (h) Q4 fog-translates-with-camera retained. `DENIED-FINAL` if the diagnostic JSONL stream cannot resolve a residual failure mode via a follow-up Phase 3.1 iteration; formal POC architectural counter-evidence; Phases 4 + 5 cancel or reframe. `ITERATING` if a residual failure mode surfaces (e.g., a NEW precision-induced artefact at extreme world coordinates that FOG-18 didn't anticipate; OR a NEW failure mode the C2' lat/lng-anchored basis introduces at exotic projections OR latitudes; OR a residual Q1 / Q1b that the Dart-only fix bundle didn't fully resolve)."
    - "On `CONFIRMED-AFTER-FIX`: ROADMAP.md Phase 03.1 row in the Progress table flips to `Complete (HYPOTHESIS CONFIRMED-AFTER-FIX)`. Phase 4 (`Wisp Particles`) status updates from `Blocked on Phase 3.1` to `Pending`. Phase 5 (`Decision Gate`) likewise. STATE.md `Current Position` updates to reflect Phase 3.1 closure. REQUIREMENTS.md FOG-04..07 + FOG-11 + FOG-15 statuses flip from `Falsified-in-production` to `Complete — Verified-by-walk (P03.1-13 CONFIRMED-AFTER-FIX 2026-05-XX after Plan 03.1-12 Dart-only fix bundle FOG-18 + FOG-19 + DEBUG-02)`. FOG-18 + FOG-19 + DEBUG-02 flip from `Complete — Verified-by-test` to `Complete — Verified-by-test + walk-time validated (P03.1-13)`. PERF-07 status updates to `Complete — Re-measured (P03.1-13)` with the actual numbers. Plan 03-08 DENIED verdict is REVERSED in the Phase 3 row note."
    - "On `DENIED-FINAL`: ROADMAP.md Phase 03.1 row updates to `Complete (HYPOTHESIS DENIED-FINAL)`. Phases 4 + 5 stay `Blocked` (or transition to `Cancelled` per developer choice — flag for user decision; mirror Plan 03.1-11 pattern). REQUIREMENTS.md FOG-04..07 + FOG-11 + FOG-15 stay `Falsified-in-production` with updated revision-clause that points at `03.1-FALSIFICATION-5.md`. FOG-18 + FOG-19 + DEBUG-02 still flip to `Complete — Verified-by-test + walk-time validated` (their CI gates ARE GREEN by Plan 03.1-12 construction; the structural fixes ARE in the codebase; the open question is whether they're SUFFICIENT). UX-02 + FOG-16 + FOG-17 retain Walk #4's walk-time-validated status (assuming they pass Walk #5 too — re-validate). STATE.md captures the formal POC architectural counter-evidence."
    - "On `ITERATING`: a Plan 03.1-14+ is opened in a follow-up plan-phase invocation, with Walk #5's diagnostic stream as input. THIS plan does NOT author 03.1-14. FOG-18 + FOG-19 + DEBUG-02 still flip to `Complete — Verified-by-test + walk-time validated` (the structural fixes ARE in the codebase; the open question is whether they're SUFFICIENT). PERF-07 updated with Walk #5 numbers regardless. FOG-04..07 + FOG-11 + FOG-15 stay `Falsified-in-production`. Phase 3.1 stays `In Progress (6th iteration)`."
    - "Phase 3 `03-FALSIFICATION.md` AND Phase 3.1 Walks #1 + #2 + #3 + #4 falsification documents AND `03.1-WALK-3B-MARKER-ANALYSIS.md` are LEFT UNTOUCHED — no edits, no overwrites. Historical records preserved across iteration cycles."
  artifacts:
    - path: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md"
      provides: "Phase 3.1 Walk #5 falsification document — pre-walk gate-pass evidence + post-walk verdict (`CONFIRMED-AFTER-FIX` | `DENIED-FINAL` | `ITERATING`)"
      min_lines: 100
      contains: "Walk #5"
    - path: ".planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-5.md"
      provides: "Walk #5 evidence transcript — developer's verbatim verdict + Q-axis interpretation + JSONL grep correlation + (if markers fired) marker-correlation analysis"
      min_lines: 40
      contains: "Walk Evidence"
  key_links:
    - from: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md"
      to: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-4.md"
      via: "explicit cross-reference in Walk #5's `## Relationship to Walk #4 Verdict` section ('Walk #4 closed iterating-with-major-progress 2026-05-04; Walk #5 either confirms or denies the Plan 03.1-12 Dart-only fix bundle: FOG-18 modulo elimination + FOG-19 zoom-invariant basis + DEBUG-02 bounding-box removal')"
      pattern: "03\\.1-FALSIFICATION-4\\.md"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md"
      via: "FOG-04..07 + FOG-11 + FOG-15 + FOG-18 + FOG-19 + DEBUG-02 + UX-02 + PERF-07 status clauses include `(P03.1-13)` cross-reference; Revisions section appends a 2026-05-XX Walk #5 entry"
      pattern: "P03\\.1-13"
    - from: ".planning/STATE.md"
      to: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md"
      via: "Current Position + Decisions sections capture the Walk #5 verdict outcome"
      pattern: "Phase 3\\.1"
    - from: ".planning/ROADMAP.md"
      to: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md"
      via: "Phase 03.1 row + Plan 03.1-13 row updated with Walk #5 verdict; on CONFIRMED-AFTER-FIX, Phase 3 status note updated to reflect verdict reversal"
      pattern: "Walk #5"
---

<objective>
Run the pre-walk gates, perform the iPhone 17 Pro sideload UAT Walk #5, capture the developer's verdict, and produce `03.1-FALSIFICATION-5.md` — the Phase 3.1 Walk #5 falsification document. Walk #5 is the empirical re-test for the Plan 03.1-12 Dart-only fix bundle:

- **C1' / FOG-18** — eliminate FOG-17a integer-wrap-modulo (forward `camera.pixelOrigin` directly; Walk #4 debug-spiral positive-control proved the wrap was the bug, not the precision penalty it was designed to address).
- **C2' / FOG-19** — replace `camera.pixelOrigin` with zoom-invariant lat/lng-anchored basis (`worldPxAtRefZoom * pow(2, zoom - refZoom)`); resolves Q1b zoom-gesture-correlated "very fast hard step looking like changing the seed of a procedural generated image very fast" failure mode.
- **C3' / DEBUG-02** — remove `MapOptions.cameraConstraint`; lets developer pan to extreme world coordinates (~100 km from Melun) for stress-test diagnostic.

The Walk #5 verdict either:
- Reverses Walks #1-#4's `ITERATING` outcome to `CONFIRMED-AFTER-FIX` (unblocking Phase 4 + Phase 5 of the original roadmap; reverses the Plan 03-08 `HYPOTHESIS DENIED` verdict).
- Strengthens to `DENIED-FINAL` (terminating the POC's same-Canvas hypothesis; opens Phase 3.2 alternative-renderer pivot conversation).
- Captures a sixth `ITERATING` verdict that opens Plan 03.1-14+ (per CONTEXT §Iteration policy: no hard cap on walk count).

Purpose: this is the empirical gate Phase 3.1 needs to close after the Plan 03.1-12 Dart-only fix bundle. Plan 03.1-12 software-completed the corrected (MIRL-compatible) candidate set surfaced by Walk #4's debug-spiral diagnostic + post-walk dialogue. Walk #5 is the architectural-evidence gate that flips FOG-04..07 + FOG-11 + FOG-15 to Complete (or strengthens the verdict).

Output: 1 new `03.1-FALSIFICATION-5.md` (the load-bearing artefact); 1 new `03.1-UAT-5.md` (the walk-evidence transcript); REQUIREMENTS.md status updates for FOG-04..07 + FOG-11 + FOG-15 + FOG-18 + FOG-19 + DEBUG-02 + UX-02 + PERF-07; ROADMAP.md Phase 03.1 Progress-table row update; STATE.md Current Position + Decisions updates. Phase 3's `03-FALSIFICATION.md` AND Phase 3.1 Walks #1 + #2 + #3 + #4 records AND `03.1-WALK-3B-MARKER-ANALYSIS.md` ALL left UNTOUCHED.

### MIRL constraint compliance (HARD — see CLAUDE.md `# MIRL solution`)

The Plan 03.1-12 fix bundle that Walk #5 validates is Dart-only — zero shader source modifications. The shader ABI (41 float slots; slot 3..4 = `uPixelOrigin` semantics) is preserved bit-for-bit. Both `atmospheric_fog.frag` AND `atmospheric_fog_debug_spiral.frag` inherit the fix automatically. Walk #5's debug-spiral toggle observation is now a SECOND positive-control regime: if FOG-19 works, the debug-spiral cell numbers should track world-pixel coordinates continuously across zoom transitions (Walk #4's "numbers moving all around, it seems it's just not scaled correctly while zooming" symptom should be GONE).

### Iteration policy (carried forward — unchanged)

Plan-revise-walk loop. **No hard cap on walk count.** Walk #5 is the 5th attempt under Phase 3.1 (Walk #1 P03.1-03 + Walk #2 P03.1-06 + Walk #3 P03.1-09 + Walk #3b implicit marker analysis + Walk #4 P03.1-11 + Walk #5 here). Each prior walk iterated the fix-axis hierarchy (constant-zero → viewport-width-period shimmer → noise-tile-period stepping → rotation-correlated rect-cover → world-coord noise + UX-02 rotation disable + FOG-17a precision pairing → FOG-18 modulo elimination + FOG-19 zoom-invariant basis + DEBUG-02 stress-test); Walk #5 closes against the Plan 03.1-12 bundle.
</objective>

<execution_context>
@C:/Users/oliver/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oliver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-CONTEXT.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-2.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-3.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-4.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-2.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-3.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-4.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-WALK-3B-MARKER-ANALYSIS.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-VALIDATION.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-11-PLAN.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-11-SUMMARY.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-12-PLAN.md

<interfaces>
<!-- Walk #5 mirrors Walk #4 (Plan 03.1-11) shape-for-shape with adjusted hypothesis re-statements. -->

`03.1-FALSIFICATION-4.md` Walk #4 structure (the immediate predecessor template):
1. Title + metadata (`Created`, `Phase`, `Walked on`, `Verdict`)
2. `## Relationship to Walk #3 Verdict` (Walk #5 inserts `## Relationship to Walk #4 Verdict`)
3. `## Hypothesis`
4. `## Falsification Criteria (written BEFORE the walk)` — `### Criterion A`, `### Criterion B`, `### Criterion C — DROPPED`, `### Criterion D — Diagnostic stream`
5. `## Walk Plan`
6. `## Walk Evidence (filled AFTER the walk)`
7. `## Verdict`
8. (Walk #4 specific) `## Supplementary Walk Evidence` — only included if Walk #5 surfaces a debug-spiral correction or post-walk dialogue clarification

Walk #5's `03.1-FALSIFICATION-5.md` mirrors Walk #4's structure exactly with these specialisations:
- Title: `# Phase 3.1 Falsification — Walk #5 (Post-Plan-03.1-12 Dart-Only Fix Bundle: FOG-18 modulo elimination + FOG-19 zoom-invariant basis + DEBUG-02 bounding-box removal)`
- Add `## Relationship to Walk #4 Verdict` section pointing at `03.1-FALSIFICATION-4.md`; summarise the Walk #4 ITERATING-WITH-MAJOR-PROGRESS outcome and the three Plan 03.1-12+ candidate axes that Plan 03.1-12 implemented.
- Hypothesis re-statement: "After the Plan 03.1-12 Dart-only fix bundle (FOG-18 eliminating the FOG-17a integer-wrap-modulo + FOG-19 replacing `camera.pixelOrigin` with a zoom-invariant lat/lng-anchored basis + DEBUG-02 removing `MapOptions.cameraConstraint` for stress-test diagnostic), the same-Canvas fog now: (a) translates with the camera during pan at all zoom levels including MAX (FOG-18 eliminates the SNAP wrap event); (b) maintains a fully stable noise pattern across zoom transitions (FOG-19 produces a continuous basis across zoom integer levels); (c) keeps the reveal hole co-located with the blue dot during pan/zoom (FOG-12 retained); (d) covers the entire viewport at any canvas-translation magnitude (FOG-13 retained); (e) is structurally invulnerable to the rotation-correlated fog mis-coverage failure mode (UX-02 retained); (f) maintains correctness at extreme world coordinates ~100 km from origin (FOG-18 + FOG-19 are precision-clean within fp32's 16.7M raw-px exact-integer mantissa ceiling)."
- Criterion A: same thresholds (PERF-07 carry-over): `medianMs ≤ 16`, `p95Ms ≤ 32`, `maxMs ≤ 48`. Walk #4: ~66× / ~6.4× / ~5.0× headroom; Walk #5 should preserve at least Walk #4 levels (FOG-18 reduces arithmetic by one modulo; FOG-19 adds one `crs.latLngToPoint` + `pow` per paint, negligible).
- Criterion B sub-claims:
  - **Q1**: "No perceived hard-step events during pure pan at ANY zoom level" — Walk #4 had Q1 PARTIAL (resolved at default zoom, residual at MAX zoom). Plan 03.1-12 FOG-18 targets the residual MAX-zoom case. Sub-test: developer pans for ≥ 30 sec at MAX zoom in one direction; expected outcome: zero dev_markers fired (Walk #4 fired 2 markers in 2.36 sec at MAX zoom).
  - **Q1b**: "No zoom-gesture-correlated very-fast hard-step (seed-of-procedural-pattern-fast)" — Walk #4 NEW failure; Plan 03.1-12 FOG-19 targets this. Sub-test: developer performs ≥ 5 deliberate zoom in/out transitions; expected outcome: zero dev_markers fired (Walk #4 surfaced this on every zoom gesture).
  - **Q2**: "No rotation-correlated fog mis-coverage" — Walk #4 RESOLVED via UX-02; re-confirm in Walk #5 (UX-02 not modified by Plan 03.1-12).
  - **Q3**: "No reveal-hole offset from blue dot during gesture" — Walks #1 + #2 + #3 + #4 retained; re-confirmation under Plan 03.1-12's coordinate-system rewrite (FOG-12 + FOG-13 unchanged but the underlying coordinate basis is different — verify no regression).
  - **Q4**: "Fog visually translates with camera during pan" — Walks #1 + #2 + #3 + #4 retained; re-confirmation; FOG-19 rewrites the translation path so a regression here would mean the basis derivation is broken.
  - **Q5 NEW (DEBUG-02 stress-test)**: "No precision-induced artefacts at extreme world coordinates (~100 km from Melun)". Sub-test: developer pans far from initial Melun bbox (now possible since DEBUG-02 removed cameraConstraint); expected outcome: fog renders correctly + zero markers + no visual anomalies.
  - No white-ellipse during fast pinch-zoom (already passed Walks #1-4)
  - No inversion at any zoom level (already passed Walks #1-4)
- Criterion C stays DROPPED.
- Criterion D (Diagnostic stream): `pixelOriginX/Y` continues to log the RAW `camera.pixelOrigin` (back-compat with Walks #1-4 grep tooling). `appliedUOffset` keys (preserved JSONL field name) now log the FOG-19 basis-derived value. **Signature shift from Walk #4:**
  - Walk #4: `uOffsetXMax` bounded under 1537 across all rollups (FOG-17a in effect).
  - Walk #5: `uOffsetXMax` tracks `pixelOriginX` magnitude monotonically when zoom held (FOG-18 in effect: no modulo bound) AND evolves continuously across zoom transitions (FOG-19 in effect: no zoom-snap discontinuity; consecutive-rollup `uOffsetX*` deltas during zoom gesture < `kPocFogSmoothCoordinateMaxDelta` = 2000).
  - **Pattern check:** during a sustained MAX-zoom pan in Walk #5's rollups, `uOffsetX*` values may exceed 1537 (Walk #4 max was 1535.74); during a zoom-out gesture in Walk #5's rollups, `uOffsetX*` deltas across consecutive rollups should NOT show O(1M) jumps (Walk #4's rollups 20-24 captured `pixelOriginX` 4.10M → 0.81M sweep — Walk #5's `uOffsetX*` should be smooth across the same gesture).
  - `canvasTx/Ty` continues to be identically zero (UX-02 retained).
  - `canvasRotation` field NOT added (UX-02 makes rotation moot).

`03.1-UAT-5.md` mirrors `03.1-UAT-4.md` shape-for-shape: metadata block + walk protocol + walk evidence (frame-delta + SDF + fog-transform rollups) + subjective verdict + verdict + deviations + summary + (optional) gaps section if Walk #5 surfaces residual failure modes + `gaps:` YAML in frontmatter for any opened-by-Walk-#5 gap entries.

The pre-walk gate sequence is identical to Plan 03.1-11 Task 1 (six gates: flutter test / analyze / format / tool tests / CI green / IPA download). Plan 03.1-11 Task 1 ran the gate sequence at SHA `b31766f`; Plan 03.1-13 Task 1 runs against the post-Plan-03.1-12 SHA (let `gh run list --branch main --limit 5` resolve at execute-phase time).

The walk shape is identical to Plan 03.1-11 PLUS the C1' / C2' / C3' validation regimes enumerated in `<scope_note>` §"NEW for Walk #5". Dev-marker workflow unchanged: tap on any residual symptom; tag `"steppy_translation"` (hardcoded since Plan 03.1-09); post-hoc disambiguation via timing.
</interfaces>

<out_of_scope>
The following items are EXPLICITLY out of scope for Plan 03.1-13:

1. **Authoring Plan 03.1-14+.** If Walk #5 returns `ITERATING`, the next plan is opened in a follow-up `/gsd:plan-phase 3.1 --gaps` invocation that consumes Walk #5's diagnostic stream as input. THIS plan does NOT author 03.1-14+.

2. **Re-enabling cameraConstraint.** DEBUG-02 (Plan 03.1-12 Task 3) removed it; Walk #5 validates it stays out for the stress-test diagnostic. Re-enabling for production UX is a Phase 5 hardening concern.

3. **MIRK-01 ABI formalization, MIRK-01 acceptance test trio, ABI uniform rename `uPixelOrigin` → `uWorldOffset`, JSONL field rename `uOffsetX*` → `uWorldOffsetX*`.** All DEFERRED per 03.1-CONTEXT.md `<deferred>` block. Walk #5 outcome may inform whether to revisit (CONFIRMED-AFTER-FIX → can revisit at leisure during Phase 5 hardening; ITERATING → defer further).

4. **PERF-08 SDF-cache-thrash addressing.** Same status as Walks #1-4 — deferred. Re-measured for context only.

5. **Pixel 4a parity walk.** Stays in Phase 5 per the original roadmap.

6. **Modifying shader source.** MIRL constraint. The Plan 03.1-12 fix bundle is Dart-only; Walk #5 validates that bundle without touching shaders.

7. **Re-running Walks #1-#4.** Historical records preserved across iteration cycles; this plan does NOT re-author or modify any prior walk's FALSIFICATION or UAT documents.
</out_of_scope>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run pre-walk gate sequence + author 03.1-FALSIFICATION-5.md skeleton + 03.1-UAT-5.md skeleton</name>
  <files>
    .planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md,
    .planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-5.md
  </files>
  <action>
    Pre-walk gate sequence + skeleton authoring. Mirror Plan 03.1-11 Task 1 structure verbatim.

    **A) Pre-walk gates (six gates, all must pass):**

    1. `flutter test` — full suite GREEN (180+ tests; expected post-Plan-03.1-12: rewritten `fog_pixel_origin_decomposition_test.dart` as FOG-18 + new `fog_world_coordinate_basis_test.dart` for FOG-19 + extended `map_screen_test.dart` for DEBUG-02). Capture `flutter test --reporter=compact` output; record total tests passed in the FALSIFICATION-5 skeleton.
    2. `flutter analyze --fatal-infos` — 0 warnings.
    3. `dart format --line-length 160 --set-exit-if-changed .` — 0 files changed.
    4. `dart test tool/test/` — 18 passed.
    5. CI green check via `gh run list --branch main --limit 5`. The post-Plan-03.1-12 SHA must show `success` for the iOS build job (`Build iOS (no-codesign, sideloadable)`). Capture the run ID + SHA in the skeleton metadata.
    6. IPA artifact download: `gh run download <RUN_ID> --name mirk-poc-debug-ios-unsigned-ipa --dir .uat-tmp/`. Verify `.uat-tmp/mirk-poc-debug-unsigned.ipa` exists and is ~11 MB.

    Each gate's pass/fail status + relevant output captured in `03.1-FALSIFICATION-5.md` §"Pre-walk gate sequence" subsection.

    **B) `03.1-FALSIFICATION-5.md` skeleton authoring:**

    Mirror `03.1-FALSIFICATION-4.md` structure:
    - Title: `# Phase 3.1 Falsification — Walk #5 (Post-Plan-03.1-12 Dart-Only Fix Bundle: FOG-18 modulo elimination + FOG-19 zoom-invariant basis + DEBUG-02 bounding-box removal)`
    - `**Created:**` (today's date)
    - `**Phase:**` 03.1-fix-fog-pan-translation
    - `**Walked on:**` (PLACEHOLDER — filled by Task 3 post-walk)
    - `**Verdict:**` PENDING
    - `## Relationship to Walk #4 Verdict` — narrative summary of Walk #4's ITERATING-WITH-MAJOR-PROGRESS outcome + the three Plan 03.1-12 candidate axes (C1' FOG-18 / C2' FOG-19 / C3' DEBUG-02) that this walk validates. Cite `03.1-FALSIFICATION-4.md` §Supplementary Walk Evidence — Debug-Spiral Diagnostic + Mechanism Correction as the corrected analysis input.
    - `## Hypothesis` — full re-statement per `<interfaces>` `Hypothesis re-statement` paragraph.
    - `## Falsification Criteria (written BEFORE the walk)` — write Criteria A / B / C-DROPPED / D in full per `<interfaces>` enumeration. Q1 / Q1b / Q2 / Q3 / Q4 / Q5 NEW sub-claims under Criterion B with explicit sub-tests.
    - `## Walk Plan` — mirror Walk #4's walk plan with the C1' / C2' / C3' validation regimes added. Pre-walk gate evidence captured in `### Pre-walk gate sequence` subsection (filled by this Task 1).
    - `## Walk Evidence (filled AFTER the walk by Plan 03.1-13 Task 3)` — empty placeholder section.
    - `## Verdict` — empty placeholder section.

    **C) `03.1-UAT-5.md` skeleton authoring:**

    Mirror `03.1-UAT-4.md` structure:
    - Frontmatter with `gaps:` YAML block (initially empty — populated by Task 3 if Walk #5 surfaces residual failure modes).
    - Walk #5 metadata block (placeholder for CI run, SHA, walk theatre, duration, gestures — filled by Task 3).
    - Walk Evidence section (placeholder).
    - Subjective verdict block (placeholder for developer's verbatim words).
    - Verdict + deviations + summary placeholders.

    **D) Atomic commit:**

    Single atomic commit at the end of Task 1:
    ```
    docs(03.1-13): pre-walk gates green + Walk #5 falsification + UAT skeletons (Task 1)

    Six pre-walk gates GREEN at SHA <sha>: flutter test (N tests passed),
    analyze 0 warnings, dart format clean, tool tests 18 passed, CI run
    <run_id> success, IPA downloaded.

    Walk #5 falsification skeleton authored at
    .planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md
    with hypothesis + criteria written BEFORE the walk per Phase 3.1
    falsification discipline. UAT skeleton at 03.1-UAT-5.md.

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    ```
  </action>
  <verify>
    <automated>flutter test &amp;&amp; flutter analyze --fatal-infos &amp;&amp; dart format --line-length 160 --set-exit-if-changed . &amp;&amp; dart test tool/test/</automated>
  </verify>
  <done>
    - All six pre-walk gates pass (test + analyze + format + tool tests + CI + IPA download).
    - `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md` exists with hypothesis + falsification criteria written; ≥ 100 lines.
    - `.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-5.md` exists as an empty-but-structured skeleton with `gaps:` frontmatter block.
    - One atomic commit `docs(03.1-13): pre-walk gates green + Walk #5 ...` lands on `main`.
    - `.uat-tmp/mirk-poc-debug-unsigned.ipa` exists and is ~11 MB.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: iPhone 17 Pro sideload UAT Walk #5</name>
  <files>.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md, .planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-5.md</files>
  <action>
    Sideload the unsigned IPA on iPhone 17 Pro via SideStore; perform Walk #5 per the protocol below; capture verbatim verdict + Mail-share session log post-walk so Task 3 can grep-correlate JSONL streams. The full walk protocol with C1' / C2' / C3' validation regimes is enumerated in the <how-to-verify> block below; this <action> block exists for plan-checker schema completeness — execute the <how-to-verify> protocol verbatim.
  </action>
  <verify>
    <automated>MISSING — PERF-07 + Criterion B + Criterion D (subjective + diagnostic-stream pattern matching + dev-marker correlation if markers fire) are inherently manual UAT walk gates per 03.1-VALIDATION.md Manual-Only Verifications table; no automated proxy exists. The developer's resume-signal IS the verification.</automated>
  </verify>
  <done>
    Walk #5 complete + developer's verbal verdict captured (`approved` / `denied:` / `iterating:`); session log Mail-shared so Task 3 can extract JSONL streams. The verdict outcome drives Task 3's branch (CONFIRMED-AFTER-FIX / DENIED-FINAL / ITERATING).
  </done>
  <what-built>
    Plan 03.1-12 Dart-only fix bundle landed on `main` and CI-green-gated:
    - **C1' / FOG-18** — FOG-17a integer-wrap-modulo eliminated; `camera.pixelOrigin` forwarded directly (no modulo bound).
    - **C2' / FOG-19** — `camera.pixelOrigin` replaced by zoom-invariant lat/lng-anchored basis at the Dart call site; both shaders inherit the fix automatically (zero shader source edits).
    - **C3' / DEBUG-02** — `MapOptions.cameraConstraint` removed; developer can now pan to extreme world coordinates (~100 km from Melun) for stress-test diagnostic.
    - All three fixes are Dart-only (MIRL constraint satisfied).
    - Pre-walk gate sequence GREEN at the post-Plan-03.1-12 SHA.
    - IPA downloaded to `.uat-tmp/mirk-poc-debug-unsigned.ipa`.
  </what-built>
  <how-to-verify>
    Walk shape mirrors Walks #1-4 with three new validation regimes for C1' / C2' / C3'.

    1. **Sideload + boot:**
       - Open SideStore on iPhone 17 Pro → install/refresh `.uat-tmp/mirk-poc-debug-unsigned.ipa`.
       - Launch the app. Permission gate → grant location.
    2. **Pre-walk shader-sanity gate (`/sanity` route):**
       - Navigate to `/sanity` via the app bar's debug menu (or the implicit route per Plan 03.1-08).
       - Confirm production fog renders with circular reveal hole (debug-spiral toggle OFF default).
       - Toggle the debug-spiral switch ON. Confirm the digit-atlas spiral pattern renders with visible numbered cells (DEBUG-01 walk-time validation; cell numbering reflects FOG-19 basis derivation — numbers should track world-pixel coordinates continuously).
       - Toggle OFF — confirm production fog returns.
       - Confirm the AppBar back button still works on `/sanity` (UX-01 retained).
       - Confirm zero shader-compile exceptions in the FileLogger output (check via `Share Logs` → Mail to self if needed for direct inspection).
       - Attempt a two-finger rotate gesture. Confirm map does NOT rotate (UX-02 retained).
    3. **Walk #5 main protocol (≥ 5 minutes through Melun):**
       - Walk through Melun for ≥ 5 minutes with the IPA running and the device awake.
       - Perform ≥ 10 deliberate combined pinch-zoom-and-pan gestures.
       - Perform ≥ 3 recenter FAB taps.
       - At least one deliberate two-finger rotate-attempt gesture (UX-02 walk-time validation).
       - **NEW: tap the dev-marker button every time you observe ANY residual symptom** (steppy translation, fog mis-coverage, anything visually anomalous). Each marker tap produces a per-frame JSONL event tagged with the marker timestamp.
    4. **C1' validation regime — sustained MAX-zoom pan:**
       - Pinch-zoom to maximum zoom (z=15 or `kPocMaxZoom`).
       - Pan in one direction continuously for ≥ 30 sec.
       - Tap the dev-marker on any perceived hard-step / SNAP event.
       - Expected outcome: zero markers fired during this regime (Walk #4 fired 2 markers in 2.36 sec at MAX zoom).
    5. **C2' validation regime — deliberate zoom in/out transitions:**
       - From any starting zoom, perform ≥ 5 deliberate zoom in/out transitions.
       - Tap the dev-marker on any perceived "very fast hard step looking like changing the seed of a procedural generated image very fast" symptom.
       - Expected outcome: zero markers fired during this regime (Walk #4 surfaced this on every zoom gesture).
    6. **C3' diagnostic stress-test — pan to ~100 km from Melun:**
       - Pan continuously in one direction for an extended period (the cameraConstraint is removed — flutter_map will not bounce you back to the bbox).
       - Goal: reach a position roughly 100 km from initial Melun centre. Use approximate landmark / direction (e.g., "pan east towards Provins" or "pan south towards Fontainebleau" but keep panning past the city).
       - Tap the dev-marker on any precision-induced artefact at distance (fog disappearing, fog rendering at wrong scale, fog jittering, anything visually anomalous).
       - Expected outcome: fog renders correctly throughout the long pan; zero markers fired.
    7. **Post-walk Mail-share:**
       - Tap the AppBar `Share Logs` button.
       - Pick Mail → send to yourself.
       - Confirm the session log file (`yyyymmdd_hhmmss_logs.txt`) arrives in inbox.
    8. **Verbal verdict:**
       - Report verdict to Claude in plain English. Use one of three verdict shapes:
         - `approved` (or `confirmed-after-fix`) — fog locks to map at all zoom levels including MAX; zoom transitions show continuous noise pattern; ~100 km pan stress-test passes; subjective verdict positive.
         - `denied: <reasoning>` — fog still fails at user-observable level despite Plan 03.1-12 fix bundle; provide diagnostic context.
         - `iterating: <reasoning>` — partial progress with specific residual failure mode(s) Plan 03.1-14+ should address.
       - Include verbatim words wherever possible (these become Walk #5's Q-axis interpretation source material).
  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - `approved` (with optional verbatim verdict comments) — Walk #5 passes; Plan 03.1-12 bundle delivers Phase 3.1 closure; Task 3 authors `CONFIRMED-AFTER-FIX` verdict.
    - `denied: <reasoning>` — Walk #5 fails at the architectural level; Task 3 authors `DENIED-FINAL` verdict; Phases 4 + 5 stay BLOCKED or transition to Cancelled per developer choice.
    - `iterating: <reasoning>` — Walk #5 surfaces residual failure mode(s); Task 3 authors `ITERATING` verdict with the surfaced gaps captured in `gaps:` frontmatter; Plan 03.1-14+ pending in follow-up plan-phase invocation.

    Mail-share the session log to yourself and reference the timestamp + filename so Task 3 can extract JSONL streams for the post-walk grep correlation.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Walk #5 verdict authoring + REQUIREMENTS / ROADMAP / STATE updates</name>
  <files>
    .planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md,
    .planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-5.md,
    .planning/REQUIREMENTS.md,
    .planning/ROADMAP.md,
    .planning/STATE.md
  </files>
  <action>
    Author the Walk #5 verdict + propagate to REQUIREMENTS/ROADMAP/STATE. Mirror Plan 03.1-11 Task 3 verbatim with verdict-branch specialization.

    **A) Extract JSONL streams from Mail-shared session log:**

    Mail-shared session log filename: `<yyyymmddTHHMMSSZ>_logs.txt` (per FileLogger naming).

    Extract the four JSONL streams to `.uat-tmp/walk5_*.jsonl`:
    - `grep -F 'infrastructure.mirk.frame_delta' <log_file> > .uat-tmp/walk5_frame_delta.jsonl`
    - `grep -F 'infrastructure.mirk.fog_transform' <log_file> > .uat-tmp/walk5_fog_transform.jsonl`
    - `grep -F 'infrastructure.mirk.sdf' <log_file> > .uat-tmp/walk5_sdf_rollups.jsonl`
    - `grep -F '"event":"dev_marker"' <log_file> > .uat-tmp/walk5_dev_markers.jsonl` (or whatever the dev-marker event tag is — check Plan 03.1-09 marker implementation)

    `.uat-tmp/walk5_*.jsonl` are transient (not committed; .gitignore'd already per Plan 03.1-09 pattern).

    **B) `03.1-FALSIFICATION-5.md` Walk Evidence section:**

    Fill the empty placeholders from Task 1. Mirror `03.1-FALSIFICATION-4.md` §Walk Evidence structure:
    - `### Pre-walk shader-sanity gate (...)` — confirm pre-walk gates passed (carry from Task 1) + UX-02 walk-time-validation (canvasTx/Ty identically zero in Walk #5 fog_transform rollups).
    - `### Probe rollup (frame-delta JSONL)` — per-rollup distribution table with `medianMs / p95Ms / maxMs` Min / Max / Median-of-rollups; PERF-07 verdict + headroom comparison vs Walk #4.
    - `### SDF rebuild rollup` — `rebuildCount/sec` distribution; PERF-08 status (deferred per Walks #1-4 pattern).
    - `### Fog-transform rollup` — KEY FOG-18 + FOG-19 SIGNATURES:
      - `canvasTx/Ty` identically zero (UX-02 retained).
      - `pixelOriginX/Y` trajectory across regimes (default-zoom + max-zoom + zoom-gesture + ~100 km stress-test).
      - **FOG-18 SIGNATURE**: `uOffsetXMax/uOffsetYMax` track `pixelOriginX/Y` magnitude monotonically (no longer bounded under 1537 — values can exceed 1537 at high pixelOrigin). Pattern check: at MAX zoom + sustained pan, `uOffsetX*` grows linearly with raw px panned (Walk #4: bounded under 1537; Walk #5: tracks raw magnitude).
      - **FOG-19 SIGNATURE**: `uOffsetX*` deltas across consecutive rollups during zoom gestures should NOT show O(1M) jumps (Walk #4 rollups 20-24 captured `pixelOriginX` 4.10M → 0.81M sweep; Walk #5's `uOffsetX*` should be smooth across the same gesture). Pattern check: zoom in/out window's consecutive `uOffsetXMax` deltas < `kPocFogSmoothCoordinateMaxDelta` = 2000.
    - `### FPS observations (PERF-03)` — coarse FpsCounterOverlay reading.
    - `### Marker correlation (Walk #4 template applied)` — IF markers fired:
      - Marker count + epochSec timestamps + correlation against fog_transform rollup at the same epochSec.
      - Per-marker zoom regime classification: was it MAX-zoom-pan (Q1 residual)? zoom-transition (Q1b residual)? extreme-distance (Q5 NEW)? other?
      - Mechanism analysis: which Q-axis sub-claim falsifies if markers fired in a specific regime.
    - `### Subjective verdict (Criterion B)` — developer's verbatim words (from Task 2 verbal verdict + any Mail-shared annotations) + Q-axis interpretation:
      - Q1: status (RESOLVED / PARTIAL / FAILED / NEW)
      - Q1b: status (RESOLVED / PARTIAL / FAILED / NEW)
      - Q2: status (RETAINED / FAILED — should be RETAINED unless something exotic surfaces)
      - Q3: status (RETAINED / FAILED)
      - Q4: status (RETAINED / FAILED)
      - Q5 NEW (DEBUG-02 stress-test): status (PASSED / FAILED / NEW failure mode at distance)

    **C) `03.1-FALSIFICATION-5.md` Verdict section — branch on subjective verdict:**

    Three branches per verdict signal from Task 2:

    **Branch A — `approved` / CONFIRMED-AFTER-FIX:**
    - Verdict: `**CONFIRMED-AFTER-FIX**` (6th Phase 3.1 verdict; reverses Plan 03-08's HYPOTHESIS DENIED).
    - All Q-axis sub-claims RESOLVED or RETAINED.
    - Plan 03.1-12 Dart-only fix bundle empirically validated.
    - Phase 3.1 closes; Phase 4 + Phase 5 unblock.
    - MirkFall migration recommendation: **PORT BACK** (with all Phase 3.1 fixes included: 03.1-02 + 03.1-04 + 03.1-05 + 03.1-07 + 03.1-08 + 03.1-10 + 03.1-12).

    **Branch B — `denied:` / DENIED-FINAL:**
    - Verdict: `**DENIED-FINAL**` (6th Phase 3.1 verdict; strengthens Plan 03-08).
    - Architectural counter-evidence: even the Dart-only fix bundle (FOG-18 modulo elimination + FOG-19 zoom-invariant basis) is insufficient for the same-Canvas hypothesis.
    - Capture the residual mechanism + suggest pivot direction (Phase 3.2 alternative renderer? port-back-with-different-architecture? POC closure?).
    - MirkFall migration recommendation: **DO NOT PORT BACK as-implemented.**

    **Branch C — `iterating:` / ITERATING:**
    - Verdict: `**ITERATING-WITH-...** (6th Phase 3.1 verdict).
    - Capture residual failure mode(s) + Plan 03.1-14+ candidate axes.
    - Per CONTEXT §Iteration policy: no hard cap on walk count.
    - Plan 03.1-14 pending in follow-up plan-phase invocation.

    **D) `03.1-UAT-5.md` finalisation:**

    - Walk metadata block: CI run ID, SHA, walk theatre, duration, gestures performed.
    - Verbatim verdict block-quoted.
    - Per-stream summary table.
    - Walk-#4-template comparison table (medianMs, headroom, canvasTx/Ty, pixelOriginX range, dev_markers fired, SDF rebuild rate, verdict).
    - `gaps:` frontmatter populated with any opened-by-Walk-#5 gap entries (Branch B / C only).

    **E) REQUIREMENTS.md updates:**

    Branch-specialized:

    **On CONFIRMED-AFTER-FIX:**
    - FOG-04..07: `Falsified-in-production` → `Complete — Verified-by-walk (P03.1-13 CONFIRMED-AFTER-FIX 2026-05-XX after layered Plan 03.1-02 + 03.1-04 + 03.1-05 + 03.1-07 + 03.1-08 + 03.1-10 + 03.1-12 fix bundle)`.
    - FOG-11: `Falsified-in-production` → `Complete — Verified-by-walk (P03.1-13 CONFIRMED-AFTER-FIX)`.
    - FOG-15: `Verified-by-test-only (historical mechanism-confirmed fix; superseded by FOG-17 + FOG-18 + FOG-19)` (note that B-3 has been further superseded; FOG-15 stays as historical record).
    - FOG-18 + FOG-19 + DEBUG-02: `Complete — Verified-by-test (P03.1-12)` → `Complete — Verified-by-test + walk-time validated (P03.1-13 Walk #5)`.
    - UX-02 + FOG-16 + FOG-17: re-validate at Walk #5 levels (annotation: `+ walk-time validated (P03.1-13 Walk #5 re-confirms Walk #4)`).
    - PERF-07: re-measured per Walk #5 numbers.
    - Walk #5 Revisions entry appended at the bottom.

    **On DENIED-FINAL:**
    - FOG-04..07 + FOG-11 + FOG-15: stay `Falsified-in-production` with revision-clause pointing at `03.1-FALSIFICATION-5.md`.
    - FOG-18 + FOG-19 + DEBUG-02: `Complete — Verified-by-test + walk-time validated (P03.1-13)` (the structural fixes are in the codebase + observed in JSONL stream; the open question is whether they're SUFFICIENT — the answer is no, but the fixes themselves are mechanically present).
    - UX-02 + FOG-16 + FOG-17: re-validate.
    - PERF-07: re-measured.
    - Add Walk #5 DENIED-FINAL Revisions entry.

    **On ITERATING:**
    - FOG-04..07 + FOG-11 + FOG-15: stay `Falsified-in-production` with revision-clause pointing at `03.1-FALSIFICATION-5.md`.
    - FOG-18 + FOG-19 + DEBUG-02: `Complete — Verified-by-test + walk-time validated (P03.1-13)`.
    - UX-02 + FOG-16 + FOG-17: re-validate.
    - PERF-07: re-measured.
    - Add Walk #5 ITERATING Revisions entry with surfaced gaps + Plan 03.1-14+ candidate axes.

    **F) ROADMAP.md updates:**

    - Phase 03.1 row in Progress table: update plan count + status:
      - CONFIRMED-AFTER-FIX: `13/13 — Complete (HYPOTHESIS CONFIRMED-AFTER-FIX 2026-05-XX)`.
      - DENIED-FINAL: `13/13 — Complete (HYPOTHESIS DENIED-FINAL 2026-05-XX)`.
      - ITERATING: `13/13 — In Progress (6th iteration; Walk #6 pending in Plan 03.1-14+)`.
    - On CONFIRMED-AFTER-FIX:
      - Phase 4 (`Wisp Particles`) status: `Blocked on Phase 3.1` → `Pending`.
      - Phase 5 (`Decision Gate`) status: `Blocked on Phase 3.1` → `Pending`.
      - Phase 3 row note: prepend `**REVERSED 2026-05-XX:** Phase 3 hypothesis ORIGINALLY DENIED 2026-05-01; Phase 3.1 closure at Walk #5 reverses to CONFIRMED-AFTER-FIX with the layered fix bundle.`
    - Phase 03.1 narrative: append the Walk #5 verdict line.
    - Plan 03.1-13 row: marked as landed with verdict summary.

    **G) STATE.md updates:**

    - `Current Position`: reflect Walk #5 outcome.
    - `Decisions log`: append Walk #5 entry.
    - `Blockers`: 
      - On CONFIRMED-AFTER-FIX: resolve `PHASE-3.1-WALK-5-PENDING`; resolve all Phase 4 + 5 BLOCKED entries.
      - On DENIED-FINAL: resolve `PHASE-3.1-WALK-5-PENDING`; transition Phase 4 + 5 to `BLOCKED-FINAL` or `CANCELLED` per developer choice.
      - On ITERATING: resolve `PHASE-3.1-WALK-5-PENDING`; open `PHASE-3.1-WALK-6-PENDING`.
    - `Session Continuity`: updated.

    **H) Atomic commit:**

    Single atomic commit at the end of Task 3:
    ```
    docs(03.1-13): Walk #5 verdict <VERDICT> + REQUIREMENTS/ROADMAP/STATE updates (Task 3)

    Walk #5 sideload UAT against CI run <run_id> (SHA <sha>) over central
    Melun delivered the 6th Phase 3.1 verdict: <VERDICT>.

    <one-paragraph-summary of verdict + Q-axis outcomes + JSONL signature
    confirmation>

    Files modified:
    - 03.1-FALSIFICATION-5.md (load-bearing verdict analysis)
    - 03.1-UAT-5.md (transcript + per-stream summary + verbatim verdict)
    - REQUIREMENTS.md (status updates per verdict branch)
    - ROADMAP.md (Phase 03.1 row + (CONFIRMED) Phase 3 reversal note + Phase 4/5 unblock)
    - STATE.md (Current Position + Decisions log + Blockers)

    Walks #1 + #2 + #3 + #4 + Walk #3b + Phase 3 historical records UNTOUCHED.

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    ```
  </action>
  <verify>
    <automated>node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" verify must-haves --plan .planning/phases/03.1-fix-fog-pan-translation/03.1-13-PLAN.md</automated>
  </verify>
  <done>
    - `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md` Walk Evidence + Verdict sections filled (≥ 100 lines total).
    - `.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-5.md` complete with verbatim verdict block-quoted + per-stream summary + `gaps:` frontmatter populated (Branch B / C only).
    - REQUIREMENTS.md updated per verdict branch (FOG-04..07 + FOG-11 + FOG-15 + FOG-18 + FOG-19 + DEBUG-02 + UX-02 + FOG-16 + FOG-17 + PERF-07 statuses + Walk #5 Revisions entry).
    - ROADMAP.md Phase 03.1 + (CONFIRMED) Phase 3 reversal + Phase 4/5 unblock per verdict branch.
    - STATE.md Current Position + Decisions + Blockers + Session Continuity per verdict branch.
    - One atomic commit `docs(03.1-13): Walk #5 verdict <VERDICT> ...` lands on `main`.
    - Walks #1-#4 + Walk #3b + Phase 3 historical records UNTOUCHED (verify via `git log --oneline --follow .planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-{1,2,3,4}.md` showing no new commits in this plan's scope).
  </done>
</task>

</tasks>

<verification>
After all three tasks:

1. Walks #1 / #2 / #3 / #4 + Walk #3b + Phase 3 historical records UNTOUCHED:
   ```bash
   git log --oneline 03.1-13-PLAN.md HEAD~..HEAD -- \
     .planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION.md \
     .planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-2.md \
     .planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-3.md \
     .planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-4.md \
     .planning/phases/03.1-fix-fog-pan-translation/03.1-WALK-3B-MARKER-ANALYSIS.md \
     .planning/phases/03-fog-of-war-the-hypothesis/03-FALSIFICATION.md
   ```
   should return EMPTY (no commits in this plan's scope touch those files).

2. New artefacts exist:
   - `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md`
   - `.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-5.md`

3. Two atomic commits land on `main`:
   - `docs(03.1-13): pre-walk gates green + Walk #5 ...` (Task 1)
   - `docs(03.1-13): Walk #5 verdict <VERDICT> ...` (Task 3)
   - Task 2 = checkpoint:human-verify, no commit.

4. Pre-walk gates GREEN at Task 1 (test + analyze + format + tool tests + CI + IPA download).

5. Walk #5 verdict captured + propagated through REQUIREMENTS / ROADMAP / STATE per the appropriate branch.
</verification>

<success_criteria>
- Two atomic commits land on `main` (pre-walk gates + skeleton; verdict authoring).
- Walk #5 sideload UAT performed by developer (Task 2 checkpoint resolved).
- `03.1-FALSIFICATION-5.md` + `03.1-UAT-5.md` exist as load-bearing artefacts.
- REQUIREMENTS / ROADMAP / STATE updated per verdict branch.
- On CONFIRMED-AFTER-FIX: Phase 3.1 closes, Phase 4 + Phase 5 unblock, Plan 03-08 DENIED reversed, MirkFall port-back recommendation issued. Phase 3.1 In Progress flips to Complete.
- On DENIED-FINAL: formal POC architectural counter-evidence captured; Phases 4 + 5 status updated per developer choice.
- On ITERATING: Plan 03.1-14+ pending in follow-up plan-phase invocation; Phase 3.1 stays In Progress.
- Walks #1-#4 + Walk #3b + Phase 3 historical records UNTOUCHED.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-fix-fog-pan-translation/03.1-13-SUMMARY.md` per the standard summary template.

Notable artefacts to capture in the summary:
- `requirements-completed`: branch-dependent
  - CONFIRMED-AFTER-FIX: `[FOG-04, FOG-05, FOG-06, FOG-07, FOG-11, FOG-15, FOG-18, FOG-19, DEBUG-02, UX-02, PERF-07]` (Phase 3.1 closure).
  - DENIED-FINAL: `[FOG-18, FOG-19, DEBUG-02, UX-02, PERF-07]` (only the structurally-landed-and-walk-validated; FOG-04..07 + FOG-11 + FOG-15 stay Falsified-in-production).
  - ITERATING: `[FOG-18, FOG-19, DEBUG-02, UX-02, PERF-07]`.
- `key-files.created`: `03.1-FALSIFICATION-5.md`, `03.1-UAT-5.md`, `03.1-13-SUMMARY.md`.
- `key-files.modified`: REQUIREMENTS.md, ROADMAP.md, STATE.md.
- `key-decisions`: enumerate the verdict + Q-axis outcomes + JSONL signature confirmations + Phase 4/5 status changes per verdict branch.
- `patterns-established`: "Walk-#5 FOG-18 + FOG-19 signature confirmation pattern" (uOffsetX* tracks pixelOriginX magnitude monotonically + evolves continuously across zoom transitions); on CONFIRMED-AFTER-FIX: "Plan 03-08 verdict reversal pattern" (the architectural-evidence gate that flipped HYPOTHESIS DENIED → CONFIRMED-AFTER-FIX after the layered Phase 3.1 fix bundle).
</output>
