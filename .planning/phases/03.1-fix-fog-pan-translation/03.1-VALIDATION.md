---
phase: 3.1
slug: fix-fog-pan-translation
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-02
---

# Phase 3.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `flutter_test` (Flutter SDK 3.41.7) — already in scope |
| **Config file** | `analysis_options.yaml` (strict-casts/inference/raw-types); no separate test config |
| **Quick run command** | `flutter test test/presentation/widgets/fog_pan_translation_test.dart test/infrastructure/mirk/fog_transform_logger_test.dart -r expanded` |
| **Full suite command** | `flutter test` |
| **Estimated runtime** | ~30s quick / ~60s full |

---

## Sampling Rate

- **After every task commit:** Run `flutter test test/presentation/widgets/fog_pan_translation_test.dart test/infrastructure/mirk/fog_transform_logger_test.dart -r expanded`
- **After every plan wave:** Run `flutter test` (full suite)
- **Before `/gsd:verify-work`:** Full suite green + `flutter analyze` zero warnings + `dart format --line-length 160 --set-exit-if-changed .` clean + LOC-03 + BOOT-02 CI gates green + sideload UAT walk verbal `approved`
- **Max feedback latency:** ~30 seconds (quick command)

---

## Per-Task Verification Map

> Populated by gsd-planner during plan creation. Each task references the new requirement IDs (FOG-09, FOG-10, PERF-07) that the planner adds to REQUIREMENTS.md.

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 03.1-02-Task-3 | 03.1-02 | 2 | FOG-09 | widget (programmatic pan + recording renderer + MockCanvas) | `flutter test test/presentation/widgets/fog_pan_translation_test.dart -r expanded` | ✅ EXISTS | ✅ green |
| 03.1-02-Task-3 | 03.1-02 | 2 | FOG-09 (vacuous-test guard) | widget (skip:true on green-main; manual run against pre-fix HEAD) | (same file) | ✅ EXISTS | ⬜ skipped |
| 03.1-01-Task-2 | 03.1-01 | 1 | FOG-10 | unit (synthetic samples + Logger.captureLogs) | `flutter test test/infrastructure/mirk/fog_transform_logger_test.dart -r expanded` | ✅ EXISTS | ✅ green |
| 03.1-01-Task-2 | 03.1-01 | 1 | FOG-10 (idle-second skip) | unit | (same file) | ✅ EXISTS | ✅ green |
| 03.1-01-Task-2 | 03.1-01 | 1 | FOG-10 (FIFO drop on overflow) | unit | (same file) | ✅ EXISTS | ✅ green |
| 03.1-01-Task-2 | 03.1-01 | 1 | FOG-10 (stop-flush-pending) | unit | (same file) | ✅ EXISTS | ✅ green |
| 03.1-02-Task-2 | 03.1-02 | 2 | FOG-07 (regression / unchanged) | widget | `flutter test test/presentation/widgets/fog_layer_camera_snapshot_test.dart -r expanded` | ✅ EXISTS | ✅ green |
| 03.1-02-Task-3 | 03.1-02 | 2 | FOG-04 (existing structural, augmented docstring) | widget | `flutter test test/presentation/widgets/fog_layer_test.dart -r expanded` | ✅ EXISTS | ✅ green |
| 03.1-03-Task-2 | 03.1-03 | 3 | PERF-07 | manual-only (UAT walk) | sideload + walk on iPhone 17 Pro + post-walk JSONL grep | manual gate | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] `test/presentation/widgets/fog_pan_translation_test.dart` — covers FOG-09 (behavioural transform-equality regression test); the keystone test for the Plan 03-08 lesson
- [x] `test/infrastructure/mirk/fog_transform_logger_test.dart` — covers FOG-10 (rollup correctness, idle-skip, FIFO-drop, stop-flush)
- [x] `lib/infrastructure/mirk/fog_transform_logger.dart` — new logger sibling to `frame_delta_probe.dart` + `sdf_rebuild_logger.dart`
- [x] `lib/config/constants.dart` additions: `kPocFogTransformLogRollupSeconds = 1`, `kPocFogTransformBufferMaxSamples = 240`, `kPocCanvasTransformEpsilon = 1e-6`
- [x] `lib/presentation/widgets/fog_layer.dart` modifications: derive `uOffsetX/Y` from `camera.pixelOrigin / size`; thread `FogTransformLogger`; instrument `paint()` with `recordPaint`
- [x] `lib/domain/map/map_screen_services.dart` extension: add `fogTransformLogger` field; `MapScreen` constructs and disposes the logger alongside `frameDeltaProbe` + `sdfRebuildLogger`
- [x] Existing `test/presentation/widgets/fog_layer_test.dart` — non-blocking docstring augmentation (forward-pointer to `fog_pan_translation_test.dart`)

**Test seams that need engineering** (per RESEARCH.md):
- `MockCanvas extends Fake implements Canvas` overriding `save`, `restore`, `clipPath`, `drawRect`, `getTransform` — Flutter SDK's own `custom_paint_test.dart` pattern; `TestRecordingCanvas` does not override transform methods.
- Recording `FogShaderRenderer` extending the Plan 03-05 `RecordingFogShaderRenderer` with the `offset` tuple if not already captured.
- `MapController` programmatic drive: `mapController.move(LatLng, zoom)` + `tester.pump()` (not `pumpAndSettle` — animations would mask the instantaneous move event).

**Framework install:** None — `flutter_test` is in the SDK; no new dev_dependency.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Frame-delta probe median ≤ 16 ms / p95 ≤ 32 ms / max ≤ 48 ms across ≥ 10 combined gestures | PERF-07 | Inherently requires a real iPhone 17 Pro at outdoor GPS coordinates with developer's hand performing combined gestures (carries over PERF-04 methodology) | Sideload IPA → walk Melun route ≥ 5 minutes → ≥ 10 deliberate combined pinch-zoom-and-pan gestures → ≥ 3 recenter taps → FrameDeltaProbe overlay visible → share post-walk log via Mail → grep `infrastructure.mirk.frame_delta` rollup for the three percentiles |
| Same-Canvas fog visually translates with camera during pan (no static fog regression) | (Phase 3.1 walk gate) | Visual confirmation requires real-device pan | Same walk; verbal `approved` once fog tracks camera through full pan range (or describe failure mode for plan-revise iteration) |
| Diagnostic JSONL log disambiguates root cause if walk fails | (Phase 3.1 fix-failure contingency) | Walk-evidence-driven | Post-walk grep `infrastructure.mirk.fog_transform` for per-second canvasTx/Ty + uOffsetX/Y + pixelOrigin; correlate against `frame_delta` + `sdf` rollups on per-second boundary |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies (planner fills task IDs)
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (6 items above)
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s (quick command)
- [ ] `nyquist_compliant: true` set in frontmatter (after planner finalizes Per-Task Verification Map)

**Approval:** pending
