---
phase: 03.1-fix-fog-pan-translation
plan: 11
type: execute
wave: 2
depends_on: ["03.1-10"]
files_modified:
  - .planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-4.md
  - .planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-4.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
gap_closure: true
scope_note: |
  Plan 03.1-11 scope = iPhone 17 Pro Walk #4 sideload UAT against the existing
  Plan 03.1-10 software-fix bundle (HEAD `b31766f`). Original framing 2026-05-03;
  re-validated 2026-05-04 against four discuss-phase decisions captured in
  03.1-CONTEXT.md `<decisions>` block (see also Discuss-phase decisions
  subsection at the end of `<objective>` below):

  1. **Walk #4 timing = single plan, immediate verdict** — all three tasks
     (pre-walk gates + sideload + verdict authoring) live in Plan 03.1-11.
     Matches the existing 3-task structure; no change required.
  2. **FOG-12 + FOG-13 Canvas-side compensation audit = DEFERRED** to
     post-Walk-4. Discuss-phase first answer was "auditer puis décider"
     (with an audit task in 03.1-11). Superseded by the developer's
     subsequent clarification (CLAUDE.md `# MIRL solution`,
     03.1-CONTEXT.md `<decisions>`): *"the architecture is malleable, I
     don't really care about the architecture for now... once we find how
     to solve our issue, it can be made better later"*. Net effect: the
     Walk #4 empirical outcome IS the audit answer (fog covers viewport
     correctly under all gestures ⇒ FOG-12/13 working; viewport gap surfaces
     ⇒ open audit + redesign in a follow-up Plan 03.1-12+). **No
     FOG-12/13 audit task in this plan.**
  3. **MIRK-01 acceptance test trio** (static uniform-list inspection +
     behavioural swap-shader test + zero-painter-branching invariant)
     = **DEFERRED** with MIRK-01 itself. **No Tasks A/B/C in this plan.**
  4. **ABI uniform rename `uPixelOrigin` → `uWorldOffset`** = **DEFERRED**
     with MIRK-01 ABI formalization. Existing `uPixelOrigin` slot 3..4
     stays as-is across both shaders + `FogShaderUniforms` + JSONL
     `uOffsetX*` field names. **No rename task in this plan.**

  Plan body remains structurally identical to the 2026-05-03 framing —
  3 tasks (Task 1 auto pre-walk gates + skeleton authoring, Task 2
  checkpoint:human-verify sideload UAT, Task 3 auto verdict authoring),
  unchanged verdict semantics, unchanged pre-walk gate sequence,
  unchanged walk shape, unchanged dev-marker workflow, unchanged locked
  architectural invariants. The 2026-05-04 discuss-phase pass confirmed
  that none of the four gray-area decisions warrant new tasks here.
requirements: [FOG-04, FOG-05, FOG-06, FOG-07, FOG-11, FOG-15, FOG-16, FOG-17, UX-02, PERF-07]
user_setup:
  - service: github-actions
    why: "Wait for CI green on the post-Plan-03.1-10 SHA + download the IPA artifact for the Walk #4 sideload"
    dashboard_config:
      - task: "Confirm CI run status (`gh run list --branch main --limit 5` + `gh run watch`)"
        location: "GitHub Actions for the mirk-poc-debug repo"
      - task: "Download IPA artifact via `gh run download <RUN_ID> --name ios-unsigned --dir .uat-tmp/`"
        location: "GitHub Actions run detail page (or `gh` CLI)"
  - service: sidestore
    why: "Sideload the unsigned IPA onto iPhone 17 Pro for Walk #4"
    dashboard_config:
      - task: "Open SideStore on iPhone 17 Pro → install/refresh the .uat-tmp/mirk-poc-debug-unsigned.ipa"
        location: "SideStore iOS app + paired desktop pairing-file"
must_haves:
  truths:
    - "A new `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-4.md` exists capturing the Walk #4 verdict. Walks #1 + #2 + #3 falsification documents AND the Walk #3b marker analysis are LEFT UNTOUCHED — historical record preserved across iteration cycles."
    - "A new `.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-4.md` exists capturing the Walk #4 transcript. Walks #1 + #2 + #3 UAT transcripts are LEFT UNTOUCHED."
    - "Pre-walk gate sequence runs cleanly: `flutter test` GREEN (158+ existing + Plan 03.1-10's new tests: FOG-17 4 sub-tests + FOG-17a 5 sub-tests + UX-02 rotation-disabled assertion + REWRITTEN FOG-14a/b sub-tests); `flutter analyze --fatal-infos` 0 warnings; `dart format --line-length 160 --set-exit-if-changed .` clean; `dart test tool/test/` GREEN; CI green on the post-Plan-03.1-10 SHA on `main`; IPA downloaded via `gh run download` to `.uat-tmp/mirk-poc-debug-unsigned.ipa`."
    - "The iPhone 17 Pro UAT Walk #4 is performed by the user. Walk shape matches Plan 03.1-09 (Walk #3) pattern: ≥ 5 minutes through Melun + ≥ 10 deliberate combined pinch-zoom-and-pan gestures + ≥ 3 recenter taps + post-walk Mail-share of the session log + pre-walk `/sanity` shader-compile gate (production fog renders + DEBUG-01 toggle works on /sanity AppBar AND /map AppBar). NEW for Walk #4: explicit dev-marker-button taps every time the developer observes ANY residual symptom (steppy translation, fog mis-coverage, anything visually anomalous). The marker button currently hardcodes `tag: \"steppy_translation\"`; tag-disambiguation is post-hoc via observation timing."
    - "Mail-share discipline RE-ESTABLISHED — Walk #3 broke the post-walk Mail-share pattern (verbal evidence only); Walk #4 explicitly requires the developer to share the session log via Mail post-walk so the JSONL streams (frame_delta + SDF + fog_transform) can be grep-correlated against any marker timestamps. The Walk #3b marker-correlation analysis (`03.1-WALK-3B-MARKER-ANALYSIS.md`) is the template — Walk #4's analysis will follow the same harness if markers fire."
    - "Post-walk: developer's verbal verdict (`approved` / `denied:` / `iterating:`) is captured. The walk evidence section of `03.1-FALSIFICATION-4.md` is filled with: developer's verbatim words + interpretation + post-walk grep of `infrastructure.mirk.fog_transform` + `infrastructure.mirk.frame_delta` + `infrastructure.mirk.sdf` rollups + per-second composite extract + (if markers fired) marker-correlation analysis mirroring the Walk #3b template."
    - "PERF-07 is re-measured for Walk #4: frame-delta probe rolling rollup over the ≥ 10-combined-gesture window with `medianMs ≤ 16`, `p95Ms ≤ 32`, `maxMs ≤ 48`. Walk #1 captured 4-7× headroom; Walk #2 captured 3.9-5.4× headroom; Walk #3 captured implicitly (no log Mail-shared); Walk #3b's 33-sec window showed median-of-medians 0.405 ms (40-110× headroom). Walk #4 should preserve similar headroom (Plan 03.1-10's Dart-side decomposition adds ~5 cycles per paint — negligible; the shader's per-fragment world-coordinate computation is also negligible — same arithmetic count as the pre-fix per-fragment fract; integer-wrap-modulo on the Dart side is at the painter level, not per-fragment)."
    - "Verdict written: `CONFIRMED-AFTER-FIX` if BOTH (a) developer's subjective verdict is `approved` AND (b) PERF-07 thresholds satisfied AND (c) Q1 stepping resolved (no perceived hard-step events during pure pan; the world-coordinate formulation should produce monotonic noise scrolling per the FOG-17 design) AND (d) Q2 rotation-correlated mis-coverage structurally absent (UX-02 disables rotation; if developer attempts a two-finger rotate gesture and observes the fog covers the viewport correctly throughout, the structural prevention is walk-time-confirmed). `DENIED-FINAL` if the diagnostic JSONL stream cannot resolve a residual failure mode via a follow-up Phase 3.1 iteration. `ITERATING` if a residual failure mode surfaces (e.g., integer-wrap stepping every ~128 sec at sustained one-direction pan, OR a NEW failure mode the layered fix bundle didn't address)."
    - "On `CONFIRMED-AFTER-FIX`: ROADMAP.md Phase 03.1 row in the Progress table flips to `Complete (HYPOTHESIS CONFIRMED-AFTER-FIX)`. Phase 4 (`Wisp Particles`) status updates from `Blocked on Phase 3.1` to `Pending`. Phase 5 (`Decision Gate`) likewise. STATE.md `Current Position` updates to reflect Phase 3.1 closure. REQUIREMENTS.md FOG-04..07 + FOG-11 + FOG-15 statuses flip from `Falsified-in-production` to `Complete — Verified-by-walk (P03.1-11 CONFIRMED-AFTER-FIX 2026-05-XX after layered Plan 03.1-02 + 03.1-04 + 03.1-05 + 03.1-07 + 03.1-08 + 03.1-10 fix bundle)`. PERF-07 status updates to `Complete — Re-measured (P03.1-11)` with the actual numbers."
    - "On `DENIED-FINAL`: ROADMAP.md Phase 03.1 row updates to `Complete (HYPOTHESIS DENIED-FINAL)`. Phases 4 + 5 stay `Blocked` (or transition to `Cancelled` per developer choice — flag for user decision). REQUIREMENTS.md FOG-04..07 + FOG-11 + FOG-15 stay `Falsified-in-production` with updated revision-clause that points at `03.1-FALSIFICATION-4.md`. UX-02 + FOG-16 + FOG-17 still flip to `Complete — Verified-by-test + walk` (their CI gates ARE GREEN by Plan 03.1-10 construction; FOG-16 path (a) UX-02 rotation prevention is structurally walk-time-validated by Q2 absence). STATE.md captures the formal POC architectural counter-evidence."
    - "On `ITERATING`: a Plan 03.1-12+ is opened in a follow-up plan-phase invocation, with Walk #4's diagnostic stream as input. THIS plan does NOT author 03.1-12. UX-02 + FOG-16 + FOG-17 still flip to `Complete — Verified-by-test + walk-time validated` (the structural fixes ARE in the codebase; the open question is whether they're SUFFICIENT). PERF-07 updated with Walk #4 numbers regardless. FOG-04..07 + FOG-11 + FOG-15 stay `Falsified-in-production`. Phase 3.1 stays `In Progress (5th iteration, walk #5 pending)`."
    - "Phase 3 `03-FALSIFICATION.md` AND Phase 3.1 Walks #1 + #2 + #3 falsification documents AND `03.1-WALK-3B-MARKER-ANALYSIS.md` are LEFT UNTOUCHED — no edits, no overwrites. Historical records preserved across iteration cycles."
  artifacts:
    - path: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-4.md"
      provides: "Phase 3.1 Walk #4 falsification document — pre-walk gate-pass evidence + post-walk verdict (`CONFIRMED-AFTER-FIX` | `DENIED-FINAL` | `ITERATING`)"
      min_lines: 100
      contains: "Walk #4"
    - path: ".planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-4.md"
      provides: "Walk #4 evidence transcript — developer's verbatim verdict + interpretation + JSONL grep correlation + (if markers fired) marker-correlation analysis"
      min_lines: 40
      contains: "Walk Evidence"
  key_links:
    - from: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-4.md"
      to: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-3.md"
      via: "explicit cross-reference in Walk #4's `## Relationship to Walk #3 Verdict` section ('Walk #3 closed iterating-with-partial-progress-and-rotation-regression 2026-05-03; Walk #4 either confirms or denies the layered Plan 03.1-10 software fix bundle')"
      pattern: "03\\.1-FALSIFICATION-3\\.md"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-4.md"
      via: "FOG-04..07 + FOG-11 + FOG-15 + FOG-16 + FOG-17 + UX-02 + PERF-07 status clauses include `(P03.1-11)` cross-reference; Revisions section appends a 2026-05-XX Walk #4 entry"
      pattern: "P03\\.1-11"
    - from: ".planning/STATE.md"
      to: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-4.md"
      via: "Current Position + Decisions sections capture the Walk #4 verdict outcome"
      pattern: "Phase 3\\.1"
---

<objective>
Run the pre-walk gates, perform the iPhone 17 Pro sideload UAT Walk #4, capture the developer's verdict, and produce `03.1-FALSIFICATION-4.md` — the Phase 3.1 Walk #4 falsification document. Walk #4 is the empirical re-test for the layered Plan 03.1-10 software fix bundle: world-coordinate noise sampling (FOG-17) + CPU-side integer/fractional decomposition (FOG-17a precision pairing) + rotation disable (UX-02 / FOG-16 path (a)). The Walk #4 verdict either reverses Walk #3's `ITERATING-WITH-PARTIAL-PROGRESS-AND-ROTATION-REGRESSION` outcome to `CONFIRMED-AFTER-FIX` (unblocking Phase 4 + Phase 5 of the original roadmap) or strengthens it to `DENIED-FINAL` (terminating the POC's same-Canvas hypothesis) or — per CONTEXT §Iteration policy — captures a fifth `ITERATING` verdict that opens Plan 03.1-12+.

Purpose: this is the empirical gate Phase 3.1 needs to close after the Plan 03.1-10 software fix bundle. Plan 03.1-10 software-completed UX-02 (rotation disabled) + FOG-16 path (a) closure + FOG-17 world-coordinate noise sampling in BOTH shaders + FOG-17a CPU-side integer/fractional decomposition. Walk #4 is the architectural-evidence gate that flips FOG-04..07 + FOG-11 + FOG-15 to Complete (or strengthens the verdict). Per CONTEXT §Iteration policy, there is no hard cap on walk count.

Output: 1 new `03.1-FALSIFICATION-4.md` (the load-bearing artefact); 1 new `03.1-UAT-4.md` (the walk-evidence transcript); REQUIREMENTS.md status updates for FOG-04..07 + FOG-11 + FOG-15 + FOG-16 + FOG-17 + UX-02 + PERF-07; ROADMAP.md Phase 03.1 Progress-table row update; STATE.md Current Position + Decisions updates. Phase 3's `03-FALSIFICATION.md` AND Phase 3.1 Walks #1 + #2 + #3 records AND `03.1-WALK-3B-MARKER-ANALYSIS.md` ALL left UNTOUCHED.

### Discuss-phase decisions (2026-05-04)

A `/gsd:discuss-phase 3.1` pass on **2026-05-04** surfaced four gray-area decisions that the planner reconciled against the developer's "architecture is malleable; validate first, architect later" clarification (CLAUDE.md `# MIRL solution`). All four resolved as either matching the existing plan structure OR as explicit deferrals — see frontmatter `scope_note` for the full enumeration and 03.1-CONTEXT.md `<decisions>` §"Discuss-phase decisions captured 2026-05-04" for verbatim record:

1. Walk #4 timing = ALL tasks in Plan 03.1-11 (single plan, immediate verdict). **Matches existing structure** (Tasks 1-2-3 below).
2. FOG-12 + FOG-13 Canvas-side compensation audit = **DEFERRED** to post-Walk-4. Walk #4 outcome IS the empirical answer. **No audit task added below.**
3. MIRK-01 acceptance test trio (static + behavioural + invariant) = **DEFERRED** with MIRK-01 formalization itself. **No Tasks A/B/C added below.**
4. ABI uniform rename `uPixelOrigin` → `uWorldOffset` = **DEFERRED** with MIRK-01. **No rename task added below.**

Net effect on this plan: zero new tasks; zero removed tasks; zero changes to verdict semantics, pre-walk gate sequence, walk shape, dev-marker workflow, or locked architectural invariants. The 2026-05-04 pass confirmed that the existing 2026-05-03 plan body already honours every locked decision; this acknowledgment block exists to make the conscious deferrals legible to future plan-checker passes (vs. accidentally omitted scope).
</objective>

<execution_context>
@C:/Users/oliver/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oliver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-CONTEXT.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-2.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-3.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-2.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-3.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-WALK-3B-MARKER-ANALYSIS.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-VALIDATION.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-09-PLAN.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-10-PLAN.md

<interfaces>
<!-- Walk #4 mirrors Walk #3 (Plan 03.1-09) shape-for-shape with adjusted hypothesis re-statements. -->

`03.1-FALSIFICATION-3.md` Walk #3 structure (the immediate predecessor template):
1. Title + metadata (`Created`, `Phase`, `Walked on`, `Verdict`)
2. `## Relationship to Walk #2 Verdict` (Walk #4 inserts `## Relationship to Walk #3 Verdict`)
3. `## Hypothesis`
4. `## Falsification Criteria (written BEFORE the walk)` — `### Criterion A`, `### Criterion B`, `### Criterion C — DROPPED`, `### Criterion D — Diagnostic stream`
5. `## Walk Plan`
6. `## Walk Evidence (filled AFTER the walk)`
7. `## Verdict`

Walk #4's `03.1-FALSIFICATION-4.md` mirrors this exactly with these specialisations:
- Title: `# Phase 3.1 Falsification — Walk #4 (Post-Plan-03.1-10 World-Coordinate Noise + Rotation Disable Fix Bundle)`
- Add `## Relationship to Walk #3 Verdict` section pointing at `03.1-FALSIFICATION-3.md`; summarise the iterating-with-partial-progress-and-rotation-regression outcome and the gap entries (FOG-16, FOG-17, FOG-17a, UX-02) closed by Plan 03.1-10.
- Hypothesis re-statement: "After the Plan 03.1-10 layered software fix bundle (UX-02 disable rotation + FOG-17 world-coordinate noise sampling in BOTH atmospheric_fog.frag + atmospheric_fog_debug_spiral.frag + FOG-17a CPU-side integer/fractional decomposition keeping shader input bounded under ~1537 raw px), the same-Canvas fog now: (a) translates with the camera during pan; (b) maintains a fully stable noise pattern across consecutive paints (FOG-17 eliminates the fract-wrap discontinuity entirely; the world-coordinate formulation scrolls NEW noise in from the viewport edge as camera pans); (c) keeps the reveal hole co-located with the blue dot during pan/zoom (FOG-12 retained); (d) covers the entire viewport at any canvas-translation magnitude (FOG-13 retained); (e) is structurally invulnerable to the rotation-correlated fog mis-coverage failure mode (UX-02 prevents MapCamera.rotation from changing — MobileLayerTransformer never accumulates rotation matrix elements)."
- Criterion A: same thresholds (PERF-07 carry-over): `medianMs ≤ 16`, `p95Ms ≤ 32`, `maxMs ≤ 48`. Walk #1: 4-7×; Walk #2: 3.9-5.4×; Walk #3b: 40-110× (33-sec window); Walk #4 should preserve at least Walk #2 levels.
- Criterion B sub-claims:
  - Q1: "No perceived hard-step events during pure pan" — Walk #3 fail; Plan 03.1-10 FOG-17 + FOG-17a target this directly. Sub-test: developer pans for ≥ 60 seconds in one direction (covers ~720 raw px at typical 12 raw px/s velocity, well within the 1537-raw-px integer-wrap window — should NOT trigger an integer wrap; if developer pans much further OR walks for several minutes, an integer wrap may fire and produce a sub-perceptible noise-pattern shift; document the duration of pan during which Q1 was tested).
  - Q2: "No rotation-correlated fog mis-coverage" — Walk #3 NEW failure; Plan 03.1-10 UX-02 disables rotation structurally. Sub-test: developer attempts a two-finger rotate gesture; the map should NOT rotate (north stays up; MapCompass widget stays at 0°). The fog viewport coverage should remain correct even during deliberate rotate-attempt gestures.
  - Q3: "No reveal-hole offset from blue dot during gesture" (Walks #1 + #2 + #3 retained — Plan 03.1-05 FOG-12 + Plan 03.1-08 FOG-13 — re-confirmation under Plan 03.1-10's coordinate-system rewrite).
  - Q4: "Fog visually translates with camera during pan" (Walks #1 + #2 + #3 retained — re-confirmation; Plan 03.1-10 doesn't touch the translation path; if Q4 regresses, Plan 03.1-10 introduced a NEW failure mode).
  - No white-ellipse during fast pinch-zoom (already passed Walks #1-3)
  - No inversion at any zoom level (already passed Walks #1-3)
- Criterion C stays DROPPED.
- Criterion D (Diagnostic stream): `pixelOriginX/Y` track gesture monotonically; `appliedUOffset` keys (which preserve the JSONL field name for back-compat with Walk #1-3 tooling) now log the bounded composite (intPx % kPocFogIntegerWrapPeriodPx + fracPx) so values in JSONL stream stay under ~1537 raw px regardless of zoom — directly visible signature of FOG-17a in effect. `canvasTx/Ty` jumps still occur — Plan 03.1-08's `canvas.translate(-canvasOffset)` compensates them mechanically (FOG-13 CI gate validates). `canvasRotation` field NOT added to schema — UX-02 makes rotation observation moot.

`03.1-UAT-4.md` mirrors `03.1-UAT-3.md` shape-for-shape: metadata block + walk protocol + walk evidence (frame-delta + SDF + fog-transform rollups) + subjective verdict + verdict + deviations + summary + (optional) gaps section if Walk #4 surfaces residual failure modes + `gaps:` YAML in frontmatter for any opened-by-Walk-#4 gap entries.

The pre-walk gate sequence is identical to Plan 03.1-09 Task 1 (six gates: flutter test / analyze / format / tool tests / CI green / IPA download).

The walk shape is identical to Plan 03.1-09 with one NEW addition: explicit dev-marker-button taps for any residual symptom. The marker button currently hardcodes `tag: "steppy_translation"`; Walk #3b proved it is operational on iPhone 17 Pro and produces JSONL events tagged with the marker timestamp at the per-frame level. The Walk #3b marker-correlation analysis (`03.1-WALK-3B-MARKER-ANALYSIS.md`) is the template for Walk #4's analysis if markers fire.
</interfaces>

<out_of_scope>
<!--
  Out-of-scope for Plan 03.1-11 — DEFERRED to post-Walk-4 follow-up plans.
  Captured here (rather than silently omitted) to pre-empt plan-checker
  confusion in future runs and to make the 2026-05-04 discuss-phase
  conscious-deferrals legible alongside the locked plan body.
-->

The following items were considered during the 2026-05-04 `/gsd:discuss-phase 3.1` pass and **intentionally deferred** out of Plan 03.1-11 per the developer's "architecture is malleable; validate first, architect later" framing (CLAUDE.md `# MIRL solution`; 03.1-CONTEXT.md `<decisions>` block):

1. **FOG-12 + FOG-13 Canvas-side compensation audit.** First-pass discuss-phase answer was "auditer puis décider" (an audit task inside 03.1-11). Superseded by developer's subsequent clarification: the empirical Walk #4 outcome IS the answer (fog covers viewport correctly under all gestures ⇒ FOG-12/13 working as expected; viewport gap surfaces ⇒ open audit + redesign in Plan 03.1-12+). No audit task in this plan. If Walk #4 surfaces fog-coverage anomalies, the follow-up plan opens with the audit + Canvas-rotation-aware compensation re-design (FOG-16 path (b) territory).

2. **MIRK-01 architectural ABI formalization.** Standardized contract between `_FogPainter` and any conforming shader (camera-derived inputs forwarded through standard uniforms; any shader implementing the ABI plugs in without painter-side changes). Captured in REQUIREMENTS.md `### Architecture` as deferred. Re-evaluate post-Walk-4 verdict if useful — the existing Plan 03.1-10 implementation (`uPixelOrigin` slot 3..4 + FOG-17a CPU decomposition) is conceptually aligned with MIRK-01 and serves as the seed for any future formalization.

3. **MIRK-01 acceptance test trio** (static (a) uniform-list inspection + behavioural (b) swap-shader test + invariant (c) zero-painter-branching audit). Tied to MIRK-01 formalization; deferred with it. Discuss-phase decision was "trio complet" but reframed by the developer's "architecture is malleable" clarification. No Tasks A/B/C in this plan.

4. **ABI uniform rename `uPixelOrigin` → `uWorldOffset`.** Tied to MIRK-01 ABI formalization; deferred with it. Existing `uPixelOrigin` slot 3..4 stays as-is across `atmospheric_fog.frag` + `atmospheric_fog_debug_spiral.frag` + `FogShaderUniforms` + recording renderer + tests + JSONL `uOffsetX*` legacy field names. No rename task in this plan.

5. **JSONL field rename** `uOffsetX*` → `uWorldOffsetX*` — deferred indefinitely; legacy names kept for grep-tool compatibility with Walks #1/2/3/3b session logs.

6. **Architecture-level CI test** for the conceptual MIRK-01 ABI (e.g., a static lint asserting `_FogPainter.paint()` contains no per-shader branching). Deferred with MIRK-01.

If Walk #4 returns `CONFIRMED-AFTER-FIX`, the post-Walk-4 conversation can revisit any of these on a per-item utility basis — none are blockers for the Phase 3 hypothesis closure or for the Phase 4/5 unblock. If Walk #4 returns `DENIED-FINAL` or `ITERATING`, items 1 + 2 may become directly load-bearing for the follow-up Plan 03.1-12+ (audit-driven Canvas-frame redesign or shader-stack pivot).
</out_of_scope>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the pre-walk `03.1-FALSIFICATION-4.md` skeleton + run the pre-walk gate sequence</name>
  <files>.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-4.md, .planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-4.md</files>
  <action>
    Create `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-4.md`. Mirror the structural shape of `03.1-FALSIFICATION-3.md` with the specialisations described in the §Interfaces block above. Use the executor's actual current date for the `Created:` field; leave `Walked on:` and `Verdict:` as `TBD` until Task 2 fills them.

    Top-of-file metadata block:
    ```markdown
    # Phase 3.1 Falsification — Walk #4 (Post-Plan-03.1-10 World-Coordinate Noise + Rotation Disable Fix Bundle)

    **Created:** 2026-05-XX  (executor: fill in real date — likely 2026-05-04 or later, on the day the pre-walk gates run)
    **Phase:** 03.1-fix-fog-pan-translation
    **Walked on:** TBD  (filled post-walk: iPhone 17 Pro, central Melun, CI run `<RUN_ID>`, SHA `<SHA>`)
    **Verdict:** TBD  (filled post-walk: `CONFIRMED-AFTER-FIX` | `DENIED-FINAL` | `ITERATING — see 03.1-12+`)

    ## Relationship to Walk #3 Verdict

    Walk #3 (Plan 03.1-09, 2026-05-03) closed `ITERATING-WITH-PARTIAL-PROGRESS-AND-ROTATION-REGRESSION`. Two findings: (1) Q1 PARTIAL — Plan 03.1-07 Branch B-3 reduced wrap frequency from viewport-width (~390 px) to noise-tile period (~16-65 px) but stepping discontinuity persisted at wrap events. (2) Q2 NEW failure — Plan 03.1-08's `canvas.translate(-canvasOffset)` compensates ONLY translation; rotation component (matrix[0,1,4,5]) was untouched, so MobileLayerTransformer rotation during pinch-zoom-rotate caused fog rect to rotate with canvas leaving wedges of un-fogged map at viewport corners. Three new requirements opened: FOG-16 (rotation handling), FOG-17 (world-coordinate noise sampling), UX-02 (disable rotation — developer-endorsed POC scope reduction). Plus FOG-17a precision pairing (CPU-side integer/fractional decomposition) added during Plan 03.1-10 planning to address fp32 precision degradation that pure FOG-17 would expose at high zoom.

    Walk #3b (the supplementary marker walk on the same SHA, documented in `03.1-WALK-3B-MARKER-ANALYSIS.md`) provided quantitative empirical anchors: 8 markers across ~24 sec, median 39 raw px between markers, 2.4 wrap events per perceived step, single-zoom session at pixelOriginX ~1.064M (NOT the high-zoom regime; FOG-17a precision pairing is motivated by Walk #2's 4.26M observation, not directly by Walk #3b). The Walk #3b analysis confirmed the FALSIFICATION-3 mechanistic interpretation that B-3 wraps fire at noise-tile-period frequency.

    Plan 03.1-10 closed UX-02 + FOG-16 path (a) + FOG-17 + FOG-17a software-side via the layered fix bundle: FlutterMap.interactionOptions clears the rotate flag bit; production + debug-spiral shaders both adopt `worldPx = fragUv * uResolution + uPixelOrigin; noiseUv = worldPx / kNoiseTilePx`; `_FogPainter.paint()` decomposes camera.pixelOrigin into (intPxX, intPxY, fracPxX, fracPxY) and forwards a bounded composite (intPx % kPocFogIntegerWrapPeriodPx + fracPx) to the shader.

    Walks #1 + #2 + #3 + Walk #3b records (`03.1-FALSIFICATION.md`, `03.1-FALSIFICATION-2.md`, `03.1-FALSIFICATION-3.md`, `03.1-UAT.md`, `03.1-UAT-2.md`, `03.1-UAT-3.md`, `03.1-WALK-3B-MARKER-ANALYSIS.md`) AND Phase 3 record (`03-FALSIFICATION.md`) ALL stand as historical record — UNTOUCHED. Walk #4's verdict is a SEPARATE artefact in this file.

    ## Hypothesis

    After the Plan 03.1-10 layered software fix bundle (UX-02 disable rotation + FOG-17 world-coordinate noise sampling in BOTH atmospheric_fog.frag + atmospheric_fog_debug_spiral.frag + FOG-17a CPU-side integer/fractional decomposition keeping shader input bounded under ~1537 raw px), the same-Canvas fog now: (a) translates with the camera during pan; (b) maintains a fully stable noise pattern across consecutive paints — the world-coordinate formulation scrolls NEW noise in from the viewport edge as camera pans, no fract-wrap, no stepping; (c) keeps the reveal hole co-located with the blue dot during pan/zoom (FOG-12 retained); (d) covers the entire viewport at any canvas-translation magnitude (FOG-13 retained); (e) is structurally invulnerable to the rotation-correlated fog mis-coverage failure mode (UX-02 prevents MapCamera.rotation from changing).

    Re-stated against the original POC core value: the fog-of-war stays perfectly locked to the map during pan, zoom, and combined gestures on a sideloaded iOS build. **CONFIRMED-AFTER-FIX** means the Phase 3 hypothesis is reinstated and the POC ports back to MirkFall with all of Plan 03.1-02 + 03.1-04 + 03.1-05 + 03.1-07 + 03.1-08 + 03.1-10 fixes included; **DENIED-FINAL** means even the layered fix bundle including world-coordinate sampling doesn't deliver fog-camera-tracking parity; **ITERATING** means a residual failure mode surfaced — most likely candidate: integer-wrap stepping at sustained one-direction pan (every ~128 sec at typical pan velocity per the Plan 03.1-10 noise-function inspection).

    ## Falsification Criteria (written BEFORE the walk)

    ### Criterion A — Frame-delta probe thresholds (PERF-07 re-validation, quantitative)

    Same thresholds as Walks #1 + #2 + #3: `medianMs ≤ 16`, `p95Ms ≤ 32`, `maxMs ≤ 48` across ≥ 10 deliberate combined gestures. Walk #1: 4-7×; Walk #2: 3.9-5.4×; Walk #3: implicit (no log Mail-shared); Walk #3b: 40-110× headroom on a 33-sec window. Walk #4 should preserve at least Walk #2 levels (Plan 03.1-10's Dart-side decomposition adds ~5 cycles per paint — negligible; the shader's per-fragment world-coordinate computation is also negligible).

    ### Criterion B — Subjective visual lock (PERF-05 carry-over, re-tested)

    Developer's verbal verdict at end of walk:
    - **Q1 — No perceived hard-step events during pure pan** (Walk #3 fail — Plan 03.1-10 FOG-17 + FOG-17a target this). Sub-test: developer pans for ≥ 60 seconds in one direction. The world-coordinate formulation should produce monotonic noise scrolling — new noise enters from the viewport edge as the camera moves, no fract-wrap, no perceived stepping. Document the duration of one-direction pan during which Q1 was tested in the Walk Evidence section. If pan duration exceeds ~128 sec at typical velocity, an integer-wrap event may fire — at the wrap boundary the visible noise pattern may shift sub-perceptibly (per the Plan 03.1-10 noise-function inspection: integer wrap preserves the noise grid for the base octave but introduces sub-grid shift for the FBM-rotated octaves). If developer perceives a wrap event, capture verbatim observation + dev-marker tap timing.
    - **Q2 — No rotation-correlated fog mis-coverage** (Walk #3 NEW fail — Plan 03.1-10 UX-02 targets this). Sub-test: developer attempts a deliberate two-finger rotate gesture. The map should NOT rotate (MapCamera.rotation stays 0.0; MapCompass widget stays at 0°). The fog viewport coverage should remain correct throughout the rotate-attempt gesture (no wedges of un-fogged map at viewport corners; the rotate gesture is functionally a no-op).
    - **Q3 — No reveal-hole offset from blue dot during gesture** (Walks #1 + #2 + #3 confirmed; re-confirmation under Plan 03.1-10's coordinate-system rewrite).
    - **Q4 — Fog visually translates with camera during pan** (Walks #1 + #2 + #3 confirmed; re-confirmation; Plan 03.1-10 doesn't touch the translation path).
    - No white-ellipse during fast pinch-zoom (already passed Walks #1-3).
    - No inversion at any zoom level (already passed Walks #1-3).

    ### Criterion C — DROPPED

    Stays dropped per Phase 3 + roadmap lock.

    ### Criterion D — Diagnostic stream confirms the layered fix

    Per the `Logger('infrastructure.mirk.fog_transform')` 1-Hz JSONL rollups:

    - **CONFIRMED-AFTER-FIX:** `pixelOriginX/Y` track gesture monotonically. `appliedUOffset` keys (which preserve the JSONL field name for back-compat with Walks #1-3 tooling) now log the bounded composite — values stay under `kPocFogIntegerWrapPeriodPx + 1` (~1537 raw px) regardless of zoom level. Direct visible signature of FOG-17a in effect: post-Plan-03.1-10 a JSONL line at zoom 16 with pxOriginX = 4.26M shows `appliedUOffsetXMin/Median/Max` all in [0, 1537] range, NOT the raw 4.26M magnitude. `canvasTx/Ty` jumps still occur — Plan 03.1-08's `canvas.translate(-canvasOffset)` compensates them mechanically.
    - **DENIED-FINAL pattern:** the diagnostic stream shows the layered fix landed mechanically AT EVERY level (pixelOrigin tracks; appliedUOffset bounded under 1537; canvas.translate compensates rect-cover; UX-02 makes camera.rotation = 0.0) AND the user-observable stepping / mis-coverage STILL persists — root cause is even deeper than the world-coordinate-sampling + rotation-disable; architecture is unfixable in `flutter_map 7.0.2` with the current shader stack.
    - **ITERATING pattern:** subjective verdict surfaces a residual failure mode. Most likely candidates: (i) integer-wrap stepping at sustained one-direction pan (every ~128 sec at 12 raw px/s; Plan 03.1-10 noise-function inspection documented this as the FBM-rotated-octave continuity caveat); (ii) NEW failure mode introduced by Plan 03.1-10 (e.g., world-coordinate sampling exposes an unrelated artefact at extreme zoom; or the FOG-17a decomposition's modulo arithmetic produces a sub-pixel jitter under specific gesture trajectories). The diagnostic stream points at the next fix axis; Plan 03.1-12+ is opened.

    ## Walk Plan

    Identical to Plan 03.1-09 (Walk #3) — same theatre, duration, gestures, HUD, post-walk Mail-share. Two new additions for Walk #4:
    - **NEW: explicit dev-marker-button taps for residual symptoms.** The marker button at the AppBar (commit `79ab978`) is operational; Walk #3b proved it produces per-marker JSONL events tagged with the marker timestamp. Developer is instructed to tap the marker every time they perceive ANY residual symptom (steppy translation, fog mis-coverage, anything visually anomalous). The Walk #3b marker-correlation analysis template (`03.1-WALK-3B-MARKER-ANALYSIS.md`) applies if markers fire.
    - **NEW: Mail-share discipline RE-ESTABLISHED.** Walk #3 broke the post-walk Mail-share pattern (verbal evidence only). Walk #4 explicitly requires the developer to share the session log via Mail post-walk so the JSONL streams (frame_delta + SDF + fog_transform) can be grep-correlated.

    - **Where:** Central Melun (lat 48.5397, lon 2.6553 area).
    - **Duration:** ≥ 5 minutes continuous walk with the IPA running and the device awake.
    - **Gestures:** ≥ 10 deliberate combined pinch-zoom-and-pan gestures, ≥ 3 recenter FAB taps. AT LEAST ONE deliberate two-finger rotate-attempt gesture (validates UX-02 / Q2 sub-claim — the gesture should be a no-op). AT LEAST ONE sustained ≥ 60-second one-direction pan (validates Q1 across the integer-wrap-period regime).
    - **Pre-walk gate (unit tests):** `flutter test` GREEN (158+ existing + Plan 03.1-10 new tests: FOG-17 4 sub-tests + FOG-17a 5 sub-tests + UX-02 rotation-disabled assertion + REWRITTEN FOG-14a/b sub-tests); `flutter analyze --fatal-infos` 0 warnings; `dart format --line-length 160 --set-exit-if-changed .` clean; `dart test tool/test/` GREEN.
    - **Pre-walk gate (CI):** `gh run list --branch main --limit 5` shows `success` for the post-Plan-03.1-10 SHA; `gh run view <RUN_ID>` confirms `gates`, `build-android`, `build-ios` all green; IPA downloaded via `gh run download <RUN_ID> --name ios-unsigned --dir .uat-tmp/`.
    - **Pre-walk gate (sideload):**
      1. Open `/sanity` route — confirm production fog renders with circular reveal hole (debug-spiral toggle OFF default).
      2. Toggle the debug-spiral switch ON (either on `/sanity` AppBar OR on `/map` AppBar — global toggle landed in Plan 03.1-08 mid-checkpoint corrective bundle commit `f707226`) — confirm the digit-atlas spiral pattern renders with visible numbered cells (DEBUG-01 walk-time validation; the post-Plan-03.1-10 cell numbering reflects the world-coordinate formulation).
      3. Toggle OFF — confirm production fog returns.
      4. Confirm the `Icons.arrow_back` AppBar leading button still works on `/sanity` (UX-01 retained).
      5. Confirm zero shader-compile exceptions in the FileLogger output.
      6. **NEW for Walk #4:** attempt a two-finger rotate gesture on `/map` — confirm the map does NOT rotate (MapCompass stays at 0°; MapCamera.rotation stays 0.0). UX-02 walk-time validation.
    - **In-app HUD during walk:** FpsCounterOverlay (top:8, right:8) + MapCompass (top:56, right:8 — should stay at 0° throughout per UX-02) + FrameDeltaProbeOverlay (top:104, right:8).
    - **Post-walk:** share the session log file via Mail (LOG-04 round-trip — REQUIRED, not optional like Walk #3); paste the relevant frame-delta + SDF + fog-transform JSONL lines into the Walk Evidence section below; if the dev-marker fired, mirror the Walk #3b marker-correlation analysis template; write the subjective verdict (Criterion B) by hand; verbal `approved` is the gate (or `denied:` / `iterating:` for failure / next-iteration paths).

    ## Walk Evidence (filled AFTER the walk by Plan 03.1-11 Task 3)

    ### Pre-walk shader-sanity gate (`/sanity` route + UX-01 back-button validation + DEBUG-01 toggle live-test + UX-02 rotation-disabled walk-time validation)
    _TBD — filled post-walk._

    ### Probe rollup (frame-delta JSONL — `Logger('infrastructure.mirk.frame_delta')`)
    _TBD — filled post-walk. Expected: median ≤ 16 ms, p95 ≤ 32 ms, max ≤ 48 ms across ≥ 10 combined gestures (PERF-07 re-validation; Walk #1 4-7×; Walk #2 3.9-5.4×; Walk #3b 40-110×; Walk #4 should preserve at least Walk #2 levels)._

    ### SDF rebuild rollup (`Logger('infrastructure.mirk.sdf')` — PERF-08 status)
    _TBD — filled post-walk. PERF-08 was falsified-by-walk-2 (rebuild rate 1-121/sec); Walk #3b confirmed thrash persists at low pan (max 48/sec). Walk #4 re-measures to capture if the layered Plan 03.1-10 changes shifted the gesture pattern's hash trajectory (no Plan 03.1-10 work targeted PERF-08 — likely unchanged; deferred to Plan 03.1-12+ if Walk #4 surfaces it as a higher-tier concern)._

    ### Fog-transform rollup (`Logger('infrastructure.mirk.fog_transform')`)
    _TBD — filled post-walk. **KEY FOG-17a SIGNATURE:** `appliedUOffsetXMin/Median/Max` values should be in [0, kPocFogIntegerWrapPeriodPx + 1] = [0, 1537] range across ALL rollups even when pixelOriginXMin/Median/Max reaches millions. If appliedUOffset values are STILL in the millions range, the FOG-17a decomposition didn't land at the call site (regression). `canvasTx/Ty` jumps may still occur — Plan 03.1-08 FOG-13 compensates them mechanically._

    ### FPS observations (PERF-03 — coarse FpsCounterOverlay reading)
    _TBD — filled post-walk._

    ### Marker correlation (Walk #3b template applied if dev-marker fired)
    _TBD — filled post-walk. If the dev-marker fired during Walk #4, mirror the Walk #3b template: per-marker pixelOrigin delta, marker-to-marker pan magnitude, correlation against the integer-wrap-period boundary (was the marker tap on or near an integer-wrap event?). If no marker fired, this section reads "No dev-markers fired — developer perceived no residual stepping or mis-coverage symptoms during the walk."_

    ### Subjective verdict (Criterion B)
    _TBD — filled post-walk. Developer's verbatim words + interpretation paragraph mapping each Criterion B sub-claim to PASS/FAIL._

    ## Verdict

    - [ ] Criterion A passed? — TBD post-walk.
    - [ ] Criterion B passed? — TBD post-walk.
    - [ ] Criterion D passed? — TBD post-walk.

    **Outcome:** TBD.

    **MirkFall migration recommendation:** TBD.
    ```

    Then create `.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-4.md` as the thin Walk #4 transcript skeleton — mirror `03.1-UAT-3.md`'s structure with metadata block + Walk Protocol + empty `## Walk Evidence` section + `## Verdict` placeholder + `## Deviations / Surprises` placeholder + `## Summary` placeholder. The frontmatter `gaps:` YAML stays empty until Task 3 fills it (or stays empty on `approved` / `denied` outcomes).

    NOW execute the pre-walk gate sequence:
    1. `flutter test` (must report 0 failures, 0 reds — Phase 1+2+3 retained tests + Plan 03.1-10 new tests all GREEN; the FOG-09 vacuous-test guard stays SKIPPED)
    2. `flutter analyze --fatal-infos` (must exit 0)
    3. `dart format --line-length 160 --set-exit-if-changed .` (must exit 0)
    4. `dart test tool/test/` (must report all GREEN)
    5. `gh run list --branch main --limit 5` (capture the post-Plan-03.1-10 SHA + run ID; the run must be `success`)
    6. `gh run view <RUN_ID>` (confirm `gates`, `build-android`, `build-ios` all `success`)
    7. `gh run download <RUN_ID> --name ios-unsigned --dir .uat-tmp/` (produces `.uat-tmp/mirk-poc-debug-unsigned.ipa`)

    If ANY pre-walk gate fails, STOP and report. Do NOT proceed to Task 2 until every gate is green.
  </action>
  <verify>
    <automated>flutter test 2>&amp;1 | tail -3 &amp;&amp; flutter analyze --fatal-infos 2>&amp;1 | tail -3 &amp;&amp; dart format --line-length 160 --set-exit-if-changed . 2>&amp;1 | tail -3 &amp;&amp; dart test tool/test/ 2>&amp;1 | tail -3 &amp;&amp; gh run list --branch main --limit 1 2>&amp;1 | tail -3</automated>
    <file_must_exist>.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-4.md</file_must_exist>
    <file_must_exist>.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-4.md</file_must_exist>
    <file_must_exist>.uat-tmp/mirk-poc-debug-unsigned.ipa</file_must_exist>
  </verify>
  <done>
    - All 4 local pre-walk gates exit 0 (test / analyze / format / tool-tests).
    - CI run on the post-Plan-03.1-10 SHA on `main` is `success` (gates + build-android + build-ios all green).
    - `.uat-tmp/mirk-poc-debug-unsigned.ipa` exists and is non-empty (≥ 5 MB).
    - `03.1-FALSIFICATION-4.md` exists with all top-level sections + the Relationship-to-Walk-3 section. Walk Evidence + Verdict bodies read `TBD`.
    - `03.1-UAT-4.md` exists as a thin transcript skeleton.
    - `git status` shows the existing `03.1-FALSIFICATION.md` + `03.1-FALSIFICATION-2.md` + `03.1-FALSIFICATION-3.md` + `03.1-UAT.md` + `03.1-UAT-2.md` + `03.1-UAT-3.md` + `03.1-WALK-3B-MARKER-ANALYSIS.md` UNCHANGED (Walks #1 + #2 + #3 + Walk #3b records preserved).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: iPhone 17 Pro sideload UAT Walk #4 + post-walk verbal verdict capture</name>
  <files>.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-4.md, .planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-4.md</files>
  <action>
    Hand off to the developer for the manual UAT Walk #4 on iPhone 17 Pro. The full procedure is documented in the checkpoint-specific `<what-built>`, `<how-to-verify>`, and `<resume-signal>` sections — those are the executor-facing instructions. Claude pauses execution, presents the checkpoint to the developer, and waits for the resume signal containing the walk evidence + verdict. After the resume signal is received, Claude proceeds to Task 3 (verdict authoring + REQUIREMENTS/ROADMAP/STATE updates).
  </action>
  <verify>
    <automated>MISSING — PERF-07 + Criterion B + Criterion D (subjective + diagnostic-stream pattern matching + dev-marker correlation if markers fire) are inherently manual UAT walk gates per 03.1-VALIDATION.md Manual-Only Verifications table; no automated proxy exists. The developer&#39;s resume-signal IS the verification.</automated>
  </verify>
  <done>
    Developer returns with one of: (a) verbal `approved` — fog tracks camera during pan + no shimmer + no integer-wrap stepping perceived during ≥ 60-sec one-direction pan + no rotation-correlated mis-coverage (UX-02 makes rotation a no-op) + reveal-hole co-located with blue dot; CONFIRMED-AFTER-FIX. (b) `denied: <verbatim>` — describe the failure mode for diagnostic JSONL correlation. (c) `iterating: <verbatim>` — same as denied: but the developer chooses to iterate (Plan 03.1-12+). Plus the post-walk session log shared via Mail (LOG-04 round-trip — REQUIRED for Walk #4).
  </done>
  <what-built>
    The Plan 03.1-10 software fix bundle is complete:

    - **UX-02 (FOG-16 path (a)):** `lib/presentation/screens/map_screen.dart` — `FlutterMap.options.interactionOptions` flags clear the rotate bit (`InteractiveFlag.all & ~InteractiveFlag.rotate`). MapCamera.rotation stays at 0.0 regardless of two-finger rotate-attempt gestures. MobileLayerTransformer never accumulates rotation matrix elements; the rotation-correlated fog mis-coverage failure mode (Walk #3 sub-section C) is structurally prevented.
    - **FOG-17 (D-1):** `assets/shaders/atmospheric_fog.frag` AND `assets/shaders/atmospheric_fog_debug_spiral.frag` BOTH adopt world-coordinate noise sampling: `vec2 worldPx = fragUv * uResolution + uPixelOrigin; vec2 noiseUv = worldPx / kNoiseTilePx;`. Each fragment samples noise at its own world-pixel position; new noise scrolls in from the viewport edge as the camera pans; no `fract()`, no wrap discontinuities. `kNoiseTilePx` is a shader-local `const float` (NOT a uniform — slot count stays at 41).
    - **FOG-17a (D-1a precision pairing):** `lib/presentation/widgets/fog_layer.dart` `_FogPainter.paint()` decomposes `camera.pixelOrigin` into integer + fractional Dart-side and forwards a bounded composite `(intPx % kPocFogIntegerWrapPeriodPx + fracPx)` to the shader. Shader input magnitude stays under `kPocFogIntegerWrapPeriodPx + 1` (~1537 raw px) regardless of zoom level. fp32 ULP at 1536 is ≈2.4e-4 raw px — three orders of magnitude better than fp32 ULP at 4.26M (≈0.5 raw px).
    - **New constants:** `kPocFogNoiseTilePx = 384.0`, `kPocFogIntegerWrapPeriodPx = 1536.0` (invariant: `kPocFogIntegerWrapPeriodPx % kPocFogNoiseTilePx == 0`).

    CI gates green for new + rewritten tests:
    - `test/presentation/widgets/fog_world_coordinate_noise_test.dart` — 4 sub-tests asserting ZERO wrap-discontinuity events across pixelOrigin magnitudes 1.064M / 4.26M / 17.04M.
    - `test/presentation/widgets/fog_pixel_origin_decomposition_test.dart` — 5 sub-tests asserting bounded shader input + fractional preservation + invariant.
    - `test/presentation/screens/map_screen_test.dart` — UX-02 rotation-disabled assertion.
    - REWRITTEN `test/presentation/widgets/fog_tile_period_invariant_test.dart` — asserts world-coordinate formulation in active code; pre-Plan-03.1-10 fract formulation absent from active code.
    - REWRITTEN `test/presentation/widgets/fog_debug_spiral_continuity_test.dart` — asserts post-Plan-03.1-10 trajectory has ZERO wraps; retains historical pre-fix + B-3 sub-tests.

    Existing tests still GREEN: FOG-04 structural, FOG-07 KEYSTONE, FOG-09 transform-equality, FOG-10 fog_transform_logger, FOG-11 smooth-noise (threshold may have been adjusted in Plan 03.1-10 to track the bounded-composite magnitude), FOG-12 canvas-frame-alignment, FOG-13 rect-viewport-coverage, PERF-08 bbox-quantisation, UX-01 sanity back button.

    Pre-walk gates (Task 1): all 6 GREEN. The IPA at `.uat-tmp/mirk-poc-debug-unsigned.ipa` is ready for sideload.
  </what-built>
  <how-to-verify>
    1. Open SideStore on iPhone 17 Pro. Refresh certificate if needed. Install or refresh `.uat-tmp/mirk-poc-debug-unsigned.ipa`.
    2. Launch `mirk-poc-debug` on iPhone 17 Pro. Allow GPS via the rationale screen → "Allow While Using App".
    3. **Pre-walk sanity gate (production fog + UX-01 + DEBUG-01 walk-time validation + UX-02 rotation-disabled walk-time validation):** tap the science (`Icons.science`) button in the AppBar to navigate to `/sanity`.
       - Confirm the production fog renders with circular reveal hole.
       - Confirm an `Icons.arrow_back` button appears in the leading slot (UX-01 retained).
       - Confirm a `Switch.adaptive` toggle for "Debug Spiral" appears in the AppBar actions (DEBUG-01).
       - Toggle the debug spiral ON. Confirm the digit-atlas spiral pattern renders. (Note: the spiral cell numbering will reflect the post-Plan-03.1-10 world-coordinate formulation — the digit cells now scroll smoothly with camera motion rather than wrapping at noise-tile period; this is the visual confirmation that FOG-17 landed.)
       - Toggle OFF. Confirm production fog returns.
       - Tap the back button. Confirm it returns to `/map`.
       - **NEW: UX-02 walk-time validation.** On `/map`, attempt a deliberate two-finger rotate gesture (place two fingers on the map; rotate them around their midpoint by ~30°). Confirm:
         - The map does NOT rotate (the tile content stays in the same orientation).
         - The MapCompass widget stays at 0° (the small compass in the top-right corner under the FPS counter does not move).
         - The pan + pinch-zoom components of the gesture (if any incidental motion) still work normally — only the rotation component is disabled.
    4. **Walk the route:** ≥ 5 minutes continuous through central Melun (same theatre as Walks #1-3). During the walk:
       - Perform ≥ 10 deliberate combined pinch-zoom-and-pan gestures.
       - Perform ≥ 3 recenter FAB taps.
       - **NEW: Perform AT LEAST ONE sustained ≥ 60-second one-direction pan** (drag the map continuously in one direction for ≥ 60 sec). This validates Q1 across the integer-wrap-period regime (1536 raw px ≈ 128 sec at typical 12 raw px/s). If an integer-wrap event fires during this sustained pan, the developer may perceive a sub-perceptible noise-pattern shift — capture verbatim observation if so.
       - **NEW: Perform AT LEAST ONE deliberate two-finger rotate-attempt gesture** during normal walk (not just at /sanity). Confirm UX-02 holds during walk-time gesture conditions.
       - Watch the FrameDeltaProbeOverlay (top:104, right:8) — note approximate `med` / `p95` / `max` values during combined gestures.
       - **NEW: Tap the dev-marker button** every time the developer perceives ANY residual symptom (steppy translation, fog mis-coverage, anything visually anomalous). The marker button currently hardcodes `tag: "steppy_translation"` but tag-disambiguation is post-hoc via observation timing.
       - **The KEY observations** (the four Walk #3 failure modes the layered fix targets):
         - **Q1: No perceived hard-step events during pure pan?** (Walk #3 fail — Plan 03.1-10 FOG-17 + FOG-17a target this). Walk #3b empirical anchor: 8 markers across ~24 sec at noise-tile period (~16-65 raw px between markers). Walk #4 expectation: ZERO markers fire during the ≥ 60-sec one-direction pan (the world-coordinate formulation eliminates wrap events within the integer-wrap window).
         - **Q2: No rotation-correlated fog mis-coverage during rotate-attempt gestures?** (Walk #3 fail — Plan 03.1-10 UX-02 targets this). Walk #4 expectation: rotate-attempt gestures are no-ops; the fog covers the viewport correctly throughout.
         - **Q3: Reveal hole co-located with the blue dot during pan/zoom?** (Walks #1-3 retained — re-confirmation).
         - **Q4: Fog visually translates with the map during pan?** (Walks #1-3 retained — re-confirmation).
    5. **Post-walk:** tap the share button in the AppBar → pick Mail → send the session log to your own email → save the attached `yyyymmdd_hhmmss_logs.txt` from your inbox. **THIS IS REQUIRED for Walk #4** — Walk #3 broke this discipline; without the JSONL streams the diagnostic anchor is verbal-only.
    6. **Capture the verdict:** describe in your own words whether each of the four Walk #3 failure modes is now resolved, and whether the dev-marker fired any times during the walk (and at what observation). Single-word `approved` is the gate for `CONFIRMED-AFTER-FIX`. Any residual failure mode (sub-perceptible integer-wrap shift, NEW failure mode, etc.) should be described — that description feeds the diagnostic JSONL grep step.
    7. **Post-walk grep correlation** (executor runs after the verbal verdict is captured):
       - `grep "infrastructure.mirk.frame_delta" <log-file> > .uat-tmp/walk4_frame_delta.jsonl`
       - `grep "infrastructure.mirk.sdf" <log-file> > .uat-tmp/walk4_sdf.jsonl`
       - `grep "infrastructure.mirk.fog_transform" <log-file> > .uat-tmp/walk4_fog_transform.jsonl`
       - `grep "dev_marker" <log-file> > .uat-tmp/walk4_dev_markers.jsonl` (if any markers fired)
       - For each line, parse the JSON, extract `epochSecond`, produce a per-second composite row. Pipe into `03.1-FALSIFICATION-4.md`'s Walk Evidence subsections.
       - **If markers fired:** mirror the `03.1-WALK-3B-MARKER-ANALYSIS.md` template to produce a marker-correlation table.

    **Expected `CONFIRMED-AFTER-FIX` evidence pattern:**
    - Walk evidence: developer says `approved` — no perceived steps; rotate gestures are no-ops; reveal hole co-located with blue dot; fog tracks camera.
    - `frame_delta` rollup: PERF-07 thresholds satisfied with similar headroom to Walks #1-3.
    - `fog_transform` rollup: `pixelOriginX/Y` monotonic; `appliedUOffsetXMin/Median/Max` values bounded under 1537 raw px across ALL rollups (FOG-17a signature); `canvasTx/Ty` may still jump but FOG-13 compensates.
    - `dev_markers` JSONL: empty (no markers fired during the walk).

    **DENIED-FINAL or ITERATING patterns** are documented in `03.1-FALSIFICATION-4.md` §Criteria — pattern matching uses the diagnostic stream + (if markers fired) marker-correlation analysis to identify residual fix axes (or, in DENIED case, the architectural-counter-evidence finding).
  </how-to-verify>
  <resume-signal>
    Type ONE of:
    - `approved — Q1-stepping-resolved + Q2-rotation-disabled + Q3-reveal-hole-aligned + Q4-fog-translates-with-camera` — all four Q1-Q4 sub-claims confirmed by developer. CONFIRMED-AFTER-FIX. Executor proceeds to Task 3 (Branch A) and flips FOG-04..07 + FOG-11 + FOG-15 + FOG-16 + FOG-17 + UX-02 + PERF-07 to Complete. If any single sub-claim fails, use `denied: <which Qx failed + verbatim>` or `iterating: <which Qx failed + verbatim>` instead.
    - `denied: <verbatim description of the residual failure mode>` — describe what went wrong. Executor opens the post-walk JSONL grep results, correlates against the failure description, and reports back: either (a) recommend a Plan 03.1-12+ follow-up fix attempt with a specific diagnostic-anchored hypothesis (e.g., FBM-rotated-octave continuity caveat manifested as integer-wrap stepping; pivot to periodic Worley/explicit-period Perlin), or (b) recommend `DENIED-FINAL` if the diagnostic evidence shows the failure is structural (Phase 3.2 alternative-renderer pivot conversation).
    - `iterating: <verbatim description>` — same as `denied:` shape but the developer chooses to iterate per CONTEXT §Iteration policy. Executor opens a Plan 03.1-12 in a follow-up plan-phase invocation.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Author the Walk #4 verdict — fill `03.1-FALSIFICATION-4.md` Walk Evidence + Verdict; update REQUIREMENTS.md / ROADMAP.md / STATE.md</name>
  <files>.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-4.md, .planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-4.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
    Branch on the developer's verdict from Task 2:

    **Branch A — `approved` → CONFIRMED-AFTER-FIX:**

    1. Fill `03.1-FALSIFICATION-4.md` Walk Evidence subsections with post-walk grep correlation. The fog_transform rollup MUST show `appliedUOffsetXMin/Median/Max` bounded under 1537 raw px across ALL rollups — direct visible signature of FOG-17a in effect. If markers DID fire, populate the Marker correlation section with the Walk #3b template applied; if no markers fired, document "No dev-markers fired — developer perceived no residual stepping or mis-coverage symptoms during the walk (the world-coordinate noise sampling + integer/fractional decomposition + rotation disable layered fix bundle delivered the user-observable closure Walk #3 verdict iterated toward)."

    2. Fill `03.1-FALSIFICATION-4.md` Verdict section:
       ```markdown
       ## Verdict

       - [x] Criterion A passed? — **YES.** Frame-delta probe rolling rollup over ≥ 10 combined gestures: median = {N} ms (≤ 16 ✓), p95 = {N} ms (≤ 32 ✓), max = {N} ms (≤ 48 ✓). PERF-07 re-validated post-Plan-03.1-10.
       - [x] Criterion B passed? — **YES.** Developer's verbatim verdict: *"<verbatim>"*. All four Walk #3 failure modes resolved: Q1 fog stepping eliminated (FOG-17 world-coordinate noise sampling produces monotonic noise scrolling; FOG-17a precision pairing keeps shader input bounded; ≥ 60-sec one-direction pan completed without perceived stepping); Q2 rotation-correlated mis-coverage structurally absent (UX-02 disables rotation; rotate-attempt gestures are no-ops); Q3 reveal hole co-located with blue dot (FOG-12 retained); Q4 fog translates with camera (Plans 03.1-02 + 03.1-04 retained).
       - [x] Criterion D passed? — **YES.** Diagnostic stream confirms the layered fix. `appliedUOffset` values bounded under 1537 raw px across ALL rollups (FOG-17a signature). No dev-markers fired. `canvasTx/Ty` jumps still occur — Plan 03.1-08 FOG-13 compensates them mechanically.

       **Outcome:** **CONFIRMED-AFTER-FIX.**

       The same-Canvas fog hypothesis is **reinstated** with the layered Plan 03.1-02 + 03.1-04 + 03.1-05 + 03.1-07 + 03.1-08 + 03.1-10 fix bundle. Phase 3's `DENIED 2026-05-01` verdict and Phase 3.1 Walks #1 + #2 + #3's `ITERATING-WITH-*` verdicts are converted to `CONFIRMED-AFTER-FIX` after Walk #4.

       **MirkFall migration recommendation:** **PORT BACK** with the full layered fix bundle:
       1. `_FogPainter.paint()` derives `pixelOrigin` from `camera.pixelOrigin` (Plan 03.1-02).
       2. `atmospheric_fog.frag` slot 3..4 uniform `uPixelOrigin` (renamed from `uOffset`) (Plan 03.1-04).
       3. `_FogPainter.paint()` reads `canvas.getTransform()` and forwards `Offset(matrix[12], matrix[13])` to `computeFogClipPath(canvasOffset: ...)` (Plan 03.1-05).
       4. `SdfCache._hash(...)` quantises viewport bbox to 1e-4 lat/lon (Plan 03.1-05; performance-only).
       5. `_FogPainter.paint()` calls `canvas.translate(-canvasOffset)` immediately after `canvas.clipPath(clipPath)` (Plan 03.1-08).
       6. `atmospheric_fog.frag` AND `atmospheric_fog_debug_spiral.frag` both adopt `worldPx = fragUv * uResolution + uPixelOrigin; noiseUv = worldPx / kNoiseTilePx;` formulation (Plan 03.1-10 FOG-17).
       7. `_FogPainter.paint()` decomposes `camera.pixelOrigin` into integer + fractional and forwards bounded composite `(intPx % kPocFogIntegerWrapPeriodPx + fracPx)` to shader (Plan 03.1-10 FOG-17a).
       8. `FlutterMap.options.interactionOptions` clears the rotate flag (Plan 03.1-10 UX-02). MirkFall donor MAY re-enable rotation if the canvas-rotation-aware compensation is implemented as part of the port-back work (FOG-16 path (b) full canvas-inverse-transform).
       9. (Optional) The DEBUG-01 debug-spiral infrastructure is OPTIONAL for port-back — permanent diagnostic tooling, useful but not required for production fog correctness.

       The MirkFall donor `_FogPainter.paint()` call site MUST be reviewed against ALL eight production-impacting changes at port-back time.

       **POC scope reduction note (UX-02 + FOG-16):** The POC ships with rotation disabled. If MirkFall after port-back wants to re-enable rotation, the canvas-rotation-aware compensation (FOG-16 path (b) — read full canvas matrix; apply inverse via `canvas.transform(matrix.inverted())`) must be implemented before re-enabling. This is a documented post-POC iteration; the current PORT BACK recommendation includes the rotation-disabled UX as part of the fix bundle.
       ```

    3. Update `.planning/REQUIREMENTS.md`:
       - FOG-04, FOG-05, FOG-06, FOG-07: change `[ ]` → `[x]`. Update inline status from `Falsified-in-production` to `Complete — Verified-by-walk (P03.1-11 CONFIRMED-AFTER-FIX 2026-05-XX after layered Plan 03.1-02+04+05+07+08+10 fix bundle)`.
       - FOG-11: change `[ ]` → `[x]`; status `Complete — Verified-by-walk (P03.1-11 — world-coordinate noise sampling closes the shimmer/stepping symptom space FOG-11 CI test was inadequate proxy for; the user-observable noise-pattern stability is now delivered by FOG-17 + FOG-17a)`.
       - FOG-15: status augmented `Complete — Verified-by-test + walk (P03.1-07 Task 3 unit + P03.1-11 walk-time confirmation post-Plan-03.1-10 world-coordinate rewrite; the B-3 partial fix is superseded by the world-coordinate formulation but FOG-15's structural acceptance — a mechanism-confirmed shader-coordinate-system fix lands in `atmospheric_fog.frag` — is satisfied by Plan 03.1-10's world-coordinate formulation as a stronger replacement)`.
       - FOG-16: status augmented `Complete — Verified-by-test + walk (P03.1-10 path (a) + P03.1-11 walk-time validation — rotate-attempt gestures during walk are no-ops; MapCamera.rotation stays 0.0 throughout)`.
       - FOG-17: status augmented `Complete — Verified-by-test + walk (P03.1-10 unit + P03.1-11 walk-time confirmation — ≥ 60-sec one-direction pan completed without perceived stepping; appliedUOffset JSONL signatures bounded under 1537 raw px confirming FOG-17a in effect)`.
       - UX-02: status augmented `Complete — Verified-by-test + walk (P03.1-10 widget test + P03.1-11 walk-time validation)`.
       - PERF-07: status `Complete — Re-measured (P03.1-11)`. Include actual Walk #4 numbers.
       - PERF-08: stays falsified-by-walk-2 (no Plan 03.1-10 work targeted it; deferred to post-POC if it becomes a higher-tier concern).
       - PERF-03 / PERF-04 / PERF-05: update statuses to reflect re-measurement at Walk #4.
       - Append a new entry to `## Revisions`:
         ```
         - **2026-05-XX (Phase 3.1 closure, Plan 03.1-11 SUMMARY — HYPOTHESIS CONFIRMED-AFTER-FIX):** Plan 03.1-11 sideload UAT Walk #4 on iPhone 17 Pro against CI run `<RUN_ID>` (SHA `<SHA>`) over central Melun delivered a **CONFIRMED-AFTER-FIX** verdict after the layered Plan 03.1-02 + 03.1-04 + 03.1-05 + 03.1-07 + 03.1-08 + 03.1-10 fix bundle. Developer's verbatim words: *"<verbatim>"*. All four Walk #3 failure modes resolved: Q1 fog stepping eliminated (FOG-17 world-coordinate noise sampling); Q2 rotation-correlated mis-coverage structurally absent (UX-02 + FOG-16 path (a) disable rotation; rotate-attempt gestures are no-ops); Q3 reveal hole co-located with blue dot (FOG-12 retained); Q4 fog translates with camera (retained). FOG-04..07 + FOG-11 + FOG-15 flip from Falsified-in-production back to Complete — Verified-by-walk. FOG-16 + FOG-17 + UX-02 verified-by-walk in addition to verified-by-test. PERF-07 re-measured. MirkFall migration recommendation: **PORT BACK** with the full 8-architectural-change layered fix bundle (with rotation-disabled UX retained per UX-02; FOG-16 path (b) full canvas-inverse-transform documented as post-POC iteration if MirkFall wants to re-enable rotation). Phase 4 (wisp particles) and Phase 5 (decision gate) **UNBLOCKED**. The new FOG-09..17 + DEBUG-01 + PERF-08 + UX-01 + UX-02 CI gates prevent regression to ANY of the documented Walk #1/2/3 failure modes without a sideload walk. Evidence: `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-4.md` + `03.1-UAT-4.md`. Phase 3.1 closes with all requirement IDs Complete (FOG-09..17, PERF-07, PERF-08-deferred-post-POC, UX-01, UX-02, DEBUG-01) plus the 4 retroactively-Complete (FOG-04..07).
         ```

    4. Update `.planning/ROADMAP.md`:
       - In `## Progress` table: Phase 03.1 row → `Complete (HYPOTHESIS CONFIRMED-AFTER-FIX)`, plans `11/11`, completed `2026-05-XX`.
       - In Phase 4 + Phase 5 rows: change status from `Blocked on Phase 3.1` to `Pending`.
       - In Phase 3 overview line: append `Phase 3.1 reversed the verdict to CONFIRMED-AFTER-FIX 2026-05-XX after a layered Plan 03.1-02+04+05+07+08+10 fix bundle (uniform rename + per-fragment modulo + clip-path shift compensation + B-3 noise-tile-period intermediate fix + canvas.translate symmetric compensation + world-coordinate noise sampling + CPU-side integer/fractional decomposition + rotation disable).`
       - In Phase 4 + Phase 5 overview lines: remove the `**BLOCKED on Phase 3.1**` clause.
       - Mark `03.1-11-PLAN.md` row in Phase 03.1 plans list as `[x]` landed.

    5. Update `.planning/STATE.md`:
       - Frontmatter: `stopped_at: Phase 3.1 CLOSED with HYPOTHESIS CONFIRMED-AFTER-FIX (Walk #4 2026-05-XX); Phase 4 + 5 UNBLOCKED.`; `last_activity` accordingly.
       - Bump `progress.completed_phases` from 3 to 4.
       - `## Current Position`: update to reflect Phase 3.1 closure + Phase 4 unblock.
       - `### Decisions`: append a Phase 3.1 closure entry.
       - `### Blockers/Concerns`: change the `PHASE-3.1-CAMERA-TRANSFORM-WALK3 (NEW ...)` entry to `PHASE-3.1-CAMERA-TRANSFORM-WALK3 (RESOLVED 2026-05-XX by Plan 03.1-11 Walk #4 CONFIRMED-AFTER-FIX after layered Plan 03.1-10 software fix bundle):` and strike-through the original wording. Resolve the `WALK-3-NO-LOG-MAIL-SHARE` blocker (Walk #4 re-established Mail-share discipline successfully).

    6. **DO NOT TOUCH** `.planning/phases/03-fog-of-war-the-hypothesis/03-FALSIFICATION.md` (Phase 3 historical record) OR `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION.md` (Walk #1) OR `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-2.md` (Walk #2) OR `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-3.md` (Walk #3) OR `.planning/phases/03.1-fix-fog-pan-translation/03.1-WALK-3B-MARKER-ANALYSIS.md` (Walk #3b). Verify via `git status` that all five files are unchanged.

    **Branch B — `denied:` → DENIED-FINAL:**

    Mirror Plan 03.1-09 Task 3 Branch B treatment with Walk #4 specifics. Walk Evidence filled with failure-mode evidence + diagnostic-stream pattern matching + (if markers fired) marker-correlation analysis. Verdict captures DENIED-FINAL outcome with MirkFall recommendation `DO NOT PORT BACK` and Phase 3.2 conversation hand-off. REQUIREMENTS.md FOG-04..07 + FOG-11 + FOG-15 stay `Falsified-in-production` with updated revision-clause appending Walk #4 evidence. ROADMAP.md Phase 03.1 row → `Complete (HYPOTHESIS DENIED-FINAL)`. Phases 4 + 5 stay `Blocked` (or transition to `Cancelled` per developer choice — flag in SUMMARY for user decision). FOG-16 + FOG-17 + UX-02 still flip to `Complete — Verified-by-test + walk-time validated where applicable` (the structural fixes ARE in the codebase and walk-time validated structurally — UX-02 disabled rotation is observable; FOG-17 world-coordinate sampling is observable as the bounded `appliedUOffset` JSONL signature; the open question is whether they're SUFFICIENT for fog-camera-tracking parity).

    **Branch C — `iterating:` → defer Plan 03.1-12 to follow-up:**

    Mirror Plan 03.1-09 Task 3 Branch C treatment with Walk #4 specifics. Walk Evidence filled with post-walk grep correlation + (if markers fired) marker-correlation analysis. Verdict: `**Outcome:** ITERATING — see Plan 03.1-12+ for the next fix attempt.`. Do NOT update REQUIREMENTS.md / ROADMAP.md / STATE.md to closure state. Mark Phase 3.1 as `In Progress (5th iteration, walk #5 pending)` in Progress table. UX-02 + FOG-16 + FOG-17 still flip to `Complete — Verified-by-test + walk-time validated structurally` (their CI gates ARE GREEN; structural prevention is observable). FOG-04..07 + FOG-11 + FOG-15 stay `Falsified-in-production`. PERF-07 updated with Walk #4 numbers regardless. Plan 03.1-12 is created via a follow-up `/gsd:plan-phase 3.1 --gaps` invocation. Populate `03.1-UAT-4.md` frontmatter `gaps:` YAML with any opened-by-Walk-#4 gap entries (most likely candidate: integer-wrap stepping at sustained pan; or a NEW failure mode introduced by Plan 03.1-10).

    For all three branches: commit all updates via the standard plan-execution commit pattern. Cross-reference Plan 03.1-11 in commit messages. Do NOT commit `.uat-tmp/` (gitignored) or post-walk `.jsonl` extracts (transient walk artefacts).
  </action>
  <verify>
    <automated>flutter test 2>&amp;1 | tail -3 &amp;&amp; flutter analyze --fatal-infos 2>&amp;1 | tail -3 &amp;&amp; node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" frontmatter validate .planning/phases/03.1-fix-fog-pan-translation/03.1-11-PLAN.md --schema plan</automated>
    <file_must_exist>.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-4.md</file_must_exist>
    <file_must_exist>.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-4.md</file_must_exist>
  </verify>
  <done>
    - `03.1-FALSIFICATION-4.md` Walk Evidence + Verdict sections are filled (no `TBD` strings remaining for the chosen branch — Branches A or B; Branch C explicitly leaves them flagged for Plan 03.1-12).
    - `03.1-UAT-4.md` carries the Walk #4 transcript.
    - REQUIREMENTS.md statuses reflect the verdict outcome (Branch A: all Complete; Branch B: FOG-04..07 + FOG-11 + FOG-15 Falsified-final + UX-02/FOG-16/FOG-17 Complete; Branch C: in-progress for FOG-04..07 + FOG-11 + FOG-15 + UX-02/FOG-16/FOG-17 Complete).
    - ROADMAP.md Progress table Phase 03.1 row reflects the verdict; Phase 4 + Phase 5 statuses updated per branch.
    - STATE.md Current Position + Decisions + Blockers/Concerns updated.
    - Walks #1 + #2 + #3 + Walk #3b + Phase 3 historical records UNCHANGED (verified via `git status`).
    - `flutter test` (full suite) GREEN — closure docs do not touch any production code.
    - Plan frontmatter validation: `{ valid: true }`.
  </done>
</task>

</tasks>

<verification>
After all three tasks complete:
- `03.1-FALSIFICATION-4.md` is filled top-to-bottom with the Walk #4 verdict and walk evidence (or marked `iterating` per Branch C).
- The Phase 3 historical record (`03-FALSIFICATION.md`) AND Walks #1 + #2 + #3 historical records (`03.1-FALSIFICATION.md`, `03.1-FALSIFICATION-2.md`, `03.1-FALSIFICATION-3.md`) AND Walk #3b marker analysis (`03.1-WALK-3B-MARKER-ANALYSIS.md`) are ALL byte-identical to their pre-Plan-03.1-11 states.
- REQUIREMENTS.md, ROADMAP.md, STATE.md all reflect the Walk #4 verdict outcome consistently.
- `flutter test` (full suite) is GREEN — Plan 03.1-11 closure docs do not touch any production code.
- Plan frontmatter validation returns `{ valid: true }`.
</verification>

<success_criteria>
- The iPhone 17 Pro Walk #4 produces a verdict (verbal `approved` / `denied:` / `iterating:`) — the empirical answer to the layered Plan 03.1-10 software fix bundle.
- `03.1-FALSIFICATION-4.md` is the load-bearing artefact capturing the Walk #4 verdict + diagnostic-JSONL evidence stream + (if markers fired) marker-correlation analysis.
- On `CONFIRMED-AFTER-FIX`: Phase 4 + Phase 5 unblock; MirkFall migration recommendation flips to PORT BACK with the full 8-change layered fix bundle (including rotation-disabled UX); Phase 5 closure conversation can proceed; FOG-04..07 + FOG-11 + FOG-15 + FOG-16 + FOG-17 + UX-02 + PERF-07 all flip to Complete.
- On `DENIED-FINAL`: the formal POC architectural counter-evidence is complete; Phase 3.2 alternative-renderer conversation is the only remaining option. UX-02 + FOG-16 + FOG-17 + Plan 03.1-10 tests stay Complete (the fix DID land in software); the failure is downstream of the world-coordinate sampling.
- On `ITERATING`: Plan 03.1-12 is opened in a follow-up invocation with Walk #4's diagnostic stream as input.
- The Phase 3 + Phase 3.1 Walks #1 + #2 + #3 + Walk #3b historical records stay untouched — historical integrity preserved across iteration cycles.
- Mail-share discipline re-established — Walk #4's JSONL streams provide the diagnostic anchor that Walk #3 lacked.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-fix-fog-pan-translation/03.1-11-SUMMARY.md` capturing:
- Pre-walk gate sequence outcome (4 local + CI + IPA download)
- Walk #4 metadata (CI run ID, SHA, walk theatre, walk duration, gesture count, ≥ 60-sec one-direction pan duration, two-finger rotate-attempt observation)
- Developer's verbatim verdict
- Post-walk JSONL grep correlation (per-second composite extract for the ≥ 10-combined-gesture window)
- (If markers fired) marker-correlation analysis mirroring the Walk #3b template
- Walk #4 Verdict outcome (`CONFIRMED-AFTER-FIX` | `DENIED-FINAL` | `ITERATING`)
- Files modified across all three tasks (5 planning docs)
- Any deviations from the plan with Rule classification
- Cross-reference to Phase 4 (the unblocked-or-still-blocked downstream phase) + the migration recommendation hand-off to Phase 5
- Comparison vs Walks #1 + #2 + #3 + Walk #3b numbers — frame_delta headroom preservation, SDF rebuild rate change (PERF-08 status), failure-mode resolution status per Walk #1/2/3 observation tables, marker firing count delta vs Walk #3b
- FOG-17a signature confirmation: `appliedUOffsetXMin/Median/Max` JSONL values bounded under 1537 raw px across ALL rollups (or note the regression if not)
</output>