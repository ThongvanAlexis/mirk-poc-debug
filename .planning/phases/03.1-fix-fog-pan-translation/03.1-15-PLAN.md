---
phase: 03.1-fix-fog-pan-translation
plan: 15
type: execute
wave: 2
depends_on: ["03.1-14"]
files_modified:
  - .planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-6.md
  - .planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-6.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
gap_closure: true
requirements: [FOG-04, FOG-05, FOG-06, FOG-07, FOG-11, FOG-15, FOG-19, DEBUG-02, DEBUG-03, UX-02, PERF-07]
scope_note: |
  Plan 03.1-15 = iPhone 17 Pro Walk #6 sideload UAT against the Plan 03.1-14
  fix bundle (FOG-19 C-b uZoomScale uniform + DEBUG-03 unique-cell-numbers
  diagnostic enhancement). 6th walk in Phase 3.1, 7th iteration overall.

  ## Three-task structure (mirrors Plans 03.1-11, 03.1-13)

  - Task 1 (auto): pre-walk gate sequence + `03.1-FALSIFICATION-6.md` skeleton + `03.1-UAT-6.md` skeleton authoring.
  - Task 2 (checkpoint:human-verify): iPhone 17 Pro sideload UAT Walk #6.
  - Task 3 (auto): post-walk verdict authoring + REQUIREMENTS/ROADMAP/STATE updates + atomic commit.

  ## NEW for Walk #6 (vs Walks #1-#5) — per CONTEXT.md C2' decisions §"Walk shape reframing"

  CONTEXT.md captured 2026-05-04 (commit `6ff67b5`): developer clarified
  *"I never actually walk in melun, I just pan and zoom and look for issue"*.
  All prior walks have been **sideload sessions at desk** with manual
  pan/zoom interaction simulating movement. Updates for Walk #6:

  - **Terminology stays "Walk #N" / `WALK-{N}`** for grep-tool compatibility
    with Walks #1-#5 session logs and existing FALSIFICATION-N.md docs.
    Renaming would break grep-correlation tooling.
  - **Plan body language reframes** "≥5 minutes through central Melun" as
    "≥5 minutes interactive sideload session in central Melun region" — no
    requirement to physically walk.
  - **NEW REQUIRED STEP (closes Walk #5 C3' gap):** explicit "pan
    continuously in one direction past city limits to reach ~50-100 km from
    initial Melun centre" step. **Lives BEFORE the C2' validation regime**
    so the developer reaches the ≥7M raw-px regime under FOG-18 + C-b
    BEFORE exercising zoom transitions. This closes the C3' / DEBUG-02
    explicit walk-time-validation gap that Walk #5 deferred (Walk #5
    reached only 4.26M = MAX zoom at Melun, NOT ~7M+).
  - **C2' / FOG-19 validation regime (NEW, VERDICT-DETERMINING):** at MAX
    zoom, perform ≥5 deliberate zoom in/out transitions; observe whether
    cells stay anchored (success) or still slide (failure or partial).
    QUANTITATIVE measurement enabled by Task A's DEBUG-03 unique cell
    numbers — developer can read off the cell-IDs across consecutive zoom
    transitions to measure any residual drift in cells (e.g., "I started
    looking at cell 5023 and after zoom-out-then-zoom-back-in I'm now at
    cell 5021 — 2 cells of drift").
  - **DEBUG-01 toggle is critical for the C2' validation regime:** the
    developer toggles the debug-spiral on at the `/map` AppBar BEFORE the
    zoom-gesture sequence so the unique cell numbers (DEBUG-03) are visible
    AND so the FOG-19 / C-b cellPx-anchoring takes effect (the debug shader
    consumes uZoomScale identically to the production shader). Walk #6 thus
    validates BOTH shaders inheriting the FOG-19 fix consistently (MIRL
    ABI-extension rule).
  - **Carry-forward Walk #5 regimes:** ≥10 deliberate combined pinch-zoom-
    and-pan gestures + ≥3 recenter taps + ≥1 sustained ≥30-sec one-direction
    pan at MAX zoom + ≥1 deliberate two-finger rotate-attempt (UX-02
    walk-time validation) + post-walk Mail-share + pre-walk `/sanity`
    shader-compile gate (production fog renders + DEBUG-01 toggle works on
    /sanity AppBar AND /map AppBar).

  ## Verdict logic (4 branches; mirrors Plan 03.1-13 structure)

  The verdict-determining axes for Walk #6 are Q1 (max-zoom SNAP — must
  remain CLOSED from Walk #5), Q1b (zoom-gesture residual — must CLOSE
  via FOG-19), Q3 (reveal hole), Q4 (translation), Q5 (C3' stress-test
  at distance — explicit walk-time-validation), and PERF-07.

  - **`CONFIRMED-AFTER-FIX (FULL)`** (target outcome — closes Phase 3.1):
    Q1 SNAP CLOSED at MAX zoom (Walk #5 confirmed via FOG-18; Walk #6
    re-validates) + **Q1b CLOSED via C-b** (cells stay anchored across
    zoom transitions; ZERO drift on DEBUG-03 unique cells; OR ≤1-2 cell
    drift = success threshold to be confirmed at execute-phase time
    based on developer's tolerance) + C3' explicit ≥7M extreme-distance
    pass (zero markers fired during ~50-100 km pan; fog renders correctly
    at distance) + Q3/Q4/Q2 retained + PERF-07 retained at Walk #5
    levels. **Phase 3.1 closes.** Phase 4 + Phase 5 unblock. Plan 03-08
    DENIED verdict REVERSED.

  - **`CONFIRMED-AFTER-FIX-PARTIAL`** (less likely now since Q1b is the
    targeted fix, but possible if FOG-19 partially reduces but doesn't
    fully close the drift): Q1 retained + Q1b improved-but-not-fully-
    closed (small residual drift via DEBUG-03 cells) + Q3/Q4/Q2 retained
    + PERF-07 retained. Phase 3.1 stays In Progress (7th iteration
    awaiting refinement plan + Walk #7).

  - **`DENIED-FINAL`** (FOG-19 / C-b is the wrong mechanism interpretation):
    Q1b unchanged or worse despite C-b. Rules out the world-coordinate-
    basis-mismatch mechanism interpretation. Architectural counter-evidence;
    Phases 4 + 5 stay BLOCKED or transition to CANCELLED per developer
    choice.

  - **`ITERATING`** (a new failure mode surfaces): Q1b improves but C-b
    introduces unforeseen artefact at extreme zoom; OR `uZoomScale =
    pow(2, ...)` numerical issues at very low/high zoom; OR C3' explicit
    walk-time-validation surfaces NEW precision artefact at ≥7M raw-px
    that Walk #5 didn't reach (since pixelOrigin pushes well past 4.26M
    when panning ~50-100 km from Melun); OR something unforeseen. Plan
    03.1-16+ pending in follow-up plan-phase invocation. THIS plan does
    NOT author 03.1-16+.

  ## Why a SEPARATE walk plan (mirrors Plans 03.1-03 / 03.1-06 / 03.1-09 / 03.1-11 / 03.1-13)

  Every Phase 3.1 fix bundle has been followed by a dedicated walk plan
  that authors the next-numbered FALSIFICATION + UAT documents. This
  separation:
  - Keeps the fix-axis attribution clean (commits in 03.1-14 are pure
    code; commits here are pure verdict authoring).
  - Lets the walk plan run with its own checkpoint:human-verify task
    without blocking the autonomous fix bundle plan.
  - Preserves the CONTEXT §Iteration policy structure: each walk is a
    falsifiable empirical event with a written-before-walk hypothesis +
    written-after-walk verdict, captured as a load-bearing artefact.

user_setup:
  - service: github-actions
    why: "Wait for CI green on the post-Plan-03.1-14 SHA + download the IPA artifact for the Walk #6 sideload"
    dashboard_config:
      - task: "Confirm CI run status (`gh run list --branch main --limit 5` + `gh run watch <RUN_ID>`)"
        location: "GitHub Actions for the mirk-poc-debug repo"
      - task: "Download IPA artifact via `gh run download <RUN_ID> --name mirk-poc-debug-ios-unsigned-ipa --dir .uat-tmp/`"
        location: "GitHub Actions run detail page (or `gh` CLI)"
  - service: sidestore
    why: "Sideload the unsigned IPA onto iPhone 17 Pro for Walk #6"
    dashboard_config:
      - task: "Open SideStore on iPhone 17 Pro → install/refresh the .uat-tmp/mirk-poc-debug-unsigned.ipa"
        location: "SideStore iOS app + paired desktop pairing-file"

must_haves:
  truths:
    - "A new `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-6.md` exists capturing the Walk #6 verdict. Walks #1 + #2 + #3 + #3b + #4 + #5 falsification documents are LEFT UNTOUCHED — historical record preserved across iteration cycles."
    - "A new `.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-6.md` exists capturing the Walk #6 transcript. Walks #1 + #2 + #3 + #4 + #5 UAT transcripts are LEFT UNTOUCHED."
    - "Pre-walk gate sequence runs cleanly: `flutter test` GREEN (≈183+ tests post-Plan-03.1-14: existing 180 + new fog_zoom_invariant_basis_test.dart sub-tests + 1 skipped); `flutter analyze --fatal-infos` 0 warnings; `dart format --line-length 160 --set-exit-if-changed .` clean; `dart test tool/test/` GREEN; CI green on the post-Plan-03.1-14 SHA on `main`; IPA downloaded via `gh run download` to `.uat-tmp/mirk-poc-debug-unsigned.ipa`."
    - "The iPhone 17 Pro UAT Walk #6 is performed by the user (sideload session at desk; no physical movement required per CONTEXT.md C2' decisions §'Walk shape reframing'). Walk shape: PRE-WALK SANITY GATE (production fog renders at /sanity; DEBUG-01 toggle works on /sanity AppBar AND /map AppBar; UX-01 back-button works on /sanity; two-finger rotate gesture is a no-op on /map per UX-02). MAIN SESSION (≥5 minutes interactive sideload session in central Melun region): ≥10 deliberate combined pinch-zoom-and-pan gestures + ≥3 recenter FAB taps + ≥1 deliberate two-finger rotate-attempt (UX-02 walk-time validation) + ≥1 sustained ≥30-sec one-direction pan at MAX zoom (Q1 re-validation per Walk #5 success — zero markers expected). NEW C3' EXTREME-DISTANCE REGIME (verdict-determining; closes Walk #5 deferred validation): pan continuously in one direction past city limits to reach ~50-100 km from initial Melun centre (cameraConstraint is removed per DEBUG-02; pixelOrigin pushes ≥7M raw px). Tap dev-marker on any precision-induced artefact at distance. Expected outcome (CONFIRMED-AFTER-FIX FULL): zero markers fired; fog renders correctly throughout. **NEW C2' / FOG-19 VALIDATION REGIME (verdict-determining):** developer toggles DEBUG-01 (debug-spiral) ON at /map AppBar; at MAX zoom, performs ≥5 deliberate zoom in/out transitions; observes whether DEBUG-03 unique-numbered cells stay anchored (success: 0 cell drift) or slide (failure: per Walk #5 verbatim 'numbers sliding'). Quantitative reading: developer can capture verbatim 'cell 5023 → cell 5021 = 2 cells drift' OR 'cell 5023 stays at 5023 across all 5 zoom transitions' = success. Carries forward from Walk #5: post-walk Mail-share REQUIRED (session log Mail-shared so JSONL streams can be grep-correlated against any dev-marker timestamps)."
    - "Mail-share discipline preserved (Walk #4 re-established; Walks #5 + #6 retain). Session log Mail-shared so JSONL streams (`infrastructure.mirk.frame_delta` + `infrastructure.mirk.sdf` + `infrastructure.mirk.fog_transform`) can be grep-correlated against any dev-marker timestamps."
    - "Post-walk: developer's verbal verdict (`approved` / `denied:` / `iterating:` / `approved-partial`) is captured. The walk evidence section of `03.1-FALSIFICATION-6.md` is filled with: developer's verbatim words + Q-axis interpretation (Q1 / Q1b / Q2 / Q3 / Q4 / Q5-stress-test / **Q6 NEW: DEBUG-03 cell-drift quantitative reading**) + post-walk grep of `infrastructure.mirk.fog_transform` + `infrastructure.mirk.frame_delta` + `infrastructure.mirk.sdf` rollups + per-second composite extract + (if markers fired) marker-correlation analysis mirroring the Walk #4 + Walk #5 templates."
    - "PERF-07 is re-measured for Walk #6 (expected at Walk #5 levels): frame-delta probe rolling rollup over the ≥10-combined-gesture window with `medianMs ≤ 16`, `p95Ms ≤ 32`, `maxMs ≤ 48`. Walk #5 captured 13×/20×/28× headroom. Walk #6 should preserve at least Walk #5 levels — the FOG-19 uZoomScale uniform addition + the per-fragment `worldPx / uZoomScale` division add minimal arithmetic to the shader's hot path; the DEBUG-03 4-digit-per-cell layout adds at most one additional `sampleDigit()` call per fragment in the debug-spiral hot path (debug only — does not affect production fog frame budget at all)."
    - "JSONL diagnostic stream confirms FOG-19 working as designed: at any zoom, `pixelOriginX/Y` JSONL field tracks `camera.pixelOrigin` magnitude monotonically (FOG-18 + FOG-19 do not introduce any Dart-side modulo or basis transform on the appliedUOffset path). The `appliedUOffset` JSONL field continues to equal `camera.pixelOrigin` exactly (FOG-18 unchanged; FOG-19 introduces zoom-scale on the SHADER side only — not on the Dart-side forwarded uniform). **Q1b validation (NEW for Walk #6):** at zoom-gesture rollups, dev-marker count = 0 (FOG-19 success) OR a SMALL count (CONFIRMED-AFTER-FIX-PARTIAL outcome) instead of Walk #5's 'numbers sliding' verbatim observation. **C3' extreme-distance validation (NEW for Walk #6):** at the ≥7M-raw-px regime (developer panned ~50-100 km from Melun), `uOffsetXMax/uOffsetYMax` exceed 7M, dev-marker count = 0, fog renders correctly. **canvasTx/Ty continues to be identically zero (UX-02 retained — 3rd consecutive walk).**"
    - "Verdict written per the verdict logic in `<scope_note>` §'Verdict logic'."
    - "On `CONFIRMED-AFTER-FIX (FULL)` (TARGET outcome — closes Phase 3.1): ROADMAP.md Phase 03.1 row in the Progress table updates to `Complete (HYPOTHESIS CONFIRMED-AFTER-FIX)`. Phase 4 + Phase 5 unblock. REQUIREMENTS.md FOG-04..07 + FOG-11 + FOG-15 flip from `Falsified-in-production` to `Complete — Verified-by-walk (P03.1-15 CONFIRMED-AFTER-FIX 2026-05-XX after Plan 03.1-14 Dart + shader fix bundle FOG-19 + DEBUG-03)`. FOG-19 + DEBUG-03 + DEBUG-02 + UX-02 + FOG-16 + FOG-17 + FOG-18 all flip to `Complete — Verified-by-test + walk-time validated`. PERF-07 status updates to `Complete — Re-measured (P03.1-15)` with the actual numbers. Plan 03-08 DENIED verdict REVERSED in the Phase 3 row note."
    - "On `CONFIRMED-AFTER-FIX-PARTIAL`: ROADMAP.md Phase 03.1 row stays `In Progress (7th iteration; awaiting refinement plan + Walk #7)`. Phase 4 + Phase 5 stay BLOCKED. STATE.md captures the partial closure outcome + the refinement-plan trigger. REQUIREMENTS.md FOG-19 flips to `Complete — Verified-by-test + walk-time validated (P03.1-15) — partial`; DEBUG-03 + DEBUG-02 + UX-02 walk-time-validated. PERF-07 re-measured. FOG-04..07 + FOG-11 + FOG-15 stay `Falsified-in-production` with revision-clauses pointing at `03.1-FALSIFICATION-6.md`."
    - "On `DENIED-FINAL`: Q1b unchanged or worse despite FOG-19; rules out the world-coordinate-basis-mismatch mechanism interpretation. ROADMAP.md Phase 03.1 row updates to `Complete (HYPOTHESIS DENIED-FINAL)`. Phases 4 + 5 stay `Blocked` (or transition to `Cancelled` per developer choice — flag for user decision; mirror Plan 03.1-11 + 03.1-13 pattern). REQUIREMENTS.md FOG-04..07 + FOG-11 + FOG-15 stay `Falsified-in-production` with updated revision-clause pointing at `03.1-FALSIFICATION-6.md`. FOG-19 + DEBUG-03 + DEBUG-02 still flip to `Complete — Verified-by-test + walk-time validated` (their CI gates ARE GREEN; the structural fixes ARE in the codebase; the open question is whether they're SUFFICIENT). UX-02 + FOG-16 + FOG-17 + FOG-18 retain Walk #5's walk-time-validated status. STATE.md captures the formal POC architectural counter-evidence."
    - "On `ITERATING` (new failure mode surfaces beyond Q1b — e.g. C3' explicit ~7M-raw-px walk-time-validation surfaces NEW precision artefact; OR FOG-19 introduces unforeseen issue at extreme zoom; OR something unforeseen): a Plan 03.1-16+ is opened in a follow-up plan-phase invocation, with Walk #6's diagnostic stream as input. THIS plan does NOT author 03.1-16. FOG-19 + DEBUG-03 + DEBUG-02 still flip to `Complete — Verified-by-test + walk-time validated` (the structural fixes ARE in the codebase). PERF-07 updated with Walk #6 numbers. FOG-04..07 + FOG-11 + FOG-15 stay `Falsified-in-production`. Phase 3.1 stays `In Progress (7th iteration)`."
    - "Phase 3 `03-FALSIFICATION.md` AND Phase 3.1 Walks #1 + #2 + #3 + #4 + #5 falsification documents AND `03.1-WALK-3B-MARKER-ANALYSIS.md` are LEFT UNTOUCHED — no edits, no overwrites. Historical records preserved across iteration cycles."
  artifacts:
    - path: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-6.md"
      provides: "Phase 3.1 Walk #6 falsification document — pre-walk gate-pass evidence + post-walk verdict (`CONFIRMED-AFTER-FIX (FULL)` | `CONFIRMED-AFTER-FIX-PARTIAL` | `DENIED-FINAL` | `ITERATING`)"
      min_lines: 100
      contains: "Walk #6"
    - path: ".planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-6.md"
      provides: "Walk #6 evidence transcript — developer's verbatim verdict + Q-axis interpretation + JSONL grep correlation + (if markers fired) marker-correlation analysis + Q6 NEW: DEBUG-03 cell-drift quantitative reading"
      min_lines: 40
      contains: "Walk Evidence"
  key_links:
    - from: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-6.md"
      to: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md"
      via: "explicit cross-reference in Walk #6's `## Relationship to Walk #5 Verdict` section ('Walk #5 closed CONFIRMED-AFTER-FIX-PARTIAL 2026-05-04; Walk #6 either confirms-FULL or denies-or-iterates the Plan 03.1-14 fix bundle: FOG-19 C-b uZoomScale uniform + DEBUG-03 unique-cell-numbers')"
      pattern: "03\\.1-FALSIFICATION-5\\.md"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-6.md"
      via: "FOG-04..07 + FOG-11 + FOG-15 + FOG-19 + DEBUG-03 + DEBUG-02 + UX-02 + PERF-07 status clauses include `(P03.1-15)` cross-reference; Revisions section appends a 2026-05-XX Walk #6 entry"
      pattern: "P03\\.1-15"
    - from: ".planning/STATE.md"
      to: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-6.md"
      via: "Current Position + Decisions sections capture the Walk #6 verdict outcome"
      pattern: "Phase 3\\.1"
    - from: ".planning/ROADMAP.md"
      to: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-6.md"
      via: "Phase 03.1 row + Plan 03.1-15 row updated with Walk #6 verdict; on CONFIRMED-AFTER-FIX (FULL), Phase 3 status note updated to reflect verdict reversal"
      pattern: "Walk #6"
---

<objective>
Run the pre-walk gates, perform the iPhone 17 Pro sideload UAT Walk #6, capture the developer's verdict, and produce `03.1-FALSIFICATION-6.md` — the Phase 3.1 Walk #6 falsification document. Walk #6 is the empirical re-test for the Plan 03.1-14 fix bundle:

- **Task A — DEBUG-03** unique-cell-numbers diagnostic enhancement (debug-spiral now renders unique 4-digit per-cell numbers; enables Walk #6 quantitative drift measurement during deliberate zoom transitions per developer's Walk #5 verbatim request).
- **Task B — FOG-19 C-b** zoom-invariant noise sampling (uZoomScale uniform forwarded by `_FogPainter.paint()` as `pow(2, camera.zoom - kPocFogReferenceZoom)`; both shaders divide noise sampling by uZoomScale; cells anchor to lat/lng during zoom).

**Walk #6 closes Phase 3.1** if the verdict is `CONFIRMED-AFTER-FIX (FULL)` — Q1 SNAP retained from Walk #5 + Q1b CLOSED via FOG-19 + C3' explicit ≥7M extreme-distance validation passes + Q3/Q4/Q2/PERF-07 retained. Phase 4 + Phase 5 unblock; Plan 03-08 DENIED verdict REVERSED.

**Closure-blocking outcomes:**
- `CONFIRMED-AFTER-FIX-PARTIAL` (FOG-19 partial improvement; small residual drift) — Phase 3.1 stays In Progress (7th iteration; refinement plan + Walk #7).
- `DENIED-FINAL` — Q1b unchanged or worse despite FOG-19; rules out world-coordinate-basis-mismatch mechanism interpretation; architectural counter-evidence.
- `ITERATING` — new failure mode surfaces (C3' explicit ~7M validation introduces NEW precision artefact; OR FOG-19 unforeseen issue at extreme zoom; OR something unforeseen). Plan 03.1-16+ pending.

Purpose: this is the empirical gate Phase 3.1 needs after the Plan 03.1-14 fix bundle. Plan 03.1-14 software-completed FOG-19 + DEBUG-03; Walk #6 is the architectural-evidence gate that flips them from `Verified-by-test` to `walk-time validated` AND closes the C3' / DEBUG-02 explicit walk-time-validation deferred from Walk #5. Walk #6 also re-validates UX-02 + FOG-16 + FOG-17 + FOG-18 (3rd consecutive walk).

Output: 1 new `03.1-FALSIFICATION-6.md` (the load-bearing artefact); 1 new `03.1-UAT-6.md` (the walk-evidence transcript); REQUIREMENTS.md status updates for FOG-04..07 + FOG-11 + FOG-15 + FOG-19 + DEBUG-03 + DEBUG-02 + UX-02 + PERF-07; ROADMAP.md Phase 03.1 Progress-table row update; STATE.md Current Position + Decisions updates. Phase 3's `03-FALSIFICATION.md` AND Phase 3.1 Walks #1 + #2 + #3 + #3b + #4 + #5 records ALL left UNTOUCHED.

### MIRL constraint compliance (UPDATED 2026-05-04 — visual-identity preservation)

The Plan 03.1-14 fix bundle that Walk #6 validates is MIRL-compatible per the visual-identity-preservation rule (CLAUDE.md `# MIRL solution` updated 2026-05-04):

- **Production shader edit (Task B):** ABI uniform addition (`uZoomScale` slot 41) consumed consistently by both production + debug shaders; coordinate-plumbing modification anchors noise to world; visual identity preserved at any settled (zoom, position) post-fix matches pre-fix at SOME equivalent (zoom, position).
- **Debug shader edits (Task A + Task B):** debug shader has full edit freedom per CLAUDE.md.

### Iteration policy (carried forward — unchanged)

Plan-revise-walk loop. **No hard cap on walk count.** Walk #6 is the 6th attempt under Phase 3.1 (Walk #1 P03.1-03 + Walk #2 P03.1-06 + Walk #3 P03.1-09 + Walk #3b implicit marker analysis + Walk #4 P03.1-11 + Walk #5 P03.1-13 + Walk #6 here). Walk #6 closes against the Plan 03.1-14 bundle (FOG-19 + DEBUG-03); subsequent walks reserved for refinement plans IF Walk #6 surfaces residual.
</objective>

<execution_context>
@C:/Users/oliver/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oliver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-CONTEXT.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-2.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-3.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-4.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-2.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-3.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-4.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-5.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-WALK-3B-MARKER-ANALYSIS.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-VALIDATION.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-13-PLAN.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-14-PLAN.md

<interfaces>
<!-- Walk #6 mirrors Walk #5 (Plan 03.1-13) shape-for-shape with these specialisations: -->

`03.1-FALSIFICATION-5.md` Walk #5 structure (the immediate predecessor template):
1. Title + metadata (`Created`, `Phase`, `Walked on`, `Verdict`)
2. `## Relationship to Walk #4 Verdict` (Walk #6 inserts `## Relationship to Walk #5 Verdict`)
3. `## Hypothesis`
4. `## Falsification Criteria (written BEFORE the walk)` — `### Criterion A`, `### Criterion B`, `### Criterion C — DROPPED`, `### Criterion D — Diagnostic stream`
5. `## Verdict logic`
6. `## Walk Plan`
7. `## Walk Evidence (filled AFTER the walk)`
8. `## Verdict`
9. `## Open Questions / Plan 03.1-16+ Candidate Axes` (only if Walk #6 closes ITERATING or DENIED-FINAL or CONFIRMED-AFTER-FIX-PARTIAL with surfaced gaps)

Walk #6's `03.1-FALSIFICATION-6.md` mirrors Walk #5's structure exactly with these specialisations:
- Title: `# Phase 3.1 Falsification — Walk #6 (Post-Plan-03.1-14 Fix Bundle: FOG-19 C-b uZoomScale uniform + DEBUG-03 unique-cell-numbers)`
- Add `## Relationship to Walk #5 Verdict` section pointing at `03.1-FALSIFICATION-5.md`; summarise the Walk #5 CONFIRMED-AFTER-FIX-PARTIAL outcome (Q1 SNAP closed at MAX zoom via FOG-18; Q1b residual still present awaiting C2' follow-up; C3' deferred since pixelOriginX max reached only 4.26M = MAX zoom at Melun, not ~7M+).
- Hypothesis re-statement: "After the Plan 03.1-14 fix bundle (FOG-19 C-b uZoomScale uniform + DEBUG-03 unique-cell-numbers), the same-Canvas fog now: (a) translates with the camera during pan at MAX zoom regimes (FOG-18 retained from Walk #5); (b) maintains correctness at extreme world coordinates ~50-100 km from Melun (C3' explicit walk-time validation NEW for Walk #6 — pixelOriginX pushes ≥7M raw px); (c) keeps the reveal hole co-located with the blue dot during pan/zoom (FOG-12 + FOG-13 retained); (d) covers the entire viewport at any canvas-translation magnitude (FOG-13 retained); (e) is structurally invulnerable to the rotation-correlated fog mis-coverage failure mode (UX-02 retained); **(f) NEW: keeps fog noise samples anchored to lat/lng during deliberate zoom in/out transitions** (FOG-19 C-b: cells stay PUT during zoom — visible cell size grows with zoom-in, shrinks with zoom-out; DEBUG-03 unique cell numbers enable quantitative drift measurement)."
- Criterion A: same thresholds (PERF-07 carry-over): `medianMs ≤ 16`, `p95Ms ≤ 32`, `maxMs ≤ 48`. Walk #5 captured 13×/20×/28× headroom; Walk #6 should preserve at least Walk #5 levels (FOG-19 adds minimal arithmetic — one float division per fragment in production shader; one cellPx scaling in debug-spiral).
- Criterion B sub-claims:
  - **Q1 (sustained MAX-zoom pan SNAP — RE-VALIDATION)**: must retain Walk #5's CLOSURE; expected outcome: zero markers at MAX zoom regardless of pan duration.
  - **Q1b (zoom-gesture seed-of-procedural-pattern-fast — VERDICT-DETERMINING for Walk #6)**: developer performs ≥5 deliberate zoom in/out transitions at MAX zoom WITH DEBUG-03 unique cell numbers visible (DEBUG-01 toggle ON). Expected outcome (CONFIRMED-AFTER-FIX FULL): cells stay anchored across consecutive zoom transitions; ZERO drift on DEBUG-03 unique cells (or ≤1-2 cell drift = success threshold). Quantitative reading: developer captures verbatim observation like 'cell 5023 → cell 5023 across 5 transitions = success' or 'cell 5023 → cell 5021 = 2 cells drift'.
  - **Q2 (no rotation-correlated fog mis-coverage)**: re-confirm UX-02 walk-time-validated (canvasTx/Ty identically zero; 3rd consecutive walk).
  - **Q3 (no reveal-hole offset from blue dot)**: re-confirm FOG-12 + FOG-13.
  - **Q4 (fog visually translates with camera during pan)**: re-confirm.
  - **Q5 (DEBUG-02 stress-test ~50-100 km from Melun — VERDICT-DETERMINING; closes Walk #5 deferred)**: developer pans continuously past city limits; expected outcome: fog renders correctly at ≥7M raw-px regime; zero markers fired; no precision-induced artefacts.
  - No white-ellipse during fast pinch-zoom (already passed Walks #1-#5).
  - No inversion at any zoom level (already passed Walks #1-#5).
- Criterion C stays DROPPED.
- Criterion D (Diagnostic stream): `pixelOriginX/Y` continues to log raw `camera.pixelOrigin` (back-compat with Walks #1-#5 grep tooling). `appliedUOffset` (preserved JSONL field name) continues to equal `camera.pixelOrigin` exactly under FOG-18 — FOG-19's zoom-scale lives shader-side, not Dart-side, so the JSONL stream's signature is identical to Walk #5's. **Pattern check (verdict-determining for Q5):** during the C3' extreme-distance pan in Walk #6's rollups, `uOffsetX*` values exceed 7M (Walk #5 max was 4.26M = MAX zoom at Melun) AND no marker fires AND `pixelOriginX` evolves smoothly = FOG-19 + FOG-18 precision-clean at distance. **Pattern check (verdict-determining for Q1b):** during the deliberate zoom-gesture sequence with DEBUG-03 cells visible, ZERO markers fire (developer would have tapped the marker on any visible cell drift; alternatively the post-walk verbatim observation captures small drift = CONFIRMED-AFTER-FIX-PARTIAL). `canvasTx/Ty` identically zero (UX-02 retained 3rd consecutive walk).

`03.1-UAT-6.md` mirrors `03.1-UAT-5.md` shape-for-shape: metadata block + walk protocol + walk evidence (frame-delta + SDF + fog-transform rollups) + subjective verdict + verdict + deviations + summary + (optional) gaps section if Walk #6 surfaces residual failure modes + `gaps:` YAML in frontmatter for any opened-by-Walk-#6 gap entries.

The pre-walk gate sequence is identical to Plans 03.1-11 + 03.1-13 Task 1 (six gates: flutter test / analyze / format / tool tests / CI green / IPA download). Plan 03.1-15 Task 1 runs against the post-Plan-03.1-14 SHA (let `gh run list --branch main --limit 5` resolve at execute-phase time).

The walk shape is Walk #5 PLUS the C3' explicit walk-time-validation regime + the C2' / FOG-19 quantitative-drift validation regime (DEBUG-01 toggle + ≥5 deliberate zoom transitions reading off DEBUG-03 unique cell numbers). Dev-marker workflow unchanged: tap on any residual symptom; tag `"steppy_translation"` (hardcoded since Plan 03.1-09); post-hoc disambiguation via timing.

</interfaces>

<out_of_scope>
The following items are EXPLICITLY out of scope for Plan 03.1-15:

1. **Authoring Plan 03.1-16+.** If Walk #6 returns `ITERATING` or `CONFIRMED-AFTER-FIX-PARTIAL` with surfaced gaps, the next plan is opened in a follow-up `/gsd:plan-phase 3.1 --gaps` invocation that consumes Walk #6's diagnostic stream as input. THIS plan does NOT author 03.1-16+.

2. **Re-enabling cameraConstraint.** DEBUG-02 (Plan 03.1-12 Task 2) removed it; Walk #6 needs the unconstrained pan to reach ~50-100 km from Melun for the C3' explicit walk-time-validation regime. Re-enabling for production UX is a Phase 5 hardening concern (or a follow-up plan if the C3' outcome surfaces a need).

3. **MIRK-01 ABI formalization, MIRK-01 acceptance test trio, ABI uniform rename `uPixelOrigin` → `uWorldOffset`, JSONL field rename `uOffsetX*` → `uWorldOffsetX*`.** All DEFERRED per 03.1-CONTEXT.md `<deferred>` block. Walk #6 outcome may inform whether to revisit (CONFIRMED-AFTER-FIX FULL → can revisit at leisure during/after Phase 5 hardening; ITERATING → defer further).

4. **PERF-08 SDF-cache-thrash addressing.** Same status as Walks #1-#5 — deferred. Re-measured for context only.

5. **Pixel 4a parity walk.** Stays in Phase 5.

6. **Modifying production shader source FURTHER beyond Plan 03.1-14.** MIRL visual-identity-preservation rule (CLAUDE.md `# MIRL solution`). The Plan 03.1-14 bundle is the empirical answer; Walk #6 validates that bundle without further shader edits.

7. **Re-running Walks #1-#5.** Historical records preserved across iteration cycles; this plan does NOT re-author or modify any prior walk's FALSIFICATION or UAT documents.

8. **CLAUDE.md `# current best version` updates.** Developer-managed section; Plan 03.1-15 does NOT touch CLAUDE.md (the `# MIRL solution` update is already-committed in commit `57b2db1`).
</out_of_scope>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run pre-walk gate sequence + author 03.1-FALSIFICATION-6.md skeleton + 03.1-UAT-6.md skeleton</name>
  <files>
    .planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-6.md,
    .planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-6.md
  </files>
  <action>
    Pre-walk gate sequence + skeleton authoring. Mirror Plan 03.1-13 Task 1 structure verbatim.

    **A) Pre-walk gates (six gates, all must pass):**

    1. `flutter test --reporter=compact` — full suite GREEN (≈183 tests + 1 skipped post-Plan-03.1-14: existing 180 + new `fog_zoom_invariant_basis_test.dart` 4 sub-tests + the existing 1 skipped FOG-09 RED-guard). Capture output; record total tests passed in the FALSIFICATION-6 skeleton.
    2. `flutter analyze --fatal-infos` — 0 warnings.
    3. `dart format --line-length 160 --set-exit-if-changed .` — 0 files changed.
    4. `dart test tool/test/` — 18 passed.
    5. CI green check via `gh run list --branch main --limit 5`. The post-Plan-03.1-14 SHA must show `success` for the iOS build job (`Build iOS (no-codesign, sideloadable)`). Capture the run ID + SHA in the skeleton metadata.
    6. IPA artifact download: `gh run download <RUN_ID> --name mirk-poc-debug-ios-unsigned-ipa --dir .uat-tmp/`. Verify `.uat-tmp/mirk-poc-debug-unsigned.ipa` exists and is ~11 MB.

    Each gate's pass/fail status + relevant output captured in `03.1-FALSIFICATION-6.md` §"Pre-walk gate sequence" subsection.

    **B) `03.1-FALSIFICATION-6.md` skeleton authoring:**

    Mirror `03.1-FALSIFICATION-5.md` structure (see `<interfaces>` for the structure outline + Walk #6 specialisations):
    - Title: `# Phase 3.1 Falsification — Walk #6 (Post-Plan-03.1-14 Fix Bundle: FOG-19 C-b uZoomScale uniform + DEBUG-03 unique-cell-numbers)`
    - `**Created:**` (today's date)
    - `**Phase:**` 03.1-fix-fog-pan-translation
    - `**Walked on:**` (PLACEHOLDER — filled by Task 3 post-walk)
    - `**Verdict:**` PENDING
    - `## Relationship to Walk #5 Verdict` — narrative summary of Walk #5's CONFIRMED-AFTER-FIX-PARTIAL outcome + the Plan 03.1-14 fix bundle (DEBUG-03 unique-cell-numbers + FOG-19 C-b uZoomScale uniform per CONTEXT.md C2' decisions; MIRL visual-identity-preservation rule update). Cite `03.1-FALSIFICATION-5.md` §Open Questions / Plan 03.1-14+ Candidate Axes (A) and (B) as the inputs for Plan 03.1-14 Tasks A + B respectively. Cite `03.1-FALSIFICATION-5.md` §Walk Evidence §C3' regime as the input for Walk #6's NEW C3' explicit walk-time-validation regime.
    - `## Hypothesis` — full re-statement per `<interfaces>` `Hypothesis re-statement`. Note Q1b explicit closure as the targeted FOG-19 fix (verdict-determining); note C3' explicit ≥7M validation (verdict-determining); note other axes retained (Q1 / Q2 / Q3 / Q4 / PERF-07).
    - `## Falsification Criteria (written BEFORE the walk)` — write Criteria A / B / C-DROPPED / D in full per `<interfaces>` enumeration. Q1 / Q1b / Q2 / Q3 / Q4 / Q5 sub-claims under Criterion B with explicit sub-tests. Mark Q1 as RE-VALIDATION; Q1b + Q5 as VERDICT-DETERMINING for Walk #6.
    - `## Verdict logic` — verbatim copy of the verdict logic from this plan's `<scope_note>` section (CONFIRMED-AFTER-FIX FULL / CONFIRMED-AFTER-FIX-PARTIAL / DENIED-FINAL / ITERATING criteria).
    - `## Walk Plan` — mirror Walk #5's walk plan with the C2' / FOG-19 validation regime + C3' explicit walk-time-validation regime added. Pre-walk gate evidence captured in `### Pre-walk gate sequence` subsection (filled by this Task 1).
    - `## Walk Evidence (filled AFTER the walk by Plan 03.1-15 Task 3)` — empty placeholder section.
    - `## Verdict` — empty placeholder section.

    **C) `03.1-UAT-6.md` skeleton authoring:**

    Mirror `03.1-UAT-5.md` structure:
    - Frontmatter with `gaps:` YAML block (initially empty — populated by Task 3 if Walk #6 surfaces residual failure modes).
    - Walk #6 metadata block (placeholder for CI run, SHA, walk theatre, duration, gestures — filled by Task 3).
    - Walk Protocol section: pre-walk sanity gate + main session + NEW C3' extreme-distance regime + NEW C2' / FOG-19 validation regime + dev-marker discipline + post-walk Mail-share. Walk #5's UAT-5.md walk-protocol section is the structural template — extend with the two NEW Walk #6 regimes.
    - Walk Evidence section (placeholder).
    - Subjective verdict block (placeholder for developer's verbatim words).
    - Q-axis verdict mapping placeholder (Q1 RE-VALIDATION + Q1b VERDICT-DETERMINING + Q2 RETAINED + Q3 RETAINED + Q4 RETAINED + Q5 VERDICT-DETERMINING + Q6 NEW: DEBUG-03 cell-drift quantitative reading).
    - Verdict + deviations + summary placeholders.

    Skeleton commit message:
    ```
    docs(03.1-15): pre-walk gate sequence + Walk #6 falsification + UAT skeletons (Task 1)

    Pre-walk gates GREEN at SHA <RUN_SHA> (CI run <RUN_ID>):
    - flutter test: <N> passed + 1 skipped
    - flutter analyze --fatal-infos: 0 warnings
    - dart format: clean
    - dart test tool/test/: 18 passed
    - CI green on post-Plan-03.1-14 SHA
    - IPA downloaded to .uat-tmp/

    Skeleton structure mirrors 03.1-FALSIFICATION-5.md + 03.1-UAT-5.md
    with Walk #6 specialisations:
    - Hypothesis re-statement covering FOG-19 C-b uZoomScale anchoring +
      DEBUG-03 unique-cell-numbers diagnostic enhancement
    - NEW C2' / FOG-19 validation regime (≥5 zoom transitions with DEBUG-01
      toggle ON; quantitative drift reading via DEBUG-03 cells)
    - NEW C3' extreme-distance walk-time-validation (~50-100 km from Melun;
      pixelOriginX ≥ 7M; closes Walk #5 deferred validation)
    - 4-branch verdict logic: CONFIRMED-AFTER-FIX FULL / PARTIAL /
      DENIED-FINAL / ITERATING

    Walk #6 to be performed by user; Task 2 checkpoint:human-verify.

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    ```
  </action>
  <verify>
    <automated>node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" frontmatter validate .planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-6.md --schema falsification 2>/dev/null || ls .planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-6.md .planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-6.md</automated>
  </verify>
  <done>
    - All six pre-walk gates pass; outputs captured in `03.1-FALSIFICATION-6.md` §Pre-walk gate sequence.
    - `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-6.md` exists with full Walk #6 skeleton (Title + Hypothesis + Falsification Criteria A/B/C-DROPPED/D + Verdict logic + Walk Plan + Pre-walk gate sequence subsection + empty Walk Evidence + empty Verdict).
    - `.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-6.md` exists with full Walk #6 skeleton (frontmatter + walk-protocol + placeholder evidence/verdict/summary sections).
    - Walks #1-#5 + Phase 3 historical records UNTOUCHED.
    - One atomic commit `docs(03.1-15): pre-walk gate sequence + Walk #6 falsification + UAT skeletons (Task 1)` lands on `main`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: iPhone 17 Pro UAT Walk #6 — sideload session against Plan 03.1-14 fix bundle (FOG-19 + DEBUG-03)</name>
  <files>.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-6.md, .planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-6.md</files>
  <action>
    Sideload the unsigned IPA on iPhone 17 Pro via SideStore; perform Walk #6 per the protocol below; capture verbatim verdict + Mail-share session log post-walk so Task 3 can grep-correlate JSONL streams. The full walk protocol with the Walk #5 carry-forward gestures + NEW C3 explicit extreme-distance regime + NEW C2 / FOG-19 quantitative-drift validation regime (DEBUG-01 toggle ON + DEBUG-03 cell-id reading) is enumerated in the &lt;how-to-verify&gt; block below; this &lt;action&gt; block exists for plan-checker schema completeness — execute the &lt;how-to-verify&gt; protocol verbatim.
  </action>
  <verify>
    <automated>MANUAL — see &lt;how-to-verify&gt; (PERF-07 + Criterion B + Criterion D subjective + diagnostic-stream pattern matching + dev-marker correlation are inherently manual UAT walk gates per 03.1-VALIDATION.md Manual-Only Verifications table; the developer s resume-signal IS the verification).</automated>
  </verify>
  <done>
    Walk #6 complete + developer s verbal verdict captured (`approved` / `approved-partial` / `denied:` / `iterating:`); session log Mail-shared so Task 3 can extract JSONL streams. The verdict outcome drives Task 3 s branch (CONFIRMED-AFTER-FIX (FULL) / CONFIRMED-AFTER-FIX-PARTIAL / DENIED-FINAL / ITERATING). Q1b quantitative cell-drift reading (Q6 NEW per CONTEXT.md C2 decisions) captured verbatim from developer.
  </done>
  <what-built>
    Plan 03.1-14 fix bundle landed on `main` with two atomic commits:
    1. `feat(03.1-14): DEBUG-03 unique-cell-numbers in debug-spiral for drift measurement (Task A)`
    2. `feat(03.1-14): FOG-19 C-b uZoomScale uniform — anchor noise to lat/lng during zoom (Task B)`

    The post-Plan-03.1-14 SHA (resolved at execute-phase time via `gh run list --branch main --limit 5`) is built into an unsigned IPA via the iOS CI job. Pre-walk gates have all passed (six gates GREEN per Task 1's `03.1-FALSIFICATION-6.md` §Pre-walk gate sequence subsection). The IPA is downloaded to `.uat-tmp/mirk-poc-debug-unsigned.ipa` (~11 MB) ready for SideStore sideload.
  </what-built>
  <how-to-verify>
    **Step 1: Sideload the IPA onto iPhone 17 Pro.**
    1. Open SideStore on iPhone 17 Pro.
    2. Refresh / install the IPA from `.uat-tmp/mirk-poc-debug-unsigned.ipa` (use AltStore-paired desktop pairing-file if needed).
    3. Launch `mirk-poc-debug` on the device.

    **Step 2: Pre-walk sanity gate (validate the build before walking).**
    1. App launches without crash; permission prompt appears (if not already granted); GPS permission grant; main map screen loads with FpsCounterOverlay (top:8, right:8) + MapCompass (top:56, right:8) + FrameDeltaProbeOverlay (top:104, right:8) visible.
    2. Tap the science-icon AppBar action → `/sanity` route — confirm the production fog renders with circular reveal hole (debug-spiral toggle OFF default).
    3. Toggle the debug-spiral switch ON (on `/sanity` AppBar) — confirm the digit-atlas spiral pattern renders with **NEW DEBUG-03 unique 4-digit cell numbers** (vs Walk #5's 0..99 cycling). Cells should display 4-digit IDs like `5050`, `5051`, etc. clustered around the world-origin region. **(DEBUG-03 walk-time validation.)**
    4. Toggle OFF — confirm production fog returns.
    5. Confirm the `Icons.arrow_back` AppBar leading button still works on `/sanity` (UX-01 retained).
    6. Confirm zero shader-compile exceptions in the FileLogger output.
    7. Return to `/map`; attempt a two-finger rotate gesture — confirm the map does NOT rotate (MapCompass stays at 0°; UX-02 retained 3rd consecutive walk).

    **Step 3: Main interactive session (≥5 minutes; central Melun region).**

    All gestures performed at-desk via touchscreen interaction (no physical movement required per CONTEXT.md C2' decisions §"Walk shape reframing"):
    - ≥10 deliberate combined pinch-zoom-and-pan gestures.
    - ≥3 recenter FAB taps.
    - ≥1 deliberate two-finger rotate-attempt gesture (re-validates UX-02 — should be a no-op).
    - ≥1 sustained ≥30-sec one-direction pan at MAX zoom (re-validates Q1 SNAP closure from Walk #5 — expected: zero markers fired).

    Tap dev-marker AppBar button on any residual symptom related to Q1 (max-zoom SNAP), Q3 (reveal-hole offset), Q4 (fog tracking).

    **Step 4: NEW Walk #6 — C3' extreme-distance regime (VERDICT-DETERMINING; closes Walk #5 deferred validation).**

    1. Pan continuously in one direction past city limits (recommended direction: south-east towards Provins, OR south towards Fontainebleau — anywhere away from Melun bbox; cameraConstraint is removed per DEBUG-02).
    2. Continue panning until the pixelOrigin pushes ~50-100 km from initial Melun centre (~7M raw px at MAX zoom; the FrameDeltaProbeOverlay does not show pixelOrigin directly, but a ~30-60 sec one-direction pan at high pan-velocity should reach it).
    3. Watch the fog rendering throughout — does it continue to render correctly at distance? Any new SNAP, jitter, white-ellipse, or precision artefacts?
    4. Tap dev-marker on ANY visual anomaly at distance.
    5. Expected outcome: fog renders correctly throughout the long pan; zero markers fired; FOG-18 + FOG-19 precision-clean at extreme world coordinates within fp32 mantissa headroom.

    **Step 5: NEW Walk #6 — C2' / FOG-19 validation regime (VERDICT-DETERMINING; the targeted Q1b fix).**

    1. Recenter the map (tap recenter FAB) to return near Melun.
    2. Toggle DEBUG-01 ON via the `/map` AppBar (the debug-spiral toggle).
    3. The screen now shows the DEBUG-03 unique 4-digit cell numbers laid out in a grid.
    4. Note a specific cell currently at the screen centre (e.g., the cell number `5023`).
    5. Pinch-zoom IN to MAX zoom; OBSERVE — does the cell at the screen centre stay anchored to the same lat/lng (i.e., the same cell number `5023`), OR does it shift (e.g., the screen centre is now at cell `5021` = 2 cells of drift)?
    6. Pinch-zoom OUT to default zoom; observe again.
    7. Repeat ≥5 deliberate zoom in/out transitions; note the cell number at screen centre after each transition.
    8. Capture verbatim observation: e.g., "cell 5023 → cell 5023 across all 5 transitions = ZERO drift = FOG-19 success" OR "cell 5023 → cell 5021 = 2 cells drift = partial fix".
    9. Toggle DEBUG-01 OFF; confirm production fog returns and shows the same anchoring behaviour at zoom transitions (production fog has no cell-grid to read, but the noise-pattern's character at the screen centre should stay stable across zoom — no "numbers sliding" / "incorrect scaling" sensation).

    **Step 6: Post-walk Mail-share (REQUIRED).**

    1. Tap the AppBar `Share Logs` button.
    2. Pick Mail; send to yourself.
    3. Confirm the session log file (`yyyymmddTHHMMSSZ_logs.txt`) arrives in your inbox.
    4. The file is required for grep-correlation against any dev-marker timestamps (Walks #4 + #5 pattern).

    **Step 7: Capture verbatim post-walk verdict.**

    Provide your verbal verdict:
    - `approved` (CONFIRMED-AFTER-FIX FULL — Q1 retained + Q1b CLOSED + C3' passes + Q3/Q4/Q2/PERF-07 retained)
    - `approved-partial` (CONFIRMED-AFTER-FIX-PARTIAL — Q1 retained + Q1b improved-but-not-fully-closed)
    - `denied:` followed by reasons (DENIED-FINAL — Q1b unchanged or worse despite FOG-19)
    - `iterating:` followed by surfaced new failure mode (ITERATING — new artefact at C3' distance, OR FOG-19 unforeseen issue, OR something else)

    Optionally update CLAUDE.md `# current best version` section yourself (developer-managed; this plan does NOT touch it).
  </how-to-verify>
  <resume-signal>Type your verdict (`approved` | `approved-partial` | `denied: <reason>` | `iterating: <new failure mode>`) plus any verbatim Q-axis observations + the C2' / FOG-19 quantitative cell-drift reading.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Post-walk verdict authoring + REQUIREMENTS / ROADMAP / STATE updates + atomic commit</name>
  <files>
    .planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-6.md,
    .planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-6.md,
    .planning/REQUIREMENTS.md,
    .planning/ROADMAP.md,
    .planning/STATE.md
  </files>
  <action>
    Post-walk verdict authoring + planning-doc cascade. Mirror Plan 03.1-13 Task 3 structure.

    **A) Mail-share extract:** capture the Mail-shared session log filename (e.g., `20260505T184412Z_logs.txt`) reported by the developer; extract the four JSONL streams to `.uat-tmp/walk6_*.jsonl`:
    - `walk6_frame_delta.jsonl`: `grep -F 'infrastructure.mirk.frame_delta' <log>`
    - `walk6_fog_transform.jsonl`: `grep -F 'infrastructure.mirk.fog_transform' <log>`
    - `walk6_sdf_rollups.jsonl`: `grep -F 'infrastructure.mirk.sdf' <log>`
    - `walk6_dev_markers.jsonl`: `grep -F 'dev_marker' <log>`

    Compute summary statistics:
    - Frame-delta probe rolling rollup: median-of-medians, max-of-p95s, max-of-maxes; PERF-07 GREEN/RED status.
    - Fog-transform: pxOriginX/Y trajectory across regimes (default-zoom + MAX-zoom + C3' extreme-distance + C2' zoom-gesture + idle); uOffsetX/Y vs pxOriginX/Y signature (must equal under FOG-18 — Walk #5 retained); canvasTx/Ty (must be identically zero under UX-02 — 3rd consecutive walk).
    - SDF: rebuild rate baseline.
    - Dev-markers: count + per-marker context (regime / pxOriginX magnitude / nearby uOffsetX value).

    **B) Fill `03.1-FALSIFICATION-6.md` §Walk Evidence section:**

    - Mail-shared session log filename + extraction stream line counts.
    - Active walk window duration (sec).
    - Composite trajectory (rollups by regime — default-zoom / MAX-zoom / C3' extreme-distance / C2' zoom-gesture / idle).
    - Pre-walk shader-sanity gate: production fog + DEBUG-01 toggle + DEBUG-03 unique-cell-numbers + UX-01 back button + UX-02 rotation-disabled all retained.
    - Probe rollup: median / p95 / max table; PERF-07 verdict.
    - SDF rebuild rollup: rate + per-rebuild cost; PERF-08 deferred.
    - Fog-transform rollup: UX-02 retained (canvasTx/Ty zero); FOG-18 signature retained (uOffsetX == pxOriginX); pxOriginX trajectory across regimes; **C3' extreme-distance regime confirmed reached** (uOffsetXMax exceeds 7M ✓ if developer panned ~100 km, OR developer fell short of 7M = note in evidence + DEBUG-02 walk-time-validation outcome).
    - Marker count: 0 across the entire walk window OR N markers fired (with per-marker context if any).
    - **NEW: Q1b verbatim observation + DEBUG-03 quantitative cell-drift reading** (per developer's Step 5 capture). E.g., "cell 5023 → cell 5023 across all 5 transitions = ZERO drift = FOG-19 success" OR "cell 5023 → cell 5021 = 2 cells drift = partial".
    - Subjective verdict block: developer's verbatim words from `<resume-signal>` capture.
    - Q-axis interpretation:
      - Q1 (sustained MAX-zoom pan SNAP) — re-validation; expected RESOLVED retained from Walk #5.
      - Q1b (zoom-gesture) — VERDICT-DETERMINING; CLOSED (FOG-19 success) OR PARTIAL OR UNCHANGED (DENIED-FINAL).
      - Q2 (rotation-correlated mis-coverage) — RETAINED (UX-02 walk-time-validated 3rd consecutive walk).
      - Q3 (reveal-hole offset) — RETAINED.
      - Q4 (fog translates with map) — RETAINED.
      - Q5 (DEBUG-02 stress-test ~50-100 km) — VERDICT-DETERMINING; PASSED (zero markers at ≥7M) OR FAILED (markers fired or fog rendering anomaly at distance) OR DEFERRED (developer didn't reach ≥7M — flag as Walk #7 follow-up).
      - Q6 NEW (DEBUG-03 quantitative cell-drift reading) — captured verbatim; feeds either CONFIRMED-AFTER-FIX FULL (zero drift) or CONFIRMED-AFTER-FIX-PARTIAL (small drift) or DENIED-FINAL (unchanged-or-worse drift).

    **C) Author `03.1-FALSIFICATION-6.md` §Verdict section:**

    Per the 4-branch verdict logic in `<scope_note>`:
    - **`CONFIRMED-AFTER-FIX (FULL)`** — Q1 retained + Q1b CLOSED + C3' PASSED + Q3/Q4/Q2/PERF-07 retained.
    - **`CONFIRMED-AFTER-FIX-PARTIAL`** — Q1b improved but not fully closed.
    - **`DENIED-FINAL`** — Q1b unchanged or worse despite FOG-19.
    - **`ITERATING`** — new failure mode surfaces.

    Note any §Open Questions / Plan 03.1-16+ Candidate Axes if the verdict is anything other than CONFIRMED-AFTER-FIX FULL.

    **D) Fill `03.1-UAT-6.md`:**

    - Walk #6 metadata block: CI run + SHA + walk theatre (central Melun for default-zoom regime + extending toward Provins/Fontainebleau for C3' regime; pixelOrigin reached during walk) + duration + gestures count.
    - `gaps:` YAML in frontmatter: per the verdict outcome, populate gap entries that feed any follow-up plan (e.g., `Q1B-RESIDUAL-DRIFT` if PARTIAL, `C3-PRECISION-ARTEFACT-AT-DISTANCE` if Q5 failed, etc.). On CONFIRMED-AFTER-FIX FULL, `gaps:` block is empty (or contains a single closure-confirmation entry).
    - Walk Evidence + Subjective verdict + Q-axis verdict mapping + Verdict + Deviations + Summary sections all filled per the FALSIFICATION-6.md §Walk Evidence content.

    **E) REQUIREMENTS.md status updates:**

    On CONFIRMED-AFTER-FIX FULL:
    - FOG-04..07 + FOG-11 + FOG-15 flip from `Falsified-in-production` to `Complete — Verified-by-walk (P03.1-15 CONFIRMED-AFTER-FIX 2026-05-XX after Plan 03.1-14 fix bundle FOG-19 + DEBUG-03)`.
    - FOG-19 flips from `Complete — Verified-by-test ... pending Walk #6 walk-time validation` to `Complete — Verified-by-test + walk-time validated (P03.1-15 Walk #6 CONFIRMED-AFTER-FIX FULL)`.
    - DEBUG-03 flips similarly.
    - DEBUG-02 flips from `Complete — Verified-by-test ... pending Walk #5 walk-time validation` (which Walk #5 deferred) to `Complete — Verified-by-test + walk-time validated (P03.1-15 Walk #6 — explicit ≥7M extreme-distance validation passed)`.
    - UX-02 + FOG-16 + FOG-17 + FOG-18 retain Walk #5's walk-time-validated status; append a Walk #6 note to each (3rd consecutive walk for UX-02; 2nd consecutive for FOG-18).
    - PERF-07 status updates to `Complete — Re-measured (P03.1-15)` with the actual numbers.

    On CONFIRMED-AFTER-FIX-PARTIAL: mirror Walk #5's status-update pattern — FOG-19 + DEBUG-03 + DEBUG-02 walk-time-validated; FOG-04..07 + FOG-11 + FOG-15 stay Falsified-in-production with revision-clauses pointing at `03.1-FALSIFICATION-6.md`.

    On DENIED-FINAL: same as CONFIRMED-AFTER-FIX-PARTIAL but with FOG-04..07 + FOG-11 + FOG-15 status indicating architectural counter-evidence outcome.

    On ITERATING: same as PARTIAL but FOG-04..07 + FOG-11 + FOG-15 stay Falsified-in-production; flag the surfaced new failure mode in the revision-clause.

    **F) ROADMAP.md updates:**

    - On CONFIRMED-AFTER-FIX FULL: Phase 03.1 row in Progress table flips to `Complete (HYPOTHESIS CONFIRMED-AFTER-FIX)`. Phase 4 Status flips from `Blocked on Phase 3.1` to `Not Started`. Phase 5 same. Phase 3 status note appended: `**Verdict REVERSED at P03.1-15 Walk #6 2026-05-XX**` after the Plan 03-08 DENIED note. Plans count for Phase 3.1: 15/15 Complete.
    - On other verdicts: Phase 03.1 row updates per the verdict; Plans count 15/15; Phase 4 + Phase 5 stay Blocked. Append a Walk #6 verdict note to the Phase 3.1 verdict-history block.

    **G) STATE.md updates:**

    - `last_activity`: bump to today's date with verdict summary.
    - `Current Position`: capture the Walk #6 outcome (verdict + key Q-axis findings + DEBUG-03 cell-drift reading + C3' regime outcome + PERF-07 numbers + status updates).
    - `Last activity`: same summary as `last_activity`.
    - `Progress` percentage: bump per Plans-count update (Phase 3.1: 15/15).

    **H) Git commit (atomic):**

    Single commit message format depends on verdict:

    On CONFIRMED-AFTER-FIX FULL:
    ```
    docs(03.1-15): Walk #6 CONFIRMED-AFTER-FIX (FULL) — Phase 3.1 closes (Task 3)

    Walk #6 against Plan 03.1-14 fix bundle (FOG-19 + DEBUG-03) closes
    Phase 3.1 with full CONFIRMED-AFTER-FIX. Q1 SNAP retained from Walk #5;
    Q1b CLOSED via FOG-19 C-b uZoomScale uniform (developer's quantitative
    DEBUG-03 cell-drift reading: <verbatim>); C3' explicit ≥7M extreme-
    distance validation PASSED (developer panned ~<X> km from Melun;
    pixelOriginX max reached <Y>M; zero markers fired); Q3/Q4/Q2 retained
    (UX-02 3rd consecutive walk); PERF-07 retained (<headroom>x/<x>x/<x>x).

    - 03.1-FALSIFICATION-6.md: full Walk #6 verdict authored
    - 03.1-UAT-6.md: walk transcript + Q-axis verdict mapping + Q6
      DEBUG-03 quantitative cell-drift reading
    - REQUIREMENTS.md: FOG-04..07 + FOG-11 + FOG-15 flip to Complete —
      Verified-by-walk; FOG-19 + DEBUG-03 + DEBUG-02 walk-time-validated;
      UX-02 + FOG-16 + FOG-17 + FOG-18 walk-time-validated 3rd/2nd
      consecutive walk; PERF-07 re-measured
    - ROADMAP.md: Phase 03.1 flips to Complete (HYPOTHESIS CONFIRMED-
      AFTER-FIX); Phase 4 + Phase 5 unblock; Plan 03-08 DENIED verdict
      REVERSED note appended
    - STATE.md: Phase 3.1 closure outcome captured

    Walks #1-#5 + Phase 3 historical records UNTOUCHED. CLAUDE.md
    `# current best version` section LEFT UNTOUCHED (developer-managed).

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    ```

    On other verdicts: tailor the commit message accordingly. Walk #5's commit message (committed 2026-05-04 as a `docs(03.1-13): Walk #5 CONFIRMED-AFTER-FIX-PARTIAL ...`) is the structural template.

    Use `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "<message>" --files <listed files>`.
  </action>
  <verify>
    <automated>flutter test test/presentation/widgets/fog_zoom_invariant_basis_test.dart && grep -q "Walk #6" .planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-6.md && grep -q "Walk #6" .planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-6.md</automated>
  </verify>
  <done>
    - `03.1-FALSIFICATION-6.md` Walk Evidence + Verdict + (optional) Open Questions sections all filled per the developer's verdict.
    - `03.1-UAT-6.md` walk transcript + Q-axis mapping + verdict + (optional) gaps frontmatter all filled.
    - REQUIREMENTS.md status updates per verdict (FOG-04..07 + FOG-11 + FOG-15 flip if CONFIRMED-AFTER-FIX FULL; FOG-19 + DEBUG-03 + DEBUG-02 walk-time-validated regardless of verdict; UX-02 + FOG-16 + FOG-17 + FOG-18 walk-time-validated; PERF-07 re-measured).
    - ROADMAP.md Phase 03.1 Progress-table row updated; Plans count 15/15; on CONFIRMED-AFTER-FIX FULL, Phase 4 + Phase 5 unblock + Plan 03-08 DENIED verdict reversed.
    - STATE.md Current Position + last_activity + Progress percentage updated.
    - Walks #1-#5 + Phase 3 historical records UNTOUCHED.
    - CLAUDE.md `# current best version` section LEFT UNTOUCHED.
    - One atomic commit `docs(03.1-15): Walk #6 <VERDICT> ... (Task 3)` lands on `main`.
    - Full `flutter test` suite GREEN; `flutter analyze --fatal-infos` 0 warnings.
  </done>
</task>

</tasks>

<verification>
After all three tasks:

1. Three commits land on `main` in order:
   - `docs(03.1-15): pre-walk gate sequence + Walk #6 falsification + UAT skeletons (Task 1)` (Task 1)
   - (Walk #6 sideload session — no commit; the developer's verdict is captured in Task 2 `<resume-signal>`)
   - `docs(03.1-15): Walk #6 <VERDICT> ... (Task 3)` (Task 3)

2. Two new artefacts created:
   - `03.1-FALSIFICATION-6.md` (load-bearing verdict document)
   - `03.1-UAT-6.md` (walk-evidence transcript)

3. REQUIREMENTS.md / ROADMAP.md / STATE.md status updates per the verdict.

4. Walks #1 + #2 + #3 + #3b + #4 + #5 falsification documents + UAT transcripts + WALK-3B-MARKER-ANALYSIS UNTOUCHED.

5. CLAUDE.md `# current best version` section UNTOUCHED.

6. On CONFIRMED-AFTER-FIX (FULL): Phase 3.1 closes; Phase 4 + Phase 5 unblock; Plan 03-08 DENIED verdict REVERSED in the Phase 3 row note.
</verification>

<success_criteria>
- Three tasks complete in order; Task 2 is checkpoint:human-verify (walk + developer's verdict capture).
- `03.1-FALSIFICATION-6.md` + `03.1-UAT-6.md` exist with full content.
- REQUIREMENTS.md / ROADMAP.md / STATE.md updates per the verdict.
- Walks #1-#5 historical records UNTOUCHED.
- On CONFIRMED-AFTER-FIX (FULL): Phase 3.1 closes; Phase 4 + Phase 5 unblock; ROADMAP.md reflects the verdict reversal.
- Full `flutter test` GREEN throughout.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-fix-fog-pan-translation/03.1-15-SUMMARY.md` per the standard summary template.

Notable artefacts to capture in the summary:
- `requirements-completed`: depends on verdict; on CONFIRMED-AFTER-FIX FULL, includes `[FOG-04, FOG-05, FOG-06, FOG-07, FOG-11, FOG-15, FOG-19, DEBUG-03, DEBUG-02, UX-02, FOG-16, FOG-17, FOG-18, PERF-07]`. On other verdicts, narrow accordingly.
- `key-files.created`: `03.1-FALSIFICATION-6.md`, `03.1-UAT-6.md`.
- `key-files.modified`: REQUIREMENTS.md, ROADMAP.md, STATE.md.
- `key-decisions`: enumerate the Walk #6 verdict + the C2' / FOG-19 quantitative DEBUG-03 cell-drift reading + the C3' / DEBUG-02 explicit ≥7M extreme-distance walk-time-validation outcome.
- `patterns-established`: "Walk #6 = the explicit DEBUG-03 quantitative drift measurement validation pattern. Walk #5's verbatim 'numbers sliding' qualitative observation gets a follow-up quantitative reading via unique cell numbers — the diagnostic-tool feedback loop pattern (developer requests instrumentation enhancement; landed in same plan as the targeted fix; subsequent walk uses the new tool to measure the fix's effect) is reusable for future architectural-evidence iterations."
- `deferred-followup-needed`: depends on verdict; on CONFIRMED-AFTER-FIX FULL, none. On other verdicts, flag the Plan 03.1-16+ trigger.
</output>
