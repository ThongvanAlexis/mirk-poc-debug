---
phase: 03.1-fix-fog-pan-translation
plan: 15
type: execute
wave: 2
depends_on: ["03.1-14"]
files_modified:
  - .planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-6.md
  - .planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-6.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
gap_closure: true
requirements: [FOG-04, FOG-05, FOG-06, FOG-07, FOG-11, FOG-15, FOG-18, FOG-19, PERF-07]
user_setup:
  - service: github-actions
    why: "Wait for CI green on the post-Plan-03.1-14 SHA + download the IPA artifact for the Walk #6 sideload"
    dashboard_config:
      - task: "Confirm CI run status (`gh run list --branch main --limit 5` + `gh run watch`)"
        location: "GitHub Actions for the mirk-poc-debug repo"
      - task: "STRUCTURAL build-provenance check: HEAD commit SHA == CI's source SHA. If HEAD has commits after the CI's SHA, push them and trigger fresh CI run BEFORE walk."
        location: "git log + gh run view comparison"
      - task: "Download IPA artifact via `gh run download <RUN_ID> --name ios-unsigned --dir .uat-tmp/`"
        location: "GitHub Actions run detail page (or `gh` CLI)"
  - service: sidestore
    why: "Sideload the unsigned IPA onto iPhone 17 Pro for Walk #6"
    dashboard_config:
      - task: "Open SideStore on iPhone 17 Pro → install/refresh the .uat-tmp/mirk-poc-debug-unsigned.ipa"
        location: "SideStore iOS app + paired desktop pairing-file"
must_haves:
  truths:
    - "A new `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-6.md` exists capturing the Walk #6 verdict. Walks #1 + #2 + #3 + #3b + #4 + #5 falsification documents AND the Walk #3b marker analysis are LEFT UNTOUCHED — historical record preserved across iteration cycles."
    - "A new `.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-6.md` exists capturing the Walk #6 transcript. Walks #1 + #2 + #3 + #3b + #4 + #5 UAT transcripts are LEFT UNTOUCHED."
    - "**STRUCTURAL build-provenance gate (NEW for Walk #6 — recurrence-prevention from Plan 03.1-13 build-provenance gap):** the developer's HEAD commit SHA on `main` MUST equal the CI run's source SHA before sideload. The Plan 03.1-13 Task 1a logger augmentation commit `2b087a3` post-dated CI run `25285037397`, leaving Walk #5's IPA without the metersPerPixel JSONL field. Plan 03.1-15 makes this a structural pre-walk gate: Task 1 explicitly verifies `git rev-parse HEAD == <CI run's headSha>` (via `gh run view <RUN_ID> --json headSha,status`); if HEAD has commits after the CI SHA, the executor pushes them and triggers a fresh CI run BEFORE proceeding to walk-readiness. This eliminates the build-provenance gap class of failure."
    - "Pre-walk gate sequence runs cleanly: `flutter test` GREEN (Plan 03.1-14's new tests: fog_meter_space_decomposition + fog_meter_wrap_invisibility + 7 REWRITTEN tests + 2 TOUCHED tests + 1 UPDATED test all GREEN); `flutter analyze --fatal-infos` 0 warnings; `dart format --line-length 160 --set-exit-if-changed .` clean; `dart test tool/test/` GREEN; CI green on the post-Plan-03.1-14 SHA on `main`; build-provenance gate confirmed (HEAD SHA == CI SHA); IPA downloaded via `gh run download` to `.uat-tmp/mirk-poc-debug-unsigned.ipa`."
    - "**STRUCTURALLY-ENFORCED MAIL-SHARE GATE RETAINED (held at Walk #5 — first time after Walks #3 + #4 broke it twice):** the developer's resume signal MUST include the absolute path of the Mail-shared session log file as a parameter (`approved [path/to/log.txt] — ...` / `denied [path/to/log.txt]: ...` / `iterating [path/to/log.txt]: ...`). The log path must point to a file accessible to the executor (developer has saved the Mail attachment to the local filesystem before reporting the verdict). The executor then runs grep against the actual log file before authoring the verdict. If no log path is provided, the executor MUST refuse to proceed to verdict authoring and re-prompt the developer. The structural gate carried Walk #5 to first-attempt success; Walk #6 retains it identically."
    - "The iPhone 17 Pro UAT Walk #6 is performed by the user. Walk shape matches Plan 03.1-13 (Walk #5) pattern: ≥ 5 minutes through Melun + ≥ 10 deliberate combined pinch-zoom-and-pan gestures + ≥ 3 recenter taps + post-walk Mail-share of the session log (STRUCTURALLY ENFORCED, retained from Plan 03.1-13) + pre-walk `/sanity` shader-compile gate + AT LEAST ONE complete zoom traversal at fixed map position with debug-spiral toggled (validates Q5 — primary closure-evidence-gate from Walk #5)."
    - "**Walk #6-specific Q1 retention re-test** (the PRIMARY anti-regression test post-Plan-03.1-14): developer pans for ≥ 60 seconds in one direction at z=15. Expected: ZERO `dev_marker steppy_translation` taps. Walk #5 fired 2 such markers; both correlated with Y-axis FOG-17a wrap events in fog_transform JSONL within ±1 sec. Plan 03.1-14's meter-space FOG-17a decomposition makes wraps invisible (4096 m / 1024 m = 4 integer cells in noiseUv per wrap → bit-identical noise samples pre/post-wrap → indistinguishable from no shift). If ANY `steppy_translation` markers fire during sustained pan, Walk #6 captures `iterating` — the architectural model is wrong (not 4-integer-cell-shift) OR a NEW second-order failure surfaces."
    - "**Walk #6-specific Q5 PRIMARY GATE retest** (zoom-traversal at fixed map position with debug-spiral toggle ON): same procedure as Walk #5 sub-tests 5a/5b/5c/5d. Walk #6 expectation: cells visibly enlarge with zoom AND at fixed map position the spiral cell number at camera centre stays the same across all zoom levels AND production fog noise pattern at fixed map position appears stable across zoom transitions (no rapid sliding/scramble). Walk #5 reported *'zooming still moves the mirl all around giving a noisy feeling'* — Plan 03.1-14's meter-space decomposition removes the zoom-dependent shader input (bounded_meters is a function of geographic position, modulo 4096 m) → Q5 expected to close."
    - "**Walk #6-specific Q2 + Q3 + Q4 retention** (implicit, retained from Walks #4-5): no rotation-correlated fog mis-coverage during rotate-attempt gestures + no reveal-hole offset from blue dot during gesture + fog visually translates with camera during pan. Plan 03.1-14 doesn't touch any of these axes; if any regress, that's a different failure mode (Plan 03.1-16+ territory; Plan 03.1-14 may have introduced a regression at the painter level)."
    - "Post-walk: developer's verbal verdict (`approved [log_path]` / `denied [log_path]:` / `iterating [log_path]:`) is captured. The walk evidence section of `03.1-FALSIFICATION-6.md` is filled with: developer's verbatim words + interpretation + post-walk grep of `infrastructure.mirk.fog_transform` + `infrastructure.mirk.frame_delta` + `infrastructure.mirk.sdf` + (if markers fired) `infrastructure.mirk.dev_marker` rollups + per-second composite extract + (if markers fired) marker-correlation analysis mirroring the Walk #3b template. **NEW for Walk #6: fog_transform JSONL grep MUST verify the Plan 03.1-14 meter-space signature** — `coordinateSpace: 'meters'` field present in rollup body; uOffsetX/Y values bounded under `kPocFogIntegerWrapPeriodMeters + 1 = 4097 m` (was 1537 raw px in Plan 03.1-12 era); metersPerPixel field (Plan 03.1-13 augmentation, NOW empirically captured because build-provenance gate holds) populates correctly."
    - "PERF-07 is re-measured for Walk #6: frame-delta probe rolling rollup over the ≥ 10-combined-gesture window with `medianMs ≤ 16`, `p95Ms ≤ 32`, `maxMs ≤ 48`. Walk #5 captured 8.9×/5.2×/4.9× headroom (median 1.808 / p95 6.105 / max 9.756 across 67 1-Hz rollups). Walk #6 should preserve at least Walk #5 levels (Plan 03.1-14 adds 2 multiplications + 1 modulo + 1 truncate + 1 subtract per axis per paint at the Dart side — negligible; the shader gains nothing per-fragment — formula is the same number of operations as pre-Plan-03.1-14, just reordered)."
    - "Verdict written: `CONFIRMED-AFTER-FIX` if BOTH (a) developer's subjective verdict is `approved` AND (b) PERF-07 thresholds satisfied AND (c) **Q1 PASSED** — ZERO `steppy_translation` markers during sustained pan AND (d) **Q5 PASSED** — zoom-scramble RESOLVED with debug-spiral cells visibly enlarging with zoom AND cells at fixed map position staying the same across zoom levels AND production fog noise pattern stable across zoom transitions AND (e) Q2-Q3-Q4 retained from Walks #4-5. `DENIED-FINAL` if even Plan 03.1-14 Fix B′ meter-space decomposition fails empirically (the diagnostic JSONL stream cannot resolve a residual failure mode via a follow-up Phase 3.1 iteration; Phase 3.2 alternative-renderer pivot conversation: `mapsforge_flutter`, custom MVT-on-Canvas, `flutter_gpu`). `ITERATING` if a residual failure mode surfaces (e.g., a NEW second-order failure mode introduced by Plan 03.1-14, OR fp32 precision degradation at extreme zoom in meter-space — the Plan 03.1-14 SUMMARY's documented sub-cell precision held mathematically but fp32 precision under FBM-rotated octaves can be empirically surprising)."
    - "On `CONFIRMED-AFTER-FIX`: ROADMAP.md Phase 03.1 row in the Progress table flips to `Complete (HYPOTHESIS CONFIRMED-AFTER-FIX)`, plans 15/15. Phase 4 (`Wisp Particles`) status updates from `Blocked on Phase 3.1` to `Pending`. Phase 5 (`Decision Gate`) likewise. STATE.md `Current Position` updates to reflect Phase 3.1 closure. REQUIREMENTS.md FOG-04..07 + FOG-11 + FOG-15 + FOG-18 + FOG-19 statuses flip to `Complete — Verified-by-walk (P03.1-15 CONFIRMED-AFTER-FIX 2026-05-XX after layered Plan 03.1-02+04+05+07+08+10+12+14 fix bundle)`. PERF-07 status updates to `Complete — Re-measured (P03.1-15)` with the actual numbers."
    - "On `DENIED-FINAL`: ROADMAP.md Phase 03.1 row updates to `Complete (HYPOTHESIS DENIED-FINAL)`. Phases 4 + 5 stay `Blocked` (or transition to `Cancelled` per developer choice — flag for user decision). REQUIREMENTS.md FOG-04..07 + FOG-11 + FOG-15 stay `Falsified-in-production` with updated revision-clause that points at `03.1-FALSIFICATION-6.md`. UX-02 + FOG-16 + FOG-17 + FOG-18 + FOG-19 still flip to `Complete — Verified-by-test + walk-time validated structurally` (their CI gates ARE GREEN by Plan 03.1-10 + Plan 03.1-12 + Plan 03.1-14 construction; the structural fixes ARE in the codebase; the open question is whether they're SUFFICIENT for fog-camera-tracking parity at the deepest coordinate-system axes). STATE.md captures the formal POC architectural counter-evidence. The Phase 3.2 alternative-renderer pivot conversation opens."
    - "On `ITERATING`: a Plan 03.1-16+ is opened in a follow-up plan-phase invocation, with Walk #6's diagnostic stream as input. THIS plan does NOT author 03.1-16. UX-02 + FOG-16 + FOG-17 + FOG-18 + FOG-19 still flip to `Complete — Verified-by-test + walk-time validated structurally` (the structural fixes ARE in the codebase; the open question is whether they're SUFFICIENT). PERF-07 updated with Walk #6 numbers regardless. FOG-04..07 + FOG-11 + FOG-15 stay `Falsified-in-production`. Phase 3.1 stays `In Progress (8th iteration, walk #7 pending)`."
    - "Phase 3 `03-FALSIFICATION.md` AND Phase 3.1 Walks #1 + #2 + #3 + #3b + #4 + #5 falsification documents AND `03.1-WALK-3B-MARKER-ANALYSIS.md` are LEFT UNTOUCHED — no edits, no overwrites. Historical records preserved across iteration cycles."
  artifacts:
    - path: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-6.md"
      provides: "Phase 3.1 Walk #6 falsification document — pre-walk gate-pass evidence + post-walk verdict (`CONFIRMED-AFTER-FIX` | `DENIED-FINAL` | `ITERATING`)"
      min_lines: 100
      contains: "Walk #6"
    - path: ".planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-6.md"
      provides: "Walk #6 evidence transcript — developer's verbatim verdict + interpretation + JSONL grep correlation + (if markers fired) marker-correlation analysis"
      min_lines: 40
      contains: "Walk Evidence"
  key_links:
    - from: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-6.md"
      to: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md"
      via: "explicit cross-reference in Walk #6's `## Relationship to Walk #5 Verdict` section ('Walk #5 closed iterating-with-Q1-regressed-and-Q5-not-fixed-from-period-commensurability-gap 2026-05-03; Walk #6 either confirms or denies the Plan 03.1-14 Fix B′ meter-space FOG-17a decomposition + FOG-19 software fix bundle')"
      pattern: "03\\.1-FALSIFICATION-5\\.md"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-6.md"
      via: "FOG-04..07 + FOG-11 + FOG-15 + FOG-18 + FOG-19 + UX-02 + FOG-16 + FOG-17 + PERF-07 status clauses include `(P03.1-15)` cross-reference; Revisions section appends a 2026-05-XX Walk #6 entry"
      pattern: "P03\\.1-15"
    - from: ".planning/STATE.md"
      to: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-6.md"
      via: "Current Position + Decisions sections capture the Walk #6 verdict outcome; build-provenance discipline blocker resolution if Walk #6 closes; Mail-share discipline blocker fully resolves if structural gate held second time"
      pattern: "Phase 3\\.1"
---

<objective>
Run the pre-walk gates, perform the iPhone 17 Pro sideload UAT Walk #6, capture the developer's verdict, and produce `03.1-FALSIFICATION-6.md` — the Phase 3.1 Walk #6 falsification document. Walk #6 is the empirical re-test for the Plan 03.1-14 software fix bundle: Fix B′ meter-space FOG-17a decomposition + FOG-19 (Dart-side `worldMeters = pixelOrigin × metersPerPixel; bounded = (intMeters % kPocFogIntegerWrapPeriodMeters) + fracMeters` with `kPocFogIntegerWrapPeriodMeters = 4096 m = 4 × kPocFogNoiseTilePxMeters`) + shader uniform slot 3..4 rename `uPixelOrigin` → `uWorldMetersOrigin`. The Walk #6 verdict either reverses Walk #5's `ITERATING-WITH-Q1-REGRESSED-AND-Q5-NOT-FIXED-FROM-PERIOD-COMMENSURABILITY-GAP` outcome to `CONFIRMED-AFTER-FIX` (unblocking Phase 4 + Phase 5 of the original roadmap) or strengthens it to `DENIED-FINAL` (terminating the POC's same-Canvas hypothesis at the deepest coordinate-system axis; Phase 3.2 alternative-renderer pivot conversation opens) or — per CONTEXT §Iteration policy — captures a seventh `ITERATING` verdict that opens Plan 03.1-16+.

Purpose: this is the empirical gate Phase 3.1 needs to close after the Plan 03.1-14 Fix B′ software fix bundle. Plan 03.1-14 software-completed FOG-19 (meta-requirement: decomposition operates in noise-anchor's coordinate space) + retargeted FOG-18. Walk #6 is the architectural-evidence gate that flips FOG-04..07 + FOG-11 + FOG-15 + FOG-18 + FOG-19 to Complete (or strengthens the verdict). Per CONTEXT §Iteration policy, there is no hard cap on walk count.

**WALK #6 KEY DIFFERENTIATORS vs Walks #1-5:**

1. **STRUCTURAL BUILD-PROVENANCE GATE (NEW for Walk #6)** — Plan 03.1-13 surfaced a build-provenance gap: Plan 03.1-13 Task 1a logger augmentation commit `2b087a3` post-dated CI run `25285037397`, leaving Walk #5's IPA without the metersPerPixel JSONL field empirically (mechanically derivable but non-empirical). Plan 03.1-15 makes build-provenance a STRUCTURAL pre-walk gate: Task 1 explicitly verifies `git rev-parse HEAD == <CI run's headSha>` BEFORE sideload. If HEAD has commits after the CI SHA, the executor pushes them and triggers a fresh CI run before proceeding. This eliminates the build-provenance gap class of failure.

2. **STRUCTURALLY-ENFORCED MAIL-SHARE GATE RETAINED** — the structural gate landed at Plan 03.1-13 (resume signal contract requires log path as parameter; executor refuses verdict authoring without it) and held at Walk #5 on first attempt. Walk #6 retains the gate identically. The discipline pattern is now established for any future POC walks.

3. **EXPLICIT Q1 RETENTION RE-TEST** at sustained ≥ 60-second one-direction pan: developer's Walk #5 markers tagged `steppy_translation` correlate with Y-axis FOG-17a pixel-space wraps in fog_transform JSONL. Walk #6 expectation: ZERO `steppy_translation` markers during sustained pan because the meter-space wrap aligns with the noise tile period (4096 m / 1024 m = 4 integer cells per wrap → invisible). Walk #6 explicitly asks the developer to fire markers if ANY residual stepping is perceived.

4. **EXPLICIT Q5 PRIMARY GATE RETEST** at zoom-traversal with debug-spiral toggle ON: same procedure as Walk #5 sub-tests 5a/5b/5c/5d. Walk #6 expectation: Q5 closes (cells enlarge with zoom; cells at fixed map position stay the same across zoom; production fog stable across zoom).

5. **NEW JSONL signature: `coordinateSpace: 'meters'` field** in fog_transform rollup body. Plan 03.1-14 Task 2 augmented the FogTransformLogger.recordPaint emit body with `coordinateSpace: 'meters'` for grep-correlation continuity (pre-Plan-03.1-14 logs implicit pixel-space; post-Plan-03.1-14 logs explicit meter-space). Post-walk grep verifies the field is present AND value is `'meters'`. The metersPerPixel field (Plan 03.1-13 augmentation) is now empirically captured because the build-provenance gate holds.

6. **Use the bbox-removed default** (`CameraConstraint.unconstrained()` — landed in Plan 03.1-11 mid-walk debug-aid commit `230db00`).

Output: 1 new `03.1-FALSIFICATION-6.md` (the load-bearing artefact); 1 new `03.1-UAT-6.md` (the walk-evidence transcript); REQUIREMENTS.md status updates for FOG-04..07 + FOG-11 + FOG-15 + FOG-16 + FOG-17 + FOG-18 + FOG-19 + UX-02 + PERF-07; ROADMAP.md Phase 03.1 Progress-table row update; STATE.md Current Position + Decisions + Blockers updates (resolve build-provenance discipline if Walk #6's structural gate succeeds; Mail-share discipline fully resolves if structural gate held second time). Phase 3's `03-FALSIFICATION.md` AND Phase 3.1 Walks #1 + #2 + #3 + #3b + #4 + #5 records ALL left UNTOUCHED.
</objective>

<execution_context>
@C:/Users/oliver/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oliver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-CONTEXT.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-2.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-3.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-4.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-5.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-2.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-3.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-4.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-5.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-WALK-3B-MARKER-ANALYSIS.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-VALIDATION.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-13-PLAN.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-13-SUMMARY.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-14-PLAN.md

<interfaces>
<!-- Walk #6 mirrors Walk #5 (Plan 03.1-13) shape-for-shape with Plan-03.1-14-specific specialisations + STRUCTURAL build-provenance gate addition. -->

`03.1-FALSIFICATION-5.md` Walk #5 structure (the immediate predecessor template):
1. Title + metadata (`Created`, `Phase`, `Walked on`, `Verdict`)
2. `## Relationship to Walk #4 Verdict` (Walk #6 inserts `## Relationship to Walk #5 Verdict`)
3. `## Hypothesis`
4. `## Falsification Criteria (written BEFORE the walk)` — `### Criterion A`, `### Criterion B` (Q1-Q5 sub-claims), `### Criterion C — DROPPED`, `### Criterion D — Diagnostic stream`
5. `## Walk Plan`
6. `## Walk Evidence (filled AFTER the walk)`
7. `## Verdict`

Walk #6's `03.1-FALSIFICATION-6.md` mirrors this exactly with these specialisations:

- Title: `# Phase 3.1 Falsification — Walk #6 (Post-Plan-03.1-14 Fix B′ Meter-Space FOG-17a Decomposition + FOG-19)`
- Add `## Relationship to Walk #5 Verdict` section pointing at `03.1-FALSIFICATION-5.md`; summarise the iterating-with-Q1-regressed-and-Q5-not-fixed-from-period-commensurability-gap outcome and the gap entries (FOG-19 NEW + FOG-18 retargeted) closed by Plan 03.1-14.
- Hypothesis re-statement: "After the Plan 03.1-14 software fix bundle (Fix B′ meter-space FOG-17a decomposition + FOG-19; Dart-side `worldMeters = pixelOrigin × metersPerPixel; bounded = (intMeters % 4096) + fracMeters`; shader uniform slot 3..4 rename `uPixelOrigin` → `uWorldMetersOrigin`; period-commensurability invariant `kPocFogIntegerWrapPeriodMeters % kPocFogNoiseTilePxMeters == 0` (= `4096 % 1024 == 0` = 4 cells per wrap)), the same-Canvas fog now: (a) translates with the camera during pan WITHOUT visible stepping at wrap events (Walk #5 Q1 regression closes — wrap injects integer-cell shift in noiseUv, indistinguishable from no shift); (b) is structurally invulnerable to rotation-correlated mis-coverage (UX-02 retained — Plan 03.1-14 doesn't touch the rotation handling); (c) maintains a fully stable noise pattern across consecutive paints AT A SINGLE ZOOM LEVEL (FOG-17 + FOG-19 retained — the world-coordinate sampling + integer/fractional decomposition both apply in meter space); (d) keeps the reveal hole co-located with the blue dot during pan/zoom (FOG-12 + FOG-13 retained); (e) is anchored to ground meters such that at a fixed geographic point, the noise sample is zoom-INVARIANT — cells visibly enlarge when zoomed in, shrink when zoomed out, and the noise pattern at any given map position stays perceptually stable across zoom transitions (FOG-18 retained — Walk #5 Q5 zoom-scramble closes because bounded_meters is geographic, not pixel)."

- Criterion A: same thresholds (PERF-07 carry-over): `medianMs ≤ 16`, `p95Ms ≤ 32`, `maxMs ≤ 48`. Walk #5 captured 8.9×/5.2×/4.9× headroom (median 1.808 / p95 6.105 / max 9.756 across 67 1-Hz rollups). Walk #6 should preserve at least Walk #5 levels (Plan 03.1-14 is per-paint negligible — formula reorder, not new operations).

- Criterion B sub-claims:
  - **Q1 (PRIMARY Walk #6 closure-evidence-gate):** "ZERO `dev_marker steppy_translation` taps during sustained ≥ 60-second one-direction pan" — Walk #5 fired 2 such markers; Walk #6 expects zero. The mechanism: meter-space wraps inject `+4` integer cells in noiseUv → bit-identical noise sampling pre/post-wrap → invisible.
  - **Q2 (RETAINED from Walks #4-5):** "No rotation-correlated fog mis-coverage" — Plan 03.1-14 doesn't touch UX-02 / FOG-16. Re-confirmation.
  - **Q3 (RETAINED from Walks #1-5):** "No reveal-hole offset from blue dot during gesture" — re-confirmation under Plan 03.1-14's coordinate-system change.
  - **Q4 (RETAINED from Walks #1-5):** "Fog visually translates with camera during pan" — re-confirmation; Plan 03.1-14 doesn't touch the translation path (only the decomposition/anchor coordinate space).
  - **Q5 (PRIMARY Walk #6 closure-evidence-gate, retest from Walk #5):** "Noise pattern is anchored to ground meters; at a fixed geographic point the noise sample is zoom-invariant; cells visibly enlarge with zoom" — Walk #5 reported failure (`zooming still moves the mirl all around giving a noisy feeling`); Plan 03.1-14 closes this because bounded_meters is geographic. Sub-tests identical to Walk #5 (5a production fog at fixed position; 5b debug-spiral with cell-number invariance; 5c debug-spiral pan at fixed zoom; 5d rapid-zoom comparison).
  - No white-ellipse during fast pinch-zoom (already passed Walks #1-5).
  - No inversion at any zoom level (already passed Walks #1-5).

- Criterion C stays DROPPED.

- Criterion D (Diagnostic stream): `pixelOriginX/Y` track gesture monotonically; `appliedUOffset` keys (semantic flipped from pixel-space to meter-space at Plan 03.1-14) bounded under `kPocFogIntegerWrapPeriodMeters + 1 = 4097 m` regardless of zoom level — Plan 03.1-14 invariant MUST hold. **NEW for Walk #6: `coordinateSpace: 'meters'` field present in fog_transform rollup body** — Plan 03.1-14 Task 2 augmentation; verifies the JSONL semantics flip landed in the IPA. **metersPerPixel field present and consistent with synthetic camera state** — Plan 03.1-13 augmentation, NOW empirically captured because build-provenance gate holds (vs Walk #5 where commit `2b087a3` post-dated CI run). At zoom 13 lat 48.5° expected ≈ 12.66; at zoom 15 ≈ 3.16; at zoom 19 ≈ 0.198. `canvasTx/Ty` jumps still occur — Plan 03.1-08 FOG-13 compensates them mechanically. **dev_marker JSONL signature for Walk #6 closure:** ZERO `steppy_translation` markers during the walk (or, if any fired, marker-correlation analysis MUST show NO correlation with `appliedUOffset` wrap-boundary crossings — confirming the wrap is invisible).

`03.1-UAT-6.md` mirrors `03.1-UAT-5.md` shape-for-shape: metadata block + walk protocol + walk evidence (frame-delta + SDF + fog-transform rollups + dev_marker rollup if any markers fired) + subjective verdict + verdict + deviations + summary + (optional) gaps section if Walk #6 surfaces residual failure modes + `gaps:` YAML in frontmatter for any opened-by-Walk-#6 gap entries.

The pre-walk gate sequence extends Plan 03.1-13 Task 1 with a NEW STRUCTURAL build-provenance step (between the local-test step and the CI-status step):
- Local test gates (4): `flutter test`, `flutter analyze --fatal-infos`, `dart format --line-length 160 --set-exit-if-changed .`, `dart test tool/test/`.
- CI status: `gh run list --branch main --limit 5` shows `success` for the post-Plan-03.1-14 SHA.
- **NEW: build-provenance check** — `gh run view <RUN_ID> --json headSha` returns the CI's source SHA; compare against `git rev-parse HEAD`; MUST be equal. If unequal: push HEAD; trigger fresh CI run; wait for green; download new IPA.
- IPA download: `gh run download <RUN_ID> --name ios-unsigned --dir .uat-tmp/`.

The walk shape extends Plan 03.1-13 with: (1) STRUCTURAL build-provenance gate at Task 1; (2) explicit Q1 retention re-test asking developer to fire `steppy_translation` markers if ANY residual stepping perceived during sustained pan; (3) NEW `coordinateSpace: 'meters'` JSONL signature verification at Task 3.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the pre-walk `03.1-FALSIFICATION-6.md` skeleton + run the pre-walk gate sequence (incl. STRUCTURAL build-provenance check)</name>
  <files>.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-6.md, .planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-6.md</files>
  <action>
    Create `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-6.md`. Mirror the structural shape of `03.1-FALSIFICATION-5.md` with the specialisations described in the §Interfaces block above. Use the executor's actual current date for the `Created:` field; leave `Walked on:` and `Verdict:` as `TBD` until Task 3 fills them.

    Top-of-file metadata block:
    ```markdown
    # Phase 3.1 Falsification — Walk #6 (Post-Plan-03.1-14 Fix B′ Meter-Space FOG-17a Decomposition + FOG-19)

    **Created:** 2026-05-XX  (executor: fill in real date)
    **Phase:** 03.1-fix-fog-pan-translation
    **Walked on:** TBD  (filled post-walk: iPhone 17 Pro, central Melun, CI run `<RUN_ID>`, SHA `<SHA>`)
    **Verdict:** TBD  (filled post-walk: `CONFIRMED-AFTER-FIX` | `DENIED-FINAL` | `ITERATING — see 03.1-16+`)

    ## Relationship to Walk #5 Verdict

    Walk #5 (Plan 03.1-13, 2026-05-03) closed `ITERATING-WITH-Q1-REGRESSED-AND-Q5-NOT-FIXED-FROM-PERIOD-COMMENSURABILITY-GAP`. The Plan 03.1-12 FOG-18 software-fix bundle (world-meter anchor in BOTH atmospheric_fog.frag + atmospheric_fog_debug_spiral.frag + new uniform uMetersPerPixel slot 41 + Dart-side metersPerPixel computation in 4 painter sites) walk-time-evaluated as architecturally landed correctly (cells now scale with zoom in meter-space; noise position is zoom-invariant in meter-space at the single-frame level) BUT Plan 03.1-12 RETAINED the FOG-17a integer/fractional decomposition in PIXEL space, leaving a period-commensurability gap with the meter-space noise tile. The 1536-px wrap translated to a 4.7421-cell shift in noiseUv at z=15 lat 48.5° — a 0.7421-cell visible discontinuity per wrap event. Walk #5 dev-markers tagged `steppy_translation` correlated with Y-axis FOG-17a wrap events in fog_transform JSONL within ±1 sec. Q1 panning REGRESSED from Walk #4's *"perfect"*; Q5 zoom-scramble NOT FIXED.

    Walk #5 STRUCTURAL ACHIEVEMENT — Mail-share gate held on first attempt. The STRUCTURALLY-ENFORCED Mail-share gate (resume-signal contract requires log path as parameter) succeeded after Walks #3 + #4 broke Mail-share twice. Quantitative JSONL grep correlation against the developer's 2 dev-markers anchored the architectural diagnosis empirically.

    Walk #5 BUILD-PROVENANCE GAP — the Plan 03.1-13 Task 1a logger augmentation commit `2b087a3` (FogTransformLogger metersPerPixel field) post-dated CI run `25285037397`. Walk #5's IPA lacks the empirical metersPerPixel JSONL signature; mechanically derivable but non-empirical. Walk #6 closes this gap with a STRUCTURAL pre-walk build-provenance check.

    Plan 03.1-14 closed FOG-19 software-side via the Fix B′ meter-space FOG-17a decomposition: Dart-side `worldMeters = pixelOrigin × metersPerPixel; bounded = (intMeters % kPocFogIntegerWrapPeriodMeters) + fracMeters` with `kPocFogIntegerWrapPeriodMeters = 4096 m = 4 × kPocFogNoiseTilePxMeters` (1024 m). Shader uniform slot 3..4 RENAMES from `uPixelOrigin` to `uWorldMetersOrigin` (semantic flip; FogShaderUniforms.totalFloatSlots STAYS at 42 — no new uniform). Period-commensurability invariant `4096 % 1024 == 0` GREEN; wrap injects exactly 4 integer cells in noiseUv → invisible across all FBM octaves (mathematical proof in `_FogPainter.paint()` decomposition block + `kPocFogIntegerWrapPeriodMeters` docstring). NEW v1 requirement FOG-19 captures the meta-invariant. NEW behavioural tests `fog_meter_space_decomposition_test` + `fog_meter_wrap_invisibility_test` GREEN. 7 REWRITTEN tests flip pixel-space invariants to meter-space.

    Walks #1 + #2 + #3 + #3b + #4 + #5 records (`03.1-FALSIFICATION.md`, `03.1-FALSIFICATION-2.md`, `03.1-FALSIFICATION-3.md`, `03.1-FALSIFICATION-4.md`, `03.1-FALSIFICATION-5.md`, `03.1-UAT.md` through `03.1-UAT-5.md`, `03.1-WALK-3B-MARKER-ANALYSIS.md`) AND Phase 3 record (`03-FALSIFICATION.md`) ALL stand as historical record — UNTOUCHED. Walk #6's verdict is a SEPARATE artefact in this file.

    ## Hypothesis

    After the Plan 03.1-14 software fix bundle (Fix B′ meter-space FOG-17a decomposition + FOG-19; shader uniform slot 3..4 rename uPixelOrigin → uWorldMetersOrigin; period-commensurability invariant `kPocFogIntegerWrapPeriodMeters % kPocFogNoiseTilePxMeters == 0` GREEN), the same-Canvas fog now:
    (a) translates with the camera during pan WITHOUT visible stepping at wrap events (Walk #5 Q1 regression closes — wrap injects exactly +4 integer cells in noiseUv, indistinguishable from no shift in the rendered FBM);
    (b) is structurally invulnerable to rotation-correlated mis-coverage (UX-02 retained — Plan 03.1-14 doesn't touch the rotation handling);
    (c) maintains a fully stable noise pattern across consecutive paints AT A SINGLE ZOOM LEVEL (FOG-17 + FOG-19 retained — the world-coordinate sampling + integer/fractional decomposition both apply in meter space);
    (d) keeps the reveal hole co-located with the blue dot during pan/zoom (FOG-12 + FOG-13 retained);
    (e) is anchored to ground meters such that at a fixed geographic point, the noise sample is zoom-INVARIANT (FOG-18 retained, with Walk #5 Q5 zoom-scramble closing because bounded_meters is geographic, not pixel).

    Re-stated against the original POC core value: the fog-of-war stays perfectly locked to the map during pan, zoom, and combined gestures on a sideloaded iOS build. **CONFIRMED-AFTER-FIX** means the Phase 3 hypothesis is reinstated and the POC ports back to MirkFall with the full Plan 03.1-02 + 03.1-04 + 03.1-05 + 03.1-07 + 03.1-08 + 03.1-10 + 03.1-12 + 03.1-14 fix bundle; **DENIED-FINAL** means even Fix B′ meter-space FOG-17a decomposition doesn't deliver fog-camera-tracking parity (Phase 3.2 alternative-renderer pivot conversation); **ITERATING** means a residual failure mode surfaces (e.g., a NEW second-order failure introduced by Plan 03.1-14, OR fp32 precision degradation at extreme zoom in meter-space — the Plan 03.1-14 SUMMARY's documented sub-cell precision held mathematically but FBM-rotated-octave behaviour can be empirically surprising).

    ## Falsification Criteria (written BEFORE the walk)

    ### Criterion A — Frame-delta probe thresholds (PERF-07 re-validation, quantitative)

    Same thresholds as Walks #1-5: `medianMs ≤ 16`, `p95Ms ≤ 32`, `maxMs ≤ 48` across ≥ 10 deliberate combined gestures. Walk #1: 4-7×; Walk #2: 3.9-5.4×; Walk #3b: 40-110× headroom on a 33-sec window; Walks #3 + #4: implicit (no log Mail-shared); Walk #5: 8.9×/5.2×/4.9× (median 1.808 / p95 6.105 / max 9.756). Walk #6 should preserve at least Walk #5 levels (Plan 03.1-14 adds 2 multiplications + 1 modulo + 1 truncate + 1 subtract per axis per paint at the Dart side — negligible; the shader gains nothing per-fragment — formula reorder, not new operations).

    **STRUCTURALLY-ENFORCED Mail-share gate retained from Plan 03.1-13** — Walk #5 succeeded on first attempt; Walk #6 retains identically.

    ### Criterion B — Subjective visual lock (PERF-05 carry-over, re-tested)

    Developer's verbal verdict at end of walk:

    - **Q1 (PRIMARY Walk #6 closure-evidence-gate)** — ZERO `dev_marker steppy_translation` taps during sustained ≥ 60-second one-direction pan. Walk #5 fired 2 such markers; Walk #6 expects zero. Mechanism: meter-space wraps inject +4 integer cells in noiseUv → bit-identical noise sampling pre/post-wrap → invisible.
    - **Q2 (RETAINED from Walks #4-5)** — No rotation-correlated fog mis-coverage. Plan 03.1-14 doesn't touch UX-02 / FOG-16. Sub-test: developer attempts a deliberate two-finger rotate gesture; the map should NOT rotate.
    - **Q3 (RETAINED from Walks #1-5)** — No reveal-hole offset from blue dot during gesture.
    - **Q4 (RETAINED from Walks #1-5)** — Fog visually translates with camera during pan.
    - **Q5 (PRIMARY Walk #6 closure-evidence-gate, retest from Walk #5)** — Noise pattern is anchored to ground meters; at a fixed geographic point the noise sample is zoom-invariant; cells visibly enlarge with zoom. Walk #5 reported failure; Plan 03.1-14 closes this because bounded_meters is geographic.
      - Sub-test 5a (production fog): developer fixes camera centre on lat 48.5397, lon 2.6553 and zooms from min to max; verifies the production fog noise pattern at the camera centre appears stable across zoom transitions (no rapid sliding/scramble).
      - Sub-test 5b (debug-spiral): developer toggles debug-spiral ON; zooms from min to max; verifies (a) cells visibly enlarge with zoom (cells are tied to ground); (b) at fixed map position the spiral cell numbering at the camera centre stays the same across all zoom levels (the fragment under the blue dot should always sample the same noise cell, regardless of zoom — direct visual confirmation of FOG-18 + FOG-19 zoom-invariance acceptance property).
      - Sub-test 5c (debug-spiral pan during fixed zoom): with debug-spiral ON at z=15, pan the camera ~500 m. Cells should scroll smoothly with NEW noise entering from the viewport edges (no fract-wrap; FOG-17 retained).
      - Sub-test 5d (rapid-zoom comparison): for direct comparison with Walk #5 (where Q5 failed), zoom in/out QUICKLY (rapid pinch). Pre-Plan-03.1-14 the noise pattern slid rapidly during zoom (the "scramble"); post-Plan-03.1-14 it should appear stable.
    - No white-ellipse during fast pinch-zoom (already passed Walks #1-5).
    - No inversion at any zoom level (already passed Walks #1-5).

    ### Criterion C — DROPPED

    Stays dropped per Phase 3 + roadmap lock.

    ### Criterion D — Diagnostic stream confirms the layered fix

    Per the `Logger('infrastructure.mirk.fog_transform')` 1-Hz JSONL rollups:

    - **CONFIRMED-AFTER-FIX:** `pixelOriginX/Y` track gesture monotonically. `appliedUOffset` keys (semantic flipped from pixel-space to meter-space at Plan 03.1-14) stay under `kPocFogIntegerWrapPeriodMeters + 1` (~4097 m) regardless of zoom level — the FOG-19 invariant MUST hold. **NEW: `coordinateSpace: 'meters'` field present** in fog_transform rollup body — Plan 03.1-14 Task 2 augmentation; verifies the JSONL semantics flip landed in the IPA. **metersPerPixel field present and consistent with synthetic camera state** — Plan 03.1-13 augmentation, NOW empirically captured because build-provenance gate holds. At zoom 13 lat 48.5° expected ≈ 12.66; at zoom 15 ≈ 3.16; at zoom 19 ≈ 0.198. `canvasTx/Ty` jumps still occur — Plan 03.1-08 FOG-13 compensates them mechanically. **dev_marker JSONL signature for Walk #6 closure:** ZERO `steppy_translation` markers during the walk (or, if any fired, marker-correlation analysis MUST show NO correlation with `appliedUOffset` wrap-boundary crossings — confirming the wrap is invisible).
    - **DENIED-FINAL pattern:** the diagnostic stream shows the layered fix landed mechanically AT EVERY level (pixelOrigin tracks; appliedUOffset bounded under 4097 m; coordinateSpace='meters'; metersPerPixel correctly varies; canvas.translate compensates rect-cover; UX-02 makes camera.rotation = 0.0) AND the user-observable Q1/Q5 STILL persist — root cause is even deeper than meter-space FOG-17a (e.g., FBM-rotated-octave non-trivial behaviour at integer-cell shifts that the continuity proof understates; or fp32 precision empirically degrades sooner than analysed). Architecture is unfixable in `flutter_map 7.0.2` with the current shader stack. Phase 3.2 alternative-renderer pivot opens.
    - **ITERATING pattern:** subjective verdict surfaces a residual failure mode. Most likely candidates: (i) NEW second-order failure mode introduced by Plan 03.1-14 (e.g., the meter-space modulo at extreme worldMeters values produces a Dart-side fp64 precision jitter visible in the rendered output); (ii) FBM-rotated-octave non-integer-multiple shift that wasn't fully captured by the continuity proof; (iii) Q2/Q3/Q4 regression introduced by Plan 03.1-14 (the painter signature changes carry risk). The diagnostic stream + (if markers fired) marker-correlation analysis points at the next fix axis; Plan 03.1-16+ is opened.

    ## Walk Plan

    Mirrors Plan 03.1-13 (Walk #5) walk shape with one NEW STRUCTURAL addition for Walk #6:

    - **NEW: STRUCTURAL build-provenance gate.** Pre-walk Task 1 explicitly verifies `git rev-parse HEAD == <CI run's headSha>`. If HEAD has commits after the CI SHA, the executor pushes them and triggers a fresh CI run BEFORE proceeding. This eliminates the build-provenance gap class of failure (Walk #5 commit `2b087a3` post-dated CI run `25285037397`).
    - **STRUCTURALLY-ENFORCED Mail-share discipline RETAINED** from Plan 03.1-13. The resume-signal contract REQUIRES the absolute path of the Mail-shared session log file as a parameter. Walk #5 succeeded on first attempt; Walk #6 retains identically.
    - **EXPLICIT Q1 retention re-test** at sustained ≥ 60-second one-direction pan: developer is instructed to fire `steppy_translation` dev-markers if ANY residual stepping is perceived during sustained pan.
    - **EXPLICIT Q5 PRIMARY GATE retest** at zoom-traversal with debug-spiral toggle ON: same procedure as Walk #5 sub-tests 5a/5b/5c/5d.

    - **Where:** Central Melun (lat 48.5397, lon 2.6553 area).
    - **Duration:** ≥ 5 minutes continuous walk with the IPA running and the device awake.
    - **Gestures:** ≥ 10 deliberate combined pinch-zoom-and-pan gestures, ≥ 3 recenter FAB taps. AT LEAST ONE deliberate two-finger rotate-attempt gesture (validates Q2). AT LEAST ONE sustained ≥ 60-second one-direction pan (validates Q1 — PRIMARY closure-evidence-gate). AT LEAST ONE complete zoom traversal (min zoom → max zoom or max → min) at a fixed map position with both production fog AND debug-spiral toggled (validates Q5 — PRIMARY closure-evidence-gate).
    - **Pre-walk gate (unit tests):** `flutter test` GREEN (Plan 03.1-14 new tests included); `flutter analyze --fatal-infos` 0 warnings; `dart format --line-length 160 --set-exit-if-changed .` clean; `dart test tool/test/` GREEN.
    - **Pre-walk gate (CI):** `gh run list --branch main --limit 5` shows `success` for the post-Plan-03.1-14 SHA; `gh run view <RUN_ID>` confirms `gates`, `build-android`, `build-ios` all green.
    - **Pre-walk gate (BUILD-PROVENANCE — STRUCTURAL):** `gh run view <RUN_ID> --json headSha` returns the CI's source SHA; `git rev-parse HEAD` returns local HEAD; the two MUST be equal. If unequal: push HEAD, trigger fresh CI run, wait for green, download new IPA.
    - **Pre-walk gate (IPA):** `gh run download <RUN_ID> --name ios-unsigned --dir .uat-tmp/` (produces `.uat-tmp/mirk-poc-debug-unsigned.ipa`).
    - **Pre-walk gate (sideload):**
      1. Open `/sanity` route — confirm production fog renders with circular reveal hole (debug-spiral toggle OFF default).
      2. Toggle the debug-spiral switch ON — confirm the digit-atlas spiral pattern renders with visible numbered cells (DEBUG-01 walk-time validation).
      3. Toggle OFF — confirm production fog returns.
      4. Confirm the `Icons.arrow_back` AppBar leading button still works on `/sanity` (UX-01 retained).
      5. Confirm zero shader-compile exceptions in the FileLogger output (Plan 03.1-14's slot-3/4 rename + worldMeters formula flip MUST NOT produce a compile error in the post-Plan-03.1-14 IPA).
      6. Attempt a two-finger rotate gesture on `/map` — confirm the map does NOT rotate (UX-02 walk-time validation).
    - **In-app HUD during walk:** `FpsCounterOverlay` (top:8, right:8) + `MapCompass` (top:56, right:8 — should stay at 0° throughout per UX-02) + `FrameDeltaProbeOverlay` (top:104, right:8).
    - **Post-walk:** **STRUCTURALLY-ENFORCED.** Share the session log file via Mail (LOG-04 round-trip — REQUIRED for Walk #6; structural gate retained from Plan 03.1-13). Save the Mail attachment to a known local path. Report the absolute path as a parameter in the resume signal: `approved [/full/path/to/walk6_session_log.txt] — Q1+Q5 closed by Plan 03.1-14 + Q2-Q3-Q4 retained`. The executor then runs grep against the actual log file before authoring the verdict.

    ## Walk Evidence (filled AFTER the walk by Plan 03.1-15 Task 3)

    ### Pre-walk shader-sanity gate (`/sanity` route + UX-01 back-button validation + DEBUG-01 toggle live-test + UX-02 rotation-disabled walk-time validation + Plan 03.1-14 cell-size visual confirmation)
    _TBD — filled post-walk._

    ### Probe rollup (frame-delta JSONL — `Logger('infrastructure.mirk.frame_delta')`)
    _TBD — filled post-walk. Expected: median ≤ 16 ms, p95 ≤ 32 ms, max ≤ 48 ms across ≥ 10 combined gestures (PERF-07 re-validation; Walk #5 8.9×/5.2×/4.9×; Walk #6 should preserve)._

    ### SDF rebuild rollup (`Logger('infrastructure.mirk.sdf')` — PERF-08 status)
    _TBD — filled post-walk. PERF-08 was falsified-by-walk-2; not load-bearing on Plan 03.1-14 fix axis._

    ### Fog-transform rollup (`Logger('infrastructure.mirk.fog_transform')`)
    _TBD — filled post-walk._
    _**KEY Plan 03.1-14 SIGNATURE:** appliedUOffsetXMin/Median/Max values bounded under `kPocFogIntegerWrapPeriodMeters + 1 = 4097 m` across ALL rollups (FOG-19 invariant MUST hold) — vs Walk #5's pixel-space bounded composite under 1537 raw px._
    _**NEW Walk #6 coordinateSpace SIGNATURE:** `coordinateSpace: 'meters'` field present in rollup body (Plan 03.1-14 Task 2 augmentation; verifies the JSONL semantics flip landed in the IPA)._
    _**metersPerPixel SIGNATURE — NOW EMPIRICAL:** Plan 03.1-13 augmentation, captured at Walk #6 because build-provenance gate holds. At zoom 13 lat 48.5° expected ≈ 12.66; at zoom 15 ≈ 3.16; at zoom 19 ≈ 0.198. The metersPerPixel field should evolve smoothly with camera zoom across the walk (no discontinuities)._

    ### Dev-marker rollup (`infrastructure.mirk.dev_marker`)
    _TBD — filled post-walk. Walk #6 closure-evidence-gate: ZERO `steppy_translation` markers during sustained pan. If any markers fired, mirror the Walk #3b template: per-marker pixelOrigin delta, marker-to-marker pan magnitude, correlation against the meter-space wrap-period boundary (was the marker tap on or near a `appliedUOffsetX/Y` crossing of 4096 m? — at Plan 03.1-14 active formulation a wrap is invisible, so the developer should not perceive it; if they did, the architectural model is wrong)._

    ### FPS observations (PERF-03 — coarse `FpsCounterOverlay` reading)
    _TBD — filled post-walk._

    ### Subjective verdict (Criterion B — Q1-Q5)
    _TBD — filled post-walk. Developer's verbatim words + interpretation paragraph mapping each Criterion B sub-claim (Q1-Q5) to PASS/FAIL. **Q1 + Q5 are PRIMARY Walk #6 closure-evidence-gates** — both must close for `CONFIRMED-AFTER-FIX`._

    ## Verdict

    - [ ] Criterion A passed? — TBD post-walk.
    - [ ] Criterion B passed? — TBD post-walk.
    - [ ] Criterion D passed? — TBD post-walk.

    **Outcome:** TBD.

    **MirkFall migration recommendation:** TBD.
    ```

    Then create `.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-6.md` as the thin Walk #6 transcript skeleton — mirror `03.1-UAT-5.md`'s structure with metadata block + Walk Protocol + empty `## Walk Evidence` section + `## Verdict` placeholder + `## Deviations / Surprises` placeholder + `## Summary` placeholder. The frontmatter `gaps:` YAML stays empty until Task 3 fills it (or stays empty on `approved` / `denied` outcomes).

    NOW execute the pre-walk gate sequence:
    1. `flutter test` (must report 0 failures, 0 reds — Plan 03.1-14 new tests + REWRITTEN tests + TOUCHED tests all GREEN; the FOG-09 vacuous-test guard stays SKIPPED)
    2. `flutter analyze --fatal-infos` (must exit 0)
    3. `dart format --line-length 160 --set-exit-if-changed .` (must exit 0)
    4. `dart test tool/test/` (must report all GREEN)
    5. `gh run list --branch main --limit 5` (capture the post-Plan-03.1-14 SHA + run ID; the run must be `success`)
    6. **STRUCTURAL build-provenance check (NEW for Walk #6):**
       - `gh run view <RUN_ID> --json headSha,status` (capture the CI's source SHA)
       - `git rev-parse HEAD` (capture local HEAD)
       - Compare: `[[ $LOCAL_HEAD == $CI_HEAD_SHA ]]` MUST be true.
       - If unequal: STOP and report. Push HEAD; trigger a fresh CI run via `git push origin main`; re-run step 5 + 6 against the new run ID.
       - If equal: proceed.
    7. `gh run view <RUN_ID>` (confirm `gates`, `build-android`, `build-ios` all `success`)
    8. `gh run download <RUN_ID> --name ios-unsigned --dir .uat-tmp/` (produces `.uat-tmp/mirk-poc-debug-unsigned.ipa`)

    If ANY pre-walk gate fails, STOP and report. Do NOT proceed to Task 2 until every gate is green AND the build-provenance check passes.
  </action>
  <verify>
    <automated>flutter test 2>&amp;1 | tail -3 &amp;&amp; flutter analyze --fatal-infos 2>&amp;1 | tail -3 &amp;&amp; dart format --line-length 160 --set-exit-if-changed . 2>&amp;1 | tail -3 &amp;&amp; dart test tool/test/ 2>&amp;1 | tail -3 &amp;&amp; gh run list --branch main --limit 1 2>&amp;1 | tail -3 &amp;&amp; CI_RUN_ID=$(gh run list --branch main --limit 1 --json databaseId --jq '.[0].databaseId') &amp;&amp; CI_HEAD_SHA=$(gh run view "$CI_RUN_ID" --json headSha --jq '.headSha') &amp;&amp; LOCAL_HEAD=$(git rev-parse HEAD) &amp;&amp; [ "$CI_HEAD_SHA" = "$LOCAL_HEAD" ] &amp;&amp; echo "BUILD-PROVENANCE: PASS — $LOCAL_HEAD" || echo "BUILD-PROVENANCE: FAIL — local=$LOCAL_HEAD ci=$CI_HEAD_SHA"</automated>
    <file_must_exist>.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-6.md</file_must_exist>
    <file_must_exist>.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-6.md</file_must_exist>
    <file_must_exist>.uat-tmp/mirk-poc-debug-unsigned.ipa</file_must_exist>
  </verify>
  <done>
    - All 4 local pre-walk gates exit 0 (test / analyze / format / tool-tests).
    - CI run on the post-Plan-03.1-14 SHA on `main` is `success` (gates + build-android + build-ios all green).
    - **STRUCTURAL build-provenance gate PASSES — `git rev-parse HEAD` equals `gh run view <RUN_ID> --json headSha`.** If gate failed, executor pushed local HEAD and triggered fresh CI run; new run ID captured + verified.
    - `.uat-tmp/mirk-poc-debug-unsigned.ipa` exists and is non-empty (≥ 5 MB).
    - `03.1-FALSIFICATION-6.md` exists with all top-level sections + the Relationship-to-Walk-5 section. Walk Evidence + Verdict bodies read `TBD`.
    - `03.1-UAT-6.md` exists as a thin transcript skeleton.
    - `git status` shows the existing `03.1-FALSIFICATION.md` + `03.1-FALSIFICATION-2.md` + `03.1-FALSIFICATION-3.md` + `03.1-FALSIFICATION-4.md` + `03.1-FALSIFICATION-5.md` + `03.1-UAT.md`...`03.1-UAT-5.md` + `03.1-WALK-3B-MARKER-ANALYSIS.md` UNCHANGED (Walks #1-5 historical records preserved).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: iPhone 17 Pro sideload UAT Walk #6 + STRUCTURALLY-ENFORCED Mail-share discipline + post-walk verbal verdict capture</name>
  <files>.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-6.md, .planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-6.md</files>
  <action>
    Hand off to the developer for the manual UAT Walk #6 on iPhone 17 Pro. The full procedure is documented in the checkpoint-specific `<what-built>`, `<how-to-verify>`, and `<resume-signal>` sections — those are the executor-facing instructions. **CRITICAL:** the resume-signal contract REQUIRES the absolute path of the Mail-shared session log file as a parameter; the executor MUST refuse verdict authoring without the log path. After the resume signal is received with a valid log path, Claude proceeds to Task 3 (verdict authoring + REQUIREMENTS/ROADMAP/STATE updates).
  </action>
  <verify>
    <automated>MISSING — PERF-07 + Criterion B (Q1-Q5 sub-claims) + Criterion D (subjective + diagnostic-stream pattern matching + dev-marker correlation if markers fire + coordinateSpace JSONL signature verification + metersPerPixel JSONL signature verification) are inherently manual UAT walk gates per 03.1-VALIDATION.md Manual-Only Verifications table; no automated proxy exists. The developer&#39;s resume-signal IS the verification.</automated>
  </verify>
  <done>
    Developer returns with one of (each requires log_path parameter):
    (a) `approved [/full/path/to/walk6_session_log.txt] — Q1+Q5-closed-by-Plan-03.1-14 + Q2-Q3-Q4-retained` — all five Q1-Q5 sub-claims confirmed; CONFIRMED-AFTER-FIX.
    (b) `denied [/full/path/to/walk6_session_log.txt]: <verbatim description>` — describe what went wrong; the executor opens post-walk JSONL grep against the log file.
    (c) `iterating [/full/path/to/walk6_session_log.txt]: <verbatim description>` — same as denied: but developer chooses to iterate (Plan 03.1-16+).
    Plus the post-walk session log shared via Mail (LOG-04 round-trip) AND saved to a local path the executor can read. **The log_path parameter is REQUIRED** — the executor refuses verdict authoring without it.
  </done>
  <what-built>
    The Plan 03.1-14 software fix bundle is complete:

    - **FOG-19 (Fix B′ meter-space FOG-17a decomposition):** `lib/presentation/widgets/fog_layer.dart` `_FogPainter.paint()` decomposes the camera anchor in METER space: `worldMeters = pixelOrigin × metersPerPixel; bounded = (intMeters % 4096) + fracMeters`. `lib/presentation/widgets/debug_spiral_layer.dart` `_DebugSpiralMapPainter.paint()` mirrors. `lib/presentation/screens/shader_sanity_screen.dart` `_DebugSpiralPainter` synthetic /sanity mirror.
    - **Shader uniform slot 3..4 RENAMED** `uPixelOrigin` → `uWorldMetersOrigin` in BOTH `assets/shaders/atmospheric_fog.frag` AND `assets/shaders/atmospheric_fog_debug_spiral.frag`. Slot indices unchanged; `FogShaderUniforms.totalFloatSlots` STAYS at 42 — no new uniform.
    - **Shader worldMeters formula REORDERED** from FOG-18 era `(fragUv * uResolution + uPixelOrigin) * uMetersPerPixel` to Fix B′ active `(fragUv * uResolution) * uMetersPerPixel + uWorldMetersOrigin`. The fragment-offset-in-meters term is computed inside the shader; the camera-anchor term (`uWorldMetersOrigin`) is the bounded meter composite forwarded by Dart. const `kNoiseTilePxMeters = 1024.0` UNCHANGED.
    - **NEW constant `kPocFogIntegerWrapPeriodMeters = 4096.0`** = 4 × `kPocFogNoiseTilePxMeters` (1024 m). Period-commensurability invariant `4096 % 1024 == 0` GREEN. At every wrap event, `worldMetersX` jumps by 4096 m → noiseUv jumps by exactly 4 integer cells → wrap is INVISIBLE for the base octave AND for FBM-rotated octaves (mathematical proof in `_FogPainter.paint()` decomposition block + `kPocFogIntegerWrapPeriodMeters` docstring + Plan 03.1-14 `continuity_proof_for_plan_03_1_14` block).
    - **FOG-17a pixel-space FOG-17a decomposition SUPERSEDED.** Constant `kPocFogIntegerWrapPeriodPx = 1536.0` retained with prepended supersession docstring (git history + historical regression sub-tests in `@Tags(['historical'])` groups).
    - **FogTransformLogger JSONL emit body** adds `coordinateSpace: 'meters'` field for grep-correlation continuity. `appliedUOffset` semantic flipped from pixel-space (Plan 03.1-12 era) to meter-space (Plan 03.1-14 era); field name preserved for grep-correlation continuity.
    - **NEW behavioural tests** `fog_meter_space_decomposition_test.dart` (FOG-19 sweep across ≥ 3 wrap events; integer-cell shifts only) + `fog_meter_wrap_invisibility_test.dart` (two-paint straddle test; (Δ noiseUv) modulo 1.0 ≈ 0 within fp32 precision). Both GREEN.
    - **7 REWRITTEN tests** flip pixel-space invariants to meter-space: fog_world_meter_anchor (FOG-18 zoom-invariance under new formula); fog_meter_decomposition (decomposition correctness in meter space); fog_world_coordinate_noise (ZERO wraps in meter space); fog_pixel_origin_decomposition (boundedMeters in [0, 4097] regardless of pixelOrigin magnitude; historical pixel-space sub-tests in `@Tags(['historical'])`); fog_tile_period_invariant (static-source-grep flip); fog_debug_spiral_continuity (Plan 03.1-14 ZERO wraps + retained historical sub-tests).
    - **2 TOUCHED tests** with magnitude assertion flipped to meter-space: fog_smooth_noise + fog_pan_translation (FOG-09).
    - **1 UPDATED test** fog_shader_uniforms_test.dart: slot-3/4 NAME source-grep flips to `uniform vec2 uWorldMetersOrigin`.

    **STRUCTURAL build-provenance gate (Task 1) PASSED:** `git rev-parse HEAD == gh run view <RUN_ID> --json headSha`. Walk #6's IPA is built from the EXACT commit on local HEAD; metersPerPixel JSONL signature (Plan 03.1-13 augmentation) + coordinateSpace JSONL signature (Plan 03.1-14 augmentation) are BOTH present in the IPA's logger emit body.

    Existing Walk #5 retentions all GREEN: FOG-04 structural, FOG-07 KEYSTONE, FOG-09 transform-equality, FOG-10 fog_transform_logger, FOG-12 canvas-frame-alignment, FOG-13 rect-viewport-coverage, PERF-08 bbox-quantisation, UX-01 sanity back button, UX-02 rotation-disabled.

    Pre-walk gates (Task 1): all 4 local + CI green + STRUCTURAL build-provenance + IPA download GREEN. The IPA at `.uat-tmp/mirk-poc-debug-unsigned.ipa` is ready for sideload.
  </what-built>
  <how-to-verify>
    1. Open SideStore on iPhone 17 Pro. Refresh certificate if needed. Install or refresh `.uat-tmp/mirk-poc-debug-unsigned.ipa`.
    2. Launch `mirk-poc-debug` on iPhone 17 Pro. Allow GPS via the rationale screen → "Allow While Using App".
    3. **Pre-walk sanity gate (production fog + UX-01 + DEBUG-01 walk-time validation + UX-02 rotation-disabled walk-time validation + Plan 03.1-14 cell-size visual confirmation):** tap the science (`Icons.science`) button in the AppBar to navigate to `/sanity`.
       - Confirm the production fog renders with circular reveal hole.
       - Confirm an `Icons.arrow_back` button appears in the leading slot (UX-01 retained).
       - Confirm a `Switch.adaptive` toggle for "Debug Spiral" appears in the AppBar actions (DEBUG-01).
       - Toggle the debug spiral ON. Confirm the digit-atlas spiral pattern renders. **Plan 03.1-14 visual continuity:** at the initial /sanity zoom (synthetic z=13 lat 48.5° → metersPerPixel ≈ 12.66), the cell size on screen is ≈ 200 / 12.66 ≈ 16 raw px (same as Walk #5 — Plan 03.1-14 doesn't change the cell-size formula, only the decomposition coordinate space).
       - Toggle OFF. Confirm production fog returns.
       - Tap the back button. Confirm it returns to `/map`.
       - Attempt a deliberate two-finger rotate gesture on `/map`. Confirm:
         - The map does NOT rotate.
         - The MapCompass widget stays at 0°.
    4. **Walk the route:** ≥ 5 minutes continuous through central Melun. During the walk:
       - Perform ≥ 10 deliberate combined pinch-zoom-and-pan gestures.
       - Perform ≥ 3 recenter FAB taps.
       - Perform AT LEAST ONE sustained ≥ 60-second one-direction pan (validates Q1 — PRIMARY Walk #6 closure-evidence-gate). **CRITICAL:** if you perceive ANY residual stepping during sustained pan (any "steppy moment"), TAP THE DEV-MARKER BUTTON. Walk #5 fired 2 such markers; Walk #6 expects ZERO. If markers fire, the architectural model is wrong (not 4-integer-cell shift) OR a NEW failure surfaces.
       - Perform AT LEAST ONE deliberate two-finger rotate-attempt gesture during normal walk (validates Q2).
       - **Q5 PRIMARY GATE retest: perform AT LEAST ONE complete zoom traversal at a fixed map position with both production fog AND debug-spiral toggled.**
         - **Sub-test 5a (production fog):** centre the camera on a recognisable feature (e.g., a road intersection or building corner). Tap recenter FAB if needed. Slowly zoom from min zoom (z=10 — country-level view) to max zoom (z=18 or higher — building-level). Watch the fog noise pattern AT THE FIXED FEATURE: it should appear stable across zoom transitions (no rapid sliding/scramble). Cells should visibly enlarge when zoomed in, shrink when zoomed out.
         - **Sub-test 5b (debug-spiral):** toggle debug-spiral ON via the `/map` AppBar global toggle. Note the cell number at the camera centre (e.g., "84"). Slowly zoom from min to max while keeping the camera centred on the same feature. Watch the cell number AT THE CAMERA CENTRE: it should stay the same across zoom levels (e.g., always "84" — the fragment under the blue dot always samples the same noise cell). Watch the cell SIZE on screen: it should visibly enlarge when zoomed in (cells "tied to ground").
         - **Sub-test 5c (debug-spiral pan during fixed zoom):** with debug-spiral ON at z=15, pan the camera ~500 m. Cells should scroll smoothly with NEW noise entering from the viewport edges (no fract-wrap; FOG-17 retained).
         - **Sub-test 5d (rapid-zoom comparison):** for direct comparison with Walk #5 (where Q5 failed), zoom in/out QUICKLY (rapid pinch). Pre-Plan-03.1-14 the noise pattern slid rapidly during zoom; post-Plan-03.1-14 it should appear stable.
       - Watch the FrameDeltaProbeOverlay (top:104, right:8) — note approximate `med` / `p95` / `max` values during combined gestures.
       - **NEW: Tap the dev-marker button** every time you perceive ANY residual symptom (steppy translation, fog mis-coverage, zoom-scramble residual, anything visually anomalous).
       - **The KEY observations** (Q1-Q5 sub-claims):
         - **Q1 (PRIMARY): ZERO `steppy_translation` markers during sustained ≥ 60-second pan?** (Walk #5 fired 2; Walk #6 expects 0).
         - **Q2: No rotation-correlated fog mis-coverage during rotate-attempt gestures?** (Walks #4-5 retained; re-confirmation).
         - **Q3: Reveal hole co-located with the blue dot during pan/zoom?** (Walks #1-5 retained; re-confirmation).
         - **Q4: Fog visually translates with the map during pan?** (Walks #1-5 retained; re-confirmation).
         - **Q5 (PRIMARY): Noise pattern is anchored to ground meters; cells visibly enlarge with zoom; at fixed map position the noise sample is zoom-invariant?** (Walk #5 reported failure; Plan 03.1-14 closes — PRIMARY Walk #6 closure-evidence-gate).
    5. **Post-walk: STRUCTURALLY-ENFORCED Mail-share.** Tap the share button in the AppBar → pick Mail → send the session log to your own email → save the attached `yyyymmdd_hhmmss_logs.txt` from your inbox to a known local path (e.g., `~/Downloads/walk6_session_log.txt` on macOS, or `C:\Users\oliver\Downloads\walk6_session_log.txt` on Windows). **The absolute path is REQUIRED in the resume signal.** The structural gate landed at Plan 03.1-13 and held at Walk #5 on first attempt; Walk #6 retains identically.
    6. **Capture the verdict:** describe in your own words whether each of the five Q1-Q5 sub-claims is now resolved. Single-word `approved [path]` is the gate for `CONFIRMED-AFTER-FIX`. Any residual failure mode should be described — that description feeds the diagnostic JSONL grep step.
    7. **Post-walk grep correlation** (executor runs after the verbal verdict + log path are captured):
       - `grep "infrastructure.mirk.frame_delta" <log-file> > .uat-tmp/walk6_frame_delta.jsonl`
       - `grep "infrastructure.mirk.sdf" <log-file> > .uat-tmp/walk6_sdf.jsonl`
       - `grep "infrastructure.mirk.fog_transform" <log-file> > .uat-tmp/walk6_fog_transform.jsonl`
       - `grep "dev_marker" <log-file> > .uat-tmp/walk6_dev_markers.jsonl` (if any markers fired)
       - For each line, parse the JSON, extract `epochSecond`, produce a per-second composite row.
       - **NEW for Walk #6: verify Plan 03.1-14 meter-space JSONL signatures:**
         - `coordinateSpace: 'meters'` field present in EVERY fog_transform rollup body (Plan 03.1-14 Task 2 augmentation).
         - `uOffsetXMin/Median/Max` and `uOffsetYMin/Median/Max` values bounded under `kPocFogIntegerWrapPeriodMeters + 1 = 4097` across ALL rollups (the FOG-19 invariant must hold in meter space; vs Walk #5's pixel-space bounded composite under 1537 raw px).
         - `metersPerPixelMin/Median/Max` field present (Plan 03.1-13 augmentation) AND consistent with synthetic camera state (e.g., at zoom 15 lat 48.5° expected ≈ 3.16). NOW empirical because build-provenance gate holds.
       - **Walk #6 closure-evidence verification — dev_marker correlation:**
         - If ZERO dev_markers fired, Q1 closes (the meter-space wrap is invisible).
         - If ANY dev_markers fired, the executor MUST run the Walk-#3b-template marker-correlation analysis: per-marker, find fog_transform rollup within ±1s window; check whether `appliedUOffsetX/Y` (now meter-space) crossed a 4096-m boundary near the marker time. If correlations exist → the architectural model is wrong (the wrap should have been invisible; it wasn't). If correlations DON'T exist → some other failure mode caused the markers (Plan 03.1-16+ territory).
       - Pipe extracts into `03.1-FALSIFICATION-6.md`'s Walk Evidence subsections.

    **Expected `CONFIRMED-AFTER-FIX` evidence pattern:**
    - Walk evidence: developer says `approved [path]` — ZERO `steppy_translation` markers during sustained pan; rotate gestures are no-ops; reveal hole co-located with blue dot; fog tracks camera; **Q5 zoom-scramble RESOLVED** — cells visibly enlarge with zoom; cells at fixed map position stay the same across zoom levels.
    - `frame_delta` rollup: PERF-07 thresholds satisfied with similar headroom to Walk #5.
    - `fog_transform` rollup: `pixelOriginX/Y` monotonic; `appliedUOffsetX/Y` bounded under 4097 m (meter-space FOG-19 invariant); **NEW: `coordinateSpace: 'meters'` field present** (Plan 03.1-14 signature); `metersPerPixel` field present, non-zero, evolving smoothly with camera zoom (Plan 03.1-13 + Plan 03.1-14 signatures EMPIRICALLY validated).
    - `dev_markers` JSONL: empty.

    **DENIED-FINAL or ITERATING patterns** are documented in `03.1-FALSIFICATION-6.md` §Criteria — pattern matching uses the diagnostic stream + (if markers fired) marker-correlation analysis to identify residual fix axes (or, in DENIED case, the architectural-counter-evidence finding pointing at Phase 3.2 alternative-renderer pivot).
  </how-to-verify>
  <resume-signal>
    Type ONE of (the absolute log path is REQUIRED — without it the executor refuses verdict authoring):

    - `approved [/full/path/to/walk6_session_log.txt] — Q1+Q5-closed-by-Plan-03.1-14 + Q2-Q3-Q4-retained` — all five Q1-Q5 sub-claims confirmed by developer. CONFIRMED-AFTER-FIX. Executor proceeds to Task 3 (Branch A) and flips FOG-04..07 + FOG-11 + FOG-15 + FOG-18 + FOG-19 + FOG-16 + FOG-17 + UX-02 + PERF-07 to Complete.

    - `denied [/full/path/to/walk6_session_log.txt]: <verbatim description of the residual failure mode>` — describe what went wrong. Executor opens the post-walk JSONL grep results from the log file, correlates against the failure description, and reports back: either (a) recommend a Plan 03.1-16+ follow-up fix attempt with a specific diagnostic-anchored hypothesis, or (b) recommend `DENIED-FINAL` if the diagnostic evidence shows the failure is structural at the renderer-architecture level (Phase 3.2 alternative-renderer pivot conversation: `mapsforge_flutter`, custom MVT-on-Canvas, `flutter_gpu`).

    - `iterating [/full/path/to/walk6_session_log.txt]: <verbatim description>` — same as `denied:` shape but the developer chooses to iterate per CONTEXT §Iteration policy. Executor opens a Plan 03.1-16 in a follow-up plan-phase invocation.

    **WITHOUT a log path:** the executor refuses verdict authoring and re-prompts the developer. The structural Mail-share gate is the load-bearing addition for the post-Walk-#3-Walk-#4 era; Walk #6 retains it.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Author the Walk #6 verdict — fill `03.1-FALSIFICATION-6.md` Walk Evidence + Verdict; update REQUIREMENTS.md / ROADMAP.md / STATE.md</name>
  <files>.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-6.md, .planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-6.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
    **PRE-CONDITION CHECK:** Verify the developer's resume signal includes a log path parameter. If not, REFUSE to proceed and re-prompt. Read the log file at the provided path; confirm it contains the expected JSONL streams (`infrastructure.mirk.frame_delta`, `infrastructure.mirk.sdf`, `infrastructure.mirk.fog_transform`, optionally `dev_marker`). If the log file is missing or malformed, REFUSE and re-prompt.

    **NEW for Walk #6 — verify Plan 03.1-14 JSONL signatures present:**
    - `grep '"coordinateSpace":"meters"' <log_path>` MUST return at least one match (the Plan 03.1-14 Task 2 augmentation MUST be in the Walk #6 IPA per the build-provenance gate that landed at Task 1).
    - `grep '"metersPerPixelMin"' <log_path>` MUST return at least one match (the Plan 03.1-13 augmentation, NOW empirical because build-provenance gate holds).

    If either is missing, the executor MUST flag a build-provenance regression (the IPA does not contain the post-Plan-03.1-14 logger augmentation) and refuse to proceed. The developer must trigger a fresh CI run + re-walk.

    Branch on the developer's verdict from Task 2:

    **Branch A — `approved [log_path]` → CONFIRMED-AFTER-FIX:**

    1. Run post-walk grep correlation against the log file at `log_path`:
       ```
       grep "infrastructure.mirk.frame_delta" <log_path> > .uat-tmp/walk6_frame_delta.jsonl
       grep "infrastructure.mirk.sdf" <log_path> > .uat-tmp/walk6_sdf.jsonl
       grep "infrastructure.mirk.fog_transform" <log_path> > .uat-tmp/walk6_fog_transform.jsonl
       grep "dev_marker" <log_path> > .uat-tmp/walk6_dev_markers.jsonl
       ```
       For each, parse JSON, extract `epochSecond`, produce per-second composite row.

    2. Fill `03.1-FALSIFICATION-6.md` Walk Evidence subsections with the post-walk grep correlation. **Plan 03.1-14 verifications:**
       - **fog_transform `coordinateSpace: 'meters'` field present in EVERY rollup body.** Direct visible signature of Plan 03.1-14 Task 2 augmentation.
       - **fog_transform `appliedUOffsetX/Y` bounded under 4097 m across ALL rollups.** Direct visible signature of Plan 03.1-14 meter-space FOG-19 invariant. Compare against Walk #5's pixel-space bounded composite envelope `[789.6, 1535.8]` — Walk #6 envelope should be in `[0, 4097]` meter-space.
       - **fog_transform `metersPerPixel` field present + consistent with camera state.** Plan 03.1-13 augmentation, NOW empirical. Each rollup line has a `metersPerPixel` (or `metersPerPixelMin/Median/Max`) field. Values are non-zero and evolve smoothly with camera zoom.
       - **dev_marker JSONL: ZERO `steppy_translation` markers during the walk** (or, if any fired, marker-correlation analysis shows NO correlation with `appliedUOffset` 4096-m-boundary crossings).

    3. Fill `03.1-FALSIFICATION-6.md` Verdict section:
       ```markdown
       ## Verdict

       - [x] Criterion A passed? — **YES.** Frame-delta probe rolling rollup over ≥ 10 combined gestures: median = {N} ms (≤ 16 ✓), p95 = {N} ms (≤ 32 ✓), max = {N} ms (≤ 48 ✓). PERF-07 re-validated post-Plan-03.1-14.
       - [x] Criterion B passed? — **YES.** Developer's verbatim verdict: *"<verbatim>"*. All five Q1-Q5 sub-claims confirmed:
         - **Q1 ✓ (Walk #6 PRIMARY closure-evidence-gate): ZERO `steppy_translation` markers during sustained ≥ 60-second pan.** Plan 03.1-14 Fix B′ closes the period-commensurability gap that Walk #5 surfaced (4096 m wrap = 4 integer cells in noiseUv = invisible).
         - Q2 ✓ (Walks #4-5 retained): rotate-attempt gestures are no-ops;
         - Q3 ✓ (Walks #1-5 retained): reveal hole co-located with blue dot;
         - Q4 ✓ (Walks #1-5 retained): fog translates with camera;
         - **Q5 ✓ (Walk #6 PRIMARY closure-evidence-gate retest): noise pattern anchored to ground meters; cells visibly enlarge with zoom; at fixed map position the noise sample is zoom-invariant.** Plan 03.1-14 Fix B′ closes Q5 because bounded_meters is geographic, not pixel.
       - [x] Criterion D passed? — **YES.** Diagnostic stream confirms the layered fix at every level. **`coordinateSpace: 'meters'` field present in EVERY fog_transform rollup body** (Plan 03.1-14 Task 2 augmentation — direct visible signature). **`appliedUOffsetX/Y` values bounded under 4097 m across ALL rollups** (Plan 03.1-14 meter-space FOG-19 invariant — direct visible signature). **`metersPerPixel` field present, non-zero, evolving smoothly with camera zoom** (Plan 03.1-13 augmentation, NOW empirical — build-provenance gate held). ZERO dev_markers fired during the walk (or markers fired only at non-correlated cadence). STRUCTURAL build-provenance gate held at Task 1: `git rev-parse HEAD == gh run view <RUN_ID> --json headSha`.

       **Outcome:** **CONFIRMED-AFTER-FIX.**

       The same-Canvas fog hypothesis is **reinstated** with the layered Plan 03.1-02 + 03.1-04 + 03.1-05 + 03.1-07 + 03.1-08 + 03.1-10 + 03.1-12 + 03.1-14 fix bundle. Phase 3's `DENIED 2026-05-01` verdict and Phase 3.1 Walks #1-5's `ITERATING-WITH-*` verdicts are converted to `CONFIRMED-AFTER-FIX` after Walk #6.

       **MirkFall migration recommendation:** **PORT BACK** with the full layered fix bundle (10 production-impacting changes — see Plan 03.1-13 Task 3 Branch A list, augmented with):
       - **Plan 03.1-14 (FOG-19 — Fix B′):** `_FogPainter.paint()` AND `_DebugSpiralMapPainter.paint()` AND `/sanity` `_DebugSpiralPainter` ALL compute `worldMeters = pixelOrigin × metersPerPixel` and decompose in METER space. Forward `boundedMeters` (NOT `boundedPixels`) to the shader via the renamed slot 3..4 uniform `uWorldMetersOrigin`. Period-commensurability invariant `kPocFogIntegerWrapPeriodMeters % kPocFogNoiseTilePxMeters == 0` MUST hold (4096 m = 4 × 1024 m). Shader source's `worldMeters` formula reorders to `(fragUv * uResolution) * uMetersPerPixel + uWorldMetersOrigin`. FogTransformLogger JSONL emit body adds `coordinateSpace: 'meters'` field for grep-correlation continuity (post-port-back forensics).

       The MirkFall donor `_FogPainter.paint()` call site MUST be reviewed against ALL ten production-impacting changes at port-back time.

       **POC scope reduction note (UX-02 + FOG-16):** The POC ships with rotation disabled. If MirkFall after port-back wants to re-enable rotation, the canvas-rotation-aware compensation (FOG-16 path (b) — read full canvas matrix; apply inverse via `canvas.transform(matrix.inverted())`) must be implemented before re-enabling.
       ```

    4. Update `.planning/REQUIREMENTS.md`:
       - FOG-04, FOG-05, FOG-06, FOG-07: change `[ ]` → `[x]`. Update inline status from `Falsified-in-production` to `Complete — Verified-by-walk (P03.1-15 CONFIRMED-AFTER-FIX 2026-05-XX after layered Plan 03.1-02+04+05+07+08+10+12+14 fix bundle)`.
       - FOG-11: `[ ]` → `[x]`; status `Complete — Verified-by-walk (P03.1-15 — Plan 03.1-14 meter-space FOG-17a decomposition closes the architectural noise-anchor axis FOG-11 CI test was inadequate proxy for; the user-observable noise-pattern stability across pan + zoom is now delivered by FOG-18 + FOG-19)`.
       - FOG-15: status augmented `Complete — Verified-by-test + walk (P03.1-07 Task 3 unit + P03.1-15 walk-time confirmation post-Plan-03.1-14 meter-space rewrite)`.
       - FOG-16 + FOG-17: status augmented `Complete — Verified-by-test + walk-4 + walk-5 + walk-6 (P03.1-10 unit + P03.1-11 + P03.1-13 + P03.1-15 walk-time confirmation)`.
       - FOG-18: change `[ ]` → `[x]`; status `Complete — Verified-by-walk (P03.1-12 unit + P03.1-15 walk-time confirmation post-Plan-03.1-14 retargeting — Q5 zoom-scramble resolved)`.
       - FOG-19: change `[ ]` → `[x]`; status `Complete — Verified-by-walk (P03.1-14 unit + P03.1-15 walk-time confirmation — meter-space FOG-17a decomposition closes period-commensurability gap; ZERO steppy_translation markers during sustained pan; coordinateSpace='meters' JSONL signature present)`.
       - UX-02: status augmented `Complete — Verified-by-test + walk-4 + walk-5 + walk-6 (P03.1-10 widget test + P03.1-11 + P03.1-13 + P03.1-15 walk-time validation)`.
       - PERF-07: `Complete — Re-measured (P03.1-15)`. Include actual Walk #6 numbers.
       - PERF-08: stays falsified-by-walk-2 (no Plan 03.1-14 work targeted it; deferred to post-POC if it becomes a higher-tier concern).
       - PERF-03 / PERF-04 / PERF-05: update statuses to reflect re-measurement at Walk #6.
       - Append a new entry to `## Revisions`:
         ```
         - **2026-05-XX (Phase 3.1 closure, Plan 03.1-15 SUMMARY — HYPOTHESIS CONFIRMED-AFTER-FIX):** Plan 03.1-15 sideload UAT Walk #6 on iPhone 17 Pro against CI run `<RUN_ID>` (SHA `<SHA>`) over central Melun delivered a **CONFIRMED-AFTER-FIX** verdict after the layered Plan 03.1-02+04+05+07+08+10+12+14 fix bundle. Developer's verbatim words: *"<verbatim>"*. All five Q1-Q5 sub-claims confirmed (Q1 PRIMARY closure-evidence: ZERO steppy_translation markers during sustained pan vs Walk #5's 2; Q5 PRIMARY closure-evidence retest: cells enlarge with zoom + cell number stable across zoom). FOG-04..07 + FOG-11 + FOG-15 flip from Falsified-in-production back to Complete — Verified-by-walk. FOG-16 + FOG-17 + FOG-18 + FOG-19 + UX-02 verified-by-walk in addition to verified-by-test. PERF-07 re-measured. **STRUCTURAL build-provenance discipline succeeded** — Walk #6 Task 1 explicit gate `git rev-parse HEAD == gh run view <RUN_ID> --json headSha`; eliminates the build-provenance gap class of failure that Walk #5 surfaced. **STRUCTURALLY-ENFORCED Mail-share discipline RETAINED** — gate held second time (Walk #5 first time after Walks #3-4 broke twice). Pattern established: structural gates at the resume-signal contract are the closure mechanism for recurring discipline-break patterns. MirkFall migration recommendation: **PORT BACK** with the full 10-architectural-change layered fix bundle. Phase 4 (wisp particles) and Phase 5 (decision gate) **UNBLOCKED**. Evidence: `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-6.md` + `03.1-UAT-6.md`. Phase 3.1 closes with all requirement IDs Complete (FOG-09..19, PERF-07, PERF-08-deferred-post-POC, UX-01, UX-02, DEBUG-01) plus the 4 retroactively-Complete (FOG-04..07).
         ```

    5. Update `.planning/ROADMAP.md`:
       - In `## Progress` table: Phase 03.1 row → `Complete (HYPOTHESIS CONFIRMED-AFTER-FIX)`, plans `15/15`, completed `2026-05-XX`.
       - In Phase 4 + Phase 5 rows: change status from `Blocked on Phase 3.1` to `Pending`.
       - In Phase 3 overview line: append `Phase 3.1 reversed the verdict to CONFIRMED-AFTER-FIX 2026-05-XX after the layered Plan 03.1-02+04+05+07+08+10+12+14 fix bundle (uniform rename + per-fragment modulo + clip-path shift compensation + B-3 noise-tile-period intermediate fix + canvas.translate symmetric compensation + world-coordinate noise sampling + CPU-side integer/fractional decomposition + rotation disable + WORLD-METER ANCHOR + METER-SPACE DECOMPOSITION).`
       - In Phase 4 + Phase 5 overview lines: remove the `**BLOCKED on Phase 3.1**` clause.
       - Mark `03.1-15-PLAN.md` row in Phase 03.1 plans list as `[x]` landed.

    6. Update `.planning/STATE.md`:
       - Frontmatter: `stopped_at: Phase 3.1 CLOSED with HYPOTHESIS CONFIRMED-AFTER-FIX (Walk #6 2026-05-XX); Phase 4 + 5 UNBLOCKED.`; `last_activity` accordingly.
       - Bump `progress.completed_phases` from 3 to 4.
       - `## Current Position`: update to reflect Phase 3.1 closure + Phase 4 unblock.
       - `### Decisions`: append a Phase 3.1 closure entry capturing the Plan 03.1-14 + Walk #6 lineage.
       - `### Blockers/Concerns`: change all Phase-3.1-related blockers to `(RESOLVED 2026-05-XX by Plan 03.1-15 Walk #6 CONFIRMED-AFTER-FIX after layered Plan 03.1-14 software fix bundle)`. Resolve the build-provenance discipline blocker (Walk #6's structural-enforcement gate succeeded) AND the WALK-MAIL-SHARE-DISCIPLINE blocker fully resolves (gate held second time after Walk #5 first time).

    7. **DO NOT TOUCH** Phase 3 + Phase 3.1 Walks #1 + #2 + #3 + #3b + #4 + #5 historical records. Verify via `git status` that all are unchanged.

    **Branch B — `denied [log_path]:` → DENIED-FINAL:**

    Mirror Plan 03.1-13 Task 3 Branch B treatment with Walk #6 specifics. Walk Evidence filled with failure-mode evidence + diagnostic-stream pattern matching (including coordinateSpace + metersPerPixel + meter-space appliedUOffset signature analysis) + (if markers fired) marker-correlation analysis. Verdict captures DENIED-FINAL outcome with MirkFall recommendation `DO NOT PORT BACK` and Phase 3.2 conversation hand-off. REQUIREMENTS.md FOG-04..07 + FOG-11 + FOG-15 stay `Falsified-in-production` with updated revision-clause appending Walk #6 evidence. ROADMAP.md Phase 03.1 row → `Complete (HYPOTHESIS DENIED-FINAL)`. Phases 4 + 5 stay `Blocked` (or transition to `Cancelled` per developer choice). FOG-16 + FOG-17 + FOG-18 + FOG-19 + UX-02 still flip to `Complete — Verified-by-test + walk-time validated structurally` (the structural fixes ARE in the codebase and walk-time validated structurally — UX-02 disabled rotation is observable; FOG-18 + FOG-19 cell-size enlargement-with-zoom + integer-cell-shift-at-wrap are observable; the open question is whether they're SUFFICIENT for fog-camera-tracking parity at the deepest coordinate-system axis). The Phase 3.2 alternative-renderer pivot conversation opens.

    **Branch C — `iterating [log_path]:` → defer Plan 03.1-16 to follow-up:**

    Mirror Plan 03.1-13 Task 3 Branch C treatment with Walk #6 specifics. Walk Evidence filled with post-walk grep correlation (including coordinateSpace + metersPerPixel + meter-space appliedUOffset signature analysis) + (if markers fired) marker-correlation analysis. Verdict: `**Outcome:** ITERATING — see Plan 03.1-16+ for the next fix attempt.`. Do NOT update REQUIREMENTS.md / ROADMAP.md / STATE.md to closure state. Mark Phase 3.1 as `In Progress (8th iteration, walk #7 pending)` in Progress table. UX-02 + FOG-16 + FOG-17 + FOG-18 + FOG-19 still flip to `Complete — Verified-by-test + walk-time validated structurally` (their CI gates ARE GREEN; structural prevention is observable). FOG-04..07 + FOG-11 + FOG-15 stay `Falsified-in-production`. PERF-07 updated with Walk #6 numbers regardless. Plan 03.1-16 is created via a follow-up `/gsd:plan-phase 3.1 --gaps` invocation. Populate `03.1-UAT-6.md` frontmatter `gaps:` YAML with any opened-by-Walk-#6 gap entries.

    For all three branches: commit all updates via the standard plan-execution commit pattern. Cross-reference Plan 03.1-15 in commit messages. Do NOT commit `.uat-tmp/` (gitignored) or post-walk `.jsonl` extracts (transient walk artefacts).
  </action>
  <verify>
    <automated>flutter test 2>&amp;1 | tail -3 &amp;&amp; flutter analyze --fatal-infos 2>&amp;1 | tail -3 &amp;&amp; node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" frontmatter validate .planning/phases/03.1-fix-fog-pan-translation/03.1-15-PLAN.md --schema plan</automated>
    <file_must_exist>.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-6.md</file_must_exist>
    <file_must_exist>.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-6.md</file_must_exist>
  </verify>
  <done>
    - Resume-signal log path parameter verified — log file readable and contains expected JSONL streams.
    - **Plan 03.1-14 JSONL signatures verified:** `coordinateSpace: 'meters'` field present in fog_transform rollups; `metersPerPixel` field present (Plan 03.1-13 augmentation NOW empirical because build-provenance gate held).
    - `03.1-FALSIFICATION-6.md` Walk Evidence + Verdict sections are filled (no `TBD` strings remaining for the chosen branch — Branches A or B; Branch C explicitly leaves them flagged for Plan 03.1-16).
    - `03.1-UAT-6.md` carries the Walk #6 transcript.
    - REQUIREMENTS.md statuses reflect the verdict outcome.
    - ROADMAP.md Progress table Phase 03.1 row reflects the verdict; Phase 4 + Phase 5 statuses updated per branch.
    - STATE.md Current Position + Decisions + Blockers/Concerns updated; build-provenance discipline blocker resolved (Branch A) or carried forward (Branch C); WALK-MAIL-SHARE-DISCIPLINE fully resolved (Branch A) or partially resolved (Branch C).
    - Walks #1 + #2 + #3 + #3b + #4 + #5 + Phase 3 historical records UNCHANGED (verified via `git status`).
    - `flutter test` (full suite) GREEN — closure docs do not touch any production code.
    - Plan frontmatter validation: `{ valid: true }`.
  </done>
</task>

</tasks>

<verification>
After all three tasks complete:
- `03.1-FALSIFICATION-6.md` is filled top-to-bottom with the Walk #6 verdict and walk evidence (or marked `iterating` per Branch C).
- The Phase 3 historical record (`03-FALSIFICATION.md`) AND Walks #1 + #2 + #3 + #3b + #4 + #5 historical records are ALL byte-identical to their pre-Plan-03.1-15 states.
- REQUIREMENTS.md, ROADMAP.md, STATE.md all reflect the Walk #6 verdict outcome consistently.
- **STRUCTURAL build-provenance gate succeeded** — Task 1's `git rev-parse HEAD == gh run view <RUN_ID> --json headSha` check held; Walk #6's IPA contains the post-Plan-03.1-14 logger augmentations empirically.
- **STRUCTURALLY-ENFORCED Mail-share discipline RETAINED** — Walk #6 resume-signal log path parameter was provided by developer; quantitative JSONL grep correlation available.
- `flutter test` (full suite) is GREEN — Plan 03.1-15 closure docs do not touch any production code.
- Plan frontmatter validation returns `{ valid: true }`.
</verification>

<success_criteria>
- The iPhone 17 Pro Walk #6 produces a verdict (verbal `approved [log_path]` / `denied [log_path]:` / `iterating [log_path]:`) — the empirical answer to the Plan 03.1-14 software fix bundle.
- `03.1-FALSIFICATION-6.md` is the load-bearing artefact capturing the Walk #6 verdict + diagnostic-JSONL evidence stream (including coordinateSpace + metersPerPixel + meter-space appliedUOffset signatures) + (if markers fired) marker-correlation analysis.
- On `CONFIRMED-AFTER-FIX`: Phase 4 + Phase 5 unblock; MirkFall migration recommendation flips to PORT BACK with the full 10-change layered fix bundle (rotation-disabled UX + world-meter anchor + meter-space decomposition); Phase 5 closure conversation can proceed; FOG-04..07 + FOG-11 + FOG-15 + FOG-16 + FOG-17 + FOG-18 + FOG-19 + UX-02 + PERF-07 all flip to Complete.
- On `DENIED-FINAL`: the formal POC architectural counter-evidence is complete at the deepest coordinate-system axis (meter-space decomposition + meter-space anchor doesn't deliver fog-camera-tracking parity); Phase 3.2 alternative-renderer conversation is the only remaining option. UX-02 + FOG-16 + FOG-17 + FOG-18 + FOG-19 + Plan 03.1-10 + Plan 03.1-12 + Plan 03.1-14 tests stay Complete (the fix DID land in software); the failure is downstream of the meter-space pipeline.
- On `ITERATING`: Plan 03.1-16 is opened in a follow-up invocation with Walk #6's diagnostic stream as input.
- The Phase 3 + Phase 3.1 Walks #1-5 historical records stay untouched — historical integrity preserved across iteration cycles.
- **STRUCTURAL build-provenance discipline pattern established** — Walk #6's `git rev-parse HEAD == gh run view <RUN_ID> --json headSha` contract eliminates the build-provenance gap class of failure that Walk #5 surfaced; pattern carries forward to any future POC walks.
- **STRUCTURALLY-ENFORCED Mail-share discipline pattern fully ratified** — the gate landed at Walk #5 (first time) and held at Walk #6 (second time); the closure-mechanism for recurring discipline-break patterns is now established.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-fix-fog-pan-translation/03.1-15-SUMMARY.md` capturing:
- Pre-walk gate sequence outcome (4 local + CI + STRUCTURAL build-provenance + IPA download)
- Walk #6 metadata (CI run ID, SHA, walk theatre, walk duration, gesture count, ≥ 60-sec one-direction pan duration, two-finger rotate-attempt observation, complete-zoom-traversal observation at fixed map position)
- Developer's verbatim verdict (with log_path parameter)
- Post-walk JSONL grep correlation (per-second composite extract for the ≥ 10-combined-gesture window)
- (If markers fired) marker-correlation analysis mirroring the Walk #3b template — for Walk #6 closure, ZERO markers fired is the expected pattern; if markers fired, the analysis MUST show NO correlation with `appliedUOffset` 4096-m-boundary crossings (else the architectural model is wrong)
- Walk #6 Verdict outcome (`CONFIRMED-AFTER-FIX` | `DENIED-FINAL` | `ITERATING`)
- Files modified across all three tasks (5 planning docs)
- Any deviations from the plan with Rule classification
- Cross-reference to Phase 4 (the unblocked-or-still-blocked downstream phase) + the migration recommendation hand-off to Phase 5
- Comparison vs Walks #1-5 numbers — frame_delta headroom preservation, SDF rebuild rate change (PERF-08 status), failure-mode resolution status per Q1-Q5 sub-claim, marker firing count delta vs Walk #5 (Walk #5 fired 2 steppy_translation markers; Walk #6 expected 0)
- **Plan 03.1-14 signature confirmation: `coordinateSpace: 'meters'` JSONL field present** + **`appliedUOffsetX/Y` bounded under 4097 m across all rollups** (the meter-space FOG-19 invariant — direct visible signature post-Plan-03.1-14)
- **metersPerPixel signature NOW EMPIRICAL** — Plan 03.1-13 augmentation captured at Walk #6 because build-provenance gate held (vs Walk #5 where commit `2b087a3` post-dated CI run)
- **STRUCTURAL build-provenance discipline outcome:** Walk #6 Task 1 explicit gate `git rev-parse HEAD == gh run view <RUN_ID> --json headSha`; pattern established for any future POC walks (recommendation in Phase 5 closure)
- **STRUCTURALLY-ENFORCED Mail-share discipline outcome:** Walk #6 retains Plan 03.1-13's gate; held second time after Walk #5 first time (third + fourth iteration patterns broken twice). Pattern fully ratified.
</output>
