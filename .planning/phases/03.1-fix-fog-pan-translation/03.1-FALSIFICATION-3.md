# Phase 3.1 Falsification — Walk #3 (Post-Plan-03.1-07 + Plan-03.1-08 Layered Fix)

**Created:** 2026-05-03
**Phase:** 03.1-fix-fog-pan-translation
**Walked on:** TBD  (filled post-walk: iPhone 17 Pro, central Melun, CI run `25275979318`, SHA `0625ba5`)
**Verdict:** TBD  (filled post-walk: `CONFIRMED-AFTER-FIX` | `DENIED-FINAL` | `ITERATING — see 03.1-10+`)

## Relationship to Walk #2 Verdict

Walk #2 (Plan 03.1-06, 2026-05-02) closed `ITERATING-WITH-PARTIAL-PROGRESS`. Three of four targeted fix-axes confirmed (PERF-07 re-validated, FOG-12 reveal-hole alignment confirmed, UX-01 back button confirmed); one fix-axis (FOG-11 SHADER-MODULO-WRAP) landed in CI but did NOT close the user-observable shimmer; one fix introduced a NEW regression (Plan 03.1-05's clip-path-only compensation left the fog-rect viewport-coverage gap). Three new requirements opened: FOG-13 (fog-rect symmetric compensation), FOG-14 (higher-fidelity noise-pattern stability test), FOG-15 (mechanism-confirmed shader-coordinate-system fix); plus DEBUG-01 (debug-spiral shader infrastructure) added by Plan 03.1-07 as the cleavage tool for Walk #2's UNKNOWN-mechanism shimmer (developer's "rotating 90° / translated fast repetitively" was speculation about an unresolvable visual phenomenon).

Plan 03.1-07 closed FOG-14 + FOG-15 + DEBUG-01 via the debug-spiral observation step (Task 2 verbal observation: Branch B-3 selected) + the mechanism-specific fix (Task 3 Branch B-3 — noise-tile-period mismatch: `fract(uPixelOrigin / uResolution)` → `fract(uPixelOrigin / tilePeriodPixels)` where `tilePeriodPixels = uResolution / max(uScaleFar, uScaleMid, uScaleNear)`).
Plan 03.1-08 closed FOG-13 via the `canvas.translate(-canvasOffset)` symmetric compensation in `_FogPainter.paint()`.

Walk #1 + Walk #2 records (`03.1-FALSIFICATION.md`, `03.1-FALSIFICATION-2.md`, `03.1-UAT.md`, `03.1-UAT-2.md`) AND Phase 3 record (`03-FALSIFICATION.md`) ALL stand as historical record — UNTOUCHED. Walk #3's verdict is a SEPARATE artefact in this file.

## Hypothesis

After the Plan 03.1-07 mechanism-confirmed fix (Branch B-3: noise-tile-period mismatch — `fract(uPixelOrigin / tilePeriodPixels)` where `tilePeriodPixels = uResolution / max(uScaleFar, uScaleMid, uScaleNear)`, applied symmetrically in production `atmospheric_fog.frag` and debug-spiral `atmospheric_fog_debug_spiral.frag`) AND the Plan 03.1-08 FOG-13 fog-rect viewport-coverage symmetric compensation (`canvas.translate(-canvasOffset)` at top of `_FogPainter.paint()`), the same-Canvas fog now: (a) translates with the camera during pan; (b) maintains a stable noise pattern across consecutive paints (FOG-14 CI gate validates wrap-period-aligned with maxScale = 10.5×); (c) keeps the reveal hole co-located with the blue dot during pan/zoom (FOG-12 retained); (d) covers the entire viewport at any canvas-translation magnitude (FOG-13 CI gate validates).

Re-stated against the original POC core value: the fog-of-war stays perfectly locked to the map during pan, zoom, and combined gestures on a sideloaded iOS build. **CONFIRMED-AFTER-FIX** means the Phase 3 hypothesis is reinstated and the POC ports back to MirkFall with all of Plan 03.1-02 + 03.1-04 + 03.1-05 + 03.1-07 + 03.1-08 fixes included; **DENIED-FINAL** means even the layered fix bundle doesn't deliver fog-camera-tracking parity; **ITERATING** means a residual failure mode surfaced that has a clear next-iteration fix axis (Plan 03.1-10+; the documented iteration path per Plan 03.1-07-SUMMARY is the world-coordinate-noise rewrite).

## Falsification Criteria (written BEFORE the walk)

### Criterion A — Frame-delta probe thresholds (PERF-07 re-validation, quantitative)

Same thresholds as Walk #1 + #2: `medianMs ≤ 16`, `p95Ms ≤ 32`, `maxMs ≤ 48` across ≥ 10 deliberate combined gestures. Walk #1: 4-7×; Walk #2: 3.9-5.4×; Walk #3 should preserve similar headroom (Plan 03.1-08's `canvas.translate` adds ~1 cycle per paint — negligible against the budget; Plan 03.1-07's tile-period-aware divisor adds 1 `max()` + 1 `vec2 / float` divide per fragment — also negligible at POC scale).

### Criterion B — Subjective visual lock (PERF-05 carry-over, re-tested)

Developer's verbal verdict at end of walk:
- **Q1 — No 'seed changing' noise-seam shimmer / stepped translation during gesture** (Walk #2 fail — Plan 03.1-07 Branch B-3 targets this; expected: stepping degraded to sub-perceptible at moderate zoom; possibly residual at extreme zoom from B-1 fp32 precision per Plan 07's "partial fix" framing)
- **Q2 — Fog covers the entire viewport at any zoom — no top-right / bottom-right un-fogged strips** (Walk #2 fail — Plan 03.1-08 FOG-13 targets this)
- **Q3 — No reveal-hole offset from blue dot during gesture** (Walk #2 confirmed — Plan 03.1-05 retained; re-confirmation under Plan 03.1-08's `canvas.translate` interaction)
- **Q4 — Fog visually translates with the camera during pan** (Walk #2 confirmed — Plans 03.1-02 + 03.1-04 retained; re-confirmation)
- No white-ellipse during fast pinch-zoom (already passed Walks #1 + #2)
- No inversion at any zoom level (already passed Walks #1 + #2)

### Criterion C — DROPPED

Stays dropped per Phase 3 + roadmap lock.

### Criterion D — Diagnostic stream confirms the layered fix

Per the `Logger('infrastructure.mirk.fog_transform')` 1-Hz JSONL rollups:

- **CONFIRMED-AFTER-FIX:** `pixelOriginX/Y` track gesture monotonically. Branch B-3 keeps the JSONL field name `appliedUOffset` for back-compat (the on-disk uniform value at the shader call site is still raw pixelOrigin per Plan 03.1-04's slot-3..4 rename — B-3's tile-period divisor lives inside the GLSL fragment, not at the Dart boundary). `canvasTx/Ty` jumps still occur — Plan 03.1-08's `canvas.translate` compensates them mechanically (FOG-13 CI gate validates the compensation; the walk's role is corroborating that no viewport-edge un-fogged strips appear at any canvas-translation magnitude).
- **DENIED-FINAL pattern:** the diagnostic stream shows the layered fix landed mechanically AT EVERY level (pixelOrigin tracks, canvas.translate compensates rect-cover, B-3 wrap-period-aligned per FOG-14a CI gate) AND the user-observable shimmer / stepped-translation / coverage-gap STILL persists — root cause is even deeper than the coordinate-system + rect-compensation; architecture is unfixable in `flutter_map 7.0.2` with `fract`-based noise sampling.
- **ITERATING pattern:** subjective verdict surfaces a residual failure mode (e.g., stepping reduced but not eliminated at extreme zoom — the documented Plan 03.1-07 partial-fix iteration boundary; new failure mode surfaces). The diagnostic stream points at the next fix axis; Plan 03.1-10 (world-coordinate-noise rewrite) is opened.

## Walk Plan

Identical to Plan 03.1-06 (Walk #2) — same theatre, duration, gestures, HUD, post-walk Mail-share. Only the post-fix SHA + CI run number differ. Pre-walk shader-sanity gate now ALSO includes a debug-spiral toggle live-test on the sideloaded build (DEBUG-01 walk-time validation — toggle works on both `/sanity` route AND `/map` AppBar global toggle from the Plan 03.1-08 mid-checkpoint corrective bundle).

- **Where:** Central Melun (lat 48.5397, lon 2.6553 area).
- **Duration:** ≥ 5 minutes continuous walk with the IPA running and the device awake.
- **Gestures:** ≥ 10 deliberate combined pinch-zoom-and-pan gestures, ≥ 3 recenter FAB taps.
- **Pre-walk gate (unit tests):** `flutter test` GREEN (158 + 1 SKIPPED at SHA 0625ba5 — verified 2026-05-03); `flutter analyze --fatal-infos` 0 warnings; `dart format --line-length 160 --set-exit-if-changed .` clean; `dart test tool/test/` GREEN.
- **Pre-walk gate (CI):** `gh run list --branch main --limit 5` shows `success` for SHA `0625ba5` (run 25275979318); `gh run view 25275979318` confirms `Lint / License / Headers / Deps`, `Build Android APK (debug)`, `Build iOS (no-codesign, sideloadable)` all `success`; IPA downloaded to `.uat-tmp/mirk-poc-debug-unsigned.ipa` (~11.6 MB, present 2026-05-03).
- **Pre-walk gate (sideload):**
  1. Open `/sanity` route — confirm production fog renders with circular reveal hole (debug-spiral toggle OFF default).
  2. Toggle the debug-spiral switch ON (either on `/sanity` AppBar OR on `/map` AppBar — global toggle landed in Plan 03.1-08 mid-checkpoint corrective bundle commit `f707226`) — confirm the digit-atlas spiral pattern renders with visible numbered cells (DEBUG-01 walk-time validation).
  3. Toggle OFF — confirm production fog returns.
  4. Confirm the `Icons.arrow_back` AppBar leading button still works on `/sanity` (UX-01 retained).
  5. Confirm zero shader-compile exceptions in the FileLogger output.
- **In-app HUD during walk:** FpsCounterOverlay (top:8, right:8) + MapCompass (top:56, right:8) + FrameDeltaProbeOverlay (top:104, right:8).
- **Post-walk:** share the session log file via Mail (LOG-04 round-trip); paste the relevant frame-delta + SDF + fog-transform JSONL lines into the Walk Evidence section below; write the subjective verdict (Criterion B) by hand; verbal `approved` is the gate (or `denied:` / `iterating:` for failure / next-iteration paths).

## Walk Evidence (filled AFTER the walk by Plan 03.1-09 Task 2)

### Pre-walk shader-sanity gate (`/sanity` route + UX-01 back-button validation + DEBUG-01 toggle live-test)
_TBD — filled post-walk._

### Probe rollup (frame-delta JSONL — `Logger('infrastructure.mirk.frame_delta')`)
_TBD — filled post-walk. Expected: median ≤ 16 ms, p95 ≤ 32 ms, max ≤ 48 ms across ≥ 10 combined gestures (PERF-07 re-validation; Walk #1 4-7×; Walk #2 3.9-5.4×; Walk #3 should preserve similar headroom)._

### SDF rebuild rollup (`Logger('infrastructure.mirk.sdf')` — PERF-08 status)
_TBD — filled post-walk. PERF-08 was falsified-by-walk-2 (rebuild rate 1-121/sec median 68/sec); Walk #3 re-measures whether the rate changed (no Plan 03.1-07/08 work targets PERF-08 — likely unchanged; the question is whether PERF-08 stays falsified or whether the layered fix incidentally shifts the gesture pattern's hash trajectory)._

### Fog-transform rollup (`Logger('infrastructure.mirk.fog_transform')`)
_TBD — filled post-walk. Expected: `pixelOriginX/Y` monotonic; `appliedUOffset` keys stay in raw-pixel back-compat range (Branch B-3's tile-period divisor lives in-shader, not at the Dart boundary); `canvasTx/Ty` may still jump non-zero but the FOG-13 + FOG-12 CI gates validate the painter's compensation._

### FPS observations (PERF-03 — coarse FpsCounterOverlay reading)
_TBD — filled post-walk._

### Subjective verdict (Criterion B)
_TBD — filled post-walk. Developer's verbatim words + interpretation paragraph mapping each Criterion B sub-claim Q1-Q4 to PASS/FAIL._

## Verdict

- [ ] Criterion A passed? — TBD post-walk.
- [ ] Criterion B passed? — TBD post-walk.
- [ ] Criterion D passed? — TBD post-walk.

**Outcome:** TBD.

**MirkFall migration recommendation:** TBD.
