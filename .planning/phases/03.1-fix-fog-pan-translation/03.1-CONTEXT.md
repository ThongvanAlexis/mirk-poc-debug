# Phase 3.1: Fix Fog Pan-Translation - Context

**Gathered:** 2026-05-02 (initial Plan 03.1-01 era)
**Revised:** 2026-05-04 (MIRK-01 architectural insight captured + Plan 03.1-11 scope locked at Walk #4 only)
**Status:** Ready for execution — Plan 03.1-11 is the closure attempt (Walk #4 sideload UAT)

<domain>
## Phase Boundary

Close Phase 3.1 by validating the **Plan 03.1-10 software-fix bundle** on iPhone 17 Pro **Walk #4** sideload UAT and authoring the verdict (`CONFIRMED-AFTER-FIX` | `DENIED-FINAL` | `ITERATING`).

The Plan 03.1-10 bundle (HEAD `b31766f`) layers three fixes:
- **UX-02** (commit `a276505`) — disable map rotation via `InteractionOptions(flags: InteractiveFlag.all & ~InteractiveFlag.rotate)`
- **FOG-17a** (commit `2b32513`) — CPU-side integer/fractional decomposition of `camera.pixelOrigin` in `_FogPainter.paint()` keeping shader input bounded ~1537 raw px regardless of zoom
- **FOG-17** (commit `dbc3e21`) — world-coord noise sampling `worldPx = fragUv * uResolution + uPixelOrigin; noiseUv = worldPx / kNoiseTilePx` in BOTH `atmospheric_fog.frag` AND `atmospheric_fog_debug_spiral.frag`

Phase 3 closed `HYPOTHESIS DENIED` (Plan 03-08 walk 2026-05-01); Phase 3.1 either reverses to `CONFIRMED-AFTER-FIX` (unblocking Phase 4 + Phase 5) or strengthens to `DENIED-FINAL` (terminating the POC). After 4 prior iterations (Walks #1/2/3 + Walk #3b marker analysis), Plan 03.1-11 = Walk #4 = closure attempt.

Out of scope for Plan 03.1-11: any architectural formalization (MIRK-01 ABI is captured as a deferred future concern, see Implementation Decisions below); wisp particles (Phase 4 — stays BLOCKED until 3.1 closes); final POC verdict promotion (Phase 5 — also BLOCKED); pivot to alternative renderers (Phase 3.2 conversation if Walk #4 surfaces architectural counter-evidence); MirkFall port-back (Phase 5 / project-close).

</domain>

<decisions>
## Implementation Decisions

### Plan 03.1-11 scope: Walk #4 sideload UAT ONLY

- **Plan 03.1-11 stays narrowly focused on the iPhone 17 Pro Walk #4 sideload UAT** against the existing Plan 03.1-10 fix bundle. No architectural rewrite, no shader rename, no new CI tests, no rollback. The plan body (3 tasks: Task 1 pre-walk gates + skeleton authoring, Task 2 sideload checkpoint, Task 3 verdict authoring) is structurally correct as it stands at 2026-05-03 framing.

### Discuss-phase decisions captured 2026-05-04

A `/gsd:discuss-phase 3.1` session 2026-05-04 surfaced 4 gray-area decisions. The developer's subsequent clarification (*"the architecture is malleable, I don't really care about the architecture for now, it can be made better later, once we find how to solve our issue, for now we don't even know how to make it work so defining an architecture seems too early"*) reframed three of them. Final decisions for the planner:

1. **Walk #4 timing** = ALL tasks (pre-walk gates + sideload + verdict authoring) in Plan 03.1-11. Single plan, immediate verdict. No split into 03.1-11 ABI + 03.1-12 walk. → matches the existing 03.1-11-PLAN.md structure (no change needed).

2. **FOG-12 + FOG-13 Canvas-side compensation audit** = **DEFERRED to post-Walk-4**. The discuss-phase first answer was "Auditer puis décider" (Plan 03.1-11 includes an explicit audit task). The developer's subsequent "architecture is malleable; don't care for now; once we find how to solve our issue" supersedes this — the empirical Walk #4 outcome IS the answer (if fog covers viewport correctly with no wedges/gaps under any gesture, FOG-12/13 are working as expected; if Walk #4 surfaces fog-coverage anomalies, a follow-up Plan 03.1-12 audit task addresses it). **Plan 03.1-11 does NOT include a FOG-12/13 audit task.** This avoids architecture work in Plan 03.1-11 per the developer's "validate first, architect later" principle.

3. **MIRK-01 acceptance test trio** (static uniform-list inspection + behavioural swap-shader test + zero-painter-branching invariant) = **DEFERRED**. MIRK-01 itself is deferred; its acceptance criteria are deferred with it. → no Tasks A/B/C in Plan 03.1-11.

4. **ABI uniform rename `uPixelOrigin` → `uWorldOffset`** = **DEFERRED**. Tied to MIRK-01 ABI formalization which is deferred. → no rename task in Plan 03.1-11; existing `uPixelOrigin` slot 3..4 stays as-is across both shaders + `FogShaderUniforms` + JSONL `uOffsetX*` field names.

**Net effect on planner output:** the planner should produce a Plan 03.1-11 that is structurally identical to the existing 491-line 2026-05-03 framing — no new tasks, no architectural work, just Walk #4 mechanics. If the planner identifies any drift between the existing plan body and the locked decisions above, it should make targeted updates ONLY (not replan from scratch).
- **Pre-walk gates** (per existing PLAN.md): `flutter test` GREEN, `flutter analyze --fatal-infos` 0 warnings, `dart format` clean, `dart test tool/test/` GREEN, CI green on the post-Plan-03.1-10 SHA on `main`, IPA downloaded via `gh run download` to `.uat-tmp/mirk-poc-debug-unsigned.ipa`.
- **Walk #4 mechanics**: ≥ 5 minutes through Melun + ≥ 10 deliberate combined pinch-zoom-and-pan gestures + ≥ 3 recenter taps + post-walk Mail-share of session log + pre-walk `/sanity` shader-compile gate. Dev-marker button (`tag: "steppy_translation"`, hardcoded since Plan 03.1-09) tapped on any residual symptom; post-hoc tag disambiguation via timing.
- **Walk evidence documents** = NEW `03.1-FALSIFICATION-4.md` + `03.1-UAT-4.md`. Walk #1 + #2 + #3 + #3b records UNTOUCHED — historical integrity across iteration cycles.
- **Verdict semantics**:
  - `CONFIRMED-AFTER-FIX` = developer verbal `approved` AND PERF-07 thresholds satisfied (medianMs ≤ 16, p95Ms ≤ 32, maxMs ≤ 48 across ≥ 10 combined gestures) AND Q1 stepping resolved (no perceived hard-step events during pure pan; the world-coordinate formulation should produce monotonic noise scrolling per FOG-17 design) AND Q2 rotation-correlated mis-coverage structurally absent (UX-02 prevents rotation gestures from accumulating matrix elements).
  - `DENIED-FINAL` = the diagnostic JSONL stream cannot resolve a residual failure mode via a follow-up Phase 3.1 iteration; formal POC architectural counter-evidence; Phases 4+5 cancel or reframe.
  - `ITERATING` = a residual failure mode surfaces; Plan 03.1-12 follows under the existing fix-axis exploration (NOT a new architecture). Per CONTEXT §Iteration policy: no hard cap on walk count.

### MIRK-01 architectural insight: captured but DEFERRED

- Per developer's 2026-05-04 clarification (CLAUDE.md `# MIRL solution`): the MIRL fog/visual layer will compose multiple different shaders in the real (non-POC) MirkFall application; non-periodic shaders supported; explicit camera↔shader coupling natural and accepted.
- **Conceptual MIRK-01** = standardized ABI between `_FogPainter` and any conforming shader (camera-derived inputs forwarded through standard uniforms; any shader implementing the ABI plugs in without painter-side changes).
- **Formalization is DEFERRED** per developer's explicit framing: *"the architecture is malleable, I don't really care about the architecture for now, it can be made better later, once we find how to solve our issue, for now we don't even know how to make it work so defining an architecture seems too early"*.
- **Existing implementation is the seed**: Plan 03.1-10 (`uPixelOrigin` slot 3..4 + FOG-17a CPU decomposition) is conceptually aligned with MIRK-01 — the `uPixelOrigin` uniform IS the camera-derived input; FOG-17a IS the architectural precision computation. No formal ABI doc, no rename, no architecture-level CI test in this plan. Revisit post-Walk-4 verdict if useful.
- **Old branch (Plans 03.1-11→16 archival, branch `old`)**: those plans iterated shader rewrites (FOG-18 world-meter, Fix B' meter-space, Worley periodic) that converged on `DENIED-final` at Walk #6. They're NOT a useful precedent — they kept iterating per-shader instead of walk-validating the existing fix.

### Mail-share discipline (re-established for Walk #4)

- Walk #3 broke the post-walk Mail-share pattern (verbal evidence only — quantitative JSONL grep correlation unavailable). **Walk #4 explicitly requires** the developer to share the session log via Mail post-walk so JSONL streams (`infrastructure.mirk.frame_delta` + `infrastructure.mirk.sdf` + `infrastructure.mirk.fog_transform`) can be grep-correlated against any dev-marker timestamps. The Walk #3b marker-correlation analysis (`03.1-WALK-3B-MARKER-ANALYSIS.md`) is the structural template.

### Iteration policy (carried forward — unchanged)

- Plan-revise-walk loop. **No hard cap on walk count.** First walk fails → read JSONL diagnostic logs from the shared session log, revise the fix plan, ship a new IPA, walk again. Iterate until either confirmed or developer manually escalates to fix-failure contingency (Phase 3.2 pivot or `DENIED-final`).
- Walk #4 is the 4th attempt under Phase 3.1 (Walk #1 P03.1-03 + Walk #2 P03.1-06 + Walk #3 P03.1-09 + Walk #3b implicit marker analysis). Each prior walk iterated the fix-axis hierarchy (constant-zero → viewport-width-period shimmer → noise-tile-period stepping → rotation-correlated rect-cover); Walk #4 closes against the layered Plan 03.1-10 bundle.

### Locked architectural invariants (non-negotiable across Phase 3.1)

- `uSdfRect = (0, 0, 1, 1)` identity (RESEARCH §Anti-Pattern 1; dynamic uSdfRect re-introduces BUG-014 root cause).
- Single `MapCamera.of(context)` snapshot per `FogLayer.build()` (FOG-07).
- `_FogPainter` reads `canvas.getTransform()` exactly ONCE per paint (FOG-12 single-matrix-snapshot).
- Stopwatch BY REFERENCE for `uTime` (anti-frozen-uTime; Plan 03-05 lock).
- Dual-clock discipline for JSONL rollups: `Stopwatch.elapsedMicroseconds` for math; `DateTime.now()` ONLY for the `epochSecond` rollup tag.
- 1-Hz log rollup cadence aligned across `frame_delta` + `sdf` + `fog_transform` loggers (wall-clock per-second boundary for grep-correlation).

### Claude's Discretion (Plan 03.1-11 planner / executor level)

- Exact wording of the Walk #4 evidence transcript headers in `03.1-UAT-4.md` (mirror Walk #1/2/3 patterns).
- Exact post-walk grep-correlation analysis structure if dev-marker fires (mirror `03.1-WALK-3B-MARKER-ANALYSIS.md` template).
- CI run number + SHA referenced in `03.1-FALSIFICATION-4.md` evidence section (filled at execute-phase time post-walk).
- Whether to surface a 2026-05-04 datestamp annotation in the plan body or frontmatter to acknowledge the discuss-phase pass — planner picks based on planning-doc-style consistency.

</decisions>

<code_context>
## Existing Code Insights — Plan 03.1-11 surfaces (read-only at Walk #4)

### State at HEAD `b31766f` (Plan 03.1-10 bundle in production)

- **`lib/presentation/widgets/fog_layer.dart`** (572 lines): `_FogPainter.paint()` reads `canvas.getTransform()` once, applies `canvas.translate(-canvasOffset)` (FOG-13), `canvas.clipPath(clipPath)` (FOG-12), then `shaderRenderer.render(pixelOrigin: ...)` (FOG-17a CPU-decomposed bounded composite), then `canvas.drawRect(Offset.zero & size, Paint()..shader = liveShader)`. Plan 03.1-11 does NOT modify this file.
- **`assets/shaders/atmospheric_fog.frag`** + **`assets/shaders/atmospheric_fog_debug_spiral.frag`**: both consume `uniform vec2 uPixelOrigin` slot 3..4 (FOG-17 world-coord noise sampling). Plan 03.1-11 does NOT modify these.
- **`lib/infrastructure/mirk/shader/fog_shader_uniforms.dart`**: 41-slot uniform layout including `uPixelOrigin` slot 3..4. Plan 03.1-11 does NOT modify.
- **`lib/infrastructure/mirk/fog_transform_logger.dart`**: per-paint observation captures `(matrix[12..13], camera.pixelOrigin, camera.center, appliedUOffset)` → 1-Hz JSONL rollups via `Logger('infrastructure.mirk.fog_transform')`. JSONL field names use `uOffset*` legacy from Plan 03.1-04 — kept as-is for grep-tool compatibility with Walks #1/2 session logs.
- **`lib/presentation/widgets/debug_spiral_layer.dart`**: separate widget rendering the debug-spiral .frag at `/sanity` AND `/map` (DEBUG-01 toggle). Plan 03.1-11 does NOT modify.

### Established walk infrastructure (carried forward)

- **`.planning/phases/03.1-fix-fog-pan-translation/03.1-WALK-3B-MARKER-ANALYSIS.md`**: structural template for Walk #4 marker-correlation analysis if dev-marker button fires.
- **`.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-3.md`** + `03.1-UAT-3.md`: structural template for `03.1-FALSIFICATION-4.md` + `03.1-UAT-4.md`.
- **CI workflows** (Phase 1/2/3 lock-in): `flutter test`, `flutter analyze --fatal-infos`, `dart format --set-exit-if-changed`, `dart test tool/test/` all wired; both APK + IPA artifacts auto-built on every push to `main`.
- **`gh` CLI**: authenticated for `gh run list` / `gh run watch` / `gh run download` IPA artifact retrieval.
- **SideStore**: paired desktop pairing-file + iPhone 17 Pro for Walk #4 sideload (per Walks #1/2/3 pattern).

### Established patterns (Phase 1 + 2 + 3 + 3.1 lock-in — non-negotiable)

- State management: plain `StatefulWidget` + `setState` + constructor-injected services via `MapScreenServices` DTO.
- Logging: `package:logging` `infrastructure.mirk.*` family; JSONL body, INFO level, 1-Hz cadence wall-clock-aligned for grep-correlation.
- Strict analysis: `strict-casts`, `strict-inference`, `strict-raw-types`, `use_build_context_synchronously: error`.
- Pinned versions: any new dep strict-pinned, audit row in `DEPENDENCIES.md`.
- GOSL header on every new `.dart` file in `lib/` and `test/`.

</code_context>

<specifics>
## Specific Ideas

- **Walk #4 is the closure attempt.** Phase 3.1 has been iterating since 2026-05-02 (4 walks, 10 plans). The Plan 03.1-10 bundle is the layered result of every prior fix axis (Plan 03.1-02 uOffset → Plan 03.1-04 SHADER-MODULO-WRAP → Plan 03.1-05 CANVAS-FRAME-ALIGNMENT → Plan 03.1-07 noise-tile-period → Plan 03.1-08 fog-rect viewport-coverage → Plan 03.1-10 world-coord noise + UX-02 rotation-disable). Walk #4 either confirms or denies this layered bundle empirically.
- **Architecture is malleable.** Per developer's 2026-05-04 clarification, formal architectural rework is PREMATURE until empirical validation lands. MIRK-01 captured as deferred future concern; Plan 03.1-11 stays narrow.
- **The Plan 03-08 verdict stands** until Walk #4 reverses it. Until then the formal POC architectural recommendation remains "do NOT port back to MirkFall as-implemented."
- **Old branch (Plans 03.1-11→16 archival, branch `old`)** is a cautionary tale: continued iterating shader rewrites (FOG-18 → Fix B' → Worley periodic) without re-validating the existing fix bundle empirically. Each iteration moved the symptom but never converged. `main` is reset to `b31766f` (the layered Plan 03.1-10 bundle BEFORE that iteration loop began); Walk #4 is the empirical answer the `old` branch never got.

</specifics>

<deferred>
## Deferred Ideas

- **MIRK-01 architectural ABI formalization** — captured as deferred future concern; formal acceptance criteria (uniform-list inspection + swap-shader behavioural test + zero-painter-branching invariant + ABI source-of-truth doc) revisited post-Walk-4 verdict if useful.
- **MIRK-01 acceptance test trio** (static (a) + behavioural (b) + audit (c)) — discuss-phase decision was "trio complet" but tied to MIRK-01 formalization which is deferred → trio also deferred.
- **ABI uniform rename `uPixelOrigin` → `uWorldOffset`** — discuss-phase decision was "rename to uWorldOffset" but tied to MIRK-01 ABI formalization which is deferred → rename also deferred. Existing `uPixelOrigin` stays in shaders + `FogShaderUniforms` + recording renderer + tests.
- **FOG-12 + FOG-13 Canvas-side compensation audit** — discuss-phase decision was "auditer puis décider" but superseded by the developer's subsequent "architecture is malleable; don't care for now; once we find how to solve our issue, it can be made better later" clarification → audit deferred to post-Walk-4. Walk #4 itself provides empirical answer: fog covers viewport correctly under all gesture types ⇒ FOG-12/13 working; viewport gap surfaces ⇒ open audit + redesign in Plan 03.1-12+.
- **JSONL field rename** `uOffsetX*` → `uWorldOffsetX*` — deferred indefinitely; legacy names kept for grep-tool compatibility with Walks #1/2 session logs.
- **Walk-replay tool** (record GPS once, replay on Pixel 4a / Windows desktop) — deferred since Phase 3; revisit if iteration count climbs (5+ walks).
- **`p99Ms` field on FrameDeltaProbe** — Phase 3 deferred; Phase 3.1 does not reopen.
- **`tool/extract_walk.dart` helper** (auto-extract probe + SDF + fog-transform stats from JSONL) — Phase 4/5 if walk count climbs.
- **`MapView` domain abstraction** — Locked OUT of POC scope per PROJECT.md (migration concern).
- **Pixel 4a parity walk for Phase 3.1** — Stays in Phase 5 per the original roadmap.
- **Pivot to alternative renderers** (`mapsforge_flutter`, custom MVT-on-Canvas, `flutter_gpu`) — Phase 3.2 conversation IF Walk #4 produces `DENIED-FINAL` AND diagnostic evidence implicates the renderer specifically.

</deferred>

---

*Phase: 03.1-fix-fog-pan-translation*
*Context gathered: 2026-05-02 (initial); revised 2026-05-04 (MIRK-01 deferred; Plan 03.1-11 = Walk #4 only)*
