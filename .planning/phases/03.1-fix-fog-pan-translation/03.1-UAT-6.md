---
phase: 03.1-fix-fog-pan-translation
plan: 15
type: uat
status: pending
source: [03.1-14-SUMMARY.md, 03.1-13-SUMMARY.md]
started: 2026-05-03
walked: TBD
ci_run: TBD
ci_sha: TBD
device: iPhone 17 Pro (ProMotion 120 Hz, A19 Pro)
location: central Melun (initial GPS fix at 48.5397, 2.6553 area)
exit_gate: developer's resume signal — verbal `approved [log_path]` / `denied [log_path]:` / `iterating [log_path]:` (log_path REQUIRED — structurally-enforced Mail-share gate retained from Plan 03.1-13; STRUCTURAL build-provenance gate NEW for Walk #6)
verdict: TBD
log_path: TBD
gaps: []
---

# Phase 3.1 UAT — Walk #6 (Post-Plan-03.1-14 Fix B′ Meter-Space FOG-17a Decomposition + FOG-19)

**Gate:** Walk #6 is the empirical answer to the Plan 03.1-14 (Fix B′ meter-space FOG-17a decomposition + FOG-19; Dart-side `worldMeters = pixelOrigin × metersPerPixel; bounded = (intMeters % 4096) + fracMeters`; shader uniform slot 3..4 rename `uPixelOrigin` → `uWorldMetersOrigin`; period-commensurability invariant `kPocFogIntegerWrapPeriodMeters % kPocFogNoiseTilePxMeters == 0` GREEN) software fix bundle. Walk #5 closed `ITERATING-WITH-Q1-REGRESSED-AND-Q5-NOT-FIXED-FROM-PERIOD-COMMENSURABILITY-GAP` 2026-05-03; Walk #6 either flips the verdict to `CONFIRMED-AFTER-FIX` (unblocking Phase 4 + Phase 5) or strengthens it to `DENIED-FINAL` (terminating the same-Canvas hypothesis at the deepest coordinate-system axis) or — per CONTEXT §Iteration policy — captures a seventh `ITERATING` verdict that opens Plan 03.1-16+.

This UAT mirrors the shape of `.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-5.md` (Walk #5's transcript) with three Walk #6-specific specialisations: (1) **NEW STRUCTURAL build-provenance gate** at the resume-signal contract — Task 1 explicitly verifies `git rev-parse HEAD == gh run view <RUN_ID> --json headSha`; if HEAD has commits after the CI SHA, the executor pushes them and triggers a fresh CI run BEFORE proceeding; (2) **STRUCTURALLY-ENFORCED Mail-share gate RETAINED from Plan 03.1-13** (log path REQUIRED in resume signal); (3) **NEW `coordinateSpace: 'meters'` JSONL signature verification** — Plan 03.1-14 Task 2 augmented FogTransformLogger emit body with `coordinateSpace: 'meters'`; post-walk grep confirms field is present + value is `'meters'`. The metersPerPixel field (Plan 03.1-13 augmentation) is now empirically captured because the build-provenance gate holds.

## Build Under Test

| Field        | Value |
| ------------ | ----- |
| CI run       | TBD (filled post-walk) |
| Run URL      | TBD |
| HEAD SHA     | TBD (Plan 03.1-14 layered fix bundle landed; build-provenance gate enforces local HEAD == CI headSha at Task 1) |
| iOS artifact | `.uat-tmp/mirk-poc-debug-unsigned.ipa` (size TBD) |
| Gates job    | TBD (expected: success) |
| Android job  | TBD (expected: success) |
| iOS job      | TBD (expected: success) |
| Tests        | 208 GREEN / 2 SKIPPED / 0 RED (Plan 03.1-14 new tests + REWRITTEN tests + TOUCHED tests; FOG-09 vacuous-test guard stays SKIPPED; pre-Plan-03.1-14 historical sub-tests `@Tags(['historical'])` stay SKIPPED) |

## Pre-walk Gates

- [ ] `flutter test` full suite GREEN (208 tests + 2 skipped historical; existing + Plan 03.1-14 new tests + REWRITTEN tests + TOUCHED tests)
- [ ] `flutter analyze --fatal-infos` 0 warnings
- [ ] `dart format --line-length 160 --set-exit-if-changed .` clean
- [ ] `dart test tool/test/` GREEN
- [ ] CI run on the post-Plan-03.1-14 SHA on `main` GREEN (gates + build-android + build-ios)
- [ ] **STRUCTURAL build-provenance gate (NEW for Walk #6)** — `git rev-parse HEAD` equals `gh run view <RUN_ID> --json headSha`. If unequal: push HEAD; trigger fresh CI run; wait for green; download new IPA. Eliminates the build-provenance gap class of failure that Walk #5 surfaced (commit `2b087a3` post-dated CI run `25285037397`).
- [ ] IPA artifact present at `.uat-tmp/mirk-poc-debug-unsigned.ipa`
- [ ] FogTransformLogger emits `metersPerPixel` field (Plan 03.1-13 Task 1a augmentation, NOW EMPIRICAL because build-provenance gate holds)
- [ ] FogTransformLogger emits `coordinateSpace: 'meters'` field (Plan 03.1-14 Task 2 augmentation; verifies the JSONL semantics flip landed in the IPA)

## Walk Protocol

- **Where:** Central Melun (initial GPS fix at 48.5397, 2.6553 area — same theatre as Walks #1-5).
- **Duration:** ≥ 5 minutes continuous walk with the IPA running and the device awake.
- **Gestures:** ≥ 10 deliberate combined pinch-zoom-and-pan gestures, ≥ 3 recenter FAB taps. AT LEAST ONE deliberate two-finger rotate-attempt gesture (validates UX-02 / Q2 sub-claim — should be a no-op). AT LEAST ONE sustained ≥ 60-second one-direction pan (validates Q1 — PRIMARY Walk #6 closure-evidence-gate; developer fires `steppy_translation` markers if ANY residual stepping perceived; Walk #5 fired 2; Walk #6 expects 0). AT LEAST ONE complete zoom traversal (min zoom → max zoom or max → min) at a fixed map position with both production fog AND debug-spiral toggled (validates Q5 — PRIMARY Walk #6 closure-evidence-gate; sub-tests 5a/5b/5c/5d mirror Walk #5).
- **HUD visible during walk:** `FpsCounterOverlay` (top:8, right:8) + `MapCompass` (top:56, right:8 — should stay at 0° throughout per UX-02) + `FrameDeltaProbeOverlay` (top:104, right:8).
- **Pre-walk shader-sanity gate:** `/sanity` route — confirm production fog renders with circular reveal hole; toggle debug-spiral ON/OFF round-trip; confirm `Icons.arrow_back` works; back-pop returns to `/map`. **NEW for Walk #6: confirm zero shader-compile exceptions** in the FileLogger output (Plan 03.1-14's slot-3/4 rename + worldMeters formula reorder MUST NOT produce a compile error in the post-Plan-03.1-14 IPA). Attempt a two-finger rotate gesture on `/map` — confirm the map does NOT rotate (UX-02 walk-time validation).
- **Dev-marker discipline RETAINED from Walk #5.** The dev-marker button at the AppBar (commit `79ab978`) is operational. Developer is instructed to tap the marker every time they perceive ANY residual symptom (steppy translation, fog mis-coverage, zoom-scramble residual, anything visually anomalous). Walk #5 fired 2 markers tagged `steppy_translation` correlated with FOG-17a wraps; Walk #6 expects 0 markers.
- **Post-walk: STRUCTURALLY-ENFORCED Mail-share (RETAINED from Plan 03.1-13).** session log shared via Mail (LOG-04 round-trip — REQUIRED for Walk #6) → saved Mail attachment to known local path → developer reports absolute path as parameter in resume signal (`approved [path]` / `denied [path]:` / `iterating [path]:`). Executor refuses verdict authoring without log path. Four JSONL streams extracted via grep into `.uat-tmp/walk6_{frame_delta,sdf,fog_transform,dev_markers}.jsonl`.

## Walk Evidence

The full evidence + Walk Evidence sections + Verdict are in the falsification document (`03.1-FALSIFICATION-6.md`). This UAT transcript holds the lighter-weight per-stream summary + the developer's verbatim verdict.

### Pre-walk shader-sanity gate

_TBD — filled post-walk._

### Frame-delta probe rollups (PERF-07, Criterion A — `infrastructure.mirk.frame_delta` JSONL)

_TBD — filled post-walk. Walk #5 baseline: medianMs ≤ 1.808 / p95Ms ≤ 6.105 / maxMs ≤ 9.756 across 67 1-Hz rollups; 8.9×/5.2×/4.9× headroom. Walk #6 should preserve at least Walk #5 levels (Plan 03.1-14 is per-paint negligible — formula reorder, not new operations)._

### SDF rebuild rollups (`infrastructure.mirk.sdf` JSONL — PERF-08 status)

_TBD — filled post-walk. PERF-08 stays at `Falsified-by-walk-2` unless Walk #6 surfaces it as a higher-tier concern. Plan 03.1-14 doesn't target PERF-08._

### Diagnostic JSONL — Walk #6 fog_transform rollup (`infrastructure.mirk.fog_transform`)

_TBD — filled post-walk._

_**KEY Plan 03.1-14 SIGNATURES to verify:**_
- _**`coordinateSpace: 'meters'` field present** in EVERY rollup body (Plan 03.1-14 Task 2 augmentation — direct visible signature)._
- _**`appliedUOffsetX/Y` bounded under `kPocFogIntegerWrapPeriodMeters + 1 = 4097 m`** across ALL rollups (FOG-19 invariant — direct visible signature; vs Walk #5's pixel-space bounded composite under 1537 raw px)._
- _**`metersPerPixel` field present, non-zero, evolving smoothly with camera zoom** (Plan 03.1-13 augmentation, NOW EMPIRICAL because build-provenance gate holds; at zoom 13 lat 48.5° expected ≈ 12.66; at zoom 15 ≈ 3.16; at zoom 19 ≈ 0.198)._

### Dev-marker JSONL (`dev_marker`)

_TBD — filled post-walk. Walk #6 closure-evidence-gate: ZERO `steppy_translation` markers during the walk. If any markers fired, mirror the Walk #3b template + Walk #5 marker-correlation analysis._

### FPS observations (PERF-03)

_TBD — filled post-walk._

### Subjective verdict (Criterion B + Q1-Q5)

_TBD — filled post-walk. Developer's composite verdict across Walk #6 (verbatim quotes per sub-claim):_

1. _Q1: TBD_
2. _Q2: TBD_
3. _Q3: TBD_
4. _Q4: TBD_
5. _Q5: TBD_

## Verdict

**Verdict:** TBD.

**Walked at:** TBD.

**Developer's verbatim words:** TBD.

**Interpretation:** TBD.

## Deviations / Surprises

_TBD — filled post-walk._

## Summary

_TBD — filled post-walk._
