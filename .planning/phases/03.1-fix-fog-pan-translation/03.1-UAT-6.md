---
phase: 03.1-fix-fog-pan-translation
plan: 15
type: uat
status: iterating
source: [03.1-14-SUMMARY.md, 03.1-13-SUMMARY.md]
started: 2026-05-03
walked: 2026-05-04
ci_run: "25291831174"
ci_sha: "15e98b3"
device: iPhone 17 Pro (ProMotion 120 Hz, A19 Pro)
location: central Melun (initial GPS fix at 48.5285, 2.6551 area)
exit_gate: developer's resume signal — verbal `denied [/full/path/to/log.txt]: <verbatim>` (log_path REQUIRED — structurally-enforced Mail-share gate retained from Plan 03.1-13; STRUCTURAL build-provenance gate held second time at Walk #6)
verdict: ITERATING-WITH-Q1-NEW-FAILURE-MODE-AND-Q5-PARTIAL
log_path: "C:\\claude_checkouts\\mirk-poc-debug\\20260503T220301Z_logs.txt\\20260503T220301Z_logs.txt"
gaps:
  - id: FOG-20
    title: Noise primitive period exactly aligned with noise tile period
    plan_target: "03.1-16"
    rationale: "Plan 03.1-14 Fix B′ delivered constant-magnitude INVARIANCE across meter-space wrap events (≈11% fbm3 dynamic-range residual phase shift, deterministic, zoom-independent, lat-independent), but Walk #6 EMPIRICALLY FALSIFIES the predicted sub-perceptibility: 7 dev-markers tagged steppy_translation correlate with meter-space wrap events in fog_transform JSONL within ±1 sec (3.5× Walk #5's 2 markers; markers fired at multiple zoom × lat combinations including z≈10 mpp ≈26.6 and z≈14 mpp ≈18). The per-wrap residual is NOT sub-perceptual at the developer's worldMeters positions. Plan 03.1-14 contingency activates: option D-3 Worley periodic noise primitive (period exactly aligned with kPocFogNoiseTilePxMeters; ZERO per-wrap discontinuity vs Plan 03.1-14's constant-magnitude residual). FOG-20 captures the missing meta-invariant — any future shader noise primitive MUST have period exactly equal to the meter-space noise tile period to eliminate the per-wrap-discontinuity class of failure."
---

# Phase 3.1 UAT — Walk #6 (Post-Plan-03.1-14 Fix B′ Meter-Space FOG-17a Decomposition + FOG-19)

**Gate:** Walk #6 is the empirical answer to the Plan 03.1-14 (Fix B′ meter-space FOG-17a decomposition + FOG-19; Dart-side `worldMeters = pixelOrigin × metersPerPixel; bounded = (intMeters % 4096) + fracMeters`; shader uniform slot 3..4 rename `uPixelOrigin` → `uWorldMetersOrigin`; period-commensurability invariant `kPocFogIntegerWrapPeriodMeters % kPocFogNoiseTilePxMeters == 0` GREEN) software fix bundle. Walk #5 closed `ITERATING-WITH-Q1-REGRESSED-AND-Q5-NOT-FIXED-FROM-PERIOD-COMMENSURABILITY-GAP` 2026-05-03; Walk #6 captures a seventh `ITERATING` verdict — `ITERATING-WITH-Q1-NEW-FAILURE-MODE-AND-Q5-PARTIAL` — opening Plan 03.1-16 (option D-3 Worley periodic noise primitive — Plan 03.1-14 contingency activates).

This UAT mirrors the shape of `.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-5.md` (Walk #5's transcript) with three Walk #6-specific specialisations: (1) **NEW STRUCTURAL build-provenance gate** at the resume-signal contract held first attempt (HEAD == CI headSha == `15e98b3`); (2) **STRUCTURALLY-ENFORCED Mail-share gate RETAINED from Plan 03.1-13** held second time (after Walk #5 first time); (3) **NEW `coordinateSpace: 'meters'` JSONL signature verification** — Plan 03.1-14 Task 2 augmented FogTransformLogger emit body with `coordinateSpace: 'meters'`; post-walk grep confirms field is present + value is `'meters'` in 51/51 rollups. The metersPerPixel field (Plan 03.1-13 augmentation) is NOW empirically captured because the build-provenance gate holds (Walk #5 had no field at all due to commit `2b087a3` post-dating CI run `25285037397`).

## Build Under Test

| Field        | Value |
| ------------ | ----- |
| CI run       | 25291831174 |
| Run URL      | https://github.com/ThongvanAlexis/mirk-poc-debug/actions/runs/25291831174 |
| HEAD SHA     | `15e98b3dd6e5332c417674f83e930b884120ce16` (docs(03.1-15): Walk #6 falsification + UAT skeletons (Task 1)) |
| iOS artifact | `.uat-tmp/mirk-poc-debug-unsigned.ipa` (11.58 MB) |
| Gates job    | success |
| Android job  | success |
| iOS job      | success |
| Tests        | 208 GREEN / 2 SKIPPED / 0 RED (Plan 03.1-14 6 NEW FOG-19 sub-tests + 7 REWRITTEN tests + 4 TOUCHED tests + Plan 03.1-13 logger augmentation; FOG-09 vacuous-test guard stays SKIPPED; pre-Plan-03.1-14 historical sub-tests `@Tags(['historical'])` stay SKIPPED) |

## Pre-walk Gates

- [x] `flutter test` full suite GREEN (208 tests + 2 skipped historical; existing + Plan 03.1-14 new tests + REWRITTEN tests + TOUCHED tests)
- [x] `flutter analyze --fatal-infos` 0 warnings
- [x] `dart format --line-length 160 --set-exit-if-changed .` clean
- [x] `dart test tool/test/` GREEN (18/18)
- [x] CI run on the post-Plan-03.1-14 SHA on `main` GREEN (gates + build-android + build-ios; run 25291831174, SHA 15e98b3)
- [x] **STRUCTURAL build-provenance gate (NEW for Walk #6) — PASSED FIRST ATTEMPT.** `git rev-parse HEAD` == `gh run view 25291831174 --json headSha` == `15e98b3dd6e5332c417674f83e930b884120ce16`. The Plan 03.1-13 Task 1a logger augmentation commit `2b087a3` is included in the CI build. The Plan 03.1-14 commit `0d65212` (Fix B′ + FOG-19) is included. Walk #6's IPA is built from the EXACT commit on local HEAD; metersPerPixel JSONL signature + coordinateSpace JSONL signature are BOTH present in the IPA's logger emit body — empirically verified post-walk (51/51 rollups carry both fields).
- [x] IPA artifact present at `.uat-tmp/mirk-poc-debug-unsigned.ipa` (11.58 MB; downloaded via `gh run download 25291831174 --name mirk-poc-debug-ios-unsigned-ipa --dir .uat-tmp/`)
- [x] FogTransformLogger emits `metersPerPixel` field (Plan 03.1-13 Task 1a augmentation, NOW EMPIRICAL because build-provenance gate holds — verified post-walk: 51/51 rollups; envelope `[3.164, 26.660]` consistent with z=10..15 walk regimes)
- [x] FogTransformLogger emits `coordinateSpace: 'meters'` field (Plan 03.1-14 Task 2 augmentation; verified post-walk: 51/51 rollups; field value is `'meters'` in every rollup — Plan 03.1-14 semantic flip landed in IPA)

## Pre-walk Sanity gate — SKIPPED by developer (deviation from plan, NOT BLOCKING)

Developer skipped the `/sanity` route smoke-test before the walk: *"I didn't do the sanity pre walk, frankly I don't get what purpose it serves"*. Documented as a Walk #6 deviation; non-blocking for the verdict (the production fog rendering at `/map` exercises the FOG-19 path regardless; the JSONL signatures + dev-marker correlation deliver the architectural diagnosis without the /sanity gate). See `03.1-FALSIFICATION-6.md` § Pre-walk shader-sanity gate for the design intent of the gate + recommendation for future walks (RETAIN structurally optional with clearer rationale; current `<how-to-verify>` block undercommunicates the build-confusion-detection purpose).

## Plan-vs-reality artifact name (Rule 3 deviation, NOT BLOCKING)

Plan 03.1-15 Task 1 step 8 specified `gh run download <RUN_ID> --name ios-unsigned --dir .uat-tmp/`. Actual workflow artifact name in `.github/workflows/ci.yml` line 120 is `mirk-poc-debug-ios-unsigned-ipa`. Task 1 used the correct artifact name; documented as Rule 3 — Blocking deviation. Future plan templates should be aligned.

## Walk Protocol

- **Where:** Central Melun (initial GPS fix at 48.5285, 2.6551 area — same theatre as Walks #1-5).
- **Duration:** ~53-second active session captured in JSONL (00:03:01.99Z..00:03:54.87Z); developer continued physically walking but the high-information-density 21-second window between markers M1-M7 carries the architectural diagnosis.
- **Gestures performed:** developer report *"I clic the bug button to mark when I notice a step when I waz zooming, then panning"* — i.e., zoom-then-pan sequence with deliberate marker-firing on each perceived stepping moment. JSONL evidence: `pixelOriginX` envelope `[505126, 4256094]` confirms full zoom-traversal across z≈10..15 (zoom-out then zoom-in cycle); `metersPerPixel` envelope `[3.164, 26.660]` consistent with z≈10..15 regime; multiple meter-space wrap events visible in the uOffsetX/Y per-second envelopes.
- **HUD visible during walk:** `FpsCounterOverlay` (top:8, right:8) + `MapCompass` (top:56, right:8 — stayed at 0° throughout per UX-02; no rotation complaint surfaced) + `FrameDeltaProbeOverlay` (top:104, right:8).
- **Pre-walk shader-sanity gate:** SKIPPED (see deviation above).
- **Dev-marker discipline:** developer fired 7 markers tagged `steppy_translation` across a 21-second window. 3.5× Walk #5's 2 markers. ALL markers correlate with meter-space wrap events in fog_transform JSONL within ±1 sec — see `03.1-FALSIFICATION-6.md` § Dev-marker rollup for per-marker correlation.
- **Post-walk: STRUCTURALLY-ENFORCED Mail-share** held second time. Session log Mail-shared (LOG-04 round-trip — REQUIRED for Walk #6) → saved Mail attachment to `C:\claude_checkouts\mirk-poc-debug\20260503T220301Z_logs.txt\20260503T220301Z_logs.txt` (1.1 MB; 5473 lines) → developer reports absolute path as parameter in resume signal: `denied [path]: panning still worse than the perfect result we had further commits away ...`. Four JSONL streams extracted via grep into `.uat-tmp/walk6_{frame_delta,sdf,fog_transform,dev_markers}.jsonl`.

## Walk Evidence

The full evidence + Walk Evidence sections + Verdict are in the falsification document (`03.1-FALSIFICATION-6.md`). This UAT transcript holds the lighter-weight per-stream summary + the developer's verbatim verdict.

### Build-provenance gate (NEW for Walk #6) — PASSED FIRST ATTEMPT

`git rev-parse HEAD == 15e98b3dd6e5332c417674f83e930b884120ce16`. CI run `25291831174` `headSha` field returns the same value. **Equal — the structural gate succeeded on first attempt.** Walk #6's IPA was built from the EXACT commit on local HEAD, including Plan 03.1-13 commit `2b087a3` (FogTransformLogger metersPerPixel field augmentation) AND Plan 03.1-14 commit `0d65212` (Fix B′ meter-space FOG-17a decomposition + FOG-19). The build-provenance gap class of failure that Walk #5 surfaced is structurally eliminated. **The metersPerPixel JSONL field is now empirically captured** (51/51 rollups; envelope `[3.164, 26.660]`).

### Frame-delta probe rollups (PERF-07, Criterion A — `infrastructure.mirk.frame_delta` JSONL)

**51 1-Hz rollups captured across the ~53-sec session.** Envelope:

| Statistic     | min       | median-of-medians | max       | Threshold | Headroom         |
| ------------- | --------- | ----------------- | --------- | --------- | ---------------- |
| `medianMs`    | 0.037 ms  | 0.311 ms          | 1.780 ms  | ≤ 16 ms   | **9.0× minimum (51× at median)** |
| `p95Ms`       | 0.223 ms  | —                 | 6.017 ms  | ≤ 32 ms   | **5.3× worst-case** |
| `maxMs`       | 0.279 ms  | —                 | 9.233 ms  | ≤ 48 ms   | **5.2× worst-case** |

`sampleCount` per rollup ranges 97-121 (consistent with ProMotion 120 Hz cadence; the 97 outlier is the first-second rollup which started mid-second).

**PERF-07 GREEN with at least Walk #5 baseline retention** (Walk #5: med 1.808 / p95 6.105 / max 9.756; Walk #6: med 0.311 / p95 6.017 / max 9.233 — median IMPROVED 5.8× because the developer spent more of the session in idle/static moments; p95 + max statistically equivalent). Plan 03.1-14's added Dart-side cycles + shader-side formula-reorder did NOT regress the frame budget. **PERF-07 status flips to `Complete — Re-measured (P03.1-15 Walk #6)`.**

### SDF rebuild rollups (`infrastructure.mirk.sdf` JSONL — PERF-08 status)

42 1-Hz INFO SDF rollups captured. Comparable to Walk #5 cadence; **PERF-08 stays at `Falsified-by-walk-2`** — no Plan 03.1-14 work targeted PERF-08; status unchanged. Per-rebuild cost stays sub-13 ms (frame budget preserved per PERF-07 GREEN above). Out-of-scope for Plan 03.1-16 unless Walk #7+ surfaces it as a higher-tier concern.

### Diagnostic JSONL — Walk #6 fog_transform rollup (`infrastructure.mirk.fog_transform`)

51 1-Hz fog_transform rollups captured. Key signatures — **Plan 03.1-14 architectural signatures EMPIRICALLY VERIFIED in IPA:**

- **`coordinateSpace: 'meters'` field present in 51/51 rollups** (Plan 03.1-14 Task 2 augmentation EMPIRICALLY VERIFIED — direct visible signature).
- **`appliedUOffsetX/Y` bounded under `kPocFogIntegerWrapPeriodMeters + 1 = 4097 m` across ALL 51 rollups** (FOG-19 invariant — direct visible signature; uOffsetXMax envelope `[241.03, 4095.08]`, uOffsetYMax envelope `[431.10, 4094.34]`; vs Walk #5's pixel-space bounded composite under 1537 raw px — semantic flip succeeded).
- **`metersPerPixel` field present, non-zero, evolving smoothly with camera zoom in 51/51 rollups** (Plan 03.1-13 augmentation, NOW EMPIRICAL because build-provenance gate holds; envelope `[3.164, 26.660]` consistent with developer's z=10..15 walk regimes — at z=15 lat 48.5° expected ≈ 3.16, observed 3.164 ✓; at z=10 lat 48.49° expected ≈ 25.3, observed 26.66 ✓).
- **`canvasTx/Ty == 0.000000` across 51/51 rollups** (FOG-13 retains; Plan 03.1-08 mechanism mechanically zeroes the canvas-frame offset).
- **`pixelOriginX/Y` evolution monotonic with gestures** (envelope `[505126, 4256094]` × `[343678, 2897350]`; full zoom-traversal across z≈10..15 confirmed).

**KEY OBSERVATION — METER-SPACE WRAP EVENTS DETECTED IN WALK #6 JSONL.** Multiple per-second uOffsetX/Y envelopes span the full `[<small>, ~4090]` meter-space range, the EMPIRICAL signature of meter-space wrap events under Plan 03.1-14 Fix B′. Wrap-event seconds correlate temporally with dev-marker firings (see § Dev-marker JSONL below). This is the empirical anchor for the FALSIFICATION-6 architectural diagnosis: the meter-space FOG-19 invariant `bounded < 4097` HOLDS at every rollup, BUT wraps occur at the cadence the developer's gestures induce, AND the per-wrap residual constant-magnitude phase shift (Plan 03.1-14 analytical bound `kPocFogFbmDiscontinuityBound = 0.20` ≈ 11% fbm3 dynamic range) IS perceptually discernible.

### Dev-marker JSONL (`dev_marker`) — SEVEN MARKERS, ALL `steppy_translation`, EMPIRICALLY CORRELATE WITH METER-SPACE WRAP EVENTS

**Seven dev-markers fired during Walk #6** (3.5× Walk #5's 2 markers). All tagged `steppy_translation` matching the developer's verbatim *"I clic the bug button to mark when I notice a step when I waz zooming, then panning"*:

| Marker | Epoch (ms)      | Wall-clock UTC          | epochSec    | Δ to prev | Mechanism (per fog_transform correlation ±1s) |
| ------ | --------------- | ----------------------- | ----------- | --------- | --------------------------------------------- |
| #1     | 1777845812749   | 2026-05-04T00:03:32.749 | 1777845812  | —         | Marker fires at zoom-step lock (mpp transitioned 5.65 → 7.58 in previous second; pixelOrigin advanced 600 raw px during the zoom transition, frozen at marker time). |
| #2     | 1777845815741   | 2026-05-04T00:03:35.741 | 1777845815  | +2.992 s  | Same zoom-transition mechanism as M1; pixelOriginX/Y in transition `(1276015, 868434) → (1258708, 856651)`, mpp transitioning `10.55 → 10.70`. |
| #3     | 1777845818883   | 2026-05-04T00:03:38.883 | 1777845818  | +3.142 s  | **NEAR meter-space wrap event** — previous second uOffsetX swept `[15.34, 4087.86]` AND uOffsetY swept `[8.22, 4094.34]` (full-range multi-axis wrap); marker fires immediately after. |
| #4     | 1777845821625   | 2026-05-04T00:03:41.625 | 1777845821  | +2.742 s  | **AFTER Y-axis meter-space wrap event** — previous second uOffsetY swept `[7.42, 4084.53]`. |
| #5     | 1777845828776   | 2026-05-04T00:03:48.776 | 1777845828  | +7.151 s  | **DURING X-axis meter-space wrap event** — marker-second uOffsetX sweeps `[11.13, 4095.08]` (full meter-space range traversal in one second). |
| #6     | 1777845831451   | 2026-05-04T00:03:51.451 | 1777845831  | +2.675 s  | **AFTER multi-axis meter-space wrap event** — previous second uOffsetX swept `[19.23, 4091.14]` AND uOffsetY swept `[4.57, 4054.99]`. |
| #7     | 1777845833443   | 2026-05-04T00:03:53.443 | 1777845833  | +1.992 s  | **AFTER Y-axis meter-space wrap event** — previous second uOffsetY swept `[11.42, 4039.03]`. |

**ALL 7 markers correlate with meter-space wrap events** (within ±1-second window). Plan 03.1-14's prediction was *the constant-magnitude phase shift bounded ≈ 11% of fbm3 dynamic range INVARIANT across all wrap events makes the residual perceptually invisible*. Walk #6 EMPIRICALLY FALSIFIES this prediction: the developer perceives stepping at meter-space wrap events at multiple zoom × lat combinations (M1-M3 at z≈14-15 mpp ≈7.58-18, M4-M7 at z≈10 mpp ≈26.6). The constant-magnitude property landed correctly in software (the JSONL signature confirms uOffset bounded under 4097 m + invariance), but the perceptual threshold prediction (≈11% fbm3 dynamic-range residual is sub-perceptual) is wrong at the developer's specific worldMeters positions and zoom regimes.

This is precisely the failure mode Plan 03.1-14's `if_walk_6_surfaces_residual_stepping` contingency anticipates. **Plan 03.1-16 fix path: option D-3 Worley periodic noise primitive** (period exactly aligned with `kPocFogNoiseTilePxMeters = 1024 m`; ZERO per-wrap discontinuity vs Plan 03.1-14's constant-magnitude residual — replaces the gradient-noise FBM in atmospheric_fog.frag + atmospheric_fog_debug_spiral.frag).

### FPS observations (PERF-03)

Per the frame_delta envelope (sub-1 ms median, sub-7 ms p95, sub-10 ms max with sampleCount 97-121 per rollup), the device sustains ~120 fps during gestures. **PERF-03 implicitly passes** at Walks #1-2 + #5 baseline. The Q1 stepping + Q5 residual stepping are NOT frame-budget-related — they are noise-output-discontinuity architectural failures at the per-wrap fbm3 amplitude axis.

### Subjective verdict (Criterion B + Q1-Q5)

Developer's composite verdict across Walk #6 (verbatim quote):

> *"panning still worse than the perfect result we had further commits away (I think we wrote somewhere which commit it was), still steppy and now translating smoothly while panning instead of being attached to the map as it should*
>
> *zoom is less worse, way less steppyness but still some*
>
> *in the log I clic the bug button to mark when I notice a step when I waz zooming, then panning.*
>
> *logs : C:\claude_checkouts\mirk-poc-debug\20260503T220301Z_logs.txt\20260503T220301Z_logs.txt*
>
> *I didn't do the sanity pre walk, frankly I don't get what purpose it serves"*

Mapping to Criterion B sub-claims:

1. **Q1 — DENIED with NEW failure mode ✗:** *"panning still worse than the perfect result we had further commits away ..., still steppy and now translating smoothly while panning instead of being attached to the map as it should"*. Two qualitatively distinct components: (a) persistent stepping (carry-over from Walk #5; 7 dev-markers fired correlating with meter-space wrap events; the Plan 03.1-14 ≈11% fbm3 dynamic-range constant-magnitude residual IS perceptually discernible at this developer's worldMeters positions); (b) NEW failure mode where fog appears to *"translate smoothly with the viewport"* instead of being attached to the map — qualitatively different from Walk #5's *"steppy moments"* characterisation. See `03.1-FALSIFICATION-6.md` § Q1 NEW FAILURE MODE for the hypothesised mechanism (wrap-event-triggered perceptual gestalt: the smooth-translation between wraps + integer-cell-shift at wraps creates the *"detached from map"* impression).
   - **Reference to Walk #4 perfect baseline:** Walk #4 (Plan 03.1-11, commit chain `dbc3e21..b31766f`, ending at SHA `b31766f`) was the only Phase 3.1 walk to produce *"this works for panning : perfect"*. That state used FOG-17 + FOG-17a in pixel-space — wrap window 1536 raw px ≈ ~128 sec at 12 raw-px/s pan velocity, sub-perceptual because the wrap rarely fired during the walk. Walk #6 inherits the meter-space architectural completion (FOG-18 + FOG-19) but exposes the per-wrap fbm3 discontinuity that Walk #4's large-pixel-window happened to dodge.
2. **Q2 — PASSED ✓ implicit:** No rotation complaint surfaced; UX-02 retained from Walks #4-5.
3. **Q3 — PASSED ✓ implicit:** No reveal-hole complaint surfaced; FOG-12 retained from Walks #1-5.
4. **Q4 — PASSED architecturally; SUBSUMED into Q1 NEW failure mode:** The fog DOES translate with the camera per the JSONL evidence (pixelOrigin monotonic; uWorldMetersOrigin sawtooth bounded under 4097 m). However, the developer's Q1 framing re-casts the smooth-translation phases as "detached from map" because of the contrast with the wrap-stepping. Q4 architecturally retained.
5. **Q5 — PARTIALLY IMPROVED ✓~:** *"zoom is less worse, way less steppyness but still some"*. Plan 03.1-14 Fix B′ DELIVERS the architectural Q5 closure Walk #5 missed — `bounded_meters` is geographic, cells visibly enlarge with zoom, at fixed map position the noise sample is zoom-invariant within an integer-cell-shift tolerance. Walk #5's *"scramble"* perception substantially attenuated. Residual stepping at zoom (markers M1-M3) corresponds to the same per-wrap fbm3 discontinuity mechanism as Q1. Q5 closure waits on the same fix that closes Q1 — Plan 03.1-16 D-3 Worley.
6. **No white-ellipse during fast pinch-zoom** — implicitly retained (no complaint).
7. **No inversion at any zoom** — implicitly retained (no complaint).

Mechanism + fix path detailed in `03.1-FALSIFICATION-6.md` Q1 + Q5 sub-sections + Causal Analysis + Iteration Recommendation. **Selected Plan 03.1-16 path: option D-3 Worley periodic noise primitive** (Plan 03.1-14 contingency activates). NEW v1 requirement candidate **FOG-20** (noise primitive period exactly aligned with noise tile period; meta-requirement preventing recurrence of the per-wrap-discontinuity class of failure).

## Verdict

**Verdict:** **ITERATING — see Plan 03.1-16+.**

Specifically: `ITERATING-WITH-Q1-NEW-FAILURE-MODE-AND-Q5-PARTIAL`. The seventh Phase 3.1 verdict per CONTEXT §Iteration policy (no hard cap on walk count).

**Walked at:** 2026-05-04 (UTC; ~22:03 local CEST 2026-05-03).

**Developer's verbatim words (composite across Walk #6):**

1. *"panning still worse than the perfect result we had further commits away (I think we wrote somewhere which commit it was), still steppy and now translating smoothly while panning instead of being attached to the map as it should"* (Q1 ✗ DENIED with NEW failure mode)
2. *"zoom is less worse, way less steppyness but still some"* (Q5 ~ partially improved)
3. *"in the log I clic the bug button to mark when I notice a step when I waz zooming, then panning"* (7 dev-markers fired; correlates with meter-space wrap events in fog_transform JSONL)
4. *"I didn't do the sanity pre walk, frankly I don't get what purpose it serves"* (Pre-walk /sanity gate skipped; deviation documented as non-blocking)

(No verbatim observations on Q2/Q3/Q4 — implicitly retained from Walks #4-5 by absence of complaint.)

**Interpretation:**

The Plan 03.1-14 software fix bundle (Fix B′ meter-space FOG-17a decomposition + FOG-19; shader uniform slot 3..4 rename; period-commensurability invariant `kPocFogIntegerWrapPeriodMeters % kPocFogNoiseTilePxMeters == 0` GREEN; analytical bound `kPocFogFbmDiscontinuityBound = 0.20` ≈ 11% fbm3 dynamic-range residual constant-magnitude phase shift) successfully landed at every architectural level. **All four direct visible JSONL signatures hold across 51/51 rollups:** `coordinateSpace: 'meters'` field present, `metersPerPixel` field non-zero + smoothly-evolving, `appliedUOffsetX/Y` bounded under 4097 m, `canvasTx/Ty == 0`. The build-provenance gate held first attempt (HEAD == CI headSha == `15e98b3`); the structural Mail-share gate held second time. Plan 03.1-14's predicted constant-magnitude INVARIANCE across wraps is empirically verified (markers fire at uniform 2-3 sec cadence consistent with constant-magnitude wrap events; not the Walk #5 variable-magnitude clustering signature).

**What FAILED is the perceptual-threshold prediction.** Plan 03.1-14 hedged: *"the architectural property is INVARIANCE across all wrap events (NOT mathematical invisibility) ... Walk #6 PRIMARY GATE empirically validates the perceptual threshold — if developer fires `steppy_translation` markers despite Plan 03.1-14 Fix B′, the architectural fall-back path is option D-3"*. Walk #6 activates the contingency: 7 markers fired (3.5× Walk #5's 2 markers), all correlating with meter-space wrap events. The ≈11% fbm3 dynamic-range residual constant-magnitude phase shift IS perceptually discernible at the developer's worldMeters positions and zoom regimes encountered during Walk #6.

**Plan 03.1-16 path forward: option D-3 Worley periodic noise primitive** (Plan 03.1-14 PLAN.md `<continuity_proof_for_plan_03_1_14>` `if_walk_6_surfaces_residual_stepping` block). Replace the gradient-noise FBM in `assets/shaders/atmospheric_fog.frag` (lines 152-207) with a periodic Worley/explicit-period-Perlin function with period exactly `kPocFogNoiseTilePxMeters = 1024 m`. Wraps then inject ZERO discontinuity (the periodic primitive's period is exactly aligned with the wrap window's noise-tile-period multiple). NEW v1 requirement candidate **FOG-20** (noise primitive period exactly aligned with noise tile period; meta-requirement preventing recurrence of the per-wrap-discontinuity class of failure).

See `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-6.md` for the formal verdict + Walk Evidence Q1-Q5 sub-sections + Per-marker correlation analysis + Causal Analysis + Iteration Recommendation.

## Deviations / Surprises

1. **Walk #6 STRUCTURAL build-provenance gate succeeded on first attempt.** Walk #5 surfaced a build-provenance gap (Plan 03.1-13 commit `2b087a3` post-dated CI run `25285037397`); Plan 03.1-15 Task 1's STRUCTURAL gate (HEAD == CI headSha) prevented recurrence. The metersPerPixel JSONL signature (Plan 03.1-13 augmentation) is now empirically captured in 51/51 rollups; the coordinateSpace: 'meters' signature (Plan 03.1-14 augmentation) is empirically captured in 51/51 rollups. Pattern fully ratified.

2. **Walk #6 STRUCTURALLY-ENFORCED Mail-share gate held second time.** Walks #3 + #4 broke Mail-share twice; Walk #5 introduced the structural gate (resume signal requires log path) and held first time; Walk #6 held second time. The `WALK-MAIL-SHARE-DISCIPLINE` recurring blocker is FULLY RESOLVED — pattern now established across two consecutive walks.

3. **Plan-vs-reality artifact name mismatch (Rule 3 deviation).** Plan 03.1-15 Task 1 step 8 specified `gh run download <RUN_ID> --name ios-unsigned --dir .uat-tmp/`. Actual workflow artifact name in `.github/workflows/ci.yml` line 120 is `mirk-poc-debug-ios-unsigned-ipa`. Task 1 used the correct artifact name; documented as Rule 3 — Blocking deviation. Future plan templates should be aligned. Recommendation: Plan 03.1-16 templates reference the canonical artifact name from `.github/workflows/ci.yml`.

4. **Pre-walk /sanity gate SKIPPED by developer (deviation from plan, NOT BLOCKING).** Developer: *"I didn't do the sanity pre walk, frankly I don't get what purpose it serves"*. The /sanity gate's design intent is build-confusion detection (catches stale-IPA installs in SideStore) + shader-compile exception check + UX-01/UX-02/DEBUG-01 walk-time validation. The build-provenance gate at Task 1 makes the build-confusion intent partially redundant for the executor side. **Recommendation for Plan 03.1-16:** RETAIN the /sanity gate as STRUCTURALLY OPTIONAL with clearer rationale ("~30 second pre-walk smoke test confirming the IPA you just installed actually contains the post-Plan-X changes — saves a 5-minute walk if SideStore cached an older IPA"). Alternative: drop the /sanity gate from future walks and rely solely on the build-provenance gate + first fog_transform rollup's `coordinateSpace` + `metersPerPixel` signature check. The current Walk #6 verdict was authored cleanly without the /sanity gate, supporting the alternative.

5. **Q1 NEW failure mode characterisation diverges from Walk #5's framing.** Walk #5 reported *"panning is less good than before, I have some steppy moment"* (one-component complaint). Walk #6 reports *"still steppy and now translating smoothly while panning instead of being attached to the map"* (two-component complaint). The NEW component is the *"translating smoothly with viewport"* perceptual gestalt — qualitatively distinct from *"steppy moments"*. Hypothesised mechanism (HIGH confidence; cannot verify on-device without Plan 03.1-16 instrumentation): the wrap-event-triggered perceptual gestalt. Between wraps, `uWorldMetersOrigin` grows linearly with camera pan, and the noise pattern translates smoothly (correctly anchored to ground). At wrap events, `uWorldMetersOrigin` resets, producing a 4-integer-cell shift in noiseUv with ≈11% fbm3 dynamic-range constant-magnitude residual. The combined perception is *"smoothly translating then snapping"* — re-cast by the developer as *"translating smoothly with the viewport instead of being attached to the map"*. **This NEW perception is the same underlying architectural failure as Q1 stepping, just from a different angle.** Closing the per-wrap discontinuity (option D-3 Worley) closes both characterisations.

6. **Architectural-depth iteration pattern continues — final architectural axis surfaced.** Each successive layered fix bundle has corrected one architectural axis while exposing the next:
   - Walks #1-3: pan-axis (constant-zero → wrap shimmer → tile-period stepping → world-coordinate-noise + integer-wrap precision pairing)
   - Walk #4: zoom-axis surfaces (pixel-space anchor → meter-space anchor at FOG-18)
   - Walk #5: coordinate-space-alignment-of-decomposition surfaces (FOG-17a in pixel-space vs FOG-18 in meter-space → non-commensurate periods)
   - **Walk #6: noise-primitive-selection axis surfaces** (gradient-noise FBM has per-octave-matrix-amplification producing ≈11% fbm3 dynamic-range constant-magnitude residual at every wrap; periodic Worley/explicit-period-Perlin has zero residual)
   
   Plan 03.1-16 closes this final axis. After Plan 03.1-16 lands, Phase 3.1 has no remaining architectural axes (the renderer architecture is sound, the coordinate system is meter-space-aligned, the decomposition is meter-space-aligned, the noise primitive is periodic). Aesthetic equivalence (FBM vs Worley visual character) is the only remaining open question — likely resolved by RESEARCH.md `kMirkFog*` constant re-tuning at Plan 03.1-16 time.

## Summary

Walk #6 closes Phase 3.1's seventh iteration with verdict `ITERATING-WITH-Q1-NEW-FAILURE-MODE-AND-Q5-PARTIAL`. The Plan 03.1-14 software fix bundle (Fix B′ meter-space FOG-17a decomposition + FOG-19) successfully landed the architectural completion at every level — all 4 direct visible JSONL signatures verified across 51/51 rollups. Walk #5's period-commensurability gap is closed (4096 % 1024 = 0 = 4 cells per wrap). Walk #5's Q1 regression mechanism (variable-magnitude per-wrap shift) is replaced with a constant-magnitude per-wrap shift as predicted. **But Plan 03.1-14's perceptual-threshold prediction (≈11% fbm3 dynamic-range residual is sub-perceptual) is FALSIFIED empirically:** 7 dev-markers tagged `steppy_translation` correlate with meter-space wrap events in fog_transform JSONL within ±1 sec; markers cluster at multiple zoom × lat combinations including z≈10 mpp ≈26.6 and z≈14 mpp ≈18. Q5 PARTIALLY IMPROVED (Plan 03.1-14 delivers the architectural noise-anchor closure Walk #5 missed; cells enlarge with zoom; residual stepping is the same per-wrap fbm3 discontinuity mechanism as Q1). NEW Q1 failure mode characterised as *"translating smoothly while panning instead of being attached to the map"* — re-cast wrap-stepping perception, same underlying mechanism.

**STRUCTURAL build-provenance discipline pattern fully ratified** — Walk #6 Task 1's `git rev-parse HEAD == gh run view 25291831174 --json headSha` check held first attempt, eliminating the build-provenance gap class of failure that Walk #5 surfaced. **STRUCTURALLY-ENFORCED Mail-share discipline pattern fully ratified** — gate held second time after Walk #5 first time; recurring discipline-break pattern resolved structurally.

Selected Plan 03.1-16 fix path: **option D-3 Worley periodic noise primitive** (Plan 03.1-14 contingency activates). NEW v1 requirement candidate **FOG-20** (noise primitive period exactly aligned with noise tile period; meta-requirement preventing recurrence of the per-wrap-discontinuity class of failure). Phase 4 + Phase 5 stay BLOCKED. MirkFall migration recommendation **DEFERRED** until Walk #7 (post-Plan-03.1-16) verdict.
