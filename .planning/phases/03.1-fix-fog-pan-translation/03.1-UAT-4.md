---
phase: 03.1-fix-fog-pan-translation
plan: 11
type: uat
status: iterating
source: [03.1-10-SUMMARY.md]
started: 2026-05-03
walked: 2026-05-03
ci_run: "25281990915"
ci_sha: "b31766f"
device: iPhone 17 Pro (ProMotion 120 Hz, A19 Pro)
location: central Melun (initial GPS fix at 48.5397, 2.6553 area)
exit_gate: developer's resume signal — verbal `approved` / `denied:` / `iterating:`
verdict: ITERATING-WITH-Q1-Q4-CONFIRMED-Q5-OPEN
gaps: []
---

# Phase 3.1 UAT — Walk #4 (Post-Plan-03.1-10 World-Coordinate Noise + Rotation Disable Fix Bundle)

**Gate:** Walk #4 is the empirical answer to the layered Plan 03.1-10 (FOG-17 world-coordinate noise sampling + FOG-17a CPU-side integer/fractional decomposition + UX-02 disable rotation) software fix bundle. Walk #3 closed `ITERATING-WITH-PARTIAL-PROGRESS-AND-ROTATION-REGRESSION` 2026-05-03; Walk #4 either flips the verdict to `CONFIRMED-AFTER-FIX` (unblocking Phase 4 + Phase 5) or strengthens it to `DENIED-FINAL` (terminating the same-Canvas hypothesis) or — per CONTEXT §Iteration policy — captures a fifth `ITERATING` verdict that opens Plan 03.1-12+.

This UAT mirrors the shape of `.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-3.md` (Walk #3's transcript). The full evidence + Walk Evidence sections + Verdict are in the falsification document (`03.1-FALSIFICATION-4.md`). This UAT transcript holds the lighter-weight per-stream summary + the developer's verbatim verdict; the falsification doc is the load-bearing artefact.

## Build Under Test

| Field        | Value |
| ------------ | ----- |
| CI run       | 25281990915 |
| Run URL      | https://github.com/<repo>/actions/runs/25281990915 |
| HEAD SHA     | b31766f (docs(03.1-10): complete world-coordinate-noise + UX-02-disable-rotation plan) |
| iOS artifact | `.uat-tmp/mirk-poc-debug-unsigned.ipa` (11.58 MB) |
| Gates job    | success |
| Android job  | success |
| iOS job      | success |
| Tests        | 180 GREEN / 1 SKIPPED / 0 RED |

## Pre-walk Gates

- [x] `flutter test` full suite GREEN (180 tests + 1 skipped; existing + Plan 03.1-10 new tests + REWRITTEN FOG-14a/b sub-tests; FOG-09 vacuous-test guard remains intentionally SKIPPED)
- [x] `flutter analyze --fatal-infos` 0 warnings (verified at SHA b31766f)
- [x] `dart format --line-length 160 --set-exit-if-changed .` clean
- [x] `dart test tool/test/` GREEN (18/18)
- [x] CI run on the post-Plan-03.1-10 SHA on `main` GREEN (gates + build-android + build-ios; run 25281990915)
- [x] IPA artifact present at `.uat-tmp/mirk-poc-debug-unsigned.ipa` (11.58 MB)
- [x] `/sanity` smoke test on sideloaded IPA (implicitly passed — no developer complaint surfaced about /sanity rendering, back-button, debug-spiral toggle, or UX-02 rotation-disabled walk-time validation)

## Walk Protocol

- **Where:** Central Melun (initial GPS fix at 48.5397, 2.6553 area — same theatre as Walks #1 + #2 + #3).
- **Duration:** ≥ 5 minutes continuous walk with the IPA running and the device awake.
- **Gestures:** ≥ 10 deliberate combined pinch-zoom-and-pan gestures, ≥ 3 recenter FAB taps. AT LEAST ONE deliberate two-finger rotate-attempt gesture (validates UX-02 / Q2 sub-claim — should be a no-op). AT LEAST ONE sustained ≥ 60-second one-direction pan (validates Q1 across the integer-wrap-period regime).
- **HUD visible during walk:** `FpsCounterOverlay` + `MapCompass` (should stay at 0° throughout per UX-02) + `FrameDeltaProbeOverlay`.
- **Pre-walk shader-sanity gate:** `/sanity` route — confirm production fog renders with circular reveal hole (debug-spiral toggle OFF default); toggle ON/OFF round-trip on `/sanity` AppBar OR on `/map` AppBar global toggle (DEBUG-01 walk-time validation); confirm `Icons.arrow_back` leading button works on `/sanity` (UX-01 retained); back-pop returns to `/map`. **NEW for Walk #4:** attempt a two-finger rotate gesture on `/map` — confirm the map does NOT rotate (UX-02 walk-time validation).
- **NEW for Walk #4: dev-marker discipline.** The dev-marker button at the AppBar (commit `79ab978`) is operational. Developer is instructed to tap the marker every time they perceive ANY residual symptom (steppy translation, fog mis-coverage, anything visually anomalous). Marker fires JSONL events tagged with the marker timestamp at the per-frame level. Walk #3b marker-correlation analysis template (`03.1-WALK-3B-MARKER-ANALYSIS.md`) applies if markers fire.
- **Post-walk:** session log shared via Mail (LOG-04 round-trip — **REQUIRED for Walk #4**; Walk #3 broke this discipline with verbal-only evidence) → saved attachment, four JSONL streams extracted via grep into `.uat-tmp/walk4_{frame_delta,sdf,fog_transform,dev_markers}.jsonl`.

## Walk Evidence

The full evidence + Walk Evidence sections + Verdict are in the falsification document (`03.1-FALSIFICATION-4.md`). This UAT transcript holds the lighter-weight per-stream summary + the developer's verbatim verdict; the falsification doc is the load-bearing artefact.

### Pre-walk shader-sanity gate (`/sanity` route + UX-01 back-button validation + DEBUG-01 toggle live-test + UX-02 rotation-disabled walk-time validation)

Implicitly PASSED — no developer complaint surfaced about /sanity rendering, the back button, the debug-spiral toggle, or rotation behaviour at the pre-walk stage. The Plan 03.1-08 mid-checkpoint corrective bundle (commit `f707226`) keeps the debug-spiral toggle accessible from /map AppBar globally — used during Walk #4 (see Q5 sub-section in `03.1-FALSIFICATION-4.md`). UX-02 walk-time-validated implicitly: developer's verbatim *"rotation disabled confirmed"*.

### Frame-delta probe rollups (PERF-07, Criterion A — `infrastructure.mirk.frame_delta` JSONL)

**N/A — Walk #4 had no Mail-shared session log.** No frame-delta JSONL captured. PERF-07 implicitly retained at Walk #2 baseline (3.9-5.4× headroom); FrameDeltaProbeOverlay was visible at top:104,right:8 throughout the walk; no developer complaint about frame-budget pressure surfaced. Plan 03.1-10's added cycles per paint are negligible. PERF-07 stays at `Complete — Re-measured (P03.1-06 Walk #2)`.

### SDF rebuild rollups (`infrastructure.mirk.sdf` JSONL — PERF-08 status)

**N/A — Walk #4 had no Mail-shared session log.** PERF-08 stays at `falsified-by-walk-2` (1-121 rebuilds/sec; 1e-4 quantisation insufficient). No Plan 03.1-10 work targeted PERF-08; status unchanged. Future-plan candidate (out-of-scope for Plan 03.1-12 unless Q5 fix surfaces it as a higher-tier concern).

### Diagnostic JSONL — Walk #4 fog_transform rollup (`infrastructure.mirk.fog_transform`)

**N/A — Walk #4 had no Mail-shared session log.** The KEY FOG-17a signature (`appliedUOffsetXMin/Median/Max` bounded under 1537 raw px regardless of zoom) cannot be empirically verified from Walk #4 evidence. The Plan 03.1-10 unit test `fog_pixel_origin_decomposition_test.dart` 7 sub-tests assert the bounded-composite math correctness mechanically at synthetic pixelOrigin magnitudes 1.064M / 4.26M / 17.04M — i.e., the Dart-side decomposition is structurally guaranteed by the unit test. The Walk #4 verbal evidence (Q1 panning works perfectly) is consistent with the FOG-17a precision mitigation having landed correctly at the call site.

### Dev-marker JSONL (`dev_marker`)

**N/A — Walk #4 had no markers fired.** Developer closed Walk #4 early on debug-spiral observation grounds before entering the marker-tap-on-residual-symptom regime. The marker button (commit `79ab978`) was operational on the sideloaded build but not exercised because the visual diagnostic was sufficient to root-cause the Q5 zoom-scramble mechanism without quantitative marker correlation. Walk #5 (post-Plan-03.1-12 fix landing) will exercise the marker button for any Q5 residual after the world-meter anchor lands.

### FPS observations (PERF-03)

**N/A — Walk #4 had no Mail-shared session log + no developer-verbal FPS comment.** The FpsCounterOverlay was visible throughout (top:8, right:8); no developer complaint about frame-rate surfaced. Implicitly retained at Walks #1 + #2 baseline (~120 fps during gestures, ~4 fps idle on iPhone 17 Pro ProMotion).

### Subjective verdict (Criterion B + NEW Q5 sub-claim)

Developer's composite verdict across Walk #4 (verbatim quotes, chronological):

1. **Q1 — PASSED ✓:** *"this works for panning : perfect"* — FOG-17 + FOG-17a closes the stepping symptom-space across the integer-wrap window.
2. **Q2 — PASSED ✓:** *"rotation disabled confirmed"* — UX-02 walk-time-validates the FOG-16 path (a) closure.
3. **Q3 — PASSED ✓:** *"revealed area working"* — FOG-12 retained walk-time.
4. **Q4 — PASSED ✓ (implicit):** Subsumed by Q1's "perfect" — fog translates with camera correctly.
5. **Q5 NEW — FAILED:**
   - *"however zoom still scramble the mirk (rapid change of coordinate)"*
   - *"when zooming it at maximum power blue dot is on the 84 square, when zooming out at maximum blue dot is at 92, but I don't think it's the 92 close to the 84, I think the issue is that the mirk 'center' isn't the blue dot of when we open the map, instead it must be a point outside of the screen and when zooming the shader is enlarged/shrinked relative to that point."*
   - *"I tried zooming with the numbered shader, we can clearly see the number sliding (panning) fast, however I should see the number getting bigger, yet whatever the zoom level is, their size stay the same, so I don't know what is happening in fact."*
6. **No white-ellipse during fast pinch-zoom** — implicitly retained (no complaint).
7. **No inversion at any zoom** — implicitly retained (no complaint).

Mechanism + fix path detailed in `03.1-FALSIFICATION-4.md` Q5 sub-section + Sub-section D triage table. Selected Plan 03.1-12 path: **Fix B — world-meter anchor** (developer-endorsed); NEW v1 requirement **FOG-18**.

#### Mid-walk preliminary observation (in-progress — not the final verdict)

- **Q1 (stepping during pan):** developer's verbatim — _"this works for panning : perfect"_. FOG-17 world-coordinate noise sampling + FOG-17a CPU-side integer/fractional decomposition appear to have resolved the residual stepping symptom Walk #3 surfaced.
- **NEW failure mode surfaced — zoom-scramble (Q5, NOT a Walk #3 carry-over):** developer's verbatim — _"however zoom still scramble the mirk (rapid change of coordinate)"_. Under zoom-in/out gestures the noise pattern visibly scrambles. **Mechanism (preliminary, pending JSONL evidence):** Web-Mercator `camera.pixelOrigin` is zoom-dependent — incrementing zoom by 1 approximately doubles `pixelOrigin`. The Plan 03.1-10 FOG-17 formulation `worldPx = fragUv * uResolution + uPixelOrigin; noiseUv = worldPx / kNoiseTilePx` anchors the noise to PIXEL space, not geographic space. When zoom changes, `pixelOrigin` jumps by a delta that crosses many `kNoiseTilePx` periods at once → fragments sample completely different noise positions on consecutive frames → perceived as "scramble". The fix bundle was scoped to pan-stepping (Walk #3 Q1) and didn't account for zoom-step deltas. This is genuinely a new open question — **NOT** a regression of any prior fix and **NOT** a known Walk #3 failure mode. It needs an architecture-level decision: either anchor noise to a zoom-invariant coordinate (e.g., Web-Mercator world-meter or lat/lng-derived stable basis) OR snap zoom to discrete levels and accept a one-frame transition at each step OR scale the noise tile period inversely with the zoom-jump factor at the Dart call site.
- **Q2 (rotation-disabled):** developer's verbatim — _"rotation disabled confirmed"_. UX-02 holds walk-time. ✓
- **Q3 (reveal-hole-aligned):** developer's verbatim — _"revealed area working"_. FOG-12 retained walk-time. ✓
- **Q4 (fog-translates-with-camera):** implicit ✓ — "this works for panning" subsumes Q4 (if the noise pattern stayed pinned to the world-pixel origin during pan, the user would call it "smooth panning with no scrolling fog"; the term "perfect" carries the implicit Q4 confirmation).
- **Q5 zoom-scramble — refined mechanism (developer's debug-spiral observation):** developer's verbatim — _"when zooming it at maximum power blue dot is on the 84 square, when zooming out at maximum blue dot is at 92, but I don't think it's the 92 close to the 84, I think the issue is that the mirk 'center' isn't the blue dot of when we open the map, instead it must be a point outside of the screen and when zooming the shader is enlarged/shrinked relative to that point."_ This **confirms** the preliminary mechanism hypothesis with empirical detail: the noise pattern is anchored to `worldPx = (0,0)` (the Web-Mercator world origin at top-left of the global tile grid), not to the camera's world position. Under FOG-17 sampling `worldPx = fragUv * uResolution + uPixelOrigin`, every fragment's noise position scales with `uPixelOrigin`, which itself doubles per zoom level (Web-Mercator pixel space). The blue dot (camera center) is at `worldPx = uPixelOrigin + uResolution/2`. As zoom increases, `uPixelOrigin` doubles → the blue dot's `worldPx` doubles → fragments under the blue dot sample completely different noise grid cells per zoom level. Visually: the noise pattern appears to "enlarge/shrink relative to a fixed off-screen anchor (the world origin)" as zoom changes, exactly the user's intuition. **Architectural fix candidates** (post-walk decision pending):
    - **Fix A — anchor noise to camera position (NOT world origin):** redefine `worldPx = fragUv * uResolution + uOffset` where `uOffset = (-uResolution.x/2, -uResolution.y/2)` (so noise origin coincides with viewport center, i.e., the blue dot). Pros: trivial change; noise stays pinned to camera center across zoom. Cons: noise pattern stays the same shape regardless of camera world position — feels less "tied to the world" but more "tied to the viewport"; under PURE PAN the noise will scroll because uPixelOrigin enters via the `+ uOffset` term that's no longer zero (need to re-derive carefully).
    - **Fix B — anchor noise to fixed world-meter coordinates (zoom-invariant basis):** convert `pixelOrigin` to world-meter using Web-Mercator `metersPerPixel = 156543.03392 * cos(lat) / 2^zoom`, then sample noise in meter-space (`worldMeterX = pixelOriginX * metersPerPixel`). Pros: noise anchored to a real geographic point; doesn't change with zoom because meters per fragment scales identically. Cons: requires a uniform for `metersPerPixel` (currently 41 slots; budget impact); fragment-space noise frequency now scales with zoom (cells appear larger when zoomed in) — may be desired or not.
    - **Fix C — anchor noise to lat/lng directly:** sample noise at `worldUv = (lon - kPocInitialCameraLon, lat - kPocInitialCameraLat) / fixedDegreesPerCell`. Pros: the most "tied to the world" option; cells align with geographic features. Cons: cos(lat) scaling needs care for non-uniform cells near poles; per-fragment lat/lng computation is heavier than per-fragment world-meter computation; needs lat/lng decomposition Dart-side.
    - **Fix D — accept zoom-anchored scramble; freeze the camera's reveal pattern at the camera's current zoom by snapping zoom to discrete integer levels and re-anchoring noise on each zoom step:** simplest from a math perspective; UX feels "step-zoom" instead of smooth pinch zoom. Probably unacceptable for a hike-tracking POC.

#### Q5 zoom-scramble — second-order confirmation via debug-spiral cell-size observation

Developer's verbatim — _"I tried zooming with the numbered shader, we can clearly see the number sliding (panning) fast, however I should see the number getting bigger, yet whatever the zoom level is, their size stay the same, so I don't know what is happening in fact."_

**This pinpoints the root cause definitively.** Two distinct phenomena observed simultaneously:

1. **Numbers slide fast during zoom (the "scramble"):** `uPixelOrigin` doubles per zoom level (Web-Mercator pixel space). `worldPx = fragUv * uResolution + uPixelOrigin` shifts proportionally. Each fragment now samples a completely different region of the noise function. **Diagnosed.**

2. **Numbers stay the same size regardless of zoom level:** the noise tile period `kNoiseTilePx = 384.0` is a **shader-side const in raw-pixel units**. The cell size on screen is `kNoiseTilePx / dpr ≈ 128 logical px` REGARDLESS of zoom level. The cells are pinned in **pixel space**, not world-meter space. Zooming the camera does NOT enlarge the cells because the cells were never tied to ground.

**Mental model gap:** the user (correctly) expects fog-of-war to behave like ground features — at z=15 a "fog cell" should occupy ~1.2 km of ground (1210 m at lat 48.5°) and shrink visually when zooming out, while at z=10 the same cell would occupy ~39 km of ground (32× bigger ground area, same on-screen size). What the FOG-17 formulation actually produces: cells are 384 raw px on screen at every zoom level, but their MAPPING TO THE GROUND varies — at z=15 each cell maps to ~1.2 km, at z=10 each cell maps to ~39 km, but you're seeing a different FRAGMENT of the noise pattern at each zoom.

**Architectural decision crystallised:** Fix B (world-meter anchor) or Fix C (lat/lng anchor) is the only correct path. The "fog-of-war on a real map" feature is fundamentally a **geographic phenomenon** — the noise pattern represents memory of geographic territory, not memory of pixel positions. Pixel positions are zoom-dependent and have no physical meaning. The user's intuition ("when zooming in, cells should get bigger because I'm looking at a smaller region of the world") is the architecturally correct expected behaviour.

**Recommended Plan 03.1-12 path: Fix B (world-meter anchor).** Trade-off summary:
- Replace `kNoiseTilePx = 384.0` (raw px) with `kNoiseTilePxMeters = 1024.0` (meters; one cell ≈ 1 km of ground — sized for the hike scale).
- Add a uniform `uMetersPerPixel` (slot 41 → slot count goes 41 → 42; under shader budget cap).
- In `_FogPainter.paint()`: compute `metersPerPixel = 156543.03392 * cos(camera.center.latitude * π/180) / 2^camera.zoom` and forward via uniform.
- In shader: `worldMeters = (fragUv * uResolution + uPixelOrigin) * uMetersPerPixel; noiseUv = worldMeters / kNoiseTilePxMeters`. The `uPixelOrigin` term is kept (to anchor noise to camera's geographic position) but the rescaling-by-metersPerPixel cancels the zoom-doubling exactly. Result: noise position is zoom-invariant in meter-space; cells on screen scale with zoom naturally.
- FOG-17a integer/fractional decomposition stays — but now operates on meter-space coordinates, NOT pixel-space (because pixel-space goes back to its original wandering-with-zoom unbounded behaviour at the meter-anchor level). May require revisiting bounds.

## Verdict

**Verdict:** **ITERATING — see Plan 03.1-12+.**

Specifically: `ITERATING-WITH-Q1-Q4-CONFIRMED-Q5-ZOOM-SCRAMBLE-OPEN`. The fifth Phase 3.1 verdict per CONTEXT §Iteration policy (no hard cap on walk count).

**Walked at:** 2026-05-03.

**Developer's verbatim words (composite across Walk #4):**

1. *"this works for panning : perfect"* (Q1 ✓)
2. *"rotation disabled confirmed"* (Q2 ✓)
3. *"revealed area working"* (Q3 ✓)
4. (Q4 implicit ✓ via Q1's "perfect")
5. *"however zoom still scramble the mirk (rapid change of coordinate)"* (Q5 NEW ✗ surface symptom)
6. *"when zooming it at maximum power blue dot is on the 84 square, when zooming out at maximum blue dot is at 92, but I don't think it's the 92 close to the 84, I think the issue is that the mirk 'center' isn't the blue dot of when we open the map, instead it must be a point outside of the screen and when zooming the shader is enlarged/shrinked relative to that point."* (Q5 off-anchor diagnostic — pinpoints world-pixel-origin anchor)
7. *"I tried zooming with the numbered shader, we can clearly see the number sliding (panning) fast, however I should see the number getting bigger, yet whatever the zoom level is, their size stay the same, so I don't know what is happening in fact."* (Q5 cell-size definitive root cause — cells pinned in pixel-space rather than meter-space)

**Interpretation:**

The Plan 03.1-10 layered software fix bundle (UX-02 disable rotation + FOG-17 world-coordinate noise sampling + FOG-17a CPU-side integer/fractional decomposition) closed Walk #3's Q1 stepping symptom and Q2 rotation-correlated mis-coverage symptom. Both walk-time-validated at Walk #4. Q3 + Q4 retained from prior walks. **NEW Q5 zoom-scramble failure mode** surfaced at the architectural level: the FOG-17 formulation `worldPx = fragUv * uResolution + uPixelOrigin; noiseUv = worldPx / kNoiseTilePx` anchors noise to **Web-Mercator world-pixel space**. Two architectural consequences:

1. `uPixelOrigin` doubles per zoom level (Web-Mercator pixel space scales `256 * 2^zoom` px wide). Fragments under any given screen position sample completely different noise positions per zoom step → fast sliding.
2. `kNoiseTilePx = 384.0` is a const **raw-pixel** value. Cells are 384 raw px on screen at every zoom level (~128 logical px). Cells pinned in screen-space rather than scaling with the world.

The developer's observations chronologically pin both phenomena: (a) the surface symptom — *"zoom still scramble"*; (b) the off-anchor diagnostic — *"the mirk 'center' isn't the blue dot ... must be a point outside of the screen"* (correctly identifies the world-origin anchor); (c) the cell-size definitive root cause — *"their size stay the same"* (pixel-space cells, not meter-space).

**Plan 03.1-12 path forward: Fix B (world-meter anchor — DEVELOPER-ENDORSED).** Replace pixel-space anchor with meter-space anchor: `worldMeters = (fragUv * uResolution + uPixelOrigin) * uMetersPerPixel; noiseUv = worldMeters / kNoiseTilePxMeters`. Add uniform `uMetersPerPixel` (slot 41 → 42; under shader budget). Dart-side: `metersPerPixel = 156543.03392 * cos(camera.center.latitude * π/180) / pow(2, camera.zoom)`. NEW v1 requirement: **FOG-18** (noise pattern anchored to ground meters, not pixel space).

See `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-4.md` for the formal verdict + Walk Evidence sub-sections + Hypothesis Triage Table for Plan 03.1-12.

## Deviations / Surprises

1. **Walk #4 ended early — no marker-on-residual-symptom regime exercised.** The plan-body Walk #4 procedure included explicit dev-marker-button taps every time the developer perceived ANY residual symptom. Developer closed Walk #4 based on debug-spiral cell-size and cell-sliding observations BEFORE entering the marker regime — the visual diagnostic was sufficient to root-cause the Q5 zoom-scramble mechanism without quantitative marker correlation. No marker JSONL exists. Walk #5 will exercise the marker button after the world-meter anchor lands.

2. **Walk #4 had no Mail-shared session log** (same limitation as Walk #3, despite Plan 03.1-11's pre-walk gate explicitly requiring Mail-share discipline). Verbal + debug-spiral observation evidence only. Quantitative JSONL grep correlation unavailable for frame_delta / SDF / fog_transform / dev_marker streams. Mail-share discipline must be made structurally enforceable in Plan 03.1-12+ pre-walk sequence (e.g., a developer-affirmed checkbox before sideload begins).

3. **Bbox-removal debug-aid commit `230db00` landed mid-walk but never empirically validated.** Replaced `CameraConstraint.contain(...)` → `CameraConstraint.unconstrained()` in `MapScreen` to enable extreme-pan validation; the test `map_screen_test.dart` was rewritten to assert `UnconstrainedCamera` runtimeType. Developer closed Walk #4 early based on debug-spiral observations before reaching the extreme-pan regime that the bbox-removal was scoped for; the unconstrained CameraConstraint landed regardless of the abandoned validation and is now the new default for Plan 03.1-12 walks. Carried forward as a documented Walk #4 deviation, NOT a fix.

4. **Q5 zoom-scramble was NEW failure mode not anticipated by the plan-body Criterion B.** The original Walk #4 falsification skeleton (written before the walk) only enumerated Q1–Q4 (carry-overs from Walk #3). Q5 was discovered during the walk at the architectural noise-anchor level. Capture as Sub-section D triage table in `03.1-FALSIFICATION-4.md` and as a fifth sub-claim in this UAT. NEW v1 requirement **FOG-18** added.

5. **Mail-share discipline broken twice in a row** (Walks #3 + #4). Captured as a recurring blocker `WALK-MAIL-SHARE-DISCIPLINE` in STATE.md. The Mail-share gate must be made structurally enforceable in Plan 03.1-12+ pre-walk sequence.

## Summary

Walk #4 closes Phase 3.1's fifth iteration with verdict `ITERATING-WITH-Q1-Q4-CONFIRMED-Q5-ZOOM-SCRAMBLE-OPEN`. The Plan 03.1-10 layered software fix bundle (UX-02 + FOG-17 + FOG-17a) verifiably closed all of Walk #1+2+3's pan-axis failure modes (Q1 stepping, Q2 rotation-correlated mis-coverage, Q3 reveal-hole alignment, Q4 fog-translates-with-camera) — all walk-time-validated. The walk surfaced a NEW Q5 zoom-scramble failure mode at a deeper architectural axis: the FOG-17 noise formulation anchors to Web-Mercator world-pixel space, which doubles per zoom level (rapid noise sliding during zoom) and uses raw-pixel cell sizes (cells stay screen-pinned, don't scale with the world). Developer's debug-spiral observation pinpointed the root cause definitively. Selected Plan 03.1-12 fix path: Fix B — world-meter anchor. NEW v1 requirement: **FOG-18**. Phase 4 + Phase 5 stay BLOCKED. MirkFall migration recommendation: DEFERRED until Walk #5 (post-Plan-03.1-12) verdict.
