---
phase: 03.1-fix-fog-pan-translation
plan: 06
type: execute
wave: 3
depends_on: ["03.1-05"]
files_modified:
  - .planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-2.md
  - .planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-2.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
gap_closure: true
requirements: [FOG-04, FOG-05, FOG-06, FOG-07, FOG-11, FOG-12, PERF-07, PERF-08, UX-01]
user_setup:
  - service: github-actions
    why: "Wait for CI green on the post-Plan-03.1-05 SHA + download the IPA artifact for sideload"
    dashboard_config:
      - task: "Confirm CI run status (use `gh run list --branch main --limit 5` + `gh run watch` until green)"
        location: "GitHub Actions for the mirk-poc-debug repo"
      - task: "Download IPA artifact via `gh run download <RUN_ID> --name ios-unsigned --dir .uat-tmp/`"
        location: "GitHub Actions run detail page (or `gh` CLI)"
  - service: sidestore
    why: "Sideload the unsigned IPA onto iPhone 17 Pro for the walk"
    dashboard_config:
      - task: "Open SideStore on iPhone 17 Pro → install/refresh the .uat-tmp/mirk-poc-debug-unsigned.ipa"
        location: "SideStore iOS app + paired desktop pairing-file"
must_haves:
  truths:
    - "A new `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-2.md` exists capturing the second-walk verdict. The existing `03.1-FALSIFICATION.md` (the iterating-with-major-progress verdict from Plan 03.1-03) is LEFT UNTOUCHED — historical record preserved as Walk #1."
    - "A new `.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-2.md` exists capturing the walk #2 transcript. The existing `03.1-UAT.md` (Walk #1 transcript) is LEFT UNTOUCHED."
    - "The pre-walk gate sequence runs cleanly: `flutter test` GREEN (all of Phase 1+2+3+3.1 tests including the new FOG-11 + FOG-12 + PERF-08 + UX-01 tests); `flutter analyze --fatal-infos` 0 warnings; `dart format --line-length 160 --set-exit-if-changed .` clean; `dart test tool/test/` GREEN; CI green on the post-Plan-03.1-05 SHA on `main`; IPA downloaded via `gh run download` to `.uat-tmp/mirk-poc-debug-unsigned.ipa`."
    - "The iPhone 17 Pro UAT walk #2 is performed by the user. Walk shape matches Plan 03.1-03 + Plan 03-08 patterns: ≥ 5 minutes through Melun + ≥ 10 deliberate combined pinch-zoom-and-pan gestures + ≥ 3 recenter taps + post-walk Mail-share of the session log."
    - "Post-walk: the developer's verbal verdict (`approved` / `denied:` / `iterating:`) is captured. The walk evidence section of `03.1-FALSIFICATION-2.md` is filled with the developer's verbatim words + interpretation + post-walk grep of `infrastructure.mirk.fog_transform` rollups + correlation against `infrastructure.mirk.frame_delta` + `infrastructure.mirk.sdf` rollups on per-second `epochSecond` boundaries."
    - "PERF-07 measurement is re-recorded for Walk #2: frame-delta probe rolling rollup over the ≥ 10-combined-gesture window with `medianMs ≤ 16`, `p95Ms ≤ 32`, `maxMs ≤ 48`. The Plan 03.1-03 walk produced these numbers with 4-7× headroom; this walk re-validates them after the Plan 03.1-04 + 03.1-05 changes."
    - "Verdict written: `CONFIRMED-AFTER-FIX` if BOTH (a) developer's subjective verdict is `approved` AND (b) the PERF-07 frame-delta thresholds are satisfied AND (c) the diagnostic JSONL streams confirm uOffset (now spanning 0..4e6 raw pixels) tracks the gesture monotonically AND `canvasTx/Ty` jumps no longer correlate with reveal-hole offset (FOG-12 test in CI gates this mechanically). `DENIED-FINAL` if the developer's verdict describes a failure mode that the diagnostic JSONL stream cannot resolve via a follow-up 3.1 iteration. `ITERATING` if a residual failure mode surfaces that has a clear next-iteration fix axis."
    - "On `CONFIRMED-AFTER-FIX`: ROADMAP.md Phase 03.1 row in the Progress table flips to `Complete (HYPOTHESIS CONFIRMED-AFTER-FIX)`. Phase 4 (`Wisp Particles`) status updates from `Blocked on Phase 3.1` to `Pending`. Phase 5 (`Decision Gate`) likewise. STATE.md `Current Position` updates to reflect Phase 3.1 closure. REQUIREMENTS.md FOG-04..07 status flips from `Falsified-in-production` to `Complete — Verified-by-walk (P03.1-06)`. PERF-07 status updates to `Complete — Re-measured (P03.1-06)` with the actual numbers. FOG-11, FOG-12, PERF-08, UX-01 flip to `Complete — Verified-by-test` (their CI-gating tests are GREEN by construction once Plan 03.1-04 + 03.1-05 land)."
    - "On `DENIED-FINAL`: ROADMAP.md Phase 03.1 row updates to `Complete (HYPOTHESIS DENIED-FINAL)`. Phases 4 + 5 stay `Blocked` (or transition to `Cancelled` per developer choice). REQUIREMENTS.md FOG-04..07 stay `Falsified-in-production` with an updated revision-clause that points at `03.1-FALSIFICATION-2.md`. STATE.md captures the formal POC architectural counter-evidence."
    - "On `ITERATING`: a Plan 03.1-07+ is opened in a follow-up plan-phase invocation, with the diagnostic stream from THIS walk as input. THIS plan does NOT author 03.1-07."
    - "Phase 3 `03-FALSIFICATION.md` AND Phase 3.1 `03.1-FALSIFICATION.md` are LEFT UNTOUCHED — no edits, no overwrites. The historical records stand; Walk #2's verdict is a SEPARATE artefact at `03.1-FALSIFICATION-2.md`."
  artifacts:
    - path: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-2.md"
      provides: "Phase 3.1 Walk #2 falsification document — pre-walk gate-pass evidence + post-walk verdict (`CONFIRMED-AFTER-FIX` | `DENIED-FINAL` | `ITERATING`)"
      min_lines: 80
      contains: "Walk #2"
    - path: ".planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-2.md"
      provides: "Walk #2 evidence transcript — captures the developer's verbatim verdict + interpretation + JSONL grep correlation"
      min_lines: 30
      contains: "Walk Evidence"
  key_links:
    - from: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-2.md"
      to: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION.md"
      via: "explicit cross-reference in Walk #2's Hypothesis section + Verdict section ('Walk #1 closed iterating-with-major-progress 2026-05-02; Walk #2 either confirms or denies the layered Plan 03.1-04 + 03.1-05 fix')"
      pattern: "03\\.1-FALSIFICATION\\.md"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-2.md"
      via: "FOG-04..07 + FOG-11 + FOG-12 + PERF-07/08 + UX-01 status clauses include `(P03.1-06)` cross-reference + the Revisions section appends a 2026-05-XX entry"
      pattern: "P03\\.1-06"
    - from: ".planning/STATE.md"
      to: ".planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-2.md"
      via: "Current Position + Decisions sections capture the Walk #2 verdict outcome"
      pattern: "Phase 3\\.1"
---

<objective>
Run the pre-walk gates, perform the iPhone 17 Pro sideload UAT walk #2, capture the developer's verdict, and produce `03.1-FALSIFICATION-2.md` — the Phase 3.1 Walk #2 falsification document. The walk #2 verdict either reverses the Plan 03.1-03 `ITERATING-WITH-MAJOR-PROGRESS` outcome to `CONFIRMED-AFTER-FIX` (unblocking Phase 4 + Phase 5 of the original roadmap) or strengthens it to `DENIED-FINAL` (terminating the POC's same-Canvas hypothesis) or — per CONTEXT §Iteration policy — captures a third `ITERATING` verdict that opens Plan 03.1-07.

Purpose: this is the empirical gate Phase 3.1 needs to close. Plan 03.1-04 (SHADER-MODULO-WRAP) + Plan 03.1-05 (CANVAS-FRAME-ALIGNMENT + PERF-08 + UX-01) are software-complete + CI-gated by the new FOG-11 + FOG-12 + PERF-08 + UX-01 tests. Walk #2 is the architectural-evidence gate that flips FOG-04..07 to Complete (or strengthens the verdict). Per CONTEXT §Iteration policy, there is no hard cap on walk count.

Output: 1 new `03.1-FALSIFICATION-2.md` (the load-bearing artefact); 1 new `03.1-UAT-2.md` (the walk-evidence transcript); REQUIREMENTS.md status updates for FOG-04..07 + FOG-11 + FOG-12 + PERF-07/08 + UX-01; ROADMAP.md Phase 03.1 Progress-table row update; STATE.md Current Position + Decisions updates. Phase 3's `03-FALSIFICATION.md` AND Phase 3.1 Walk #1's `03.1-FALSIFICATION.md` BOTH left UNTOUCHED.
</objective>

<execution_context>
@C:/Users/oliver/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oliver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-CONTEXT.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-RESEARCH.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-VALIDATION.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-04-SUMMARY.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-05-SUMMARY.md
@.planning/phases/03.1-fix-fog-pan-translation/03.1-03-PLAN.md
@.planning/phases/03-fog-of-war-the-hypothesis/03-FALSIFICATION.md

<interfaces>
<!-- The two reference artefacts the new docs mirror. -->
<!-- Plan 03.1-03 set the Walk #1 template; this plan follows it shape-for-shape with adjusted hypothesis re-statements. -->

`03.1-FALSIFICATION.md` Walk #1 structure (the immediate predecessor):
1. Title + metadata (`Created`, `Phase`, `Walked on`, `Verdict`)
2. `## Relationship to Phase 3 Verdict`
3. `## Hypothesis`
4. `## Falsification Criteria (written BEFORE the walk)` — `### Criterion A`, `### Criterion B`, `### Criterion C — DROPPED`, `### NEW: Phase 3.1 diagnostic stream`
5. `## Walk Plan`
6. `## Walk Evidence (filled AFTER the walk)`
7. `## Verdict`

Walk #2's `03.1-FALSIFICATION-2.md` mirrors this exactly with these specialisations:
- Title: `# Phase 3.1 Falsification — Walk #2 (Post-Plan-03.1-04 + Plan-03.1-05 Layered Fix)`
- Add a new `## Relationship to Walk #1 Verdict` section pointing at `03.1-FALSIFICATION.md` (the Walk #1 record); summarise the iterating-with-major-progress outcome and the four gap entries authored.
- Hypothesis re-statement: "After the Plan 03.1-04 SHADER-MODULO-WRAP fix (uniform renamed `uOffset` → `uPixelOrigin`; modulo applied per-fragment via `fract(uPixelOrigin / uResolution)`) AND the Plan 03.1-05 CANVAS-FRAME-ALIGNMENT fix (`_FogPainter.paint()` reads `canvas.getTransform()` and pre-shifts the clip-path holes by `-(matrix[12], matrix[13])`), the same-Canvas fog now translates with the camera during pan AND maintains a stable noise pattern AND keeps the reveal hole co-located with the blue dot."
- Criterion A is the same thresholds (PERF-07 carry-over): `medianMs ≤ 16`, `p95Ms ≤ 32`, `maxMs ≤ 48`. Walk #1 produced 4-7× headroom; Walk #2 re-validates after the layered fix.
- Criterion B sub-claims:
  - "Fog visually translates with the camera during pan" (Walk #1: intermittent — observation 3)
  - "No 'seed changing' noise-seam shimmer during gesture" (Walk #1 observation 2 — fixed by Plan 03.1-04)
  - "No reveal-hole offset from blue dot during gesture" (Walk #1 observation 4 — fixed by Plan 03.1-05)
  - "No white-ellipse during fast pinch-zoom" (already passed Walk #1)
  - "No inversion at any zoom level" (already passed Walk #1)
- Criterion C stays DROPPED.
- The diagnostic stream Criterion D (NEW for Walk #2): the `infrastructure.mirk.fog_transform` JSONL rollup keys `uOffsetXMin/Median/Max` are now in raw-pixel units (0..4e6) instead of normalised UV (0..1). The expected pattern: `pixelOriginX/Y` track the gesture monotonically (Walk #1 confirmed); `uOffsetX/Y` (raw-pixel-magnitude in this walk) likewise track monotonically WITHOUT the multi-wrap-per-second sweeping that Walk #1's Finding 3 documented; `canvasTx/Ty` may still jump non-zero (Plan 03.1-05 does NOT eliminate that — it COMPENSATES for it via clip-path shift), but the FOG-12 test in CI confirms the compensation works mechanically without a sideload walk.

`03.1-UAT.md` Walk #1 structure (the immediate predecessor): metadata block + walk protocol + walk evidence (frame-delta + SDF + fog_transform rollups) + subjective verdict + verdict + deviations + summary + gaps.

Walk #2's `03.1-UAT-2.md` mirrors this; the new section is `### Diagnostic JSONL — Walk #2 fog_transform rollup` capturing the per-second canvasTransform / pixelOrigin / uOffset evidence stream after the Plan 03.1-04 + Plan 03.1-05 layered fix. The post-walk grep correlation pattern is identical to Walk #1's:
- `grep "infrastructure.mirk.frame_delta" <log-file> > .uat-tmp/walk2_frame_delta.jsonl`
- `grep "infrastructure.mirk.sdf" <log-file> > .uat-tmp/walk2_sdf.jsonl`
- `grep "infrastructure.mirk.fog_transform" <log-file> > .uat-tmp/walk2_fog_transform.jsonl`

The pre-walk gate sequence is identical to Plan 03.1-03 Task 1 (six gates: flutter test / analyze / format / tool tests / CI green / IPA download).

The walk shape is identical to Plan 03.1-03: ≥ 5 minutes Melun + ≥ 10 combined gestures + ≥ 3 recenter taps + LOG-04 Mail share.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the pre-walk `03.1-FALSIFICATION-2.md` skeleton + run the pre-walk gate sequence</name>
  <files>.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-2.md, .planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-2.md</files>
  <action>
    Create `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-2.md`. Mirror the structural shape of `03.1-FALSIFICATION.md` (Walk #1) with the specialisations described in the §Interfaces block above. Use the executor's actual current date for the `Created:` field; leave `Walked on:` and `Verdict:` as `TBD` until Task 2 fills them.

    Top-of-file metadata block:
    ```markdown
    # Phase 3.1 Falsification — Walk #2 (Post-Plan-03.1-04 + Plan-03.1-05 Layered Fix)

    **Created:** 2026-05-XX  (executor: fill in real date)
    **Phase:** 03.1-fix-fog-pan-translation
    **Walked on:** TBD  (filled post-walk: iPhone 17 Pro, central Melun, CI run `<RUN_ID>`, SHA `<SHA>`)
    **Verdict:** TBD  (filled post-walk: `CONFIRMED-AFTER-FIX` | `DENIED-FINAL` | `ITERATING — see 03.1-07+`)

    ## Relationship to Walk #1 Verdict

    Walk #1 (Plan 03.1-03, 2026-05-02) closed `ITERATING-WITH-MAJOR-PROGRESS`. The Plan 03.1-02 fix (uOffset derivation from camera.pixelOrigin) was structurally addressed but the post-fix walk surfaced three new failure modes resolved into four gap entries:

    1. **SHADER-MODULO-WRAP** (observations 2 + 3) — closed by Plan 03.1-04 (uniform renamed `uOffset` → `uPixelOrigin`; modulo applied per-fragment inside the GPU via `fract(uPixelOrigin / uResolution)`). FOG-11 behavioural smooth-noise-coordinate-evolution test added.
    2. **CANVAS-FRAME-ALIGNMENT** (observation 4) — closed by Plan 03.1-05 (`_FogPainter.paint()` reads `canvas.getTransform()` and forwards `Offset(matrix[12], matrix[13])` to `computeFogClipPath(canvasOffset: ...)` so the clip-path holes are pre-shifted to compensate for the `MobileLayerTransformer`'s Canvas-level translation). FOG-12 behavioural test added.
    3. **SDF-CACHE-VIEWPORT-THRASH** (residual hypothesis from SDF Anomaly) — closed by Plan 03.1-05 (`SdfCache._hash(...)` quantises viewport bbox edges to 1e-4 lat/lon). PERF-08 unit test added.
    4. **SANITY-NO-BACK-BUTTON** (observation 5) — closed by Plan 03.1-05 (AppBar leading IconButton + `/sanity` reached via `context.push`). UX-01 widget test added.

    Walk #1's `03.1-FALSIFICATION.md` AND `03.1-UAT.md` stand as the historical record — UNTOUCHED. Walk #2's verdict is a SEPARATE artefact in this file.

    ## Hypothesis

    After the Plan 03.1-04 SHADER-MODULO-WRAP fix (uniform renamed `uOffset` → `uPixelOrigin`; modulo applied per-fragment via `fract(uPixelOrigin / uResolution)`) AND the Plan 03.1-05 CANVAS-FRAME-ALIGNMENT fix (`_FogPainter.paint()` reads `canvas.getTransform()` and pre-shifts the clip-path holes by `-(matrix[12], matrix[13])`), the same-Canvas fog now (a) translates with the camera during pan, (b) maintains a stable noise pattern across consecutive paints, and (c) keeps the reveal hole co-located with the blue dot during pan/zoom/combined gestures.

    Re-stated against the original POC core value: the fog-of-war stays perfectly locked to the map during pan, zoom, and combined gestures on a sideloaded iOS build. **CONFIRMED-AFTER-FIX** means the Phase 3 hypothesis is reinstated and the POC ports back to MirkFall with all of Plan 03.1-02 + 03.1-04 + 03.1-05 fixes included; **DENIED-FINAL** means even the layered fix doesn't deliver fog-camera-tracking parity and the POC produces the formal architectural counter-evidence; **ITERATING** means a residual failure mode surfaced that has a clear next-iteration fix axis (Plan 03.1-07+).

    ## Falsification Criteria (written BEFORE the walk)

    ### Criterion A — Frame-delta probe thresholds (PERF-07 re-validation, quantitative)

    Same thresholds as Walk #1: `medianMs ≤ 16`, `p95Ms ≤ 32`, `maxMs ≤ 48` across ≥ 10 deliberate combined gestures. Walk #1 captured these with 4-7× headroom; Walk #2 re-validates the headroom is preserved after the Plan 03.1-04 + 03.1-05 changes (the new `canvas.getTransform()` per-paint allocation could in principle cause a regression — RESEARCH §Pitfall B — but the magnitude is single-Float64List per paint, expected far below the budget). Persisted via `Logger('infrastructure.mirk.frame_delta')` 1-Hz JSONL rollups.

    ### Criterion B — Subjective visual lock (PERF-05 carry-over, re-tested)

    Developer's verbal verdict at end of walk:

    - **Fog visually translates with the camera during pan** (Walk #1 observation 3 was intermittent — Plan 03.1-04 + 03.1-05 should make it consistent)
    - **No 'seed changing' noise-seam shimmer during gesture** (Walk #1 observation 2 — Plan 03.1-04 SHADER-MODULO-WRAP fix targets this)
    - **No reveal-hole offset from blue dot during gesture** (Walk #1 observation 4 — Plan 03.1-05 CANVAS-FRAME-ALIGNMENT fix targets this)
    - No white-ellipse during fast pinch-zoom (already passed Walk #1)
    - No inversion (fog appearing where reveal should be) at any zoom level (already passed Walk #1)

    ### Criterion C — DROPPED

    Stays dropped per Phase 3 + roadmap lock.

    ### Criterion D (NEW for Walk #2) — Diagnostic stream confirms the layered fix

    Per the `Logger('infrastructure.mirk.fog_transform')` 1-Hz JSONL rollups:

    - **CONFIRMED-AFTER-FIX:** `pixelOriginX/Y` track the gesture monotonically (continuing Walk #1's Finding 2). The `uOffsetXMin/Median/Max` keys (now in raw-pixel units 0..4e6 — see Plan 03.1-04 SUMMARY for the unit-of-measure shift) likewise track monotonically WITHOUT the multi-wrap-per-second sweeping that Walk #1's Finding 3 captured. `canvasTxMin/Median/Max` may still jump non-zero — Plan 03.1-05 does NOT eliminate the `MobileLayerTransformer` Canvas translation; it COMPENSATES for it. The FOG-12 CI gate validates the compensation mechanically; the walk's role here is corroborating subjective alignment.
    - **DENIED-FINAL pattern #1:** `pixelOriginX/Y` track AND `uOffsetX/Y` are smooth AND fog still appears static during pan — root cause is downstream of both the shader uniform AND the clip-path shift; the architecture is unfixable in `flutter_map 7.0.2`.
    - **ITERATING pattern:** subjective verdict surfaces a residual failure mode (e.g., shimmer reduced but not eliminated; reveal-hole offset reduced but not eliminated; new failure mode like "fog stutters at zoom step" that wasn't in Walk #1). The diagnostic stream points at the next fix axis; Plan 03.1-07 is opened.

    ## Walk Plan

    Identical to Plan 03.1-03 (Walk #1) — same theatre, duration, gestures, HUD, post-walk Mail-share. Only the post-fix SHA + CI run number differ.

    - **Where:** Central Melun (lat 48.5397, lon 2.6553 area).
    - **Duration:** ≥ 5 minutes continuous walk with the IPA running and the device awake.
    - **Gestures:** ≥ 10 deliberate combined pinch-zoom-and-pan gestures, ≥ 3 recenter FAB taps.
    - **Pre-walk gate (unit tests):** `flutter test` GREEN (FOG-09, FOG-10, FOG-11, FOG-12, PERF-08, UX-01, plus all Phase 1+2+3 retained); `flutter analyze --fatal-infos` 0 warnings; `dart format --line-length 160 --set-exit-if-changed .` clean; `dart test tool/test/` GREEN.
    - **Pre-walk gate (CI):** `gh run list --branch main --limit 5` shows `success` for the post-Plan-03.1-05 SHA; `gh run view <RUN_ID>` confirms `gates`, `build-android`, `build-ios` all green; IPA downloaded via `gh run download <RUN_ID> --name ios-unsigned --dir .uat-tmp/`.
    - **Pre-walk gate (sideload):** open `/sanity` route on the sideloaded build first; confirm the fog renders with a circular reveal hole; confirm the new `Icons.arrow_back` AppBar leading button works (UX-01 walk-time validation); back-pop returns to `/map`. Confirm zero shader-compile exceptions in the FileLogger output.
    - **In-app HUD during walk:** FpsCounterOverlay (top:8, right:8) + MapCompass (top:56, right:8) + FrameDeltaProbeOverlay (top:104, right:8) — three lines colour-coded green/yellow/red against Criterion A.
    - **Post-walk:** share the session log file via Mail (LOG-04 round-trip); paste the relevant frame-delta + SDF + fog-transform JSONL lines into the Walk Evidence section below; write the subjective verdict (Criterion B) by hand; verbal `approved` is the gate (or `denied:` / `iterating:` for the failure / next-iteration paths).

    ## Walk Evidence (filled AFTER the walk by Plan 03.1-06 Task 2)

    ### Pre-walk shader-sanity gate (`/sanity` route + UX-01 back-button validation)
    _TBD — filled post-walk._

    ### Probe rollup (frame-delta JSONL — `Logger('infrastructure.mirk.frame_delta')`)
    _TBD — filled post-walk. Expected: median ≤ 16 ms, p95 ≤ 32 ms, max ≤ 48 ms across ≥ 10 combined gestures (PERF-07 re-validation)._

    ### SDF rebuild rollup (`Logger('infrastructure.mirk.sdf')` — PERF-08 validation)
    _TBD — filled post-walk. Expected: rebuilds correlated with new GPS fixes only — NOT per-paint thrash. Walk #1 captured 12-115 rebuilds/sec during gesture seconds; Walk #2 should show single-digit rebuilds/sec or zero idle._

    ### Fog-transform rollup (`Logger('infrastructure.mirk.fog_transform')`)
    _TBD — filled post-walk. Expected: `pixelOriginX/Y` monotonic per Walk #1 Finding 2; `uOffsetX/Y` (now raw-pixel units per Plan 03.1-04) likewise monotonic WITHOUT the multi-wrap-per-second sweeping of Walk #1 Finding 3; `canvasTx/Ty` may still jump non-zero but the FOG-12 CI gate validates the painter's compensation._

    ### FPS observations (PERF-03 — coarse FpsCounterOverlay reading)
    _TBD — filled post-walk. Coarse-grained reading off the FpsCounterOverlay (gate: pan-FPS ≥ 30 with fog active; idle-fog-animation FPS ≥ 50)._

    ### Subjective verdict (Criterion B)
    _TBD — filled post-walk. Developer's verbatim words + interpretation paragraph mapping each Criterion B sub-claim to PASS/FAIL._

    ## Verdict

    - [ ] Criterion A passed? — TBD post-walk.
    - [ ] Criterion B passed? — TBD post-walk.
    - [ ] Criterion D passed? — TBD post-walk.

    **Outcome:** TBD.

    **MirkFall migration recommendation:** TBD.
    ```

    Then create `.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-2.md` as the thin Walk #2 transcript skeleton — mirror `03.1-UAT.md`'s structure with metadata block + Walk Protocol + empty `## Walk Evidence` section + `## Verdict` placeholder + `## Deviations / Surprises` placeholder + `## Summary` placeholder.

    NOW execute the pre-walk gate sequence. Run each command and capture the exit status:
    1. `flutter test` (must report 0 failures, 0 reds — Phase 1+2+3 retained tests + new FOG-09, FOG-10, FOG-11, FOG-12, PERF-08, UX-01 GREEN; the FOG-09 vacuous-test guard stays SKIPPED)
    2. `flutter analyze --fatal-infos` (must exit 0)
    3. `dart format --line-length 160 --set-exit-if-changed .` (must exit 0)
    4. `dart test tool/test/` (must report all GREEN)
    5. `gh run list --branch main --limit 5` (capture the post-Plan-03.1-05 SHA + run ID; the run must be `success`)
    6. `gh run view <RUN_ID>` (confirm `gates`, `build-android`, `build-ios` all `success`)
    7. `gh run download <RUN_ID> --name ios-unsigned --dir .uat-tmp/` (produces `.uat-tmp/mirk-poc-debug-unsigned.ipa`)

    If ANY pre-walk gate fails, STOP and report. Do NOT proceed to Task 2 until every gate is green. Re-running after a fix-and-push is acceptable.
  </action>
  <verify>
    <automated>flutter test 2>&amp;1 | tail -3 &amp;&amp; flutter analyze --fatal-infos 2>&amp;1 | tail -3 &amp;&amp; dart format --line-length 160 --set-exit-if-changed . 2>&amp;1 | tail -3 &amp;&amp; dart test tool/test/ 2>&amp;1 | tail -3 &amp;&amp; gh run list --branch main --limit 1 2>&amp;1 | tail -3</automated>
    <file_must_exist>.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-2.md</file_must_exist>
    <file_must_exist>.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-2.md</file_must_exist>
    <file_must_exist>.uat-tmp/mirk-poc-debug-unsigned.ipa</file_must_exist>
  </verify>
  <done>
    - All 4 local pre-walk gates exit 0 (test / analyze / format / tool-tests).
    - CI run on the post-Plan-03.1-05 SHA on `main` is `success` (gates + build-android + build-ios all green).
    - `.uat-tmp/mirk-poc-debug-unsigned.ipa` exists and is non-empty (≥ 5 MB).
    - `03.1-FALSIFICATION-2.md` exists with all six top-level sections + the Relationship-to-Walk-1 section. Walk Evidence + Verdict bodies read `TBD`.
    - `03.1-UAT-2.md` exists as a thin transcript skeleton.
    - `git status` shows the existing `03.1-FALSIFICATION.md` and `03.1-UAT.md` UNCHANGED (Walk #1 records preserved).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: iPhone 17 Pro sideload UAT walk #2 + post-walk verbal verdict capture</name>
  <files>.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-2.md, .planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-2.md</files>
  <action>
    Hand off to the developer for the manual UAT walk #2 on iPhone 17 Pro. The full procedure is documented in the checkpoint-specific `<what-built>`, `<how-to-verify>`, and `<resume-signal>` sections — those are the executor-facing instructions. Claude pauses execution, presents the checkpoint to the developer, and waits for the resume signal containing the walk evidence + verdict. After the resume signal is received, Claude proceeds to Task 3 (verdict authoring + REQUIREMENTS/ROADMAP/STATE updates).
  </action>
  <verify>
    <automated>MISSING — PERF-07 + Criterion B + Criterion D (subjective + diagnostic-stream pattern matching) are inherently manual UAT walk gates per 03.1-VALIDATION.md Manual-Only Verifications table; no automated proxy exists. The developer&#39;s resume-signal IS the verification.</automated>
  </verify>
  <done>
    Developer returns with one of: (a) verbal &#96;approved&#96; — fog tracks camera during pan + no shimmer + no reveal-hole offset; CONFIRMED-AFTER-FIX. (b) &#96;denied: &lt;verbatim description&gt;&#96; — describe the failure mode for the diagnostic JSONL correlation step. (c) &#96;iterating: &lt;verbatim description&gt;&#96; — same as denied: but the developer chooses to iterate (Plan 03.1-07+ follow-up). Plus the post-walk session log shared via Mail (LOG-04 round-trip) so the executor can grep for the three rollup streams.
  </done>
  <what-built>
    The Plan 03.1-04 + 03.1-05 layered fix is software-complete:

    - **Plan 03.1-04 (SHADER-MODULO-WRAP):** atmospheric_fog.frag uniform `uOffset` renamed `uPixelOrigin`; the modulo wrap moved from CPU (`(pixelOrigin / size) % 1.0` at the Dart call site) to GPU (`fract(uPixelOrigin / uResolution)` per-fragment). Eliminates the per-paint single-frame discontinuities that produced the "seed of the mirk was changing" failure mode in Walk #1 (observation 2).
    - **Plan 03.1-05 (CANVAS-FRAME-ALIGNMENT):** `_FogPainter.paint()` reads `canvas.getTransform()` once per paint, derives `canvasOffset = Offset(matrix[12], matrix[13])`, forwards to `computeFogClipPath(canvasOffset: ...)`. The clip-path holes are pre-shifted by `-canvasOffset` so they stay co-located with the sibling blue-dot CircleLayer's UNTRANSLATED frame. Compensates for `MobileLayerTransformer`'s Canvas-level translation that surfaced in Walk #1 observation 4.
    - **Plan 03.1-05 (PERF-08):** `SdfCache._hash(...)` quantises viewport bbox edges to 1e-4 lat/lon, dropping per-paint cache thrash from 12-115/sec to ~1/sec.
    - **Plan 03.1-05 (UX-01):** `/sanity` route AppBar gains an `Icons.arrow_back` leading IconButton; the science-icon AppBar action uses `context.push` (was `context.go`) so back-pop returns to `/map`. No more force-close to return.

    CI gates green for new tests:
    - FOG-11 (`fog_smooth_noise_test.dart`) — pixelOrigin smoothness
    - FOG-12 (`fog_canvas_frame_alignment_test.dart`) — clip-path bounds shift by -canvasOffset
    - PERF-08 (augmented `sdf_cache_test.dart`) — sub-quantisation viewport drift = ONE rebuild
    - UX-01 (`shader_sanity_screen_test.dart`) — back-button pops to previous route

    Existing tests still GREEN: FOG-04 structural, FOG-07 KEYSTONE, FOG-09 transform-equality, FOG-10 fog_transform_logger.

    Pre-walk gates (Task 1): all 6 GREEN. The IPA at `.uat-tmp/mirk-poc-debug-unsigned.ipa` is ready for sideload.
  </what-built>
  <how-to-verify>
    1. Open SideStore on iPhone 17 Pro. Refresh certificate if needed. Install or refresh `.uat-tmp/mirk-poc-debug-unsigned.ipa`.
    2. Launch `mirk-poc-debug` on iPhone 17 Pro. Allow GPS via the rationale screen → "Allow While Using App".
    3. **Pre-walk sanity gate (also UX-01 walk-time validation):** tap the science (`Icons.science`) button in the AppBar to navigate to `/sanity`.
       - Confirm the fog renders with a circular reveal hole.
       - Confirm an `Icons.arrow_back` button appears in the leading slot of the sanity screen's AppBar (UX-01 fix).
       - Tap the back button. Confirm it returns to `/map` without force-closing the app.
    4. **Walk the route:** ≥ 5 minutes continuous through central Melun (same theatre as Walk #1). During the walk:
       - Perform ≥ 10 deliberate combined pinch-zoom-and-pan gestures.
       - Perform ≥ 3 recenter FAB taps.
       - Watch the FrameDeltaProbeOverlay (top:104, right:8) — note approximate `med` / `p95` / `max` values during combined gestures.
       - **The KEY observations** (the three Walk #1 failure modes the layered fix targets):
         - Does the fog visually translate with the map during pan? (Walk #1 obs 3 — intermittent)
         - Does the noise pattern stay STABLE during gesture, or does it shimmer / "seed change"? (Walk #1 obs 2)
         - Is the reveal hole co-located with the blue dot during pan/zoom? (Walk #1 obs 4)
    5. **Post-walk:** tap the share button in the AppBar → pick Mail → send the session log to your own email → save the attached `yyyymmdd_hhmmss_logs.txt` from your inbox.
    6. **Capture the verdict:** describe in your own words whether each of the three Walk #1 failure modes is now resolved. Single-word `approved` is the gate for `CONFIRMED-AFTER-FIX`. Any residual failure mode (partial shimmer, partial reveal-hole offset, new failure mode) should be described — that description feeds the diagnostic JSONL grep step.
    7. **Post-walk grep correlation** (executor runs after the verbal verdict is captured):
       - `grep "infrastructure.mirk.frame_delta" <log-file> > .uat-tmp/walk2_frame_delta.jsonl`
       - `grep "infrastructure.mirk.sdf" <log-file> > .uat-tmp/walk2_sdf.jsonl`
       - `grep "infrastructure.mirk.fog_transform" <log-file> > .uat-tmp/walk2_fog_transform.jsonl`
       - For each line, parse the JSON, extract `epochSecond`, produce a per-second composite row. Pipe into `03.1-FALSIFICATION-2.md`'s Walk Evidence subsections.

    **Expected `CONFIRMED-AFTER-FIX` evidence pattern:**
    - Walk evidence: developer says `approved` — fog visually tracks camera; no shimmer; reveal hole co-located with blue dot.
    - `frame_delta` rollup: `medianMs ≤ 16` / `p95Ms ≤ 32` / `maxMs ≤ 48` (PERF-07 re-validated; should match Walk #1's headroom).
    - `fog_transform` rollup: `pixelOriginX/Y` track gesture monotonically; `uOffsetX/Y` (raw-pixel units) likewise track monotonically WITHOUT multi-wrap-per-second sweeping (Walk #1 Finding 3 NOT reproduced); `canvasTx/Ty` may still jump non-zero but the FOG-12 CI gate validates the compensation.
    - `sdf` rollup: `rebuildCount` per gesture-second drops from Walk #1's 12-115 down to single digits or zero (PERF-08 validation in production).

    **DENIED-FINAL or ITERATING patterns** are documented in `03.1-FALSIFICATION-2.md` §Criteria — pattern matching uses the diagnostic stream to identify the residual fix axis (or, in the DENIED case, the architectural-counter-evidence finding).
  </how-to-verify>
  <resume-signal>
    Type ONE of:
    - `approved` — fog tracks camera during pan + no shimmer + no reveal-hole offset; CONFIRMED-AFTER-FIX. Executor proceeds to Task 3 (Branch A) and flips FOG-04..07 + PERF-07/08 + UX-01 + FOG-11/12 to Complete.
    - `denied: <verbatim description of the failure mode>` — describe what went wrong. Executor opens the post-walk JSONL grep results, correlates against the failure description, and reports back: either (a) recommend a Plan 03.1-07+ follow-up fix attempt with a specific diagnostic-anchored hypothesis, or (b) recommend `DENIED-FINAL` if the diagnostic evidence shows the failure is structural (Phase 3.2 alternative-renderer pivot conversation).
    - `iterating: <verbatim description>` — same as `denied:` shape, but the developer chooses to iterate per CONTEXT §Iteration policy. Executor opens a Plan 03.1-07 in a follow-up plan-phase invocation.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Author the Walk #2 verdict — fill `03.1-FALSIFICATION-2.md` Walk Evidence + Verdict; update REQUIREMENTS.md / ROADMAP.md / STATE.md</name>
  <files>.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-2.md, .planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-2.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
    Branch on the developer's verdict from Task 2:

    **Branch A — `approved` → CONFIRMED-AFTER-FIX:**

    1. Fill `03.1-FALSIFICATION-2.md` Walk Evidence subsections with the post-walk grep correlation from Task 2. Mirror the structure used in `03.1-FALSIFICATION.md` Walk #1 §Walk Evidence:
       - `### Pre-walk shader-sanity gate (+ UX-01 walk-time validation)`: `PASSED. Developer confirmed fog rendered at /sanity with circular reveal hole; UX-01 back button returned to /map without force-close.`
       - `### Probe rollup (frame-delta JSONL)`: paste per-second composite extract for the ≥ 10-combined-gesture window. Compute rolling rollup (median-of-medians, max-of-p95s, max-of-maxes). Compare against Walk #1's numbers (4-7× headroom expected to persist).
       - `### SDF rebuild rollup`: paste per-second extract. Walk #1 captured 12-115 rebuilds/sec; Walk #2 should drop to single digits (PERF-08 validation in production).
       - `### Fog-transform rollup`: paste per-second extract. Confirm `pixelOriginX/Y` monotonic, `uOffsetX/Y` (raw-pixel units) monotonic without multi-wrap sweeping, `canvasTx/Ty` jumps tolerated by FOG-12 compensation.
       - `### FPS observations`: coarse FpsCounterOverlay reading.
       - `### Subjective verdict`: developer verbatim + interpretation mapping each Criterion B sub-claim to PASS.

    2. Fill `03.1-FALSIFICATION-2.md` Verdict section:
       ```markdown
       ## Verdict

       - [x] Criterion A passed? — **YES.** Frame-delta probe rolling rollup over ≥ 10 combined gestures: median = {N} ms (≤ 16 ✓), p95 = {N} ms (≤ 32 ✓), max = {N} ms (≤ 48 ✓). PERF-07 re-validated post-Plan-03.1-04+05 layered fix.
       - [x] Criterion B passed? — **YES.** Developer's verbatim verdict: *"<verbatim>"*. All three Walk #1 failure modes resolved: fog translates with camera during pan ✓; no 'seed changing' shimmer ✓; reveal hole co-located with blue dot ✓.
       - [x] Criterion D passed? — **YES.** Diagnostic stream confirms `pixelOriginX/Y` + `uOffsetX/Y` (raw-pixel) monotonic without multi-wrap sweeping; `canvasTx/Ty` jumps tolerated by Plan 03.1-05's clip-path-shift compensation as validated by the FOG-12 CI gate.

       **Outcome:** **CONFIRMED-AFTER-FIX.**

       The same-Canvas fog hypothesis is **reinstated** with the layered Plan 03.1-02 + 03.1-04 + 03.1-05 fix bundle. Phase 3's `DENIED 2026-05-01` verdict is reversed; Phase 3.1 Walk #1's `ITERATING-WITH-MAJOR-PROGRESS` verdict is converted to `CONFIRMED-AFTER-FIX` after the second walk. Three architectural lessons baked into CI by FOG-09 + FOG-11 + FOG-12 keystones — any future regression to the static-fog / modulo-wrap / canvas-frame-misalignment failure modes trips a CI gate without a sideload walk.

       **MirkFall migration recommendation:** **PORT BACK** with the full layered fix bundle:
       1. `_FogPainter.paint()` derives `pixelOrigin` from `camera.pixelOrigin` (Plan 03.1-02).
       2. `atmospheric_fog.frag` slot 3..4 uniform `uPixelOrigin` (renamed from `uOffset`) with per-fragment `fract()` modulo (Plan 03.1-04).
       3. `_FogPainter.paint()` reads `canvas.getTransform()` and forwards `Offset(matrix[12], matrix[13])` to `computeFogClipPath(canvasOffset: ...)` for clip-path shift compensation (Plan 03.1-05).
       4. `SdfCache._hash(...)` quantises viewport bbox to 1e-4 lat/lon (Plan 03.1-05; performance-only — port-back-priority is medium).
       The MirkFall donor `_FogPainter.paint()` call site MUST be reviewed against ALL three production-impacting changes at port-back time.
       ```

    3. Update `.planning/REQUIREMENTS.md`:
       - FOG-04, FOG-05, FOG-06, FOG-07: change `[ ]` → `[x]`. Update the inline status from `Falsified-in-production (P03-08 walk DENIED 2026-05-01)` to `Complete — Verified-by-walk (P03.1-06 CONFIRMED-AFTER-FIX 2026-05-XX after layered Plan 03.1-02+04+05 fix)`. Append a brief status-clause referencing both Walk #1 (iterating) and Walk #2 (confirmed) for traceability.
       - FOG-11: change `[ ]` → `[x]`; status `Complete — Verified-by-test (P03.1-04 — fog_smooth_noise_test.dart)`.
       - FOG-12: change `[ ]` → `[x]`; status `Complete — Verified-by-test (P03.1-05 — fog_canvas_frame_alignment_test.dart)`.
       - PERF-07: status `Complete — Re-measured (P03.1-06)`. Include actual Walk #2 numbers.
       - PERF-08: change `[ ]` → `[x]`; status `Complete — Verified-by-test + walk (P03.1-05 unit + P03.1-06 walk; rebuild rate dropped from 12-115/sec to {N}/sec)`.
       - UX-01: change `[ ]` → `[x]`; status `Complete — Verified-by-test + walk (P03.1-05 widget + P03.1-06 walk-time confirmation)`.
       - PERF-03 / PERF-04 / PERF-05: update statuses to reflect re-measurement at Walk #2 (mirror the Walk #1 pattern from Plan 03.1-03).
       - Append a new entry to `## Revisions`:
         ```
         - **2026-05-XX (Phase 3.1 closure, Plan 03.1-06 SUMMARY — HYPOTHESIS CONFIRMED-AFTER-FIX):** Plan 03.1-06 sideload UAT walk #2 on iPhone 17 Pro against CI run `<RUN_ID>` (SHA `<SHA>`) over central Melun delivered a **CONFIRMED-AFTER-FIX** verdict after the layered Plan 03.1-02 + 03.1-04 + 03.1-05 fix bundle. Developer's verbatim words: *"<verbatim>"*. All three Walk #1 failure modes resolved: fog visually translates with camera during pan; no 'seed changing' noise-seam shimmer (Plan 03.1-04 SHADER-MODULO-WRAP fix); reveal hole co-located with blue dot (Plan 03.1-05 CANVAS-FRAME-ALIGNMENT fix). FOG-04, FOG-05, FOG-06, FOG-07 flip from Falsified-in-production back to Complete — Verified-by-walk. PERF-07 re-measured. PERF-08 verified in production (SDF rebuild rate dropped from 12-115/sec to {N}/sec). UX-01 walk-time validated (back button works on the sideloaded build). MirkFall migration recommendation: **PORT BACK** with the full layered fix bundle (3 architectural changes + 1 perf change documented in `03.1-FALSIFICATION-2.md`). Phase 4 (wisp particles) and Phase 5 (decision gate) **UNBLOCKED**. The new FOG-09 + FOG-11 + FOG-12 + PERF-08 + UX-01 CI gates prevent regression to ANY of the Walk #1 failure modes without a sideload walk. Evidence: `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-2.md` + `03.1-UAT-2.md`. Phase 3.1 closes with all 7 requirement IDs Complete (FOG-09, FOG-10, FOG-11, FOG-12, PERF-07, PERF-08, UX-01) plus the 4 retroactively-Complete (FOG-04..07).
         ```

    4. Update `.planning/ROADMAP.md`:
       - In `## Progress` table: Phase 03.1 row → `Complete (HYPOTHESIS CONFIRMED-AFTER-FIX)`, plans `6/6`, completed `2026-05-XX`.
       - In Phase 4 + Phase 5 rows: change status from `Blocked on Phase 3.1` to `Pending`.
       - In Phase 3 overview line: append `Phase 3.1 reversed the verdict to CONFIRMED-AFTER-FIX 2026-05-XX after a layered Plan 03.1-02+04+05 fix bundle (uniform rename + per-fragment modulo + clip-path shift compensation).`
       - In Phase 4 + Phase 5 overview lines: remove the `**BLOCKED on Phase 3.1**` clause.

    5. Update `.planning/STATE.md`:
       - Frontmatter: `stopped_at: Phase 3.1 CLOSED with HYPOTHESIS CONFIRMED-AFTER-FIX (Walk #2 2026-05-XX); Phase 4 + 5 UNBLOCKED.`; `last_activity` accordingly.
       - Bump `progress.completed_phases` from 3 to 4.
       - `## Current Position`: update to reflect Phase 3.1 closure + Phase 4 unblock.
       - `### Decisions`: append a Phase 3.1 closure entry (mirror the Plan 03.1-03 entry shape).
       - `### Blockers/Concerns`: change the `PHASE-3.1-CAMERA-TRANSFORM (PARTIALLY-RESOLVED ...)` entry to `PHASE-3.1-CAMERA-TRANSFORM (RESOLVED 2026-05-XX by Plan 03.1-06 Walk #2 CONFIRMED-AFTER-FIX after layered fix bundle):` and strike-through the original wording. Change `SANITY-SCREEN-NO-BACK-BUTTON (NEW ...)` to `SANITY-SCREEN-NO-BACK-BUTTON (RESOLVED 2026-05-XX by Plan 03.1-05 UX-01 fix; walk-time validated 2026-05-XX by Plan 03.1-06):`.

    6. **DO NOT TOUCH** `.planning/phases/03-fog-of-war-the-hypothesis/03-FALSIFICATION.md` (Phase 3 historical record) OR `.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION.md` (Walk #1 historical record). Verify via `git status` that both files are unchanged.

    **Branch B — `denied:` → DENIED-FINAL:**

    Mirror Plan 03.1-03's Branch B treatment. Walk Evidence filled with the failure-mode evidence + diagnostic-stream pattern matching. Verdict section captures DENIED-FINAL outcome with MirkFall recommendation `DO NOT PORT BACK` and Phase 3.2 conversation hand-off. REQUIREMENTS.md FOG-04..07 stay `Falsified-in-production` with updated revision-clause appending Walk #2 evidence. ROADMAP.md Phase 03.1 row → `Complete (HYPOTHESIS DENIED-FINAL)`. Phases 4 + 5 stay `Blocked` (or transition to `Cancelled` per developer choice — flag this in the SUMMARY for the user to decide). FOG-11, FOG-12, PERF-08, UX-01 stay GREEN at the test level (the layered fix DID land — the architectural failure was at a level NEITHER Plan 03.1-04 NOR Plan 03.1-05 reached) and flip to `Complete — Verified-by-test (P03.1-04..05)` independently of the walk verdict.

    **Branch C — `iterating:` → defer Plan 03.1-07 to follow-up:**

    Walk Evidence filled with the post-walk grep correlation. Verdict section: `**Outcome:** ITERATING — see Plan 03.1-07+ for the next fix attempt. Diagnostic stream from Walk #2: <description>.`. Do NOT update REQUIREMENTS.md / ROADMAP.md / STATE.md to closure state. Mark Phase 3.1 as `In Progress (iterating, walk #3 pending)` in the Progress table. FOG-11, FOG-12, PERF-08, UX-01 still flip to Complete — Verified-by-test (their CI gates ARE GREEN). FOG-04..07 stay Falsified-in-production. PERF-07 updated with Walk #2 numbers regardless. Plan 03.1-07 is created via a follow-up `/gsd:plan-phase 3.1 --gaps` invocation.

    For all three branches: commit all updates via the standard plan-execution commit pattern. Cross-reference Plan 03.1-06 in commit messages. Do NOT commit `.uat-tmp/` (gitignored) or the post-walk `.jsonl` extracts (transient walk artefacts).
  </action>
  <verify>
    <automated>flutter test 2>&amp;1 | tail -3 &amp;&amp; flutter analyze --fatal-infos 2>&amp;1 | tail -3 &amp;&amp; node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" frontmatter validate .planning/phases/03.1-fix-fog-pan-translation/03.1-06-PLAN.md --schema plan</automated>
    <file_must_exist>.planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION-2.md</file_must_exist>
    <file_must_exist>.planning/phases/03.1-fix-fog-pan-translation/03.1-UAT-2.md</file_must_exist>
  </verify>
  <done>
    - `03.1-FALSIFICATION-2.md` Walk Evidence + Verdict sections are filled (no `TBD` strings remaining for the chosen branch — Branches A or B; Branch C explicitly leaves them flagged for Plan 03.1-07).
    - `03.1-UAT-2.md` carries the Walk #2 transcript.
    - REQUIREMENTS.md FOG-04..07 + FOG-11 + FOG-12 + PERF-03/04/05/07/08 + UX-01 status fields reflect the verdict outcome (Branch A: all Complete; Branch B: FOG-04..07 Falsified-final + new IDs Complete; Branch C: in-progress for FOG-04..07 + new IDs Complete).
    - ROADMAP.md Progress table Phase 03.1 row reflects the verdict; Phase 4 + Phase 5 statuses updated per branch.
    - STATE.md Current Position + Decisions + Blockers/Concerns updated.
    - `git status .planning/phases/03-fog-of-war-the-hypothesis/03-FALSIFICATION.md` shows the file is UNCHANGED (Phase 3 historical record).
    - `git status .planning/phases/03.1-fix-fog-pan-translation/03.1-FALSIFICATION.md` shows the file is UNCHANGED (Walk #1 historical record).
    - `flutter test` (full suite) is GREEN — the closure docs do not touch any production code.
    - `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" frontmatter validate .planning/phases/03.1-fix-fog-pan-translation/03.1-06-PLAN.md --schema plan` returns `{ valid: true }`.
  </done>
</task>

</tasks>

<verification>
After all three tasks complete:
- `03.1-FALSIFICATION-2.md` is filled top-to-bottom with the Walk #2 verdict and walk evidence (or marked `iterating` per Branch C).
- The Phase 3 historical record (`03-FALSIFICATION.md`) AND the Walk #1 historical record (`03.1-FALSIFICATION.md`) are BOTH BYTE-IDENTICAL to their pre-Plan-03.1-06 states.
- REQUIREMENTS.md, ROADMAP.md, STATE.md, all reflect the Walk #2 verdict outcome consistently.
- `flutter test` (full suite) is GREEN — Plan 03.1-06 closure docs do not touch any production code.
- `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" frontmatter validate .planning/phases/03.1-fix-fog-pan-translation/03.1-06-PLAN.md --schema plan` returns `{ valid: true }`.
</verification>

<success_criteria>
- The iPhone 17 Pro Walk #2 produces a verdict (verbal `approved` / `denied:` / `iterating:`) — the empirical answer to the layered Plan 03.1-04 + 03.1-05 fix bundle.
- `03.1-FALSIFICATION-2.md` is the load-bearing artefact capturing the Walk #2 verdict + the diagnostic-JSONL evidence stream.
- On `CONFIRMED-AFTER-FIX`: Phase 4 + Phase 5 unblock; the MirkFall migration recommendation flips to PORT BACK with the full layered fix bundle (3 architectural + 1 perf change); the formal POC closure conversation can proceed at Phase 5; FOG-04..07 + FOG-11/12 + PERF-07/08 + UX-01 all flip to Complete.
- On `DENIED-FINAL`: the formal POC architectural counter-evidence is complete; the Phase 3.2 alternative-renderer conversation is the only remaining option. New tests stay Complete (the fix DID land in software; the architectural failure was at a level neither plan reached).
- On `ITERATING`: Plan 03.1-07 is opened in a follow-up invocation with the diagnostic stream from Walk #2 as input.
- The Phase 3 + Phase 3.1 Walk #1 historical records stay untouched — historical integrity preserved across iteration cycles.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-fix-fog-pan-translation/03.1-06-SUMMARY.md` capturing:
- The pre-walk gate sequence outcome (4 local + CI + IPA download)
- The Walk #2 metadata (CI run ID, SHA, walk theatre, walk duration, gesture count)
- The developer's verbatim verdict
- The post-walk JSONL grep correlation (per-second composite extract for the ≥ 10-combined-gesture window)
- The Walk #2 Verdict outcome (`CONFIRMED-AFTER-FIX` | `DENIED-FINAL` | `ITERATING`)
- Files modified across all three tasks (the 5 planning docs)
- Any deviations from the plan with Rule classification
- Cross-reference to Phase 4 (the unblocked-or-still-blocked downstream phase) + the migration recommendation hand-off to Phase 5
- Comparison vs Walk #1 numbers — frame_delta headroom preservation, SDF rebuild rate change (per PERF-08 validation), failure-mode resolution status per Walk #1 observation table
</output>
